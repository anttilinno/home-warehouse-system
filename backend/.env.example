# Database connection (async driver for app)
DATABASE_URL=postgresql+asyncpg://wh:wh@localhost:5432/warehouse_dev

# Database connection for dbmate migrations (sync driver)
DBMATE_DATABASE_URL=postgresql://wh:wh@localhost:5432/warehouse_dev?sslmode=disable

# Redis for job queue
REDIS_URL=redis://localhost:6379/0

# JWT authentication
SECRET_KEY=change-me-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# Debug mode (enables detailed error traces)
APP_DEBUG=true
