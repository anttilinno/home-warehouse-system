# RESUME — v3.0 frontend2 parity autonomous run

**Updated:** 2026-06-13, context handoff after Phase 15.
**Mandate:** orchestrate all of `docs/FRONTEND2_FEATURE_PARITY_PLAN.md` via GSD + Opus subagents, hands-off; user reviews only the final result. Branch `v3.0-frontend2-parity`. Memory: `project_parity_orchestrator_mandate`.
**User cadence (CLARIFIED 2026-06-13):** autonomous — DON'T stop between phases. I can't self-trigger `/clear` (harness command). Write the per-phase handoff (clear-safe checkpoint), then roll straight into the next phase. A compaction/clear is safe any time because the handoff captures resume state.

## NOW: v3.0 MILESTONE COMPLETE — all 21 phases (1-17 incl. 7b/10b/13b/14b) DONE. No phase left to orchestrate.
Phase 17 (Polish & Quality) finished 2026-06-14 — the final v3.0 phase. Every requirement (149/149) is flipped `[x]` + traceability Complete; STATE milestone `status: completed`. **There is nothing for the next orchestrator session to execute.**

### Next action is the USER's, not autonomous: close the milestone.
- Run `gsd-complete-milestone` (archives v3.0, prepares next version) — or start a new milestone with `gsd-new-milestone`.
- BEFORE retiring legacy `frontend/`: do the **one-week dogfooding** run (POL-06) and clear the human-UAT residues in `.planning/v3.0-FINAL-REVIEW-CHECKLIST.md` (every phase 2-17 logged its visual/UAT/translation-quality items there — this is the single review queue for the user).
- The `e2e-frontend2.yml` CI workflow (axe + keyboard, postgres+go+vite) is committed BEST-EFFORT and has never run in GitHub Actions — validate + tune it on the first real PR.

### Deferred-to-backlog (already in ROADMAP §Backlog): quick-capture (G-11), web-push (G-5, service-worker forbidden by the online-only grep guard), companies+favorites UI (G-10), dark theme (needs token set v2). Plus 3 niche inventory endpoints (`/inventory/{id}/loans`, `/available/{itemId}`, `/total-quantity/{itemId}`) — unused by any v3.0 screen.

### Standing quality infrastructure now in place (use it in any future frontend phase):
- **Bundle budget** (POL-04): `frontend2/bundle-budget.json` + `scripts/check-bundle-budget.mjs` + `lint:bundle` + the `bundle-budget` CI job. Per-chunk gz ceilings: main 215k / charts 120k / scanner 65k / palette 20k / messages 22k (per-locale, alternates not additive). Replaces the old ad-hoc per-phase bundle greps. ANY new heavy dep must lazy-chunk in vite.config or it fails CI.
- **a11y sweep** (POL-02): `frontend2/e2e/a11y-sweep.spec.ts` (`@axe-core/playwright`) sweeps every route, zero serious/critical. Run after ANY new page. MUST NOT use `networkidle` (open SSE never idles) — settle on DOM landmark; 240s timeout for the ~33-route single-login sweep.
- **keyboard-nav** (POL-03): `e2e/keyboard-nav.spec.ts` + global `*:focus-visible` fallback in globals.css.
- **responsive** (POL-05): `e2e/responsive.spec.ts` (320/360/768/1024/1440 + Sidebar/Fab swap + no-overflow). Grid uses `minmax(0,1fr)` + `min-width:0` on shell areas — keep that or narrow viewports overflow.

## (history) Phase 17 (Polish & Quality) — DONE 2026-06-14. POL-01..06 flipped. FINAL v3.0 PHASE.
4 plans / 3 waves (config-file contention forced serialization). 17-01 (POL-04 bundle budget — solo); 17-02 (POL-02 axe + POL-03 keyboard — branched post-17-01 for package.json); 17-03 (POL-05 responsive) ∥ 17-04 (POL-01 coverage matrix + POL-06 endpoint diff). gsd-verifier 6/6 PASS.
- **Plan-checker corrected pre-exec:** responsive contract is Fab `md:hidden` <768 / Sidebar `hidden md:block` ≥768 (Bottombar is `hidden md:flex` desktop-only — NOT asserted <768); bundle messages = LARGEST single locale not the sum; `lint:bundle` uses bare `vite build` (skip prebuild lint:imports hook).
- **17-04 found ONE genuine zero-coverage flow:** declutter — added `backend/internal/infra/postgres/declutter_repository_integration_test.go` (`-tags=integration`, ListUnused+score / GetCounts cents / MarkUsed / cross-tenant 404; passes green vs warehouse_test). Endpoint diff conclusion: **parity essentially complete** — the investigator's first-pass "33 gaps" were nearly all false positives (auth/oauth/sessions/analytics/labels/inventory-by-*/notifications-pagination all present); residual deltas are superseded (aggregate analytics endpoint, client-side tree breadcrumb) or 3 niche unused inventory queries.
- **LIVE GATE FOUND REAL BUGS (orchestrator fixed, commit 7826de12) — the value of POL:**
  - **a11y (axe sweep):** (1) user-pill button nameless on EVERY authed route — AppShell passes no `user` prop so avatar is aria-hidden + name span empty → added `aria-label={user?.full_name||user?.email||"Account menu"}` to TopBar user-pill (killed ~33 button-name criticals); (2) RetroFileInput `<input type=file class=sr-only>` had no label → `useId`+`<label htmlFor>` (fixed /imports CSV + /settings/profile avatar — both use RetroFileInput); (3) `aria-multiselectable="true"` invalid on plain `<table>` → removed from ItemsListPage + DemoPage (RetroTable just spreads props; multi-select stays JS-driven via useTableSelection); (4) `--fg-muted` (4.47) + `--accent-mint-deep` (4.32) failed AA on the **titlebar-blue selected-row fill** → nudged to `#56565f` / `#1a5e3a` (clears 4.5:1 on blue AND white). tokens.test.ts only checks contrast vs bg-panel — titlebar-blue-as-text-bg is now guarded LIVE by axe, not the unit test.
  - **responsive (POL-05):** real horizontal overflow at 320 (whole shell min-width ~497px) AND 768. Root cause: grid `1fr` track = `minmax(auto,1fr)` floors at content min-content, so non-shrinking chrome forces the column past the viewport. Fix: grid tracks → `minmax(0,1fr)` + `min-width:0` on `.app-topbar` (already on .app-main); TopBar shrinks gap/padding + hides brand wordmark & ONLINE label below `sm`; PageHeader + Bottombar hide the ~410px decorative SESSION/LAST-SYNC Clock below `lg` + Bottombar `overflow-hidden`.
  - **a11y-sweep spec robustness:** `waitForLoadState("networkidle")` ALWAYS times out (SSEProvider holds an open EventSource → network never idles) → switched to DOM-landmark settle + `test.setTimeout(240_000)` for the long single-login ~33-route sweep.
- Gate: lint:tsc + 1141 unit + build + bundle budget + lint:imports + lint:i18n(1016)/format ALL green; backend go build/test + declutter integration green; live chromium a11y-sweep + keyboard-nav + responsive 5/5 + login-dashboard/command-palette regression 8/8. gsd-verifier 6/6.
- Residues → FINAL-REVIEW-CHECKLIST: pixel-diff vs sketch 006 (screenshots captured); e2e-frontend2.yml first-PR validation; dogfooding week; 3 niche inventory endpoints.
- **REUSABLE:** the 4 quality gates above (bundle/a11y/keyboard/responsive) are now standing infra for any future frontend work.

## (history) Phase 16 (Command Palette) — DONE 2026-06-14. TUI-05 flipped.
3 plans / 3 SERIAL waves (each touched a different single-writer file). cmdk@1.1.1 + tinykeys@4.0.0 added. Deps `frontend` sibling already shipped cmdk so vetted. gsd-verifier 4/4 PASS; bundle gate PASS; live E2E 3/3 chromium.
- **Architecture:** open chord = SEPARATE window-level **tinykeys** listener in `usePaletteChord.ts` (NOT useShortcuts — ShortcutsProvider returns early on any meta/ctrl/alt combo, so ⌘K/Ctrl+K can never reach it; F2 matches no single-letter shortcut). Mounted in **ShellChrome** (AppShell.tsx) exactly like the `helpOpen`+`<F1HelpDialog>` pattern — it's the only place inside `WorkspaceProvider` (wsId) + QueryClient + BrowserRouter. Palette BODY is `React.lazy(() => import("@/features/command-palette"))` under `<Suspense>` — cmdk loads only on first open.
- **Wave 1 (16-01):** deps + `vite.config.ts` manualChunks `palette` rule. cmdk hard-imports `@radix-ui/react-dialog` (non-tree-shakable) → the rule routes BOTH `cmdk` AND `@radix-ui/react-dialog`; the chunk carries ~15-20KB gz that the gate keeps OUT of entry. `command-score` is VENDORED in cmdk (don't install).
- **Wave 2 (16-02):** the `features/command-palette/` module — `CommandPalette.tsx` (cmdk `shouldFilter={false}` + controlled `value`/`query`, client `matches()` substring filter on Routes/Workspaces/Recent static groups, stable `kind:id` item values to stop async-result selection flicker, `useModalStack(open,onClose)` for ESC, retro scrim+pinstriped titlebar), `usePaletteChord.ts` (`$mod+k`+`F2`, `{ignore:()=>false}`, preventDefault, mount-once useEffect([])+openRef), `useEntitySearch.ts` (250ms ref-debounce, items/borrowers/locations/containers `/search`, ≥2 chars, `[domain,wsId,"search",q]` keys, limit≤100), `recentActions.ts` (localStorage MRU `hws-palette-recent`, cap 10, dedup-by-id, mirrors scan-history.ts), `paletteRoutes.ts` (palette-LOCAL 18-entry `msg`-descriptor table — Sidebar nav not exported, single-writer). 5 new msgids filled et+ru. 21 unit tests.
- **Wave 3 (16-03):** mount in AppShell.tsx + chromium E2E + POL-04 bundle gate. **Deviation caught:** importing `usePaletteChord` from the barrel `@/features/command-palette` re-exports the default `CommandPalette` → `INEFFECTIVE_DYNAMIC_IMPORT`, leaks the body into entry. Fix: import `usePaletteChord` from its SPECIFIC module path; keep the dynamic `import()` as the sole path to the body.
- **POST-MERGE E2E REGRESSION (orchestrator fixed — 2 bugs):** (1) bare `<Command>` (we don't use `Command.Dialog`) does NOT focus the input on open → typed text never reached `query` → routes stayed unfiltered → ArrowDown+Enter landed on `/scan` (index right after Borrowers). Fix: add `autoFocus` to `CommandInput` (CODE fix — committed). (2) spec pressed `Meta+k` which never fires on the Linux CI box — tinykeys `$mod` = **Ctrl** on non-Apple; fix to Playwright `ControlOrMeta+k`. (3) SC4 seed used `quantity` → 422; items-create requires `sku`. LESSON: tinykeys `$mod` is platform-resolved → ALWAYS press `ControlOrMeta+k` in Playwright; and a bare cmdk `<Command>` needs explicit `autoFocus` on its input (only `Command.Dialog` manages focus).
- Gate: lint:tsc + test (1141) + build + lint:imports + lint:i18n (1016 msgids en/et/ru parity) + lint:i18n:format ALL green; bundle gate cmdk+radix absent from entry; live E2E 3/3 chromium; verifier 4/4.
- Residues → v3.0-FINAL-REVIEW-CHECKLIST: retro-panel visual fidelity (browser-UAT), cross-workspace switch (needs ≥2 ws seeded), et/ru palette-string translation quality.
- **REUSABLE for Phase 17:** the `palette` lazy-chunk + the per-phase bundle GREP gate are now THREE ad-hoc gates (scanner/charts/palette) — POL-04 should formalize them into one budget manifest + CI job.

## (history) Phase 15 (i18n catalog et + ru) — DONE 2026-06-13. I18N-01/02/03 flipped.
4 plans / 2 waves + an orchestrator re-extract between waves. **Catalogs were STALE** — at phase start en/et/ru each had 744 msgids; `bun run i18n:extract` refreshed en to **1010** (266 new from 7b/10b/13b/14/14b), et+ru = 1010 EMPTY. No inline-literal gap (new pages built with `<Trans>` discipline — extract found all 1010). I18N-02 switcher was ALREADY shipped in Phase 12 (LanguagePage PATCH-then-loadCatalog-in-onSuccess, render-loop-safe) — verify-only, zero code. Format hooks did NOT exist — built them.
- Wave 1 (parallel, disjoint): **15-01** = NEW `frontend2/src/lib/format/{tokens,hooks,index}.ts` — `useDateFormat`/`useTimeFormat`/`useNumberFormat` read the shared `["me"]` query, derive the persisted regional tokens (date_format/time_format/thousand_separator/decimal_separator — exact strings from RegionalFormatsPage 12-04), memoize on token PRIMITIVES (render-loop guard), default when `["me"]` pending; pure token→string helpers use UTC decomposition (match MovementsPanel); routed 7 render sites (relativeTime abs-branch → pure `formatDateToken` since it's a PURE fn not a component; MonthlyLoanActivityChart monthTick → pure helper since Recharts formatter is OUTSIDE the hook call stack; NotificationsDropdown/LoanPanels/BorrowerLoanPanels/MovementsPanel/SecurityPage → hook at component top); 2 decorative wall-clocks (Clock.tsx, AppShell.tsx) ALLOWLISTED via `// i18n-format-ignore` (locale-fixed chrome, not user data); money.ts UNTOUCHED (Intl currency, out of scope). **15-02** = two CI guards at repo-root `scripts/` (NOT frontend2/scripts — `lint:imports` invokes `node ../scripts/...`): `check-i18n-format.mjs` (greps src/features+src/components for raw `.toLocale*`/Date.toString; allowlists `// i18n-format-ignore`, `src/lib/format/`, `*.test.*`; does NOT flag `Intl.NumberFormat`) + `check-i18n-catalog.mjs` (po parity: fail on diverging msgid sets OR empty et/ru msgstr; skip header; `--only <locale>` flag for per-locale Wave-2 verify) + `__tests__` self-tests (11/11) + package.json `lint:i18n`/`lint:i18n:format` + 2 new `lint-frontend2.yml` jobs.
- ORCHESTRATOR between waves: `bun run i18n:extract` → commit refreshed catalogs (en 1010 + et/ru empty shells). Wave 2 branched off THAT.
- Wave 2 (parallel, disjoint by locale): **15-03** et + **15-04** ru — each filled 1010 msgstr (105 lifted verbatim from legacy `frontend/messages/{en,et,ru}.json` next-intl by English-leaf match, 905 hand-translated by Opus); ICU placeholders preserved verbatim; edited ONLY msgstr lines (msgid/comments/header byte-identical); `--only <locale>` guard exit 0 each.
- Gate: catalog guard exit 0 (1010 parity, 0 missing et+ru); format guard exit 0 (only the 2 ignore-tagged clocks remain); guard self-tests 11/11; `bun run lint:tsc && test (1120) && build && lint:imports && lint:i18n && lint:i18n:format` ALL green; live E2E login-dashboard 5/5 + settings 4/4 chromium; gsd-verifier 3/3 must-haves PASS.
- **POST-MERGE E2E REGRESSION (orchestrator fixed):** `e2e/settings.spec.ts` "language: switch en→et" failed once et was REAL — `loadCatalog(et)` re-localizes BEFORE the toast, so the success toast reads "Keel uuendatud." (not English) AND the select's own label becomes "Liidese keel", so `getByLabel(/interface language/i)` no longer resolves. Fix: target the page's single `getByRole("combobox")` (locale-stable) + match toast `/language updated|keel uuendatud/i`. LESSON: once a locale is fully translated, any E2E that asserts English UI text AFTER a locale switch breaks — target by role/testid + accept both languages. This test is now the live proof of I18N-02 instant activation.
- Residues → v3.0-FINAL-REVIEW-CHECKLIST: I18N-02 live visual eyeball (covered by E2E, low risk); native et/ru translation-quality review (905/locale machine-translated); ru Cyrillic end-to-end layout check.
- **REUSABLE for Phase 16+:** the format hooks (`@/lib/format`) + the two i18n CI guards now exist; any new feature page rendering a date/number MUST use the hooks or the format guard fails CI, and any new `<Trans>` string MUST be translated in et+ru or the catalog guard fails. Run `bun run i18n:extract` + fill et/ru as part of any future string-adding phase.

## (history) Phase 14b (Attachments + Paperless) — DONE 2026-06-13. ATT-01..03 + PPL-01..03 flipped.
Pre-written CONTEXT/VALIDATION were accurate. 5 plans / 3 waves. Wave 1 (backend, parallel disjoint): 14b-01 ATT-03 SECURITY (NO prod code change — workspace scoping was ALREADY complete on every route + SQL `WHERE id=$1 AND workspace_id=$2`; added the net-new cross-tenant 404 `//go:build integration` test mirroring item/handler_integration_test.go; flipped audit F1 → ✅ RESOLVED — the F1 row was stale, code was fixed under f49e4b48 but never marked); 14b-02 byte-storage (the REAL fix — old huma `/upload` is JSON+metadata-only and load-bearing for Phase-10b repair file-id minting, DON'T touch it; added a NEW Chi multipart route `POST /items/{id}/attachments/file` persisting real bytes via storage.Save (io.TeeReader+SHA-256+rollback) + a workspace-scoped serve route `GET /attachments/{id}/file` (double scoped lookup before streaming bytes, nosniff + Content-Disposition: attachment) + byte-roundtrip integration test; injected the existing `photoStorage` instance into attachment.NewService — sig 2→3 args). Wave 2 (frontend, parallel disjoint): 14b-03 item-attachment UI (lib/api/attachments.ts + useItemAttachments + ItemAttachmentPanel + AddAttachmentDialog; upload→the NEW /file route, multipart field name `file`; query key LOCKED `["items",wsId,itemId,"attachments"]`); 14b-04 paperless UI (lib/api/paperless.ts + PaperlessPage settings + usePaperlessSettings + PaperlessLinkDialog; PPL-03 link = create-attachment with external_doc_id=String(docId), dms_type derived server-side; types local to lib/api/*.ts NOT shared lib/types.ts to keep the two W2 worktrees disjoint). Wave 3: 14b-05 wiring (routes/index.tsx /settings/paperless lazy under SettingsLayout + real Settings-landing LinkRow + ItemDetailPage FILES tab mounts ItemAttachmentPanel + overflow "Link Paperless document" → PaperlessLinkDialog).
- Gate: frontend 1098 unit green + lint:tsc/build/lint:imports green; backend go build/test green + tagged attachment integration (cross-tenant 404 + byte round-trip) GREEN; live E2E attachments-paperless.spec.ts 2/2 chromium (upload→list→delete real bytes + paperless settings render). gsd-verifier 6/6 PASS; gsd-security-auditor: F1 genuinely resolved, no residual IDOR/zip-slip/content-sniff.
- **MERGE LANDMINE (cost one fix):** parallel worktrees branch off the SAME base, so 14b-01's test (written against the 2-arg NewService) failed to COMPILE after 14b-02 widened NewService to 3 args (+storage.Storage). Git merged textually clean but `go test -tags=integration` broke. Orchestrator fixed: inject `storage.NewLocalStorage(t.TempDir())` into 14b-01's test (mirror 14b-02's storage test). LESSON: when a backend wave has one plan changing a shared constructor signature and a sibling plan calling it, expect a post-merge SEMANTIC (not textual) conflict — run the tagged build on the MERGED tree, not just per-worktree.
- Residues → v3.0-FINAL-REVIEW-CHECKLIST: PPL-02/03 need a live Paperless-ngx (dev stack has none) — search+link are unit/MSW-only, human-UAT; set-primary browser-UAT; download-stream browser-UAT.

## (history) Phase 14 (System group) — DONE 2026-06-13. SYS-01..04 + WISH-01/02 + DECL-01/02 flipped.
8 plans / 2 waves. Wave 1 (6 disjoint greenfield): 14-01 Approvals (extends pendingChanges.ts w/ approve/reject; ApprovalsPage = activity-table + useTableSelection Shift+Click + BulkActionBar A/R + useShortcuts A/R; bulk = Promise.allSettled partial-failure; Defer DROPPED — no backend; owner/admin 403 calm-guard); 14-02 My-changes (/my-pending-changes); 14-03 Wishlist (status tabs wanted/ordered/acquired + CRUD + transition); 14-04 Declutter (score badge + grouping + client CSV + mark-used, formula-injection-safe); 14-05 Imports (async multipart POST /imports/upload — the JOB-creating path, NOT sync /import/{entity}; plural entity enum; jobs table /imports/jobs envelope `jobs`; export reuses settingsApi); 14-06 Sync-history (honest online-only empty-state). Wave 2: 14-07 inventory client CSV export button (no server /export/inventory); 14-08 wiring (6 routes + Sidebar SYSTEM group + Settings→/settings).
- Gate: 1084 unit green; live E2E system-group.spec.ts 1/1 chromium (approvals + wishlist tabs + sync-history); gsd-verifier all-PASS no-blockers.
- **Plan-checker caught the blocker:** 14-05 conflated the SYNCHRONOUS `/import/{entity}` (creates no job) with the ASYNC multipart `/imports/upload` (creates the job feeding the table) — rewired to upload, entity enum → plural importjob set (items/inventory/locations/containers/categories/borrowers), base64→FormData.
- **LANDMINE: lint:imports (FOUND-02) substring-matches `sync`** — a `features/sync-history/` dir FALSELY trips it. 14-06 relocated to `features/system-history/Page.tsx` (export SyncHistoryPage, route /sync-history); 14-08 imports `@/features/system-history/Page`. Remember this for any future sync-named module.
- Residues → v3.0-FINAL-REVIEW-CHECKLIST: SC-wording drift (SYS-01 "A/R/D"→A/R only; SYS-03 "past sync events"→online-only empty — update ROADMAP before milestone close); 6 human-UAT items (need seeded data/member account).

## (history) Phase 13b (Analytics) — DONE 2026-06-13. ANL-01..04 flipped.
Sketch 009 (retro-os-charts) made FIRST (Gap G-6 closed) — locked series palette (titlebar blue/pink/mint/butter + sand, deep companions on white), pastel-fill/2px-ink-stroke marks, mono value labels, Silkscreen titles only. 5 plans / 2 waves. Wave 1: 13b-01 analytics api + 3 hooks (useAnalyticsSummary, **useMonthlyLoanActivity** [separate endpoint — see blocker], useOutOfStock; all bare-body, `["analytics",wsId,...]` keys); 13b-02 recharts@3.8.1 install + retroChartTheme + 6 chart components (RRetroChartFrame); 13b-03 OutOfStockTable (rows → /items/{id}). Wave 2: 13b-04 AnalyticsPage composition; 13b-05 wiring (React.lazy /analytics route + vite `charts` manualChunk + Sidebar Analytics `to="/analytics"`).
- Gate: 1004 unit green; live E2E analytics.spec.ts 1/1 chromium; **ANL-03 bundle gate PASS** (recharts ONLY in charts-*.js, entry index-*.js recharts-free); gsd-verifier 4/4 PASS.
- **Plan-checker caught the big blocker pre-exec:** `/analytics/summary` does NOT accept `?months` and NEVER returns `monthly_loan_activity` (verified handler.go:96 `AnalyticsSummaryRequest struct{}` + service.go GetAnalyticsSummary never calls GetMonthlyLoanActivity). Fixed: monthly chart sources the SEPARATE `/analytics/loans/monthly?months=N` via useMonthlyLoanActivity, NOT the summary. Also fixed: silenced `tsc -b --noEmit 2>/dev/null;` → `lint:tsc &&`; bundle-gate greps `index-*.js` entry (not `ls -S`).
- Merge landmine: 13b-02 transcribed `features/analytics/types.ts` (owned by 13b-01) since it needed the types to compile pre-merge → add/add conflict on merge; resolved by `git checkout --ours` (13b-01 canonical, identical shapes). Post-merge regression: Sidebar.test.tsx asserted Analytics as the disabled nav fixture → retargeted to Settings (still unwired).
- Residues → v3.0-FINAL-REVIEW-CHECKLIST: live chart-mark render + lazy-chunk-at-runtime + out-of-stock row nav (all browser-UAT; E2E covers titles data-agnostically).

## (history) Phase 13 (Dashboard) — DONE 2026-06-13. DASH-01..05 + NOTIF-01..03 flipped.
5 plans / 2 waves. Wave 1 (parallel worktrees, disjoint files): 13-01 notifications feature + TopBar bell (USER-scoped `/api/notifications/*`, key `["notifications"]` no wsId, badge polls `unread/count` via refetchInterval, dropdown ESC-pops via modal stack); 13-02 pendingChanges api+hook + PendingApprovalsPanel (envelope key `changes` NOT `items`, owner/admin-only → 403 silent-degrade `return null`); 13-03 SystemAlertsPanel (REUSES useExpiringQuery + useMaintenanceDueQuery — no new api) + DashboardSideRail; 13-04 HudRow (flag-gated `VITE_FEATURE_HUD_ROLLUPS` default OFF, hand-rolled SVG, NO chart lib). Wave 2: 13-05 DashboardPage EXTEND — relativeTime util (<24h relative/absolute after) + Actor col (`user_id.slice(0,8)`/"—", no backend actor name) + Status col (action-derived pill) + side-rail grid + HUD mount + N/S/L shortcuts (stable navigate ref, no render-loop).
- Gate: 968 unit green; live E2E login-dashboard.spec.ts 5/5 chromium (ADDED bell + side-rail System-Alerts + dropdown-ESC test). gsd-verifier 8/8 PASS (automated). No backend changes this phase.
- Plan-checker caught 1 blocker pre-exec: DASH-02 missing Actor column → added to 13-05. Warning (rail order) → asserted in 13-05 page test (13-03's W1 worktree can't render-test PendingApprovalsPanel, a same-wave sibling import seam).
- Residues → v3.0-FINAL-REVIEW-CHECKLIST.md: DASH-01 visual label sign-off (richer tile layout, pre-approved); NOTIF-02 live mark-all-read UAT; HUD data-pending (no backend `capacity_target` / `/activity?days=14` — backend-coordination); `/approvals` route absent (Phase 14).
- LANDMINE confirmed: bare `tsc --noEmit` exits 0 SILENTLY on this project-references repo — use `bun run lint:tsc` (`tsc -b`) or `tsc -p tsconfig.app.json` for a REAL typecheck. 13-01's live bell added useQuery → needed TopBar.test.tsx QueryClientProvider wrap.

## DONE + verified (committed, reqs flipped)
Phases **2–7, 7b, 8, 9, 10, 10b, 11, 12, 13, 13b, 14, 14b, 15, 16** complete. STATE.md current. ONLY Phase 17 (Polish & Quality) remains → milestone close.
- 12 Settings SETT-01..11 ✅ (extended existing Security/Accounts scaffold; landing + Profile/avatar + Appearance light-only + Language/Regional/Notifications prefs + Data Storage + Members). Backend: MemberResponse enriched (email/full_name) + POST /members email→resolve. SETT-04→light-only (SETT-11). Phase-15 pref token contract recorded in 12-04-SUMMARY. E2E pinned chromium (shared-seeder-user race). 2 pre-existing backend test fails logged in 12-settings/deferred-items.md (auth/me 404, approval-pipeline gating) — NOT regressions; candidates for Phase 14/14b.
- Unit suite ~931 green; live E2E: + settings (chromium). Backend restarted with member enrich live.
- 11 Scan SCAN-01..12 ✅ (scanner deps @yudiel/react-qr-scanner@2.5.1 + barcode-detector@3.0.8 + ios-haptics@0.1.4 added; scanner code-split; by-barcode E2E re-added = G-65-01 guard restored). SCAN-12 = PORT LEGACY create-entity (user decision; claim-as-loan text superseded). SCAN-10 orphan-banner bug found by verifier + fixed. Camera/torch/haptic = device-only human-UAT residues.
- Unit suite ~891 green; live E2E: loans-lifecycle, borrowers, taxonomy, repairs-maintenance, scan-lookup (all 2/2 chromium+firefox).
- 8 Loans LOAN-01..06 ✅ · 9 Borrowers BORR-01..05 ✅ · 10 Taxonomy TAX-01..07 ✅ · 10b Repairs+Maintenance RPR-01..04 + MNT-01..03 ✅.
- Phase 10 fixed 3 BACKEND defects (category archive persist, label PATCH update, inventory container_id filter) — integration-tested; backend restarted live.
- Phase 10b fixed 2 FRONTEND bugs (E2E-found): money.ts null-currency white-screen on repair-complete; MaintenanceForm/RepairForm date→RFC3339 (was 422).
- Full unit suite ~794 green; live E2E specs: loans-lifecycle, borrowers, taxonomy, repairs-maintenance (all 2/2 chromium+firefox).
- KNOWN CARRIED GAP → Phase 14b: attachment byte-storage is a project-wide backend stub (POST .../attachments/upload records a File row, stores NO bytes). RPR-04 + item attachments are link-only until then; ties to the cross-tenant attachment IDOR audit finding.

## (history) Phase 12 (Settings) — DONE. Landing with 8 grouped rows + Profile + Security (sessions/password/delete) + Appearance + Language + Regional Formats + Notifications + Connected Accounts + Data Storage (online-only: clear cache + export + import). Reconcile SETT-04 (premium-terminal prose, stale) vs SETT-11 (light-only) — retro-os is light-only, so SETT-11 wins. Depends on Phase 6 (providers).
- Next: scan settings/profile/security/preferences backend surface (users/me, preferences, sessions, password, connected accounts, workspace backup/restore); synth 12-CONTEXT, spawn researchers (UI phase), resolve OQs, VALIDATION, planner, plan-checker, execute.
- Remaining after 12: 13, 13b(charts sketch first), 14, 14b(attachment IDOR + byte-storage), 15, 16, 17.

## (history) Phase 11 (Scan) — DONE. Single `/scan` route, BarcodeScanner mounted ONCE, QR/UPC/EAN/Code128, pause-on-match, Android torch, manual fallback, AudioContext beep, ios-haptics, scan history (×10/×20 localStorage), 4-state result banner, post-match quick-action overlay, UPC opt-in prefill. PLUS re-add the by-barcode Playwright spec (CLAUDE.md standing gap since v2.2 wipe). Deep-link target exists: /loans/new?itemId= (Phase 8).
- Next step: scan backend surface (by-barcode lookup handler exists — Phase 65 integration test guards GET /items/by-barcode/{code}) + check for a scanner lib already in deps; synth 11-CONTEXT, spawn researchers (UI phase — needs camera/scanner UX), resolve OQs, VALIDATION, planner, plan-checker, execute.
- Remaining after 11: 12, 13, 13b(needs charts sketch first), 14, 14b(fix attachment IDOR + byte-storage), 15, 16, 17.

## PROVEN ORCHESTRATION LOOP (followed 3× this session, 8/9/10)
1. Scan backend+frontend surface → write NN-CONTEXT.md (verified API shapes + OQs) → spawn phase-researcher (+ui-researcher for UI phases) opus background → annotate OQs RESOLVED in CONTEXT → write NN-VALIDATION.md → gsd-planner (opus) → gsd-plan-checker (sonnet) → apply surgical fixes → commit plans → `gsd-sdk query state.begin-phase --phase N --name <slug> --plans <count>`.
2. Execute per-wave: `git worktree add -b exec/NN-PP .wt/NN-PP <baseSHA>` (baseSHA = current HEAD), spawn gsd-executor (opus, background) with FULL worktree setup + hard rules (no STATE/ROADMAP/api.ts/vite/backend unless scoped; bun install --frozen-lockfile; explicit cwd; SUMMARY committed; declare EVERY edited file). Same-wave plans MUST be disjoint files → run parallel (one Agent msg, multiple calls). routes/index.tsx is single-writer → serialize across waves.
3. Merge (orchestrator owns): deletion-guard `git diff --diff-filter=D`; backup STATE.md+ROADMAP.md; `git merge exec/NN-PP --no-ff`; restore the 2 backups (amend); `git worktree remove --force` + `git branch -D`; gate `cd frontend2 && bun run lint:tsc && bun run test && bun run build && bun run lint:imports`; `gsd-sdk query roadmap.update-plan-progress N PP complete`.
4. Phase gate: full unit suite + live E2E (`E2E_USER=seeder@test.local E2E_PASS=password123 bun run test:e2e e2e/<spec>` — run the phase's spec ISOLATED, 20/min auth limiter); gsd-verifier (sonnet); flip reqs (`[x]` + traceability Complete) + log residues to `.planning/v3.0-FINAL-REVIEW-CHECKLIST.md`; `gsd-sdk query state.complete-phase --phase N`; commit.
5. Verification is REAL — caught/fixed this session: 2 E2E selector bugs (selectOption needs string not RegExp; name dup needs .first()), 1 stale Phase-8 route-guard test, 3 backend repo Save update-path bugs. Investigate every gate failure; mass login-fail = rate limiter (restart backend), not code.

## Env (stack UP)
docker `warehouse-postgres-dev` + `warehouse-redis`. Backend :8080 fixed code running (`cd backend`, env: JWT_SECRET, GO_DATABASE_URL=postgresql://wh:wh@localhost:5432/warehouse_dev?sslmode=disable, REDIS_URL, APP_DEBUG/ENV, FRONTEND_URL/APP_URL=:5173, IMPORT_UPLOAD_DIR=/tmp/imports, `go run ./cmd/server/main.go`). RESTART = pkill the `main` exe holding :8080 (orphan survives killing `go run`), then relaunch. Frontend :5173 `cd frontend2 && bun run dev`. Backend tests: `TEST_DATABASE_URL=postgresql://wh:wh@localhost:5432/warehouse_test ... -tags=integration`. config: commit_docs=false, parallelization=true, model_profile=quality.

### Phase 8/9/10 binding overrides (for reference / regression awareness)
- Loans: key on `inventory_id` not item_id; overdue=server `is_overdue`; client CSV (no /export/loan); tabs active/overdue/history(client !is_active); LoanPanels stub made real; BorrowerLoanPanels component-only.
- Borrowers: list bare `{items}` no-total → client paginate (PER_PAGE 25, fetch limit=100); search client-filter (separate /search endpoint left for pickers); delete guard proactive(active-loan count)+reactive(400 string); hard-delete only.
- Taxonomy: client buildTree (categories `parent_category_id` vs locations `parent_location` — different keys!); archive usage-warning client-computed (items/inventory list total); container delete=bare DELETE (Postgres SET NULL); locations ARCHIVE-only; per-endpoint envelope split; RetroTree atom + per-tab sessionStorage; RetroCombobox/SearchPicker NOT retrofit into shipped forms.

## Remaining: 17 (final phase — Polish & Quality, then milestone close)

## ORCHESTRATION PROTOCOL (proven 7×, follow exactly)
1. **Plan:** synth `NN-CONTEXT.md` from ROADMAP+REQUIREMENTS+parity §4/§5 + prior carry-forward → `gsd-phase-researcher` (+`gsd-ui-researcher` for UI phases), opus, background → resolve their Open Questions inline (annotate `## Open Questions (RESOLVED)`), normalize filenames to lowercase `NN-`. Write `NN-VALIDATION.md`. → `gsd-planner` (opus) → `gsd-plan-checker` (sonnet); fix blockers surgically. Commit. `gsd-sdk query state.begin-phase --phase N --name <slug> --plans <count>`.
2. **Execute:** per plan spawn `gsd-executor` (opus, isolation worktree, background) with FULL worktree_branch_check + base SHA = current HEAD. Parallel = disjoint files, one Agent per message. Executor rules: no STATE/ROADMAP edits, SUMMARY committed before return, `bun install --frozen-lockfile`, explicit cwd (#3097), no api.ts/vite/backend unless scoped.
3. **Merge (orchestrator owns):** deletion-guard → backup STATE.md+ROADMAP.md → `git merge <branch> --no-ff` → restore the 2 backups → `git worktree remove --force` + `git branch -D` → gate `cd frontend2 && bun run test && bun run build && bun run lint:imports` → `roadmap.update-plan-progress N <plan> complete`.
4. **Phase gate:** unit suite + live E2E (`E2E_USER=seeder@test.local E2E_PASS=password123 bun run test:e2e`) IN BATCHES or restart backend first (20/min auth limiter clears on restart). `gsd-verifier` (sonnet). human_needed + all-automated-pass → flip to passed, log visual residues to `.planning/v3.0-FINAL-REVIEW-CHECKLIST.md`. Flip reqs (checkbox `[x]` + traceability `Complete`). Commit. TaskUpdate complete + next in_progress.
5. **Verification is real** — caught/fixed 2 bugs in gates (sku-422, limit=200 join). Investigate E2E fails; but mass login-fail = rate limiter (restart backend), not code.

## Env (stack UP)
docker `postgres-dev` + `redis`. Backend :8080 — `cd backend`, `export JWT_SECRET=$(openssl rand -base64 48) GO_DATABASE_URL=postgresql://wh:wh@localhost:5432/warehouse_dev?sslmode=disable REDIS_URL=redis://localhost:6379 APP_DEBUG=true APP_ENV=development FRONTEND_URL=http://localhost:5173 APP_URL=http://localhost:5173 IMPORT_UPLOAD_DIR=/tmp/imports`, `go run ./cmd/server/main.go`. Frontend :5173 — `cd frontend2 && bun run dev`. Integration: `TEST_DATABASE_URL=postgresql://wh:wh@localhost:5432/warehouse_test ... -tags=integration`. config: commit_docs=false, parallelization=true, model_profile=quality.

## Recurring landmines
- RENDER-LOOP (4×): no `t`/mutation-wrapper/fresh-object in useShortcuts/useMemo/useEffect deps — `t` via ref, destructure `.mutate`.
- Backend list `limit` caps 100 (422 over) — clamp picker/join queries ≤100.
- Ignore stale replayed task-notifications on dead ids ("claude-fable-5" model errors on old ids = harmless).
- Phase 13b needs a retro charts sketch before planning; 14b must fix attachment IDOR (audit F1); Phase 12 reconcile SETT-04 (premium-terminal prose) vs SETT-11 (light-only).
