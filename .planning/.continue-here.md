---
context: default
phase: none (debug-session work, not a GSD phase)
task: 3
total_tasks: 3
status: paused
last_updated: 2026-05-31T20:08:53.709Z
---

# Continue Here — Backend hardening (3 fixes)

All 3 fixes are DONE and verified, but UNCOMMITTED. Branch: `docs/paperless-dms-switch`.

<current_state>
Three backend correctness/security fixes were applied via the GSD debug workflow,
each surfaced from a graphify knowledge-graph trace of `backend/`. ~50 files modified,
all in the working tree, nothing committed. `go build ./...` and `go vet ./...` clean;
all relevant domain/handler/middleware tests pass. Only 2 tests fail tree-wide, both
PROVEN pre-existing (stash-test on clean tree) and unrelated to this work.
</current_state>

<completed_work>
- Fix #1 — Tenant mutation `workspace_id` scoping (session: `tenant-mutation-scope`, resolved):
  Added `AND workspace_id = $N` to mutating SQL in 7 domains (locations, categories,
  containers, borrowers, companies, labels, repair_logs); regenerated sqlc; threaded
  `workspaceID` through repo signatures (Delete×7, borrower Archive/Restore, category
  HasChildren, repairlog MarkReminderSent) + callers. Service `GetByID` read-before-write
  guard preserved as the 1st defense layer.
- Fix #3 — Approval pipeline (session: `approval-pipeline-fixes`, resolved):
  (A) `ApproveChange` now `Transactor.WithTx` (approve+apply+save atomic) + in-tx
  `status==pending` re-check (idempotent retry, no double-apply).
  (B) All 8 `apply*Change` create/update route through domain `ServiceInterface` →
  full submitted-field fidelity (was dropping fields).
  (C) Gate kept at 8 entity types; deliberate exclusions documented in new
  `docs/APPROVAL_PIPELINE.md` + code comments on extractEntityType/isValidEntityType.
- Fix #2 — Error→HTTP mapping (session: `error-status-mapping`, resolved):
  Added `MapDomainError` + taught `ErrorTransformer` to map `*shared.DomainError`
  sentinels (ErrNotFound→404, ErrInvalidInput→400, ErrAlreadyExists/Conflict→409,
  ErrUnauthorized→401, ErrForbidden→403, ErrInternal→500), surfaces `Field` as Location.
  Swept 69 hardcoded `huma.Error400BadRequest(err.Error())` sites through typed mapping;
  kept legitimate request-parse 400s.
</completed_work>

<remaining_work>
- Decide + commit the 3 fixes (uncommitted, on disk but not version-controlled).
- Audit frontend callers for the API contract change (see anti-patterns below).
- (optional) Fix the 2 pre-existing test failures.
</remaining_work>

<decisions_made>
- Fix #1 scoped to ALL 7 affected domains (user choice over RLS-backstop / single-domain pilot).
- Fix #3(B) routes through domain services because item/inventory optional fields are unexported.
- Fix #3(C) documents exclusions rather than extending the gate (conservative).
- All fixes done via GSD debug workflow per repo CLAUDE.md enforcement.
</decisions_made>

<blockers>
- None. (Commit decision is pending but non-blocking — files persist on disk.)
</blockers>

## Required Reading (in order)
1. `graphify-out/memory/query_20260531_185823_*.md` — tenant isolation diagnosis (fix #1)
2. `graphify-out/memory/query_20260531_190938_*.md` — approval pipeline diagnosis (fix #3)
3. `graphify-out/memory/query_20260531_190102_*.md` — NewFieldError / error mapping diagnosis (fix #2)
4. `.planning/debug/tenant-mutation-scope.md`, `approval-pipeline-fixes.md`, `error-status-mapping.md` — full root-cause + fix detail per session
5. `docs/APPROVAL_PIPELINE.md` (new) — entity-gating coverage + deliberate exclusions

## Critical Anti-Patterns (do NOT repeat / be aware)
- [API CONTRACT CHANGE]: Fix #2 intentionally changed some responses 400→404 (not-found on
  location/container/label Archive/Restore/Delete) and 400→409 (has-children / has-inventory).
  → Before declaring done, grep frontend2 for callers that key on the old 400 status and update them.
- [PRE-EXISTING FAILURES — not ours]: `loan.TestService_Return_InventoryDeleted` (mock-setup
  panic in loan/service.go:153) and `jobs.TestCleanupConfig_RetentionPeriodUsage` (90-day date
  arithmetic). Proven pre-existing via stash-test. Do NOT attribute these to the 3 fixes.
- [pendingchange integration test]: `handler_integration_test.go` has a pre-existing structural
  import cycle (package pendingchange importing infra/postgres). Out of scope; only its NewService
  call signature was updated. Don't try to "fix" by reverting fix #3.

## Infrastructure State
- Branch: `docs/paperless-dms-switch`. ~50 modified files uncommitted (see `git status`).
- 3 debug sessions resolved under `.planning/debug/` (some moved to `.planning/debug/resolved/`).
- graphify knowledge graph of backend at `graphify-out/` (8408 nodes, graph.html, GRAPH_REPORT.md, 6 query traces).
- No running servers/watchers.

<context>
The work started as an interactive graphify exploration of the backend graph; tracing the
top bridge nodes surfaced 3 real defects, which the user approved fixing (GSD workflow, all
domains for #1). Each fix is independent and self-contained. The natural next step is the
commit decision + frontend contract audit.
</context>

<next_action>
Start with: decide whether to commit the 3 fixes. If yes, create a branch off
docs/paperless-dms-switch and commit (consider 3 atomic commits, one per fix/debug-session).
Then grep frontend2 for callers depending on the old 400 statuses now returning 404/409.
</next_action>
