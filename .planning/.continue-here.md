# RESUME — v3.0 frontend2 parity autonomous run

**Updated:** 2026-06-13, context handoff after Phase 10b.
**Mandate:** orchestrate all of `docs/FRONTEND2_FEATURE_PARITY_PLAN.md` via GSD + Opus subagents, hands-off; user reviews only the final result. Branch `v3.0-frontend2-parity`. Memory: `project_parity_orchestrator_mandate`.

## DONE + verified (committed, reqs flipped)
Phases **2–7, 7b, 8, 9, 10, 10b** complete. STATE.md current.
- 8 Loans LOAN-01..06 ✅ · 9 Borrowers BORR-01..05 ✅ · 10 Taxonomy TAX-01..07 ✅ · 10b Repairs+Maintenance RPR-01..04 + MNT-01..03 ✅.
- Phase 10 fixed 3 BACKEND defects (category archive persist, label PATCH update, inventory container_id filter) — integration-tested; backend restarted live.
- Phase 10b fixed 2 FRONTEND bugs (E2E-found): money.ts null-currency white-screen on repair-complete; MaintenanceForm/RepairForm date→RFC3339 (was 422).
- Full unit suite ~794 green; live E2E specs: loans-lifecycle, borrowers, taxonomy, repairs-maintenance (all 2/2 chromium+firefox).
- KNOWN CARRIED GAP → Phase 14b: attachment byte-storage is a project-wide backend stub (POST .../attachments/upload records a File row, stores NO bytes). RPR-04 + item attachments are link-only until then; ties to the cross-tenant attachment IDOR audit finding.

## NOW: Phase 11 (Scan) — SCAN-01..12. Single `/scan` route, BarcodeScanner mounted ONCE, QR/UPC/EAN/Code128, pause-on-match, Android torch, manual fallback, AudioContext beep, ios-haptics, scan history (×10/×20 localStorage), 4-state result banner, post-match quick-action overlay, UPC opt-in prefill. PLUS re-add the by-barcode Playwright spec (CLAUDE.md standing gap since v2.2 wipe). Deep-link target exists: /loans/new?itemId= (Phase 8).
- Next step: scan backend surface (by-barcode lookup handler exists — Phase 65 integration test guards GET /items/by-barcode/{code}) + check for a scanner lib already in deps; synth 11-CONTEXT, spawn researchers (UI phase — needs camera/scanner UX), resolve OQs, VALIDATION, planner, plan-checker, execute.
- Remaining after 11: 12, 13, 13b(needs charts sketch first), 14, 14b(fix attachment IDOR + byte-storage), 15, 16, 17.

## PROVEN ORCHESTRATION LOOP (followed 3× this session, 8/9/10)
1. Scan backend+frontend surface → write NN-CONTEXT.md (verified API shapes + OQs) → spawn phase-researcher (+ui-researcher for UI phases) opus background → annotate OQs RESOLVED in CONTEXT → write NN-VALIDATION.md → gsd-planner (opus) → gsd-plan-checker (sonnet) → apply surgical fixes → commit plans → `gsd-sdk query state.begin-phase --phase N --name <slug> --plans <count>`.
2. Execute per-wave: `git worktree add -b exec/NN-PP .wt/NN-PP <baseSHA>` (baseSHA = current HEAD), spawn gsd-executor (opus, background) with FULL worktree setup + hard rules (no STATE/ROADMAP/api.ts/vite/backend unless scoped; bun install --frozen-lockfile; explicit cwd; SUMMARY committed; declare EVERY edited file). Same-wave plans MUST be disjoint files → run parallel (one Agent msg, multiple calls). routes/index.tsx is single-writer → serialize across waves.
3. Merge (orchestrator owns): deletion-guard `git diff --diff-filter=D`; backup STATE.md+ROADMAP.md; `git merge exec/NN-PP --no-ff`; restore the 2 backups (amend); `git worktree remove --force` + `git branch -D`; gate `cd frontend2 && bun run lint:tsc && bun run test && bun run build && bun run lint:imports`; `gsd-sdk query roadmap.update-plan-progress N PP complete`.
4. Phase gate: full unit suite + live E2E (`E2E_USER=seeder@test.local E2E_PASS=password123 bun run test:e2e e2e/<spec>` — run the phase's spec ISOLATED, 20/min auth limiter); gsd-verifier (sonnet); flip reqs (`[x]` + traceability Complete) + log residues to `.planning/v3.0-FINAL-REVIEW-CHECKLIST.md`; `gsd-sdk query state.complete-phase --phase N`; commit.
5. Verification is REAL — caught/fixed this session: 2 E2E selector bugs (selectOption needs string not RegExp; name dup needs .first()), 1 stale Phase-8 route-guard test, 3 backend repo Save update-path bugs. Investigate every gate failure; mass login-fail = rate limiter (restart backend), not code.

## Env (stack UP)
docker `warehouse-postgres-dev` + `warehouse-redis`. Backend :8080 fixed code running (`cd backend`, env: JWT_SECRET, GO_DATABASE_URL=postgresql://wh:wh@localhost:5432/warehouse_dev?sslmode=disable, REDIS_URL, APP_DEBUG/ENV, FRONTEND_URL/APP_URL=:5173, IMPORT_UPLOAD_DIR=/tmp/imports, `go run ./cmd/server/main.go`). RESTART = pkill the `main` exe holding :8080 (orphan survives killing `go run`), then relaunch. Frontend :5173 `cd frontend2 && bun run dev`. Backend tests: `TEST_DATABASE_URL=postgresql://wh:wh@localhost:5432/warehouse_test ... -tags=integration`. config: commit_docs=false, parallelization=true, model_profile=quality.

### Phase 8/9/10 binding overrides (for reference / regression awareness)
- Loans: key on `inventory_id` not item_id; overdue=server `is_overdue`; client CSV (no /export/loan); tabs active/overdue/history(client !is_active); LoanPanels stub made real; BorrowerLoanPanels component-only.
- Borrowers: list bare `{items}` no-total → client paginate (PER_PAGE 25, fetch limit=100); search client-filter (separate /search endpoint left for pickers); delete guard proactive(active-loan count)+reactive(400 string); hard-delete only.
- Taxonomy: client buildTree (categories `parent_category_id` vs locations `parent_location` — different keys!); archive usage-warning client-computed (items/inventory list total); container delete=bare DELETE (Postgres SET NULL); locations ARCHIVE-only; per-endpoint envelope split; RetroTree atom + per-tab sessionStorage; RetroCombobox/SearchPicker NOT retrofit into shipped forms.

## Remaining: 8, 9, 10, 10b, 11, 12, 13, 13b, 14, 14b, 15, 16, 17

## ORCHESTRATION PROTOCOL (proven 7×, follow exactly)
1. **Plan:** synth `NN-CONTEXT.md` from ROADMAP+REQUIREMENTS+parity §4/§5 + prior carry-forward → `gsd-phase-researcher` (+`gsd-ui-researcher` for UI phases), opus, background → resolve their Open Questions inline (annotate `## Open Questions (RESOLVED)`), normalize filenames to lowercase `NN-`. Write `NN-VALIDATION.md`. → `gsd-planner` (opus) → `gsd-plan-checker` (sonnet); fix blockers surgically. Commit. `gsd-sdk query state.begin-phase --phase N --name <slug> --plans <count>`.
2. **Execute:** per plan spawn `gsd-executor` (opus, isolation worktree, background) with FULL worktree_branch_check + base SHA = current HEAD. Parallel = disjoint files, one Agent per message. Executor rules: no STATE/ROADMAP edits, SUMMARY committed before return, `bun install --frozen-lockfile`, explicit cwd (#3097), no api.ts/vite/backend unless scoped.
3. **Merge (orchestrator owns):** deletion-guard → backup STATE.md+ROADMAP.md → `git merge <branch> --no-ff` → restore the 2 backups → `git worktree remove --force` + `git branch -D` → gate `cd frontend2 && bun run test && bun run build && bun run lint:imports` → `roadmap.update-plan-progress N <plan> complete`.
4. **Phase gate:** unit suite + live E2E (`E2E_USER=seeder@test.local E2E_PASS=password123 bun run test:e2e`) IN BATCHES or restart backend first (20/min auth limiter clears on restart). `gsd-verifier` (sonnet). human_needed + all-automated-pass → flip to passed, log visual residues to `.planning/v3.0-FINAL-REVIEW-CHECKLIST.md`. Flip reqs (checkbox `[x]` + traceability `Complete`). Commit. TaskUpdate complete + next in_progress.
5. **Verification is real** — caught/fixed 2 bugs in gates (sku-422, limit=200 join). Investigate E2E fails; but mass login-fail = rate limiter (restart backend), not code.

## Env (stack UP)
docker `postgres-dev` + `redis`. Backend :8080 — `cd backend`, `export JWT_SECRET=$(openssl rand -base64 48) GO_DATABASE_URL=postgresql://wh:wh@localhost:5432/warehouse_dev?sslmode=disable REDIS_URL=redis://localhost:6379 APP_DEBUG=true APP_ENV=development FRONTEND_URL=http://localhost:5173 APP_URL=http://localhost:5173 IMPORT_UPLOAD_DIR=/tmp/imports`, `go run ./cmd/server/main.go`. Frontend :5173 — `cd frontend2 && bun run dev`. Integration: `TEST_DATABASE_URL=postgresql://wh:wh@localhost:5432/warehouse_test ... -tags=integration`. config: commit_docs=false, parallelization=true, model_profile=quality.

## Recurring landmines
- RENDER-LOOP (4×): no `t`/mutation-wrapper/fresh-object in useShortcuts/useMemo/useEffect deps — `t` via ref, destructure `.mutate`.
- Backend list `limit` caps 100 (422 over) — clamp picker/join queries ≤100.
- Ignore stale replayed task-notifications on dead ids ("claude-fable-5" model errors on old ids = harmless).
- Phase 13b needs a retro charts sketch before planning; 14b must fix attachment IDOR (audit F1); Phase 12 reconcile SETT-04 (premium-terminal prose) vs SETT-11 (light-only).
