---
phase: 40-database-migration-and-backend-oauth-core
verified: 2026-02-22T16:00:00Z
status: passed
score: 12/12 must-haves verified
re_verification: false
---

# Phase 40: Database Migration and Backend OAuth Core Verification Report

**Phase Goal:** Backend delivers fully functional OAuth login/signup API for Google and GitHub with all critical security protections built in from day one
**Verified:** 2026-02-22T16:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | OAuth-only users can be created in the database with NULL password_hash | VERIFIED | Migration 012 alters password_hash to DROP NOT NULL; `NewOAuthUser` sets empty passwordHash; `scanUser` uses `*string` for nullable scanning |
| 2  | Existing users retain has_password=true after migration | VERIFIED | Migration adds `has_password BOOLEAN NOT NULL DEFAULT true`; all existing rows default to true |
| 3  | User entity exposes HasPassword() boolean and handles empty password hash | VERIFIED | `entity.go` line 152: `func (u *User) HasPassword() bool { return u.hasPassword }` and `CheckPassword` guards against empty hash at line 193 |
| 4  | GET /users/me returns has_password field in response | VERIFIED | `handler.go` UserResponse includes `HasPassword bool` at line 1021; all response builders populate it; uploadAvatar raw JSON includes it at line 783 |
| 5  | sqlc queries for oauth_accounts table are generated and compile | VERIFIED | `backend/internal/infra/queries/oauth_accounts.sql.go` exists with all 5 queries; `go build ./...` succeeds cleanly |
| 6  | OAuth service can resolve Google profile to existing or new user | VERIFIED | `FindOrCreateUser` in service.go checks existing link, then email verification, then auto-links or creates new user; `FetchGoogleProfile` fetches from googleapis.com/oauth2/v3/userinfo |
| 7  | OAuth service can resolve GitHub profile to existing or new user | VERIFIED | `FetchGitHubProfile` uses `/user/emails` endpoint for private-email users; returns profile with EmailVerified based on primary+verified email |
| 8  | Auto-linking only occurs when provider confirms email is verified | VERIFIED | service.go line 68: `if !profile.EmailVerified { return nil, false, ErrEmailNotVerified }` — security gate before any auto-link or create |
| 9  | Unverified provider email is rejected with a domain error | VERIFIED | `ErrEmailNotVerified = shared.NewDomainError(shared.ErrForbidden, ...)` in errors.go; handler redirects to frontend with `email_not_verified` error code |
| 10 | New OAuth user gets a personal workspace created automatically | VERIFIED | `FindOrCreateUser` calls `wsCreator.CreatePersonalWorkspace` before creating OAuth link; `workspaceCreatorAdapter` in router.go wraps workspace service using same logic as register |
| 11 | Full Authorization Code flow with PKCE + CSRF state in HttpOnly cookie | VERIFIED | handler.go: `oauth2.GenerateVerifier()`, `oauth2.S256ChallengeOption(verifier)` for initiate; `oauth2.VerifierOption(verifier)` for exchange; cookie `oauth_state` is HttpOnly, stores `state|verifier` |
| 12 | Callback endpoints are rate-limited at 10 requests per minute per IP | VERIFIED | router.go lines 302-306: `NewRateLimiter(10, time.Minute)` applied to callback route group |

**Score:** 12/12 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `backend/db/migrations/012_oauth_nullable_password.sql` | Schema changes for OAuth-only users | VERIFIED | Contains exact `ALTER TABLE auth.users ALTER COLUMN password_hash DROP NOT NULL` and `ADD COLUMN has_password BOOLEAN NOT NULL DEFAULT true` |
| `backend/db/queries/oauth_accounts.sql` | sqlc query definitions for user_oauth_accounts | VERIFIED | Contains `GetOAuthAccountByProviderAndID`, `ListOAuthAccountsByUser`, `CreateOAuthAccount`, `DeleteOAuthAccountByProvider`, `CountOAuthAccountsByUser` |
| `backend/internal/infra/queries/oauth_accounts.sql.go` | Generated sqlc Go code | VERIFIED | Generated by sqlc v1.30.0; all 5 queries with proper types; compiles cleanly |
| `backend/internal/domain/auth/user/entity.go` | User entity with hasPassword and NewOAuthUser | VERIFIED | `NewOAuthUser` constructor at line 73; `HasPassword()` getter at line 152; `CheckPassword` empty-hash guard at line 192 |
| `backend/internal/domain/auth/oauth/entity.go` | OAuthAccount entity and OAuthProfile value object | VERIFIED | Private-field entity with Reconstruct; `OAuthProfile` struct with EmailVerified bool |
| `backend/internal/domain/auth/oauth/providers.go` | Google and GitHub provider configs and profile fetchers | VERIFIED | `FetchGoogleProfile` and `FetchGitHubProfile` both exist and are substantive; GitHub uses `/user/emails` |
| `backend/internal/domain/auth/oauth/service.go` | FindOrCreateUser with email verification gate | VERIFIED | Full 4-step flow implemented; `FindOrCreateUser`, `ListAccounts`, `UnlinkAccount`, `GetProviderConfig` all present |
| `backend/internal/domain/auth/oauth/repository.go` | Repository interface for oauth accounts | VERIFIED | `type Repository interface` with 5 methods |
| `backend/internal/domain/auth/oauth/errors.go` | Domain errors for OAuth | VERIFIED | All 5 domain errors defined using `shared.NewDomainError` |
| `backend/internal/domain/auth/oauth/handler.go` | HTTP handlers for OAuth initiate, callback, exchange, list, unlink | VERIFIED | All 5 handlers (`Initiate`, `Callback`, `ExchangeCode`, `ListAccounts`, `UnlinkAccount`) implemented and substantive |
| `backend/internal/infra/postgres/oauth_repository.go` | PostgreSQL implementation of OAuth repository | VERIFIED | Implements all 5 Repository interface methods using sqlc queries |
| `backend/internal/api/router.go` | OAuth route registration with rate limiting | VERIFIED | All routes registered: initiate (public), callback (10/min rate limit), exchange (shared auth RL), list+unlink (JWT-protected) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `user_repository.go` | `oauth_accounts.sql` | scanUser handles nullable password_hash via *string | WIRED | `passwordHash *string` at line 164; nil-to-empty conversion at lines 193-197 |
| `handler.go` (user) | `entity.go` (user) | HasPassword in UserResponse | WIRED | `HasPassword bool` in UserResponse struct; all response builders populate `HasPassword: u.HasPassword()` |
| `oauth/service.go` | `user/service.go` | UserService interface for GetByEmail, CreateOAuthUser | WIRED | `UserService` interface in service.go; concrete `user.Service` passed via `wsCreator` adapter in router.go |
| `oauth/service.go` | `oauth/repository.go` | Repository for FindByProviderAndID, Create | WIRED | `oauthRepo Repository` field; all methods called in FindOrCreateUser, ListAccounts, UnlinkAccount |
| `infra/postgres/oauth_repository.go` | `infra/queries/oauth_accounts.sql.go` | sqlc generated queries | WIRED | `r.q.GetOAuthAccountByProviderAndID`, `r.q.CreateOAuthAccount`, etc. directly used |
| `oauth/handler.go` | `oauth/service.go` | FindOrCreateUser in callback handler | WIRED | `h.service.FindOrCreateUser(r.Context(), *profile)` at line 204 |
| `oauth/handler.go` | Redis | One-time code storage and retrieval | WIRED | `h.redisClient.Set(... oauthCodePrefix+oneTimeCode ...)` and `h.redisClient.GetDel(... oauthCodePrefix+code)` |
| `router.go` | `oauth/handler.go` | Route registration | WIRED | `oauthHandler.Initiate`, `oauthHandler.Callback`, `oauthHandler.ExchangeCode`, `oauthHandler.ListAccounts`, `oauthHandler.UnlinkAccount` all registered |
| `oauth/handler.go` | `shared/jwt` | JWT token generation after OAuth | WIRED | `h.jwtService.GenerateToken(...)` and `h.jwtService.GenerateRefreshToken(...)` called in Callback |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| SCHM-01 | 40-01 | Database migration makes password_hash nullable for OAuth-only accounts | SATISFIED | Migration 012 `ALTER COLUMN password_hash DROP NOT NULL` confirmed in file |
| SCHM-02 | 40-01 | Database migration adds has_password boolean column to auth.users | SATISFIED | Migration 012 `ADD COLUMN has_password BOOLEAN NOT NULL DEFAULT true` confirmed in file |
| OAUTH-01 | 40-02, 40-03 | User can log in with Google OAuth (Authorization Code flow with PKCE) | SATISFIED | Google provider config, FetchGoogleProfile, full callback flow with PKCE verified |
| OAUTH-02 | 40-02, 40-03 | User can log in with GitHub OAuth (Authorization Code flow with PKCE) | SATISFIED | GitHub provider config, FetchGitHubProfile with /user/emails, full callback flow verified |
| OAUTH-03 | 40-02, 40-03 | User can sign up via Google OAuth (creates account with no password, personal workspace) | SATISFIED | FindOrCreateUser step 4: creates user via CreateOAuthUser (no password), then CreatePersonalWorkspace |
| OAUTH-04 | 40-02, 40-03 | User can sign up via GitHub OAuth (creates account with no password, personal workspace) | SATISFIED | Same FindOrCreateUser path; GitHub profile returned with EmailVerified=true triggers workspace creation |
| OAUTH-05 | 40-02, 40-03 | Social login auto-links to existing account when provider email matches and is verified | SATISFIED | FindOrCreateUser step 3: after email verification gate, GetByEmail result triggers oauthRepo.Create for existing user |
| OAUTH-06 | 40-02, 40-03 | Social login rejects auto-link when provider email is not verified | SATISFIED | service.go line 68: `if !profile.EmailVerified { return nil, false, ErrEmailNotVerified }` |
| SEC-01 | 40-02, 40-03 | OAuth flow uses CSRF state parameter (random 32-byte, HttpOnly cookie, validated on callback) | SATISFIED | handler.go: 32-byte rand state, HttpOnly cookie with SameSite=Lax, state validated against query param before proceeding |
| SEC-03 | 40-03 | OAuth callback endpoints are rate-limited (10 req/min per IP) | SATISFIED | router.go: `NewRateLimiter(10, time.Minute)` applied only to the callback route group |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `handler.go` | 231 | `if h.sessionSvc != nil` session creation is optional (silently skipped on error with `_, _ =`) | Info | Session may not be tracked for OAuth logins if sessionSvc returns error; non-blocking for OAuth flow |

No blockers or warnings found. The single Info item is an intentional defensive pattern (sessionSvc nil-guard) that does not affect the OAuth authentication flow.

### Human Verification Required

#### 1. Full Google OAuth Round-Trip

**Test:** Configure GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET, start backend, navigate to `GET /auth/oauth/google`, complete Google sign-in, verify redirect to frontend `/auth/callback?code=...`, then POST `/auth/oauth/exchange` with the code.
**Expected:** Access and refresh JWT cookies set; user created in database with has_password=false; personal workspace created.
**Why human:** Requires live Google OAuth credentials and network access to Google APIs.

#### 2. Full GitHub OAuth Round-Trip

**Test:** Configure GITHUB_CLIENT_ID and GITHUB_CLIENT_SECRET, start backend, navigate to `GET /auth/oauth/github`, complete GitHub sign-in (including private-email users).
**Expected:** Profile fetched from /user/emails; verified primary email used; user resolved or created; tokens exchanged.
**Why human:** Requires live GitHub OAuth credentials; private-email scenario requires a GitHub account with that setting.

#### 3. CSRF State Validation Under Attack

**Test:** Initiate OAuth flow, capture the state cookie, then submit the callback with a modified `state` query parameter.
**Expected:** Callback rejects the request and redirects to frontend with `error=invalid_state`.
**Why human:** Requires controlled HTTP manipulation; automated tests would need mocking.

#### 4. Rate Limit Enforcement on Callback

**Test:** Send 11+ rapid requests to `/auth/oauth/google/callback` from the same IP within 60 seconds.
**Expected:** Requests 11+ receive 429 Too Many Requests.
**Why human:** Requires timing-dependent load test; rate limiter state is in-memory and not unit-testable without live server.

### Gaps Summary

No gaps found. All 12 must-haves from all three plans (40-01, 40-02, 40-03) are verified at all three levels: existence, substantive implementation, and correct wiring. All 10 requirement IDs (SCHM-01, SCHM-02, OAUTH-01 through OAUTH-06, SEC-01, SEC-03) are satisfied with direct evidence in the codebase.

The backend OAuth core is fully implemented:
- **Data layer (Plan 01):** Migration 012 applied, sqlc queries generated, user entity and repository handle OAuth-only users.
- **Domain layer (Plan 02):** OAuth entity, errors, repository interface, provider configs, profile fetchers, and FindOrCreateUser service all implemented with correct security logic.
- **HTTP layer (Plan 03):** All 5 handlers implemented with PKCE, CSRF state cookies, one-time Redis codes, rate-limited callback, and JWT-protected account management — all wired into the router.

The build compiles cleanly (`go build ./...`) and all unit tests pass (no failures across 30+ packages).

---

_Verified: 2026-02-22T16:00:00Z_
_Verifier: Claude (gsd-verifier)_
