---
phase: 40-database-migration-and-backend-oauth-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/db/migrations/012_oauth_nullable_password.sql
  - backend/db/queries/oauth_accounts.sql
  - backend/internal/infra/queries/oauth_accounts.sql.go
  - backend/internal/infra/queries/models.go
  - backend/internal/domain/auth/user/entity.go
  - backend/internal/domain/auth/user/entity_test.go
  - backend/internal/domain/auth/user/service.go
  - backend/internal/domain/auth/user/service_test.go
  - backend/internal/domain/auth/user/handler.go
  - backend/internal/domain/auth/user/handler_test.go
  - backend/internal/domain/auth/user/repository.go
  - backend/internal/infra/postgres/user_repository.go
  - backend/internal/infra/postgres/user_repository_test.go
  - backend/internal/testutil/factory/user.go
autonomous: true
requirements: [SCHM-01, SCHM-02]

must_haves:
  truths:
    - "OAuth-only users can be created in the database with NULL password_hash"
    - "Existing users retain has_password=true after migration"
    - "User entity exposes HasPassword() boolean and handles empty password hash"
    - "GET /users/me returns has_password field in response"
    - "sqlc queries for oauth_accounts table are generated and compile"
  artifacts:
    - path: "backend/db/migrations/012_oauth_nullable_password.sql"
      provides: "Schema changes for OAuth-only users"
      contains: "ALTER TABLE auth.users ALTER COLUMN password_hash DROP NOT NULL"
    - path: "backend/db/queries/oauth_accounts.sql"
      provides: "sqlc query definitions for user_oauth_accounts"
      contains: "GetOAuthAccountByProviderAndID"
    - path: "backend/internal/domain/auth/user/entity.go"
      provides: "User entity with hasPassword field and NewOAuthUser constructor"
      contains: "func NewOAuthUser"
  key_links:
    - from: "backend/internal/infra/postgres/user_repository.go"
      to: "backend/db/queries/oauth_accounts.sql"
      via: "scanUser handles nullable password_hash via *string"
      pattern: "scanUser.*\\*string"
    - from: "backend/internal/domain/auth/user/handler.go"
      to: "backend/internal/domain/auth/user/entity.go"
      via: "HasPassword in UserResponse"
      pattern: "HasPassword.*bool"
---

<objective>
Database migration for OAuth-only users and user entity extension with sqlc queries for oauth_accounts

Purpose: Establish the data layer foundation that all OAuth functionality depends on -- nullable password, has_password tracking, and oauth_accounts repository queries.
Output: Migration 012, updated user entity/repository/handler, and generated sqlc code for oauth_accounts.
</objective>

<execution_context>
@/home/antti/.claude/get-shit-done/workflows/execute-plan.md
@/home/antti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-database-migration-and-backend-oauth-core/40-RESEARCH.md

Key references (read before implementing):
@backend/internal/domain/auth/user/entity.go
@backend/internal/domain/auth/user/handler.go
@backend/internal/domain/auth/user/service.go
@backend/internal/domain/auth/user/repository.go
@backend/internal/infra/postgres/user_repository.go
@backend/internal/testutil/factory/user.go
@backend/db/migrations/001_initial_schema.sql (lines 109-177 for users + user_oauth_accounts tables)
@backend/db/migrations/011_notification_preferences.sql (latest migration pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 012 and sqlc queries for oauth_accounts</name>
  <files>
    backend/db/migrations/012_oauth_nullable_password.sql
    backend/db/queries/oauth_accounts.sql
    backend/internal/infra/queries/oauth_accounts.sql.go
    backend/internal/infra/queries/models.go
  </files>
  <action>
    1. Create `backend/db/migrations/012_oauth_nullable_password.sql`:
       - migrate:up:
         - `ALTER TABLE auth.users ALTER COLUMN password_hash DROP NOT NULL;`
         - `ALTER TABLE auth.users ADD COLUMN has_password BOOLEAN NOT NULL DEFAULT true;`
       - migrate:down:
         - `UPDATE auth.users SET password_hash = '$2a$10$placeholder' WHERE password_hash IS NULL;`
         - `ALTER TABLE auth.users ALTER COLUMN password_hash SET NOT NULL;`
         - `ALTER TABLE auth.users DROP COLUMN IF EXISTS has_password;`

    2. Run `mise run migrate` to apply migration.

    3. Create `backend/db/queries/oauth_accounts.sql` with these sqlc queries (the `auth.user_oauth_accounts` table already exists from migration 001):
       - `GetOAuthAccountByProviderAndID :one` -- SELECT by provider + provider_user_id
       - `ListOAuthAccountsByUser :many` -- SELECT by user_id ORDER BY created_at
       - `CreateOAuthAccount :one` -- INSERT with user_id, provider, provider_user_id, email, display_name, avatar_url (leave access_token, refresh_token, token_expires_at NULL per project decision)
       - `DeleteOAuthAccountByProvider :exec` -- DELETE by user_id + provider
       - `CountOAuthAccountsByUser :one` -- SELECT COUNT(*) by user_id

    4. Run `mise run sqlc` to generate Go code.

    5. Verify generated code compiles: `cd backend && go build ./...`
  </action>
  <verify>
    - `mise run migrate` succeeds
    - `mise run sqlc` succeeds without errors
    - `cd backend && go build ./...` compiles cleanly
    - `psql` confirms: `\d auth.users` shows `password_hash` as nullable, `has_password` as `boolean NOT NULL DEFAULT true`
  </verify>
  <done>Migration 012 applied, oauth_accounts sqlc queries generated, backend compiles</done>
</task>

<task type="auto">
  <name>Task 2: Extend user entity, repository, handler, and all call sites for hasPassword + NewOAuthUser</name>
  <files>
    backend/internal/domain/auth/user/entity.go
    backend/internal/domain/auth/user/entity_test.go
    backend/internal/domain/auth/user/service.go
    backend/internal/domain/auth/user/service_test.go
    backend/internal/domain/auth/user/handler.go
    backend/internal/domain/auth/user/handler_test.go
    backend/internal/domain/auth/user/repository.go
    backend/internal/infra/postgres/user_repository.go
    backend/internal/infra/postgres/user_repository_test.go
    backend/internal/testutil/factory/user.go
  </files>
  <action>
    **User Entity (entity.go):**
    1. Add `hasPassword bool` field to User struct (after `passwordHash`).
    2. Add `HasPassword() bool` getter.
    3. Add `NewOAuthUser(email, fullName string) (*User, error)` constructor:
       - Validates email and fullName are non-empty (same errors as NewUser).
       - Returns User with empty passwordHash, `hasPassword: false`, default preferences (same as NewUser: dateFormat="YYYY-MM-DD", language="en", theme="system", timeFormat="24h", thousandSeparator=",", decimalSeparator=".", notificationPreferences=empty map, avatarPath=nil).
    4. Update `CheckPassword()`: if `u.passwordHash == ""`, return false immediately (prevents bcrypt panic on empty string).
    5. Update `Reconstruct` signature: add `hasPassword bool` parameter after `passwordHash`. Wire to struct field.
    6. Update `UpdatePassword()`: after setting passwordHash, also set `u.hasPassword = true`.

    **Repository interface (repository.go):**
    7. Add `CreateOAuthUser(ctx, *User) error` method to repository interface (same as Save but for OAuth users -- or reuse Save if it can handle nullable password_hash).

    **PostgreSQL Repository (user_repository.go):**
    8. Update `scanUser()` function: change password_hash scan target from `string` to `*string` (nullable). Convert nil to empty string when constructing User via Reconstruct.
    9. Add `has_password` to all SELECT column lists and INSERT/UPDATE queries in the repository.
    10. Pass hasPassword through Reconstruct call in scanUser.
    11. Update Save/Create to handle empty passwordHash by writing NULL for empty string.

    **Handler (handler.go):**
    12. Add `HasPassword bool` field to `UserResponse` struct (JSON tag: `has_password`).
    13. Add `HasPassword bool` field to `UserAdminResponse` struct.
    14. Update all response builders that create UserResponse/UserAdminResponse to include `HasPassword: u.HasPassword()`.
    15. Check the `uploadAvatar` handler -- it also returns a JSON response with user fields; add has_password there too.

    **Service (service.go):**
    16. Add `CreateOAuthUser(ctx, CreateOAuthUserInput) (*User, error)` method that calls `NewOAuthUser()` then saves via repository.
    17. Define `CreateOAuthUserInput` struct with `Email, FullName string`.

    **Tests and Factory:**
    18. Update ALL `Reconstruct()` call sites across test files to include the new `hasPassword` parameter. Grep for `Reconstruct(` across the entire backend to find every call site.
       - `entity_test.go`: Update Reconstruct calls, add `true` for existing user tests.
       - `service_test.go`: Update Reconstruct calls.
       - `handler_test.go`: Update Reconstruct calls.
       - `user_repository_test.go`: Update Reconstruct calls.
       - `factory/user.go`: Update Reconstruct calls in WithEmail and WithSuperuser builders.
       - Any other files found by grep.
    19. Add unit test for `NewOAuthUser`: verify empty passwordHash, hasPassword=false, CheckPassword returns false.
    20. Add unit test for `CheckPassword` with empty hash: must return false, must not panic.
    21. Verify: `cd backend && go test ./internal/domain/auth/user/... -v`

    CRITICAL: After changing Reconstruct signature, grep for ALL callers across the entire `backend/` directory: `grep -r "Reconstruct(" backend/internal/` and update each one. Missing a call site will cause compilation failure.
  </action>
  <verify>
    - `cd backend && go build ./...` compiles without errors
    - `cd backend && go test ./internal/domain/auth/user/... -v` all tests pass
    - `cd backend && go test ./internal/testutil/... -v` compiles
    - `cd backend && go test ./internal/infra/postgres/... -v -run TestUser` passes (if unit tests exist)
    - `grep -r "HasPassword" backend/internal/domain/auth/user/handler.go` confirms has_password in responses
  </verify>
  <done>User entity has hasPassword field and NewOAuthUser constructor; repository handles nullable password_hash; handler returns has_password in API responses; all existing tests pass with updated Reconstruct signatures</done>
</task>

</tasks>

<verification>
1. Database: `psql -c "SELECT column_name, is_nullable FROM information_schema.columns WHERE table_name='users' AND column_name IN ('password_hash', 'has_password')"` shows password_hash nullable, has_password NOT NULL
2. Build: `cd backend && go build ./...` succeeds
3. Tests: `cd backend && go test ./...` passes (excluding integration tests)
4. API response includes has_password: start dev server, login, check GET /users/me response body
</verification>

<success_criteria>
- Migration 012 creates nullable password_hash and has_password column
- Existing users have has_password=true
- NewOAuthUser constructor creates user with empty password hash and hasPassword=false
- CheckPassword returns false for empty hash without panicking
- UserResponse includes has_password field
- sqlc queries for oauth_accounts are generated
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/40-database-migration-and-backend-oauth-core/40-01-SUMMARY.md`
</output>
