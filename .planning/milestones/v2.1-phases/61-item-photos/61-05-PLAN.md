---
phase: 61-item-photos
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [.mise.toml]
autonomous: true
requirements: [PHOTO-01]
gap_closure: true
gap_source: 61-UAT.md
gaps_addressed:
  - "Gallery tile transitions from PROCESSING... to rendered thumbnail after backend processes the photo"

must_haves:
  truths:
    - "mise run start launches the asynq worker process alongside the backend and frontend"
    - "Thumbnail jobs queued in Redis are consumed and processed after startup"
    - "Gallery tiles transition from PROCESSING... to a rendered thumbnail after upload"
  artifacts:
    - path: ".mise.toml"
      provides: "tasks.start with worker process included"
      contains: "mise run worker"
  key_links:
    - from: ".mise.toml tasks.start"
      to: "backend/cmd/worker/main.go"
      via: "mise run worker process in start run command"
      pattern: "mise run worker"
---

<objective>
Add the asynq thumbnail worker to the `tasks.start` run command in `.mise.toml`.

Purpose: The worker process (`cmd/worker/main.go`) consumes thumbnail jobs from Redis. Without it running, jobs queue indefinitely and `thumbnail_status` never advances from `pending`. UAT test 1 failed because `mise run start` omits `mise run worker`.

Output: `.mise.toml` with `tasks.start` updated so that `mise run worker` starts alongside `mise run dev` and `mise run fe-dev`. Single-line change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add worker to tasks.start run command</name>
  <files>.mise.toml</files>
  <action>
In `.mise.toml`, locate `[tasks.start]` (around line 69). Its `run` field currently reads:

```toml
run = "mise run dev & mise run fe-dev"
```

Change it to:

```toml
run = "mise run dev & mise run worker & mise run fe-dev"
```

No other changes. The `[tasks.worker]` task already exists (line 123) and is correct — it runs `cd backend && go run cmd/worker/main.go` with `dc-up` as a dependency.

Note the ordering: `dev` (backend API server with hot reload) starts first, `worker` (asynq job consumer) starts second, `fe-dev` (Vite dev server) starts last. All three run in parallel via `&`.
  </action>
  <verify>
    <automated>grep -n "mise run worker" /home/antti/Repos/Misc/home-warehouse-system/.mise.toml</automated>
  </verify>
  <done>`tasks.start` run value contains `mise run dev & mise run worker & mise run fe-dev` and the grep returns the matching line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localhost task runner | mise executes shell commands as the local user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-05-01 | Elevation of Privilege | mise task runner | accept | Local dev tool only; no production exposure; worker process runs with same user permissions as other tasks |
</threat_model>

<verification>
After the edit, confirm:

1. `grep "mise run worker" .mise.toml` returns exactly one line inside `[tasks.start]`
2. The `tasks.start` run line reads: `mise run dev & mise run worker & mise run fe-dev`
3. No other tasks.start fields were modified (description, depends still present)
</verification>

<success_criteria>
Running `mise run start` launches three background processes: the Go backend dev server, the asynq worker, and the Vite frontend dev server. Thumbnail jobs submitted after photo upload are consumed by the worker, causing `thumbnail_status` to advance from `pending` to `ready` and gallery tiles to render actual thumbnails instead of the PROCESSING... placeholder.
</success_criteria>

<output>
After completion, create `.planning/phases/61-item-photos/61-05-SUMMARY.md` following the standard summary template.
</output>
