---
phase: 63
plan: 01
subsystem: frontend2-navigation-and-i18n
tags:
  - navigation
  - i18n
  - ui-polish
  - frontend2
dependencies:
  requires:
    - Phase 58 (TaxonomyPage + tabs)
    - Phase 59 (BorrowersListPage)
    - Phase 60 (ItemsListPage v2.1)
    - Phase 62 (LoansListPage v2.1)
  provides:
    - Sidebar parity with v2.1 surfaces (NAV-01)
    - Dashboard quick-action cards verified to resolve to real list pages (NAV-02)
    - LocationsTab empty-state copy aligned to UI-SPEC
    - Full Estonian catalog coverage for all v2.1 surfaces (REQ-04)
  affects:
    - frontend2/src/components/layout/Sidebar.tsx
    - frontend2/src/features/taxonomy/tabs/LocationsTab.tsx
    - frontend2/locales/en/messages.po
    - frontend2/locales/et/messages.po
tech-stack:
  added: []
  patterns:
    - Sidebar mt-auto spacer pattern for pushing SETTINGS to the bottom of the flex column
    - Lingui extract → manual translation → Lingui compile catalog workflow
key-files:
  created:
    - .planning/phases/63-navigation-and-polish/63-01-SUMMARY.md
    - .planning/phases/63-navigation-and-polish/deferred-items.md
  modified:
    - frontend2/src/components/layout/Sidebar.tsx
    - frontend2/src/features/taxonomy/tabs/LocationsTab.tsx
    - frontend2/locales/en/messages.po (auto-regenerated by lingui extract)
    - frontend2/locales/et/messages.po (regenerated by extract, then manually gap-filled)
decisions:
  - D-01 applied: BORROWERS + TAXONOMY NavLinks inserted in UI-SPEC order
  - D-02 applied: mt-auto spacer (single <div className="mt-auto" />) pushes SETTINGS to bottom; no horizontal divider
  - D-03 applied: no /scan NavLink added (scanning deferred to v2.2)
  - D-04 applied: NAV-02 treated as verification-only; QuickActionCards.tsx unchanged
  - D-05 applied: UI-SPEC wins audit; LocationsTab body rewritten to match table; other 9 empty states confirmed verbatim
  - D-06 applied: full ET catalog sweep; zero non-header empty msgstr; zero [ET] … placeholders
metrics:
  duration_minutes: 17
  completed_date: 2026-04-17
  task_count: 3 (plus Task 4 checkpoint returned to orchestrator)
  files_modified: 4
---

# Phase 63 Plan 01: Navigation & Polish Summary

Wired BORROWERS and TAXONOMY into the retro sidebar (plus mt-auto spacer before SETTINGS),
aligned the LocationsTab empty-state body with UI-SPEC copy, and completed a full Estonian
catalog sweep so every v2.1 surface renders translated strings instead of [ET] placeholders.

## Final Children Order of Sidebar.tsx <nav>

Source order (with line numbers from the post-edit file):

1. `NavLink to="/" end` → `{t\`DASHBOARD\`}` (line 24)
2. `NavLink to="/items"` → `{t\`ITEMS\`}` (line 34)
3. `NavLink to="/loans"` → `{t\`LOANS\`}` (line 43)
4. `NavLink to="/borrowers"` → `{t\`BORROWERS\`}` (line 52) — NEW
5. `NavLink to="/taxonomy"` → `{t\`TAXONOMY\`}` (line 61) — NEW
6. `<div className="mt-auto" />` (line 70) — NEW spacer
7. `NavLink to="/settings"` → `{t\`SETTINGS\`}` (line 71) — pushed to bottom

All new NavLinks reuse the existing `navItemBase`/`navItemDefault`/`navItemActive`
class constants verbatim; no new CSS tokens or classes introduced. No `/scan` NavLink
was added (D-03 honored).

## LocationsTab.tsx Body Diff

```diff
 {workspaceId && query.isSuccess && allItems.length === 0 && (
   <RetroEmptyState
     title={t`NO LOCATIONS YET`}
-    body={t`Create your first location to describe where items live.`}
+    body={t`Create your first location to start placing items.`}
     action={
       <RetroButton variant="primary" onClick={handleNew}>
         {t`+ NEW LOCATION`}
       </RetroButton>
     }
   />
 )}
```

Title (`NO LOCATIONS YET`) and action (`+ NEW LOCATION`) unchanged; only the body
string was rewritten to match the UI-SPEC empty-state copy table verbatim.

## Empty-State Audit Results (all 10 surfaces)

| Surface | File | Outcome |
|---------|------|---------|
| /items — NO ITEMS YET | ItemsListPage.tsx:291-292 | Confirmed verbatim match |
| /items — NO MATCHES | ItemsListPage.tsx:259-260 | Confirmed verbatim match |
| /items — NO ACTIVE ITEMS | ItemsListPage.tsx:275-276 | Confirmed verbatim match |
| /loans tab=active — NO ACTIVE LOANS | LoansListPage.tsx:150-151 | Confirmed verbatim match |
| /loans tab=overdue — NO OVERDUE LOANS | LoansListPage.tsx:164-165 | Confirmed verbatim match |
| /loans tab=history — NO LOAN HISTORY | LoansListPage.tsx:173-174 | Confirmed verbatim match |
| /borrowers — NO BORROWERS YET | BorrowersListPage.tsx:196-197 | Confirmed verbatim match |
| /taxonomy categories — NO CATEGORIES YET | CategoriesTab.tsx:135-136 | Confirmed verbatim match |
| /taxonomy locations — NO LOCATIONS YET | LocationsTab.tsx:137-138 | **Rewrote body** (see diff above) |
| /taxonomy containers — NO CONTAINERS YET | ContainersTab.tsx:167-168 | Confirmed verbatim match |

9 verbatim matches + 1 rewritten body = 10 / 10 surfaces aligned with UI-SPEC.

## Estonian Catalog Metrics (Before vs After)

| Metric | Before extract | After extract | After gap-fill |
|--------|----------------|---------------|----------------|
| Total msgids | 456 | 457 (+1 new: `Create your first location to start placing items.`) | 457 |
| Empty `msgstr ""` entries (non-header) | 214 | 218 (+4 newly extracted) | 0 |
| `[ET] …` placeholder entries | 117 | 117 | 0 |
| Header `msgstr ""` (PO metadata) | 1 | 1 | 1 |
| Obsolete (`#~`) entries | 5 | 7 (+2: old locations string and old JPEG/PNG/HEIC string) | 7 (all have ET translations even in obsolete form, as documentation) |

Result: 335 new or replaced Estonian translations written in this plan (218 previously
empty + 117 previously `[ET] …` placeholder).

Key translations applied (from UI-SPEC):

- Sidebar: `BORROWERS` → `LAENAJAD`, `TAXONOMY` → `TAKSONOOMIA` (new); `DASHBOARD` → `TÖÖLAUD`, `ITEMS` → `ESEMED`, `LOANS` → `LAENUTUSED`, `SETTINGS` → `SEADED` (pre-existing, confirmed unchanged)
- New-entity actions: `+ UUS ESE`, `+ UUS LAENUTUS`, `+ UUS LAENAJA`, `+ UUS KATEGOORIA`, `+ UUS ASUKOHT`, `+ UUS KONTEINER`
- Empty-state titles: `ESEMEID POLE VEEL`, `VASTEID POLE`, `AKTIIVSEID ESEMEID POLE`, `AKTIIVSEID LAENUTUSI POLE`, `ÜLETATUD TÄHTAJAGA LAENUTUSI POLE`, `LAENUTUSAJALUGU POLE`, `LAENAJAID POLE VEEL`, `KATEGOORIAID POLE VEEL`, `ASUKOHTI POLE VEEL`, `KONTEINEREID POLE VEEL`
- Empty-state bodies: `Lisa oma esimene ese, et alustada laoseisu jälgimist.`, `Lisa oma esimene asukoht, et hakata esemeid paigutama.`, `Konteinerid kuuluvad asukoha juurde. Loo esmalt asukoht ja lisa seejärel konteinerid.`, etc.

Lingui compile regenerated both locale bundles (`frontend2/locales/{en,et}/messages.ts`,
gitignored). Compiled bundle mtime (18:01) is newer than the .po file mtime (18:00),
confirming fresh compilation.

## Dashboard Card Routing (NAV-02) Verification

Per D-04, NAV-02 is a verification step only — no code change expected, since
`QuickActionCards.tsx` was already wired correctly in phase 57/60.

Verified via `grep`:

```
frontend2/src/features/dashboard/QuickActionCards.tsx:
  line 16: { labelKey: "ADD ITEM", to: "/items" }
  line 17: { labelKey: "SCAN BARCODE", to: "/scan" }
  line 18: { labelKey: "VIEW LOANS", to: "/loans" }
```

Static verification (the Task 4 human-verify checkpoint covers runtime confirmation):

- `ADD ITEM` → `/items` → `frontend2/src/features/items/ItemsListPage.tsx` (real v2.1 list page, phase 60)
- `VIEW LOANS` → `/loans` → `frontend2/src/features/loans/LoansListPage.tsx` (real v2.1 list page, phase 62)
- `SCAN BARCODE` → `/scan` → `frontend2/src/features/scan/ScanPage.tsx` (v2.2 placeholder — intentional)

Both NAV-02 target cards resolve to real list pages. No 404, no DemoPage.

## Tests / Lint / Build Status

- `cd frontend2 && bun run test` → 78 test files / 483 tests passing (unchanged before/after)
- `cd frontend2 && bun run i18n:extract` → exit 0; 457 total msgids; 0 missing in EN, 222→0 missing in ET after gap-fill
- `cd frontend2 && bun run i18n:compile` → exit 0; bundles regenerated
- `cd frontend2 && bun run lint` → FAILS due to 10 pre-existing errors in unrelated files (see Deferred Issues). Phase 63 files themselves (Sidebar.tsx, LocationsTab.tsx, messages.po) pass lint cleanly.

## Deviations from Plan

### Auto-fixed Issues

None — Tasks 1-3 executed exactly as written. No Rule 1 bugs discovered, no Rule 2
missing-critical additions needed, no Rule 3 blockers encountered.

### Auth Gates

None — this plan is pure frontend wiring/polish; no auth-gated flows touched.

## Deferred Issues

### Pre-existing lint errors (out of scope per SCOPE BOUNDARY)

`bun run lint` exited with 10 errors and 8 warnings in `frontend2/`. All errors/warnings
exist in files unrelated to Phase 63 scope:

- `src/features/taxonomy/panel/SlideOverPanel.tsx:94` — `react-hooks/refs` error (Phase 58 artifact)
- `src/features/taxonomy/tree/TreeNode.tsx:31` — unused `_expandedIds` variable (Phase 58 artifact)
- `src/lib/api.ts:89` — unnecessary try/catch wrapper (Phase 56 artifact)
- Five `react-hooks/exhaustive-deps` warnings across phases 58, 61 hooks
- One unused `eslint-disable` directive in ContainersTab.tsx:63

Captured against clean master (HEAD = e2e9efd) before Phase 63 edits: 19 problems
(11 errors, 8 warnings). After Phase 63 edits: 18 problems (10 errors, 8 warnings) —
Phase 63 introduced zero new lint violations and actually removed one. Full log at
`.planning/phases/63-navigation-and-polish/deferred-items.md`.

Impact: the plan's acceptance criteria require `bun run lint` to exit 0, but the
pre-existing errors in unrelated files prevent this. This should be cleaned up in
a dedicated tech-debt phase, not folded into the v2.1 milestone-close-out plan.

## Commit Log

| Commit | Task | Message |
|--------|------|---------|
| `c431720` | Task 1 | feat(63-01): add BORROWERS and TAXONOMY sidebar NavLinks with mt-auto spacer |
| `dad7f63` | Task 2 | fix(63-01): rewrite LocationsTab empty-state body to match UI-SPEC |
| `02c9949` | Task 3 | feat(63-01): run Lingui extract and complete ET catalog gap-fill |

## Task 4 (Human-Verify Checkpoint) Outcome

Returned to orchestrator for runtime UAT. Plan execution paused at Task 4 per the
checkpoint-handling protocol — a fresh agent (or the user) will spin up
`cd frontend2 && bun run dev`, walk through the 6 steps in the plan's `<how-to-verify>`
block, and record `approved` or a failure list. Phase 63 is code-complete; only
visual sign-off remains.

## Self-Check: PASSED

- [x] `frontend2/src/components/layout/Sidebar.tsx` exists and contains BORROWERS/TAXONOMY NavLinks + mt-auto spacer
- [x] `frontend2/src/features/taxonomy/tabs/LocationsTab.tsx` exists and contains the new empty-state body
- [x] `frontend2/locales/en/messages.po` regenerated with new msgids
- [x] `frontend2/locales/et/messages.po` has zero non-header empty msgstrs and zero [ET] placeholders
- [x] Commit `c431720` found in `git log --oneline --all`
- [x] Commit `dad7f63` found in `git log --oneline --all`
- [x] Commit `02c9949` found in `git log --oneline --all`
- [x] All 483 vitest tests still pass
- [x] Lingui extract + compile both exit 0
