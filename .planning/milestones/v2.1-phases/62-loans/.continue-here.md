---
context: phase
phase: 62-loans
task: 6
total_tasks: 9
status: in_progress
last_updated: 2026-04-17T12:42:56.945Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

_No blocking constraints from this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| inventoryId vs itemId confusion | Backend loans API expects `inventory_id` (UUID of an inventory *row*), but frontend item pickers yield item *definition* IDs. WR-04 and WR-05 were both caused by passing the wrong ID type. | advisory | Always check which ID type is expected at the API boundary before wiring a picker. `loan.item.id` = item def ID; `loan.inventory_id` = inventory row UUID. |

<current_state>
Phase 62 (loans) is functionally complete. All 4 plans executed, all 8 code-review findings fixed, verification 5/5. Two post-review improvements (LoanForm inventory resolution + useLoanMutations date serialization) are coded but uncommitted. The only gate left is human UAT.
</current_state>

<completed_work>

Completed Tasks:
- Plan 62-01: Backend loans domain, repository, API — Done
- Plan 62-02: Frontend hooks (useLoansForItem, useLoanMutations, etc) — Done
- Plan 62-03: LoansListPage, LoanPanel, LoanForm (create/edit) — Done
- Plan 62-04: Item + Borrower detail panels, end-to-end wiring — Done
- Code review fix (8 findings CR-01–WR-05): all 8 fixed and committed — Done
- Post-review fix: LoanForm now resolves item definition ID → available inventory row UUID via `inventoryApi.available` — Coded, uncommitted
- Post-review fix: useLoanMutations appends `T00:00:00Z` to date strings before sending to API — Coded, uncommitted
</completed_work>

<remaining_work>

- Commit uncommitted files: `frontend2/src/features/loans/forms/LoanForm.tsx`, `frontend2/src/features/loans/hooks/useLoanMutations.ts`, `frontend2/src/lib/api/inventory.ts`, `.planning/phases/62-loans/62-REVIEW-FIX.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`
- Human UAT: run 29-step loan flow in `62-HUMAN-UAT.md` (requires backend :8000 + frontend :5173)
- Mark phase 62 complete → advance to Phase 63 (Navigation & Polish)
</remaining_work>

<decisions_made>

- LoanForm resolves item def ID → available inventory row UUID at pick time via `inventoryApi.available` endpoint. Backend expects inventory_id (row UUID), UI pickers return item definition IDs.
- Dates from HTML date inputs (YYYY-MM-DD) are serialized as YYYY-MM-DDT00:00:00Z in `useCreateLoan` and `useUpdateLoan` before sending to the API.
- `LoanRepository.Delete` returns an explicit error instead of silent no-op (CR-02 fix).
- Non-atomic Create/Return paths documented via TODO(WR-01) comments; full transaction wrapping deferred to avoid scope creep.
</decisions_made>

<blockers>
- Human UAT gate: 29-step end-to-end test in `62-HUMAN-UAT.md` requires real backend + frontend running. Cannot be automated.
</blockers>

## Required Reading (in order)
1. `.planning/phases/62-loans/62-HUMAN-UAT.md` — the 29-step UAT checklist; run this after committing uncommitted files
2. `.planning/phases/62-loans/62-VERIFICATION.md` — verification status (5/5 automated truths verified)
3. `.planning/phases/62-loans/62-REVIEW-FIX.md` — all 8 code review findings and how they were fixed

## Critical Anti-Patterns (do NOT repeat these)

- [WRONG ID TYPE IN LOAN MUTATIONS]: Passing item definition ID where inventory row UUID is expected (or vice versa) → always confirm which ID the API endpoint expects before wiring a picker or cache key

## Infrastructure State
- Backend: `cd backend && mise run dev` (port 8000) — needed for UAT
- Frontend: `cd frontend2 && bun run dev` (port 5173) — needed for UAT
- Cleanup tests `internal/jobs/cleanup_test.go` have DST-flakiness (pre-existing, unrelated to phase 62)

<context>
Session ended at context exhaustion (90%). Phase 62 is the last functional phase of v2.1 before Phase 63 (Navigation & Polish). The post-review LoanForm and useLoanMutations changes are the last programmatic work — they fix inventory ID resolution and date serialization issues discovered during manual testing. After committing these and passing UAT, phase 62 closes and phase 63 can begin.
</context>

<next_action>
Start with: `git add frontend2/src/features/loans/forms/LoanForm.tsx frontend2/src/features/loans/hooks/useLoanMutations.ts frontend2/src/lib/api/inventory.ts .planning/phases/62-loans/62-REVIEW-FIX.md .planning/ROADMAP.md .planning/STATE.md && git commit -m "fix(62): post-review inventory resolution and date serialization fixes"` — then run human UAT per 62-HUMAN-UAT.md.
</next_action>
