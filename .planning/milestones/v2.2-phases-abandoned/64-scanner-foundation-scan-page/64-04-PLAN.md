---
phase: 64
plan: 04
type: execute
wave: 1
depends_on: [64-01]
files_modified:
  - frontend2/src/lib/api/scan.ts
  - frontend2/src/lib/api/index.ts
  - frontend2/src/features/scan/hooks/useScanLookup.ts
  - frontend2/src/features/scan/hooks/__tests__/useScanLookup.test.ts
autonomous: true
requirements: []
requirements_addressed: []
must_haves:
  truths:
    - "frontend2/src/lib/api/scan.ts exports `ScanLookupStatus` discriminated union ('idle' | 'loading' | 'success' | 'error'), `ScanLookupResult` interface, `scanApi` stub object, and `scanKeys` factory"
    - "frontend2/src/lib/api/index.ts barrel re-exports from `./scan`"
    - "useScanLookup(code) always returns { status: 'idle', match: null, error: null, refetch: () => {} } in Phase 64 regardless of input (D-01 + D-18)"
    - "The ScanLookupResult shape matches what Phase 65 will fill in — consumers written in Wave 2/3 do not change when Phase 65 lands"
  artifacts:
    - path: "frontend2/src/lib/api/scan.ts"
      provides: "API client scaffold for scan lookup; empty body in Phase 64, filled by Phase 65 (LOOK-01)"
      exports: ["scanApi", "scanKeys", "ScanLookupStatus", "ScanLookupResult"]
    - path: "frontend2/src/features/scan/hooks/useScanLookup.ts"
      provides: "Stub lookup hook — shape-stable for Phase 65 swap"
      exports: ["useScanLookup"]
  key_links:
    - from: "frontend2/src/features/scan/hooks/useScanLookup.ts"
      to: "frontend2/src/lib/api/scan.ts type ScanLookupResult"
      via: "type import; hook return is typed as ScanLookupResult"
      pattern: "ScanLookupResult"
---

<objective>
Create the Phase 65 ↔ Phase 64 boundary scaffolding: an empty `scanApi` object + `scanKeys` factory in `frontend2/src/lib/api/scan.ts`, and a stub `useScanLookup(code)` hook that always returns `{ status: 'idle', match: null, error: null, refetch: () => {} }` per D-01 + D-18. The stub's return type must be the EXACT shape Phase 65 will fill with a real TanStack Query call — so downstream callsites (banner, history-tap handler) stay untouched when Phase 65 lands.

Purpose: The post-decode banner (Plan 07) and the page-level orchestration (Plan 09) both consume `useScanLookup`. Defining the stable result shape in this plan means those later plans can be authored against a real type, not a TODO. D-18 locks the full 4-state enum into the Phase 64 type so Phase 65 is a "one-file swap."

Output: `scan.ts` API module (scaffold, no HTTP calls), `scan` barrel re-export from `lib/api/index.ts`, `useScanLookup.ts` hook + test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-scanner-foundation-scan-page/64-CONTEXT.md
@.planning/phases/64-scanner-foundation-scan-page/64-RESEARCH.md
@.planning/phases/64-scanner-foundation-scan-page/64-PATTERNS.md
@.planning/phases/64-scanner-foundation-scan-page/64-VALIDATION.md
@frontend2/src/lib/api/index.ts
@frontend2/src/lib/api/inventory.ts
@frontend2/src/lib/api/items.ts
@frontend2/src/features/items/hooks/useItem.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold lib/api/scan.ts + barrel re-export</name>
  <read_first>
    - frontend2/src/lib/api/inventory.ts (27 LOC — the closest "smallest API module" analog per PATTERNS.md §6)
    - frontend2/src/lib/api/index.ts (current barrel)
    - frontend2/src/lib/api/items.ts (verify the Item type export name)
    - .planning/phases/64-scanner-foundation-scan-page/64-CONTEXT.md D-01 + D-18 (stub shape + full 4-state enum)
    - .planning/phases/64-scanner-foundation-scan-page/64-PATTERNS.md §6 lib/api/scan.ts (lines 300-350)
    - .planning/phases/64-scanner-foundation-scan-page/64-RESEARCH.md §Pattern 8 (lines 610-648) — the full stub shape including refetch
  </read_first>
  <files>frontend2/src/lib/api/scan.ts, frontend2/src/lib/api/index.ts</files>
  <action>
    Create `frontend2/src/lib/api/scan.ts`. Verify the `Item` export from `@/lib/api/items` first — if the export is named differently (e.g. `ItemDetail`), use the actual name; record the confirmed name in the SUMMARY.

    File contents:

    ```ts
    // Phase 64 scaffold - Phase 65 (LOOK-01) replaces scanApi + useScanLookup body
    // with a real get() call against GET /workspaces/{wsId}/items?search={code}&limit=1.
    // Shape locked here so Phase 64 callsites (ScanResultBanner, ScanPage) stay intact.
    import type { Item } from "@/lib/api/items";

    export type ScanLookupStatus = "idle" | "loading" | "success" | "error";

    export interface ScanLookupResult {
      status: ScanLookupStatus;
      match: Item | null;
      error: Error | null;
      refetch: () => void;
    }

    // Phase 64: no endpoints. Phase 65 adds lookupByBarcode(wsId, code).
    export const scanApi = {
      // intentionally empty in Phase 64
    } as const;

    export const scanKeys = {
      all: ["scan"] as const,
      lookups: () => [...scanKeys.all, "lookup"] as const,
      lookup: (code: string) => [...scanKeys.lookups(), code] as const,
    };
    ```

    Then read the current `frontend2/src/lib/api/index.ts` barrel and APPEND exactly one line at the end of the existing `export * from ...` list: `export * from "./scan";`. Preserve existing export order.

    Do NOT add any `get()` / `post()` calls. Do NOT import `@tanstack/react-query` in `scan.ts` — the query composition happens in the hook (Task 2) and in Phase 65's replacement. Keep the file truly empty-of-runtime-calls.
  </action>
  <verify>
    <automated>cd frontend2 && test -f src/lib/api/scan.ts && grep -q "export type ScanLookupStatus" src/lib/api/scan.ts && grep -q "export interface ScanLookupResult" src/lib/api/scan.ts && grep -q "export const scanApi" src/lib/api/scan.ts && grep -q "export const scanKeys" src/lib/api/scan.ts && grep -q "./scan" src/lib/api/index.ts && bunx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `frontend2/src/lib/api/scan.ts` exists
    - `grep -c "export type ScanLookupStatus" frontend2/src/lib/api/scan.ts` outputs `1`
    - All four status literals appear: `grep -c '"idle"' frontend2/src/lib/api/scan.ts` ≥ 1; same for `"loading"`, `"success"`, `"error"` (D-18 full enum)
    - `grep -c "export interface ScanLookupResult" frontend2/src/lib/api/scan.ts` outputs `1`
    - `grep -c "match: Item | null" frontend2/src/lib/api/scan.ts` outputs `1`
    - `grep -c "error: Error | null" frontend2/src/lib/api/scan.ts` outputs `1`
    - `grep -c "refetch: () => void" frontend2/src/lib/api/scan.ts` outputs `1`
    - `grep -c "export const scanApi" frontend2/src/lib/api/scan.ts` outputs `1`
    - `grep -c "export const scanKeys" frontend2/src/lib/api/scan.ts` outputs `1`
    - `grep -c 'all: \["scan"\]' frontend2/src/lib/api/scan.ts` outputs ≥ 1
    - `grep -c '"./scan"' frontend2/src/lib/api/index.ts` outputs ≥ 1 (barrel re-exports the new module)
    - NO runtime HTTP call: `grep -cE "get\(|post\(|put\(|delete\(|fetch\(" frontend2/src/lib/api/scan.ts` outputs `0`
    - `cd frontend2 && bunx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>
    `scan.ts` is an empty scaffold with the Phase 65-stable ScanLookupResult shape; barrel re-exports it; TypeScript clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useScanLookup stub hook + unit test</name>
  <read_first>
    - frontend2/src/features/items/hooks/useItem.ts (16 LOC — exact target shape Phase 65 will match, per PATTERNS.md §9)
    - .planning/phases/64-scanner-foundation-scan-page/64-PATTERNS.md §9 useScanLookup (lines 495-532)
    - .planning/phases/64-scanner-foundation-scan-page/64-RESEARCH.md §Pattern 8 (lines 610-648)
    - .planning/phases/64-scanner-foundation-scan-page/64-CONTEXT.md D-01 + D-18 (stub content + full enum)
    - frontend2/src/lib/api/scan.ts (from Task 1 — the ScanLookupResult type import source)
    - frontend2/src/features/items/__tests__/useCategoryNameMap.test.ts (analog hook test shape, cited in PATTERNS.md §Hook tests)
  </read_first>
  <behavior>
    - Test 1: `useScanLookup(null)` returns `{ status: 'idle', match: null, error: null, refetch: <a function> }`
    - Test 2: `useScanLookup("ABC-123")` returns the identical shape — Phase 64 stub ignores input
    - Test 3: `useScanLookup("")` returns the identical shape
    - Test 4: calling `result.current.refetch()` does not throw (no-op function)
    - Test 5: `ScanLookupStatus` type accepts all four states (compile-time check via type-only assignments + runtime array length check)
  </behavior>
  <files>frontend2/src/features/scan/hooks/useScanLookup.ts, frontend2/src/features/scan/hooks/__tests__/useScanLookup.test.ts</files>
  <action>
    RED-GREEN. Write the test file first; confirm it fails (hook file does not exist yet). Then create the hook.

    Hook file — `frontend2/src/features/scan/hooks/useScanLookup.ts`:

    ```ts
    // Phase 64 STUB - always returns { status: 'idle', match: null, error: null, refetch: no-op }.
    // Phase 65 (LOOK-01) replaces the body with a real useQuery call; the return shape
    // stays identical so callsites (ScanResultBanner, ScanPage) do not change.
    import type { ScanLookupResult } from "@/lib/api/scan";

    export function useScanLookup(_code: string | null): ScanLookupResult {
      return {
        status: "idle",
        match: null,
        error: null,
        refetch: () => {},
      };
    }
    ```

    The leading underscore on `_code` signals "intentionally unused in Phase 64" and satisfies eslint.

    Test file — `frontend2/src/features/scan/hooks/__tests__/useScanLookup.test.ts`. Use `renderHook` from `@testing-library/react`. No provider wrapper needed — the hook does not use React context or TanStack Query in Phase 64.

    The test file must cover all 5 behaviors above. Use Vitest's `describe` + `it` + `expect`. The type-acceptance test (Test 5) should declare `const idle: ScanLookupStatus = "idle";` (and same for `"loading"`, `"success"`, `"error"`) then assert the array has length 4 — this is a runtime-visible sentinel that also exercises the type.

    Create the two parent directories if absent: `frontend2/src/features/scan/hooks/` and `frontend2/src/features/scan/hooks/__tests__/`. (The parent `frontend2/src/features/scan/` already exists from the current `ScanPage.tsx` stub.)

    Verify: `cd frontend2 && bun run test -- useScanLookup --run` — must exit 0 with ≥ 5 tests.
  </action>
  <verify>
    <automated>cd frontend2 && test -f src/features/scan/hooks/useScanLookup.ts && test -f src/features/scan/hooks/__tests__/useScanLookup.test.ts && grep -q "export function useScanLookup" src/features/scan/hooks/useScanLookup.ts && grep -q '"idle"' src/features/scan/hooks/useScanLookup.ts && grep -q "match: null" src/features/scan/hooks/useScanLookup.ts && grep -q "refetch: () => {}" src/features/scan/hooks/useScanLookup.ts && bun run test -- useScanLookup --run</automated>
  </verify>
  <acceptance_criteria>
    - `frontend2/src/features/scan/hooks/useScanLookup.ts` exists
    - `grep -c "export function useScanLookup" frontend2/src/features/scan/hooks/useScanLookup.ts` outputs `1`
    - `grep -c 'import type { ScanLookupResult } from "@/lib/api/scan"' frontend2/src/features/scan/hooks/useScanLookup.ts` outputs `1` (type import from Task 1's module)
    - `grep -c 'status: "idle"' frontend2/src/features/scan/hooks/useScanLookup.ts` outputs `1`
    - `grep -c "match: null" frontend2/src/features/scan/hooks/useScanLookup.ts` outputs `1`
    - `grep -c "error: null" frontend2/src/features/scan/hooks/useScanLookup.ts` outputs `1`
    - `grep -c "refetch: () => {}" frontend2/src/features/scan/hooks/useScanLookup.ts` outputs `1`
    - NO call to `useQuery`: `grep -c "useQuery" frontend2/src/features/scan/hooks/useScanLookup.ts` outputs `0` (D-01 — Phase 65 adds this, not Phase 64)
    - `frontend2/src/features/scan/hooks/__tests__/useScanLookup.test.ts` exists
    - `cd frontend2 && bun run test -- useScanLookup --run` exits 0 with ≥ 5 passing test cases
    - `cd frontend2 && bun run test --run` exits 0 (full suite regression-clean)
    - `cd frontend2 && bunx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>
    useScanLookup stub returns the locked shape; ≥ 5 unit tests pass; TypeScript clean; no useQuery call (that's Phase 65's job).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 64 → Phase 65 type contract | ScanLookupResult shape is the interphase API; Phase 65 must not break it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-12 | Tampering | Phase 65 author silently changes ScanLookupResult shape | mitigate | Shape is type-locked in `@/lib/api/scan` and imported by downstream consumers; any shape change in Phase 65 breaks compile across banner + page. CI tsc will catch. |
| T-64-13 | Information Disclosure | scanKeys factory includes user-entered code as a cache key | accept | Cache keys are local-only (no network egress in Phase 64). Phase 65 uses the same key for the real query; codes are not secrets. |

</threat_model>

<verification>
- `cd frontend2 && bun run test -- useScanLookup --run` passes (≥ 5 cases)
- `cd frontend2 && bun run test --run` full suite green
- `cd frontend2 && bunx tsc --noEmit -p tsconfig.json` clean
- `cd frontend2 && bun run lint:imports` clean
- `frontend2/src/lib/api/index.ts` re-exports from `./scan`
- No `useQuery` call in Phase 64's `useScanLookup.ts`
</verification>

<success_criteria>
- `lib/api/scan.ts` exports the full scaffold (ScanLookupStatus, ScanLookupResult, scanApi, scanKeys) with no runtime HTTP calls
- `useScanLookup` stub returns the exact Phase 65-compatible shape with `status === "idle"` for any input
- Full 4-state enum landed so Phase 65's replacement is pure-body
- Tests pass; full suite green; TS clean
</success_criteria>

<output>
After completion, create `.planning/phases/64-scanner-foundation-scan-page/64-04-SUMMARY.md` recording: the confirmed `Item` type name as imported from `@/lib/api/items`, the exact ScanLookupResult interface, the scanKeys factory key structure (`["scan","lookup",code]`), and test count.
</output>
