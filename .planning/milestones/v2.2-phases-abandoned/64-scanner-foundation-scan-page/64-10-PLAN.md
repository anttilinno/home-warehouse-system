---
phase: 64
plan: 10
type: execute
wave: 4
depends_on: [64-09]
files_modified:
  - frontend2/locales/en/messages.po
  - frontend2/locales/et/messages.po
autonomous: true
requirements: [SCAN-01, SCAN-02, SCAN-03, SCAN-04, SCAN-05, SCAN-06, SCAN-07]
requirements_addressed: [SCAN-01, SCAN-02, SCAN-03, SCAN-04, SCAN-05, SCAN-06, SCAN-07]
must_haves:
  truths:
    - "Running `bun run i18n:extract` in frontend2/ produces no pending msgids missing from en/messages.po — every t`...` string landed in Waves 1-3 is in the EN catalog"
    - "frontend2/locales/et/messages.po has a non-empty msgstr for every new msgid added in Phase 64 (per Phase 63 precedent — no EN-only strings)"
    - "bun run i18n:compile produces zero orphan warnings"
    - "bun run test --run is green across the full suite"
    - "bun run lint:imports is green — no idb/serwist/offline/sync substrings in frontend2/src"
    - "bun run build succeeds AND dist/assets/scanner-*.js exists (scanner chunk isolated)"
    - "Bundle gate: /scan main-bundle contribution <= 20 kB gzip. Verified by (a) identifying the main entry chunk, (b) confirming it does NOT contain the @yudiel/react-qr-scanner or zxing-wasm modules, (c) measuring the main chunk's gzipped delta vs the pre-phase baseline (<= 20 kB added)"
  artifacts:
    - path: "frontend2/locales/en/messages.po"
      provides: "EN Lingui catalog including every Phase 64 t`...` msgid"
      contains: "SCAN AGAIN"
    - path: "frontend2/locales/et/messages.po"
      provides: "ET Lingui catalog with manual translations for every Phase 64 msgid"
      contains: "SKANEERI UUESTI"
  key_links:
    - from: "Phase 64 source .tsx files"
      to: "frontend2/locales/en/messages.po via bun run i18n:extract"
      via: "Lingui SWC plugin scans source + writes the catalog"
      pattern: "bun run i18n:extract"
    - from: "frontend2/locales/et/messages.po"
      to: "UI-SPEC §ET catalog draft translations (lines 243-267)"
      via: "manual fill per Phase 63 precedent"
      pattern: "msgstr"
---

<objective>
Final wave: run `bun run i18n:extract` to capture every Phase 64 `t\`...\`` msgid into the EN + ET catalogs, hand-fill the ET translations using the UI-SPEC §ET catalog draft table as the starting point (Phase 63 precedent), compile the catalogs with zero orphan warnings, and execute the full phase gate: `bun run test --run` + `bun run lint:imports` + `bun run build` + bundle-gate verification (≤ 20 kB gzip main-bundle contribution for /scan).

Purpose: Phase 63 established that i18n gap-fill happens in the SAME phase, not a later stabilization phase (no EN-only strings ship). The bundle gate is non-negotiable — if scanner WASM deps leak into the main bundle, the phase is unshippable per CONTEXT.md §Bundle Budget.

Output: Updated locale catalogs, full green-light evidence recorded in the SUMMARY, and the [BLOCKING] bundle gate verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-scanner-foundation-scan-page/64-UI-SPEC.md
@.planning/phases/64-scanner-foundation-scan-page/64-CONTEXT.md
@.planning/phases/64-scanner-foundation-scan-page/64-VALIDATION.md
@.planning/phases/63-navigation-and-polish/63-CONTEXT.md
@frontend2/locales/en/messages.po
@frontend2/locales/et/messages.po
@frontend2/package.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract EN catalog + fill ET translations</name>
  <read_first>
    - frontend2/locales/en/messages.po (current EN catalog — format reference)
    - frontend2/locales/et/messages.po (current ET catalog — to be extended)
    - .planning/phases/64-scanner-foundation-scan-page/64-UI-SPEC.md §ET catalog (lines 243-267) — draft ET translations for every new msgid
    - .planning/phases/64-scanner-foundation-scan-page/64-UI-SPEC.md §Copywriting Contract (lines 136-241) — canonical EN strings
    - .planning/phases/63-navigation-and-polish/63-CONTEXT.md (D-06 — full per-phase gap-fill precedent)
  </read_first>
  <files>frontend2/locales/en/messages.po, frontend2/locales/et/messages.po</files>
  <action>
    1. Run `cd frontend2 && bun run i18n:extract`. This invokes `lingui extract`, which scans all `t\`...\`` calls in `src/**/*.{ts,tsx}` and appends new msgids to both `en/messages.po` AND `et/messages.po`. Commit the resulting EN catalog diff AS-IS — do not hand-edit EN msgstr entries; Lingui auto-fills EN msgstr to match the msgid for the `en` locale.

    2. Open `frontend2/locales/et/messages.po` and fill every empty `msgstr ""` that was added in step 1. Source of truth for translations is UI-SPEC §ET catalog (lines 243-267). Cross-check against the exact msgids in the diff — some extracted msgids may differ from the UI-SPEC copy table if a developer chose different wording for helper text or the like. For any msgid NOT in the UI-SPEC draft table, produce a best-effort Estonian translation following these rules:
       - UPPERCASE headings / buttons / labels stay UPPERCASE in ET
       - Match the register (sentence-case body copy, UPPERCASE labels)
       - Keep punctuation + special characters (arrows →, ellipsis … ) identical
       - Do NOT translate code values or format names (`qr_code`, `MANUAL`)

    3. Confirmed translation set from UI-SPEC §ET catalog table (verbatim — use EXACTLY these values for the listed msgids):
       - SCAN → SKANEERI
       - MANUAL → KÄSITSI
       - HISTORY → AJALUGU
       - SCAN AGAIN → SKANEERI UUESTI
       - LOOK UP CODE → OTSI KOOD
       - RETRY → PROOVI UUESTI
       - USE MANUAL ENTRY → KASUTA KÄSITSI
       - CLEAR HISTORY → TÜHJENDA AJALUGU
       - YES, CLEAR → JAH, TÜHJENDA
       - KEEP HISTORY → HOIA ALLES
       - CAMERA ACCESS DENIED → KAAMERALE LIGIPÄÄS KEELATUD
       - NO CAMERA FOUND → KAAMERAT EI LEITUD
       - SCANNER FAILED TO LOAD → SKÄNNER EI LAADINUD
       - SCANNING UNSUPPORTED → SKANEERIMINE POLE TOETATUD
       - NO SCANS YET → SKANEERIMISI POLE VEEL
       - SCANNED → SKANEERITUD
       - CODE → KOOD
       - FORMAT → FORMAAT
       - `[◉] TORCH ON` → `[◉] TULI SEES`
       - `[◉] TORCH OFF` → `[◉] TULI VÄLJAS`
       - BARCODE OR CODE → VÖÖTKOOD VÕI KOOD

    4. Additional strings that will appear in the extract but may NOT have a pre-made draft (derive cleanly; source of truth is a reasonable ET rendering):
       - `Enter code manually` → `Sisesta kood käsitsi`
       - `Any code supported — QR text, UPC, EAN, Code128 alphanumeric.` → `Toetatud on iga kood — QR-tekst, UPC, EAN, Code128 tähed-numbrid.`
       - `Enter a code before submitting.` → `Enne saatmist sisesta kood.`
       - `Code must be 256 characters or fewer.` → `Kood võib olla maksimaalselt 256 tähemärki.`
       - `Scanned codes appear here. Your last 10 scans are kept on this device.` → `Skaneeritud koodid ilmuvad siia. Selles seadmes hoitakse 10 viimast skaneerimist.`
       - `SCAN HISTORY` → `SKANEERIMISE AJALUGU`
       - `MANUAL ENTRY` → `KÄSITSI SISESTUS`
       - Panel bodies — permission-denied: `Barcode scanning needs camera permission. You can enable it in your browser settings, or enter codes manually.` → `Vöötkoodi skaneerimiseks on vaja kaamera luba. Luba saab seadistada brauseris või sisesta koodid käsitsi.`
       - Panel bodies — no-camera: `This device does not report a working camera. If one is attached, make sure no other app is using it, then reload.` → `See seade ei leia töötavat kaamerat. Kui kaamera on ühendatud, veendu, et ükski teine rakendus seda ei kasuta, ja lae leht uuesti.`
       - Panel bodies — library-init-fail: `The barcode engine could not initialize. Check your connection and retry, or enter codes manually.` → `Vöötkoodimootor ei käivitunud. Kontrolli ühendust ja proovi uuesti või sisesta koodid käsitsi.`
       - Panel bodies — unsupported-browser: `This browser does not support camera scanning. Try the latest Safari, Chrome, Firefox, or Edge — or enter codes manually.` → `See brauser ei toeta kaameraga skaneerimist. Proovi uusimat Safarit, Chrome'i, Firefoxi või Edge'i — või sisesta koodid käsitsi.`
       - Platform hints — iOS: `On iPhone / iPad: Settings → Safari → Camera → Allow.` → `iPhone'is / iPadis: Seaded → Safari → Kaamera → Luba.`
       - Platform hints — Android: `On Android Chrome: tap the lock icon in the address bar → Permissions → Camera → Allow.` → `Androidi Chrome'is: puuduta aadressiribal luku ikooni → Load → Kaamera → Luba.`
       - Platform hints — fallback: `Open your browser's site settings and allow Camera for this page.` → `Ava brauseri saidisätted ja luba sellele lehele kaamera kasutamine.`
       - Destructive confirm body: `All 10 most-recent scanned codes on this device will be removed. This cannot be undone.` → `Selles seadmes eemaldatakse kõik 10 kõige hiljutisemat skaneeritud koodi. Seda ei saa tagasi võtta.`
       - Confirm-dialog title: `CLEAR SCAN HISTORY` → `TÜHJENDA SKANEERIMISE AJALUGU`
       - Suspense fallback: `LOADING SCANNER…` → `SKÄNNERIT LAADITAKSE…` and body `Please wait.` → `Palun oota.`

    5. If the extract produces any msgid not covered above, apply the register rules in step 2 and record the additions in the SUMMARY for review.

    6. Run `cd frontend2 && bun run i18n:compile`. Confirm it exits 0 with no warnings. If it warns about orphan catalog entries OR missing msgstr, fix them immediately (fill the blanks; remove orphans only if they are truly unused — never drop a msgid that Phase 64 source code still references).

    7. Run `grep -c 'msgstr ""' frontend2/locales/et/messages.po` AFTER the hand-fill — the count must equal the count BEFORE Phase 64 (i.e., no NEW empty msgstr strings introduced by this phase). Baseline count can be captured from `git show HEAD:frontend2/locales/et/messages.po | grep -c 'msgstr ""'` run before editing.
  </action>
  <verify>
    <automated>cd frontend2 && bun run i18n:extract && bun run i18n:compile && grep -q "SCAN AGAIN" locales/en/messages.po && grep -q "SKANEERI UUESTI" locales/et/messages.po && grep -q "SCAN HISTORY" locales/en/messages.po && grep -q "CAMERA ACCESS DENIED" locales/en/messages.po && grep -q "KAAMERALE LIGIPÄÄS KEELATUD" locales/et/messages.po && grep -q "USE MANUAL ENTRY" locales/en/messages.po && grep -q "KASUTA KÄSITSI" locales/et/messages.po && grep -q "NO SCANS YET" locales/en/messages.po && grep -q "SKANEERIMISI POLE VEEL" locales/et/messages.po</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend2 && bun run i18n:extract` exits 0
    - `cd frontend2 && bun run i18n:compile` exits 0 with NO warnings (redirect output to a log; `grep -c "warning\|WARN" /tmp/i18n-compile.log` outputs `0`)
    - EN catalog contains every required msgid: `grep -c "SCAN AGAIN" frontend2/locales/en/messages.po` at least 1; same for `USE MANUAL ENTRY`, `CAMERA ACCESS DENIED`, `NO CAMERA FOUND`, `SCANNER FAILED TO LOAD`, `SCANNING UNSUPPORTED`, `NO SCANS YET`, `SCANNED`, `CLEAR HISTORY`, `YES, CLEAR`, `KEEP HISTORY`, `SCAN HISTORY`, `MANUAL ENTRY`, `BARCODE OR CODE`, `LOOK UP CODE`, `LOADING SCANNER`, `CLEAR SCAN HISTORY`
    - ET catalog contains the paired translations: `grep -c "SKANEERI UUESTI" frontend2/locales/et/messages.po` at least 1; same for `KASUTA KÄSITSI`, `KAAMERALE LIGIPÄÄS KEELATUD`, `KAAMERAT EI LEITUD`, `SKÄNNER EI LAADINUD`, `SKANEERIMINE POLE TOETATUD`, `SKANEERIMISI POLE VEEL`, `SKANEERITUD`, `TÜHJENDA AJALUGU`, `JAH, TÜHJENDA`, `HOIA ALLES`, `SKANEERIMISE AJALUGU`, `KÄSITSI SISESTUS`, `VÖÖTKOOD VÕI KOOD`, `OTSI KOOD`, `SKÄNNERIT LAADITAKSE`, `TÜHJENDA SKANEERIMISE AJALUGU`
    - No NEW empty msgstr in ET: the post-Phase-64 count of `grep -c 'msgstr ""' frontend2/locales/et/messages.po` is less than or equal to the pre-Phase-64 baseline (which was 1 — the mandatory first empty msgstr for the header). Capture baseline with `git show HEAD:frontend2/locales/et/messages.po | grep -c 'msgstr ""'` and confirm parity.
    - `cd frontend2 && bun run test --run` full suite green
    - `cd frontend2 && bun run lint:imports` exits 0
    - `cd frontend2 && bunx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>
    Every Phase 64 msgid is in both EN and ET catalogs; no new empty msgstr strings in ET; i18n:compile is warning-free.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: [BLOCKING] Bundle gate verification</name>
  <read_first>
    - frontend2/vite.config.ts (confirm manualChunks entry for `scanner` from Plan 02 Task 1)
    - frontend2/src/routes/index.tsx (confirm React.lazy on /scan from Plan 09 Task 3)
    - .planning/phases/64-scanner-foundation-scan-page/64-CONTEXT.md §Bundle Budget (lines 164-168)
    - .planning/phases/64-scanner-foundation-scan-page/64-VALIDATION.md Bundle gate row (line 45)
    - .planning/phases/64-scanner-foundation-scan-page/64-RESEARCH.md §Pitfall 12 (lines 720-729)
  </read_first>
  <files></files>
  <action>
    This task does NOT write any source files — it verifies the build artifact.

    1. Establish baseline (optional but recommended for SUMMARY reporting). If the git history allows a pre-phase build measurement, record it:
       - `git stash` (unstaged changes) / `git worktree add ../phase64-baseline <pre-phase-sha>` to get a pre-phase tree
       - Build + measure the main entry chunk gzipped size pre-phase
       - Return to the phase working tree
       If the baseline is too painful, skip — focus on the absolute constraint: the scanner chunk MUST be isolated and the main chunk MUST NOT import from `@yudiel/react-qr-scanner` / `barcode-detector` / `zxing-wasm`.

    2. Run `cd frontend2 && bun run build`. Confirm it exits 0.

    3. Inspect `frontend2/dist/assets/`:
       - `ls -la frontend2/dist/assets/` — there must be a file matching `scanner-*.js` AND the main entry chunk (typically `index-*.js` or similar).
       - `du -b frontend2/dist/assets/scanner-*.js` — record the raw byte size of the scanner chunk
       - `gzip -c frontend2/dist/assets/scanner-*.js | wc -c` — record the gzipped size (expect 120-180 kB per RESEARCH.md §Pattern 5)

    4. **Critical containment check** (the actual bundle gate):
       - The main entry chunk must NOT contain scanner deps. Grep the main chunk:

         ```
         ls frontend2/dist/assets/index-*.js | head -n 1 | xargs -I {} grep -oE "yudiel|zxing-wasm|barcode-detector|webrtc-adapter" {} | sort -u
         ```

         This should return ZERO matches. Any hit means a scanner dep slipped through the manualChunks rule — the gate FAILS and the phase is not shippable.

       - The scanner chunk must contain the deps. Grep the scanner chunk:

         ```
         ls frontend2/dist/assets/scanner-*.js | head -n 1 | xargs -I {} grep -oE "yudiel|barcode-detector" {} | head -n 1
         ```

         This should return at least one match, confirming the deps landed in the correct chunk.

       - Mandatory check: if there is a separate Vite/rollup main chunk name like `main-*.js` instead of `index-*.js`, adjust the pattern. The rule is: the chunk that loads FIRST (referenced by `dist/index.html`) must NOT import scanner deps.

    5. Record the final sizes in the SUMMARY: raw bytes + gzipped bytes for `scanner-*.js`, main-chunk gzipped bytes delta (if baseline captured), total dist size, and the containment-grep results.

    6. Run the full phase gate in sequence:
       - `cd frontend2 && bun run test --run`
       - `cd frontend2 && bun run lint:imports`
       - `cd frontend2 && bun run build`
       - `cd frontend2 && bunx tsc --noEmit -p tsconfig.json`
       - `cd frontend2 && bun run i18n:compile`

       All must exit 0. If ANY fails, this plan does NOT complete — diagnose and fix (or escalate — the phase is unshippable until the gate is green).

    7. If everything passes, write the SUMMARY recording every exit code + numerical result.

    **[BLOCKING]**: If this task's bundle gate grep finds scanner deps in the main chunk, this plan CANNOT be marked done. Stop and file a recovery plan — the most likely fix is (a) verify vite.config.ts manualChunks was not reverted, (b) verify routes/index.tsx still uses React.lazy, (c) re-run `bun install` to refresh node_modules, (d) re-run the build. If after all three fixes the deps still leak, escalate: the manualChunks rule may need a functional form instead of the declarative record (see https://rollupjs.org/configuration-options/#output-manualchunks docs).
  </action>
  <verify>
    <automated>cd frontend2 && bun run test --run && bun run lint:imports && bunx tsc --noEmit -p tsconfig.json && bun run build && bun run i18n:compile && ls dist/assets/ | grep -q "scanner-" && test -z "$(ls dist/assets/index-*.js 2>/dev/null | head -n 1 | xargs -I {} grep -oE 'yudiel|zxing-wasm|barcode-detector|webrtc-adapter' {} 2>/dev/null | sort -u)"</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend2 && bun run test --run` exits 0
    - `cd frontend2 && bun run lint:imports` exits 0
    - `cd frontend2 && bunx tsc --noEmit -p tsconfig.json` exits 0
    - `cd frontend2 && bun run build` exits 0
    - `cd frontend2 && bun run i18n:compile` exits 0 with no warnings
    - `ls frontend2/dist/assets/ | grep -c "scanner-"` outputs at least `1` (scanner chunk emitted)
    - Main entry chunk (typically `index-*.js`) contains NO strings matching `yudiel`, `zxing-wasm`, `barcode-detector`, or `webrtc-adapter` — verify with grep above; the command `ls dist/assets/index-*.js | head -n 1 | xargs -I {} grep -oE 'yudiel|zxing-wasm|barcode-detector|webrtc-adapter' {} 2>/dev/null | sort -u` must produce EMPTY output (zero lines)
    - Scanner chunk `scanner-*.js` contains at least one match for `yudiel` OR `barcode-detector` — verified as a containment positive
    - Scanner chunk gzipped size recorded in SUMMARY (target range 120-180 kB per RESEARCH.md; any value is acceptable as long as the containment check passes — the specific size is informational)
    - No new empty msgstr in ET catalog (cross-reference Task 1)
  </acceptance_criteria>
  <done>
    Full phase gate green: tests, lint:imports, tsc, build, i18n:compile all clean. Scanner chunk isolated; main chunk does not import scanner deps. Bundle budget honored. Phase 64 is shippable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lingui extract scanner -> .po files | Build-time tool; trust level = build toolchain |
| Rollup manualChunks -> chunk output | Build-time partitioning; integrity verified via grep containment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-32 | DoS | Bundle bloat pushes /scan main-chunk over budget for every route | mitigate | manualChunks + React.lazy split; this plan verifies via build artifact grep; if fails, phase is unshippable |
| T-64-33 | Information Disclosure | ET translation contains PII or leaks | accept | All translations are UI chrome strings (no user data); draft translations reviewed against the UI-SPEC canonical copy |
| T-64-34 | Spoofing | Mistranslated ET strings confuse users (security/UX boundary) | accept | Draft translations from UI-SPEC + consistent register; native-speaker review is a UAT item, not a phase blocker |

</threat_model>

<verification>
Full phase gate — every command must exit 0:
- `cd frontend2 && bun run test --run`
- `cd frontend2 && bun run lint:imports`
- `cd frontend2 && bunx tsc --noEmit -p tsconfig.json`
- `cd frontend2 && bun run build`
- `cd frontend2 && bun run i18n:compile`

Bundle containment — must produce empty output:
- `ls frontend2/dist/assets/index-*.js | head -n 1 | xargs -I {} grep -oE 'yudiel|zxing-wasm|barcode-detector|webrtc-adapter' {} | sort -u`

Bundle isolation — must produce at least one match:
- `ls frontend2/dist/assets/scanner-*.js | head -n 1 | xargs -I {} grep -oE 'yudiel|barcode-detector' {} | head -n 1`

Catalog completeness — every Phase 64 msgid present in both catalogs with non-empty ET msgstr.
</verification>

<success_criteria>
- All Phase 64 UI strings extracted into EN + ET catalogs; no EN-only strings ship
- i18n:compile is warning-free
- Bundle gate: scanner chunks isolated, main chunk does NOT import scanner deps (verified by grep on built artifact)
- Full phase gate (test / lint:imports / tsc / build / i18n:compile) exits 0 everywhere
- Phase 64 is shippable; SCAN-01..07 all have rendered paths + automated tests + ET translations
</success_criteria>

<output>
After completion, create `.planning/phases/64-scanner-foundation-scan-page/64-10-SUMMARY.md` recording:
1. Exit codes for each gate command (test / lint:imports / tsc / build / i18n:compile)
2. `dist/assets/` file listing including scanner-*.js raw + gzipped sizes
3. Main chunk containment grep result (empty = pass)
4. Count of new Phase 64 msgids landed in EN + ET
5. Count of remaining empty msgstr in ET (must equal pre-phase baseline)
6. Every manual-UAT row from VALIDATION.md that remains pending sign-off on a physical device (iOS haptic is NOT in that list because D-17 removed it; iOS first-beep audio, iOS permission-persist, Android torch hardware all remain manual)
7. The Phase 64 pass/fail verdict
</output>
