---
phase: 64
plan: 03
type: execute
wave: 1
depends_on: [64-01]
files_modified:
  - frontend2/src/lib/scanner/init-polyfill.ts
  - frontend2/src/lib/scanner/feedback.ts
  - frontend2/src/lib/scanner/scan-history.ts
  - frontend2/src/lib/scanner/types.ts
  - frontend2/src/lib/scanner/index.ts
  - frontend2/src/lib/scanner/__tests__/init-polyfill.test.ts
  - frontend2/src/lib/scanner/__tests__/feedback.test.ts
  - frontend2/src/lib/scanner/__tests__/scan-history.test.ts
autonomous: true
requirements: [SCAN-03, SCAN-06]
requirements_addressed: [SCAN-03, SCAN-06]
must_haves:
  truths:
    - "frontend2/src/lib/scanner/ is a 5-file port of frontend/lib/scanner/ with `use client` directives removed, entity imports stripped, and scan-lookup exports dropped (no lookup in Phase 64)"
    - "init-polyfill exports `initBarcodePolyfill()` and `isBarcodeDetectionAvailable()` with verbatim legacy behavior"
    - "feedback.ts keeps `navigator.vibrate` (D-17 LOCKED — no `ios-haptics` here) and adds a new `resumeAudioContext()` helper"
    - "scan-history.ts reads/writes localStorage key `hws-scan-history`, capped at 10, dedupe-to-top on `addToScanHistory`"
    - "types.ts exports `EntityMatch` as an inline stub (no Item/Container/Location import), `ScanHistoryEntry` interface, `SUPPORTED_FORMATS` const + `BarcodeFormat` type"
    - "barrel `@/lib/scanner` re-exports from types/init-polyfill/feedback/scan-history (no scan-lookup export)"
    - "unit tests exist for init-polyfill, feedback, scan-history covering the behaviors above"
  artifacts:
    - path: "frontend2/src/lib/scanner/init-polyfill.ts"
      provides: "Barcode Detection API availability check + dynamic polyfill loader"
      exports: ["initBarcodePolyfill", "isBarcodeDetectionAvailable"]
    - path: "frontend2/src/lib/scanner/feedback.ts"
      provides: "Module-singleton AudioContext, oscillator beep, navigator.vibrate haptic, resumeAudioContext for iOS gesture unlock"
      exports: ["initAudioContext", "resumeAudioContext", "playBeep", "playSuccessBeep", "triggerHaptic", "triggerScanFeedback"]
    - path: "frontend2/src/lib/scanner/scan-history.ts"
      provides: "localStorage-backed scan history module: getScanHistory, addToScanHistory (dedupe+cap), removeFromScanHistory, clearScanHistory, createHistoryEntry, formatScanTime"
      contains: "hws-scan-history"
    - path: "frontend2/src/lib/scanner/types.ts"
      provides: "ScanHistoryEntry, EntityMatch stub, SUPPORTED_FORMATS, BarcodeFormat"
      exports: ["ScanHistoryEntry", "EntityMatch", "SUPPORTED_FORMATS", "BarcodeFormat"]
    - path: "frontend2/src/lib/scanner/index.ts"
      provides: "Barrel re-exporting types, polyfill, feedback, scan-history (no scan-lookup)"
  key_links:
    - from: "frontend2/src/lib/scanner/scan-history.ts"
      to: "window.localStorage"
      via: "setItem/getItem with key hws-scan-history"
      pattern: "hws-scan-history"
    - from: "frontend2/src/lib/scanner/feedback.ts resumeAudioContext()"
      to: "module-scope AudioContext singleton"
      via: "ctx.resume() when state === 'suspended'"
      pattern: "resumeAudioContext"
---

<objective>
Port the five legacy `/frontend/lib/scanner/*` files into `/frontend2/src/lib/scanner/` as a 1:1 port with the targeted edits flagged by RESEARCH.md and PATTERNS.md (strip `"use client"`, remove Next-specific Item/Container/Location entity imports, drop `scan-lookup` exports, add a new `resumeAudioContext()` helper in feedback.ts for the iOS gesture-unlock pattern — D-08). Create Vitest unit tests for the three behavior-bearing modules (init-polyfill, feedback, scan-history). This plan owns the lowest layer of Phase 64 — every hook, component, and page in Waves 1-3 imports from this barrel.

Purpose: The legacy lib/scanner is production-audited code (ships in `/frontend` since v1.3). Porting verbatim preserves its correctness while the retro UI wrapping happens in Wave 2. The unit tests exist to satisfy Nyquist for SCAN-03 (feedback) + SCAN-06 (scan-history) at the module layer; hook-layer tests (Plan 05) and page-layer tests (Plan 09) stack on top.

Output: Five source files + three test files under `frontend2/src/lib/scanner/`; full Vitest suite still green; forbidden-imports lint still green (no `idb`/`serwist`/`offline`/`sync` introduced).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-scanner-foundation-scan-page/64-CONTEXT.md
@.planning/phases/64-scanner-foundation-scan-page/64-RESEARCH.md
@.planning/phases/64-scanner-foundation-scan-page/64-PATTERNS.md
@.planning/phases/64-scanner-foundation-scan-page/64-VALIDATION.md
@frontend/lib/scanner/init-polyfill.ts
@frontend/lib/scanner/feedback.ts
@frontend/lib/scanner/scan-history.ts
@frontend/lib/scanner/types.ts
@frontend/lib/scanner/index.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Port lib/scanner/{init-polyfill,types,index}.ts — the pure-module trio</name>
  <read_first>
    - frontend/lib/scanner/init-polyfill.ts (full file — source of truth for port)
    - frontend/lib/scanner/types.ts (full file — source of truth for port)
    - frontend/lib/scanner/index.ts (full file — source of truth for port)
    - .planning/phases/64-scanner-foundation-scan-page/64-PATTERNS.md §1 init-polyfill (lines 90-105), §4 types (lines 254-278), §5 index (lines 281-297), §Legacy Port Mapping (lines 931-971)
    - .planning/phases/64-scanner-foundation-scan-page/64-RESEARCH.md §Port: init-polyfill (lines 781-809), §Port: types.ts (lines 834-862)
  </read_first>
  <files>frontend2/src/lib/scanner/init-polyfill.ts, frontend2/src/lib/scanner/types.ts, frontend2/src/lib/scanner/index.ts</files>
  <action>
    Create all three files in the new `frontend2/src/lib/scanner/` directory. Port each from its legacy counterpart with EXACT edits per PATTERNS.md §Legacy Port Mapping.

    **init-polyfill.ts** — copy verbatim from `frontend/lib/scanner/init-polyfill.ts`, then:
    - Remove the `"use client";` directive (legacy line 12)
    - No other changes. The single `import("barcode-detector/polyfill")` on line ~35 stays bare-specifier; Vite resolves it identically.

    **types.ts** — copy verbatim from `frontend/lib/scanner/types.ts`, then:
    - DELETE the 3 `import type { ... } from "@/lib/types/..."` lines (legacy lines 7-9 — Item, Container, Location)
    - REPLACE the `EntityMatch` union with the inline stub:

      ```ts
      export type EntityMatch =
        | { type: "item"; entity: { id: string; name: string } }
        | { type: "container"; entity: { id: string; name: string } }
        | { type: "location"; entity: { id: string; name: string } }
        | { type: "not_found"; code: string };
      ```

    - KEEP verbatim: `ScanHistoryEntry` interface (shape: `{ code, format, entityType: "item"|"container"|"location"|"unknown", entityId?, entityName?, timestamp }`), `SUPPORTED_FORMATS = ["qr_code","ean_13","ean_8","upc_a","upc_e","code_128"] as const` (6-entry SUPERSET — SCAN-02 subset is selected at the `<Scanner>` prop site in Wave 2, not here — per RESEARCH.md §Discrepancies #7), `BarcodeFormat` = `(typeof SUPPORTED_FORMATS)[number]`.

    **index.ts** — copy verbatim from `frontend/lib/scanner/index.ts`, then:
    - DELETE the `export { lookupByShortCode, getEntityDisplayName, getEntityUrl } from "./scan-lookup";` block (legacy lines 42-47) — Phase 64 has no lookup.
    - KEEP the re-exports from `./types`, `./init-polyfill`, `./feedback`, `./scan-history`.
    - ADD `resumeAudioContext` to the feedback re-export list once Task 2 adds it. If this task runs before Task 2, stub the re-export with a TODO and restore it in Task 2's edit — OR (preferred) land Task 1 without re-exporting `resumeAudioContext`, then Task 2 ADDS the export to `./feedback` re-exports. Do NOT create import errors in index.ts. Safest: on first write of `index.ts`, list only the legacy surface (everything except `resumeAudioContext`), then Task 2 appends.

    All three files must live at `frontend2/src/lib/scanner/{init-polyfill,types,index}.ts`. Use existing `/frontend2` import style (no `@/` prefix needed for sibling module imports like `"./types"`).
  </action>
  <verify>
    <automated>cd frontend2 && test -f src/lib/scanner/init-polyfill.ts && test -f src/lib/scanner/types.ts && test -f src/lib/scanner/index.ts && ! grep -q '"use client"' src/lib/scanner/init-polyfill.ts && ! grep -q "@/lib/types/items" src/lib/scanner/types.ts && ! grep -q "scan-lookup" src/lib/scanner/index.ts && grep -q "initBarcodePolyfill" src/lib/scanner/init-polyfill.ts && grep -q "isBarcodeDetectionAvailable" src/lib/scanner/init-polyfill.ts && grep -q "export type EntityMatch" src/lib/scanner/types.ts && grep -q "SUPPORTED_FORMATS" src/lib/scanner/types.ts && grep -q "ScanHistoryEntry" src/lib/scanner/types.ts && bunx tsc --noEmit -p tsconfig.json 2>&1 | tee /tmp/tsc-64-03-t1.log | grep -vE "^$" | wc -l | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: `frontend2/src/lib/scanner/init-polyfill.ts`, `types.ts`, `index.ts`
    - `grep -c '"use client"' frontend2/src/lib/scanner/init-polyfill.ts` outputs `0`
    - `grep -c "export.*initBarcodePolyfill" frontend2/src/lib/scanner/init-polyfill.ts` outputs at least `1`
    - `grep -c "export.*isBarcodeDetectionAvailable" frontend2/src/lib/scanner/init-polyfill.ts` outputs at least `1`
    - `grep -c "barcode-detector/polyfill" frontend2/src/lib/scanner/init-polyfill.ts` outputs `1` (dynamic import preserved)
    - `grep -c "@/lib/types/items" frontend2/src/lib/scanner/types.ts` outputs `0` (entity imports dropped)
    - `grep -c "export type EntityMatch" frontend2/src/lib/scanner/types.ts` outputs `1`
    - `grep -c "type: \"item\"" frontend2/src/lib/scanner/types.ts` outputs at least `1` (inline stub for item branch)
    - `grep -c "type: \"not_found\"" frontend2/src/lib/scanner/types.ts` outputs `1`
    - `grep -c "SUPPORTED_FORMATS" frontend2/src/lib/scanner/types.ts` outputs at least `1`
    - `grep -c "qr_code" frontend2/src/lib/scanner/types.ts` outputs `1` (superset constant has 6 entries — verified by presence of qr_code)
    - `grep -c "scan-lookup" frontend2/src/lib/scanner/index.ts` outputs `0` (scan-lookup export block dropped)
    - `grep -c "from \"./types\"" frontend2/src/lib/scanner/index.ts` outputs at least `1` (types re-export preserved)
    - `cd frontend2 && bunx tsc --noEmit -p tsconfig.json` exits 0 (NOTE: index.ts may still re-export from feedback.ts + scan-history.ts which don't exist until Tasks 2/3 — acceptable if this task runs AFTER Tasks 2 and 3, OR the index.ts is authored conservatively to only re-export from already-landed files within this task and updated in Task 4. If it fails here, move index.ts write to Task 4 of this plan.)
  </acceptance_criteria>
  <done>
    Three pure-module files are ported with the exact edits spec'd. types.ts exposes the inline `EntityMatch` stub + `SUPPORTED_FORMATS` superset; init-polyfill has no `"use client"`; index.ts has no scan-lookup export.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port feedback.ts with resumeAudioContext() extension + unit tests</name>
  <read_first>
    - frontend/lib/scanner/feedback.ts (full file — source of truth; ~149 LOC)
    - .planning/phases/64-scanner-foundation-scan-page/64-PATTERNS.md §2 feedback.ts (lines 108-195) — highlights module-scope singleton, playBeep at 880Hz×100ms, navigator.vibrate, and the new resumeAudioContext helper
    - .planning/phases/64-scanner-foundation-scan-page/64-RESEARCH.md §Pattern 3 (lines 373-418) — resumeAudioContext contract, pointerdown rationale
    - .planning/phases/64-scanner-foundation-scan-page/64-CONTEXT.md D-17 (lines 63-64) — ios-haptics LOCKED OUT; use navigator.vibrate verbatim
    - .planning/phases/64-scanner-foundation-scan-page/64-VALIDATION.md SCAN-03 rows (lines 32-34)
    - frontend2/src/lib/api/__tests__/queryKeys.test.ts (lines 1-30 as cited in PATTERNS §Test Pattern Assignments Analog) — module-function unit test style
  </read_first>
  <behavior>
    - Test 1: `initAudioContext()` is idempotent — calling it twice only instantiates one AudioContext
    - Test 2: `initAudioContext()` tolerates SSR-like env (no `window`) by early-returning without throwing
    - Test 3: `resumeAudioContext()` calls `ctx.resume()` when state === "suspended"
    - Test 4: `resumeAudioContext()` is a no-op when state === "running"
    - Test 5: `playSuccessBeep()` creates an oscillator at 880 Hz (frequency set via oscillator.frequency.value = 880) and stops it ~100 ms after start
    - Test 6: `triggerHaptic()` calls `navigator.vibrate` when available; silently no-ops when not (D-17 — ios-haptics is NOT wired)
    - Test 7: `triggerScanFeedback()` fires both `playSuccessBeep` AND `triggerHaptic` (composition test)
  </behavior>
  <files>frontend2/src/lib/scanner/feedback.ts, frontend2/src/lib/scanner/__tests__/feedback.test.ts, frontend2/src/lib/scanner/index.ts</files>
  <action>
    **RED → GREEN** against the `<behavior>` block.

    Port `frontend/lib/scanner/feedback.ts` verbatim EXCEPT:
    1. Remove the `"use client";` directive on legacy line 12
    2. D-17 LOCK: keep `triggerHaptic` using `navigator.vibrate` only. Do NOT import or call anything from `ios-haptics`. Do NOT add `ios-haptics` anywhere.
    3. ADD a new exported helper at the bottom of the file:

       ```ts
       export function resumeAudioContext(): void {
         if (!audioContext) initAudioContext();
         if (audioContext?.state === "suspended") {
           audioContext.resume().catch(() => {
             /* swallow — iOS rejects when not in a user gesture; retry is a no-op */
           });
         }
       }
       ```

    The module-scope singleton variables (`audioContext`, `audioInitialized`) stay intact — `resumeAudioContext` reads them.

    Update `frontend2/src/lib/scanner/index.ts` (from Task 1) to ADD `resumeAudioContext` to the `./feedback` re-export list. If Task 1 wrote a conservative index.ts without the feedback re-exports, now is the time to add ALL feedback exports: `initAudioContext, resumeAudioContext, playBeep, playSuccessBeep, triggerHaptic, triggerScanFeedback`. Read the current index.ts first, then append.

    **Tests — follow the PATTERNS.md §Module unit tests analog.** Create `frontend2/src/lib/scanner/__tests__/feedback.test.ts`:

    - Use `vi.stubGlobal("AudioContext", ...)` to install a fake constructor returning `{ state, resume: vi.fn(), createOscillator: vi.fn(() => fakeOsc), createGain: vi.fn(() => fakeGain), currentTime: 0, destination: {} }`. Include a reset via `vi.unstubAllGlobals()` in `afterEach`.
    - Use `vi.stubGlobal("navigator", { vibrate: vi.fn() })` for haptic tests; unstub per test to assert the "no navigator.vibrate" branch.
    - Tests 5 (880 Hz): capture the returned oscillator mock; assert `oscillator.frequency.value === 880` after `playSuccessBeep()`; assert `oscillator.start` and `oscillator.stop` were both called and the delta (stopTime - startTime) ≈ 0.1 s (100 ms).
    - Test 3 (resume): create an AudioContext mock with `state: "suspended"` and `resume: vi.fn(() => Promise.resolve())`; call `resumeAudioContext()` — assert `resume` was called once.
    - Test 4 (no-op when running): `state: "running"` — assert `resume` was NOT called.

    Land tests as failing first (because the file does not yet exist); implement to pass.

    Do NOT import `ios-haptics` anywhere — acceptance_criteria will fail if you do.
  </action>
  <verify>
    <automated>cd frontend2 && test -f src/lib/scanner/feedback.ts && test -f src/lib/scanner/__tests__/feedback.test.ts && ! grep -q "ios-haptics" src/lib/scanner/feedback.ts && ! grep -q '"use client"' src/lib/scanner/feedback.ts && grep -q "export function resumeAudioContext" src/lib/scanner/feedback.ts && grep -q "navigator.vibrate" src/lib/scanner/feedback.ts && grep -q "resumeAudioContext" src/lib/scanner/index.ts && bun run test -- lib/scanner/__tests__/feedback --run</automated>
  </verify>
  <acceptance_criteria>
    - `frontend2/src/lib/scanner/feedback.ts` exists
    - `grep -c '"use client"' frontend2/src/lib/scanner/feedback.ts` outputs `0`
    - `grep -c "ios-haptics" frontend2/src/lib/scanner/feedback.ts` outputs `0` (D-17 compliance)
    - `grep -c "export function resumeAudioContext" frontend2/src/lib/scanner/feedback.ts` outputs `1`
    - `grep -c "navigator.vibrate" frontend2/src/lib/scanner/feedback.ts` outputs at least `1` (legacy haptic path preserved)
    - `grep -c "frequency.value = 880" frontend2/src/lib/scanner/feedback.ts` outputs at least `1` (880 Hz beep invariant)  (NOTE: if legacy uses `playBeep(880, 100, 0.25)` wrapping `oscillator.frequency.value = frequency`, a grep for `frequency.value = frequency` is also acceptable — drop the `880` in the greppable and match by `playBeep(880, 100, 0.25)` instead)
    - `grep -c "playSuccessBeep" frontend2/src/lib/scanner/feedback.ts` outputs at least `1`
    - `grep -c "triggerScanFeedback" frontend2/src/lib/scanner/feedback.ts` outputs at least `1`
    - `grep -c "resumeAudioContext" frontend2/src/lib/scanner/index.ts` outputs at least `1` (re-exported from barrel)
    - `cd frontend2 && bun run test -- lib/scanner/__tests__/feedback --run` exits 0; ≥ 7 test cases pass (one per behavior)
    - `cd frontend2 && bun run lint:imports` exits 0
  </acceptance_criteria>
  <done>
    feedback.ts ported verbatim + resumeAudioContext added; `navigator.vibrate` preserved per D-17; index.ts re-exports the new helper; 7 unit tests cover init/resume/beep/haptic.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Port scan-history.ts + unit tests (SCAN-06 coverage)</name>
  <read_first>
    - frontend/lib/scanner/scan-history.ts (full file — source of truth; ~196 LOC)
    - .planning/phases/64-scanner-foundation-scan-page/64-PATTERNS.md §3 scan-history (lines 199-252), §Legacy Port Mapping (lines 948-955)
    - .planning/phases/64-scanner-foundation-scan-page/64-RESEARCH.md §Port: scan-history.ts (lines 811-830)
    - .planning/phases/64-scanner-foundation-scan-page/64-VALIDATION.md SCAN-06 rows (lines 40-44)
    - .planning/phases/64-scanner-foundation-scan-page/64-CONTEXT.md D-03 (dedupe-to-top) + D-04 (hook is single API surface)
  </read_first>
  <behavior>
    - Test 1: `getScanHistory()` returns `[]` when localStorage has no `hws-scan-history` key
    - Test 2: `getScanHistory()` returns `[]` when the key holds non-array JSON (corrupt-JSON graceful fallback)
    - Test 3: `getScanHistory()` filters out entries missing `code` or `timestamp` (type-guard)
    - Test 4: `addToScanHistory({ code, format, entityType })` writes one entry with a `timestamp` close to `Date.now()`
    - Test 5: Adding the same `code` twice yields exactly ONE entry (dedupe) and moves it to position 0 with an updated timestamp (dedupe-to-top, D-03)
    - Test 6: Adding 11 distinct codes caps the list at 10 (oldest dropped — `MAX_HISTORY_SIZE = 10`)
    - Test 7: `removeFromScanHistory(code)` removes the entry with that code and leaves others intact
    - Test 8: `clearScanHistory()` empties the key (or removes it) so `getScanHistory()` returns `[]`
    - Test 9: `createHistoryEntry(code, format, match)` returns an object with the legacy shape `{ code, format, entityType, entityId?, entityName?, timestamp }` — for a `match.type === "not_found"` input, `entityType` is `"unknown"` and `entityId`/`entityName` are absent
    - Test 10: `formatScanTime(timestamp)` returns `"Just now"` for `Date.now()`, `"N min ago"` for ~N minutes ago, etc. (exact strings per legacy — copy literally from the legacy implementation)
  </behavior>
  <files>frontend2/src/lib/scanner/scan-history.ts, frontend2/src/lib/scanner/__tests__/scan-history.test.ts</files>
  <action>
    **RED → GREEN** against the `<behavior>` block.

    Port `frontend/lib/scanner/scan-history.ts` verbatim — there is NO `"use client"` directive in this file (PATTERNS.md §Legacy Port Mapping confirms). The file imports `ScanHistoryEntry` and `EntityMatch` from `./types`; keep as-is — both are exported by Task 1's types.ts.

    Keep ALL of the following intact, byte-for-byte:
    - Constants `SCAN_HISTORY_KEY = "hws-scan-history"` and `MAX_HISTORY_SIZE = 10`
    - `getScanHistory()` with array + code/timestamp type-guard
    - `addToScanHistory()` with the dedupe (`filter h.code !== entry.code`) + slice(0, 10) cap
    - `removeFromScanHistory(code)`
    - `clearScanHistory()`
    - `createHistoryEntry(code, format, match)` helper (even though Phase 64 always passes `match.type === "not_found"`, the shape is consumed by Phase 65 unchanged — see CONTEXT.md <specifics>)
    - `formatScanTime(timestamp)` relative-time formatter

    Do NOT touch the strings returned by `formatScanTime` — downstream tests + UI-SPEC timestamp copy depend on the exact legacy strings (e.g. `"Just now"`, `"N min ago"`).

    **Tests** — Create `frontend2/src/lib/scanner/__tests__/scan-history.test.ts`. Use `vi.stubGlobal("localStorage", fakeStorage)` where `fakeStorage` is a minimal `{ getItem, setItem, removeItem, clear }` backed by a `new Map()`; reset the map in `beforeEach`. For the 10-cap test, push 11 distinct codes and assert `getScanHistory().length === 10`. For the dedupe test, push `A`, `B`, `A` and assert `[A, B]` ordering with `A.timestamp > B.timestamp`. For the `formatScanTime` tests, use `vi.setSystemTime(new Date("2026-04-18T12:00:00Z"))` + `vi.useFakeTimers()` so the relative strings are deterministic.

    All 10 test cases must land in the `.test.ts` file; run `bun run test -- scan-history --run` and confirm pass. The module itself is a verbatim port, so the RED cycle is short — the tests fail only because the ported file does not yet exist; writing it (verbatim copy) immediately flips them GREEN.
  </action>
  <verify>
    <automated>cd frontend2 && test -f src/lib/scanner/scan-history.ts && test -f src/lib/scanner/__tests__/scan-history.test.ts && grep -q "hws-scan-history" src/lib/scanner/scan-history.ts && grep -q "MAX_HISTORY_SIZE = 10" src/lib/scanner/scan-history.ts && grep -q "addToScanHistory" src/lib/scanner/scan-history.ts && grep -q "clearScanHistory" src/lib/scanner/scan-history.ts && grep -q "formatScanTime" src/lib/scanner/scan-history.ts && grep -q "createHistoryEntry" src/lib/scanner/scan-history.ts && bun run test -- lib/scanner/__tests__/scan-history --run</automated>
  </verify>
  <acceptance_criteria>
    - `frontend2/src/lib/scanner/scan-history.ts` exists
    - `grep -c "hws-scan-history" frontend2/src/lib/scanner/scan-history.ts` outputs `1` (legacy localStorage key preserved)
    - `grep -c "MAX_HISTORY_SIZE = 10" frontend2/src/lib/scanner/scan-history.ts` outputs `1`
    - `grep -c "export function getScanHistory" frontend2/src/lib/scanner/scan-history.ts` outputs `1`
    - `grep -c "export function addToScanHistory" frontend2/src/lib/scanner/scan-history.ts` outputs `1`
    - `grep -c "export function removeFromScanHistory" frontend2/src/lib/scanner/scan-history.ts` outputs `1`
    - `grep -c "export function clearScanHistory" frontend2/src/lib/scanner/scan-history.ts` outputs `1`
    - `grep -c "export function createHistoryEntry" frontend2/src/lib/scanner/scan-history.ts` outputs `1`
    - `grep -c "export function formatScanTime" frontend2/src/lib/scanner/scan-history.ts` outputs `1`
    - `grep -c "slice(0, MAX_HISTORY_SIZE)" frontend2/src/lib/scanner/scan-history.ts` outputs at least `1` (cap preserved)
    - `grep -c "filter((h) => h.code !== entry.code)" frontend2/src/lib/scanner/scan-history.ts` outputs at least `1` (dedupe filter preserved — OR the legacy may use a slightly different whitespace; if so, grep for `h.code !== entry.code` as a more robust match)
    - `cd frontend2 && bun run test -- lib/scanner/__tests__/scan-history --run` exits 0; ≥ 10 test cases pass (one per behavior)
    - `cd frontend2 && bun run lint:imports` exits 0 (no `idb`/`serwist`/`offline`/`sync` introduced)
  </acceptance_criteria>
  <done>
    scan-history.ts ported verbatim; 10 unit tests cover SCAN-06 behaviors (dedupe-to-top, 10-cap, get/add/remove/clear/format).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Write init-polyfill unit tests + finalize index.ts barrel</name>
  <read_first>
    - frontend2/src/lib/scanner/init-polyfill.ts (from Task 1 — module under test)
    - frontend2/src/lib/scanner/index.ts (from Tasks 1-2 — barrel being finalized)
    - .planning/phases/64-scanner-foundation-scan-page/64-PATTERNS.md §Module unit tests (lines 1008-1033)
  </read_first>
  <behavior>
    - Test 1: `isBarcodeDetectionAvailable()` returns `true` when `"BarcodeDetector"` is in `window`
    - Test 2: `isBarcodeDetectionAvailable()` returns `false` when absent
    - Test 3: `initBarcodePolyfill()` early-returns without dynamic import when `"BarcodeDetector" in window` (native support — no polyfill needed)
    - Test 4: `initBarcodePolyfill()` is idempotent — second call short-circuits via the `polyfillLoaded` flag
    - Test 5: When native is absent, `initBarcodePolyfill()` awaits the dynamic `import("barcode-detector/polyfill")` (assert via a `vi.mock("barcode-detector/polyfill", ...)` whose factory returns an empty module object)
  </behavior>
  <files>frontend2/src/lib/scanner/__tests__/init-polyfill.test.ts, frontend2/src/lib/scanner/index.ts</files>
  <action>
    **Test authoring (RED → GREEN).** Create `frontend2/src/lib/scanner/__tests__/init-polyfill.test.ts`:

    - Use `vi.stubGlobal("window", { ... })` with/without a `BarcodeDetector` key across tests.
    - For Test 5, use `vi.mock("barcode-detector/polyfill", () => ({}))` at the top of the test file so the dynamic import resolves without touching the real polyfill.
    - Note: Module-scope `polyfillLoaded` in init-polyfill.ts persists between tests. Use `vi.resetModules()` in `beforeEach` and re-`import()` the module to get a fresh flag each test.

    **Barrel finalization.** Read the current `frontend2/src/lib/scanner/index.ts` (from Tasks 1-2). Confirm the final export surface is:

    ```ts
    // types
    export type {
      ScanHistoryEntry,
      EntityMatch,
      BarcodeFormat,
    } from "./types";
    export { SUPPORTED_FORMATS } from "./types";

    // init-polyfill
    export {
      initBarcodePolyfill,
      isBarcodeDetectionAvailable,
    } from "./init-polyfill";

    // feedback
    export {
      initAudioContext,
      resumeAudioContext,
      playBeep,
      playSuccessBeep,
      triggerHaptic,
      triggerScanFeedback,
    } from "./feedback";

    // scan-history
    export {
      getScanHistory,
      addToScanHistory,
      removeFromScanHistory,
      clearScanHistory,
      createHistoryEntry,
      formatScanTime,
    } from "./scan-history";
    ```

    Do NOT export anything from `./scan-lookup` (file does not exist in frontend2; lookup deferred to Phase 65). Update the file if any of these exports are missing.

    Run the full test suite to confirm no regression: `cd frontend2 && bun run test --run`.
  </action>
  <verify>
    <automated>cd frontend2 && test -f src/lib/scanner/__tests__/init-polyfill.test.ts && grep -q "resumeAudioContext" src/lib/scanner/index.ts && grep -q "getScanHistory" src/lib/scanner/index.ts && grep -q "SUPPORTED_FORMATS" src/lib/scanner/index.ts && ! grep -q "scan-lookup" src/lib/scanner/index.ts && bun run test -- lib/scanner --run && bun run lint:imports && bunx tsc --noEmit -p tsconfig.json 2>&1 | tee /tmp/tsc-64-03-t4.log | grep -vE "^$" | wc -l | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend2/src/lib/scanner/__tests__/init-polyfill.test.ts` exists; `bun run test -- init-polyfill --run` passes with ≥ 5 test cases
    - `grep -c "from \"./types\"" frontend2/src/lib/scanner/index.ts` outputs at least `1`
    - `grep -c "from \"./init-polyfill\"" frontend2/src/lib/scanner/index.ts` outputs at least `1`
    - `grep -c "from \"./feedback\"" frontend2/src/lib/scanner/index.ts` outputs at least `1`
    - `grep -c "from \"./scan-history\"" frontend2/src/lib/scanner/index.ts` outputs at least `1`
    - `grep -c "scan-lookup" frontend2/src/lib/scanner/index.ts` outputs `0`
    - `grep -c "resumeAudioContext" frontend2/src/lib/scanner/index.ts` outputs `1`
    - `grep -c "SUPPORTED_FORMATS" frontend2/src/lib/scanner/index.ts` outputs `1`
    - `cd frontend2 && bun run test -- lib/scanner --run` exits 0 (all lib/scanner tests pass)
    - `cd frontend2 && bun run test --run` exits 0 (full suite regression-clean)
    - `cd frontend2 && bunx tsc --noEmit -p tsconfig.json` exits 0
    - `cd frontend2 && bun run lint:imports` exits 0
  </acceptance_criteria>
  <done>
    init-polyfill has 5 unit tests; barrel exports exactly the Phase 64 surface (no scan-lookup); full Vitest suite green; TS clean; lint:imports clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| window.localStorage ↔ scan-history module | Untrusted local state (user may edit localStorage manually in DevTools); module type-guards on read |
| window ↔ init-polyfill dynamic import | Vite-resolved dynamic import from a trusted npm dep (polyfill ships from barcode-detector@3.0.8) |
| module-scope AudioContext singleton | Cross-component shared audio instance; gesture-unlock is security-irrelevant but UX-critical |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-08 | Tampering | User-edited localStorage `hws-scan-history` | mitigate | `getScanHistory()` type-guards every entry (`typeof entry.code === "string"` + `typeof entry.timestamp === "number"`); corrupt-JSON path falls through to `[]`. Scanned codes are not secrets (low-value target). |
| T-64-09 | Denial of Service | localStorage quota exhaustion via history growth | mitigate | Hard-cap at 10 entries (`MAX_HISTORY_SIZE = 10`). Even with 256-char codes + JSON overhead, worst case is <5 kB. |
| T-64-10 | Information Disclosure | Shared localStorage across users on same browser profile | accept | Scope-by-user deferred to v2.3+ per CONTEXT.md <deferred>. Home-warehouse-system is single-user-per-device context. |
| T-64-11 | Tampering | Dynamic import of `barcode-detector/polyfill` | mitigate | npm integrity hashes in bun.lock; transitive through @yudiel@2.5.1 (exact pin); monitored via GitHub Advisory DB |

</threat_model>

<verification>
- `cd frontend2 && bun run test -- lib/scanner --run` passes (≥ 22 tests across 3 files: init-polyfill ≥5, feedback ≥7, scan-history ≥10)
- `cd frontend2 && bun run test --run` full suite green
- `cd frontend2 && bunx tsc --noEmit -p tsconfig.json` clean
- `cd frontend2 && bun run lint:imports` clean (no `idb`/`serwist`/`offline`/`sync` introduced — verified since ported source uses only `localStorage`, `navigator`, `window`, `AudioContext`)
- `ls frontend2/src/lib/scanner/ | sort` lists exactly: `__tests__`, `feedback.ts`, `index.ts`, `init-polyfill.ts`, `scan-history.ts`, `types.ts`
- No `ios-haptics` import appears in any ported file (D-17)
</verification>

<success_criteria>
- 5 source files under `frontend2/src/lib/scanner/` (port-verbatim + 1 added helper `resumeAudioContext`)
- 3 test files under `frontend2/src/lib/scanner/__tests__/` providing ≥ 22 test cases total
- Barrel re-exports `SUPPORTED_FORMATS`, `BarcodeFormat`, `ScanHistoryEntry`, `EntityMatch`, + all behavior-bearing functions; scan-lookup is absent
- Full Vitest suite, tsc, and lint:imports are all green
- No `ios-haptics` anywhere (D-17)
</success_criteria>

<output>
After completion, create `.planning/phases/64-scanner-foundation-scan-page/64-03-SUMMARY.md` recording: exported surface of each module, total test count per file, and the exact `resumeAudioContext` implementation (the only addition beyond the verbatim port).
</output>
