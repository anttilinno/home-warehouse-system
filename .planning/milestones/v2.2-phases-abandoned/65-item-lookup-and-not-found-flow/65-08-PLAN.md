---
phase: 65-item-lookup-and-not-found-flow
plan: 08
type: execute
wave: 5
depends_on: [65-01, 65-02, 65-03, 65-04, 65-05, 65-06, 65-07]
files_modified:
  - frontend2/locales/en/messages.po
  - frontend2/locales/et/messages.po
  - frontend2/locales/en/messages.js
  - frontend2/locales/et/messages.js
  - .planning/phases/65-item-lookup-and-not-found-flow/65-BUNDLE-BASELINE.md
autonomous: false
requirements: [LOOK-01, LOOK-02, LOOK-03]
tags: [i18n, lingui, bundle-gate, blocking, et-gap-fill, release-gate]

must_haves:
  truths:
    - "Every new t`...` string added in Plans 65-05/06/07 is extracted into frontend2/locales/en/messages.po with a non-empty msgstr — including the new BRAND label added in Plan 65-05 Task 1 ItemForm and the BRAND copy re-used in UpcSuggestionBanner (D-23)"
    - "Every new msgid in en/messages.po has a corresponding non-empty Estonian translation in frontend2/locales/et/messages.po — zero orphan/untranslated warnings on compile"
    - "pnpm build produces a scanner chunk whose gzip size is ≤ the Phase 64 baseline captured in 65-BUNDLE-BASELINE.md (zero regression tolerance — Phase 64's −37.8 kB savings MUST be preserved)"
    - "pnpm build produces a main index chunk whose gzip size is ≤ (Phase 64 baseline main + 5120 bytes) — the ≤ 5 kB main-chunk delta budget from 65-CONTEXT `<code_context>`"
    - "The full Phase 65 test suite (test + lint:imports + typecheck + i18n:compile + build) exits 0 on the merged tree"
  artifacts:
    - path: "frontend2/locales/en/messages.po"
      provides: "EN catalog with every Phase 65 msgid and matching msgstr"
      contains: "SUGGESTIONS AVAILABLE"
    - path: "frontend2/locales/et/messages.po"
      provides: "ET catalog with every Phase 65 msgid translated"
      contains: "SUGGESTIONS AVAILABLE"
    - path: ".planning/phases/65-item-lookup-and-not-found-flow/65-BUNDLE-BASELINE.md"
      provides: "Updated with post-Phase-65 measurements + delta analysis"
      contains: "POST-PHASE-65"
  key_links:
    - from: "pnpm i18n:extract"
      to: "Every new t`...` call across Plans 65-05/06/07"
      via: "Lingui extractor scanning src/**"
      pattern: "t`SUGGESTIONS AVAILABLE`"
    - from: "pnpm build bundle gate"
      to: "65-BUNDLE-BASELINE.md Plan 65-01 snapshot"
      via: "gzip -c <file> | wc -c comparison"
      pattern: "scanner chunk gzip"
---

<objective>
Extract every new Phase 65 string via `pnpm i18n:extract`, fill the Estonian translations in-phase (per Phase 63/64 precedent), verify `pnpm i18n:compile` has zero warnings, and run the [BLOCKING] release gate: full-suite test + lint + typecheck + bundle comparison. Compare the post-Phase-65 `dist/assets/scanner-*.js.gz` and `index-*.js.gz` byte counts against the baseline captured in Plan 65-01's 65-BUNDLE-BASELINE.md — fail hard on any scanner-chunk regression and on any main-chunk delta > 5 kB.

Purpose: LOOK-01/02/03 acceptance is incomplete without i18n shipping in both EN + ET (per the v2.0 Lingui decision + the Phase 63/64 precedent of doing extract + gap-fill in-phase). The bundle gate is Pitfall #7 from 65-RESEARCH.md — it protects the 37.8 kB gzip savings Phase 64 shipped and caps the Phase 65 delta. This is the last wave; if the gate fails, Phase 65 is not shippable.

Output: Filled EN + ET catalogs + compiled JS artifacts; updated baseline doc with post-phase measurements + delta analysis; final verification output for the phase SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65-item-lookup-and-not-found-flow/65-CONTEXT.md
@.planning/phases/65-item-lookup-and-not-found-flow/65-UI-SPEC.md
@.planning/phases/65-item-lookup-and-not-found-flow/65-PATTERNS.md
@.planning/phases/65-item-lookup-and-not-found-flow/65-BUNDLE-BASELINE.md
@.planning/phases/65-item-lookup-and-not-found-flow/65-07-SUMMARY.md
@.planning/phases/64-scanner-foundation-scan-page/64-10-SUMMARY.md

<interfaces>
<!-- Tools + artifacts + baselines -->

Lingui scripts from frontend2/package.json (expected names; verify on Task 1 start):
- `pnpm i18n:extract` — scans src/**/*.{ts,tsx} for t`...` and msgid, merges into locales/{en,et}/messages.po
- `pnpm i18n:compile` — compiles .po to .js runtime artifacts; exits non-zero on orphan / missing translations (Phase 64 Plan 64-10 precedent)

Expected new msgids this phase (from UI-SPEC + Plan 65-05/06/07 wiring):
Page chrome / ItemFormPage (already-reused msgids in catalog — DO NOT duplicate):
- NEW ITEM, CREATE ITEM, WORKING…, CANCEL, DISCARD CHANGES?, Your edits will be lost., ← BACK, DISCARD (all from Phase 60/57)

ScanResultBanner 4 states (NEW):
- MATCHED
- NAME (may exist from ItemForm — deduplicate after extract)
- VIEW ITEM
- LOOKING UP…
- NOT FOUND
- `No item in this workspace matches this barcode.`
- CREATE ITEM WITH THIS BARCODE
- LOOKUP FAILED
- `Could not reach the server. Check your connection and retry, or create a new item with this barcode.`
- RETRY

UpcSuggestionBanner (NEW):
- SUGGESTIONS AVAILABLE
- BRAND (NOTE: Plan 65-05 Task 1 also adds `BRAND` as a first-class ItemForm RetroFormField label + `Optional` helper — per D-23. Lingui dedupes so the single `BRAND` msgid covers both render sites)
- [USE]
- USE ALL
- DISMISS
- `Category hint: {0} — pick manually below.` (ICU placeholder)

ItemForm BRAND field (NEW — D-23, Plan 65-05 Task 1):
- BRAND (dedupes with UpcSuggestionBanner — single entry)
- Optional (already exists from BARCODE field helper — dedupes)
- `e.g. DeWalt` (placeholder — NEW; confirm in extract diff)

Schema error message (updated in Plan 65-02):
- "Use letters, numbers, hyphens, or underscores only." — NOT lingui-wrapped (zod literal per existing convention); NOT extracted. Confirm by inspecting en/messages.po after extract — this string MUST NOT appear as an msgid.
- The OLD message "Use letters and numbers only." was removed in Plan 65-02; confirm it is NOT present as an orphan msgid in en/messages.po after extract.

Bundle baseline from Plan 65-01:
- Recorded in `.planning/phases/65-item-lookup-and-not-found-flow/65-BUNDLE-BASELINE.md`
- Scanner chunk gzip: <captured in Plan 65-01>
- Main index chunk gzip: <captured in Plan 65-01>
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ET translation quality | Developer/translator provides Estonian strings — no runtime check on translation fidelity, only on presence (non-empty msgstr) |
| Bundle measurement | Plan 65-01 baseline must be on the same git commit lineage as the new measurement — both use `gzip -c <file> | wc -c` on the raw .js |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-08-01 | Repudiation | shipped strings without ET fill | mitigate | `pnpm i18n:compile` exits non-zero on any msgid with empty msgstr (Phase 64 Plan 64-10 precedent). Manual ET fill by developer per Claude's Discretion; CI gate blocks merge |
| T-65-08-02 | Denial of service | scanner chunk regression breaks Phase 64 savings | mitigate | Task 3 hard-gate: scanner gzip post-phase MUST be ≤ baseline (zero tolerance). If regressed, investigate the import graph — most likely cause: useBarcodeEnrichment imported from features/scan/** instead of features/items/** |
| T-65-08-03 | Tampering | manual bundle measurement skew | mitigate | Task 3 uses the EXACT same command the Plan 65-01 baseline used: `gzip -c <file> | wc -c` on the raw .js (not on a vite-emitted .gz, not on file-system brotli). Same node/bun/pnpm lockfile (no lockfile change mid-phase) |
| T-65-08-04 | Elevation | orphan translations shipped | mitigate | Task 1 ends with `pnpm i18n:compile` passing zero-orphan-warning gate. Any orphan is fixed before the release gate in Task 3 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Run i18n:extract + fill EN catalog + compile to verify zero orphans</name>
  <files>
    frontend2/locales/en/messages.po,
    frontend2/locales/en/messages.js
  </files>
  <read_first>
    - frontend2/package.json (verify script names `i18n:extract` + `i18n:compile`; if repo uses `bun run` instead of `pnpm run`, adjust — infer from 64-10 SUMMARY)
    - frontend2/locales/en/messages.po (current catalog — DO NOT manually edit; extractor writes)
    - .planning/phases/64-scanner-foundation-scan-page/64-10-SUMMARY.md (precedent for extract + compile flow)
    - All Phase 65 production files that use `t`...``: frontend2/src/features/items/ItemFormPage.tsx + frontend2/src/features/items/UpcSuggestionBanner.tsx + frontend2/src/features/items/forms/ItemForm.tsx (D-23 brand field + placeholder — NEW) + frontend2/src/components/scan/ScanResultBanner.tsx (+ any incidental strings Plan 65-07 added in ScanPage.tsx — per 65-07 summary)
    - .planning/phases/65-item-lookup-and-not-found-flow/65-UI-SPEC.md §"Copywriting Contract" (reference deck for every string)
  </read_first>
  <action>
    STEP A — Run the extractor:
    ```bash
    cd frontend2 && pnpm i18n:extract
    ```
    (Use `bun run i18n:extract` if that is the script name — confirm against package.json.)

    The command updates `frontend2/locales/en/messages.po` + `frontend2/locales/et/messages.po` with any new msgids. EN gets the msgid as the default msgstr (English self-translation). ET gets the msgid as the default msgstr (marked as untranslated — Lingui tracks this).

    STEP B — Inspect the diff:
    ```bash
    git diff frontend2/locales/en/messages.po
    ```
    Expected new msgids (per UI-SPEC + Plan 65-05/06 outputs):
    - `MATCHED`, `NAME` (may dedupe), `VIEW ITEM`, `LOOKING UP…`, `NOT FOUND`, `No item in this workspace matches this barcode.`, `CREATE ITEM WITH THIS BARCODE`, `LOOKUP FAILED`, `Could not reach the server. Check your connection and retry, or create a new item with this barcode.`, `RETRY`
    - `SUGGESTIONS AVAILABLE`, `BRAND` (may appear once — dedupes ItemForm + UpcSuggestionBanner render sites), `[USE]`, `USE ALL`, `DISMISS`, `Category hint: {0} — pick manually below.`
    - Possibly new: `e.g. DeWalt` (ItemForm BRAND placeholder — Plan 65-05 Task 1)

    For EN: the auto-generated msgstr == msgid is the correct EN authoring (EN strings are authored inline). NO manual edits to EN unless a string is genuinely different from the msgid (Phase 65 doesn't have any such case).

    Also verify the zod-literal error message did NOT leak into the catalog: `rg "Use letters, numbers, hyphens, or underscores only\\." frontend2/locales/en/messages.po` MUST return 0 lines (Plan 65-02 kept this as a non-lingui-wrapped zod string per repo convention). If it appears, something wrapped the string in `t\`...\`` by accident — trace and revert before committing the extract.

    STEP C — Run the compiler to check for orphans (msgids in .po without non-empty msgstr, or dead msgids in .po without corresponding src references):
    ```bash
    cd frontend2 && pnpm i18n:compile
    ```
    Exit code MUST be 0 with ZERO orphan warnings on the EN catalog. If there are warnings:
    - Orphan msgid in .po (not in src anymore) → acceptable if Phase 65 removed an intermediate string; document in 65-08-SUMMARY
    - Missing translation in EN → should never happen (EN is self-authoring); if it does, manually add msgstr = msgid
    - If `"Use letters and numbers only."` is now an orphan (removed in Plan 65-02 D-24), let the extractor purge it (Lingui's default behavior) or remove it manually and document in SUMMARY

    Commit: `docs(65-08): extract Phase 65 i18n strings into EN catalog`
  </action>
  <verify>
    <automated>cd frontend2 && pnpm i18n:compile</automated>
    <automated>rg "^msgid \"SUGGESTIONS AVAILABLE\"" frontend2/locales/en/messages.po</automated>
    <automated>rg "^msgid \"CREATE ITEM WITH THIS BARCODE\"" frontend2/locales/en/messages.po</automated>
    <automated>rg "^msgid \"LOOKING UP…\"" frontend2/locales/en/messages.po</automated>
    <automated>rg "^msgid \"LOOKUP FAILED\"" frontend2/locales/en/messages.po</automated>
    <automated>rg "^msgid \"MATCHED\"" frontend2/locales/en/messages.po</automated>
    <automated>rg "^msgid \"BRAND\"" frontend2/locales/en/messages.po</automated>
  </verify>
  <acceptance_criteria>
    - `rg "^msgid \"SUGGESTIONS AVAILABLE\"" frontend2/locales/en/messages.po | wc -l` returns `1`
    - `rg "^msgid \"CREATE ITEM WITH THIS BARCODE\"" frontend2/locales/en/messages.po | wc -l` returns `1`
    - `rg "^msgid \"LOOKING UP…\"" frontend2/locales/en/messages.po | wc -l` returns `1`
    - `rg "^msgid \"LOOKUP FAILED\"" frontend2/locales/en/messages.po | wc -l` returns `1`
    - `rg "^msgid \"NOT FOUND\"" frontend2/locales/en/messages.po | wc -l` returns `1`
    - `rg "^msgid \"MATCHED\"" frontend2/locales/en/messages.po | wc -l` returns `1`
    - `rg "^msgid \"VIEW ITEM\"" frontend2/locales/en/messages.po | wc -l` returns `1`
    - `rg "^msgid \"USE ALL\"" frontend2/locales/en/messages.po | wc -l` returns `1`
    - `rg "^msgid \"DISMISS\"" frontend2/locales/en/messages.po | wc -l` returns `1`
    - `rg "^msgid \"BRAND\"" frontend2/locales/en/messages.po | wc -l` returns `1` (D-23 — single dedup'd entry covers ItemForm + UpcSuggestionBanner)
    - `rg "Use letters, numbers, hyphens, or underscores only\\." frontend2/locales/en/messages.po | wc -l` returns `0` (zod literal NOT extracted — confirms repo convention preserved)
    - `cd frontend2 && pnpm i18n:compile` exits 0 with zero orphan warnings
  </acceptance_criteria>
  <done>EN catalog updated with all Phase 65 msgids (including BRAND and any new ItemForm placeholders); zod-literal error strings confirmed absent; i18n compile passes zero-orphan gate; ET catalog has stub entries awaiting Task 2 fill.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Fill Estonian translations for every new Phase 65 msgid</name>
  <what-built>
    Every new Phase 65 msgid has an auto-inserted empty (or msgid-echo) msgstr in `frontend2/locales/et/messages.po`. The translator / developer must replace each with an Estonian translation, matching the retro industrial operator-console tone from UI-SPEC §"Copywriting Contract" (short uppercase labels, present-tense imperatives, no emoji, no exclamation marks).
  </what-built>
  <how-to-verify>
    1. Open `frontend2/locales/et/messages.po` in an editor.
    2. Search for each of these msgids and fill the adjacent msgstr with an Estonian translation. Suggested starting-point translations (adjust for ET tone consistency with existing Phase 64 catalog — 64-10-SUMMARY.md for precedent):

       | EN msgid | Suggested ET msgstr |
       |----------|---------------------|
       | `MATCHED` | `VASTE LEITUD` |
       | `NAME` | `NIMI` (may already be in catalog from ItemForm — verify; Lingui dedupes at compile time) |
       | `VIEW ITEM` | `VAATA ESET` |
       | `LOOKING UP…` | `OTSIN…` |
       | `NOT FOUND` | `EI LEITUD` |
       | `No item in this workspace matches this barcode.` | `Selle vöötkoodiga eset ei leitud selles tööruumis.` |
       | `CREATE ITEM WITH THIS BARCODE` | `LOO UUS ESE SELLE VÖÖTKOODIGA` |
       | `LOOKUP FAILED` | `OTSING EBAÕNNESTUS` |
       | `Could not reach the server. Check your connection and retry, or create a new item with this barcode.` | `Serverit ei õnnestunud tabada. Kontrolli ühendust ja proovi uuesti, või loo selle vöötkoodiga uus ese.` |
       | `RETRY` | `PROOVI UUESTI` |
       | `SUGGESTIONS AVAILABLE` | `SOOVITUSED SAADAVAL` |
       | `BRAND` | `BRÄND` |
       | `[USE]` | `[KASUTA]` |
       | `USE ALL` | `KASUTA KÕIK` |
       | `DISMISS` | `SULGE` |
       | `Category hint: {0} — pick manually below.` | `Kategooria vihje: {0} — vali all käsitsi.` |
       | `e.g. DeWalt` (if extracted — ItemForm BRAND placeholder) | `nt DeWalt` |

       Translations are developer-tunable — the table above is a STARTING POINT matching UI-SPEC tone; review against Phase 64's ET catalog conventions in `frontend2/locales/et/messages.po` before committing. If any EN msgid above ALREADY exists (e.g. `NAME` is reused from ItemForm), Lingui dedupes — do not add a second entry.

    3. Run `cd frontend2 && pnpm i18n:compile` — MUST exit 0 with zero warnings.
    4. Run `cd frontend2 && pnpm test:run` — every test MUST stay green (ET-specific test snapshots, if any, update cleanly).
    5. Visually verify in browser (set locale to ET): navigate to `/scan` → scan unknown code → banner reads `EI LEITUD` (or the committed translation). Navigate to `/items/new?barcode=5449000000996` → if upstream enrichment returns found:true the banner reads `SOOVITUSED SAADAVAL`. Also confirm the ItemForm now renders a `BRÄND` labeled field below BARCODE (D-23).
  </how-to-verify>
  <resume-signal>Type "et-fill-approved-65-08" after ET translations are committed and i18n:compile is green, or describe specific translations that need adjustment. (The grep-friendly signal keyword makes it easy to find in STATE.md / session logs later.)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Release gate — full test suite + bundle comparison against Plan 65-01 baseline</name>
  <files>
    .planning/phases/65-item-lookup-and-not-found-flow/65-BUNDLE-BASELINE.md
  </files>
  <read_first>
    - .planning/phases/65-item-lookup-and-not-found-flow/65-BUNDLE-BASELINE.md (Plan 65-01 output — baseline bytes + the exact build command used)
    - .planning/phases/65-item-lookup-and-not-found-flow/65-CONTEXT.md §"Bundle Budget" lines 149-150 + 195-198 (budget: ≤ 5 kB main delta, zero scanner regression)
    - .planning/phases/65-item-lookup-and-not-found-flow/65-07-SUMMARY.md (ScanPage.tsx delta line count — sanity check)
    - frontend2/vite.config.ts (manual-chunks definition — confirm `scanner` group hasn't changed)
  </read_first>
  <action>
    STEP A — Run the full release gate. This MUST use the SAME package manager as Plan 65-01 Task 1 (pnpm vs bun — whichever the repo established):

    ```bash
    cd frontend2 && pnpm test:run
    cd frontend2 && pnpm lint:imports
    cd frontend2 && pnpm typecheck
    cd frontend2 && pnpm i18n:compile
    cd frontend2 && pnpm build
    ```

    If any exits non-zero, STOP. Return to the appropriate upstream plan to fix the issue (do not ship with a broken gate).

    STEP B — After a successful `pnpm build`, measure the post-Phase-65 chunk sizes with the SAME method the baseline used:
    ```bash
    cd frontend2
    MAIN_JS=$(ls dist/assets/index-*.js | head -1)
    SCANNER_JS=$(ls dist/assets/scanner-*.js | head -1)
    echo "main raw: $(stat -c%s "$MAIN_JS")"
    echo "main gzip: $(gzip -c "$MAIN_JS" | wc -c)"
    echo "scanner raw: $(stat -c%s "$SCANNER_JS")"
    echo "scanner gzip: $(gzip -c "$SCANNER_JS" | wc -c)"
    ```

    STEP C — Compute deltas vs the baseline stored in 65-BUNDLE-BASELINE.md:
    - `scanner_delta_gzip = post.scanner_gzip − baseline.scanner_gzip`
    - `main_delta_gzip = post.main_gzip − baseline.main_gzip`

    Apply the gate:
    - **FAIL** if `scanner_delta_gzip > 0` (zero regression tolerance on the Phase 64 savings — CONTEXT.md line 150)
    - **FAIL** if `main_delta_gzip > 5120` (≤ 5 kB = 5 120 bytes budget — CONTEXT.md line 198)
    - **PASS** otherwise

    STEP D — Append the post-phase section to `.planning/phases/65-item-lookup-and-not-found-flow/65-BUNDLE-BASELINE.md`:
    ```markdown

    ---

    ## POST-PHASE-65 measurements

    **Captured:** <YYYY-MM-DD HH:MM:SS timezone>
    **Source commit:** <output of `git rev-parse HEAD` — post-65-07 or HEAD of phase branch>

    | Chunk | Pre bytes (gzip) | Post bytes (gzip) | Delta (gzip) | Gate |
    |-------|------------------|-------------------|--------------|------|
    | main | <baseline.main> | <post.main> | <delta> | <PASS/FAIL> (budget ≤ 5120) |
    | scanner | <baseline.scanner> | <post.scanner> | <delta> | <PASS/FAIL> (budget ≤ 0) |

    ## Gate result

    <PASS / FAIL>

    ## Notes

    <any observation — e.g. which new chunk absorbed the Phase 65 code, any unexpected growth that merits Phase 71 / 72 stabilization follow-up>
    ```

    If PASS: commit `chore(65-08): release gate PASS — bundle deltas within budget`.

    If FAIL: STOP, investigate. Most likely causes (in order of frequency, per Pitfall #7 from 65-RESEARCH.md):
    1. `useBarcodeEnrichment` accidentally imported from `features/scan/**` (pulls enrichment code into scanner chunk) — audit with `rg "useBarcodeEnrichment" frontend2/src/features/scan/`
    2. A new lazy import accidentally triggered re-bundling — check vite manual-chunks config
    3. A Phase 65 component imports a heavy dep transitively

    Fix and re-run the gate BEFORE committing the baseline update.

    STEP E — Commit final phase marker:
    ```bash
    git commit -m "chore(65-08): Phase 65 release gate complete — all LOOK-01..03 requirements met"
    ```
  </action>
  <verify>
    <automated>cd frontend2 && pnpm test:run</automated>
    <automated>cd frontend2 && pnpm lint:imports</automated>
    <automated>cd frontend2 && pnpm typecheck</automated>
    <automated>cd frontend2 && pnpm i18n:compile</automated>
    <automated>cd frontend2 && pnpm build</automated>
    <automated>rg "^## POST-PHASE-65 measurements" .planning/phases/65-item-lookup-and-not-found-flow/65-BUNDLE-BASELINE.md</automated>
    <automated>rg "^## Gate result" .planning/phases/65-item-lookup-and-not-found-flow/65-BUNDLE-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend2 && pnpm test:run` exits 0 — every test green (including Phase 64 regression surface, LOOK-01/02/03 assertions, and all it.todo conversions from Plans 65-01..07)
    - `cd frontend2 && pnpm lint:imports` exits 0 — no forbidden `idb` / `serwist` / `offline` / `sync` substrings introduced anywhere in frontend2/src/
    - `cd frontend2 && pnpm typecheck` exits 0
    - `cd frontend2 && pnpm i18n:compile` exits 0 with zero orphan warnings
    - `cd frontend2 && pnpm build` exits 0 (production build succeeds)
    - 65-BUNDLE-BASELINE.md now has a POST-PHASE-65 section with measured bytes + delta + PASS verdict
    - `rg "^## Gate result$" .planning/phases/65-item-lookup-and-not-found-flow/65-BUNDLE-BASELINE.md` matches AND the line after it reads `PASS` (case-insensitive)
    - scanner chunk gzip POST ≤ baseline scanner gzip PRE (Pitfall #7 — zero regression)
    - main chunk gzip POST ≤ baseline main gzip PRE + 5120 bytes (≤ 5 kB main delta budget)
  </acceptance_criteria>
  <done>Phase 65 release gate PASS. All three LOOK-0N requirements land with shipping EN + ET catalogs + compiled JS artifacts + no bundle regression + full test suite green.</done>
</task>

</tasks>

<verification>
- `cd frontend2 && pnpm test:run && pnpm lint:imports && pnpm typecheck && pnpm i18n:compile && pnpm build` exits 0 end-to-end
- 65-BUNDLE-BASELINE.md POST section shows `PASS` on both chunks
- EN catalog + ET catalog both contain every Phase 65 msgid with non-empty msgstr (including BRAND from D-23)
- compiled `locales/en/messages.js` + `locales/et/messages.js` regenerated (these are build artifacts that should be committed per 64-10 precedent — verify by checking git status)
</verification>

<success_criteria>
- Phase 65 is SHIPPABLE — LOOK-01, LOOK-02, LOOK-03 all functionally complete, internationalized EN + ET, tests green, bundle within budget
- Zero regression on Phase 64 bundle savings (scanner chunk ≤ baseline bytes)
- Zero regression on existing test suite (Phase 60 items, Phase 64 scanner, all green)
- All scaffolded it.todo entries from Plan 65-01 are converted to real assertions across Plans 65-02..07 (including the new D-23/D-24 schemas.test.ts cases)
- Ready for `/gsd-verify-work` → `/gsd-retrospect` → v2.2 Phase 66 start
</success_criteria>

<output>
After completion, create `.planning/phases/65-item-lookup-and-not-found-flow/65-08-SUMMARY.md` recording:
- Exact baseline gzip bytes + exact post-phase gzip bytes + deltas in bytes and percent
- Full list of new msgids extracted (count + names — for Phase 71 stabilization docs reference); confirm `BRAND` appears exactly once (ItemForm + UpcSuggestionBanner dedup'd)
- Any orphan msgid removed during extract (expected: the old `"Use letters and numbers only."` zod-literal from pre-D-24 — if it was ever lingui-wrapped, which it was not in the baseline; likely zero orphans this phase)
- The final `pnpm test:run` pass/fail count (e.g. "612 passed / 612 total")
- Confirmation of ET catalog completeness (every Phase 65 msgid has non-empty msgstr)
- The ET-fill resume signal used (`et-fill-approved-65-08`) — for cross-reference in STATE.md session history
</output>
