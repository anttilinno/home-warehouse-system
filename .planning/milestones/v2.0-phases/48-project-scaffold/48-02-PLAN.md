---
phase: 48-project-scaffold
plan: 02
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - frontend2/src/App.tsx
  - frontend2/src/routes/index.tsx
  - frontend2/src/lib/i18n.ts
  - frontend2/locales/en/messages.po
  - frontend2/locales/et/messages.po
autonomous: false
requirements:
  - SCAF-02
  - SCAF-04

must_haves:
  truths:
    - "Navigating between / and /settings routes works without full page reloads"
    - "Navigating to a nonexistent route shows a 404 page with SECTOR NOT FOUND heading"
    - "A test string wrapped in Lingui t macro renders in English by default"
    - "Switching locale to Estonian renders the test string in Estonian"
  artifacts:
    - path: "frontend2/src/routes/index.tsx"
      provides: "Route definitions with placeholder pages"
      contains: "Routes"
    - path: "frontend2/src/lib/i18n.ts"
      provides: "Lingui i18n instance and locale loader"
      contains: "loadCatalog"
    - path: "frontend2/src/App.tsx"
      provides: "Root component with BrowserRouter and I18nProvider"
      contains: "I18nProvider"
    - path: "frontend2/locales/en/messages.po"
      provides: "English message catalog"
    - path: "frontend2/locales/et/messages.po"
      provides: "Estonian message catalog"
  key_links:
    - from: "frontend2/src/App.tsx"
      to: "frontend2/src/routes/index.tsx"
      via: "import and render AppRoutes"
      pattern: "AppRoutes"
    - from: "frontend2/src/App.tsx"
      to: "frontend2/src/lib/i18n.ts"
      via: "I18nProvider with i18n instance"
      pattern: "I18nProvider"
    - from: "frontend2/src/lib/i18n.ts"
      to: "frontend2/locales/*/messages.po"
      via: "dynamic import for catalog loading"
      pattern: "import.*locales"
---

<objective>
Add React Router v7 routing with placeholder pages and Lingui v5 i18n with EN + ET locale support, proving the full scaffold works end-to-end.

Purpose: Complete the scaffold by wiring routing and i18n -- the two remaining success criteria (client-side navigation and locale switching).
Output: Working placeholder pages with retro styling, client-side navigation, and locale switching.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-project-scaffold/48-CONTEXT.md
@.planning/phases/48-project-scaffold/48-RESEARCH.md
@.planning/phases/48-project-scaffold/48-UI-SPEC.md
@.planning/phases/48-project-scaffold/48-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 artifacts that this plan consumes -->

From frontend2/src/styles/globals.css (Tailwind retro tokens):
- bg-retro-charcoal, bg-retro-cream, text-retro-ink, border-retro-ink
- border-retro-thick, shadow-retro-raised
- bg-hazard-stripe utility class
- p-lg (24px), p-md (16px), mt-sm (8px), mb-md (16px)
- font-mono for monospace text

From frontend2/vite.config.ts:
- @lingui/swc-plugin configured in SWC plugins
- @lingui/vite-plugin configured as lingui()
- Path alias: "@" -> "./src"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add React Router v7 routes and Lingui i18n provider</name>
  <files>
    frontend2/src/routes/index.tsx,
    frontend2/src/lib/i18n.ts,
    frontend2/src/App.tsx
  </files>
  <read_first>
    .planning/phases/48-project-scaffold/48-RESEARCH.md,
    .planning/phases/48-project-scaffold/48-UI-SPEC.md,
    frontend2/src/App.tsx,
    frontend2/vite.config.ts,
    frontend2/lingui.config.ts
  </read_first>
  <action>
**Step 1: Create src/lib/i18n.ts** -- the Lingui i18n instance and dynamic catalog loader:

```typescript
import { i18n } from "@lingui/core";

export const locales = {
  en: "English",
  et: "Eesti",
} as const;

export type Locale = keyof typeof locales;

export const defaultLocale: Locale = "en";

export async function loadCatalog(locale: string) {
  const { messages } = await import(`../locales/${locale}/messages.po`);
  i18n.load(locale, messages);
  i18n.activate(locale);
}

export { i18n };
```

**Step 2: Create src/routes/index.tsx** -- route definitions with 3 placeholder pages.

Import from `react-router` (NOT `react-router-dom` -- per RESEARCH.md Pitfall 4, v7 consolidated into `react-router`).

Define these routes:
- `/` -- Dashboard placeholder page
- `/settings` -- Settings placeholder page
- `*` -- 404 Not Found page

Each placeholder page renders using the retro panel layout from the UI-SPEC Placeholder Page Visual Contract:
- Charcoal background, cream panel with 3px ink border, hazard stripe bar (8px tall), heading (20px bold uppercase), body text, retro-raised shadow, max-w-[640px] centered.

Specific copy per UI-SPEC Copywriting Contract:
- Dashboard (`/`): heading "DASHBOARD", body "Inventory HUD loading..."
- Settings (`/settings`): heading "SETTINGS", body "Configuration panels standing by."
- 404 (`*`): heading "SECTOR NOT FOUND", body "The requested area does not exist. Return to base.", with a link labeled "RETURN TO BASE" that navigates to `/`.

The Dashboard page must include:
1. The i18n test string using the Lingui `useLingui` macro from `@lingui/react/macro`: wrap "Welcome to Home Warehouse" in a `t` template literal tag
2. A locale switcher `<select>` element that calls `loadCatalog` on change, showing all locale options from the `locales` object, with a label "LANGUAGE" (uppercase, bold, per D-06)

Include a simple navigation section on each page (except 404) with `<Link>` elements to `/` and `/settings` so client-side navigation can be tested.

```tsx
// Route structure
import { Routes, Route, Link } from "react-router";

export function AppRoutes() {
  return (
    <Routes>
      <Route path="/" element={<DashboardPage />} />
      <Route path="/settings" element={<SettingsPage />} />
      <Route path="*" element={<NotFoundPage />} />
    </Routes>
  );
}
```

**Step 3: Update src/App.tsx** to wrap routing and i18n:

```tsx
import { useEffect, useState } from "react";
import { BrowserRouter } from "react-router";
import { I18nProvider } from "@lingui/react";
import { i18n, loadCatalog, defaultLocale } from "@/lib/i18n";
import { AppRoutes } from "@/routes";

export default function App() {
  const [ready, setReady] = useState(false);

  useEffect(() => {
    loadCatalog(defaultLocale).then(() => setReady(true));
  }, []);

  if (!ready) return null;

  return (
    <I18nProvider i18n={i18n}>
      <BrowserRouter>
        <AppRoutes />
      </BrowserRouter>
    </I18nProvider>
  );
}
```

The app loads the default locale catalog before rendering (avoids flash of untranslated content).
  </action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system/frontend2 && bun run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend2/src/lib/i18n.ts contains "loadCatalog" and "i18n.load" and "i18n.activate" and "defaultLocale" and 'en: "English"' and 'et: "Eesti"'
    - frontend2/src/routes/index.tsx contains "Routes" and "Route" and "Link" (all from "react-router", NOT "react-router-dom")
    - frontend2/src/routes/index.tsx contains "DASHBOARD" and "SETTINGS" and "SECTOR NOT FOUND" and "RETURN TO BASE"
    - frontend2/src/routes/index.tsx contains "bg-retro-charcoal" and "bg-retro-cream" and "border-retro-thick" and "bg-hazard-stripe"
    - frontend2/src/routes/index.tsx contains "useLingui" imported from "@lingui/react/macro"
    - frontend2/src/routes/index.tsx contains "loadCatalog" for locale switching
    - frontend2/src/App.tsx contains "I18nProvider" and "BrowserRouter" and "AppRoutes" and "loadCatalog"
    - frontend2/src/App.tsx does NOT contain "react-router-dom"
    - `bun run build` in frontend2/ exits 0
  </acceptance_criteria>
  <done>React Router v7 routes defined with 3 placeholder pages (dashboard, settings, 404), all using retro design tokens, with client-side navigation links and Lingui i18n provider wrapping the app.</done>
</task>

<task type="auto">
  <name>Task 2: Extract and compile Lingui message catalogs for EN and ET</name>
  <files>
    frontend2/locales/en/messages.po,
    frontend2/locales/et/messages.po
  </files>
  <read_first>
    frontend2/src/routes/index.tsx,
    frontend2/lingui.config.ts,
    .planning/phases/48-project-scaffold/48-UI-SPEC.md
  </read_first>
  <action>
**Step 1: Run Lingui extract** to generate PO catalog files from source:
```bash
cd /home/antti/Repos/Misc/home-warehouse-system/frontend2
bun run i18n:extract
```

This scans `src/` for `t` macro usage and creates/updates `locales/en/messages.po` and `locales/et/messages.po`.

**Step 2: Edit locales/et/messages.po** -- add Estonian translations for extracted strings.

Per UI-SPEC Copywriting Contract, the i18n test string translation is:
- EN: "Welcome to Home Warehouse"
- ET: "Tere tulemast Home Warehouse'i"

Open the generated `locales/et/messages.po` file. For each `msgid`, add the corresponding `msgstr`:
- `msgid "Welcome to Home Warehouse"` -> `msgstr "Tere tulemast Home Warehouse'i"`

Any other extracted strings (like "LANGUAGE" label if it was wrapped in `t`) should also get Estonian translations. Use reasonable Estonian translations:
- "LANGUAGE" -> "KEEL"

**Step 3: Run Lingui compile** to generate optimized JS catalogs:
```bash
bun run i18n:compile
```

This compiles the PO files into importable modules that the Vite plugin can load dynamically.

**Step 4: Verify the full flow** -- run `bun run build` to confirm that:
1. TypeScript compiles without errors
2. Vite bundles with Lingui-compiled catalogs
3. Tailwind processes the globals.css with retro tokens
  </action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system/frontend2 && bun run i18n:extract 2>&1 && bun run i18n:compile 2>&1 && bun run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend2/locales/en/messages.po exists and contains "Welcome to Home Warehouse"
    - frontend2/locales/et/messages.po exists and contains "Tere tulemast Home Warehouse'i"
    - `bun run i18n:extract` exits 0
    - `bun run i18n:compile` exits 0
    - `bun run build` exits 0
  </acceptance_criteria>
  <done>Lingui message catalogs extracted from source, Estonian translations added, catalogs compiled. Full build pipeline (TypeScript + Tailwind + Lingui + Vite) passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify scaffold in browser</name>
  <files>none</files>
  <action>Start the Vite dev server with `cd frontend2 && bun run dev` and present the verification checklist to the user for browser-based visual verification of retro design tokens, client-side routing, and locale switching.</action>
  <what-built>Complete Phase 48 scaffold: Vite + React 19 project with React Router v7 routing (3 placeholder pages), Tailwind CSS 4 retro design tokens (cream panels, charcoal background, hazard stripes, thick borders), and Lingui i18n (EN/ET locale switching).</what-built>
  <how-to-verify>
    1. Start the dev server: `cd frontend2 && bun run dev`
    2. Open http://localhost:5173 in browser
    3. Verify: Charcoal background with cream panel, hazard stripe bar at top, "DASHBOARD" heading in uppercase, "Inventory HUD loading..." body text, thick dark border, beveled shadow
    4. Verify: "Welcome to Home Warehouse" text is visible (i18n test string)
    5. Verify: Locale switcher dropdown shows "English" and "Eesti"
    6. Switch locale to "Eesti" -- verify text changes to "Tere tulemast Home Warehouse'i"
    7. Click the "Settings" navigation link -- verify URL changes to /settings without page reload, heading shows "SETTINGS"
    8. Navigate to http://localhost:5173/nonexistent -- verify "SECTOR NOT FOUND" 404 page with "RETURN TO BASE" link
    9. Click "RETURN TO BASE" -- verify navigation back to / without page reload
    10. Verify: IBM Plex Mono font loads (check any monospace-styled text in dev tools, or Network tab shows Google Fonts request)
  </how-to-verify>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system/frontend2 && bun run build 2>&1 | tail -3</automated>
  </verify>
  <done>User has visually confirmed: retro design tokens render correctly (cream panel, charcoal background, hazard stripes, thick borders, beveled shadows), client-side navigation works between / and /settings without page reload, 404 page renders for unknown routes, and locale switching changes displayed language from English to Estonian.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Vite dev server | Local dev only, no production exposure |
| Lingui catalog import | Dynamic import of PO files compiled to JS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-04 | Tampering | Lingui PO files | accept | PO files are source-controlled, compiled at build time. No runtime injection risk. |
| T-48-05 | Information Disclosure | React Router client routes | accept | Placeholder pages only, no sensitive data. Auth is Phase 49. |
</threat_model>

<verification>
1. `bun run build` in frontend2/ exits 0
2. `bun run i18n:extract` finds and extracts t-macro strings
3. `bun run i18n:compile` compiles PO catalogs without errors
4. Browser: navigating between / and /settings works without page reload
5. Browser: locale switcher changes displayed language from EN to ET
6. Browser: 404 page renders for unknown routes
</verification>

<success_criteria>
- React Router v7 (library mode) provides client-side navigation between at least 2 routes plus 404
- Lingui i18n test string renders in English by default and Estonian when locale is switched
- All placeholder pages use retro design tokens (cream panel, charcoal background, hazard stripes, thick borders)
- Full build pipeline (tsc + Tailwind + Lingui compile + Vite build) passes
</success_criteria>

<output>
After completion, create `.planning/phases/48-project-scaffold/48-02-SUMMARY.md`
</output>
