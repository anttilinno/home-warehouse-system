---
phase: 50-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend2/src/components/retro/RetroButton.tsx
  - frontend2/src/components/retro/RetroPanel.tsx
  - frontend2/src/components/retro/RetroInput.tsx
  - frontend2/src/components/retro/HazardStripe.tsx
  - frontend2/src/components/retro/index.ts
  - frontend2/src/components/retro/__tests__/RetroButton.test.tsx
  - frontend2/src/components/retro/__tests__/RetroPanel.test.tsx
  - frontend2/src/components/retro/__tests__/RetroInput.test.tsx
  - frontend2/src/components/retro/__tests__/HazardStripe.test.tsx
autonomous: true
requirements:
  - DS-01
  - DS-02
  - DS-03
  - DS-10

must_haves:
  truths:
    - "RetroButton renders with thick 3px ink border, raised shadow, and accepts variant prop (neutral/primary/danger)"
    - "RetroPanel renders with cream background, optional hazard stripe, optional close button with aria-label='Close'"
    - "RetroInput renders with monospace font, icon prefix slot, and error message display"
    - "HazardStripe renders the yellow-black diagonal stripe pattern at configurable height"
    - "All four components forward refs and merge custom className props"
  artifacts:
    - path: "frontend2/src/components/retro/RetroButton.tsx"
      provides: "Button component with neutral/primary/danger variants"
      exports: ["RetroButton"]
    - path: "frontend2/src/components/retro/RetroPanel.tsx"
      provides: "Panel component with optional hazard stripe and close button"
      exports: ["RetroPanel"]
    - path: "frontend2/src/components/retro/RetroInput.tsx"
      provides: "Input component with icon prefix and error state"
      exports: ["RetroInput"]
    - path: "frontend2/src/components/retro/HazardStripe.tsx"
      provides: "Decorative hazard stripe divider"
      exports: ["HazardStripe"]
    - path: "frontend2/src/components/retro/index.ts"
      provides: "Barrel export for all retro components"
  key_links:
    - from: "frontend2/src/components/retro/RetroPanel.tsx"
      to: "frontend2/src/components/retro/HazardStripe.tsx"
      via: "import for optional hazard stripe header"
      pattern: "import.*HazardStripe"
---

<objective>
Build the four core retro UI primitives extracted from existing auth page patterns: RetroButton (DS-01), RetroPanel (DS-02), RetroInput (DS-03), and HazardStripe (DS-10). These are the foundation components that all other components and pages build upon.

Purpose: Establish the reusable design system primitives so auth pages can be refactored and future phases (layout, dashboard, settings) have a consistent component vocabulary.
Output: Four component files, barrel export, and unit tests in `frontend2/src/components/retro/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-design-system/50-CONTEXT.md
@.planning/phases/50-design-system/50-RESEARCH.md
@.planning/phases/50-design-system/50-UI-SPEC.md
@frontend2/src/styles/globals.css
@frontend2/src/features/auth/LoginForm.tsx
@frontend2/src/features/auth/AuthPage.tsx
@frontend2/src/test-utils.tsx

<interfaces>
<!-- Design tokens from globals.css (source of truth for all styling) -->
Colors: retro-cream (#F5F0E1), retro-charcoal (#2A2A2A), retro-ink (#1A1A1A), retro-amber (#D4A017), retro-red (#CC3333), retro-green (#4A7C4A), retro-blue (#3366AA), retro-gray (#8B8B8B)
Borders: border-retro-thick (3px), border-retro-extra-thick (4px)
Shadows: shadow-retro-raised (2px 2px 0px #1A1A1A), shadow-retro-pressed (inset 2px 2px 0px rgba(0,0,0,0.3))
Spacing: xs(4px), sm(8px), md(16px), lg(24px), xl(32px), 2xl(48px)
Fonts: font-sans (system-ui), font-mono (IBM Plex Mono / JetBrains Mono)
Utility: .bg-hazard-stripe (repeating diagonal yellow/black gradient)

<!-- Existing inline patterns to extract (verified in auth page source) -->
Button pattern (LoginForm.tsx lines 150-161):
  h-[44px], border-retro-thick border-retro-ink, text-[14px] font-bold uppercase
  Normal: bg-retro-cream text-retro-ink shadow-retro-raised hover:bg-retro-amber active:shadow-retro-pressed
  Disabled: bg-retro-gray text-retro-gray cursor-not-allowed

Input pattern (LoginForm.tsx lines 84-92):
  h-[40px], border-retro-thick border-retro-ink, bg-retro-cream, font-mono text-[14px]
  pl-[40px] (icon prefix slot), outline-2 outline-offset-2 outline-transparent focus:outline-retro-amber

Panel pattern (AuthPage.tsx line 53):
  bg-retro-cream border-retro-thick border-retro-ink shadow-retro-raised p-lg
  Close button: absolute top-sm right-sm w-[24px] h-[24px] bg-retro-red border-retro-thick border-retro-ink text-white

Tab classes (AuthPage.tsx lines 24-27):
  tabBase: min-w-[120px] h-[36px] text-[14px] font-bold uppercase border-retro-thick border-retro-ink
  active: bg-retro-cream border-b-0
  inactive: bg-retro-gray

<!-- Test utilities available -->
From frontend2/src/test-utils.tsx:
  renderWithRouter(ui, options?) - renders with MemoryRouter
  createRouterWrapper(initialEntries?) - creates wrapper component
  @testing-library/jest-dom matchers available (imported in setup)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build RetroButton, HazardStripe components with tests</name>
  <files>
    frontend2/src/components/retro/RetroButton.tsx,
    frontend2/src/components/retro/HazardStripe.tsx,
    frontend2/src/components/retro/__tests__/RetroButton.test.tsx,
    frontend2/src/components/retro/__tests__/HazardStripe.test.tsx
  </files>
  <read_first>
    frontend2/src/styles/globals.css,
    frontend2/src/features/auth/LoginForm.tsx,
    frontend2/src/test-utils.tsx,
    .planning/phases/50-design-system/50-UI-SPEC.md
  </read_first>
  <behavior>
    RetroButton:
    - Renders a button element with text-[14px] font-bold uppercase
    - Default variant "neutral": bg-retro-cream text-retro-ink shadow-retro-raised, hover:bg-retro-amber active:shadow-retro-pressed
    - Variant "primary": bg-retro-amber text-retro-ink, hover:brightness-110 active:shadow-retro-pressed
    - Variant "danger": bg-retro-red text-white, hover:brightness-110 active:shadow-retro-pressed
    - Disabled state: bg-retro-gray cursor-not-allowed shadow-none
    - focus-visible: outline-2 outline-offset-2 outline-retro-amber
    - Height: h-[44px], padding: px-md, border: border-retro-thick border-retro-ink
    - Forwards ref to underlying button element
    - Merges custom className with base classes
    - Spreads remaining ButtonHTMLAttributes onto the button element

    HazardStripe:
    - Renders a div with bg-hazard-stripe class
    - Default height 8px (h-[8px])
    - Accepts height prop as number (in px), applies as style={{ height }}
    - Accepts className prop for additional styling
    - Full width (w-full)
  </behavior>
  <action>
    Create `frontend2/src/components/retro/__tests__/` directory.

    **RetroButton.tsx** — Create using `forwardRef<HTMLButtonElement, RetroButtonProps>`:
    - Interface: `interface RetroButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> { variant?: "primary" | "danger" | "neutral" }`
    - Variant class map: `{ primary: "bg-retro-amber text-retro-ink hover:brightness-110", danger: "bg-retro-red text-white hover:brightness-110", neutral: "bg-retro-cream text-retro-ink hover:bg-retro-amber" }[variant]`
    - Base classes: `h-[44px] px-md border-retro-thick border-retro-ink text-[14px] font-bold uppercase outline-2 outline-offset-2 outline-transparent focus-visible:outline-retro-amber`
    - Active (non-disabled): `shadow-retro-raised active:shadow-retro-pressed cursor-pointer`
    - Disabled classes: `disabled:bg-retro-gray disabled:cursor-not-allowed disabled:shadow-none disabled:text-retro-cream`
    - Compose: `${baseClasses} ${props.disabled ? '' : activeClasses} ${variantClass} ${className}`
    - Spread `...props` onto `<button>`, forward `ref`, render `{children}`
    - Add `RetroButton.displayName = "RetroButton"`

    **HazardStripe.tsx** — Simple presentational component:
    - Props: `{ height?: number; className?: string }`
    - Render: `<div className={"bg-hazard-stripe w-full " + (className || "")} style={height ? { height } : { height: 8 }} />`
    - Export as named export (no forwardRef needed for a decorative element)

    **Tests** — Use `@testing-library/react` `render` and `screen`:
    - RetroButton.test.tsx: test renders with default variant, renders each variant, disabled state applies correct classes, forwards ref, merges custom className, spreads HTML attributes (e.g. type="submit")
    - HazardStripe.test.tsx: test renders with bg-hazard-stripe class, default height 8px, custom height prop applies via style
  </action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system/frontend2 && bunx vitest run src/components/retro/__tests__/RetroButton.test.tsx src/components/retro/__tests__/HazardStripe.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - RetroButton.tsx exports `RetroButton` via named export
    - RetroButton.tsx contains `forwardRef<HTMLButtonElement`
    - RetroButton.tsx contains `variant?: "primary" | "danger" | "neutral"`
    - RetroButton.tsx contains `h-[44px]` and `border-retro-thick` and `shadow-retro-raised` and `active:shadow-retro-pressed`
    - RetroButton.tsx contains `disabled:bg-retro-gray` and `disabled:cursor-not-allowed`
    - RetroButton.tsx contains `focus-visible:outline-retro-amber`
    - HazardStripe.tsx exports `HazardStripe` via named export
    - HazardStripe.tsx contains `bg-hazard-stripe`
    - All test files pass: `bunx vitest run src/components/retro/__tests__/RetroButton.test.tsx src/components/retro/__tests__/HazardStripe.test.tsx` exits 0
  </acceptance_criteria>
  <done>RetroButton renders all 3 variants with correct state handling; HazardStripe renders diagonal stripe pattern at configurable height; all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build RetroPanel, RetroInput components with tests and barrel export</name>
  <files>
    frontend2/src/components/retro/RetroPanel.tsx,
    frontend2/src/components/retro/RetroInput.tsx,
    frontend2/src/components/retro/index.ts,
    frontend2/src/components/retro/__tests__/RetroPanel.test.tsx,
    frontend2/src/components/retro/__tests__/RetroInput.test.tsx
  </files>
  <read_first>
    frontend2/src/styles/globals.css,
    frontend2/src/features/auth/AuthPage.tsx,
    frontend2/src/features/auth/LoginForm.tsx,
    frontend2/src/components/retro/HazardStripe.tsx,
    frontend2/src/components/retro/RetroButton.tsx,
    frontend2/src/test-utils.tsx,
    .planning/phases/50-design-system/50-UI-SPEC.md
  </read_first>
  <behavior>
    RetroPanel:
    - Renders a div with bg-retro-cream border-retro-thick border-retro-ink shadow-retro-raised p-lg relative
    - Optional title prop: renders h2 with text-[20px] font-bold uppercase text-retro-ink
    - Optional showHazardStripe prop (boolean): renders HazardStripe component at top with mb-md
    - Optional showClose prop (boolean) + onClose callback: renders 24x24 red X close button at absolute top-sm right-sm with aria-label="Close"
    - Close button: w-[24px] h-[24px] bg-retro-red border-retro-thick border-retro-ink flex items-center justify-center text-white text-[12px] font-bold leading-none
    - Forwards ref and merges className
    - Renders children

    RetroInput:
    - Renders a wrapper div containing optional icon prefix and native input element
    - Input: h-[40px] border-retro-thick border-retro-ink bg-retro-cream font-mono text-[14px] text-retro-ink placeholder:text-retro-gray
    - Icon prefix: 40px wide slot on left, icon centered vertically, input gets pl-[40px]; when no icon, input gets pl-sm
    - Icon wrapper includes aria-hidden="true"
    - Focus: outline-2 outline-offset-2 outline-transparent focus:outline-retro-amber
    - Error state: border changes to border-retro-red, error message renders below as p with text-retro-red text-[12px] mt-xs
    - Disabled: bg-retro-gray cursor-not-allowed
    - Forwards ref to the native input element
    - Spreads InputHTMLAttributes onto native input
  </behavior>
  <action>
    **RetroPanel.tsx** — Create using `forwardRef<HTMLDivElement, RetroPanelProps>`:
    - Interface: `interface RetroPanelProps { title?: string; showClose?: boolean; onClose?: () => void; showHazardStripe?: boolean; className?: string; children: ReactNode }`
    - Import `HazardStripe` from `./HazardStripe`
    - Base classes on outer div: `bg-retro-cream border-retro-thick border-retro-ink shadow-retro-raised p-lg relative ${className || ""}`
    - If showClose: render `<button type="button" onClick={onClose} aria-label="Close" className="absolute top-sm right-sm w-[24px] h-[24px] bg-retro-red border-retro-thick border-retro-ink flex items-center justify-center text-white text-[12px] font-bold leading-none cursor-pointer hover:brightness-110">X</button>`
    - If showHazardStripe: render `<HazardStripe className="mb-md" />`
    - If title: render `<h2 className="text-[20px] font-bold uppercase text-retro-ink mb-md">{title}</h2>`
    - Render `{children}`
    - Add `RetroPanel.displayName = "RetroPanel"`

    **RetroInput.tsx** — Create using `forwardRef<HTMLInputElement, RetroInputProps>`:
    - Interface: `interface RetroInputProps extends InputHTMLAttributes<HTMLInputElement> { icon?: ReactNode; error?: string }`
    - Wrapper div: `relative` class
    - If icon: render `<span className="absolute left-sm top-1/2 -translate-y-1/2 pointer-events-none" aria-hidden="true">{icon}</span>`
    - Input classes: `w-full h-[40px] border-retro-thick ${error ? 'border-retro-red' : 'border-retro-ink'} bg-retro-cream font-mono text-[14px] text-retro-ink placeholder:text-retro-gray ${icon ? 'pl-[40px]' : 'pl-sm'} pr-sm outline-2 outline-offset-2 outline-transparent focus:outline-retro-amber disabled:bg-retro-gray disabled:cursor-not-allowed ${className || ""}`
    - Spread `...rest` (excluding icon, error, className) onto input, forward ref
    - If error: render `<p className="text-retro-red text-[12px] mt-xs">{error}</p>`
    - Add `RetroInput.displayName = "RetroInput"`

    **index.ts** — Create barrel export:
    ```
    export { RetroButton } from "./RetroButton";
    export { RetroPanel } from "./RetroPanel";
    export { RetroInput } from "./RetroInput";
    export { HazardStripe } from "./HazardStripe";
    ```

    **Tests:**
    - RetroPanel.test.tsx: renders children, renders hazard stripe when showHazardStripe=true, renders close button with aria-label="Close" when showClose=true, close button calls onClose on click, renders title as h2, forwards ref, merges className
    - RetroInput.test.tsx: renders input element, renders icon prefix with aria-hidden="true", applies pl-[40px] when icon present and pl-sm when absent, shows error message when error prop set, applies border-retro-red on error, disabled state applies bg-retro-gray, forwards ref, spreads HTML attributes (placeholder, type, required)
  </action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system/frontend2 && bunx vitest run src/components/retro/__tests__/RetroPanel.test.tsx src/components/retro/__tests__/RetroInput.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - RetroPanel.tsx exports `RetroPanel` via named export
    - RetroPanel.tsx contains `forwardRef<HTMLDivElement`
    - RetroPanel.tsx contains `aria-label="Close"` and `bg-retro-red`
    - RetroPanel.tsx imports `HazardStripe` from `./HazardStripe`
    - RetroInput.tsx exports `RetroInput` via named export
    - RetroInput.tsx contains `forwardRef<HTMLInputElement`
    - RetroInput.tsx contains `font-mono` and `pl-[40px]` and `border-retro-red`
    - RetroInput.tsx contains `aria-hidden="true"` on icon wrapper
    - RetroInput.tsx contains `text-retro-red text-[12px]` for error message
    - index.ts exports RetroButton, RetroPanel, RetroInput, HazardStripe
    - All test files pass: `bunx vitest run src/components/retro/__tests__/` exits 0
  </acceptance_criteria>
  <done>RetroPanel renders with optional hazard stripe and close button; RetroInput handles icon prefix and error states; barrel export exposes all 4 components; all tests green</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** 1
**Threats identified:**
- RetroInput must not suppress native HTML validation attributes (type, required, pattern, maxLength) -- mitigated by spreading all InputHTMLAttributes onto native input element
- Close buttons must be keyboard-accessible -- mitigated by using native `<button>` elements with aria-label="Close"
**Residual risk:** Low -- these are pure presentational UI components with no data processing, authentication, or network calls
</threat_model>

<verification>
All component unit tests pass: `cd frontend2 && bunx vitest run src/components/retro/__tests__/`
TypeScript compiles: `cd frontend2 && bunx tsc --noEmit`
</verification>

<success_criteria>
- Four retro components (RetroButton, RetroPanel, RetroInput, HazardStripe) are exported from `components/retro/index.ts`
- RetroButton has 3 visual variants (neutral, primary, danger) and all interaction states (hover, active, disabled, focus-visible)
- RetroPanel supports optional hazard stripe, close button with aria-label, and title
- RetroInput supports icon prefix with aria-hidden, error display, and disabled state
- HazardStripe renders the yellow-black diagonal pattern at configurable height
- All components forward refs and accept className
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/50-design-system/50-01-SUMMARY.md`
</output>
