---
phase: 50-design-system
plan: 04
type: execute
wave: 4
depends_on: ["50-03"]
files_modified:
  - frontend2/src/pages/DemoPage.tsx
  - frontend2/src/routes/index.tsx
  - frontend2/src/features/auth/AuthPage.tsx
  - frontend2/src/features/auth/LoginForm.tsx
  - frontend2/src/features/auth/RegisterForm.tsx
  - frontend2/src/features/auth/OAuthButtons.tsx
  - frontend2/src/App.tsx
autonomous: false
requirements:
  - DS-01
  - DS-02
  - DS-03
  - DS-04
  - DS-05
  - DS-06
  - DS-07
  - DS-08
  - DS-09
  - DS-10

must_haves:
  truths:
    - "/demo page is accessible without authentication (public route, per D-03)"
    - "/demo page showcases all 10 retro components with interactive states (per D-02)"
    - "Auth pages (AuthPage, LoginForm, RegisterForm, OAuthButtons) use design system components instead of inline retro classes (per D-01)"
    - "Demo page uses exact copywriting contract text from UI-SPEC"
    - "ToastProvider wraps the app so useToast() is available on demo page and everywhere"
  artifacts:
    - path: "frontend2/src/pages/DemoPage.tsx"
      provides: "Interactive living style guide with all 10 components"
      exports: ["DemoPage"]
    - path: "frontend2/src/routes/index.tsx"
      provides: "Public /demo route"
      contains: "DemoPage"
    - path: "frontend2/src/App.tsx"
      provides: "ToastProvider wrapping the app"
      contains: "ToastProvider"
  key_links:
    - from: "frontend2/src/pages/DemoPage.tsx"
      to: "frontend2/src/components/retro/index.ts"
      via: "imports all 10 components + useToast"
      pattern: "import.*from.*@/components/retro"
    - from: "frontend2/src/features/auth/LoginForm.tsx"
      to: "frontend2/src/components/retro/index.ts"
      via: "imports RetroButton, RetroInput"
      pattern: "import.*RetroButton.*RetroInput.*from.*@/components/retro"
    - from: "frontend2/src/features/auth/AuthPage.tsx"
      to: "frontend2/src/components/retro/index.ts"
      via: "imports RetroTabs, RetroPanel"
      pattern: "import.*from.*@/components/retro"
---

<objective>
Build the interactive `/demo` living style guide page showcasing all 10 retro components, refactor auth pages to consume the design system (per D-01), and wire ToastProvider into the app.

Purpose: Validate all components work in real usage (demo page), eliminate inline retro patterns from auth pages (D-01), and make toast system available app-wide.
Output: DemoPage component, updated routes, refactored auth files, ToastProvider in App.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-design-system/50-CONTEXT.md
@.planning/phases/50-design-system/50-UI-SPEC.md
@frontend2/src/routes/index.tsx
@frontend2/src/features/auth/AuthPage.tsx
@frontend2/src/features/auth/LoginForm.tsx
@frontend2/src/features/auth/RegisterForm.tsx
@frontend2/src/features/auth/OAuthButtons.tsx
@frontend2/src/App.tsx
@.planning/phases/50-design-system/50-01-SUMMARY.md
@.planning/phases/50-design-system/50-02-SUMMARY.md
@.planning/phases/50-design-system/50-03-SUMMARY.md

<interfaces>
<!-- Full retro component barrel (all 10 + toast) -->
From frontend2/src/components/retro/index.ts:
  export { RetroButton } from "./RetroButton";
  export { RetroPanel } from "./RetroPanel";
  export { RetroInput } from "./RetroInput";
  export { HazardStripe } from "./HazardStripe";
  export { RetroCard } from "./RetroCard";
  export { RetroDialog } from "./RetroDialog";
  export type { RetroDialogHandle } from "./RetroDialog";
  export { RetroTable } from "./RetroTable";
  export { RetroTabs } from "./RetroTabs";
  export { RetroBadge } from "./RetroBadge";
  export { ToastProvider, useToast } from "./RetroToast";

<!-- Component prop signatures (summary) -->
RetroButton: variant?: "primary"|"danger"|"neutral", extends ButtonHTMLAttributes
RetroPanel: title?: string, showClose?: boolean, onClose?: () => void, showHazardStripe?: boolean, className?, children
RetroInput: icon?: ReactNode, error?: string, extends InputHTMLAttributes
RetroCard: className?, children
RetroDialog: onClose?: () => void, children (imperative: ref.open(), ref.close())
RetroTable: columns: {key,header,className?}[], data: Record<string,ReactNode>[], className?
RetroTabs: tabs: {key,label}[], activeTab: string, onTabChange: (key) => void, className?
RetroBadge: variant?: "neutral"|"success"|"danger"|"warning"|"info", className?, children
HazardStripe: height?: number, className?
ToastProvider: children (wraps app)
useToast(): { addToast(message, variant?) }

<!-- Copywriting contract from UI-SPEC (exact copy to use) -->
Page heading: "RETRO COMPONENT LIBRARY"
Subheading: "SYSTEM STATUS: ALL COMPONENTS OPERATIONAL"
Section labels: "RETROBUTTON", "RETROPANEL", etc.
Toast examples: Success: "OPERATION COMPLETE" / Error: "SYSTEM ERROR DETECTED" / Info: "NEW TRANSMISSION RECEIVED"
Dialog title: "CONFIRM ACTION"
Dialog body: "Are you sure you want to proceed with this operation?"
Dialog confirm: "CONFIRM" (primary button)
Dialog cancel: "ABORT" (neutral button)
Button labels: "EXECUTE", "ABORT MISSION", "STANDBY"
Badge labels: "ACTIVE", "OFFLINE", "WARNING", "CRITICAL", "NOMINAL"
Input placeholder: "ENTER DESIGNATION..."
Input error demo: "INVALID INPUT DETECTED"
Table empty: "NO DATA LOADED"

<!-- Auth refactor mapping from UI-SPEC -->
AuthPage.tsx tabs (lines 24-27, 33-49) -> <RetroTabs>
AuthPage.tsx panel (line 53) -> <RetroPanel showClose showHazardStripe>
AuthPage.tsx close button (lines 55-61) -> Built into RetroPanel
AuthPage.tsx hazard stripe (line 67) -> Built into RetroPanel
LoginForm/RegisterForm inputs -> <RetroInput icon={...}>
LoginForm/RegisterForm submit buttons -> <RetroButton variant="neutral">
OAuthButtons buttons -> <RetroButton variant="neutral">
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build interactive DemoPage, add /demo route, wire ToastProvider</name>
  <files>
    frontend2/src/pages/DemoPage.tsx,
    frontend2/src/routes/index.tsx,
    frontend2/src/App.tsx
  </files>
  <read_first>
    frontend2/src/routes/index.tsx,
    frontend2/src/App.tsx,
    frontend2/src/components/retro/index.ts,
    frontend2/src/components/retro/RetroButton.tsx,
    frontend2/src/components/retro/RetroPanel.tsx,
    frontend2/src/components/retro/RetroInput.tsx,
    frontend2/src/components/retro/RetroCard.tsx,
    frontend2/src/components/retro/RetroDialog.tsx,
    frontend2/src/components/retro/RetroTable.tsx,
    frontend2/src/components/retro/RetroTabs.tsx,
    frontend2/src/components/retro/RetroToast.tsx,
    frontend2/src/components/retro/RetroBadge.tsx,
    frontend2/src/components/retro/HazardStripe.tsx,
    .planning/phases/50-design-system/50-UI-SPEC.md,
    .planning/phases/50-design-system/50-CONTEXT.md
  </read_first>
  <action>
    **DemoPage.tsx** — Create at `frontend2/src/pages/DemoPage.tsx`:

    Import all components from `@/components/retro`: RetroButton, RetroPanel, RetroInput, RetroCard, RetroDialog, RetroDialogHandle, RetroTable, RetroTabs, RetroBadge, HazardStripe, useToast, ToastProvider.
    Import `useState, useRef` from react.

    The demo page is FULLY INTERACTIVE per D-02. Structure:

    Full page: `min-h-dvh bg-retro-charcoal p-xl`
    Inner container: `max-w-[960px] mx-auto`

    **Page header:**
    - `<h1 className="text-[28px] font-bold uppercase text-retro-cream mb-xs">RETRO COMPONENT LIBRARY</h1>`
    - `<p className="text-retro-green font-mono text-[14px] mb-2xl">SYSTEM STATUS: ALL COMPONENTS OPERATIONAL</p>`

    **Section template pattern:** Each section has:
    - `<h2 className="text-[20px] font-bold uppercase text-retro-cream mb-sm">{COMPONENT_NAME}</h2>`
    - `<HazardStripe className="mb-md" />`
    - Component examples on a RetroCard or inline

    **Sections (in this order per UI-SPEC primary focal point rule):**

    1. **RETROBUTTON** (primary focal point, first section):
       - Row of 3 buttons: `<RetroButton variant="neutral">EXECUTE</RetroButton>`, `<RetroButton variant="primary">ABORT MISSION</RetroButton>`, `<RetroButton variant="danger">STANDBY</RetroButton>` (note: "STANDBY" is the danger demo label per copywriting contract -- visually demonstrates the variant, label is thematic)
       - Row of disabled: `<RetroButton disabled>DISABLED</RetroButton>`
       - Wrap in flex gap-md flex-wrap

    2. **RETROPANEL:**
       - `<RetroPanel title="SYSTEM PANEL" showHazardStripe showClose onClose={() => {}}>` with paragraph content
       - Panel without hazard stripe: `<RetroPanel>` with simple content

    3. **RETROINPUT:**
       - Normal input: `<RetroInput placeholder="ENTER DESIGNATION..." />` with an envelope icon (reuse the SVG from LoginForm)
       - Error state input: `<RetroInput placeholder="ENTER DESIGNATION..." error="INVALID INPUT DETECTED" />`
       - Disabled input: `<RetroInput placeholder="DISABLED" disabled />`
       - Use local state for live typing demonstration

    4. **RETROCARD:**
       - `<RetroCard>` with sample content paragraphs

    5. **RETRODIALOG:**
       - Button to trigger dialog: `<RetroButton onClick={() => dialogRef.current?.open()}>OPEN DIALOG</RetroButton>`
       - `<RetroDialog ref={dialogRef}>` containing:
         - `<h3 className="text-[20px] font-bold uppercase text-retro-ink mb-md">CONFIRM ACTION</h3>`
         - `<p className="text-[14px] text-retro-ink mb-lg">Are you sure you want to proceed with this operation?</p>`
         - Two buttons in flex row: `<RetroButton variant="primary" onClick={() => dialogRef.current?.close()}>CONFIRM</RetroButton>` and `<RetroButton onClick={() => dialogRef.current?.close()}>ABORT</RetroButton>`

    6. **RETROTABLE:**
       - Columns: [{key:"id",header:"ID"},{key:"name",header:"DESIGNATION"},{key:"status",header:"STATUS"},{key:"sector",header:"SECTOR"}]
       - Data: 3 rows of sample inventory data (e.g., {id:"001",name:"WRENCH SET",status:<RetroBadge variant="success">ACTIVE</RetroBadge>,sector:"A-7"})
       - Also show empty table note below: "NO DATA LOADED" in retro-gray text

    7. **RETROTABS:**
       - Local state for activeTab
       - tabs: [{key:"alpha",label:"ALPHA"},{key:"bravo",label:"BRAVO"},{key:"charlie",label:"CHARLIE"}]
       - Below tabs, show a RetroPanel with text indicating which tab is active

    8. **RETROTOAST:**
       - Three buttons to fire toasts: `<RetroButton variant="primary" onClick={() => addToast("OPERATION COMPLETE", "success")}>SUCCESS TOAST</RetroButton>`, error toast ("SYSTEM ERROR DETECTED"), info toast ("NEW TRANSMISSION RECEIVED")
       - The DemoPage must be wrapped in its own `<ToastProvider>` so toasts work on the demo page independently

    9. **RETROBADGE:**
       - Row of 5 badges: `<RetroBadge variant="success">ACTIVE</RetroBadge>`, `<RetroBadge variant="neutral">OFFLINE</RetroBadge>`, `<RetroBadge variant="warning">WARNING</RetroBadge>`, `<RetroBadge variant="danger">CRITICAL</RetroBadge>`, `<RetroBadge variant="info">NOMINAL</RetroBadge>`
       - Wrap in flex gap-sm flex-wrap

    10. **HAZARDSTRIPE:**
        - Default height: `<HazardStripe />`
        - Custom height: `<HazardStripe height={16} />`
        - Large: `<HazardStripe height={32} />`

    Section spacing: `mb-2xl` between each section.

    NOTE: Wrap the entire DemoPage content in a local `<ToastProvider>` so demo toasts work. The app-level ToastProvider in App.tsx is for real app toasts; the demo has its own instance.

    **routes/index.tsx** — Add the `/demo` route:
    - Import `DemoPage` from `@/pages/DemoPage`
    - Add route: `<Route path="/demo" element={<DemoPage />} />` (public, NO RequireAuth wrapper, per D-03)
    - Also replace the inline `RetroPanel` component in routes/index.tsx with the formal import from `@/components/retro`: `import { RetroPanel, HazardStripe } from "@/components/retro"`. The existing inline RetroPanel function (lines 28-37) should be replaced. Update DashboardPage, SettingsPage, NotFoundPage to use the imported RetroPanel with `showHazardStripe` prop instead of manually rendering the hazard stripe div.

    **App.tsx** — Wrap with ToastProvider:
    - Import `{ ToastProvider }` from `@/components/retro`
    - Add `<ToastProvider>` wrapping inside `<AuthProvider>` (but outside `<AppRoutes>`):
      ```
      <AuthProvider>
        <ToastProvider>
          <AppRoutes />
        </ToastProvider>
      </AuthProvider>
      ```
  </action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system/frontend2 && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - DemoPage.tsx exists at frontend2/src/pages/DemoPage.tsx
    - DemoPage.tsx imports from `@/components/retro` (all 10 components + useToast)
    - DemoPage.tsx contains exact copywriting strings: "RETRO COMPONENT LIBRARY", "SYSTEM STATUS: ALL COMPONENTS OPERATIONAL", "EXECUTE", "ABORT MISSION", "STANDBY", "OPERATION COMPLETE", "SYSTEM ERROR DETECTED", "NEW TRANSMISSION RECEIVED", "CONFIRM ACTION", "Are you sure you want to proceed with this operation?", "CONFIRM", "ABORT", "ENTER DESIGNATION...", "INVALID INPUT DETECTED", "ACTIVE", "OFFLINE", "WARNING", "CRITICAL", "NOMINAL"
    - DemoPage.tsx contains `dialogRef.current?.open()` (interactive dialog per D-02)
    - DemoPage.tsx contains `addToast` calls (interactive toasts per D-02)
    - DemoPage.tsx contains `useState` for tabs and input (interactive per D-02)
    - routes/index.tsx contains `<Route path="/demo"` without `RequireAuth` wrapper (per D-03)
    - routes/index.tsx does NOT contain the old inline RetroPanel function definition
    - routes/index.tsx imports RetroPanel from `@/components/retro`
    - App.tsx contains `ToastProvider` wrapping AppRoutes
    - Build succeeds: `bun run build` exits 0
  </acceptance_criteria>
  <done>Demo page at /demo showcases all 10 components interactively; inline RetroPanel replaced with formal component; ToastProvider wraps the app</done>
</task>

<task type="auto">
  <name>Task 2: Refactor auth pages to use design system components (D-01)</name>
  <files>
    frontend2/src/features/auth/AuthPage.tsx,
    frontend2/src/features/auth/LoginForm.tsx,
    frontend2/src/features/auth/RegisterForm.tsx,
    frontend2/src/features/auth/OAuthButtons.tsx
  </files>
  <read_first>
    frontend2/src/features/auth/AuthPage.tsx,
    frontend2/src/features/auth/LoginForm.tsx,
    frontend2/src/features/auth/RegisterForm.tsx,
    frontend2/src/features/auth/OAuthButtons.tsx,
    frontend2/src/components/retro/RetroButton.tsx,
    frontend2/src/components/retro/RetroPanel.tsx,
    frontend2/src/components/retro/RetroInput.tsx,
    frontend2/src/components/retro/RetroTabs.tsx,
    frontend2/src/components/retro/HazardStripe.tsx,
    frontend2/src/components/retro/index.ts,
    .planning/phases/50-design-system/50-UI-SPEC.md,
    .planning/phases/50-design-system/50-CONTEXT.md
  </read_first>
  <action>
    Per D-01, replace all inline retro utility class patterns in auth pages with design system components. The auth pages must maintain identical visual appearance and functionality.

    **AuthPage.tsx** refactoring:

    1. Import: `import { RetroTabs, RetroPanel } from "@/components/retro"`
    2. Remove inline tab class definitions (lines 24-27: `tabBase`, `activeTabClass`, `inactiveTabClass`)
    3. Replace the tab buttons div (lines 33-49) with:
       ```tsx
       <RetroTabs
         tabs={[
           { key: "login", label: t`LOGIN` },
           { key: "register", label: t`REGISTER` },
         ]}
         activeTab={activeTab}
         onTabChange={(key) => switchTab(key as Tab)}
       />
       ```
    4. Replace the panel div (line 53: `<div className="bg-retro-cream border-retro-thick...">`) with:
       ```tsx
       <RetroPanel showHazardStripe showClose onClose={() => {}}>
       ```
       The decorative X button lines (55-61) are removed since RetroPanel includes it via `showClose`. Note: the existing X button has `aria-hidden="true"` and `tabIndex={-1}` because it's decorative. The RetroPanel close button uses `aria-label="Close"` -- since it calls `onClose={() => {}}` (no-op), this is functionally the same but more accessible.
    5. Remove the inline hazard stripe div (line 66-67: `<div className="bg-hazard-stripe h-[8px] mb-md" />`) since RetroPanel renders it via `showHazardStripe`
    6. Keep form content and OAuthButtons as-is (they get their own refactoring below)
    7. The outer wrapper div (`min-h-dvh bg-retro-charcoal...`) stays as-is since it's a page layout concern, not a component concern

    **LoginForm.tsx** refactoring:

    1. Import: `import { RetroButton, RetroInput } from "@/components/retro"`
    2. Replace each input wrapper (the `<div className="relative">` + `<span>` icon + `<input>` pattern) with:
       ```tsx
       <RetroInput
         id="login-email"
         type="email"
         value={email}
         onChange={(e) => setEmail(e.target.value)}
         placeholder={t`EMAIL ADDRESS`}
         required
         icon={<EnvelopeIcon />}
       />
       ```
       Move the inline SVG icons into named local components (they already are in RegisterForm; LoginForm has them inline). Create `EnvelopeIcon` and `LockIcon` as local arrow functions with the same SVG content but WITHOUT the `aria-hidden` attribute (RetroInput's icon wrapper adds it). Keep the `<label className="sr-only">` elements.
    3. Replace the submit button (lines 150-161) with:
       ```tsx
       <RetroButton type="submit" disabled={isSubmitting} className="w-full">
         {isSubmitting ? t`PROCESSING...` : t`LOG IN`}
       </RetroButton>
       ```
       The `w-full` className is passed through to make the button full-width (RetroButton merges className).
    4. Keep the error message `<p>` block as-is (it's a simple text element, not a component pattern)

    **RegisterForm.tsx** refactoring:

    1. Import: `import { RetroButton, RetroInput } from "@/components/retro"`
    2. Replace the `inputClass` variable (line 55-56) usage -- each input's wrapper `<div className="relative">` + `<span>` + `<input>` becomes:
       ```tsx
       <RetroInput
         id="register-name"
         type="text"
         value={name}
         onChange={(e) => setName(e.target.value)}
         placeholder={t`FULL NAME`}
         required
         icon={<PersonIcon />}
       />
       ```
       Repeat for email (EnvelopeIcon), password (LockIcon), confirm password (LockIcon).
       Keep local icon components but remove `aria-hidden` from SVGs (RetroInput wrapper handles it).
       Remove the `inputClass` variable since it's no longer used.
    3. Replace submit button with `<RetroButton type="submit" disabled={isSubmitting} className="w-full">...</RetroButton>`
    4. Keep sr-only labels and error message as-is

    **OAuthButtons.tsx** refactoring:

    1. Import: `import { RetroButton } from "@/components/retro"`
    2. Remove the `buttonClass` variable (line 7)
    3. Replace each OAuth `<button>` with:
       ```tsx
       <RetroButton
         type="button"
         aria-label={t`Sign in with Google`}
         onClick={() => { window.location.href = "/api/auth/oauth/google"; }}
         className="w-full flex items-center gap-sm"
       >
         <GoogleIcon />
         {t`SIGN IN WITH GOOGLE`}
       </RetroButton>
       ```
       Extract the inline Google and GitHub SVGs into named local components (GoogleIcon, GitHubIcon). Keep `aria-hidden="true"` on the SVGs since they're decorative alongside text labels.
    4. Keep the OR divider section as-is (it's a simple layout element)

    **IMPORTANT:** After refactoring, all auth pages must maintain the EXACT same visual appearance and functionality. The refactoring replaces implementation (inline classes) with composition (component props) but the rendered result should be visually identical.

    Run existing auth tests after refactoring to verify nothing broke: `cd frontend2 && bunx vitest run src/features/auth/`
  </action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system/frontend2 && bunx vitest run && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - AuthPage.tsx imports `RetroTabs` and `RetroPanel` from `@/components/retro`
    - AuthPage.tsx does NOT contain `tabBase`, `activeTabClass`, or `inactiveTabClass` variables
    - AuthPage.tsx contains `<RetroTabs` and `<RetroPanel`
    - AuthPage.tsx does NOT contain inline `bg-hazard-stripe` div (RetroPanel handles it)
    - LoginForm.tsx imports `RetroButton` and `RetroInput` from `@/components/retro`
    - LoginForm.tsx contains `<RetroInput` and `<RetroButton`
    - LoginForm.tsx does NOT contain the inline input className string `w-full h-[40px] border-retro-thick`
    - RegisterForm.tsx imports `RetroButton` and `RetroInput` from `@/components/retro`
    - RegisterForm.tsx contains `<RetroInput` and `<RetroButton`
    - RegisterForm.tsx does NOT contain the `inputClass` variable
    - OAuthButtons.tsx imports `RetroButton` from `@/components/retro`
    - OAuthButtons.tsx does NOT contain the `buttonClass` variable
    - All tests pass: `bunx vitest run` exits 0
    - Build succeeds: `bun run build` exits 0
  </acceptance_criteria>
  <done>All four auth files refactored to use design system components (per D-01); inline retro patterns eliminated; visual appearance and functionality unchanged; all tests and build pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of demo page and refactored auth pages</name>
  <files>frontend2/src/pages/DemoPage.tsx, frontend2/src/routes/index.tsx</files>
  <action>
    Human verifies the visual quality of all 10 retro components on /demo and the refactored auth pages at /login.
  </action>
  <what-built>Complete retro design system with 10 components, interactive demo page at /demo, and auth pages refactored to use design system components</what-built>
  <how-to-verify>
    1. Start dev server: `cd frontend2 \&\& bun run dev`
    2. Open http://localhost:5173/demo (no login required per D-03)
    3. Verify all 10 component sections are visible with retro industrial styling (thick borders, cream backgrounds, beveled shadows)
    4. Click "EXECUTE", "ABORT MISSION", "STANDBY" buttons -- verify hover and press states
    5. Click "OPEN DIALOG" -- verify modal appears with hazard stripe, X close, backdrop
    6. Click "CONFIRM" or "ABORT" in dialog -- verify it closes
    7. Click success/error/info toast buttons -- verify toasts appear at bottom-right with colored left borders
    8. Verify toast X button dismisses toast early (per D-04)
    9. Verify toasts auto-dismiss after ~4 seconds
    10. Type in the demo input field -- verify live typing works
    11. Switch between ALPHA/BRAVO/CHARLIE tabs -- verify active tab styling changes
    12. Check badges render with 5 different color variants
    13. Navigate to http://localhost:5173/login -- verify auth page still looks correct
    14. Verify auth page uses RetroTabs (file-folder tabs), RetroPanel (with hazard stripe), RetroInput (with icon prefixes), RetroButton (submit buttons)
    15. Test login/register tab switching still works
    16. On mobile viewport (DevTools device emulation, ~375px width): verify demo page and auth page are responsive
  </how-to-verify>
  <verify>Human visual inspection of /demo and /login pages</verify>
  <done>All 10 components render with retro industrial aesthetic; demo page is fully interactive; auth pages look correct after refactoring</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
**ASVS Level:** 1
**Threats identified:**
- Demo page is public (no auth) per D-03: it contains no sensitive data, only component examples -- accept (development tool, no user data exposure)
- Auth page refactoring must not break form submission or OAuth redirects -- mitigated by running full test suite after refactoring and manual verification
- RetroInput in auth forms must continue to pass through type="password" to native input -- mitigated by RetroInput spreading all InputHTMLAttributes
**Residual risk:** Low -- demo page has no sensitive functionality; auth refactor is a visual-only change with test verification
</threat_model>

<verification>
Build succeeds: `cd frontend2 && bun run build`
All tests pass: `cd frontend2 && bunx vitest run`
Demo page loads at /demo without auth
Auth pages render correctly at /login
</verification>

<success_criteria>
- /demo page renders all 10 retro components interactively (D-02)
- /demo is public (D-03)
- Demo uses exact copywriting from UI-SPEC
- Auth pages use RetroButton, RetroInput, RetroPanel, RetroTabs (D-01)
- No inline retro patterns remain in auth files
- ToastProvider wraps the app
- All tests and build pass
- Human verification approves visual quality
</success_criteria>

<output>
After completion, create `.planning/phases/50-design-system/50-04-SUMMARY.md`
</output>
