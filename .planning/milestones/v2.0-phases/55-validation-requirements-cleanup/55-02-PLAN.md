---
plan: 55-02
title: "Requirements file fixes and v2.0 traceability table"
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v1.9-REQUIREMENTS.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Requirements file Design System section lists DS-01 through DS-10, not SYNC-01–04"
    - "Requirements file Dashboard section lists DASH-01 through DASH-03, not COMP-01–04"
    - "All 33 v2.0 requirement checkboxes are checked [x] (SCAF-01–04, AUTH-01–05, DS-01–10, LAY-01–03, DASH-01–03, SET-01–08)"
    - "Traceability table contains rows for all 33 v2.0 REQ-IDs mapped to their phases with status Complete"
    - "v1.9 traceability rows are preserved (QC, BATCH, SYNC, COMP entries must remain)"
  artifacts:
    - path: ".planning/milestones/v1.9-REQUIREMENTS.md"
      provides: "Corrected v2.0 Design System and Dashboard sections plus complete traceability table"
      contains: "DS-01"
  key_links:
    - from: "v1.9-REQUIREMENTS.md Design System section"
      to: "ROADMAP.md Phase 50 requirements"
      via: "DS-01 through DS-10 IDs"
      pattern: "DS-0[1-9]|DS-10"
    - from: "v1.9-REQUIREMENTS.md traceability table"
      to: "ROADMAP.md phase mappings"
      via: "REQ-ID → Phase N column"
      pattern: "SCAF|AUTH|DS|LAY|DASH|SET"
---

<objective>
Fix the v1.9-REQUIREMENTS.md file so it accurately reflects the v2.0 Retro Frontend scope: replace the two wrong requirement sections, check all 33 verified-complete v2.0 checkboxes, and add a complete v2.0 traceability table.

Purpose: The requirements file is the single source of truth for what was committed to and delivered. Currently it has a split personality — correct SCAF/AUTH/SET/LAY sections but wrong DS/DASH sections (carrying v1.9 SYNC/COMP IDs), all checkboxes unchecked, and a traceability table with no v2.0 entries. This plan brings the file into accurate alignment with the delivered milestone.

Output: One updated file where all 33 v2.0 requirements are correctly listed and checked, and the traceability table maps each REQ-ID to its phase with Complete status.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/v2.0-MILESTONE-AUDIT.md
@.planning/milestones/v1.9-REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Design System section with DS-01–DS-10</name>
  <files>.planning/milestones/v1.9-REQUIREMENTS.md</files>
  <read_first>
    - .planning/milestones/v1.9-REQUIREMENTS.md (current content: lines 36–39 show the wrong SYNC-01–04 items under "### Design System")
    - .planning/v2.0-MILESTONE-AUDIT.md (authoritative DS requirement descriptions, lines 92–103)
    - .planning/ROADMAP.md (Phase 50 section for DS requirement descriptions)
  </read_first>
  <action>
In `.planning/milestones/v1.9-REQUIREMENTS.md`, locate the `### Design System` section. It currently contains:

```
- [x] **SYNC-01**: Quick capture works fully offline — items queued in IndexedDB
- [x] **SYNC-02**: Photos stored as blobs in IndexedDB for offline display
- [x] **SYNC-03**: Photos upload automatically after item syncs to server (chained sync)
- [x] **SYNC-04**: Offline-captured items appear in item list with pending indicator
```

Replace the entire content of the `### Design System` section (the four SYNC lines) with these ten DS requirements, all checked `[x]`:

```
- [x] **DS-01**: RetroButton component with neutral, primary, danger, and secondary variants and distinct hover/press/disabled states
- [x] **DS-02**: RetroPanel component with optional hazard stripe header and close button
- [x] **DS-03**: RetroInput component with icon prefix slot, monospace text, and inline validation error state
- [x] **DS-04**: RetroCard lightweight content container component
- [x] **DS-05**: RetroDialog modal using native `<dialog>` element with imperative open/close API via ref
- [x] **DS-06**: RetroTable data table with retro charcoal header and alternating row styling
- [x] **DS-07**: RetroTabs file-folder style tab bar with controlled active tab
- [x] **DS-08**: RetroToast ToastProvider context and useToast hook with auto-dismiss and color variants
- [x] **DS-09**: RetroBadge inline status badge with 5 color variants (default, success, danger, warning, info)
- [x] **DS-10**: HazardStripe configurable decorative divider using diagonal yellow-black gradient
```

Do not alter any other section. Do not change the heading `### Design System`.
  </action>
  <verify>
    <automated>grep -c "DS-0" .planning/milestones/v1.9-REQUIREMENTS.md && grep -c "SYNC-0" .planning/milestones/v1.9-REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "DS-01" .planning/milestones/v1.9-REQUIREMENTS.md` returns a match
    - `grep "DS-10" .planning/milestones/v1.9-REQUIREMENTS.md` returns a match
    - `grep "SYNC-01" .planning/milestones/v1.9-REQUIREMENTS.md` returns no match (SYNC entries must be gone from Design System section)
    - The grep count for DS-0 lines is 10 (DS-01 through DS-09 = 9 plus DS-10 = 10 total)
    - All 10 DS lines begin with `- [x]` (checked)
  </acceptance_criteria>
  <done>Design System section replaced with DS-01 through DS-10, all checked [x]</done>
</task>

<task type="auto">
  <name>Task 2: Replace Dashboard section with DASH-01–DASH-03 and check all remaining unchecked v2.0 requirements</name>
  <files>.planning/milestones/v1.9-REQUIREMENTS.md</files>
  <read_first>
    - .planning/milestones/v1.9-REQUIREMENTS.md (current content after Task 1: Dashboard section still shows COMP-01–04; SCAF/AUTH/SET/LAY checkboxes still unchecked)
    - .planning/v2.0-MILESTONE-AUDIT.md (DASH requirement descriptions lines 119–122; all 33 requirements confirmed satisfied)
    - .planning/ROADMAP.md (Phase 52 DASH requirement descriptions)
  </read_first>
  <action>
This task makes two changes to `.planning/milestones/v1.9-REQUIREMENTS.md`:

**Change 1 — Replace Dashboard section:**

Locate `### Dashboard` section. It currently contains:

```
- [x] **COMP-01**: Quick-captured items are flagged as "needs details" in the database
- [x] **COMP-02**: User can filter item list to show only "needs details" items
- [x] **COMP-03**: User can mark an item as complete (remove "needs details" flag)
- [x] **COMP-04**: User sees session summary when ending quick capture (count + thumbnails)
```

Replace the four COMP lines with these three DASH requirements, all checked `[x]`:

```
- [x] **DASH-01**: Dashboard HUD panels display total items, categories, and locations counts fetched from the analytics API
- [x] **DASH-02**: Terminal-styled activity feed shows recent inventory actions; SSE EventSource triggers live re-fetch without page reload
- [x] **DASH-03**: Quick-access action cards for Add Item, Scan Barcode, and View Loans navigate to /items, /scan, /loans respectively
```

Do not alter the heading `### Dashboard`.

**Change 2 — Check all unchecked v2.0 requirement checkboxes:**

Find every v2.0 requirement line that currently reads `- [ ]` and change it to `- [x]`. This affects:
- All SCAF-01 through SCAF-04 lines (currently `[ ]`)
- All AUTH-01 through AUTH-05 lines (currently `[ ]`)
- All SET-01 through SET-08 lines (currently `[ ]`)
- All LAY-01 through LAY-03 lines (currently `[ ]`)

The DS-01–10 and DASH-01–03 lines written in Task 1 and this task are already `[x]`.

Do not change the checked `[x]` items in the v2.1+ sections — leave those as-is.
  </action>
  <verify>
    <automated>grep -c "\- \[ \]" .planning/milestones/v1.9-REQUIREMENTS.md && grep -c "DASH-0" .planning/milestones/v1.9-REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "DASH-01" .planning/milestones/v1.9-REQUIREMENTS.md` returns a match
    - `grep "DASH-03" .planning/milestones/v1.9-REQUIREMENTS.md` returns a match
    - `grep "COMP-01" .planning/milestones/v1.9-REQUIREMENTS.md` returns no match (COMP entries must be gone)
    - `grep "- \[ \]" .planning/milestones/v1.9-REQUIREMENTS.md` returns zero matches under the v2.0 sections (SCAF, AUTH, Design System, Dashboard, Settings, Layout must all be `[x]`)
    - The count of `- [x]` lines under v2.0 sections totals 33 (SCAF-01–04=4, AUTH-01–05=5, DS-01–10=10, LAY-01–03=3, DASH-01–03=3, SET-01–08=8)
  </acceptance_criteria>
  <done>Dashboard section replaced with DASH-01–03; all 33 v2.0 checkboxes checked [x]</done>
</task>

<task type="auto">
  <name>Task 3: Replace traceability table with complete v2.0 entries</name>
  <files>.planning/milestones/v1.9-REQUIREMENTS.md</files>
  <read_first>
    - .planning/milestones/v1.9-REQUIREMENTS.md (current content: traceability table has only v1.9 QC/BATCH/SYNC/COMP entries; coverage note claims 33 v2.0 requirements mapped but table is missing all of them)
    - .planning/ROADMAP.md (authoritative REQ-ID → phase mapping for all v2.0 requirements)
  </read_first>
  <action>
In `.planning/milestones/v1.9-REQUIREMENTS.md`, locate the `## Traceability` section.

The current table has these entries (v1.9 only):
QC-01 through QC-08, BATCH-01 through BATCH-04, SYNC-01 through SYNC-04, COMP-01 through COMP-04.

Keep all existing v1.9 rows exactly as-is. After the last v1.9 row (`| COMP-04 | Phase 47 | Complete |`), add a blank separator row and then all 33 v2.0 requirement rows:

```
| | | |
| SCAF-01 | Phase 48 | Complete |
| SCAF-02 | Phase 48 | Complete |
| SCAF-03 | Phase 48 | Complete |
| SCAF-04 | Phase 48 | Complete |
| AUTH-01 | Phase 49 | Complete |
| AUTH-02 | Phase 49 | Complete |
| AUTH-03 | Phase 49 | Complete |
| AUTH-04 | Phase 49 | Complete |
| AUTH-05 | Phase 49 | Complete |
| DS-01 | Phase 50 | Complete |
| DS-02 | Phase 50 | Complete |
| DS-03 | Phase 50 | Complete |
| DS-04 | Phase 50 | Complete |
| DS-05 | Phase 50 | Complete |
| DS-06 | Phase 50 | Complete |
| DS-07 | Phase 50 | Complete |
| DS-08 | Phase 50 | Complete |
| DS-09 | Phase 50 | Complete |
| DS-10 | Phase 50 | Complete |
| LAY-01 | Phase 51 | Complete |
| LAY-02 | Phase 51 | Complete |
| LAY-03 | Phase 51 | Complete |
| DASH-01 | Phase 52 | Complete |
| DASH-02 | Phase 52 | Complete |
| DASH-03 | Phase 52 | Complete |
| SET-01 | Phase 53 | Complete |
| SET-02 | Phase 53 | Complete |
| SET-03 | Phase 53 | Complete |
| SET-04 | Phase 53 | Complete |
| SET-05 | Phase 53 | Complete |
| SET-06 | Phase 53 | Complete |
| SET-07 | Phase 53 | Complete |
| SET-08 | Phase 53 | Complete |
```

Also update the coverage note below the table. Replace:
```
**Coverage:**
- v2.0 requirements: 33 total
- Mapped to phases: 33
- Unmapped: 0
```

With:
```
**Coverage:**
- v1.9 requirements: 20 total (QC-01–08, BATCH-01–04, SYNC-01–04, COMP-01–04) — all mapped
- v2.0 requirements: 33 total (SCAF-01–04, AUTH-01–05, DS-01–10, LAY-01–03, DASH-01–03, SET-01–08) — all mapped
- Total: 53 requirements across v1.9 and v2.0 — all Complete
```

Also update the footer of the file. Replace:
```
*Last updated: 2026-04-08 after roadmap creation -- all 33 requirements mapped to phases 48-53*
```
With:
```
*Last updated: 2026-04-14 after Phase 55 cleanup -- all 33 v2.0 requirements verified complete, traceability table complete*
```
  </action>
  <verify>
    <automated>grep -c "Phase 48\|Phase 49\|Phase 50\|Phase 51\|Phase 52\|Phase 53" .planning/milestones/v1.9-REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "SCAF-01" .planning/milestones/v1.9-REQUIREMENTS.md` returns a match in the traceability table
    - `grep "SET-08" .planning/milestones/v1.9-REQUIREMENTS.md` returns a match in the traceability table with "Phase 53"
    - `grep "DS-10" .planning/milestones/v1.9-REQUIREMENTS.md` returns a match in the traceability table with "Phase 50"
    - `grep "LAY-01" .planning/milestones/v1.9-REQUIREMENTS.md` returns a match in the traceability table with "Phase 51"
    - All 33 v2.0 REQ-IDs appear in the traceability table (verify by counting: SCAF=4, AUTH=5, DS=10, LAY=3, DASH=3, SET=8 = 33)
    - All existing v1.9 rows (QC, BATCH, SYNC, COMP) still present — `grep "QC-01" .planning/milestones/v1.9-REQUIREMENTS.md` still returns a match
    - Coverage note updated to reflect v1.9 and v2.0 totals
    - Footer date updated to 2026-04-14
  </acceptance_criteria>
  <done>Traceability table has all 33 v2.0 REQ-IDs mapped to phases with Complete status; v1.9 rows preserved</done>
</task>

</tasks>

<verification>
After all tasks complete, run these checks:

```bash
# Verify DS section correct
grep -A 12 "### Design System" .planning/milestones/v1.9-REQUIREMENTS.md | head -12

# Verify Dashboard section correct
grep -A 5 "### Dashboard" .planning/milestones/v1.9-REQUIREMENTS.md | head -5

# Verify no unchecked v2.0 requirements
grep "- \[ \]" .planning/milestones/v1.9-REQUIREMENTS.md | grep -v "ITEM\|EXT\|INT\|OFFL" | wc -l

# Verify traceability table has all 33 v2.0 entries
grep "Complete" .planning/milestones/v1.9-REQUIREMENTS.md | grep -E "SCAF|AUTH|DS-|LAY|DASH|SET" | wc -l
```

Expected results:
1. DS section shows DS-01 through DS-10 (no SYNC entries)
2. Dashboard section shows DASH-01 through DASH-03 (no COMP entries)
3. Zero unchecked v2.0 requirement lines
4. Count of 33 Complete entries for v2.0 REQ-IDs
</verification>

<success_criteria>
- Design System section lists DS-01 through DS-10, all `[x]`
- Dashboard section lists DASH-01 through DASH-03, all `[x]`
- All 33 v2.0 requirement checkboxes are `[x]` (SCAF-01–04, AUTH-01–05, DS-01–10, LAY-01–03, DASH-01–03, SET-01–08)
- Traceability table includes all 33 v2.0 REQ-IDs mapped to their phases with Complete status
- All v1.9 traceability rows are preserved
- Coverage note and footer date updated
</success_criteria>

<output>
After completion, create `.planning/phases/55-validation-requirements-cleanup/55-02-SUMMARY.md`
</output>
