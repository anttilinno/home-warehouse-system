---
phase: 06-providers
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - frontend2/src/App.tsx
  - frontend2/src/components/layout/AppShell.tsx
  - frontend2/src/components/layout/TopBar.tsx
  - frontend2/src/components/layout/PageHeader.tsx
  - frontend2/src/components/layout/AppShell.test.tsx
  - frontend2/src/components/layout/TopBar.test.tsx
  - frontend2/src/components/layout/PageHeader.test.tsx
  - frontend2/src/components/retro/feedback/RetroToast.mount.test.tsx
  - frontend2/e2e/sse-online.spec.ts
autonomous: true
requirements: [PROV-01, PROV-03, PROV-04]

must_haves:
  truths:
    - "RetroToaster is mounted once at the App root (under QueryClientProvider, sibling of the router subtree) so toasts survive navigation and render on /login"
    - "SSEProvider is mounted inside WorkspaceProvider in AppShell so it always has a valid wsId and an authed session"
    - "TopBar ONLINE dot binds to useSSEStatus().connected and the sse-slot renders RetroStatusDot fed live state"
    - "PageHeader LAST SYNC binds to lastEventAt (replacing the em-dash placeholder)"
    - "ShortcutsProvider position is verified in the canonical stack (not moved, not rebuilt)"
    - "retroToast.promise resolves loading→success against the mounted RetroToaster"
    - "After login the ONLINE dot reaches connected against the live stack (e2e smoke)"
  artifacts:
    - path: "frontend2/src/App.tsx"
      provides: "RetroToaster mounted at root in the canonical provider order"
      contains: "RetroToaster"
    - path: "frontend2/src/components/layout/AppShell.tsx"
      provides: "SSEProvider wrapping the shell subtree under WorkspaceProvider"
      contains: "SSEProvider"
    - path: "frontend2/e2e/sse-online.spec.ts"
      provides: "Live-stack smoke: ONLINE dot connected post-login"
  key_links:
    - from: "frontend2/src/components/layout/AppShell.tsx"
      to: "frontend2/src/features/sse"
      via: "SSEProvider import + mount under WorkspaceProvider"
      pattern: "SSEProvider"
    - from: "frontend2/src/components/layout/TopBar.tsx"
      to: "frontend2/src/features/sse useSSEStatus"
      via: "useSSEStatus() feeds online + RetroStatusDot state"
      pattern: "useSSEStatus"
    - from: "frontend2/src/components/layout/PageHeader.tsx"
      to: "lastEventAt"
      via: "AppShell passes formatted lastEventAt into PageHeader lastSync"
      pattern: "lastSync"
---

<objective>
Wire the Phase 6 providers into the live app: mount `RetroToaster` once at the `App.tsx` root and `SSEProvider` inside `WorkspaceProvider` in `AppShell`; bind the TopBar ONLINE dot + `sse-slot` `RetroStatusDot` and the PageHeader LAST SYNC to real `useSSEStatus()` state; verify `ShortcutsProvider` position and `retroToast.promise`; add the live-stack ONLINE-dot e2e smoke.

Purpose: PROV-01 (canonical mount order), PROV-03 (ShortcutsProvider verified), PROV-04 (RetroToaster mounted + promise ergonomics). PROV-03/PROV-04 are verification/mount tasks, NOT rebuilds — both atoms already shipped (Plans 03-01, 04-05).

Output: edits to App/AppShell/TopBar/PageHeader, extended layout tests, a RetroToaster mount test, and `e2e/sse-online.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-providers/06-RESEARCH.md
@.planning/phases/06-providers/06-VALIDATION.md
@.planning/phases/06-providers/06-01-PLAN.md

<read_first>
- `.planning/phases/06-providers/06-RESEARCH.md` §"Resolved Stale-Spec Questions §1" (the FINAL canonical provider tree — RetroToaster at App root, SSEProvider under WorkspaceProvider, append-without-reorder) + §"Code Examples" (Chrome binding: TopBar `online` prop at TopBar.tsx:21, sse-slot at :110-119, PageHeader `lastSync` at :18) + Pitfall 6 (NEVER import SSE inside RetroStatusDot).
- `06-01-PLAN.md` + the shipped `frontend2/src/features/sse/index.ts` — `SSEProvider`, `useSSEStatus`, `useSSE` are the consumed contracts.
- `frontend2/src/App.tsx` — current order: `I18nProvider > BrowserRouter > QueryClientProvider > ShortcutsProvider > ModalStackProvider > AppRoutes`. RetroToaster appends as a sibling under QueryClientProvider (NOT inside AppShell — so a /login toast still renders).
- `frontend2/src/components/layout/AppShell.tsx` — `WorkspaceProvider` wraps the shell (line 65). SSEProvider wraps the subtree INSIDE WorkspaceProvider.
- `frontend2/src/components/layout/TopBar.tsx` — `online?: boolean` prop (line 21, drives dot at :85-96); static `sse-slot` placeholder at :110-119 (replace `● live` with `<RetroStatusDot state={...}/>`).
- `frontend2/src/components/layout/PageHeader.tsx` — `lastSync?: string` prop (line 19); `LAST_SYNC_PLACEHOLDER = "—"` (line 13); `data-testid="page-header-lastsync"`.
- `frontend2/src/components/retro/feedback/RetroStatusDot.tsx` — `state: "live"|"idle"|"error"` (dumb atom — feed it, never let it import SSE).
- `frontend2/src/components/retro/feedback/RetroToast.tsx` (`RetroToaster`) + `retroToast.ts` (`.promise` forwarded).
- `frontend2/src/components/layout/AppShell.test.tsx` — `renderShell()` harness; once SSEProvider mounts in AppShell it needs the global EventSource stub (installed in setup.ts by Plan 06-01) — the harness should "just work" but verify; the MSW server answers `/api/users/me/workspaces`.
- `frontend2/e2e/login-dashboard.spec.ts` — the existing v3.0 spec + CLAUDE.md §E2E runbook (seeder creds, `/login → /`, cookie inheritance) — model the SSE smoke on it.
</read_first>

<interfaces>
From src/features/sse/index.ts (shipped in 06-01):
```typescript
export function SSEProvider(props: { children: ReactNode }): JSX.Element;
export function useSSEStatus(): { connected: boolean; lastEventAt: Date | null };
export function useSSE(args: { onEvent: (e: SSEEvent) => void }): void;
```

TopBar already accepts `online?: boolean`; PageHeader already accepts `lastSync?: string`. RetroStatusDot accepts `state: "live" | "idle" | "error"`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount RetroToaster (App root) + SSEProvider (AppShell under WorkspaceProvider)</name>
  <files>frontend2/src/App.tsx, frontend2/src/components/layout/AppShell.tsx, frontend2/src/components/layout/AppShell.test.tsx</files>
  <action>
In `App.tsx`, mount `<RetroToaster />` once at the root as a sibling of the router subtree under `QueryClientProvider` — place it alongside the devtools `Suspense` (NOT inside AppShell), so a toast fired from `/login` still renders and toasts persist across navigation (RESEARCH §1 decision). Do NOT reorder the existing `I18nProvider > BrowserRouter > QueryClientProvider > ShortcutsProvider > ModalStackProvider` chain — Phase 6 only APPENDS (the 03-06/05-03 append-without-reorder contract). Update the App.tsx provider-order comment to record the final canonical tree.

In `AppShell.tsx`, wrap the shell subtree INSIDE `WorkspaceProvider` (line 65) with `<SSEProvider>` from `@/features/sse`, so SSE always has a valid wsId (D-12) and an authed session (the shell only renders under `RequireAuth`). The `<div className="app-shell">` and everything below it become SSEProvider's children.

Extend `AppShell.test.tsx`: the `renderShell()` harness now mounts SSEProvider (which constructs an EventSource — the global stub from Plan 06-01's setup.ts must be present). Add an assertion that the shell renders without throwing under SSEProvider and that exactly one EventSource instance opened (read the stub registry). If the harness lacks WorkspaceProvider context for SSE, confirm SSEProvider sits inside it via the real AppShell tree (it does — AppShell owns WorkspaceProvider). Keep existing assertions green.
  </action>
  <verify>
    <automated>cd frontend2 &amp;&amp; grep -q "RetroToaster" src/App.tsx &amp;&amp; grep -q "SSEProvider" src/components/layout/AppShell.tsx &amp;&amp; npx vitest run src/components/layout/AppShell.test.tsx --reporter=dot 2>&amp;1 | tail -5 &amp;&amp; npx tsc -b --noEmit 2>&amp;1 | tail -3</automated>
  </verify>
  <done>RetroToaster mounted at App root (sibling of router subtree, under QueryClientProvider, NOT in AppShell); SSEProvider wraps the shell inside WorkspaceProvider; AppShell.test passes with one EventSource open; tsc clean; existing provider order unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire chrome — TopBar online + sse-slot RetroStatusDot, PageHeader LAST SYNC</name>
  <files>frontend2/src/components/layout/TopBar.tsx, frontend2/src/components/layout/PageHeader.tsx, frontend2/src/components/layout/TopBar.test.tsx, frontend2/src/components/layout/PageHeader.test.tsx</files>
  <behavior>
    - TopBar consuming useSSEStatus shows ONLINE dot driven by `connected` (mint when true, faint when false)
    - sse-slot renders `<RetroStatusDot state={connected ? "live" : "idle"} />` (no static "● live")
    - PageHeader LAST SYNC renders the formatted lastEventAt; falls back to "—" when lastEventAt is null
    - RetroStatusDot source still has NO useSSE/EventSource import (Phase 4 guard at RetroStatusDot.test.tsx:68 stays green)
  </behavior>
  <action>
TopBar reads `useSSEStatus()` (from `@/features/sse`) and uses `connected` for the existing ONLINE dot AND replaces the static `sse-slot` `● live` markup (TopBar.tsx:110-119) with `<RetroStatusDot state={connected ? "live" : "idle"} />`. Feed the atom — never import SSE inside `RetroStatusDot` (Pitfall 6; the guard test asserts the atom source has no `useSSE|sseStatus|EventSource`). Keep TopBar's `online?: boolean` prop for test injectability, but default it from `useSSEStatus().connected` when not supplied (so AppShell needn't thread it; the e2e + live path uses the hook). Decide cleanly: prefer reading the hook inside TopBar and dropping the explicit prop pass-through from AppShell, OR keep the prop and have AppShell read `useSSEStatus` once and pass `online` + `lastSync` down — pick one and document it in the TopBar comment. Whichever you choose, the sse-slot RetroStatusDot must reflect live `connected`.

PageHeader: bind LAST SYNC to `lastEventAt`. AppShell reads `useSSEStatus()` and passes a formatted `lastSync` string (e.g. `HH:MM:SS` local time of `lastEventAt`) into `<PageHeader lastSync={...}/>`; when `lastEventAt` is null, keep the `"—"` placeholder fallback (PageHeader already defaults to it). Keep the `data-testid="page-header-lastsync"` slot stable.

Extend `TopBar.test.tsx`: assert the sse-slot contains RetroStatusDot in `live` state when status is connected and `idle` when not (mock `useSSEStatus`). Extend `PageHeader.test.tsx`: assert a passed `lastSync` renders in the slot and that absent lastSync shows `"—"`. Run RED→GREEN.

NOTE: if AppShell now reads `useSSEStatus()` to thread `lastSync`/`online`, that read must be INSIDE SSEProvider — i.e. split the part of AppShell that consumes status into a child component rendered under `<SSEProvider>`, since a component cannot consume a context it also renders. Structure AppShell so the chrome (TopBar/PageHeader) lives in a child under SSEProvider.
  </action>
  <verify>
    <automated>cd frontend2 &amp;&amp; grep -q "useSSEStatus" src/components/layout/TopBar.tsx &amp;&amp; grep -q "RetroStatusDot" src/components/layout/TopBar.tsx &amp;&amp; test $(grep -c "useSSE\|EventSource" src/components/retro/feedback/RetroStatusDot.tsx) -eq 0 &amp;&amp; npx vitest run src/components/layout/TopBar.test.tsx src/components/layout/PageHeader.test.tsx src/components/retro/feedback/RetroStatusDot.test.tsx --reporter=dot 2>&amp;1 | tail -8 &amp;&amp; npx tsc -b --noEmit 2>&amp;1 | tail -3</automated>
  </verify>
  <done>TopBar binds ONLINE dot + sse-slot RetroStatusDot to useSSEStatus; PageHeader LAST SYNC binds lastEventAt with "—" fallback; RetroStatusDot has zero SSE import (Phase 4 guard green); TopBar/PageHeader tests pass; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Verify ShortcutsProvider position + retroToast.promise; add SSE ONLINE e2e smoke; full gate</name>
  <files>frontend2/src/components/retro/feedback/RetroToast.mount.test.tsx, frontend2/e2e/sse-online.spec.ts</files>
  <action>
PROV-03 (verify-only): add an assertion to the App composition / an existing layout test that `ShortcutsProvider` is present in the rendered tree at its shipped position (inside the router, above the shell) — do NOT move or rebuild it. A render-and-assert (e.g. a child that calls `useShortcuts` mounts without throwing under the App tree) suffices; reference the existing shortcuts tests rather than duplicating them.

PROV-04: create `RetroToast.mount.test.tsx` — render `<RetroToaster />`, fire a `retroToast.promise(p, { loading, success })` against a resolving promise, and assert the loading message then the success message appear (use fake timers / `await` resolution as the sonner test pattern requires). This proves the mounted Toaster + `.promise` ergonomics (SC4).

Create `frontend2/e2e/sse-online.spec.ts` modeled on `login-dashboard.spec.ts` + CLAUDE.md §E2E runbook: log in via real `/login` (seeder creds, env-overridable), land on `/`, and assert the ONLINE dot reaches the connected/`live` state within a timeout against the live stack (the TopBar dot + sse-slot RetroStatusDot in `live`). Keep it minimal — one smoke test. The full e2e run happens at the orchestrator phase gate; the in-plan gate only lists it (`npx playwright test --list`) to confirm it's collected.

Run the full unit gate.
  </action>
  <verify>
    <automated>cd frontend2 &amp;&amp; npx playwright test --list 2>&amp;1 | grep -q "sse-online" &amp;&amp; npx vitest run --reporter=dot 2>&amp;1 | tail -6 &amp;&amp; npx tsc -b --noEmit 2>&amp;1 | tail -3 &amp;&amp; node ../scripts/check-forbidden-imports.mjs src 2>&amp;1 | tail -3</automated>
  </verify>
  <done>ShortcutsProvider position asserted (not moved); RetroToast.mount.test proves retroToast.promise loading→success; e2e/sse-online.spec.ts collected by Playwright; full vitest suite green; tsc + lint:imports clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live SSE stream → chrome | Connection status crosses into TopBar/PageHeader; only the coarse status is read, never raw event `data` |
| public route → toast region | RetroToaster mounted at root renders toasts on public (`/login`) and authed pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Information Disclosure | RetroStatusDot coupling | mitigate | Atom stays dumb — TopBar reads `useSSEStatus` and passes `state`; the Phase 4 guard test (RetroStatusDot.test.tsx:68 — no `useSSE|sseStatus|EventSource`) is re-asserted in Task 2 verify so the atom never gains a live-stream import. |
| T-06-06 | Tampering | event data → DOM | accept | Chrome renders only `connected`/`lastEventAt` (booleans/timestamp), never raw event payload — no injection surface introduced by wiring. |
| T-06-07 | DoS (self) | status re-render storm | mitigate | Status consumers (TopBar/PageHeader) read the split STATUS context with coarse `lastEventAt` (1s) from Plan 06-01 — event bursts do not thrash chrome. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs this phase (no new deps). No checkpoint needed. |
</threat_model>

<verification>
- `cd frontend2 && npx vitest run` — full unit suite green (incl. extended AppShell/TopBar/PageHeader + RetroToast.mount + the Phase 4 RetroStatusDot guard).
- `cd frontend2 && npx tsc -b --noEmit` — clean.
- `cd frontend2 && node ../scripts/check-forbidden-imports.mjs src` — clean (online-only guard).
- `cd frontend2 && npx playwright test --list` — includes `sse-online`. Full e2e run deferred to the orchestrator phase gate (needs the live stack).
- Manual (06-VALIDATION): kill backend 5s → ONLINE dot drops + recovers; create an entity in another tab → LAST SYNC ticks.
</verification>

<success_criteria>
- PROV-01: canonical provider tree mounted (RetroToaster at App root; SSEProvider inside WorkspaceProvider in AppShell) with no reordering of the shipped chain; documented in the 06-01 contract doc.
- PROV-03: ShortcutsProvider position verified (not moved/rebuilt).
- PROV-04: RetroToaster mounted app-wide; `retroToast.promise` loading→success proven.
- Chrome bound to live state: TopBar ONLINE dot + sse-slot RetroStatusDot ← `useSSEStatus().connected`; PageHeader LAST SYNC ← `lastEventAt`.
- SSE ONLINE e2e smoke collected; full unit suite + tsc + lint green.
</success_criteria>

<output>
Create `.planning/phases/06-providers/06-02-SUMMARY.md` when done.
</output>
