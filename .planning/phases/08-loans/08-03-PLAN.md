---
phase: 08-loans
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - frontend2/src/features/loans/LoanFormPage.tsx
  - frontend2/src/features/loans/LoanFormPage.test.tsx
  - frontend2/src/features/loans/schema.ts
  - frontend2/src/features/loans/hooks/useLoanPickerOptions.ts
  - frontend2/src/routes/index.tsx
autonomous: true
requirements: [LOAN-02]
must_haves:
  truths:
    - "User can open /loans/new and create a loan via an inventory-entry picker + borrower picker"
    - "Create posts inventory_id (NOT item_id), borrower_id, quantity, optional due_date + notes"
    - "?itemId= PRE-FILTERS the inventory-entry picker to that item's entries (locked FROM ITEM badge), it does NOT preselect an item"
    - "Pickers clamp to limit ≤ 100 (the 7b/Phase-7 422 cap lesson)"
    - "Due-date-in-the-past is a client zod refinement; on success invalidates ['loans', wsId]"
  artifacts:
    - path: "frontend2/src/features/loans/LoanFormPage.tsx"
      provides: "Create-loan form (RHF + zod)"
      min_lines: 80
    - path: "frontend2/src/features/loans/hooks/useLoanPickerOptions.ts"
      provides: "inventory-entry + borrower options at limit=100, ?itemId filter"
      contains: "limit=100"
  key_links:
    - from: "frontend2/src/features/loans/LoanFormPage.tsx"
      to: "loansApi.create"
      via: "submit"
      pattern: "loansApi\\.create"
    - from: "frontend2/src/routes/index.tsx"
      to: "LoanFormPage"
      via: "route /loans/new"
      pattern: "loans/new"
---

<objective>
Build the `/loans/new` create-loan form (LOAN-02): a blue-titled single-window RHF+zod
form with an inventory-entry picker (NOT an item picker — Pitfall 1, override 1) and a
borrower picker, both clamped to limit=100. The `?itemId=` deep-link PRE-FILTERS the
inventory-entry picker to that item's entries and shows a locked FROM ITEM badge.

Purpose: The only way to create a loan; the forward-compat target for the Phase 11 scan
deep link (`/loans/new?itemId=`).
Output: LoanFormPage, schema, useLoanPickerOptions, route registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-loans/08-RESEARCH.md
@.planning/phases/08-loans/08-UI-SPEC.md

<read_first>
- frontend2/src/features/inventory/InventoryFormPage.tsx — the blue-Window RHF+zod form pattern to MIRROR (Controller + RetroSelect pickers, dirty-guard butter confirm, form-level error banner, max-w-[560px]).
- frontend2/src/features/inventory/hooks/usePickerOptions.ts — the limit=100 read-list→{id,label} pattern to MIRROR for inventory entries + borrowers.
- frontend2/src/lib/api/inventory.ts — inventoryApi.list (full envelope) for the inventory-entry picker source; entries carry item_id + location_id.
- frontend2/src/lib/api/loans.ts — loansApi.create + CreateLoanBody (Plan 01).
- frontend2/src/test/msw/loanHandlers.ts — MSW handlers (Plan 01); add inventory + borrower list handlers in this plan's test if absent.
</read_first>

<interfaces>
CreateLoanBody (Plan 01): `{ inventory_id: string; borrower_id: string; quantity: number; loaned_at?: string; due_date?: string; notes?: string }`.
loansApi.create(ws, body: CreateLoanBody) → Loan.
Inventory entry row (from inventoryApi.list items): has `id`, `item_id`, `location_id`, `quantity`, `item`-name resolvable. The picker LABEL = item name + location/qty disambiguator; the picker VALUE = inventory entry `id`.
Borrower row (GET /workspaces/{ws}/borrowers?limit=100 → {items:[{id,name}]}).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: zod schema + picker-options hook</name>
  <files>frontend2/src/features/loans/schema.ts, frontend2/src/features/loans/hooks/useLoanPickerOptions.ts</files>
  <action>Create schema.ts: a zod object `{ inventory_id: nonempty string (required), borrower_id: nonempty string (required), due_date: optional date-string, notes: optional string maxLength 1000 }` with a refinement that a supplied `due_date` is not before today (UI-SPEC §2 cross-field — "Due date can't be in the past."; only when a date is supplied). Export `LoanFormInput`/`LoanFormValues` types (mirror inventory schema's input/output split; quantity defaults to 1 and is not a user field this phase). Create useLoanPickerOptions.ts mirroring usePickerOptions: fetch inventory entries via inventoryApi.list(ws, {limit:100}) and borrowers via `get<{items:{id;name}[]}>('/workspaces/{ws}/borrowers?limit=100')`, each keyed `["inventory", wsId, {limit:100}]` / `["borrowers", wsId, {limit:100}]` (prefix matches SSE). Map inventory entries to `{ id: entry.id, label, item_id: entry.item_id }` (label = resolved item name + a location/qty disambiguator; reuse the item-name join approach from InventoryListPage if the entry lacks an embedded name). Accept an optional `itemIdFilter` arg: when present, return only inventory options whose `item_id === itemIdFilter` (override 1 / Pitfall 6 — ?itemId filters the inventory picker, does not preselect an item). Clamp both lists to limit=100 (the 422 cap lesson). Return `{ inventoryOptions, borrowerOptions, isLoading }`.</action>
  <verify>
    <automated>cd frontend2 && bun run lint:tsc 2>&1 | tail -3 && grep -c "limit=100\|limit: 100\|limit:100" src/features/loans/hooks/useLoanPickerOptions.ts</automated>
  </verify>
  <done>tsc clean; both pickers clamp to 100; itemIdFilter narrows inventory options by item_id.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LoanFormPage — create form with inventory + borrower pickers, ?itemId filter</name>
  <files>frontend2/src/features/loans/LoanFormPage.tsx, frontend2/src/routes/index.tsx</files>
  <behavior>
    - Renders a blue Window NEW LOAN with an inventory-entry RetroSelect (required) + borrower RetroSelect (required) + optional Due date + Notes.
    - Submitting with a chosen inventory entry + borrower POSTs `{ inventory_id, borrower_id, quantity:1, due_date?, notes? }` to loansApi.create — body uses `inventory_id` (assert via MSW request body, NOT item_id).
    - `/loans/new?itemId=X` filters the inventory picker to entries whose item_id===X, auto-selects the entry if exactly one, and shows a FROM ITEM badge + the locked-field hint; the borrower field auto-focuses.
    - Empty inventory source → inventory select disabled + hint; empty borrower source → borrower select disabled + hint.
    - Supplying a past due date → field error "Due date can't be in the past." (no POST).
  </behavior>
  <action>Create LoanFormPage mirroring InventoryFormPage: blue Window `NEW LOAN`, `max-w-[560px] mx-auto`, RHF + zodResolver(loanFormSchema), Controller-wrapped RetroSelect for inventory_id + borrower_id, RetroInput type=date for due_date (optional, hint `Optional — leave blank for no due date.`), RetroTextarea for notes. Read `?itemId=` via useSearchParams and pass as `itemIdFilter` to useLoanPickerOptions; when present, render a RetroBadge info `FROM ITEM` in the inventory field label row + the hint `Loaning this item — pick a borrower.`, and auto-select when exactly one entry matches (per UI-SPEC §2 ?itemId — but note override 1: this filters the INVENTORY picker, it does not lock an item field; if multiple entries match the user still chooses which entry). Form-level error banner + dirty-guard butter confirm (mirror InventoryFormPage). On submit call loansApi.create; on success retroToast.success + invalidate `["loans", wsId]` + (if created from ?itemId) the item's `["loans", wsId, "by-item", itemId]` key, then navigate `/loans?tab=active` (or back to `/items/{itemId}` when created from ?itemId). Footer CANCEL + CREATE LOAN. Register the route in routes/index.tsx as `<Route path="loans/new" element={<LoanFormPage />} />` BEFORE the `loans` literal route added by Plan 02 and BEFORE any `loans/:id` param route (literal-before-param, AP-1). This plan runs in wave 3 (depends_on 08-02) so Plan 02's `loans` route is already merged — add `loans/new` above it; no parallel conflict.</action>
  <verify>
    <automated>cd frontend2 && bun run lint:tsc 2>&1 | tail -3 && grep -c "loans/new" src/routes/index.tsx && grep -c "inventory_id\|FROM ITEM\|itemIdFilter" src/features/loans/LoanFormPage.tsx</automated>
  </verify>
  <done>tsc clean; /loans/new route registered before param routes; form posts inventory_id; ?itemId filters the inventory picker with FROM ITEM badge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: LoanFormPage component test (MSW)</name>
  <files>frontend2/src/features/loans/LoanFormPage.test.tsx</files>
  <action>Create LoanFormPage.test.tsx (read InventoryFormPage.test.tsx for the harness). Add MSW handlers for GET inventory?limit=100 (≥2 entries, one matching a target item_id) + GET borrowers?limit=100, plus POST /loans (capture request body). Cover the five behavior cases from Task 2 — most importantly: (a) on submit the captured POST body has `inventory_id` set and NO `item_id`; (b) `?itemId=X` filters the inventory options to entries with item_id===X and renders FROM ITEM; (c) a past due date blocks submit with the field error. Use locale-agnostic matchers.</action>
  <verify>
    <automated>cd frontend2 && bun run test src/features/loans/LoanFormPage.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <done>LoanFormPage.test.tsx passes; the inventory_id-not-item_id assertion is green; ?itemId filter + past-due refinement covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (create loan) | inventory_id/borrower_id submitted; server validates same-workspace ownership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Elevation of Privilege | loansApi.create body | mitigate | Backend validates inventory_id + borrower_id belong to the request's workspace (V4/V5, server-authoritative); frontend posts only ids drawn from the wsId-scoped picker reads. |
| T-08-VAL | Tampering | due_date / quantity | mitigate | zod mirrors server validation (due_date ≥ today, quantity ≥ 1) for UX; server is authoritative. |
| T-08-XSS | Tampering | borrower/item picker labels | accept | React escapes select option text; no innerHTML. |
</threat_model>

<verification>
- `cd frontend2 && bun run test src/features/loans/LoanFormPage.test.tsx` green.
- `cd frontend2 && bun run lint:tsc` clean.
- /loans/new renders inventory + borrower pickers; ?itemId pre-filters.
</verification>

<success_criteria>
- LOAN-02 satisfied: create via /loans/new with inventory-entry + borrower pickers.
- Create posts inventory_id (verified by test); ?itemId filters the inventory picker.
- Pickers clamp ≤ 100.
</success_criteria>

<output>
Create `.planning/phases/08-loans/08-03-SUMMARY.md` when done.
</output>
