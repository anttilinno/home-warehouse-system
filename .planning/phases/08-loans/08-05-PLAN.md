---
phase: 08-loans
plan: 05
type: execute
wave: 3
depends_on: ["08-01", "08-04"]
files_modified:
  - frontend2/src/features/loans/components/BorrowerLoanPanels.tsx
  - frontend2/src/features/loans/components/BorrowerLoanPanels.test.tsx
  - frontend2/src/features/loans/hooks/useBorrowerLoans.ts
autonomous: true
requirements: [LOAN-06]
must_haves:
  truths:
    - "BorrowerLoanPanels renders Active Loans + Loan History panels for a borrowerId"
    - "It is a prop-driven COMPONENT only — no borrower route is added this phase (Phase 9 mounts it)"
    - "Active rows expose RETURN + EXTEND (reusing Plan 04 dialogs/mutations); status from server flags"
    - "Backed by GET /borrowers/{borrowerId}/loans, partitioned on is_active"
  artifacts:
    - path: "frontend2/src/features/loans/components/BorrowerLoanPanels.tsx"
      provides: "reusable borrower loan panels (Active + History)"
      min_lines: 40
    - path: "frontend2/src/features/loans/hooks/useBorrowerLoans.ts"
      provides: "per-borrower partitioned loans query"
      contains: "byBorrower"
  key_links:
    - from: "frontend2/src/features/loans/hooks/useBorrowerLoans.ts"
      to: "loansApi.byBorrower"
      via: "queryFn"
      pattern: "loansApi\\.byBorrower"
---

<objective>
Build LOAN-06's borrower-side loan surfaces as a reusable, `borrowerId`-driven component
(`BorrowerLoanPanels`) with an Active Loans panel and a Loan History panel, backed by
`GET /borrowers/{borrowerId}/loans`. No borrower route exists in frontend2 yet (RESEARCH
OQ2 RESOLVED — borrower detail page is Phase 9), so this ships component-only with unit
tests; Phase 9 (BORR-03) mounts it.

Purpose: Closes LOAN-06 within Phase 8 without an orphan route; Phase 9 imports it.
Output: BorrowerLoanPanels, useBorrowerLoans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-loans/08-RESEARCH.md
@.planning/phases/08-loans/08-UI-SPEC.md

<read_first>
- frontend2/src/features/items/components/LoanPanels.tsx — the Active/History panel language to MIRROR (Window titlebars, panel-2 rows, RETURN/EXTEND, empty states), made real in Plan 04.
- frontend2/src/lib/api/loans.ts — loansApi.byBorrower (Plan 01).
- frontend2/src/features/loans/loanStatus.ts — three-way status pill (Plan 01).
- frontend2/src/features/loans/hooks/useLoanMutations.ts + components/{ReturnLoanDialog,ExtendLoanDialog}.tsx — the dialogs/mutations to REUSE (Plan 04).
- frontend2/src/test/msw/loanHandlers.ts — MSW byBorrower handler (Plan 01; add if missing).
</read_first>

<interfaces>
loansApi.byBorrower(ws, borrowerId) → `{ items: Loan[] }` (Plan 01).
loanStatus(l) → `{variant, label}` (Plan 01).
useLoanMutations() → `{ returnLoan, extendLoan, updateLoan }` (Plan 04).
ReturnLoanDialog / ExtendLoanDialog (Plan 04) — reuse for the row actions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useBorrowerLoans — partitioned per-borrower query</name>
  <files>frontend2/src/features/loans/hooks/useBorrowerLoans.ts</files>
  <action>Create useBorrowerLoans(wsId, borrowerId) → useQuery keyed `["loans", wsId, "by-borrower", borrowerId]` (prefix matches SSE invalidation) calling loansApi.byBorrower then partitioning into `{ active, history }` on is_active (mirror loansApi.byItem's partition). `enabled: Boolean(wsId) && Boolean(borrowerId)`. Export a `PartitionedLoans`-shaped result (reuse the type from lib/api/loans.ts).</action>
  <verify>
    <automated>cd frontend2 && bun run lint:tsc 2>&1 | tail -3 && grep -c "by-borrower\|byBorrower" src/features/loans/hooks/useBorrowerLoans.ts</automated>
  </verify>
  <done>tsc clean; key prefix ["loans", wsId, "by-borrower", borrowerId]; partitions on is_active.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BorrowerLoanPanels component + test</name>
  <files>frontend2/src/features/loans/components/BorrowerLoanPanels.tsx, frontend2/src/features/loans/components/BorrowerLoanPanels.test.tsx</files>
  <behavior>
    - `<BorrowerLoanPanels wsId borrowerId />`, borrower with no active loans: Active panel mint titlebar `● Nothing out`.
    - Has active loans: a list of active-loan rows (a borrower may hold several), each row = item name + due chip (neutral `due in {n}d` or danger `⚠ −{n}d` when is_overdue) + status pill (loanStatus) + RETURN + EXTEND actions; item name links to /items/{item_id}.
    - Loan History panel: returned loans (item name · loaned→returned · Returned pill); empty → `NO LOAN HISTORY` / `This borrower hasn't returned anything yet.`
    - RETURN fires the Plan-04 return mutation; status/overdue read server flags.
    - NO route is registered (component-only).
  </behavior>
  <action>Create BorrowerLoanPanels.tsx mirroring the LoanPanels language (UI-SPEC §7): two Windows. Active Loans panel — pink titlebar when ≥1 active loan, mint when none; map active rows to panel-2 rows with item-name link, a due chip (neutral or danger ⚠ −{n}d from is_overdue — NOT client date math), the loanStatus pill, and RETURN + EXTEND buttons opening the Plan-04 ReturnLoanDialog/ExtendLoanDialog (local open state per row). Loan History panel — reuse the LoanHistoryList language (item name · loaned→returned · Returned pill; the three-way loanStatus pill). Use useBorrowerLoans for data + useLoanMutations for actions. Mutations invalidate the borrower key + ["loans", wsId] + the affected item's by-item key (onSettled in useLoanMutations already invalidates the ["loans", wsId] prefix, which covers all three — confirm the by-borrower + by-item keys share that prefix). Create BorrowerLoanPanels.test.tsx (MSW byBorrower handler with active+overdue+returned fixtures) covering the behavior cases — especially that it renders WITHOUT any router route (mount the component directly in a MemoryRouter test wrapper) and that RETURN fires the mutation.</action>
  <verify>
    <automated>cd frontend2 && bun run test src/features/loans/components/BorrowerLoanPanels.test.tsx 2>&1 | tail -8 && grep -c "path=\"borrowers" src/routes/index.tsx</automated>
  </verify>
  <done>Test green; Active + History panels render from byBorrower; RETURN fires; NO borrower route added (the routes grep returns 0).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (byBorrower reads + mutations) | borrowerId-scoped read; server re-scopes by workspace_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Information Disclosure | loansApi.byBorrower | mitigate | Call is `/workspaces/{wsId}/borrowers/{id}/loans`; backend re-scopes by workspace_id and does NOT trust the borrowerId as an auth signal (V4). |
| T-08-OVERDUE | Spoofing | borrower due chip | mitigate | Overdue chip reads is_overdue server flag, never client date math (override 2). |
| T-08-04 | Tampering | row mutations | mitigate | Reuses Plan-04 useLoanMutations (snapshot/revert on 4xx). |
</threat_model>

<verification>
- `cd frontend2 && bun run test src/features/loans/` green.
- `cd frontend2 && bun run lint:tsc && bun run build` clean.
- No borrower route added (grep confirms).
</verification>

<success_criteria>
- LOAN-06 satisfied: reusable BorrowerLoanPanels (Active + History) component-only.
- Backed by byBorrower; status + overdue from server flags; RETURN/EXTEND reuse Plan 04.
- No orphan route (Phase 9 mounts it).
</success_criteria>

<output>
Create `.planning/phases/08-loans/08-05-SUMMARY.md` when done.
</output>
