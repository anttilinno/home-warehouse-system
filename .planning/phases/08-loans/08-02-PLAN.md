---
phase: 08-loans
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - frontend2/src/features/loans/LoansListPage.tsx
  - frontend2/src/features/loans/LoansListPage.test.tsx
  - frontend2/src/features/loans/hooks/useLoansQuery.ts
  - frontend2/src/components/layout/Sidebar.tsx
  - frontend2/src/routes/index.tsx
  - frontend2/src/features/loans/components/LoanRowActions.tsx
autonomous: true
requirements: [LOAN-01]
must_haves:
  truths:
    - "User can open /loans and see Active/Overdue/History tabs (RetroTabs, ?tab= URL-driven, default active)"
    - "Active tab hits GET /loans/active, Overdue hits /loans/overdue, History hits GET /loans client-filtered !is_active"
    - "Each row shows item / borrower / loaned / due / status pill (info/danger/ok from server flags)"
    - "Overdue rows are signalled three ways: bg-danger-bg tint + Overdue pill word + ⚠ −{n}d due chip (not color-only)"
    - "EXPORT CSV button downloads the current tab's rows client-generated (no backend export call)"
    - "Sidebar Loans entry navigates to /loans"
  artifacts:
    - path: "frontend2/src/features/loans/LoansListPage.tsx"
      provides: "Tabbed loans list page"
      min_lines: 80
    - path: "frontend2/src/features/loans/hooks/useLoansQuery.ts"
      provides: "tab→endpoint selector keyed ['loans', wsId, tab]"
      contains: "loans/active"
  key_links:
    - from: "frontend2/src/features/loans/hooks/useLoansQuery.ts"
      to: "loansApi.active/overdue/list"
      via: "tab selector"
      pattern: "loansApi\\.(active|overdue|list)"
    - from: "frontend2/src/routes/index.tsx"
      to: "LoansListPage"
      via: "route /loans"
      pattern: "path=\"loans\""
---

<objective>
Build the `/loans` tabbed list page (LOAN-01): RetroTabs folders Active/Overdue/History
(URL-driven `?tab=`), a RetroTable of item/borrower/loaned/due/status rows, overdue-row
highlighting (three non-color cues), per-tab client-generated CSV export, and the Sidebar
`/loans` link. Row actions (return/extend/edit) are seams here — Plan 04 wires the mutation
dialogs; this plan ships the table, tabs, filter, export, and empty states.

Purpose: The primary loan browsing surface and the route the Sidebar + scan flow target.
Output: LoansListPage, useLoansQuery, route registration, Sidebar enable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-loans/08-RESEARCH.md
@.planning/phases/08-loans/08-UI-SPEC.md

<read_first>
- frontend2/src/features/inventory/InventoryListPage.tsx — the density/FilterBar/RetroTable/RetroPagination/useShortcuts/render-loop-guard pattern to MIRROR (tRef ref, stable .mutate destructure, workspaceName).
- frontend2/src/features/items/ItemDetailPage.tsx:174-294 — the RetroTabs `tabs: RetroTab[]` + `value`/`onChange` usage pattern.
- frontend2/src/lib/api/loans.ts — the Plan-01-extended loansApi (active/overdue/list).
- frontend2/src/features/loans/loanStatus.ts — status pill derivation (Plan 01).
- frontend2/src/features/loans/loanCsv.ts — loansToCsvBlob + triggerCsvDownload (Plan 01).
- frontend2/src/test/msw/loanHandlers.ts — MSW handlers for the test (Plan 01).
- frontend2/src/components/layout/Sidebar.tsx:144 — the disabled Loans NavItem to enable.
</read_first>

<interfaces>
RetroTabs (from @/components/retro):
```typescript
interface RetroTab { id: string; label: ReactNode; content: ReactNode; disabled?: boolean }
function RetroTabs({ tabs, value, onChange }: { tabs: RetroTab[]; value: string; onChange: (id: string) => void });
```
loansApi (Plan 01): `active(ws)`, `overdue(ws)`, `list(ws, page?, limit?)` → `{ items: Loan[] }`.
loanStatus(l) → `{ variant: "ok"|"danger"|"info"; label: string }`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useLoansQuery — tab→endpoint selector</name>
  <files>frontend2/src/features/loans/hooks/useLoansQuery.ts</files>
  <action>Create the hook reading the `?tab=` search param (default "active"). Map tab→endpoint: `overdue`→loansApi.overdue, `history`→loansApi.list, else loansApi.active (Pattern 1 in 08-RESEARCH). Query key MUST be `["loans", wsId, tab]` (the `["loans", wsId]` prefix matches the existing SSE invalidation rule — do NOT use exact:true, do NOT append to invalidationMap). `enabled: Boolean(wsId)`. For the history tab, filter the response `items` to `!is_active` client-side (override 4 — there is no /loans/returned endpoint). Return `{ items: Loan[], isLoading, isError, tab }`. Read wsId from useWorkspace. The list envelope is bare `{items}` with NO pagination metadata (Pitfall 3) — do not read total/total_pages/page from the response.</action>
  <verify>
    <automated>cd frontend2 && bun run lint:tsc 2>&1 | tail -3 && grep -c "loans/active\|loansApi.active\|\"loans\"" src/features/loans/hooks/useLoansQuery.ts</automated>
  </verify>
  <done>tsc clean; key prefix is ["loans", wsId, tab]; history filters !is_active; tab default active.</done>
</task>

<task type="auto">
  <name>Task 2: LoansListPage — tabs, table, overdue highlight, CSV, route, sidebar</name>
  <files>frontend2/src/features/loans/LoansListPage.tsx, frontend2/src/routes/index.tsx, frontend2/src/components/layout/Sidebar.tsx</files>
  <action>Create LoansListPage in a single mint-titled Window `LOANS — {workspace}` (mirror InventoryListPage's workspaceName + Window usage). Render a RetroTabs row (ACTIVE / OVERDUE / HISTORY; Silkscreen labels via Trans) whose `value` = current `?tab=` and `onChange` writes `?tab=` via setSearchParams (URL-driven per UI-SPEC R3, default active). Each tab's content uses useLoansQuery for that tab. Below the tabs render a FilterBar (search filters current rows by item OR borrower name substring — client-side; show `{n} loans` count) with the EXPORT CSV BevelButton at the right end (calls loansToCsvBlob(currentRows) + triggerCsvDownload with filename `loans-{tab}.csv`; disabled with a `Nothing to export.` toast when the tab is empty) and a mint `⊕ NEW LOAN` primary CTA → navigate `/loans/new`. Render a RetroTable with columns Item / Borrower / Loaned / Due / Status (per UI-SPEC §1 column set): Item cell font-semibold, row onClick → `/items/{loan.item.id}`; Due column renders a neutral `due {date}`/`due in {n}d` chip normally, the History tab shows `returned {date}`, and OVERDUE rows render a danger `⚠ −{n}d` chip; Status cell = StatusPill with variant+label from loanStatus(loan). Overdue rows (loan.is_overdue) get `bg-danger-bg` row tint (the THREE-cue highlight: tint + Overdue pill word + ⚠ chip — UI-SPEC §Overdue-row highlight, not color-only). Row action buttons (RETURN / EXTEND / ↧ EDIT) are rendered as a `<LoanRowActions loan={loan} tab={tab} />` slot with `stopPropagation`. Create `frontend2/src/features/loans/components/LoanRowActions.tsx` NOW as a real exported stub file (props `{ loan: Loan; tab?: string }`, renders the three BevelButtons disabled-with-hint so the column exists) and import it into LoansListPage from that path. Plan 04 (wave 3, after this merges) OVERWRITES the file body with the wired dialogs — same path, same props, so LoansListPage needs NO change from Plan 04 (this is why the stub is a real file, not an inline component — avoids Plan 04 having to edit LoansListPage; plan-checker handoff note). Per-tab empty states via RetroEmptyState (copy from UI-SPEC Copywriting Contract). Register `useShortcuts("loans", [{key:"N", action: goNew}, {key:"/", action: focusSearch}])` using the InventoryListPage render-loop guard (tRef + stable useCallback deps — the 4× prior bug). Then: in routes/index.tsx add literal route `<Route path="loans" element={<LoansListPage />} />` under the AppShell branch (before any `loans/:id` param route). NOTE: Plan 03 registers the sibling `loans/new` route, but it now runs in a LATER wave (wave 3, depends_on 08-02) so the two route edits merge sequentially — no parallel-worktree conflict on routes/index.tsx (plan-checker BLOCKER 1, resolved via serialize). In Sidebar.tsx add `to="/loans"` to the existing Loans NavItem (line 144).</action>
  <verify>
    <automated>cd frontend2 && bun run lint:tsc 2>&1 | tail -3 && grep -c "path=\"loans\"" src/routes/index.tsx && grep -c "to=\"/loans\"" src/components/layout/Sidebar.tsx</automated>
  </verify>
  <done>tsc clean; /loans route registered; Sidebar Loans links to /loans; page renders three tabs + table + CSV button.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: LoansListPage component test (MSW)</name>
  <files>frontend2/src/features/loans/LoansListPage.test.tsx</files>
  <behavior>
    - Renders at `/loans` (default tab active) → Active tab fetches /loans/active, rows show item + borrower names from the MSW fixture.
    - Switching to the Overdue tab (click OVERDUE) fetches /loans/overdue; the overdue fixture row carries the danger `Overdue` pill word AND a `⚠` chip AND the row has the danger-bg tint class (assert at least the pill word + ⚠ glyph — the two non-color cues).
    - History tab fetches GET /loans and shows only !is_active rows (the returned fixture), each with a `Returned` pill.
    - Clicking EXPORT CSV with rows present triggers a download (spy on URL.createObjectURL or triggerCsvDownload); with an empty tab it is disabled / fires the Nothing-to-export toast.
  </behavior>
  <action>Create LoansListPage.test.tsx using the project's MSW + RTL render harness (read a sibling list-page test e.g. InventoryListPage.test.tsx for the renderWithProviders + MemoryRouter + MSW server setup convention). Register the Plan-01 loanHandlers. Cover the four behavior cases. Use locale-agnostic matchers (`/overdue/i`) consistent with the repo's i18n test discipline. Assert tab→endpoint by either MSW request interception or by the rendered fixture rows differing per tab.</action>
  <verify>
    <automated>cd frontend2 && bun run test src/features/loans/LoansListPage.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <done>LoansListPage.test.tsx passes; overdue non-color cues asserted; history filters !is_active; CSV export exercised.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | /loans/active|overdue + /loans reads, all wsId-scoped |
| fetched rows → CSV | borrower/notes cells → spreadsheet (escaped in Plan 01 util) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Information Disclosure | useLoansQuery endpoints | mitigate | wsId from useWorkspace SSOT; all reads `/workspaces/{wsId}/...`; server re-scopes (V4). |
| T-08-XSS | Tampering | RetroTable cells (item/borrower name) | accept | React auto-escapes; no dangerouslySetInnerHTML. |
| T-08-CSV | Tampering | EXPORT CSV | mitigate | Escaping lives in Plan 01 loanCsv (T-08-CSV); this plan only invokes it with current rows. |
</threat_model>

<verification>
- `cd frontend2 && bun run test src/features/loans/` green.
- `cd frontend2 && bun run lint:tsc && bun run lint:imports && bun run build` clean.
- Navigating to /loans shows three working tabs; overdue rows visibly tinted + ⚠ + pill.
</verification>

<success_criteria>
- LOAN-01 satisfied: tabbed Active/Overdue/History RetroTable with item/borrower/due/status.
- Overdue rows carry three non-color cues; status from server flags.
- Per-tab CSV is client-generated; Sidebar + /loans route wired.
</success_criteria>

<output>
Create `.planning/phases/08-loans/08-02-SUMMARY.md` when done.
</output>
