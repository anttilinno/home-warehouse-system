---
phase: 08-loans
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - frontend2/src/features/loans/hooks/useLoanMutations.ts
  - frontend2/src/features/loans/components/ReturnLoanDialog.tsx
  - frontend2/src/features/loans/components/ExtendLoanDialog.tsx
  - frontend2/src/features/loans/components/EditLoanDialog.tsx
  - frontend2/src/features/loans/components/LoanRowActions.tsx
  - frontend2/src/features/loans/hooks/useLoanMutations.test.tsx
  - frontend2/src/features/items/components/LoanPanels.tsx
  - frontend2/src/features/items/components/LoanPanels.test.tsx
autonomous: true
requirements: [LOAN-03, LOAN-04, LOAN-05]
must_haves:
  truths:
    - "User can mark a loan returned via a blue confirm dialog; it optimistically moves to History and reverts on error"
    - "User can extend a loan's due date via a blue dialog (PATCH /loans/{id}/extend)"
    - "User can edit a loan's due date + notes via a blue dialog (PATCH /loans/{id})"
    - "Item detail Active Loan panel has live RETURN + EXTEND (was disabled stub) and a ⊕ LOAN THIS ITEM CTA when available"
    - "All loan mutations are optimistic with revert-on-error and invalidate ['loans', wsId] + the item by-item key"
  artifacts:
    - path: "frontend2/src/features/loans/hooks/useLoanMutations.ts"
      provides: "return/extend/update optimistic mutations with revert"
      contains: "cancelQueries"
    - path: "frontend2/src/features/items/components/LoanPanels.tsx"
      provides: "real (interactive) item-detail loan panels"
      contains: "RETURN"
  key_links:
    - from: "frontend2/src/features/loans/hooks/useLoanMutations.ts"
      to: "loansApi.return/extend/update"
      via: "mutationFn"
      pattern: "loansApi\\.(return|extend|update)"
    - from: "frontend2/src/features/items/components/LoanPanels.tsx"
      to: "useLoanMutations"
      via: "RETURN/EXTEND handlers"
      pattern: "useLoanMutations"
---

<objective>
Wire the loan lifecycle mutations (LOAN-03 return, LOAN-04 edit + extend) as optimistic
mutations with revert, surface them through the return/extend/edit dialogs, fill the
LoanRowActions seam Plan 02 left in the list, and make the Phase 7 item-detail LoanPanels
stub REAL (LOAN-05) — live RETURN + EXTEND + a ⊕ LOAN THIS ITEM CTA, three-way status pill.

Purpose: The interactive heart of the phase — turns the read-only list + stub panels into
a working lifecycle. ItemDetailPage is the single-writer for this wave.
Output: useLoanMutations, three dialogs, LoanRowActions, real LoanPanels.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-loans/08-RESEARCH.md
@.planning/phases/08-loans/08-UI-SPEC.md

<read_first>
- frontend2/src/features/inventory/hooks/useInventoryMutations.ts — the optimisticPatch/restore snapshot pattern to MIRROR (cancelQueries → getQueriesData snapshot → setQueriesData patch → onError restore + retroToast → onSettled invalidate).
- frontend2/src/features/inventory/components/MoveDialog.tsx — the RetroDialog (blue titlebar, useModalStack ESC, optimistic submit) dialog pattern to MIRROR for extend/edit.
- frontend2/src/features/items/components/LoanPanels.tsx — the STUB to make real (ActiveLoanPanel disabled RETURN + Phase-8 hint; LoanHistoryList two-way pill; useItemLoans).
- frontend2/src/features/loans/loanStatus.ts — three-way pill derivation (Plan 01).
- frontend2/src/lib/api/loans.ts — return/extend/update (Plan 01).
- frontend2/src/components/retro/overlay (RetroConfirmDialog: titlebarVariant, confirmVariant, confirmLabel, onConfirm, onCancel, disabled).
</read_first>

<interfaces>
loansApi (Plan 01): `return(ws,id)→Loan`, `extend(ws,id,new_due_date)→Loan`, `update(ws,id,{due_date?,notes?})→Loan`.
RetroConfirmDialog props: `{ open, title, titlebarVariant?, confirmVariant?, confirmLabel, cancelLabel?, onConfirm, onCancel, onClose, disabled? }`.
RetroDialog props (overlay): standard open/onClose modal-stack dialog (see MoveDialog for usage).
loanStatus(l) → `{variant:"ok"|"danger"|"info"; label}`.
useItemLoans(wsId,itemId) → useQuery<PartitionedLoans> keyed ["loans", wsId, "by-item", itemId] (existing in LoanPanels.tsx — keep).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useLoanMutations — optimistic return/extend/update with revert</name>
  <files>frontend2/src/features/loans/hooks/useLoanMutations.ts, frontend2/src/features/loans/hooks/useLoanMutations.test.tsx</files>
  <behavior>
    - `returnLoan.mutate(id)` optimistically flips the matching loan's is_active→false across all ["loans", wsId] caches; onError restores the snapshot + fires a persistent retroToast.error; onSettled invalidates ["loans", wsId].
    - `extendLoan.mutate({id, new_due_date})` optimistically patches due_date (and, since the server recomputes, leaves is_overdue to the re-invalidation); onError restores + toasts.
    - `updateLoan.mutate({id, due_date?, notes?})` optimistically patches those fields; onError restores + toasts.
    - All three snapshot via getQueriesData on the ["loans", wsId] prefix and restore on error (no client-trusted state survives a 4xx).
  </behavior>
  <action>Create useLoanMutations.ts mirroring useInventoryMutations EXACTLY (cancelQueries → getQueriesData(prefix) snapshot → setQueriesData(prefix) map-patch the matching loan by id → onError restore + retroToast.error → onSettled invalidate). prefix = `["loans", wsId]`. The list-cache shape is `{ items: Loan[] } & Record<string, unknown>` (bare {items}, Pitfall 4 — guard Array.isArray(old.items)). Three mutations: returnLoan (loansApi.return, patch `{is_active:false, returned_at: <now ISO>}`), extendLoan (loansApi.extend, patch `{due_date}`), updateLoan (loansApi.update, patch `{due_date, notes}`). Error toasts per UI-SPEC Copywriting (`Couldn't return/extend/save this loan.`). Read wsId from useWorkspace. Create useLoanMutations.test.tsx (read useInventoryMutations test if one exists, else mirror the RQ optimistic test harness) covering: return optimistic flip + revert-on-error, extend patch, update patch — using the Plan 01 loanHandlers (override one to 4xx for the revert case).</action>
  <verify>
    <automated>cd frontend2 && bun run test src/features/loans/hooks/useLoanMutations.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <done>Test green; all three mutations snapshot+revert on error; prefix ["loans", wsId]; onSettled invalidates.</done>
</task>

<task type="auto">
  <name>Task 2: Return/Extend/Edit dialogs + LoanRowActions</name>
  <files>frontend2/src/features/loans/components/ReturnLoanDialog.tsx, frontend2/src/features/loans/components/ExtendLoanDialog.tsx, frontend2/src/features/loans/components/EditLoanDialog.tsx, frontend2/src/features/loans/components/LoanRowActions.tsx</files>
  <action>Create three dialogs (all BLUE titlebars — UI-SPEC R9/R10/R11; return is a reversible completion, NOT pink). ReturnLoanDialog: RetroConfirmDialog `titlebarVariant="blue"` `confirmVariant="primary"`, title `RETURN LOAN?`, body `Mark "{item}" returned by {borrower}?`, onConfirm → returnLoan.mutate(loan.id); success toast `Returned — moved to History.`. ExtendLoanDialog: RetroDialog blue (mirror MoveDialog), context line `{item} — currently due {date or "no due date"}.`, a required New due date RetroInput type=date (default = current due + 7d or today + 7d), onConfirm → extendLoan.mutate({id, new_due_date}); the body shape is `{new_due_date}` per Plan 01 loansApi.extend. EditLoanDialog: RetroDialog blue, context `{item} — loaned to {borrower}.`, Due date (optional, clearing removes) + Notes textarea, onConfirm → updateLoan.mutate({id, due_date, notes}). Each routes ESC through useModalStack (see MoveDialog) and restores focus to its opener. Then create LoanRowActions.tsx — the component that REPLACES the Plan 02 placeholder: given `{ loan }`, renders small BevelButtons RETURN / EXTEND / ↧ EDIT (per-tab: History rows render no lifecycle actions — accept a `readOnly`/`tab` prop), each opening its dialog (local open state), with stopPropagation so the row click doesn't navigate. Export LoanRowActions so Plan 02's LoansListPage imports it (Plan 02 left a placeholder; THIS plan owns the real one — update the LoansListPage import only if the placeholder lived inline; if Plan 02 exported a placeholder from this same path, simply implement it here — coordinate by keeping the file path `components/LoanRowActions.tsx` as the single source).</action>
  <verify>
    <automated>cd frontend2 && bun run lint:tsc 2>&1 | tail -3 && grep -l "titlebarVariant=\"blue\"\|titlebarVariant='blue'" src/features/loans/components/ReturnLoanDialog.tsx && grep -c "new_due_date" src/features/loans/components/ExtendLoanDialog.tsx</automated>
  </verify>
  <done>tsc clean; return dialog is blue (not pink); extend sends new_due_date; LoanRowActions wires all three.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make item-detail LoanPanels real (LOAN-05)</name>
  <files>frontend2/src/features/items/components/LoanPanels.tsx, frontend2/src/features/items/components/LoanPanels.test.tsx</files>
  <behavior>
    - ActiveLoanPanel, no active loan: mint titlebar `● Available` PLUS a new `⊕ LOAN THIS ITEM` BevelButton → /loans/new?itemId={itemId} (the stub had no CTA).
    - ActiveLoanPanel, on loan: pink titlebar `● On loan to {borrower}` + due date PLUS live RETURN (opens ReturnLoanDialog) + EXTEND (opens ExtendLoanDialog) buttons (replacing the disabled RETURN + "arrive in Phase 8" hint).
    - ActiveLoanPanel, overdue active loan: pink panel with a danger `⚠ Overdue by {n}d` due chip + `This loan is overdue.` line (use loan.is_overdue, NOT client date math) + the same RETURN/EXTEND.
    - LoanHistoryList: pill mapping upgraded from two-way OUT/RETURNED to the three-way loanStatus (Active/Overdue/Returned).
    - Mutations invalidate BOTH the by-item key AND ["loans", wsId].
  </behavior>
  <action>Edit LoanPanels.tsx: keep useItemLoans + PartitionedLoans. ActiveLoanPanel gains the itemId prop (so the CTA + dialogs can target it) — thread it from the LoanPanels wrapper and from ItemDetailPage's existing `<ActiveLoanPanel active={activeLoans} />` callsite (ItemDetailPage is THIS plan's single-writer for the wave — update the callsite to pass itemId; do not relayout). Replace the disabled RETURN block with: when no active loan, render the `⊕ LOAN THIS ITEM` button (navigate /loans/new?itemId); when on loan, render live RETURN + EXTEND opening the Plan-04 dialogs (local open state) operating on active[0]; when active[0].is_overdue, show the danger overdue chip + line ABOVE the buttons. Replace LoanHistoryList's `is_active ? info "OUT" : ok "RETURNED"` pill with `loanStatus(loan)` (three-way). Use the Plan-01 loanStatus + the Plan-04 useLoanMutations/dialogs. Extend LoanPanels.test.tsx (it exists read-only) with: RETURN now enabled and firing the return mutation, the ⊕ LOAN THIS ITEM CTA presence on available, and the three-way history pill. Keep the existing passing assertions that still hold.</action>
  <verify>
    <automated>cd frontend2 && bun run test src/features/items/components/LoanPanels.test.tsx 2>&1 | tail -8 && grep -vE '^\s*//|^\s*\*' src/features/items/components/LoanPanels.tsx | grep -c "disabled aria-disabled\|arrive in Phase 8"</automated>
  </verify>
  <done>LoanPanels.test.tsx passes; the disabled-RETURN stub + Phase-8 hint are gone (grep returns 0); ⊕ LOAN THIS ITEM + live RETURN/EXTEND present; three-way pill.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (return/extend/update) | mutations wsId+id scoped; server authoritative for state transitions + overdue recompute |
| optimistic cache → UI | a 4xx must not leave client-fabricated state visible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | useLoanMutations optimistic patch | mitigate | onError restores the pre-mutation snapshot; onSettled re-invalidates so the SERVER value (incl. recomputed is_overdue) is authoritative — no client-trusted state survives a 4xx. |
| T-08-OVERDUE | Spoofing | LoanPanels overdue display | mitigate | Overdue chip/line read loan.is_overdue (server flag), never client due-date math (override 2). |
| T-08-05 | Information Disclosure | return/extend/update endpoints | mitigate | All calls `/workspaces/{wsId}/loans/{id}/...`; server re-scopes by workspace_id (V4). |
</threat_model>

<verification>
- `cd frontend2 && bun run test src/features/loans/ src/features/items/components/LoanPanels.test.tsx` green.
- `cd frontend2 && bun run lint:tsc && bun run lint:imports && bun run build` clean.
- Item detail RETURN/EXTEND fire; available item shows ⊕ LOAN THIS ITEM.
</verification>

<success_criteria>
- LOAN-03/04 satisfied: return (blue confirm, optimistic→History), extend + edit (blue dialogs).
- LOAN-05 satisfied: item-detail LoanPanels made real; stub disabled-RETURN gone.
- All mutations optimistic + revert; overdue from server flag.
</success_criteria>

<output>
Create `.planning/phases/08-loans/08-04-SUMMARY.md` when done.
</output>
