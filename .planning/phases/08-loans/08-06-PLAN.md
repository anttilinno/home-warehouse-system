---
phase: 08-loans
plan: 06
type: execute
wave: 4
depends_on: ["08-02", "08-03", "08-04"]
files_modified:
  - frontend2/e2e/loans-lifecycle.spec.ts
autonomous: true
requirements: [LOAN-01, LOAN-02, LOAN-03]
must_haves:
  truths:
    - "A single batched Playwright spec drives the real backend: create loan → appears in Active → mark returned → moves to History"
    - "The spec logs in ONCE and reuses the cookie (one login, to respect the 20/min auth limiter)"
    - "It seeds an inventory entry + borrower via the inherited cookie before creating the loan"
  artifacts:
    - path: "frontend2/e2e/loans-lifecycle.spec.ts"
      provides: "live loan lifecycle E2E"
      contains: "tab=history"
  key_links:
    - from: "frontend2/e2e/loans-lifecycle.spec.ts"
      to: "real backend /loans"
      via: "page.request + UI"
      pattern: "loans"
---

<objective>
Add ONE batched Playwright spec exercising the real loan lifecycle against the live
backend + Postgres: log in once, seed an inventory entry + borrower, create a loan via
the UI (inventory + borrower pickers), assert it appears in the Active tab, mark it
returned via the confirm dialog, and assert it moved to History. Single login to dodge the
documented 20/min auth limiter (Pitfall 5).

Purpose: The phase-gate regression guard at the HTTP boundary (POL-01 pattern, applied to
loans). Catches frontend OR backend reverts of the lifecycle contract.
Output: frontend2/e2e/loans-lifecycle.spec.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-loans/08-RESEARCH.md
@./CLAUDE.md

<read_first>
- frontend2/e2e/login-dashboard.spec.ts — the existing live spec: real /login flow, cookie inherited by page + page.request, the auth contract. MIRROR its login + seeding style.
- ./CLAUDE.md §E2E Tests — the runbook, seeder creds (E2E_USER/E2E_PASS), the auth contract (single submit button, cookie inheritance), and the 20/min limiter warning.
- frontend2/playwright.config.ts — chromium + firefox projects, baseURL :5173, no webServer (expects the dev stack running).
- frontend2/src/lib/api/loans.ts — the create body shape (inventory_id) for the seeding/assert reference.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: loans-lifecycle.spec.ts — batched live lifecycle</name>
  <files>frontend2/e2e/loans-lifecycle.spec.ts</files>
  <action>Create ONE spec (a single test.describe with ONE test, or a serial-ordered set sharing one login) per Pitfall 5 — log in exactly once via the real /login flow (mirror login-dashboard.spec.ts: fill Email + Password, click the submit button, wait for the dashboard; the access_token cookie is then inherited by both page and page.request). Resolve the workspace id from the authenticated context (read it the same way login-dashboard.spec does, e.g. from the workspaces query or the URL/storage). Seed prerequisites via cookie-authenticated page.request.post: a borrower (`POST /api/workspaces/{wsId}/borrowers`) and ensure at least one inventory entry exists for a known item (create one via the inventory endpoint, or reuse a seeded entry — capture its inventory_id). Then drive the UI: navigate /loans/new, pick the seeded inventory entry + borrower in the RetroSelects, submit; assert the new loan appears in the Active tab (/loans?tab=active) showing the borrower name; click RETURN on that row → confirm in the dialog; assert the loan leaves Active and appears under /loans?tab=history with a Returned status. Use unique per-run identifiers (e.g. borrower name `E2E-loan-${Date.now()}`) so reruns don't collide; best-effort cleanup in a finally/afterAll. Keep all backend calls on the inherited cookie (no manual token plumbing). Use locale-agnostic text matchers consistent with the repo (e.g. `/returned/i`).</action>
  <verify>
    <automated>cd frontend2 && npx playwright test --list e2e/loans-lifecycle.spec.ts 2>&1 | tail -15</automated>
  </verify>
  <done>`playwright test --list` discovers the spec (in-plan gate — full run is the phase gate, run isolated per Pitfall 5 to avoid the auth limiter); the spec logs in once, seeds borrower+inventory, creates→active→returns→history.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E client→live backend | real cookie-JWT session; seeds + asserts cross the real HTTP + Postgres boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-E2E | Denial of Service (self) | auth rate limiter | mitigate | One login per spec, cookie reused for all seeding (Pitfall 5); run isolated, not batched with other auth-heavy specs. |
| T-08-01 | Information Disclosure | seeded test data | accept | Test-only data in the dev/test DB; unique per-run ids + best-effort cleanup. |
</threat_model>

<verification>
- `cd frontend2 && npx playwright test --list e2e/loans-lifecycle.spec.ts` discovers the spec.
- Phase gate (manual / isolated, per runbook): `E2E_USER=... E2E_PASS=... bun run test:e2e e2e/loans-lifecycle.spec.ts` green against the running dev stack.
</verification>

<success_criteria>
- One batched spec: create → Active → return → History, single login (limiter-safe).
- Seeds borrower + inventory via the inherited cookie; locale-agnostic matchers.
</success_criteria>

<output>
Create `.planning/phases/08-loans/08-06-SUMMARY.md` when done.
</output>
