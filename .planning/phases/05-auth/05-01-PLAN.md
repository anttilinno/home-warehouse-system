---
phase: 05-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/api/middleware/auth.go
  - backend/internal/api/router.go
  - backend/tests/integration/auth_test.go
  - docs/audit/BACKEND-SECURITY.md
autonomous: true
requirements: [AUTH-12, AUTH-07]
must_haves:
  truths:
    - "After logout, replaying the old refresh token returns 401 (revocation enforced server-side)"
    - "A revoked refresh token cannot mint a new session (F3 — no legacy-token re-creation)"
    - "The sessions list marks the active session is_current=true and revoke-all-others succeeds"
  artifacts:
    - path: backend/tests/integration/auth_test.go
      provides: "TestLogout_RevokesSession + TestRefresh_RevokedSession_NoNewSession + TestSessions_CurrentSessionMarked integration tests"
      contains: "func TestLogout_RevokesSession"
    - path: backend/internal/api/middleware/auth.go
      provides: "JWTAuth resolves the refresh-cookie session and calls WithCurrentSessionID"
      contains: "WithCurrentSessionID"
  key_links:
    - from: backend/internal/api/middleware/auth.go
      to: session.Service.FindByTokenHash
      via: "refresh_token cookie hash lookup in JWTAuth"
      pattern: "FindByTokenHash"
    - from: backend/tests/integration/auth_test.go
      to: POST /auth/logout
      via: "cookie-bearing logout request then refresh replay"
      pattern: "refresh_token"
---

<objective>
Lock the AUTH-12 logout-revocation fix (already shipped in commit f49e4b48) behind a Go integration regression guard, and close the real undocumented backend gap that blocks AUTH-07: JWTAuth never populates the current-session id, so `is_current` is always false and "revoke all other sessions" 400s.

Purpose: AUTH-12's server-side revocation is the security spine of this phase — without an integration test, any future revert silently re-opens revoked sessions. AUTH-07's sessions UI (Plan 05) is non-functional until the current-session id flows through context.

Output: a cookie-bearing logout-revocation integration test (+ F3 no-resurrection guard + is_current guard), a `WithCurrentSessionID` wiring in JWTAuth, and the corrected audit doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-auth/05-RESEARCH.md
@.planning/phases/05-auth/05-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Extracted from backend source — executor uses these directly, no exploration needed. -->

From backend/internal/domain/auth/user/handler.go (logout/refresh — ALREADY FIXED, f49e4b48; DO NOT change handler logic, only test it):
- `refreshToken(ctx, input)` — validates refresh JWT, then `session.HashToken(refresh)` → `sessionSvc.FindByTokenHash`; on error returns `huma.Error401Unauthorized("session has been revoked")`. NO legacy-token fallback (F3 fix). On success rotates session via `UpdateActivity`.
- `logout(ctx, input)` — reads `input.RefreshToken` (`cookie:"refresh_token"` required:"false"), `session.HashToken` it, `FindByTokenHash` → `Revoke(ctx, sess.UserID(), sess.ID())`, clears both cookies.
- `RefreshTokenInput.Body.RefreshToken string json:"refresh_token"`; refresh route is `POST /auth/refresh` (public).
- Login/register response Body: `{ token string, refresh_token string }` + sets `access_token`/`refresh_token` cookies.

From backend/internal/domain/auth/session/service.go:
- `HashToken(token string) string` (package-level), `FindByTokenHash`, `Revoke(ctx, userID, sessionID)`, `RevokeAllExcept(ctx, userID, currentSessionID)`, `RevokeAll`, `UpdateActivity`.

From backend/internal/domain/auth/session/handler.go:
- `SessionResponse{ id, device_info, ip_address?, last_active_at, created_at, is_current }`.
- list reads `currentSessionID, _ := appMiddleware.GetCurrentSessionID(ctx)`; `IsCurrent: s.ID() == currentSessionID`.
- revoke-all-others returns `huma.Error400BadRequest("current session not found")` when `GetCurrentSessionID` returns `ok=false`.
- Routes registered via `sessionHandler.RegisterRoutes(protectedAPI)`: `GET /users/me/sessions`, `DELETE /users/me/sessions/{id}`, `DELETE /users/me/sessions`.

From backend/internal/api/middleware/auth.go:
- `JWTAuth(jwtService)` validates access token from Authorization header / `access_token` cookie / `token` query param; sets `UserContextKey`. It NEVER calls `WithCurrentSessionID` (the gap).
- `WithCurrentSessionID(ctx, sessionID uuid.UUID) context.Context` and `GetCurrentSessionID(ctx) (uuid.UUID, bool)` exist (dead-coded today).

From backend/tests/integration/setup.go (the harness — Bearer-only, NO cookie jar):
- `NewTestServer(t)` → `{ Router, Pool, Server *httptest.Server, Token }`; `ts.Server.URL` is the base.
- `ts.Post/Get/Patch/Delete`, `ts.SetToken(token)` (sets `Authorization: Bearer`), `ParseResponse[T]`, `RequireStatus`, `ParseErrorResponse`.
- The harness sets ONLY `Authorization: Bearer` — it does NOT send cookies. Logout/refresh read the `refresh_token` COOKIE, so a cookie-bearing request helper is required (Pitfall 3).
- `cfg` has NO session service wired? — NO: `api.NewRouter(pool, cfg)` builds the full router incl. sessionSvc. The integration server exercises the real logout/refresh/session path.

From backend/internal/api/router.go:
- Protected group: `r.Group` with `r.Use(appMiddleware.JWTAuth(jwtService))`, then `protectedAPI := humachi.New(r, protectedConfig)`, then `sessionHandler.RegisterRoutes(protectedAPI)`. `sessionSvc` + `jwtService` are in scope at that point.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire WithCurrentSessionID into the protected request path (AUTH-07 backend gap)</name>
  <files>backend/internal/api/middleware/auth.go, backend/internal/api/router.go</files>
  <read_first>
    - backend/internal/api/middleware/auth.go (JWTAuth — the middleware to extend)
    - backend/internal/domain/auth/session/service.go (HashToken, FindByTokenHash signatures)
    - backend/internal/api/router.go:386-403 (protected group: JWTAuth use + sessionHandler.RegisterRoutes; sessionSvc in scope)
    - 05-RESEARCH.md Pitfall 2 (the exact gap + tolerate-no-cookie requirement)
  </read_first>
  <action>
    Make `GetCurrentSessionID(ctx)` return the active session for cookie-authenticated requests so the AUTH-07 sessions UI works (is_current badge, revoke-all-others). Add a new middleware `CurrentSession(sessionSvc *session.Service)` in middleware/auth.go that, AFTER JWTAuth, reads the `refresh_token` cookie (the only request-bound handle to the session row — the access JWT is stateless and has no jti), `session.HashToken`es it, calls `sessionSvc.FindByTokenHash`, and on success wraps the request context with `WithCurrentSessionID(ctx, sess.ID())`. Tolerate every failure path silently (no cookie, lookup miss, revoked) by passing the request through unchanged — SSE query-param auth and programmatic callers have no refresh cookie and MUST still authenticate. Do NOT make session resolution a hard auth requirement. In router.go, apply this middleware on the protected group immediately after `r.Use(appMiddleware.JWTAuth(jwtService))` (e.g. `r.Use(appMiddleware.CurrentSession(sessionSvc))`) so `protectedAPI` handlers see the populated context. Keep errors wrapped with `%w` where any are surfaced; this path swallows lookup errors by design (document why in a comment). Import the session package in middleware/auth.go.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/api/middleware/... ./internal/domain/auth/session/...</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && go build ./...` exits 0.
    - `grep -n "WithCurrentSessionID" backend/internal/api/middleware/auth.go` shows the call inside the new CurrentSession middleware (not just the helper def).
    - `grep -n "CurrentSession" backend/internal/api/router.go` shows the middleware applied on the protected group after JWTAuth.
    - No new hard-auth rejection added (no `http.Error(...Unauthorized)` introduced in the CurrentSession path) — `grep -c "Unauthorized" backend/internal/api/middleware/auth.go` unchanged from baseline.
  </acceptance_criteria>
  <done>JWTAuth-protected requests carrying a valid refresh cookie populate the current session id; cookieless protected requests still authenticate. Build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add cookie-bearing request helper + logout-revocation + F3 + is_current integration tests (AUTH-12, AUTH-07)</name>
  <files>backend/tests/integration/setup.go, backend/tests/integration/auth_test.go</files>
  <read_first>
    - backend/tests/integration/setup.go (NewTestServer, Request, Post, ParseResponse, RequireStatus — Bearer-only)
    - backend/tests/integration/auth_test.go (TestRegister_Success pattern: unique email via uuid; ParseResponse shape)
    - backend/internal/domain/auth/user/handler.go logout/refreshToken (the behavior under test — interfaces above)
    - backend/internal/domain/auth/session/handler.go (SessionResponse.is_current; revoke-all-others 400 path)
    - 05-RESEARCH.md Pitfall 3 (harness has no cookie jar) + Code Examples "Go logout-revocation integration test"
  </read_first>
  <behavior>
    - TestLogout_RevokesSession: register+login captures `refresh_token` from JSON body; POST /auth/logout WITH a `refresh_token` cookie → 200/204; then POST /auth/refresh `{refresh_token: <same>}` → 401 with detail containing "revoked".
    - TestRefresh_RevokedSession_NoNewSession (F3 guard): after the revoked refresh above, a SECOND POST /auth/refresh with the same token → still 401, NOT 200 — assert no new session row by re-querying GET /users/me/sessions (Bearer from a fresh login of the SAME user) and asserting the revoked session is absent / count did not grow.
    - TestSessions_CurrentSessionMarked (AUTH-07): login → set access cookie/Bearer + refresh cookie on the sessions GET → exactly one returned session has is_current=true; DELETE /users/me/sessions (revoke-all-others) with the current-session context → 200 (NOT 400 "current session not found").
  </behavior>
  <action>
    Add a cookie-capable request helper to the integration harness (extend setup.go with a `RequestWithCookies(method, path string, body interface{}, cookies map[string]string)` or a small `PostWithCookie` that builds `http.NewRequest`, sets Content-Type, optional Bearer, and `req.AddCookie(&http.Cookie{Name, Value})` for each cookie). Reuse it for the access_token cookie too so the is_current test can carry both cookies on the sessions GET. In auth_test.go (guarded by the existing `//go:build integration` tag), add the three tests above using unique-per-run emails (`uuid.New().String()[:8]`) per the existing register pattern. Capture the login/register `refresh_token` from the JSON body (the harness body shape is `{token, refresh_token}` — also capture the access token to set the access_token cookie where Bearer is insufficient; the sessions GET needs the refresh cookie for CurrentSession middleware to resolve is_current). Assert exact status codes and that the 401 detail contains "revoked" via ParseErrorResponse. For the F3 no-new-session assertion, list sessions for the user and assert the revoked session id is not present. Table-driven where natural; keep each test independent (own NewTestServer).
  </action>
  <verify>
    <automated>cd backend && TEST_DATABASE_URL=postgresql://wh:wh@localhost:5432/warehouse_test go test -tags=integration -count=1 ./tests/integration/ -run 'Logout|RevokedSession|CurrentSessionMarked' -v</automated>
  </verify>
  <acceptance_criteria>
    - The three named tests PASS (exit 0) against warehouse_test.
    - `grep -c "func TestLogout_RevokesSession\|func TestRefresh_RevokedSession_NoNewSession\|func TestSessions_CurrentSessionMarked" backend/tests/integration/auth_test.go` returns 3.
    - The logout test sends a refresh_token COOKIE (not just a body) — `grep -n "AddCookie\|RequestWithCookies\|PostWithCookie" backend/tests/integration/` matches.
    - Default lane stays fast/green: `cd backend && go test ./tests/integration/...` (NO integration tag) compiles and runs without these tests (build-tag gated).
  </acceptance_criteria>
  <done>Logout revocation, F3 no-resurrection, and is_current are all guarded by live Postgres integration tests; revert of f49e4b48 or the CurrentSession wiring fails at least one.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the stale BACKEND-SECURITY audit doc for F2/F3</name>
  <files>docs/audit/BACKEND-SECURITY.md</files>
  <read_first>
    - docs/audit/BACKEND-SECURITY.md (F2 + F3 sections — describe PRE-fix state)
    - 05-RESEARCH.md State of the Art table (F2/F3 RESOLVED in f49e4b48) + "Deprecated/outdated" note
  </read_first>
  <action>
    Update the F2 and F3 sections of docs/audit/BACKEND-SECURITY.md to reflect reality: both were fixed in commit f49e4b48 ("tenant isolation threading + security hardening (F1-F20)"). Use the auditable-history annotation pattern (preserve the ORIGINAL finding text, append a RESOLVED block) — F2: logout now revokes the server session via the refresh-cookie hash; F3: refresh with an unknown session hash returns 401 and the legacy-token resurrection fallback is removed. Cite the new regression tests (TestLogout_RevokesSession, TestRefresh_RevokedSession_NoNewSession in backend/tests/integration/auth_test.go) as the guard. Do NOT touch unrelated findings (F1 attachment IDOR, F4-F17). Note that AUTH-12 is closed by this phase.
  </action>
  <verify>
    <automated>grep -c "RESOLVED" docs/audit/BACKEND-SECURITY.md && grep -c "f49e4b48" docs/audit/BACKEND-SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "f49e4b48" docs/audit/BACKEND-SECURITY.md` matches (commit cited).
    - `grep -n "TestLogout_RevokesSession" docs/audit/BACKEND-SECURITY.md` matches (test cited as guard).
    - Original F2/F3 finding text preserved (annotation pattern — not deleted).
  </acceptance_criteria>
  <done>Audit doc no longer misrepresents the fixed F2/F3 as open; references the regression guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser cookie → /auth/logout, /auth/refresh | untrusted refresh token crosses; server is the sole revocation authority |
| browser cookie → JWTAuth/CurrentSession | access JWT (stateless) + refresh cookie (session-bound) cross into protected handlers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Repudiation/Elevation | refresh after logout | mitigate | TestLogout_RevokesSession proves replay → 401; revocation is server-authoritative (Revoke deletes the session row keyed by HashToken) |
| T-05-02 | Elevation | revoked-token session resurrection (F3) | mitigate | refresh has NO legacy-token fallback; TestRefresh_RevokedSession_NoNewSession guards that a revoked token mints no new session |
| T-05-03 | Spoofing | CurrentSession middleware mis-binding session id | mitigate | id resolved ONLY from HashToken(refresh cookie) match; failure is silent pass-through, never a privilege grant; is_current is display/UX only — revoke authorization still checks ownership in the service |
| T-05-04 | Information disclosure | tokens in logs/test output | accept | tests capture tokens in memory only; no token logged; harness prints body only on failure (assertion diff) — low value, test-only |
| T-05-05 | Tampering | npm/pip/cargo installs | accept | NO new packages this plan (Go module + frontend deps unchanged) — Package Legitimacy Audit N/A per 05-RESEARCH.md |

Cookie flags (F13) are OUT OF SCOPE this phase — `createAuthCookie`/`clearAuthCookie` Secure/HttpOnly/SameSite are untouched and already addressed in f49e4b48. Noted explicitly: this plan does NOT modify cookie attributes.
</threat_model>

<verification>
- `cd backend && go build ./... && go test ./...` (default lane, no integration tag) green.
- `cd backend && TEST_DATABASE_URL=postgresql://wh:wh@localhost:5432/warehouse_test go test -tags=integration -count=1 ./tests/integration/ -run 'Logout|RevokedSession|CurrentSessionMarked' -v` green.
- Audit doc F2/F3 annotated RESOLVED with commit + test citation.
</verification>

<success_criteria>
- AUTH-12 logout revocation + F3 no-resurrection guarded by passing integration tests.
- AUTH-07 backend gap closed: protected requests populate current-session id; is_current + revoke-all-others functional.
- Audit doc corrected.
</success_criteria>

<output>
Create `.planning/phases/05-auth/05-01-SUMMARY.md` when done.
</output>
