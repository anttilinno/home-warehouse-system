---
phase: 05-auth
plan: 03
type: execute
wave: 2
depends_on: [05-02]
files_modified:
  - frontend2/src/features/workspace/WorkspaceProvider.tsx
  - frontend2/src/features/workspace/useWorkspace.ts
  - frontend2/src/features/workspace/WorkspaceProvider.test.tsx
  - frontend2/src/components/layout/WorkspaceSwitcher.tsx
  - frontend2/src/components/layout/WorkspaceSwitcher.test.tsx
  - frontend2/src/components/layout/TopBar.tsx
  - frontend2/src/components/layout/AppShell.tsx
  - frontend2/src/features/dashboard/DashboardPage.tsx
  - frontend2/src/features/dashboard/DashboardPage.test.tsx
autonomous: true
requirements: [AUTH-06]
must_haves:
  truths:
    - "currentWorkspaceId is app-level state (context + localStorage), initialized from /users/me/workspaces with first-workspace fallback"
    - "The TopBar pill is a live switcher; selecting a workspace updates context, persists to localStorage, and invalidates entity queries (no page reload)"
    - "DashboardPage reads wsId from useWorkspace() — the workspaces.data?.[0]?.id hardcode is GONE"
  artifacts:
    - path: frontend2/src/features/workspace/WorkspaceProvider.tsx
      provides: "WorkspaceProvider context (D-12 SSOT) + first-workspace heal"
      contains: "localStorage"
    - path: frontend2/src/components/layout/WorkspaceSwitcher.tsx
      provides: "TopBar pill → Popover listbox switcher"
      min_lines: 30
  key_links:
    - from: frontend2/src/features/workspace/WorkspaceProvider.tsx
      to: queryClient.invalidateQueries
      via: "setWorkspace invalidates entity caches"
      pattern: "invalidateQueries"
    - from: frontend2/src/features/dashboard/DashboardPage.tsx
      to: useWorkspace
      via: "wsId sourced from context, not first-workspace"
      pattern: "useWorkspace"
---

<objective>
Build the D-12 WorkspaceProvider — the single source of truth for every workspace-scoped entity API call — make the TopBar pill a real switcher, and remove DashboardPage's first-workspace hardcode so every later feature phase derives wsId from context.

Purpose: AUTH-06. D-12 locks workspace state as React context + localStorage (NOT a route param). This provider gates the entire entity surface of Phases 7+; getting the SSOT + invalidate-on-switch right here means no stale-data bug class downstream (Pitfall 6).

Output: WorkspaceProvider + useWorkspace, a Popover-based WorkspaceSwitcher wired into TopBar, and DashboardPage reading wsId from context.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-auth/05-RESEARCH.md
@.planning/phases/05-auth/05-UI-SPEC.md
@frontend2/src/features/dashboard/DashboardPage.tsx
@frontend2/src/components/layout/TopBar.tsx
@frontend2/src/components/layout/AppShell.tsx

<interfaces>
<!-- D-12 + Pattern 1 from 05-RESEARCH (the SSOT). -->
- Query key `["workspaces"]` is ALREADY used by RequireAuth + DashboardPage. WorkspaceProvider MUST reuse it (no extra request) — `get<Workspace[]>("/users/me/workspaces")`, `retry: false`.
- localStorage key: `"workspace_id"` (D-12; matches useLogout reset in Plan 05).
- `Workspace { id, name, slug, description, role, is_personal }`.
- `setWorkspace(id)` → localStorage.setItem + setState + `queryClient.invalidateQueries()` (NOT window.location.reload — legacy anti-pattern, Pitfall 6).
- Heal: once `["workspaces"]` resolves, if current id is null OR not in the list → set to `workspaces[0].id` (first-workspace fallback).

<!-- TopBar pill slot (current — to replace). -->
- TopBar has a disabled placeholder span `data-testid="workspace-pill"` `aria-disabled="true"` title "Switch workspace (Phase 5)". Replace with the live WorkspaceSwitcher (keep the data-testid so existing TopBar tests can be updated, not deleted). TopBar is `hidden ... sm:inline-flex` for the pill.
- TopBar already mounts inside AppShell; AppShell is inside RequireAuth (authenticated branch) — WorkspaceProvider must wrap the AppShell subtree so the switcher + pages read context. Mount WorkspaceProvider in AppShell (or in routes/index.tsx around the AppShell layout route) — choose AppShell to keep the authenticated-only scope.

<!-- DashboardPage (current — de-hardcode). -->
- `const wsId = workspaces.data?.[0]?.id;` → `const { currentWorkspaceId: wsId } = useWorkspace();`. Its query keys `["dashboard", wsId]` / `["activity", wsId]` already parameterize on wsId — only the SOURCE changes. Keep the `enabled: !!wsId` guards. Keep its own `["workspaces"]` query for the empty-state check OR read `workspaces` from useWorkspace (prefer the latter to drop the duplicate query).

<!-- Retro atoms available. -->
- `Popover` from @/components/retro (chromeless utility; on useModalStack for ESC).
- `retroToast` from @/components/retro (mint success toast "Switched to {name}.").
- `RetroEmptyState` for the zero-workspace popover row.
- `useModalStack` from @/components/modal (ESC arbiter — LOCKED; ESC closes popover, never logs out).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WorkspaceProvider + useWorkspace (D-12 SSOT)</name>
  <files>frontend2/src/features/workspace/WorkspaceProvider.tsx, frontend2/src/features/workspace/useWorkspace.ts, frontend2/src/features/workspace/WorkspaceProvider.test.tsx</files>
  <read_first>
    - 05-RESEARCH.md Pattern 1 (WorkspaceProvider full pattern) + Pitfall 6 (invalidate, not reload)
    - frontend2/src/features/auth/RequireAuth.tsx (the ["workspaces"] query shape to reuse)
    - 05-UI-SPEC.md §4 (switcher states: loading, single-workspace non-expanding, zero-workspace)
  </read_first>
  <behavior>
    - On mount with localStorage empty and workspaces=[{id:A},{id:B}] → currentWorkspaceId heals to A (first).
    - With localStorage="B" and B present → currentWorkspaceId is B (persisted value honored).
    - With localStorage="GONE" not in the list → heals to first workspace and rewrites localStorage.
    - setWorkspace("B") → localStorage["workspace_id"]==="B", context value B, AND queryClient.invalidateQueries called once.
    - While ["workspaces"] pending → currentWorkspaceId is null (consumers guard with enabled:!!wsId).
  </behavior>
  <action>
    Create `WorkspaceProvider.tsx` per D-12 / Pattern 1: a context holding `{ currentWorkspaceId: string | null, setWorkspace(id), workspaces: Workspace[] | undefined, isLoading }`. Use `useQuery(["workspaces"], () => get<Workspace[]>("/users/me/workspaces"), { retry: false })` — reuse the existing key so RequireAuth's cache is shared (no extra request). Initialize `currentWorkspaceId` state from `localStorage.getItem("workspace_id")`. In a `useEffect` keyed on `[workspaces, currentWorkspaceId]`, heal: if workspaces resolved and current id is null/absent-from-list, set first workspace id + write localStorage. `setWorkspace(id)` writes localStorage, sets state, and calls `queryClient.invalidateQueries()`. Export `useWorkspace()` from `useWorkspace.ts` (throws if used outside provider). Add `WorkspaceProvider.test.tsx` covering the five behaviors with a QueryClientProvider + MSW (`server.use` to vary the workspaces response) and a localStorage reset in beforeEach; assert invalidateQueries via a spy on the test queryClient (`vi.spyOn(queryClient, "invalidateQueries")`).
  </action>
  <verify>
    <automated>cd frontend2 && bun run test src/features/workspace/WorkspaceProvider.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Provider test passes all five behaviors.
    - `grep -n "invalidateQueries" frontend2/src/features/workspace/WorkspaceProvider.tsx` matches; `grep -c "location.reload" frontend2/src/features/workspace/WorkspaceProvider.tsx` == 0 (no reload anti-pattern).
    - `grep -n '"workspace_id"' frontend2/src/features/workspace/WorkspaceProvider.tsx` matches (D-12 localStorage key).
    - useWorkspace throws outside provider (assert in test).
  </acceptance_criteria>
  <done>WorkspaceProvider is the SSOT: first-workspace heal, localStorage persistence, invalidate-on-switch — all proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WorkspaceSwitcher Popover + TopBar wiring</name>
  <files>frontend2/src/components/layout/WorkspaceSwitcher.tsx, frontend2/src/components/layout/WorkspaceSwitcher.test.tsx, frontend2/src/components/layout/TopBar.tsx, frontend2/src/components/layout/AppShell.tsx</files>
  <read_first>
    - frontend2/src/components/layout/TopBar.tsx (the disabled workspace-pill span to replace; FOCUS_RING; menu pattern)
    - 05-UI-SPEC.md §4 (switcher: trigger aria-haspopup=listbox; popover min-w-[220px]; current row titlebar-blue + ✓; loading/single/zero states; ESC via useModalStack)
    - 05-UI-SPEC.md Accessibility (switcher row=option aria-selected; current aria-current; focus returns to pill)
    - frontend2/src/components/retro Popover + retroToast + RetroEmptyState signatures
  </read_first>
  <behavior>
    - Renders current workspace name in the pill trigger; `aria-haspopup="listbox"`, `aria-expanded` reflects open state.
    - Open popover lists all workspaces as role="option"; the current one has aria-selected and a ✓ + titlebar-blue fill.
    - Clicking a non-current row calls setWorkspace(id), closes the popover, fires a mint toast "Switched to {name}.".
    - Exactly one workspace → pill renders the name, non-expanding (aria-disabled, no ▾, no popover).
    - Loading (workspaces pending) → pill shows aria-busy skeleton label.
    - Zero workspaces → popover shows "No workspaces. Contact an owner." empty row.
    - ESC closes the popover via useModalStack and does NOT log out (focus returns to pill).
  </behavior>
  <action>
    Create `WorkspaceSwitcher.tsx`: consumes `useWorkspace()`, renders the bevel pill as a `<button>` trigger (keep `data-testid="workspace-pill"`, drop aria-disabled when >1 workspace), and a Phase 4 `Popover` (plain titlebar, `min-w-[220px]`) with a `role="listbox"`. Rows = workspaces as `role="option" aria-selected`; current row gets `bg-titlebar-blue` + `✓` + `aria-current="true"`. Hover/keyboard-active row = `bg-titlebar-blue`. Click non-current → `setWorkspace(id)`, close, `retroToast` mint `Switched to {name}.` (wrap copy in `<Trans>`/`t` per i18n convention). Implement the single-workspace (non-expanding, aria-disabled), loading (aria-busy), and zero-workspace (RetroEmptyState row) states per UI-SPEC §4. ESC routes through `useModalStack` (the popover already integrates it; verify). In TopBar.tsx, replace the disabled placeholder span with `<WorkspaceSwitcher />` (keep the `hidden sm:inline-flex` responsive wrapper). In AppShell.tsx, wrap the shell subtree in `<WorkspaceProvider>` so TopBar + Outlet pages read context (mount it around the grid root or the TopBar+main region — choose the smallest wrapper that covers TopBar and Outlet). Add `WorkspaceSwitcher.test.tsx` covering the seven behaviors with MSW-varied workspaces + a spy on setWorkspace (provide a stub WorkspaceProvider value via a test wrapper or mock useWorkspace). Update TopBar.test.tsx if the pill assertion changed (the pill is now interactive) — keep BAR-05 ESC-never-logs-out coverage intact.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test src/components/layout/WorkspaceSwitcher.test.tsx src/components/layout/TopBar.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - WorkspaceSwitcher + TopBar tests pass; `grep -n "aria-haspopup=\"listbox\"\|role=\"listbox\"" frontend2/src/components/layout/WorkspaceSwitcher.tsx` matches.
    - `grep -n "WorkspaceProvider" frontend2/src/components/layout/AppShell.tsx` matches (provider mounted).
    - `grep -c "aria-disabled" frontend2/src/components/layout/TopBar.tsx` for the workspace pill == 0 in the multi-workspace path (the static disabled placeholder is gone; aria-disabled only conditional on single/zero workspace inside the switcher).
    - ESC test asserts no logout (reuse/keep the BAR-05 assertion).
  </acceptance_criteria>
  <done>The TopBar pill is a live, accessible workspace switcher mounted under WorkspaceProvider; switching invalidates queries and toasts.</done>
</task>

<task type="auto">
  <name>Task 3: De-hardcode DashboardPage to read wsId from useWorkspace (grep gate)</name>
  <files>frontend2/src/features/dashboard/DashboardPage.tsx, frontend2/src/features/dashboard/DashboardPage.test.tsx</files>
  <read_first>
    - frontend2/src/features/dashboard/DashboardPage.tsx (the `workspaces.data?.[0]?.id` hardcode + the duplicate ["workspaces"] query + the empty-state block)
    - frontend2/src/features/workspace/useWorkspace.ts (the hook from Task 1)
  </read_first>
  <action>
    Replace `const wsId = workspaces.data?.[0]?.id;` with `const { currentWorkspaceId: wsId, workspaces } = useWorkspace();`. Remove the local duplicate `useQuery(["workspaces"], ...)` in DashboardPage — read the workspaces list from useWorkspace for the empty-state check (`workspaces && workspaces.length === 0`). Keep `["dashboard", wsId]` / `["activity", wsId]` query keys and their `enabled: !!wsId` guards unchanged (only the wsId SOURCE moved). Update DashboardPage.test.tsx to render within a WorkspaceProvider (or mock useWorkspace) and assert stats/activity fetch under the context wsId; add an assertion that the page renders the empty-state Window when useWorkspace returns zero workspaces. The grep gate below is the AUTH-06 "hardcode GONE" proof.
  </action>
  <verify>
    <automated>cd frontend2 && grep -c 'workspaces.data?.\[0\]?.id' src/features/dashboard/DashboardPage.tsx; bun run test src/features/dashboard/DashboardPage.test.tsx 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'workspaces.data?.\[0\]?.id' frontend2/src/features/dashboard/DashboardPage.tsx` == 0 (hardcode removed — the AUTH-06 grep gate).
    - `grep -n "useWorkspace" frontend2/src/features/dashboard/DashboardPage.tsx` matches.
    - DashboardPage.test.tsx passes; `cd frontend2 && bun run test src/ && bun run lint:tsc && bun run build` green.
  </acceptance_criteria>
  <done>DashboardPage derives wsId from the WorkspaceProvider SSOT; the first-workspace hardcode is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser localStorage → workspace context | workspace_id is a CLIENT preference, NOT a privilege boundary |
| client → /workspaces/{wsId}/... | backend enforces tenancy on every entity call regardless of the selected wsId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Elevation | tampered localStorage workspace_id to access another tenant | mitigate | wsId is NOT authority — backend repos enforce `workspace_id = $1 AND <ownership>` (proven by Phase 65 cross-tenant guard); a forged id yields 403/404, not access |
| T-05-11 | Information disclosure | stale entity data leaking across workspace switch | mitigate | every entity query key includes wsId (D-12) AND setWorkspace invalidates all queries (Pitfall 6) — both together |
| T-05-12 | Denial of service | switcher ESC accidentally logs out | mitigate | ESC routed through useModalStack arbiter (LOCKED); switcher test asserts no logout |
| T-05-13 | Tampering | npm installs | accept | NO new packages — Package Legitimacy Audit N/A |
</threat_model>

<verification>
- `cd frontend2 && bun run test src/ && bun run lint:tsc && bun run lint:imports && bun run build` green.
- Grep gate: `workspaces.data?.[0]?.id` absent from DashboardPage.
- Switcher accessible (listbox/option/aria-selected) and ESC-safe.
</verification>

<success_criteria>
- WorkspaceProvider is the D-12 SSOT with first-workspace heal + invalidate-on-switch.
- TopBar switcher functional and accessible.
- DashboardPage de-hardcoded.
</success_criteria>

<output>
Create `.planning/phases/05-auth/05-03-SUMMARY.md` when done.
</output>
