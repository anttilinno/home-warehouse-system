---
phase: 05-auth
plan: 06
type: execute
wave: 5
depends_on: [05-03, 05-04, 05-05]
files_modified:
  - frontend2/e2e/auth.spec.ts
autonomous: true
requirements: [AUTH-02, AUTH-06, AUTH-12]
must_haves:
  truths:
    - "A register E2E creates a unique-email account against the live stack and lands on the dashboard"
    - "A logout-revocation E2E proves the old session cannot be reused after logout (AUTH-12 end-to-end)"
    - "The workspace switcher is visible/interactive in the live authenticated shell"
  artifacts:
    - path: frontend2/e2e/auth.spec.ts
      provides: "register + logout-revocation + switcher E2E vs live stack; OAuth skip-with-reason"
      contains: "test.skip"
  key_links:
    - from: frontend2/e2e/auth.spec.ts
      to: /register and /auth/logout
      via: "live-stack browser flows"
      pattern: "register"
---

<objective>
Add the browser-level regression guards that the unit/integration layers cannot reach: a real register flow, a real logout-that-actually-revokes proof (the AUTH-12 end-to-end complement to Plan 01's Go integration test), and the workspace switcher's live presence. OAuth flows are skip-with-reason (no provider creds in CI) per parity §7.

Purpose: The Phase 65 discipline — every two-layer fix gets a matched browser + backend guard. Plan 01 proves logout revokes at the Go/Postgres layer; this proves it through the real cookie-JWT browser round-trip against the live stack.

Output: frontend2/e2e/auth.spec.ts extending the existing live-stack Playwright project (chromium + firefox).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-auth/05-RESEARCH.md
@.planning/phases/05-auth/05-VALIDATION.md
@frontend2/e2e/login-dashboard.spec.ts
@CLAUDE.md

<interfaces>
<!-- Existing E2E contract (CLAUDE.md + login-dashboard.spec.ts). -->
- Live stack required: backend :8080, Postgres warehouse_dev, Vite :5173. No webServer auto-launch (config expects the running stack).
- playwright.config.ts: chromium + firefox projects; baseURL http://localhost:5173 (override E2E_BASE_URL).
- Auth contract: /login -> fill Email + Password labels -> click the submit button matching /^log in$/i. After login the access_token cookie is held by BOTH the page context AND page.request (cookie inherited) — additional API calls need no manual token plumbing.
- Seeder creds: E2E_USER=seeder@test.local / E2E_PASS=password123 (overridable).
- NOTE (CLAUDE.md): with OAuth buttons landing THIS phase, the /login page now has MULTIPLE buttons — switch back to exact-match discipline (/^log in$/i for the primary submit; the OAuth buttons read "Sign in with…"). Verify the primary submit selector still resolves uniquely.
- vitest excludes **/e2e/**; Playwright owns e2e/*.spec.ts.

<!-- New surfaces to exercise (from Plans 03/04/05). -->
- /register: full_name + email + password + confirm -> submit "Create account" -> lands on / (dashboard).
- workspace switcher: TopBar pill (data-testid="workspace-pill") now interactive (aria-haspopup="listbox").
- logout: user pill menu -> Log out -> confirm dialog -> POST /auth/logout revokes server-side.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: auth.spec.ts — register + switcher-visible + logout-revocation; OAuth skip-with-reason</name>
  <files>frontend2/e2e/auth.spec.ts</files>
  <read_first>
    - frontend2/e2e/login-dashboard.spec.ts (the live-stack login pattern: fill labels, exact-match submit, cookie inheritance, page.request seeding)
    - CLAUDE.md §E2E Tests (runbook, seeder creds, auth contract, exact-match note for the new multi-button login)
    - 05-RESEARCH.md "E2E additions" (register unique email; logout actually revokes; switcher visible; OAuth skip-with-reason) + Pitfall 4 (APP_URL note — OAuth live skipped anyway)
    - 05-VALIDATION.md AUTH-12 row (E2E: logout then back-button/api call -> /login)
  </read_first>
  <action>
    Create `frontend2/e2e/auth.spec.ts` with these tests (chromium + firefox via the existing projects):
    (1) REGISTER — navigate /register, fill full_name + a UNIQUE email (`e2e+${Date.now()}@test.local`) + password (>=8) + matching confirm, click "Create account", assert the URL becomes the dashboard (/) and an authenticated marker renders (reuse whatever the login spec asserts for the dashboard). Best-effort cleanup is optional (no delete endpoint required).
    (2) SWITCHER VISIBLE — log in with seeder creds (exact-match /^log in$/i submit), assert the workspace pill (data-testid="workspace-pill") is visible and interactive (aria-haspopup="listbox" present, not aria-disabled); open it and assert at least the current workspace row renders. (Switching requires >1 workspace which the seeder may not have — assert presence/openability, not a switch, to stay deterministic.)
    (3) LOGOUT-REVOCATION (AUTH-12 end-to-end) — log in; capture the session via page.request (the cookie is inherited); confirm an authenticated API call succeeds (e.g. GET /api/users/me -> 200). Then perform logout through the UI (user pill -> Log out -> confirm) OR via page.request.post("/api/auth/logout"). After logout, assert that a navigation to an authenticated route redirects to /login AND a fresh page.request to a protected endpoint with the now-revoked refresh path fails (401) — i.e. the old session cannot be reused. Keep it robust to cookie-clearing: the key assertion is "post-logout the app lands on /login and protected access is denied".
    (4) OAUTH — `test.skip(true, "OAuth requires provider creds + APP_URL=:5173; skip in CI per parity §7")` placeholders for Google + GitHub initiate so the skip is RECORDED with a reason (not silently absent), per the Phase 65 rebuild-the-wiped-spec discipline.
    Use the exact-match submit discipline now that OAuth buttons share the login page. Do NOT add a webServer to the config (live stack per CLAUDE.md). Reuse the cookie-inheritance pattern; no manual token plumbing.
  </action>
  <verify>
    <automated>cd frontend2 && npx playwright test --list e2e/auth.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --list e2e/auth.spec.ts` lists the register, switcher, logout-revocation tests + the 2 skipped OAuth tests across chromium + firefox (in-plan gate — full run happens at the orchestrator phase gate vs the live stack).
    - `grep -c "test.skip" frontend2/e2e/auth.spec.ts` >= 2 (OAuth skip-with-reason recorded, not omitted).
    - `grep -n "Date.now()" frontend2/e2e/auth.spec.ts` matches (unique register email — no cross-run collision).
    - The logout test asserts post-logout denial (redirect to /login + 401 on a protected call) — `grep -n "401\|/login" frontend2/e2e/auth.spec.ts` matches in the logout test.
    - No webServer added to playwright.config.ts (unchanged — live-stack contract).
  </acceptance_criteria>
  <done>Register, switcher-visible, and logout-revocation E2E exist and list cleanly; OAuth is skipped with a recorded reason. The full run executes at the orchestrator phase gate against the live stack.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (Playwright) -> live backend | the real cookie-JWT round-trip; the E2E exercises the trust boundary end-to-end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-24 | Repudiation/Elevation | logout revocation regression at the browser layer | mitigate | logout-revocation E2E proves a revoked session is denied through the real cookie round-trip — the browser-layer complement to Plan 01's Go guard (matched-pair discipline, Phase 65 precedent) |
| T-05-25 | Information disclosure | seeder/test creds in the spec | accept | creds come from env (E2E_USER/E2E_PASS) with documented defaults; test-only seeder account; no production secret |
| T-05-26 | Tampering | npm installs | accept | NO new packages (@playwright/test already present) — Package Legitimacy Audit N/A |
</threat_model>

<verification>
- `cd frontend2 && npx playwright test --list e2e/auth.spec.ts` lists all tests (in-plan gate).
- Full live-stack run (register + switcher + logout-revocation green; OAuth skipped) executes at the orchestrator phase gate per 05-VALIDATION sampling.
</verification>

<success_criteria>
- Register, switcher, logout-revocation E2E added to the live-stack project; OAuth skip-with-reason recorded.
- AUTH-12 guarded at BOTH layers (Go integration in Plan 01 + browser here).
</success_criteria>

<output>
Create `.planning/phases/05-auth/05-06-SUMMARY.md` when done.
</output>
