---
phase: 05-auth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend2/src/test/msw/server.ts
  - frontend2/src/test/msw/handlers.ts
  - frontend2/src/lib/api.ts
  - frontend2/src/lib/api.test.ts
  - frontend2/src/features/auth/RequireAuth.tsx
  - frontend2/src/features/auth/RequireAuth.test.tsx
  - frontend2/vitest.config.ts
autonomous: true
requirements: [AUTH-01, AUTH-05]
must_haves:
  truths:
    - "RequireAuth redirects to /login on HttpError 401/403 and does NOT log out on network/5xx — it shows a retry surface"
    - "When the single-flight refresh fails, api.ts emits a single auth-expired event (no scattered logout)"
    - "The locked api.ts invariants (credentials include, single-flight refreshPromise, BASE_URL, isFormData branch) are preserved"
  artifacts:
    - path: frontend2/src/test/msw/server.ts
      provides: "MSW node server shared fixture for auth-endpoint unit tests"
      contains: "setupServer"
    - path: frontend2/src/lib/api.test.ts
      provides: "single-flight refresh + auth-expired event invariant tests"
      contains: "auth-expired"
    - path: frontend2/src/features/auth/RequireAuth.test.tsx
      provides: "both AUTH-05 branches (401→redirect; 5xx→retry-not-logout)"
      min_lines: 30
  key_links:
    - from: frontend2/src/lib/api.ts
      to: window auth-expired event
      via: "doRefresh failure path dispatches CustomEvent"
      pattern: "auth-expired"
    - from: frontend2/src/features/auth/RequireAuth.tsx
      to: workspaces.refetch
      via: "retry surface on non-401/403 error"
      pattern: "refetch"
---

<objective>
Lay the frontend auth-test substrate and harden the two locked-invariant surfaces every later plan depends on: MSW (the repo has none yet), the api.ts auth-expired event (extend, never restructure), and RequireAuth's network/5xx retry affordance (the v2.0 spurious-logout fix's missing half).

Purpose: Plans 03/04/05 mock auth endpoints with MSW and rely on the auth-expired event + status-aware guard. Getting the locked api.ts invariants proven here means downstream plans inherit a trustworthy client.

Output: shared MSW server/handlers, api.ts extended with an `auth-expired` CustomEvent + invariant tests, RequireAuth with a retry branch + both-branch tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-auth/05-RESEARCH.md
@.planning/phases/05-auth/05-VALIDATION.md
@.planning/phases/05-auth/05-UI-SPEC.md
@frontend2/src/lib/api.ts
@frontend2/src/features/auth/RequireAuth.tsx
@CLAUDE.md

<interfaces>
<!-- Current api.ts (LOCKED INVARIANTS — extend only). -->
- `BASE_URL = "/api"` (Vite proxies to :8080). DO NOT change.
- `class HttpError extends Error { status: number }`.
- module-level `storedRefreshToken`, `refreshPromise`; `setRefreshToken`/`getRefreshToken`.
- `doRefresh()`: throws `HttpError(401)` when no token; on `!res.ok` sets `storedRefreshToken = null` and throws `HttpError(401)`.
- `request<T>()`: on 401 single-flights `doRefresh` via `refreshPromise`, retries once. Every fetch carries `credentials: "include"`. `isFormData ? {} : {"Content-Type":"application/json"}` branch is locked.
- helpers `get/post/postMultipart/patch/del`.

<!-- Current RequireAuth.tsx -->
- probes `useQuery({ queryKey: ["workspaces"], queryFn: () => get<Workspace[]>("/users/me/workspaces"), retry: false })`.
- redirects on `HttpError` 401/403; renders `Loading…` while pending; else returns children.
- MISSING: the `isError && not-401/403` branch (network/5xx) currently falls through to `return children` — a half-rendered shell. Add a retry surface.

<!-- vitest config note -->
- frontend2/vitest.config.ts excludes `**/e2e/**`. MSW server must be registered in the existing jsdom setup file (find `setupFiles` in vitest.config.ts); if none, add `src/test/setup.ts` wiring `beforeAll(server.listen) / afterEach(server.resetHandlers) / afterAll(server.close)`.

<!-- types -->
- `Workspace { id, name, slug, description, role, is_personal }` from @/lib/types.
- `AuthTokenResponse { token, refresh_token }`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the MSW shared fixture (server + auth handlers)</name>
  <files>frontend2/src/test/msw/server.ts, frontend2/src/test/msw/handlers.ts, frontend2/src/test/setup.ts, frontend2/vitest.config.ts</files>
  <read_first>
    - frontend2/package.json (msw ^2.14.2 present; vitest ^4.1.5)
    - frontend2/vitest.config.ts (setupFiles + exclude **/e2e/**)
    - 05-RESEARCH.md Validation Architecture "MSW server/handlers shared fixture" Wave 0 gap
    - 05-UI-SPEC.md Backend contract table (endpoint shapes for handlers)
  </read_first>
  <action>
    Create `src/test/msw/handlers.ts` exporting an array of MSW v2 `http`/`HttpResponse` handlers covering the auth endpoints used in unit tests (note the `/api` BASE_URL prefix — handlers match `/api/...`): `POST /api/auth/login`, `POST /api/auth/register`, `POST /api/auth/oauth/exchange`, `GET /api/users/me`, `GET /api/users/me/workspaces`, `GET /api/users/me/sessions`, `DELETE /api/users/me/sessions/:id`, `DELETE /api/users/me/sessions`, `PATCH /api/users/me/password`, `GET /api/users/me/can-delete`, `DELETE /api/users/me`, `GET /api/auth/oauth/accounts`, `DELETE /api/auth/oauth/accounts/:provider`. Return contract-shaped happy-path JSON by default (shapes per 05-UI-SPEC Backend contract table — e.g. workspaces array, `{ has_password, full_name, email }` for /users/me, SessionResponse[] with one is_current=true). Create `src/test/msw/server.ts` exporting `server = setupServer(...handlers)`. Register it in the vitest setup file (extend existing `setupFiles`, or create `src/test/setup.ts` and point vitest at it) with `beforeAll(() => server.listen({ onUnhandledRequest: "error" }))`, `afterEach(() => server.resetHandlers())`, `afterAll(() => server.close())`. Tests override per-case with `server.use(...)`.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test src/test/msw 2>&1 | tail -5; bun run lint:tsc</automated>
  </verify>
  <acceptance_criteria>
    - `frontend2/src/test/msw/server.ts` exports `server` via `setupServer`; `grep -n "setupServer" frontend2/src/test/msw/server.ts` matches.
    - vitest setup registers listen/resetHandlers/close — `grep -rn "server.listen\|resetHandlers\|server.close" frontend2/src/test/` matches.
    - `cd frontend2 && bun run lint:tsc` clean (no type errors in the fixture).
  </acceptance_criteria>
  <done>A shared MSW server is wired into the vitest jsdom run; auth endpoints have default happy-path handlers overridable per test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend api.ts with an auth-expired event + invariant tests (AUTH-01)</name>
  <files>frontend2/src/lib/api.ts, frontend2/src/lib/api.test.ts</files>
  <read_first>
    - frontend2/src/lib/api.ts (the locked client — Pattern 4 in 05-RESEARCH)
    - 05-RESEARCH.md Pattern 4 (single-flight refresh + auth-expired event; the 4 invariants NOT to touch)
  </read_first>
  <behavior>
    - When `doRefresh()` fails (no stored token OR refresh `!res.ok`), api.ts dispatches a single `window` CustomEvent named "auth-expired" exactly once per failed refresh, AFTER clearing `storedRefreshToken`.
    - Concurrent 401s share ONE in-flight refresh (refreshPromise single-flight) — two simultaneous requests trigger doRefresh once; on its failure exactly one auth-expired fires (not two).
    - A successful refresh + retry does NOT fire auth-expired and returns the retried payload.
    - Invariants preserved: every fetch has credentials:"include"; BASE_URL stays "/api"; FormData requests omit Content-Type.
  </behavior>
  <action>
    In api.ts, add a single `window.dispatchEvent(new CustomEvent("auth-expired"))` in the doRefresh failure path — after `storedRefreshToken = null`, before throwing `HttpError(401)` — and also in the no-stored-token early throw so a refresh attempt with a gone session also signals expiry. Guard against double-fire across the single-flight: dispatch from doRefresh itself (which is single-flighted via refreshPromise), so concurrent callers awaiting the same promise see one event. Do NOT restructure `request`, the refreshPromise machinery, BASE_URL, credentials, or the isFormData branch. Add `src/lib/api.test.ts` with vitest: use `vi.spyOn(window, "dispatchEvent")` (or an `addEventListener` counter) and stub `fetch` (or use MSW) to assert: (1) failed refresh fires auth-expired once; (2) two concurrent 401-triggering requests share one doRefresh and fire one event; (3) successful refresh fires zero auth-expired and returns the payload; (4) a request with a FormData body has no Content-Type header (read the fetch init) and credentials:"include". Keep tests deterministic (reset storedRefreshToken between cases via setRefreshToken(null)).
  </action>
  <verify>
    <automated>cd frontend2 && bun run test src/lib/api.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - api.test.ts passes; `grep -c "auth-expired" frontend2/src/lib/api.test.ts` >= 3 (the three event assertions).
    - `grep -n "auth-expired" frontend2/src/lib/api.ts` matches exactly the failure paths (not the success path).
    - Locked invariants intact: `grep -c 'credentials: "include"' frontend2/src/lib/api.ts` unchanged from baseline; `grep -n 'BASE_URL = "/api"' frontend2/src/lib/api.ts` matches; the `isFormData` branch line is unchanged.
  </acceptance_criteria>
  <done>api.ts emits a single auth-expired event on refresh failure with the single-flight invariant proven; no restructuring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RequireAuth network/5xx retry affordance + both-branch tests (AUTH-05)</name>
  <files>frontend2/src/features/auth/RequireAuth.tsx, frontend2/src/features/auth/RequireAuth.test.tsx</files>
  <read_first>
    - frontend2/src/features/auth/RequireAuth.tsx (current guard)
    - 05-RESEARCH.md Pitfall 1 (RequireAuth no network/5xx affordance) + AUTH-05 row in Test Map
    - 05-UI-SPEC.md Copywriting Contract: network/5xx row ("Couldn't reach the server. Check your connection and retry." + Retry)
  </read_first>
  <behavior>
    - HttpError status 401 → renders Navigate to /login (no retry surface).
    - HttpError status 403 → renders Navigate to /login.
    - A non-HttpError error (network) OR HttpError 500 → does NOT redirect/logout; renders a retry surface with the copy "Couldn't reach the server. Check your connection and retry." and a Retry control that calls workspaces.refetch.
    - isPending → Loading.
    - Success → children.
  </behavior>
  <action>
    Add an explicit branch to RequireAuth.tsx: after the existing 401/403 redirect check and the isPending check, if `workspaces.isError` (and it was not the 401/403 case already handled), return a retry surface — a centered `<main>` with `role="alert"`, the network copy (wrapped in `<Trans>` per the i18n convention used in the file), and a Retro `BevelButton` (or a button) labeled Retry whose onClick calls `workspaces.refetch()`. Keep the 401/403 redirect as the FIRST check so an HttpError 403 never reaches the retry branch. Add `src/features/auth/RequireAuth.test.tsx` rendering RequireAuth inside a QueryClientProvider + MemoryRouter; use MSW `server.use` to force each case: 401 (assert redirect — e.g. a Routes setup where /login renders a sentinel), 403 (redirect), 500 (assert retry surface present + no /login sentinel + clicking Retry refetches → after server.use flips to 200, children render), network error (assert retry surface, not logout). Assert the guard NEVER navigates to /login on the 500/network cases (the v2.0 bug regression guard).
  </action>
  <verify>
    <automated>cd frontend2 && bun run test src/features/auth/RequireAuth.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - RequireAuth.test.tsx passes both branches; `grep -c "refetch" frontend2/src/features/auth/RequireAuth.tsx` >= 1.
    - The retry-surface test asserts the /login sentinel is NOT rendered on 500/network (regression guard) — `grep -n "not.*login\|queryByText\|not.toBeInTheDocument" frontend2/src/features/auth/RequireAuth.test.tsx` shows a negative assertion.
    - `cd frontend2 && bun run test src/features/auth/ && bun run lint:tsc` green.
  </acceptance_criteria>
  <done>RequireAuth redirects only on 401/403 and shows a working retry surface on network/5xx (no logout) — both branches tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api proxy → backend | all auth probes cross; client trusts HttpError status to route auth state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Denial of service (self-inflicted) | spurious logout on transient network error | mitigate | RequireAuth stays on route + retry on non-401/403; tested negative assertion guards the v2.0 regression |
| T-05-07 | Tampering | auth-expired event spoofed by page script | accept | same-origin trusted code; the event only triggers a /login navigation (no privilege) — a hostile same-origin script could already navigate |
| T-05-08 | Information disclosure | tokens in test fixtures/logs | mitigate | MSW handlers return placeholder tokens; no real secrets; refresh token stays in-memory (never localStorage) per locked invariant |
| T-05-09 | Tampering | npm installs | accept | NO new packages (msw/vitest already present) — Package Legitimacy Audit N/A |
</threat_model>

<verification>
- `cd frontend2 && bun run test src/ && bun run lint:tsc && bun run lint:imports && bun run build` green.
- api.ts diff is additive only (auth-expired dispatch) — no invariant lines changed.
- MSW server reused by Plans 03/04/05.
</verification>

<success_criteria>
- MSW substrate exists and is wired into vitest.
- api.ts emits auth-expired with single-flight invariant proven.
- RequireAuth handles both AUTH-05 branches with tests.
</success_criteria>

<output>
Create `.planning/phases/05-auth/05-02-SUMMARY.md` when done.
</output>
