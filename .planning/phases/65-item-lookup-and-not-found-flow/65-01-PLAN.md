---
phase: 65-item-lookup-and-not-found-flow
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend2/src/lib/api/__tests__/items.lookupByBarcode.test.ts
  - frontend2/src/lib/api/__tests__/barcode.test.ts
  - frontend2/src/features/items/hooks/__tests__/useBarcodeEnrichment.test.ts
  - frontend2/src/features/items/__tests__/ItemFormPage.test.tsx
  - frontend2/src/features/items/__tests__/UpcSuggestionBanner.test.tsx
  - frontend2/src/features/items/forms/__tests__/schemas.test.ts
  - frontend2/src/components/scan/__tests__/ScanResultBanner.states.test.tsx
  - frontend2/src/test-utils-query.tsx
  - .planning/phases/65-item-lookup-and-not-found-flow/65-BUNDLE-BASELINE.md
autonomous: true
requirements: [LOOK-01, LOOK-02, LOOK-03]
tags: [testing, scaffolding, nyquist, bundle-baseline, frontend, react, tanstack-query, vitest]

must_haves:
  truths:
    - "Seven Wave 0 test files exist, importable by Vitest, and fail with a deterministic TODO-style message (not a random runtime crash) so Waves 1-4 turn them green one at a time"
    - "A shared renderHookWithQueryClient + renderWithQueryClient helper exists at frontend2/src/test-utils-query.tsx so query-backed hook/component tests do not each re-invent a QueryClientProvider wrapper"
    - "The pre-Phase-65 /scan chunk gzip byte size AND main index chunk gzip byte size are captured in 65-BUNDLE-BASELINE.md before any Phase 65 production code lands, so the Plan 08 bundle gate has a concrete number to compare against"
    - "A schemas.test.ts scaffold exists covering D-23 (optional brand field) + D-24 (loosened barcode regex accepting hyphens/underscores) as it.todo entries — Plan 65-02 Task 1 converts them to green tests"
  artifacts:
    - path: "frontend2/src/lib/api/__tests__/items.lookupByBarcode.test.ts"
      provides: "Skeleton tests for D-06/D-07/D-08 (empty list, case-sensitive guard, workspace mismatch, happy path)"
      contains: "describe(\"itemsApi.lookupByBarcode\""
    - path: "frontend2/src/lib/api/__tests__/barcode.test.ts"
      provides: "Skeleton tests for D-11 (barcodeApi.lookup URL shape + barcodeKeys factory)"
      contains: "describe(\"barcodeApi.lookup\""
    - path: "frontend2/src/features/items/hooks/__tests__/useBarcodeEnrichment.test.ts"
      provides: "Regex-gate matrix skeleton (D-12: 7, 8, 14, 15-digit, non-numeric) + staleTime:Infinity + silent-fail assertions"
      contains: "describe(\"useBarcodeEnrichment\""
    - path: "frontend2/src/features/items/__tests__/ItemFormPage.test.tsx"
      provides: "Skeleton integration tests for D-01..D-05 (route smoke, ?barcode= prefill, SKU-once, dirty-guard, invalidation, navigate)"
      contains: "describe(\"ItemFormPage\""
    - path: "frontend2/src/features/items/__tests__/UpcSuggestionBanner.test.tsx"
      provides: "Skeleton component tests for D-13..D-16 (hazard stripe + header + per-field [USE] + USE ALL + DISMISS + helper-text-only category + silent fail)"
      contains: "describe(\"UpcSuggestionBanner\""
    - path: "frontend2/src/features/items/forms/__tests__/schemas.test.ts"
      provides: "Skeleton schema-regression tests for D-23 (optional brand field) + D-24 (barcode regex accepts hyphens/underscores)"
      contains: "describe(\"itemCreateSchema"
    - path: "frontend2/src/components/scan/__tests__/ScanResultBanner.states.test.tsx"
      provides: "Skeleton state-contract tests for D-17..D-21 (LOADING / MATCH / NOT-FOUND / ERROR)"
      contains: "describe(\"ScanResultBanner states\""
    - path: "frontend2/src/test-utils-query.tsx"
      provides: "renderHookWithQueryClient + renderWithQueryClient helpers for Phase 65 query-backed tests"
      exports: ["renderHookWithQueryClient", "renderWithQueryClient", "createTestQueryClient"]
    - path: ".planning/phases/65-item-lookup-and-not-found-flow/65-BUNDLE-BASELINE.md"
      provides: "Pre-Phase-65 gzip baseline snapshot for Plan 08 gate"
      contains: "scanner chunk gzip:"
  key_links:
    - from: "All seven Wave 0 test files"
      to: "frontend2/src/test-utils-query.tsx"
      via: "import { renderHookWithQueryClient, renderWithQueryClient, createTestQueryClient } from \"@/test-utils-query\""
      pattern: "test-utils-query"
    - from: "65-BUNDLE-BASELINE.md"
      to: "Plan 65-08 bundle gate"
      via: "numeric byte-size comparison"
      pattern: "scanner chunk gzip"
    - from: "frontend2/src/features/items/forms/__tests__/schemas.test.ts"
      to: "Plan 65-02 Task 1"
      via: "it.todo → real it() conversion for D-23 brand field + D-24 barcode regex"
      pattern: "itemCreateSchema"
---

<objective>
Land the Nyquist test scaffold (seven RED tests + a shared QueryClient test helper) and capture the pre-Phase-65 bundle byte-size baseline BEFORE any Phase 65 production code lands. Every Wave 0 gap listed in 65-VALIDATION.md §Wave 0 Requirements gets a file that imports the target symbol, expresses one `it.todo()` per acceptance criterion, and (for the few facts the test infra already knows how to assert — e.g. module importability, key-factory shape) has one `it(...)` that is ALREADY green. This makes the executor's Waves 1-4 a mechanical "turn the todos into real assertions as the production code lands" loop and guarantees the Plan 08 bundle gate has a concrete comparison number.

Purpose: Per 65-VALIDATION.md (Nyquist) every LOOK-0N acceptance criterion must map to an `<automated>` command. Six of the nine test files do not yet exist — if we do not scaffold them now, the Wave 1-4 plans cite `MISSING` tests which breaks the sampling-rate contract. A seventh schemas.test.ts scaffold is required because D-23 (optional brand field) and D-24 (loosened barcode regex) are user-locked scope additions in 65-CONTEXT.md — Plan 65-02 converts these todos to green tests as part of its TDD RED-first cycle. Additionally Open Question #5 (65-RESEARCH.md line 1144) mandates the bundle baseline be captured BEFORE Phase 65 code lands — otherwise the Plan 08 regression gate has nothing to compare against.

Output: Seven new test files (initially RED via `it.todo()` for behavior that is yet to exist, GREEN for import-smoke / factory-shape assertions that are already true), one shared test helper, one baseline-capture doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-item-lookup-and-not-found-flow/65-CONTEXT.md
@.planning/phases/65-item-lookup-and-not-found-flow/65-RESEARCH.md
@.planning/phases/65-item-lookup-and-not-found-flow/65-UI-SPEC.md
@.planning/phases/65-item-lookup-and-not-found-flow/65-VALIDATION.md
@.planning/phases/65-item-lookup-and-not-found-flow/65-PATTERNS.md

<interfaces>
<!-- Locked contracts the test scaffolds must assert against. DO NOT re-derive. -->

From frontend2/src/lib/api/scan.ts (Phase 64 LOCKED, DO NOT EDIT):
```ts
export type ScanLookupStatus = "idle" | "loading" | "success" | "error";
export interface ScanLookupResult {
  status: ScanLookupStatus;
  match: Item | null;
  error: Error | null;
  refetch: () => void;
}
export const scanKeys = {
  all: ["scan"] as const,
  lookups: () => [...scanKeys.all, "lookup"] as const,
  lookup: (code: string) => [...scanKeys.lookups(), code] as const,
};
```

From frontend2/src/lib/api/items.ts (Plan 02 will append `lookupByBarcode`):
```ts
// Future signature (assert against it in it.todo strings; fall back to typed import-reference):
// lookupByBarcode(wsId: string, code: string): Promise<Item | null>
```

Future barcode module (Plan 03 creates):
```ts
export interface BarcodeProduct {
  barcode: string;
  name: string;
  brand?: string | null;
  category?: string | null;
  image_url?: string | null;
  found: boolean;
}
export const barcodeApi = { lookup: (code: string) => Promise<BarcodeProduct> };
export const barcodeKeys = {
  all: ["barcode"] as const,
  lookups: () => [...barcodeKeys.all, "lookup"] as const,
  lookup: (code: string) => [...barcodeKeys.lookups(), code] as const,
};
```

From frontend2/src/features/items/forms/schemas.ts (current — pre-Phase-65):
```ts
export const itemCreateSchema = z.object({
  name: z.string().min(1).max(200),
  sku: z.string().min(1).max(64).regex(/^[A-Za-z0-9_-]+$/, ...),
  barcode: z.string().max(64).regex(/^[A-Za-z0-9]+$/, ...).optional().or(z.literal("")),
  description: z.string().max(2000).optional().or(z.literal("")),
  category_id: z.string().uuid(...).optional().or(z.literal("")),
});
// D-23 adds:    brand: z.string().max(120).optional()
// D-24 changes: barcode regex → /^[A-Za-z0-9\-_]+$/ + error message update
```

From frontend2/src/lib/api/__tests__/queryKeys.test.ts (existing test style — mirror the "key-factory shape" assertion pattern).

From frontend2/src/lib/api/itemPhotos.test.ts (existing vi.spyOn + beforeEach/afterEach teardown style).

From frontend2/src/features/items/__tests__/ItemDetailPage.test.tsx (existing MemoryRouter + QueryClient + mocked useAuth pattern — read before writing ItemFormPage.test.tsx scaffold).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → production imports | Test files import production modules that will be rewritten in later waves; scaffold must not hide real bugs by swallowing errors |
| bundle-baseline measurement → CI gate | Baseline bytes determine a HARD gate in Plan 08; miscaptured baseline → gate silently passes regressed bundles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01-01 | Tampering | bundle-baseline capture | mitigate | Capture bytes from committed production code (master HEAD = b04ae7c post-64-10), record exact file hashes in 65-BUNDLE-BASELINE.md, include git SHA + `pnpm build` timestamp so Plan 08 can verify the baseline came from the correct commit |
| T-65-01-02 | Repudiation | test-scaffold false green | mitigate | Every acceptance criterion uses `it.todo(...)` NOT `it.skip(...)` — `it.todo` surfaces as a distinct pending state in Vitest output so executor cannot mistake it for green. Waves 1-4 convert todos → real `it(...)` blocks as production code lands |
| T-65-01-03 | Information disclosure | test fixtures | accept | No PII or secrets in scaffolds — barcode fixtures use public UPC (`5449000000996` = Coca-Cola, publicly indexed globally) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Capture pre-Phase-65 bundle baseline + create shared QueryClient test helper</name>
  <files>
    .planning/phases/65-item-lookup-and-not-found-flow/65-BUNDLE-BASELINE.md,
    frontend2/src/test-utils-query.tsx
  </files>
  <read_first>
    - frontend2/vite.config.ts (manual-chunks definition for the `scanner` group; confirm chunk filename pattern)
    - frontend2/package.json (scripts: `build`, `test`)
    - frontend2/src/features/scan/hooks/__tests__/useScanLookup.test.ts (existing test harness style for the scan feature)
    - frontend2/src/lib/api/__tests__/queryKeys.test.ts (existing minimal test style)
    - frontend2/src/test-utils.tsx (existing setup-file registered in vitest.config.ts; confirm barrel / helper conventions)
    - .planning/phases/65-item-lookup-and-not-found-flow/65-RESEARCH.md §Open Questions line 1144 (bundle baseline capture procedure)
    - .planning/phases/65-item-lookup-and-not-found-flow/65-PATTERNS.md §"No Analog Found" (QueryClient helper gap documented — this task closes that gap)
    - .planning/phases/64-scanner-foundation-scan-page/64-10-SUMMARY.md (Phase 64 shipped with main-chunk gzip −37.8 kB; confirm final chunk filenames)
  </read_first>
  <action>
    STEP A — Capture bundle baseline (BEFORE any Phase 65 production code lands):

    1. Run `cd frontend2 && pnpm build` (or `bun run build` if the repo uses bun — detect via `frontend2/.mise.toml` or `package.json` scripts; prefer whichever script name is declared; fall back to `pnpm run build`).
    2. From the resulting `frontend2/dist/assets/` directory, identify:
       - The main app chunk: `dist/assets/index-<hash>.js` (largest `index-*.js` by byte count).
       - The scanner chunk: `dist/assets/scanner-<hash>.js` (the `manualChunks` group named `scanner` in `vite.config.ts`).
    3. Compute gzip size of each bare JS file with `gzip -c <file> | wc -c` (so it matches Plan 08's regression measurement method 1-for-1). Do NOT trust any `.gz` artifact vite itself may emit — compute from the plain `.js` via `gzip -c` so the method is reproducible.
    4. Record in `.planning/phases/65-item-lookup-and-not-found-flow/65-BUNDLE-BASELINE.md`:
       ```markdown
       # Phase 65 Bundle Baseline (pre-phase snapshot)

       **Captured:** <YYYY-MM-DD HH:MM:SS timezone>
       **Source commit:** <output of `git rev-parse HEAD`> (must be master @ post-Phase-64-10 state)
       **Source tree status:** <output of `git status --short` — MUST be clean; fail the task if dirty>
       **Build command:** <exact command executed>
       **Node/bun/pnpm versions:** <output of the tooling the script used>

       ## Chunk sizes

       | Chunk | File | Raw bytes | gzip bytes |
       |-------|------|-----------|------------|
       | main (index) | dist/assets/index-<hash>.js | <n> | <n> |
       | scanner | dist/assets/scanner-<hash>.js | <n> | <n> |

       ## Gate for Plan 65-08

       - `scanner chunk gzip` AFTER Phase 65 ships MUST be ≤ `<captured scanner gzip>` bytes (zero regression tolerance — Phase 64 shipped −37.8 kB and MUST be preserved).
       - `main index chunk gzip` AFTER Phase 65 ships MUST be ≤ `<captured main gzip> + 5120` bytes (≤ 5 kB main-chunk delta per 65-CONTEXT `<code_context>` budget).

       ## Reproducibility

       Plan 08 MUST use the same `gzip -c <file> | wc -c` method on the post-Phase-65 chunks and record both numbers + delta in 65-08-SUMMARY.md.
       ```
    5. If `git status --short` is non-empty after `pnpm build` (i.e. build artifacts changed outside `dist/`), abort with a structured error so the baseline reflects the committed post-Phase-64 state. `dist/` is gitignored and expected to change.

    STEP B — Create the shared QueryClient test helper:

    Create `frontend2/src/test-utils-query.tsx` exporting three symbols:

    ```tsx
    import { ReactNode } from "react";
    import { render, type RenderResult } from "@testing-library/react";
    import { renderHook, type RenderHookResult } from "@testing-library/react";
    import { QueryClient, QueryClientProvider } from "@tanstack/react-query";

    /**
     * Creates a QueryClient configured for tests: retries disabled, gcTime
     * capped so unmounted queries don't linger, logger silenced. Use a FRESH
     * client per test to avoid cross-test cache bleed.
     */
    export function createTestQueryClient(): QueryClient {
      return new QueryClient({
        defaultOptions: {
          queries: { retry: false, gcTime: 0 },
          mutations: { retry: false },
        },
      });
    }

    /**
     * Renders a component inside a fresh QueryClientProvider.
     * Returns the RTL render result plus the client so tests can inspect the
     * cache (e.g. spy on invalidateQueries via vi.spyOn(client, "invalidateQueries")).
     */
    export function renderWithQueryClient(
      ui: ReactNode,
      options?: { client?: QueryClient },
    ): RenderResult & { client: QueryClient } {
      const client = options?.client ?? createTestQueryClient();
      const result = render(
        <QueryClientProvider client={client}>{ui}</QueryClientProvider>,
      );
      return { ...result, client };
    }

    /**
     * Renders a hook inside a fresh QueryClientProvider — mirrors
     * @testing-library/react's renderHook signature but guarantees a provider.
     */
    export function renderHookWithQueryClient<Result, Props>(
      hook: (initialProps: Props) => Result,
      options?: { client?: QueryClient; initialProps?: Props },
    ): RenderHookResult<Result, Props> & { client: QueryClient } {
      const client = options?.client ?? createTestQueryClient();
      const wrapper = ({ children }: { children: ReactNode }) => (
        <QueryClientProvider client={client}>{children}</QueryClientProvider>
      );
      const result = renderHook(hook, {
        wrapper,
        initialProps: options?.initialProps,
      });
      return { ...result, client };
    }
    ```

    Verification that the helper compiles: add a one-line import to an existing test file (revert before commit) or rely on vitest collection in Task 3 picking it up.

    Commit message: `test(65-01): capture Phase 65 bundle baseline + add QueryClient test helper`
  </action>
  <verify>
    <automated>cd frontend2 && pnpm typecheck</automated>
    <automated>cat .planning/phases/65-item-lookup-and-not-found-flow/65-BUNDLE-BASELINE.md | grep -E "^\| (main|scanner) \| dist/assets/"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/65-item-lookup-and-not-found-flow/65-BUNDLE-BASELINE.md` exists AND contains a line matching regex `\| main \| dist/assets/index-[a-f0-9A-Z]+\.js \| \d+ \| \d+ \|` AND a line matching `\| scanner \| dist/assets/scanner-[a-f0-9A-Z]+\.js \| \d+ \| \d+ \|`
    - `.planning/phases/65-item-lookup-and-not-found-flow/65-BUNDLE-BASELINE.md` records `git rev-parse HEAD` output (verifiable: `rg "^\*\*Source commit:\*\* [a-f0-9]{7,}" .planning/phases/65-item-lookup-and-not-found-flow/65-BUNDLE-BASELINE.md`)
    - `frontend2/src/test-utils-query.tsx` exists and exports exactly three named symbols: `createTestQueryClient`, `renderWithQueryClient`, `renderHookWithQueryClient` (verifiable: `rg "^export function (createTestQueryClient|renderWithQueryClient|renderHookWithQueryClient)" frontend2/src/test-utils-query.tsx | wc -l` returns `3`)
    - `cd frontend2 && pnpm typecheck` exits 0
    - `cd frontend2 && pnpm test:run -- test-utils-query` either runs zero tests (no `.test.*` file targets the helper directly) OR is green — it MUST NOT fail
  </acceptance_criteria>
  <done>Pre-Phase-65 baseline captured in 65-BUNDLE-BASELINE.md with verifiable git SHA + chunk byte counts; shared QueryClient helper exported and typechecks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold four domain/hook/schema test files (items.lookupByBarcode, barcode, useBarcodeEnrichment, schemas) with it.todo + import-smoke</name>
  <files>
    frontend2/src/lib/api/__tests__/items.lookupByBarcode.test.ts,
    frontend2/src/lib/api/__tests__/barcode.test.ts,
    frontend2/src/features/items/hooks/__tests__/useBarcodeEnrichment.test.ts,
    frontend2/src/features/items/forms/__tests__/schemas.test.ts
  </files>
  <read_first>
    - frontend2/src/lib/api/itemPhotos.test.ts (analog — vi.spyOn + beforeEach/afterEach teardown pattern)
    - frontend2/src/lib/api/__tests__/queryKeys.test.ts (analog — key-factory shape assertion pattern)
    - frontend2/src/lib/api/items.ts (verify current `itemsApi` exports; the `lookupByBarcode` method will be ADDED in Plan 65-02 — scaffold uses `it.todo` for behavioral tests and one green `expect(typeof itemsApi.list).toBe("function")` import-smoke case)
    - frontend2/src/features/items/forms/schemas.ts (current schema; Plan 65-02 adds `brand` + loosens barcode regex per D-23/D-24 in CONTEXT.md)
    - frontend2/src/features/scan/hooks/__tests__/useScanLookup.test.ts (analog — ScanLookupStatus compile-time assertion pattern to mirror for key-factory typing)
    - frontend2/src/test-utils-query.tsx (Task 1 helper — import `renderHookWithQueryClient` for the enrichment hook scaffold)
    - .planning/phases/65-item-lookup-and-not-found-flow/65-CONTEXT.md §Decisions D-23 (optional brand field) + D-24 (barcode regex loosening) — scaffold mirrors these locked scope additions
    - .planning/phases/65-item-lookup-and-not-found-flow/65-VALIDATION.md §"Per-Task Verification Map" (every acceptance criterion must map to an `it` or `it.todo`)
    - .planning/phases/65-item-lookup-and-not-found-flow/65-RESEARCH.md §"Phase Requirements → Test Map" lines 1009-1037 (enumerates every acceptance criterion → test case for these files)
  </read_first>
  <action>
    Create each file with imports that compile today and `it.todo("...")` entries for behavior that will land in Waves 1-2. Add `it(...)` blocks ONLY for facts already true (import-smoke, type shape).

    FILE 1 — `frontend2/src/lib/api/__tests__/items.lookupByBarcode.test.ts`:
    ```ts
    // Phase 65 Wave 0 scaffold (Plan 65-01 Task 2). Behavioral tests are
    // todos until Plan 65-02 adds itemsApi.lookupByBarcode. Turn each
    // it.todo into a real it() with vi.spyOn(itemsApi, "list") assertions
    // as part of Plan 65-02.
    import { describe, it, expect } from "vitest";
    import { itemsApi } from "@/lib/api/items";

    describe("itemsApi.lookupByBarcode", () => {
      it("itemsApi barrel is importable and has the expected siblings", () => {
        expect(typeof itemsApi.list).toBe("function");
        expect(typeof itemsApi.get).toBe("function");
        expect(typeof itemsApi.create).toBe("function");
      });

      // D-06: wraps list(wsId, { search: code, limit: 1 })
      it.todo("D-06: calls itemsApi.list with { search: code, limit: 1 }");
      // D-07: case-sensitive exact-barcode guard — returns null on mismatch
      it.todo("D-07: returns null when items[0].barcode !== code (case-sensitive)");
      it.todo("D-07: returns null on empty items[] response");
      // D-08: workspace defense-in-depth + structured console.error
      it.todo("D-08: returns null + logs { kind: \"scan-workspace-mismatch\" } when workspace_id mismatch");
      // Happy path
      it.todo("returns the Item on exact barcode + workspace_id match");
    });
    ```

    FILE 2 — `frontend2/src/lib/api/__tests__/barcode.test.ts`:
    ```ts
    // Phase 65 Wave 0 scaffold (Plan 65-01 Task 2). barcode.ts is created
    // in Plan 65-03; this file uses it.todo until then. DO NOT import from
    // @/lib/api/barcode — the module does not yet exist and the import
    // would break collection. Re-enable the import in Plan 65-03.
    import { describe, it } from "vitest";

    describe("barcodeApi.lookup (D-11)", () => {
      it.todo("D-11: calls GET /barcode/{encodeURIComponent(code)} (public, unauth path — no wsId prefix)");
      it.todo("D-11: passes through the BarcodeProduct response shape");
    });

    describe("barcodeKeys factory (D-11)", () => {
      it.todo("barcodeKeys.all equals [\"barcode\"]");
      it.todo("barcodeKeys.lookups() equals [\"barcode\", \"lookup\"]");
      it.todo("barcodeKeys.lookup(\"5449000000996\") equals [\"barcode\", \"lookup\", \"5449000000996\"]");
    });
    ```

    FILE 3 — `frontend2/src/features/items/hooks/__tests__/useBarcodeEnrichment.test.ts`:
    ```ts
    // Phase 65 Wave 0 scaffold (Plan 65-01 Task 2). Hook body lands in
    // Plan 65-03. Scaffold documents the full regex-gate matrix + staleTime
    // + silent-fail expectations so Plan 65-03 turns todos into real
    // renderHookWithQueryClient assertions.
    import { describe, it } from "vitest";

    describe("useBarcodeEnrichment regex gate (D-12) — /^\\d{8,14}$/", () => {
      it.todo("code=null → enabled: false (no fetch)");
      it.todo("code=\"\" → enabled: false");
      it.todo("code=\"1234567\" (7 digits, below floor) → enabled: false");
      it.todo("code=\"12345678\" (8 digits, boundary) → enabled: true");
      it.todo("code=\"12345678901234\" (14 digits, boundary) → enabled: true");
      it.todo("code=\"123456789012345\" (15 digits, above ceiling) → enabled: false");
      it.todo("code=\"ABC12345678\" (non-numeric) → enabled: false");
      it.todo("code=\"5449000000996\" (valid UPC) → enabled: true, fetches barcodeApi.lookup");
    });

    describe("useBarcodeEnrichment caching (D-12)", () => {
      it.todo("staleTime: Infinity — second render with same code does not refetch");
      it.todo("retry: false — network error does not auto-retry");
    });

    describe("useBarcodeEnrichment silent failure (D-16)", () => {
      it.todo("network error → logs { kind: \"upc-enrichment-fail\", code, error, timestamp } then throws to TanStack (status: error)");
      it.todo("{ found: false } response → query resolves success with data.found === false (banner will noop-render)");
    });
    ```

    FILE 4 — `frontend2/src/features/items/forms/__tests__/schemas.test.ts` (NEW — covers D-23 + D-24 per 65-CONTEXT.md):
    ```ts
    // Phase 65 Wave 0 scaffold (Plan 65-01 Task 2). itemCreateSchema gains an
    // optional brand field (D-23) and a loosened barcode regex accepting
    // hyphens and underscores (D-24) in Plan 65-02 Task 1. Scaffold enumerates
    // the new regression cases as it.todo — Plan 65-02 converts them into
    // real passing assertions after the schema changes land.
    //
    // The single green it() below proves the schema module is importable and
    // the existing pre-Phase-65 barcode regex still rejects "abc def" — a
    // regression tripwire that survives the D-24 loosening (spaces are still
    // forbidden).
    import { describe, it, expect } from "vitest";
    import { itemCreateSchema } from "@/features/items/forms/schemas";

    describe("itemCreateSchema module import smoke", () => {
      it("itemCreateSchema rejects barcode with a space character (regression tripwire)", () => {
        const result = itemCreateSchema.safeParse({
          name: "X",
          sku: "ITEM-1",
          barcode: "has space",
        });
        expect(result.success).toBe(false);
        if (!result.success) {
          // Some zod issue should be on the barcode field
          const barcodeIssue = result.error.issues.find((i) => i.path[0] === "barcode");
          expect(barcodeIssue).toBeDefined();
        }
      });

      // D-24: barcode regex loosening — Plan 65-02 Task 1 converts these to real it() blocks
      it.todo("D-24: itemCreateSchema.barcode accepts \"ABC-123\" (hyphen)");
      it.todo("D-24: itemCreateSchema.barcode accepts \"code_128\" (underscore)");
      it.todo("D-24: itemCreateSchema.barcode still rejects \"abc def\" (space forbidden)");

      // D-23: optional brand field — Plan 65-02 Task 1 converts these to real it() blocks
      it.todo("D-23: itemCreateSchema accepts optional brand=\"Coca-Cola\"");
      it.todo("D-23: itemCreateSchema accepts empty/omitted brand (undefined)");
    });
    ```

    All four files MUST exit 0 under `pnpm test:run` collection (it.todo reports PENDING, not failing). One green `it(...)` each in FILE 1 and FILE 4 proves the test harness wires correctly.

    Commit message: `test(65-01): scaffold 4 domain/hook/schema test files (items.lookupByBarcode, barcode, useBarcodeEnrichment, schemas)`
  </action>
  <verify>
    <automated>cd frontend2 && pnpm test:run -- src/lib/api/__tests__/items.lookupByBarcode.test.ts src/lib/api/__tests__/barcode.test.ts src/features/items/hooks/__tests__/useBarcodeEnrichment.test.ts src/features/items/forms/__tests__/schemas.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend2/src/lib/api/__tests__/items.lookupByBarcode.test.ts` exists; `rg "it\.todo\(" frontend2/src/lib/api/__tests__/items.lookupByBarcode.test.ts | wc -l` returns `5`; `rg "^\s*it\(" frontend2/src/lib/api/__tests__/items.lookupByBarcode.test.ts | wc -l` returns `1` (the import-smoke case)
    - `frontend2/src/lib/api/__tests__/barcode.test.ts` exists; `rg "it\.todo\(" frontend2/src/lib/api/__tests__/barcode.test.ts | wc -l` returns `5`
    - `frontend2/src/features/items/hooks/__tests__/useBarcodeEnrichment.test.ts` exists; `rg "it\.todo\(" frontend2/src/features/items/hooks/__tests__/useBarcodeEnrichment.test.ts | wc -l` returns `13`
    - `frontend2/src/features/items/forms/__tests__/schemas.test.ts` exists; `rg "it\.todo\(" frontend2/src/features/items/forms/__tests__/schemas.test.ts | wc -l` returns `5` (3 D-24 + 2 D-23); `rg "^\s*it\(" frontend2/src/features/items/forms/__tests__/schemas.test.ts | wc -l` returns `1` (the space-rejection tripwire)
    - All four files compile under `pnpm typecheck`
    - `cd frontend2 && pnpm test:run -- src/lib/api/__tests__/items.lookupByBarcode.test.ts src/lib/api/__tests__/barcode.test.ts src/features/items/hooks/__tests__/useBarcodeEnrichment.test.ts src/features/items/forms/__tests__/schemas.test.ts` exits 0 with 2 import-smoke + tripwire tests green and 28 todos reported as pending
  </acceptance_criteria>
  <done>Four scaffold files land; vitest collects them; 2 green tests prove the test harness is wired; 28 todos enumerate every behavioral acceptance criterion for Waves 1-2 (including D-23 + D-24 user-locked scope additions).</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold three component/integration test files (ItemFormPage, UpcSuggestionBanner, ScanResultBanner.states)</name>
  <files>
    frontend2/src/features/items/__tests__/ItemFormPage.test.tsx,
    frontend2/src/features/items/__tests__/UpcSuggestionBanner.test.tsx,
    frontend2/src/components/scan/__tests__/ScanResultBanner.states.test.tsx
  </files>
  <read_first>
    - frontend2/src/features/items/__tests__/ItemDetailPage.test.tsx (analog — MemoryRouter + QueryClient + mocked useAuth idiom)
    - frontend2/src/features/items/__tests__/ItemPanel.test.tsx (analog — dirty-guard + SKU-once + ItemForm consumer)
    - frontend2/src/components/scan/__tests__/ScanResultBanner.test.tsx (existing 113-line file; the NEW states scaffold is a SIBLING file, not a modification — existing tests 1-7 stay green under the current MATCH-only banner, Plan 65-06 migrates them)
    - frontend2/src/test-utils-query.tsx (Task 1 — import `renderWithQueryClient`)
    - .planning/phases/65-item-lookup-and-not-found-flow/65-UI-SPEC.md §"Copywriting Contract" (exact copy strings for every state + banner)
    - .planning/phases/65-item-lookup-and-not-found-flow/65-RESEARCH.md §"Phase Requirements → Test Map" lines 1022-1043 (enumerates every acceptance criterion → test case for these three files)
  </read_first>
  <action>
    FILE 1 — `frontend2/src/features/items/__tests__/ItemFormPage.test.tsx`:
    ```tsx
    // Phase 65 Wave 0 scaffold (Plan 65-01 Task 3). ItemFormPage lands in
    // Plan 65-05. Scaffold enumerates every acceptance criterion from
    // 65-CONTEXT D-01..D-05 + D-13..D-16 integration as it.todo. Plan 65-05
    // turns these into real user-event flows inside MemoryRouter +
    // QueryClientProvider.
    import { describe, it } from "vitest";

    describe("ItemFormPage route + URL-state (D-01, D-02)", () => {
      it.todo("D-01: <Route path=\"items/new\" element={<ItemFormPage />}> is reachable from AppRoutes");
      it.todo("D-02: reads ?barcode= via useSearchParams and passes to ItemForm defaultValues.barcode");
      it.todo("D-02: generateSku() fires exactly once per mount (not per render)");
      it.todo("D-02: renders normally with no ?barcode= param (form usable as plain create page)");
    });

    describe("ItemFormPage chrome (D-03)", () => {
      it.todo("D-03: heading renders t`NEW ITEM` at h1 level");
      it.todo("D-03: submit button reads t`CREATE ITEM` when idle, t`WORKING…` when mutation isPending");
      it.todo("D-03: CANCEL button on CLEAN form calls navigate(-1)");
      it.todo("D-03: CANCEL button on DIRTY form opens RetroConfirmDialog with t`DISCARD CHANGES?` title");
      it.todo("D-03: DISCARD destructive button inside dialog calls navigate(-1)");
      it.todo("D-03: ← BACK button inside dialog closes dialog without nav");
    });

    describe("ItemFormPage create flow (D-04, D-05)", () => {
      it.todo("D-04: successful create invalidates scanKeys.lookup(barcode) AND itemKeys.all");
      it.todo("D-04: successful create navigates to /items/{created.id}");
      it.todo("D-05: uses useCreateItem from ../hooks/useItemMutations (no duplicate mutation logic)");
    });

    describe("ItemFormPage + UpcSuggestionBanner integration (D-13..D-16)", () => {
      it.todo("D-13: renders UpcSuggestionBanner ABOVE ItemForm when enrichment.data.found === true");
      it.todo("D-14: [USE] chip on a suggested field calls setValue(field, value, { shouldDirty: true })");
      it.todo("D-15: category shown as helper text only — no [USE] chip on category row");
      it.todo("D-16: enrichment.isError → banner does NOT render + console.error kind: upc-enrichment-fail logged");
      it.todo("D-16: enrichment.data.found === false → banner does NOT render");
    });
    ```

    FILE 2 — `frontend2/src/features/items/__tests__/UpcSuggestionBanner.test.tsx`:
    ```tsx
    // Phase 65 Wave 0 scaffold (Plan 65-01 Task 3). UpcSuggestionBanner
    // lands in Plan 65-05. Scaffold enumerates D-13..D-16 as it.todo.
    import { describe, it } from "vitest";

    describe("UpcSuggestionBanner render contract (D-13)", () => {
      it.todo("D-13: renders yellow HazardStripe + h2 t`SUGGESTIONS AVAILABLE`");
      it.todo("D-13: no new retro atom imported — uses RetroPanel + HazardStripe + RetroButton from @/components/retro barrel only");
    });

    describe("UpcSuggestionBanner per-field acceptance (D-14, D-23)", () => {
      it.todo("D-14: name row renders label t`NAME` + value + [USE] RetroButton variant=\"primary\"");
      it.todo("D-14/D-23: brand row renders label t`BRAND` + value + [USE] when brand non-empty — writes to form.brand field (NOT description)");
      it.todo("D-14: brand row does NOT render when brand is null/empty");
      it.todo("D-14: [USE] click calls useFormContext().setValue(field, value, { shouldDirty: true })");
      it.todo("D-14/D-23: USE ALL button applies every non-empty field in one click (name AND brand via first-class setValue calls)");
      it.todo("D-14: DISMISS collapses the banner locally (no cache / no form writes)");
    });

    describe("UpcSuggestionBanner category hint (D-15)", () => {
      it.todo("D-15: non-empty external_category renders helper text t`Category hint: ${cat} — pick manually below.` with NO [USE] chip");
      it.todo("D-15: empty external_category does not render the helper row");
      it.todo("D-15: setValue is NEVER called with (\"category_id\", ...) anywhere in the banner's render/event path");
    });

    describe("UpcSuggestionBanner silent failure (D-16)", () => {
      it.todo("D-16: renders null when data.found === false (caller is responsible for gating)");
    });
    ```

    FILE 3 — `frontend2/src/components/scan/__tests__/ScanResultBanner.states.test.tsx` (SIBLING of existing `ScanResultBanner.test.tsx`, not a replacement — Plan 65-06 will migrate/merge):
    ```tsx
    // Phase 65 Wave 0 scaffold (Plan 65-01 Task 3). ScanResultBanner is
    // widened in Plan 65-06 to four states (LOADING / MATCH / NOT-FOUND /
    // ERROR per D-17..D-21). This file is a SIBLING to
    // ScanResultBanner.test.tsx — the existing file's 7 tests stay green
    // under the Phase 64 MATCH-only shape until Plan 65-06 migrates them.
    // Plan 65-06 converts these it.todo into real assertions using the
    // renderWithQueryClient helper from @/test-utils-query.
    import { describe, it } from "vitest";

    describe("ScanResultBanner LOADING state (D-20)", () => {
      it.todo("D-20: lookupStatus=\"loading\" renders h2 t`LOOKING UP…`");
      it.todo("D-20: LOADING renders dimmed code echo (text-retro-charcoal/60)");
      it.todo("D-20: LOADING renders SCAN AGAIN button AS INTERACTIVE (not disabled)");
      it.todo("D-20: LOADING renders a blinking cursor glyph ▍ with class that maps to @keyframes retro-cursor-blink");
      it.todo("D-20: LOADING does NOT render a hazard stripe");
    });

    describe("ScanResultBanner MATCH state (D-18)", () => {
      it.todo("D-18: lookupStatus=\"success\" + match!=null renders h2 t`MATCHED`");
      it.todo("D-18: MATCH renders t`NAME` label + match.name value (24px mono bold)");
      it.todo("D-18: MATCH renders t`CODE` label + match.short_code value (mono)");
      it.todo("D-18: MATCH VIEW ITEM button onClick calls onViewItem(match.id)");
      it.todo("D-18: MATCH also renders SCAN AGAIN");
    });

    describe("ScanResultBanner NOT-FOUND state (D-19)", () => {
      it.todo("D-19: lookupStatus=\"success\" + match===null renders yellow HazardStripe");
      it.todo("D-19: NOT-FOUND renders h2 t`NOT FOUND`");
      it.todo("D-19: NOT-FOUND renders helper line t`No item in this workspace matches this barcode.`");
      it.todo("D-19: NOT-FOUND CREATE ITEM WITH THIS BARCODE button onClick calls onCreateWithBarcode(code)");
      it.todo("D-19: NOT-FOUND also renders SCAN AGAIN");
    });

    describe("ScanResultBanner ERROR state (D-21)", () => {
      it.todo("D-21: lookupStatus=\"error\" renders red HazardStripe");
      it.todo("D-21: ERROR renders h2 t`LOOKUP FAILED`");
      it.todo("D-21: ERROR RETRY button onClick calls onRetry");
      it.todo("D-21: ERROR also renders CREATE ITEM WITH THIS BARCODE fallback");
      it.todo("D-21: ERROR also renders SCAN AGAIN");
    });
    ```

    Commit message: `test(65-01): scaffold 3 component/integration test files (ItemFormPage, UpcSuggestionBanner, ScanResultBanner.states)`
  </action>
  <verify>
    <automated>cd frontend2 && pnpm test:run -- src/features/items/__tests__/ItemFormPage.test.tsx src/features/items/__tests__/UpcSuggestionBanner.test.tsx src/components/scan/__tests__/ScanResultBanner.states.test.tsx</automated>
    <automated>cd frontend2 && pnpm test:run -- src/components/scan/__tests__/ScanResultBanner.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Three new scaffold files exist
    - `rg "it\.todo\(" frontend2/src/features/items/__tests__/ItemFormPage.test.tsx | wc -l` returns `17`
    - `rg "it\.todo\(" frontend2/src/features/items/__tests__/UpcSuggestionBanner.test.tsx | wc -l` returns `12`
    - `rg "it\.todo\(" frontend2/src/components/scan/__tests__/ScanResultBanner.states.test.tsx | wc -l` returns `21`
    - All three scaffolds collect under `pnpm test:run` and exit 0 (todos report as pending, not failing)
    - Existing `frontend2/src/components/scan/__tests__/ScanResultBanner.test.tsx` STAYS GREEN (its 7 Phase 64 MATCH-only tests must still pass — Plan 65-06 will migrate them, not this plan)
    - All three scaffolds compile under `pnpm typecheck`
  </acceptance_criteria>
  <done>Seven Wave 0 test files in place (6 original + 1 new schemas); 78 total `it.todo` entries enumerate every Phase 65 acceptance criterion (73 original + 5 D-23/D-24); existing Phase 64 tests remain green; the sampling-rate contract from 65-VALIDATION.md is now satisfiable by Waves 1-4 simply converting todos to real assertions.</done>
</task>

</tasks>

<verification>
- All seven Wave 0 test files listed in 65-VALIDATION.md §"Wave 0 Requirements" (+ the new D-23/D-24 schemas scaffold) now exist
- `cd frontend2 && pnpm test:run` still exits 0 (new files contribute pending todos, not failures; Phase 64 + Phase 60 existing tests stay green)
- `cd frontend2 && pnpm typecheck` exits 0
- `cd frontend2 && pnpm lint:imports` exits 0 (no forbidden `idb` / `serwist` / `offline` / `sync` substrings introduced)
- 65-BUNDLE-BASELINE.md records pre-phase gzip bytes + git SHA + build command, verifiable via `rg "Source commit: [a-f0-9]" .planning/phases/65-item-lookup-and-not-found-flow/65-BUNDLE-BASELINE.md`
- `rg "^export function (createTestQueryClient|renderWithQueryClient|renderHookWithQueryClient)" frontend2/src/test-utils-query.tsx | wc -l` returns `3`
</verification>

<success_criteria>
- 65-VALIDATION.md §"Wave 0 Requirements" checklist: all boxes can be checked (including the new schemas.test.ts scaffold)
- 78 `it.todo` entries across 7 scaffold files cover every LOOK-01/02/03 acceptance criterion + D-01..D-24 decision-level test obligation (D-23 + D-24 are user-locked scope additions in CONTEXT.md)
- Bundle baseline captured with reproducible gzip measurement method (future Plan 08 can re-run and diff)
- Phase 64 regression surface untouched (ScanPage.test.tsx Test 15 + existing ScanResultBanner tests remain green)
</success_criteria>

<output>
After completion, create `.planning/phases/65-item-lookup-and-not-found-flow/65-01-SUMMARY.md` with:
- Exact byte numbers captured in 65-BUNDLE-BASELINE.md (so downstream plans can cite without re-reading)
- Count of it.todo entries per file (for quick reconciliation in Plan 08 gate) — expected: 5 + 5 + 13 + 17 + 12 + 5 + 21 = 78
- Any deviation from the scaffold (e.g. if `itemPhotos.test.ts` analog revealed a different vi.spyOn convention that scaffolds adopted)
</output>
