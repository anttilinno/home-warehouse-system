---
phase: 54-tech-debt-code-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend2/src/lib/types.ts
  - frontend2/src/routes/index.tsx
  - frontend2/locales/en/messages.po
  - frontend2/locales/et/messages.po
  - frontend2/src/features/auth/AuthCallbackPage.tsx
  - frontend2/src/features/settings/AppearancePage.tsx
  - frontend2/src/features/settings/DataPage.tsx
  - frontend2/src/features/settings/NotificationsPage.tsx
  - frontend2/src/features/settings/LanguagePage.tsx
  - frontend2/src/features/settings/FormatsPage.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "entity_name field on RecentActivity is typed as string | null (not optional)"
    - "NotFoundPage strings are wrapped in Lingui t macro"
    - "SECTOR NOT FOUND and its body string are present in both EN and ET catalogs"
    - "AuthCallbackPage uses <HazardStripe> component instead of inline div"
    - "All 5 settings pages import useToast from @/components/retro barrel"
  artifacts:
    - path: frontend2/src/lib/types.ts
      provides: "entity_name without optional marker"
      contains: "entity_name: string | null"
    - path: frontend2/locales/en/messages.po
      provides: "SECTOR NOT FOUND catalog entry"
      contains: "SECTOR NOT FOUND"
    - path: frontend2/locales/et/messages.po
      provides: "ET translation for SECTOR NOT FOUND"
      contains: "SEKTOR EI LEITUD"
    - path: frontend2/src/features/auth/AuthCallbackPage.tsx
      provides: "HazardStripe component usage"
      contains: "<HazardStripe"
  key_links:
    - from: frontend2/src/features/auth/AuthCallbackPage.tsx
      to: frontend2/src/components/retro/index.ts
      via: "import { HazardStripe } from '@/components/retro'"
      pattern: "from \"@/components/retro\""
---

<objective>
Close four code-level tech debt items in the frontend2 i18n, types, and component consistency layer: fix the entity_name type optional marker (SC-4), wrap NotFoundPage strings in the Lingui t macro and update catalogs (SC-5), replace AuthCallbackPage's inline hazard stripe div with the HazardStripe component (SC-6), and migrate all five settings pages from direct RetroToast imports to the barrel (SC-7).

Purpose: Correct type contract mismatch, missing i18n coverage, and component inconsistency — all pure correctness fixes with no behavior change.
Output: Fixed types.ts, localized NotFoundPage with updated .po files, HazardStripe in AuthCallbackPage, barrel imports in 5 settings pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/54-tech-debt-code-fixes/54-CONTEXT.md
@.planning/phases/54-tech-debt-code-fixes/54-RESEARCH.md
@.planning/phases/54-tech-debt-code-fixes/54-VALIDATION.md
</context>

<interfaces>
<!-- Key patterns extracted from source files for executor reference -->

From frontend2/src/components/retro/HazardStripe.tsx (verified):
- Props: `height?: number` (default 8), `className?: string`
- Default height 8px matches the inline div's `h-[8px]`
- Usage: `<HazardStripe className="mb-md" />` (passes spacing class; height defaults to 8)

From frontend2/src/components/retro/index.ts (verified):
- `useToast` exported on line 11
- `HazardStripe` exported on line 4
- Barrel path: `@/components/retro`

From frontend2/src/features/settings/AppearancePage.tsx (verified):
- Line 4: `import { RetroPanel, RetroButton } from "@/components/retro";`
- Line 7: `import { useToast } from "@/components/retro/RetroToast";`  ← merge into line 4

Lingui t macro pattern (established in project):
```tsx
const { t } = useLingui();
// inside JSX:
{t`SECTOR NOT FOUND`}
```
`useLingui` imported from `@lingui/react/macro`.

ET translations for NotFoundPage (from RESEARCH.md, flag for human review):
- "SECTOR NOT FOUND" → "SEKTOR EI LEITUD"
- "The requested area does not exist. Return to base." → "Soovitud ala ei eksisteeri. Naase baasi."
- "RETURN TO BASE" → already in ET catalog as "TAGASI BAASI" (do not duplicate)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix entity_name optional marker in types.ts</name>
  <files>
    frontend2/src/lib/types.ts
  </files>
  <read_first>
    - frontend2/src/lib/types.ts — full file; locate RecentActivity interface; confirm `entity_name?: string | null` at line 107
  </read_first>
  <action>
    In the `RecentActivity` interface in `frontend2/src/lib/types.ts`, change:
    ```typescript
    entity_name?: string | null;
    ```
    to:
    ```typescript
    entity_name: string | null;
    ```
    Remove the `?` optional marker. The field is always present in backend responses (nullable value, never absent key).

    After making this change, run `bun run build` to catch any call sites that used optional chaining (`?.entity_name`) assuming the field might be absent. If TypeScript reports errors, update those call sites: change `obj?.entity_name` to `obj.entity_name` (the null check becomes `!= null` not `?.` since the field is always present but may be null).
  </action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system/frontend2 && bun run build</automated>
  </verify>
  <done>
    - `grep "entity_name: string | null" frontend2/src/lib/types.ts` returns a match
    - `grep "entity_name?: string | null" frontend2/src/lib/types.ts` returns no match
    - `bun run build` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Wrap NotFoundPage strings in t macro and update EN + ET catalogs</name>
  <files>
    frontend2/src/routes/index.tsx
    frontend2/locales/en/messages.po
    frontend2/locales/et/messages.po
  </files>
  <read_first>
    - frontend2/src/routes/index.tsx — NotFoundPage function (lines 22-41); current imports (no useLingui import present); RetroPanel, Link imports already present
    - frontend2/locales/en/messages.po — check whether "SECTOR NOT FOUND" already exists (research says it does NOT); confirm "RETURN TO BASE" already exists
    - frontend2/locales/et/messages.po — same checks; ET translation for "RETURN TO BASE" is "TAGASI BAASI" (already present, do not duplicate)
  </read_first>
  <action>
    In `frontend2/src/routes/index.tsx`:
    1. Add `useLingui` to the imports. There is currently no Lingui import in this file. Add: `import { useLingui } from "@lingui/react/macro";`
    2. Inside the `NotFoundPage` function body (before the return), add: `const { t } = useLingui();`
    3. Wrap all three hardcoded strings in the `t` tagged-template macro:
       - `"SECTOR NOT FOUND"` → `{t\`SECTOR NOT FOUND\`}`
       - `"The requested area does not exist. Return to base."` → `{t\`The requested area does not exist. Return to base.\`}`
       - `"RETURN TO BASE"` → `{t\`RETURN TO BASE\`}` (inside the Link element)

    Then run Lingui extraction to update catalogs:
    ```bash
    cd frontend2 && bun run i18n:extract
    ```
    This updates both `locales/en/messages.po` and `locales/et/messages.po` with new keys.

    After extraction, manually add ET translations in `locales/et/messages.po`:
    - Find `msgid "SECTOR NOT FOUND"` → set `msgstr "SEKTOR EI LEITUD"`
    - Find `msgid "The requested area does not exist. Return to base."` → set `msgstr "Soovitud ala ei eksisteeri. Naase baasi."`
    - `"RETURN TO BASE"` already has `msgstr "TAGASI BAASI"` — verify it is present with that translation and do NOT add a duplicate entry. The extraction may add a second source comment to the existing entry; that is expected and correct.

    Important: `bun run i18n:extract` must be run from the `frontend2/` directory (not project root).
  </action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system/frontend2 && grep "SECTOR NOT FOUND" locales/en/messages.po && grep "SEKTOR EI LEITUD" locales/et/messages.po && bun run build</automated>
  </verify>
  <done>
    - `grep "SECTOR NOT FOUND" frontend2/locales/en/messages.po` returns a match
    - `grep "SEKTOR EI LEITUD" frontend2/locales/et/messages.po` returns a match
    - `grep "useLingui" frontend2/src/routes/index.tsx` returns a match
    - EN catalog has exactly ONE `msgid "RETURN TO BASE"` entry (not duplicated)
    - `bun run build` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 3: Replace inline hazard stripe div in AuthCallbackPage + fix barrel imports in 5 settings pages</name>
  <files>
    frontend2/src/features/auth/AuthCallbackPage.tsx
    frontend2/src/features/settings/AppearancePage.tsx
    frontend2/src/features/settings/DataPage.tsx
    frontend2/src/features/settings/NotificationsPage.tsx
    frontend2/src/features/settings/LanguagePage.tsx
    frontend2/src/features/settings/FormatsPage.tsx
  </files>
  <read_first>
    - frontend2/src/features/auth/AuthCallbackPage.tsx — line 50: `<div className="bg-hazard-stripe h-[8px] mb-md" />`; current imports (no @/components/retro import present — must add one)
    - frontend2/src/features/settings/AppearancePage.tsx — line 4 barrel import, line 7 direct RetroToast import; check what symbols are already imported from barrel to merge correctly
    - frontend2/src/features/settings/DataPage.tsx — line 4 barrel import, line 5 direct HazardStripe import, line 7 direct RetroToast import; note: also has `HazardStripe` imported directly from `@/components/retro/HazardStripe` — fix BOTH non-barrel imports in this file
    - frontend2/src/features/settings/NotificationsPage.tsx — check current imports for both useToast and HazardStripe direct imports (research notes HazardStripe also imported directly here)
    - frontend2/src/features/settings/LanguagePage.tsx — check current imports; line 6 direct RetroToast import
    - frontend2/src/features/settings/FormatsPage.tsx — check current imports; line 7 direct RetroToast import
  </read_first>
  <action>
    **AuthCallbackPage.tsx:**
    Add a new import line for HazardStripe. AuthCallbackPage currently has NO import from `@/components/retro`. Add:
    ```typescript
    import { HazardStripe } from "@/components/retro";
    ```
    Place it after the existing `@/lib/api` import and before the `@/lib/types` import, or grouped with other component imports — follow the existing import ordering convention in the file.
    
    Replace the inline div on line 50:
    ```tsx
    // Remove:
    <div className="bg-hazard-stripe h-[8px] mb-md" />
    // Add:
    <HazardStripe className="mb-md" />
    ```
    HazardStripe default height is 8px — no `height` prop needed.

    **Settings pages — barrel import fix (per D-12):**
    For each of the 5 settings pages, merge the `useToast` import from `@/components/retro/RetroToast` into the existing barrel import from `@/components/retro`. Pattern:
    ```typescript
    // Before (example from AppearancePage):
    import { RetroPanel, RetroButton } from "@/components/retro";
    import { useToast } from "@/components/retro/RetroToast";
    // After:
    import { RetroPanel, RetroButton, useToast } from "@/components/retro";
    ```
    Read each file first to see exactly which symbols are already imported from the barrel, then merge `useToast` (and `HazardStripe` if it has its own non-barrel line) into that import.

    For DataPage.tsx: also fix the `HazardStripe` import (line 5: `import { HazardStripe } from "@/components/retro/HazardStripe"`) — merge into the barrel import alongside useToast.

    For NotificationsPage.tsx: if HazardStripe is imported directly (not from barrel), merge it into the barrel import as well.

    After all changes, run the test suite and build to confirm no regressions.
  </action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system/frontend2 && grep -r "retro/RetroToast" src/features/settings/ && echo "FAIL: direct imports remain" || echo "PASS: no direct RetroToast imports"; bun run test && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "retro/RetroToast" frontend2/src/features/settings/` exits 1 (no matches — all direct imports removed)
    - `grep "HazardStripe" frontend2/src/features/auth/AuthCallbackPage.tsx` returns `<HazardStripe` (component usage, not div)
    - `grep 'from "@/components/retro/HazardStripe"' frontend2/src/features/auth/AuthCallbackPage.tsx` returns no match
    - `bun run build` exits 0
    - All tests pass
  </acceptance_criteria>
  <done>
    - No settings page imports useToast from `@/components/retro/RetroToast` (direct path)
    - AuthCallbackPage uses `<HazardStripe className="mb-md" />` component, not inline div
    - AuthCallbackPage imports HazardStripe from `@/components/retro` barrel
    - TypeScript build clean, all tests pass
  </done>
</task>

</tasks>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| entity_name type change breaks call sites that assumed optional presence | low | After removing `?`, executor runs `bun run build` immediately to catch TypeScript errors at all call sites before committing. Runtime behavior unchanged (field is always present in API responses). |
| ET translation strings incorrect or culturally wrong | low | Translations flagged as human-reviewed recommendations in RESEARCH.md ("SEKTOR EI LEITUD", "Naase baasi."). Low severity — strings are UI labels, not security-relevant data. |
| Duplicate catalog key for "RETURN TO BASE" after i18n:extract | low | Lingui merges source comments on repeated keys — one msgid entry with two source comment lines is expected and correct. Executor must verify only ONE msgid "RETURN TO BASE" entry exists after extraction. |
</threat_model>

<verification>
After all three tasks:
1. `grep "entity_name: string | null" frontend2/src/lib/types.ts` — no `?` marker
2. `grep "SECTOR NOT FOUND" frontend2/locales/en/messages.po` — entry present
3. `grep "SEKTOR EI LEITUD" frontend2/locales/et/messages.po` — ET translation present
4. `grep "useLingui" frontend2/src/routes/index.tsx` — Lingui import present in file
5. `grep "<HazardStripe" frontend2/src/features/auth/AuthCallbackPage.tsx` — component used
6. `grep -r "retro/RetroToast" frontend2/src/features/settings/` — exits 1 (no matches)
7. `cd frontend2 && bun run test && bun run build` — full suite green, TypeScript clean
</verification>

<success_criteria>
- SC-4: `entity_name: string | null` in RecentActivity (no `?`); TypeScript build clean
- SC-5: NotFoundPage strings wrapped in t macro; "SECTOR NOT FOUND" + body string in both EN and ET .po files
- SC-6: AuthCallbackPage uses `<HazardStripe className="mb-md" />` from `@/components/retro`; no inline hazard div
- SC-7: Zero occurrences of `@/components/retro/RetroToast` import in settings pages; all use `@/components/retro` barrel
- All existing tests continue to pass; build clean
</success_criteria>

<output>
After completion, create `.planning/phases/54-tech-debt-code-fixes/54-02-SUMMARY.md`
</output>
