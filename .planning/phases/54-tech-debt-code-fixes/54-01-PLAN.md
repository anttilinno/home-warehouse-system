---
phase: 54-tech-debt-code-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend2/src/components/layout/Sidebar.tsx
  - frontend2/src/components/layout/__tests__/Sidebar.test.tsx
  - frontend2/src/lib/api.ts
  - frontend2/src/features/auth/AuthContext.tsx
  - frontend2/src/features/settings/DataPage.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Sidebar renders 4 NavLinks in order: DASHBOARD, ITEMS, LOANS, SETTINGS"
    - "Sidebar tests cover all 4 NavLinks with href and active-state assertions"
    - "api.ts exports HttpError class that extends Error with a status: number field"
    - "AuthContext clears refresh token only on HttpError with status 401 or 403"
    - "DataPage EXPORT button is disabled when exporting OR when workspaceId is falsy"
    - "DataPage IMPORT button is disabled when importing OR when workspaceId is falsy"
  artifacts:
    - path: frontend2/src/lib/api.ts
      provides: "HttpError class export"
      contains: "export class HttpError"
    - path: frontend2/src/components/layout/Sidebar.tsx
      provides: "4 NavLinks"
      contains: "to=\"/items\""
    - path: frontend2/src/features/settings/DataPage.tsx
      provides: "workspaceId null-guard on both buttons"
      contains: "!workspaceId"
  key_links:
    - from: frontend2/src/features/auth/AuthContext.tsx
      to: frontend2/src/lib/api.ts
      via: "import { HttpError } from '@/lib/api'"
      pattern: "instanceof HttpError"
---

<objective>
Close four code-level tech debt items in the frontend2 Nav + Auth layer: add ITEMS and LOANS sidebar links (SC-1), introduce the HttpError class and harden AuthContext to only clear tokens on 401/403 (SC-2), and null-guard the DataPage export/import buttons against a missing workspaceId (SC-3).

Purpose: Correct three observable defects — broken navigation, over-aggressive session invalidation, and crash-prone export/import buttons.
Output: Updated Sidebar.tsx + Sidebar.test.tsx, HttpError in api.ts, hardened AuthContext catch block, null-guarded DataPage buttons.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/54-tech-debt-code-fixes/54-CONTEXT.md
@.planning/phases/54-tech-debt-code-fixes/54-RESEARCH.md
@.planning/phases/54-tech-debt-code-fixes/54-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ITEMS and LOANS NavLinks to Sidebar + update tests</name>
  <files>
    frontend2/src/components/layout/Sidebar.tsx
    frontend2/src/components/layout/__tests__/Sidebar.test.tsx
  </files>
  <read_first>
    - frontend2/src/components/layout/Sidebar.tsx — current 2-link state; navItemBase, navItemDefault, navItemActive constants; useLingui pattern; NavLink import
    - frontend2/src/components/layout/__tests__/Sidebar.test.tsx — existing test patterns (getByText text queries, MemoryRouter + I18nProvider setup, attribute assertions)
  </read_first>
  <behavior>
    - After adding ITEMS and LOANS links:
    - `screen.getByText("ITEMS")` is in the document
    - `screen.getByText("LOANS")` is in the document
    - `screen.getByText("ITEMS").closest("a")` has `href="/items"`
    - `screen.getByText("LOANS").closest("a")` has `href="/loans"`
    - At route "/items", ITEMS NavLink has classes `bg-retro-amber shadow-retro-pressed`
    - At route "/loans", LOANS NavLink has classes `bg-retro-amber shadow-retro-pressed`
    - Existing 7 tests continue to pass unchanged
  </behavior>
  <action>
    In Sidebar.tsx, insert two new NavLink blocks between DASHBOARD and SETTINGS, following the exact pattern of the existing SETTINGS NavLink (same className callback, same onClick, same navItemBase/navItemDefault/navItemActive constants). Use `{t\`ITEMS\`}` and `{t\`LOANS\`}` for link text, `to="/items"` and `to="/loans"` for routes. Do NOT add `end` prop (only DASHBOARD uses `end`). Final order: DASHBOARD (end), ITEMS, LOANS, SETTINGS.

    In Sidebar.test.tsx, update the test named "renders two NavLinks with text DASHBOARD and SETTINGS" to also assert ITEMS and LOANS are present. Update the test "Dashboard NavLink has to='/' and Settings NavLink has to='/settings'" to also assert ITEMS has `href="/items"` and LOANS has `href="/loans"`. Add two new tests:
    - "active NavLink (Items at /items) has class bg-retro-amber and shadow-retro-pressed" — renderSidebar(["/items"]), check items anchor
    - "active NavLink (Loans at /loans) has class bg-retro-amber and shadow-retro-pressed" — renderSidebar(["/loans"]), check loans anchor
    
    Follow the exact same test helper (`renderSidebar`) and assertion style as existing tests. Use `screen.getByText("ITEMS").closest("a")` queries — not aria queries.
  </action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system/frontend2 && bun run test -- Sidebar</automated>
  </verify>
  <done>All Sidebar tests pass. Sidebar.tsx has 4 NavLinks in order: DASHBOARD (to="/", end), ITEMS (to="/items"), LOANS (to="/loans"), SETTINGS (to="/settings").</done>
</task>

<task type="auto">
  <name>Task 2: Add HttpError class to api.ts and harden AuthContext catch block</name>
  <files>
    frontend2/src/lib/api.ts
    frontend2/src/features/auth/AuthContext.tsx
  </files>
  <read_first>
    - frontend2/src/lib/api.ts — current parseError function (returns `new Error(...)`), full export list, imports
    - frontend2/src/features/auth/AuthContext.tsx — current catch block in loadUser (line 60: `if (!(err instanceof TypeError))`), import of `get, post, setRefreshToken` from `@/lib/api`
  </read_first>
  <action>
    In api.ts, add the HttpError class BEFORE the parseError function (top of file, after imports):
    ```typescript
    export class HttpError extends Error {
      constructor(
        public readonly status: number,
        message: string
      ) {
        super(message);
        this.name = "HttpError";
      }
    }
    ```
    Update parseError to return `new HttpError(response.status, error.detail || error.message || \`HTTP \${response.status}\`)` in the try block and `new HttpError(response.status, \`HTTP \${response.status}: \${response.statusText || "Request failed"}\`)` in the catch block. Update parseError return type annotation from `Promise<Error>` to `Promise<HttpError>`.

    In AuthContext.tsx, add `HttpError` to the import from `@/lib/api`:
    ```typescript
    import { get, post, setRefreshToken, HttpError } from "@/lib/api";
    ```
    Replace the catch block guard (per D-05):
    ```typescript
    // Before:
    if (!(err instanceof TypeError)) {
      setRefreshToken(null);
    }
    // After:
    if (err instanceof HttpError && (err.status === 401 || err.status === 403)) {
      setRefreshToken(null);
    }
    ```
    The comment above it should be updated to reflect the new logic: "Only clear the refresh token on definitive auth rejections (401/403). Transient network failures and server errors do not invalidate the session."
  </action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system/frontend2 && bun run test && bun run build</automated>
  </verify>
  <done>
    - `grep "export class HttpError" frontend2/src/lib/api.ts` returns a match
    - `grep "instanceof HttpError" frontend2/src/features/auth/AuthContext.tsx` returns a match
    - `grep "err.status === 401" frontend2/src/features/auth/AuthContext.tsx` returns a match
    - `bun run build` exits 0 (TypeScript clean)
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: DataPage workspaceId null-guard on export and import buttons</name>
  <files>
    frontend2/src/features/settings/DataPage.tsx
  </files>
  <read_first>
    - frontend2/src/features/settings/DataPage.tsx — current EXPORT button `disabled={exporting}` (line 102) and IMPORT button `disabled={importing}` (line 120); workspaceId already destructured from useAuth() on line 14
  </read_first>
  <action>
    Update the EXPORT RetroButton's disabled prop from `disabled={exporting}` to `disabled={exporting || !workspaceId}`.
    Update the IMPORT RetroButton's disabled prop from `disabled={importing}` to `disabled={importing || !workspaceId}`.
    
    Both existing conditions (exporting / importing) MUST be preserved — the null-guard is additive. No other changes to DataPage.tsx. The `workspaceId` variable is already in scope from the `useAuth()` destructure on line 14.

    Do NOT change the HazardStripe import on line 5 in this task — that is handled in Plan 02 Task 4 (barrel import fix). Only touch the two `disabled` props.
  </action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system/frontend2 && bun run test && bun run build</automated>
  </verify>
  <done>
    - `grep "!workspaceId" frontend2/src/features/settings/DataPage.tsx` returns 2 matches (one per button)
    - `grep "exporting || !workspaceId" frontend2/src/features/settings/DataPage.tsx` returns a match
    - `grep "importing || !workspaceId" frontend2/src/features/settings/DataPage.tsx` returns a match
    - Build exits 0
  </done>
</task>

</tasks>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Session token cleared on transient network error (pre-fix behavior) | medium | D-05: HttpError guard ensures `setRefreshToken(null)` only fires on confirmed 401/403, not TypeError or 5xx. This is the fix being implemented. |
| Export/import triggered with null workspaceId causes API 404 | low | D-07: `disabled={exporting \|\| !workspaceId}` prevents button activation when workspaceId is absent. Existing server-side auth still validates workspace ownership. |
| HttpError instanceOf check fails across module boundaries | low | HttpError is exported from and imported from the same `@/lib/api` module — no cross-realm issue in a single Vite bundle. |
</threat_model>

<verification>
After all three tasks:
1. `cd frontend2 && bun run test -- Sidebar` — Sidebar suite green, 4 NavLinks covered
2. `cd frontend2 && bun run test && bun run build` — full suite green, TypeScript clean
3. `grep "export class HttpError" frontend2/src/lib/api.ts` — returns match
4. `grep "instanceof HttpError && (err.status === 401" frontend2/src/features/auth/AuthContext.tsx` — returns match
5. `grep "exporting || !workspaceId" frontend2/src/features/settings/DataPage.tsx` — returns match
6. `grep "importing || !workspaceId" frontend2/src/features/settings/DataPage.tsx` — returns match
</verification>

<success_criteria>
- SC-1: Sidebar renders 4 NavLinks (DASHBOARD, ITEMS, LOANS, SETTINGS) with correct routes; tests cover all 4
- SC-2: api.ts exports HttpError; AuthContext catch block clears token only on 401/403
- SC-3: DataPage EXPORT disabled when `exporting || !workspaceId`; IMPORT disabled when `importing || !workspaceId`
- TypeScript build clean after all changes
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/54-tech-debt-code-fixes/54-01-SUMMARY.md`
</output>
