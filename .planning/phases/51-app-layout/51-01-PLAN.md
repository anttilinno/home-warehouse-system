---
phase: 51-app-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend2/src/components/layout/__tests__/Sidebar.test.tsx
  - frontend2/src/components/layout/__tests__/TopBar.test.tsx
  - frontend2/src/components/layout/__tests__/LoadingBar.test.tsx
  - frontend2/src/components/layout/__tests__/ErrorBoundary.test.tsx
  - frontend2/src/components/layout/useRouteLoading.ts
  - frontend2/src/components/layout/Sidebar.tsx
  - frontend2/src/components/layout/TopBar.tsx
  - frontend2/src/components/layout/LoadingBar.tsx
  - frontend2/src/components/layout/ErrorBoundaryPage.tsx
  - frontend2/src/components/layout/index.ts
autonomous: true
requirements:
  - LAY-01
  - LAY-03

must_haves:
  truths:
    - "Sidebar component renders two NavLink items (Dashboard at /, Settings at /settings) with retro styling"
    - "Active nav item shows amber background with pressed shadow"
    - "TopBar shows HOME WAREHOUSE title on left, user name + avatar + logout button on right"
    - "LoadingBar renders a thin amber progress bar on route changes"
    - "ErrorBoundaryPage renders a full-screen retro error page with SYSTEM ERROR heading and RETURN TO BASE button"
  artifacts:
    - path: "frontend2/src/components/layout/Sidebar.tsx"
      provides: "Sidebar navigation with NavLink active states"
      exports: ["Sidebar"]
    - path: "frontend2/src/components/layout/TopBar.tsx"
      provides: "Top bar with user info and logout"
      exports: ["TopBar"]
    - path: "frontend2/src/components/layout/LoadingBar.tsx"
      provides: "Route-transition loading indicator"
      exports: ["LoadingBar"]
    - path: "frontend2/src/components/layout/useRouteLoading.ts"
      provides: "Custom hook for route change detection"
      exports: ["useRouteLoading"]
    - path: "frontend2/src/components/layout/ErrorBoundaryPage.tsx"
      provides: "Error boundary page for uncaught errors"
      exports: ["ErrorBoundaryPage"]
    - path: "frontend2/src/components/layout/index.ts"
      provides: "Barrel export for all layout components"
  key_links:
    - from: "frontend2/src/components/layout/Sidebar.tsx"
      to: "react-router NavLink"
      via: "NavLink with isActive className function"
      pattern: "NavLink.*isActive"
    - from: "frontend2/src/components/layout/TopBar.tsx"
      to: "frontend2/src/features/auth/AuthContext.tsx"
      via: "useAuth() hook for user and logout"
      pattern: "useAuth"
    - from: "frontend2/src/components/layout/LoadingBar.tsx"
      to: "frontend2/src/components/layout/useRouteLoading.ts"
      via: "useRouteLoading() hook"
      pattern: "useRouteLoading"
---

<objective>
Create the individual layout components (Sidebar, TopBar, LoadingBar, ErrorBoundaryPage) with test-first development, plus the useRouteLoading hook and barrel export.

Purpose: These components are the building blocks of the app shell. Building and testing them individually before assembly (Plan 02) keeps each plan focused and under context budget.

Output: Five tested layout components, one custom hook, and a barrel export -- all ready for AppShell assembly in Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-app-layout/51-CONTEXT.md
@.planning/phases/51-app-layout/51-RESEARCH.md
@.planning/phases/51-app-layout/51-UI-SPEC.md
@.planning/phases/51-app-layout/51-VALIDATION.md

@frontend2/src/routes/index.tsx
@frontend2/src/components/retro/index.ts
@frontend2/src/components/retro/RetroButton.tsx
@frontend2/src/components/retro/RetroPanel.tsx
@frontend2/src/features/auth/AuthContext.tsx
@frontend2/src/features/auth/RequireAuth.tsx
@frontend2/src/lib/types.ts
@frontend2/src/styles/globals.css
@frontend2/src/test-utils.tsx
@frontend2/src/components/retro/__tests__/RetroButton.test.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/types.ts:
```typescript
export interface User {
  id: string;
  email: string;
  full_name: string;
  avatar_url: string | null;
  // ... other fields
}
```

From src/features/auth/AuthContext.tsx:
```typescript
interface AuthContextValue {
  user: User | null;
  isLoading: boolean;
  isAuthenticated: boolean;
  logout: () => Promise<void>;
  // ... other methods
}
export function useAuth(): AuthContextValue;
```

From src/components/retro/index.ts:
```typescript
export { RetroButton } from "./RetroButton";
export { RetroPanel } from "./RetroPanel";
export { HazardStripe } from "./HazardStripe";
```

From src/components/retro/RetroButton.tsx:
```typescript
interface RetroButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: "primary" | "danger" | "neutral" | "secondary";
}
```

From src/test-utils.tsx:
```typescript
export function createRouterWrapper(initialEntries?: string[]): React.FC;
export function renderWithRouter(ui: React.ReactElement, options?): RenderResult;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test stubs for Sidebar, TopBar, LoadingBar, ErrorBoundaryPage</name>
  <files>
    frontend2/src/components/layout/__tests__/Sidebar.test.tsx,
    frontend2/src/components/layout/__tests__/TopBar.test.tsx,
    frontend2/src/components/layout/__tests__/LoadingBar.test.tsx,
    frontend2/src/components/layout/__tests__/ErrorBoundary.test.tsx
  </files>
  <read_first>
    frontend2/src/components/retro/__tests__/RetroButton.test.tsx,
    frontend2/src/test-utils.tsx,
    frontend2/src/features/auth/AuthContext.tsx,
    frontend2/src/lib/types.ts,
    frontend2/src/styles/globals.css
  </read_first>
  <behavior>
    Sidebar.test.tsx:
    - Test: renders a `nav` element with aria-label "Main navigation"
    - Test: renders two NavLinks with text "DASHBOARD" and "SETTINGS"
    - Test: Dashboard NavLink has `to="/"` and Settings NavLink has `to="/settings"`
    - Test: active NavLink (matching current route) has class `bg-retro-amber` and `shadow-retro-pressed`
    - Test: inactive NavLink has class `bg-retro-cream` and `shadow-retro-raised`
    - Test: NavLinks have retro styling classes: `border-retro-thick`, `border-retro-ink`, `font-bold`, `uppercase`

    TopBar.test.tsx:
    - Test: renders a `header` element
    - Test: displays "HOME WAREHOUSE" app title
    - Test: displays user full_name from useAuth
    - Test: displays user avatar initial when avatar_url is null (first letter of full_name, uppercase)
    - Test: displays avatar img when avatar_url is provided
    - Test: renders a logout button with text "LOGOUT"
    - Test: calls useAuth().logout when logout button is clicked

    LoadingBar.test.tsx:
    - Test: renders nothing when not loading (isLoading=false, progress=0)
    - Test: renders a div with role="progressbar" when loading
    - Test: has aria-label "Loading page"
    - Test: has class bg-retro-amber and z-40

    ErrorBoundary.test.tsx:
    - Test: renders "SYSTEM ERROR" heading in uppercase
    - Test: renders error message in monospace font
    - Test: renders "RETURN TO BASE" button
    - Test: renders a RetroPanel with HazardStripe
    - Test: has charcoal background (bg-retro-charcoal)
  </behavior>
  <action>
    Create the `__tests__` directory under `components/layout/` and write four test files.

    Follow the exact testing pattern from `RetroButton.test.tsx`: import from `@testing-library/react`, use `render` and `screen`, check `className` contains expected Tailwind classes.

    For Sidebar tests: use `renderWithRouter` from `@/test-utils` (since NavLink requires a Router context). Use `initialEntries: ["/"]` to test Dashboard active state, and `initialEntries: ["/settings"]` to test Settings active state.

    For TopBar tests: mock `useAuth` from `@/features/auth/AuthContext`. Use `vi.mock("@/features/auth/AuthContext")` and return a mock with `user: { full_name: "Test User", avatar_url: null }`, `logout: vi.fn()`. Wrap in `MemoryRouter` since TopBar itself does not use router features but may be nested within one.

    For LoadingBar tests: mock `useRouteLoading` from `../useRouteLoading`. Control return values: `{ isLoading: false, progress: 0 }` for hidden state, `{ isLoading: true, progress: 90 }` for visible state.

    For ErrorBoundary tests: render `ErrorBoundaryPage` directly with a mock `error` prop (string message). Wrap in `MemoryRouter` since it contains a Link/navigation action.

    All tests should initially FAIL (RED) because the components do not exist yet. Import paths:
    - `import { Sidebar } from "../Sidebar";`
    - `import { TopBar } from "../TopBar";`
    - `import { LoadingBar } from "../LoadingBar";`
    - `import { ErrorBoundaryPage } from "../ErrorBoundaryPage";`

    Do NOT use `useLingui` in tests -- the components will use it internally but tests check rendered text output.
  </action>
  <verify>
    <automated>cd frontend2 && bun vitest run src/components/layout/__tests__/ 2>&1 | grep -E "(FAIL|Cannot find module|does not provide)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend2/src/components/layout/__tests__/Sidebar.test.tsx` exists and contains `NavLink`, `bg-retro-amber`, `shadow-retro-pressed`, `aria-label`, `Main navigation`
    - File `frontend2/src/components/layout/__tests__/TopBar.test.tsx` exists and contains `HOME WAREHOUSE`, `LOGOUT`, `useAuth`, `logout`
    - File `frontend2/src/components/layout/__tests__/LoadingBar.test.tsx` exists and contains `progressbar`, `bg-retro-amber`, `useRouteLoading`
    - File `frontend2/src/components/layout/__tests__/ErrorBoundary.test.tsx` exists and contains `SYSTEM ERROR`, `RETURN TO BASE`, `bg-retro-charcoal`, `font-mono`
    - All four test files fail because components do not exist yet (RED phase)
  </acceptance_criteria>
  <done>Four test files created in components/layout/__tests__/, all importing from non-existent component files, all tests fail with import/module errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Sidebar, LoadingBar, useRouteLoading, and ErrorBoundaryPage (GREEN)</name>
  <files>
    frontend2/src/components/layout/Sidebar.tsx,
    frontend2/src/components/layout/useRouteLoading.ts,
    frontend2/src/components/layout/LoadingBar.tsx,
    frontend2/src/components/layout/ErrorBoundaryPage.tsx
  </files>
  <read_first>
    frontend2/src/components/layout/__tests__/Sidebar.test.tsx,
    frontend2/src/components/layout/__tests__/LoadingBar.test.tsx,
    frontend2/src/components/layout/__tests__/ErrorBoundary.test.tsx,
    frontend2/src/components/retro/RetroButton.tsx,
    frontend2/src/components/retro/RetroPanel.tsx,
    frontend2/src/styles/globals.css,
    .planning/phases/51-app-layout/51-UI-SPEC.md,
    .planning/phases/51-app-layout/51-RESEARCH.md
  </read_first>
  <behavior>
    Sidebar:
    - Renders `<nav aria-label="Main navigation">` wrapping a flex column with gap-sm
    - Two `<NavLink>` items: to="/" with `end` prop (text: t`DASHBOARD`), to="/settings" (text: t`SETTINGS`)
    - className function on NavLink: isActive ? active classes : default classes
    - Active classes: `bg-retro-amber text-retro-ink shadow-retro-pressed` (per D-05)
    - Default classes: `bg-retro-cream text-retro-ink shadow-retro-raised hover:bg-retro-amber`
    - Both states share base: `w-full text-left px-md py-sm font-bold uppercase text-[14px] border-retro-thick border-retro-ink cursor-pointer outline-2 outline-offset-2 outline-transparent focus-visible:outline-retro-amber`
    - Accepts `className` prop for external styling (hidden/flex control from AppShell)
    - Accepts optional `onNavClick` prop (called when any nav item clicked -- used by mobile drawer to close)

    useRouteLoading:
    - Uses useLocation() to detect pathname changes
    - On change: sets isLoading=true, progress=90, then after 300ms sets progress=100, then after 200ms resets to isLoading=false, progress=0
    - Returns { isLoading: boolean, progress: number }

    LoadingBar (per D-09):
    - Uses useRouteLoading() hook
    - When isLoading=false and progress=0: returns null (no DOM)
    - Otherwise: renders fixed div at top-0 left-0, h-[4px], bg-retro-amber, z-40
    - Width set via inline style: `width: ${progress}%`
    - Opacity via inline style: progress >= 100 ? 0 : 1
    - Transition durations: progress >= 100 ? 200ms : 300ms
    - role="progressbar", aria-label from t`Loading page`

    ErrorBoundaryPage (per D-10):
    - Full-screen: min-h-dvh bg-retro-charcoal flex items-center justify-center p-lg
    - Centered RetroPanel with showHazardStripe, max-w-[480px] w-full
    - Heading: "SYSTEM ERROR" in text-[20px] font-bold uppercase text-retro-ink
    - Error message: displayed in a code block with bg-retro-charcoal text-retro-cream p-md border-retro-thick border-retro-ink font-mono text-[14px]
    - Error message text: tries useRouteError() from react-router, falls back to prop, falls back to generic "Something went wrong..."
    - Retry button: RetroButton variant="neutral" with text t`RETURN TO BASE`, uses useNavigate() to navigate to "/"
    - Spacing: gap-md between heading, error block, and button via flex flex-col gap-md
  </behavior>
  <action>
    Implement four files to make the Sidebar, LoadingBar, and ErrorBoundary tests pass (GREEN phase).

    **Sidebar.tsx** (per D-04, D-05):
    - Import `NavLink` from `react-router`, `useLingui` from `@lingui/react/macro`
    - Props interface: `{ className?: string; onNavClick?: () => void }`
    - Define three class strings as module-level constants:
      - `navItemBase = "w-full text-left px-md py-sm font-bold uppercase text-[14px] border-retro-thick border-retro-ink cursor-pointer outline-2 outline-offset-2 outline-transparent focus-visible:outline-retro-amber"`
      - `navItemDefault = "bg-retro-cream text-retro-ink shadow-retro-raised hover:bg-retro-amber"`
      - `navItemActive = "bg-retro-amber text-retro-ink shadow-retro-pressed"`
    - NavLink className receives `({ isActive })` and returns `${navItemBase} ${isActive ? navItemActive : navItemDefault}`
    - Dashboard NavLink uses `end` prop to prevent matching all routes
    - Both NavLinks have `onClick={onNavClick}` to support drawer close on navigation

    **useRouteLoading.ts**:
    - Import `useState`, `useEffect`, `useRef` from react, `useLocation` from react-router
    - Track `prevPath` via useRef initialized to `location.pathname`
    - On pathname change: set isLoading=true, progress=90
    - setTimeout 300ms: set progress=100, inner setTimeout 200ms: reset to isLoading=false, progress=0
    - Clean up timeouts on unmount/re-trigger
    - Return `{ isLoading, progress }`

    **LoadingBar.tsx** (per D-09):
    - Import `useRouteLoading` from `./useRouteLoading`, `useLingui` from `@lingui/react/macro`
    - If `!isLoading && progress === 0` return null
    - Render div with: `role="progressbar"`, `aria-label={t\`Loading page\`}`, className `fixed top-0 left-0 h-[4px] bg-retro-amber z-40 transition-all ease-out`, inline style for width/opacity/transitionDuration

    **ErrorBoundaryPage.tsx** (per D-10):
    - Import `useRouteError`, `useNavigate` from `react-router`
    - Import `RetroPanel`, `RetroButton` from `@/components/retro`
    - Import `useLingui` from `@lingui/react/macro`
    - Try `useRouteError()` for error message; if it has a `.message` property use that, otherwise stringify it, otherwise use fallback text t`Something went wrong. Return to the home screen or reload the page to try again.`
    - Full charcoal background, centered panel, heading, code block, button structure as specified above
  </action>
  <verify>
    <automated>cd frontend2 && bun vitest run src/components/layout/__tests__/Sidebar.test.tsx src/components/layout/__tests__/LoadingBar.test.tsx src/components/layout/__tests__/ErrorBoundary.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `Sidebar.tsx` contains `NavLink`, `end` prop, `bg-retro-amber`, `shadow-retro-pressed`, `aria-label="Main navigation"`, `onNavClick`
    - `useRouteLoading.ts` contains `useLocation`, `useRef`, `useEffect`, returns `{ isLoading, progress }`
    - `LoadingBar.tsx` contains `useRouteLoading`, `role="progressbar"`, `bg-retro-amber`, `z-40`, `h-[4px]`
    - `ErrorBoundaryPage.tsx` contains `RetroPanel`, `showHazardStripe`, `SYSTEM ERROR`, `RETURN TO BASE`, `RetroButton`, `bg-retro-charcoal`, `font-mono`, `useNavigate`
    - All Sidebar, LoadingBar, and ErrorBoundary tests pass
  </acceptance_criteria>
  <done>Sidebar, useRouteLoading, LoadingBar, and ErrorBoundaryPage components implemented. All corresponding tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement TopBar and barrel export (GREEN)</name>
  <files>
    frontend2/src/components/layout/TopBar.tsx,
    frontend2/src/components/layout/index.ts
  </files>
  <read_first>
    frontend2/src/components/layout/__tests__/TopBar.test.tsx,
    frontend2/src/features/auth/AuthContext.tsx,
    frontend2/src/lib/types.ts,
    frontend2/src/components/retro/RetroButton.tsx,
    .planning/phases/51-app-layout/51-UI-SPEC.md
  </read_first>
  <behavior>
    TopBar:
    - Renders `<header>` element with sticky top-0, h-[48px], bg-retro-cream, border-b-retro-thick border-retro-ink, shadow-retro-raised, z-30
    - Flex row with items-center justify-between px-md
    - Left section: hamburger button (mobile only, md:hidden) + app title "HOME WAREHOUSE" in text-[20px] font-bold uppercase text-retro-ink
    - Right section: user avatar + user name + logout button, with gap-sm between avatar and name, gap-md between user info group and logout
    - Avatar: 32x32 circle. If user.avatar_url exists, render img with rounded-full object-cover border-retro-thick border-retro-ink. If null, render div circle with bg-retro-charcoal text-retro-cream, first letter of full_name uppercase, 14px bold
    - Logout: RetroButton variant="neutral" text t`LOGOUT`, onClick calls useAuth().logout()
    - Props: `onMenuClick: () => void`, `drawerOpen: boolean` (for hamburger button state)
    - Hamburger button: RetroButton with className override for w-[44px] h-[44px] px-0, md:hidden, aria-label toggles between "Open navigation"/"Close navigation", aria-expanded={drawerOpen}
    - Hamburger icon: inline SVG with 3 horizontal bars (each 2px thick, stroke-retro-ink) or text "☰"

    Barrel export (index.ts):
    - Export Sidebar, TopBar, LoadingBar, ErrorBoundaryPage, useRouteLoading
  </behavior>
  <action>
    **TopBar.tsx** (per D-06, D-07, D-08):
    - Import `useAuth` from `@/features/auth/AuthContext`
    - Import `RetroButton` from `@/components/retro`
    - Import `useLingui` from `@lingui/react/macro`
    - Props: `{ onMenuClick: () => void; drawerOpen: boolean }`
    - Get `{ user, logout }` from `useAuth()`
    - Left section with flex items-center gap-sm:
      - Hamburger button (md:hidden): `<RetroButton variant="neutral" className="w-[44px] !px-0 md:hidden" onClick={onMenuClick} aria-label={drawerOpen ? t\`Close navigation\` : t\`Open navigation\`} aria-expanded={drawerOpen}>` containing an inline SVG with 3 horizontal lines (viewBox="0 0 24 24", three `<rect>` elements at y=4, y=11, y=18, each width=16 height=2 fill="currentColor" x=4) or simpler: three `<line>` elements
      - Title span: `<span className="text-[20px] font-bold uppercase text-retro-ink">HOME WAREHOUSE</span>` -- wrap with `{t\`HOME WAREHOUSE\`}` for i18n
    - Right section with flex items-center gap-md:
      - User info group (flex items-center gap-sm):
        - Avatar: conditional render based on `user?.avatar_url`
        - Name: `<span className="text-[14px] text-retro-ink hidden sm:inline">{user?.full_name}</span>` (hidden on very small screens)
      - Logout: `<RetroButton variant="neutral" onClick={logout}>{t\`LOGOUT\`}</RetroButton>`

    **index.ts** barrel export:
    ```typescript
    export { Sidebar } from "./Sidebar";
    export { TopBar } from "./TopBar";
    export { LoadingBar } from "./LoadingBar";
    export { ErrorBoundaryPage } from "./ErrorBoundaryPage";
    export { useRouteLoading } from "./useRouteLoading";
    ```
  </action>
  <verify>
    <automated>cd frontend2 && bun vitest run src/components/layout/__tests__/</automated>
  </verify>
  <acceptance_criteria>
    - `TopBar.tsx` contains `<header`, `HOME WAREHOUSE`, `LOGOUT`, `useAuth`, `RetroButton`, `onMenuClick`, `aria-expanded`, `md:hidden`
    - `TopBar.tsx` contains avatar logic: `avatar_url`, `rounded-full`, `bg-retro-charcoal`
    - `index.ts` contains exports for `Sidebar`, `TopBar`, `LoadingBar`, `ErrorBoundaryPage`, `useRouteLoading`
    - All layout component tests pass (Sidebar, TopBar, LoadingBar, ErrorBoundary)
    - `cd frontend2 && bun run test` exits 0 (all project tests pass)
  </acceptance_criteria>
  <done>TopBar implemented with user info, avatar, hamburger button, and logout. Barrel export created. All layout tests green, full test suite green.</done>
</task>

</tasks>

<threat_model>
<threats>
- T-51-01: XSS via user.full_name or user.avatar_url rendered in TopBar -- Severity: medium -- Mitigation: React auto-escapes text content; avatar_url used only in img src attribute (safe); no dangerouslySetInnerHTML used anywhere
- T-51-02: Logout button accessible without re-authentication -- Severity: low -- Mitigation: Logout is low-risk (user can log back in); no confirmation dialog needed per UI-SPEC
- T-51-03: Error boundary may leak stack traces in production -- Severity: medium -- Mitigation: ErrorBoundaryPage displays error.message only (not stack); production builds strip detailed errors
</threats>
<asvs_level>1</asvs_level>
<block_on>high</block_on>
</threat_model>

<verification>
1. `cd frontend2 && bun vitest run src/components/layout/__tests__/` -- all layout component tests pass
2. `cd frontend2 && bun run test` -- full test suite passes
3. `cd frontend2 && bun run build` -- TypeScript compiles without errors
</verification>

<success_criteria>
- Five layout components (Sidebar, TopBar, LoadingBar, ErrorBoundaryPage, useRouteLoading) exist in `components/layout/`
- Barrel export in `components/layout/index.ts` exports all five
- All component tests pass (Sidebar active states, TopBar user info, LoadingBar visibility, ErrorBoundary content)
- Full project test suite and build remain green
</success_criteria>

<output>
After completion, create `.planning/phases/51-app-layout/51-01-SUMMARY.md`
</output>
