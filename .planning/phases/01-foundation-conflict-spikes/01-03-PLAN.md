---
phase: 01-foundation-conflict-spikes
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - .planning/research/I18N-DECISION.md
  - frontend2/package.json
  - frontend2/bun.lockb
  - frontend2/vite.config.ts
  - frontend2/vitest.config.ts
  - frontend2/lingui.config.ts
  - frontend2/src/locales/en/messages.po
  - frontend2/src/locales/et/messages.po
  - frontend2/src/locales/ru/messages.po
  - frontend2/src/lib/i18n.ts
autonomous: true
requirements: [FOUND-04]
must_haves:
  truths:
    - "An empirical three-part test (D-01: compile under Vite 8 + SWC; CLI extract; runtime translation) was run against exactly two candidates — Lingui v6 + @lingui/swc-plugin AND react-intl + @formatjs/cli only (D-02: two-way test, Native Intl API not a candidate), on a throwaway branch never merged to master"
    - "I18N-DECISION.md records: spike branch SHA, three evidence rows per candidate, final verdict with D-03 tiebreaker applied (Lingui v6 wins on tie), rationale, and bundle-size for the winner"
    - "The winning library is installed in the main scaffold (frontend2/package.json + vite.config.ts + vitest.config.ts updated)"
    - "Running `cd frontend2 && bun run build` after the install produces a bundle containing the winner's runtime; typecheck and forbidden-imports guards still pass"
    - "If Lingui wins: lingui.config.ts pins locales [en, et, ru], format po; @lingui/swc-plugin is exact-pinned (no caret) per Pitfall 1"
    - "If react-intl wins: no Babel plugin contaminates the SWC pipeline (FormatJS CLI extracts out-of-band per AP-7)"
  artifacts:
    - path: ".planning/research/I18N-DECISION.md"
      provides: "Locked empirical i18n decision with evidence per D-01..D-04"
      contains: "Verdict"
    - path: "frontend2/src/lib/i18n.ts"
      provides: "i18n runtime singleton + loadCatalog helper (winner-specific)"
      min_lines: 10
    - path: "frontend2/lingui.config.ts"
      provides: "Lingui config (only if Lingui wins; otherwise file is absent)"
      contains: "locales"
    - path: "frontend2/src/locales/en/messages.po"
      provides: "Empty EN catalog scaffold (Phase 15 fills in real strings)"
  key_links:
    - from: "frontend2/vite.config.ts"
      to: "frontend2/src/lib/i18n.ts"
      via: "Lingui plugin (if Lingui won) OR no plugin (if react-intl won)"
      pattern: "@lingui/vite-plugin|react-intl"
    - from: "frontend2/package.json"
      to: ".planning/research/I18N-DECISION.md"
      via: "Installed deps match the locked decision"
      pattern: "@lingui/core|react-intl"
---

<objective>
Run the locked three-part empirical i18n spike (D-01..D-04) on a throwaway branch comparing Lingui v6 + @lingui/swc-plugin against react-intl + @formatjs/cli under Vite 8 + SWC. Lock the decision in `.planning/research/I18N-DECISION.md` with evidence. Install the winner into the main `frontend2/` scaffold (package.json + vite.config.ts + vitest.config.ts + a minimal i18n runtime singleton + empty en/et/ru locale catalog scaffolds). Closes FOUND-04.

Purpose: Phase 5 (Auth) and Phase 15 (i18n catalog gap-fill) cannot start until the i18n library is locked. The empirical test is required because Lingui v6 + @lingui/swc-plugin's compatibility with Vite 8.0.10 + @vitejs/plugin-react-swc 4.3.0 is the single highest-risk assumption (A1 in the research log).

Output: I18N-DECISION.md with evidence + scaffold updated to include the winner; throwaway branch retained but never merged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-foundation-conflict-spikes/01-CONTEXT.md
@.planning/phases/01-foundation-conflict-spikes/01-RESEARCH.md
@.planning/phases/01-foundation-conflict-spikes/01-PATTERNS.md
@frontend2/package.json
@frontend2/vite.config.ts
@frontend2/vitest.config.ts

<interfaces>
<!-- The spike runs on a SEPARATE branch (D-04). The scaffold modifications below happen on the main branch AFTER the decision is locked. -->

Spike test harness (per 01-RESEARCH.md Pattern 2): three minimal source files per candidate exercising:
1. One translation: `<Trans>Hello, world</Trans>` (Lingui) OR `<FormattedMessage id="greeting" defaultMessage="Hello, world" />` (react-intl)
2. One plural rule: zero/one/other (e.g., "no items" / "1 item" / "{n} items")
3. One interpolation: `{username}` style placeholder

Three test parts (D-01) — same fixture, both candidates:
| # | Test | Pass criterion |
|---|------|----------------|
| 1 | Compile under Vite 8 + SWC | `bun run build` exits 0; bundle includes runtime |
| 2 | CLI extraction | Catalog file (.po or .json) contains the test message IDs |
| 3 | Runtime render in `et` locale | Browser DOM (or jsdom snapshot) shows TRANSLATED string, not source key |

Tiebreaker (D-03): if both pass all three, **Lingui v6 wins** (v2.0 precedent + existing catalogs + intended SWC path).

If Lingui wins → install:
- Deps: `@lingui/core@^6.0.1`, `@lingui/react@^6.0.1`
- Dev deps: `@lingui/cli@^6.0.1`, `@lingui/vite-plugin@^6.0.1`, `@lingui/swc-plugin` PINNED EXACT (no caret) at the version that worked in the spike — Pitfall 1
- Scripts: `"i18n:extract": "lingui extract"`, `"i18n:compile": "lingui compile"` in frontend2/package.json
- vite.config.ts: add `lingui()` to plugins; add `["@lingui/swc-plugin", {}]` to react() inner plugins
- vitest.config.ts: same SWC plugin slot for parity (Pattern C)
- frontend2/lingui.config.ts with locales `["en", "et", "ru"]`, sourceLocale `en`, format `po`
- frontend2/src/lib/i18n.ts: minimal singleton exporting `i18n`, `defaultLocale = "en"`, async `loadCatalog(locale)` helper

If react-intl wins → install:
- Deps: `react-intl@^6.0.3`
- Dev deps: `@formatjs/cli@^7` (extraction CLI; runs out-of-band per AP-7)
- Do NOT install `vite-plugin-babel` or `babel-plugin-formatjs` (AP-7 — keep SWC pipeline clean)
- Scripts: `"i18n:extract": "formatjs extract 'src/**/*.{ts,tsx}' --out-file src/locales/en/messages.json"`, `"i18n:compile": ...` (or omit if react-intl needs no compile step)
- vite.config.ts unchanged (no plugin needed for react-intl runtime)
- vitest.config.ts unchanged
- frontend2/src/lib/i18n.ts: minimal singleton with `defaultLocale = "en"`, async `loadMessages(locale)` returning `Record<string, string>` for `<IntlProvider>`

In BOTH cases:
- src/locales/{en,et,ru}/messages.{po|json} created EMPTY (Phase 15 owns string population — D-04 winner-installs only)
- App.tsx is NOT modified in this plan (Phase 5 mounts I18nProvider; Phase 1 keeps placeholder shell unwrapped)
- Forbidden-imports guard must still pass after install (no `idb`/`serwist`/`/(offline|sync)/i` introduced)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run i18n spike on throwaway branch and write I18N-DECISION.md</name>
  <files>
    .planning/research/I18N-DECISION.md
  </files>
  <read_first>
    .planning/phases/01-foundation-conflict-spikes/01-CONTEXT.md (D-01..D-04 — locked spike methodology),
    .planning/phases/01-foundation-conflict-spikes/01-RESEARCH.md (Pattern 2 spike protocol; Pitfalls 1, 2, 8; Code Examples for Lingui + react-intl configs),
    frontend2/package.json (current deps — must NOT be modified during spike branch work)
  </read_first>
  <action>
    PRE-CONDITION: Confirm `git status` shows a clean working tree on master before creating the spike branch.

    1. **Create throwaway branch** off the current commit:
       ```
       git checkout -b spike/i18n-decision
       ```
       All spike work happens on this branch. NEVER merge to master (D-04).

    2. **Spike test for Lingui v6** (work on the spike branch in `frontend2/`):
       a. Install Lingui deps:
          ```
          cd frontend2
          bun add @lingui/core@^6.0.1 @lingui/react@^6.0.1
          bun add -d @lingui/cli@^6.0.1 @lingui/vite-plugin@^6.0.1 @lingui/swc-plugin@^6.0.0
          ```
          Record the resolved exact version of `@lingui/swc-plugin` from `bun.lockb` for Pitfall 1.
       b. Add Lingui plugin to vite.config.ts (per 01-PATTERNS.md Pattern C):
          ```typescript
          import { lingui } from "@lingui/vite-plugin";
          // plugins: [react({ plugins: [["@lingui/swc-plugin", {}]] }), tailwindcss(), lingui()]
          ```
       c. Create `frontend2/lingui.config.ts`:
          ```typescript
          import { defineConfig } from "@lingui/cli";
          export default defineConfig({
            sourceLocale: "en",
            locales: ["en", "et"],
            catalogs: [{ path: "<rootDir>/src/locales/{locale}/messages", include: ["src"] }],
            format: "po",
          });
          ```
       d. Create test fixture `frontend2/src/spike-i18n.tsx` with three messages: one Trans, one Plural, one interpolation. Wrap in `<I18nProvider>`. Render once at module init for compile-test.
       e. Add scripts to package.json: `"i18n:extract": "lingui extract"`, `"i18n:compile": "lingui compile"`.
       f. Run the three tests:
          - **Test 1 (compile)**: `bun run build` — must exit 0. Capture last 30 lines of output to log.
          - **Test 2 (extract)**: `bun run i18n:extract` — verify `frontend2/src/locales/en/messages.po` contains all three msgids.
          - **Test 3 (runtime)**: Add a vitest test that imports `spike-i18n.tsx`, activates `et` locale via Lingui's `i18n.activate("et")` (after stubbing the `et` catalog with a translated msgstr), renders, and asserts the translated string appears. Run `bun run test`.
       g. Capture evidence: build log excerpt, extracted .po excerpt, test output.

    3. **Spike test for react-intl** (still on spike branch — uninstall Lingui first to avoid contamination):
       a. Revert vite.config.ts to bare `react()` plugin. Uninstall Lingui:
          ```
          bun remove @lingui/core @lingui/react @lingui/cli @lingui/vite-plugin @lingui/swc-plugin
          ```
       b. Install react-intl:
          ```
          bun add react-intl@^6.0.3
          bun add -d @formatjs/cli@^7
          ```
       c. Replace test fixture: `frontend2/src/spike-i18n.tsx` with three FormattedMessage uses (one plain, one with `<FormattedMessage values={{n}}>` plural, one with `{username}` interpolation). Wrap in `<IntlProvider>`.
       d. Add scripts: `"i18n:extract": "formatjs extract 'src/**/*.{ts,tsx}' --out-file src/locales/en/messages.json"`.
       e. Run the three tests:
          - **Test 1 (compile)**: `bun run build` — must exit 0.
          - **Test 2 (extract)**: `bun run i18n:extract` — verify `frontend2/src/locales/en/messages.json` contains all three msgids.
          - **Test 3 (runtime)**: vitest test renders fixture under `<IntlProvider locale="et" messages={{...translated...}}>`; asserts translated string appears.
          - If Test 1 reveals SWC cannot transform FormattedMessage default-message-as-id optimizations (rare), document the failure mode but DO NOT add `vite-plugin-babel` (AP-7 — extraction can run out-of-band via FormatJS CLI; runtime path works without compile-time optimization).
       f. Capture evidence: build log excerpt, extracted .json excerpt, test output.

    4. **Decide the winner per D-03**:
       - If both pass all three parts → **Lingui v6 wins** (D-03 tiebreaker).
       - If only one passes → that one wins.
       - If neither passes → BLOCK Phase 1, escalate to user (no viable path; do NOT proceed to Task 2).

    5. **Bundle size measurement for the winner**: re-checkout the winner's branch state, run `bun run build`, record the gzipped size of the main chunk via `find dist/assets -name "index-*.js" -exec sh -c 'gzip -c "$1" | wc -c' _ {} \;`.

    6. **Tag the spike branch head**:
       ```
       git add -A && git commit -m "spike(i18n): empirical evidence for D-01..D-04"
       SPIKE_SHA=$(git rev-parse HEAD)
       git tag spike/i18n-decision-evidence
       ```
       Record SPIKE_SHA — referenced in I18N-DECISION.md.

    7. **Switch back to master** (do NOT merge spike branch):
       ```
       git checkout master
       ```
       The spike branch + tag stay in git for archaeology (per 01-RESEARCH.md Open Question 3 recommendation).

    8. **Write `.planning/research/I18N-DECISION.md`** on master:

       ```markdown
       # i18n Library Decision (FOUND-04, D-01..D-04)

       **Date:** <YYYY-MM-DD>
       **Spike branch SHA:** <SPIKE_SHA>
       **Tag:** `spike/i18n-decision-evidence`

       ## Verdict

       **<Lingui v6 | react-intl>** wins.

       Rationale: <1-3 sentences citing test results and D-03 tiebreaker if applicable>

       ## Evidence

       ### Candidate A: Lingui v6 + @lingui/swc-plugin

       | Test | Result | Evidence |
       |------|--------|----------|
       | 1. Compile under Vite 8 + SWC | PASS / FAIL | `<5-line excerpt of bun run build output>` |
       | 2. CLI extraction | PASS / FAIL | `<excerpt of generated en/messages.po>` |
       | 3. Runtime render in et | PASS / FAIL | `<vitest output line>` |

       Resolved version of `@lingui/swc-plugin`: `<X.Y.Z>` (record exact for Pitfall 1 pinning)

       ### Candidate B: react-intl + @formatjs/cli

       | Test | Result | Evidence |
       |------|--------|----------|
       | 1. Compile under Vite 8 + SWC | PASS / FAIL | `<excerpt>` |
       | 2. CLI extraction | PASS / FAIL | `<excerpt of en/messages.json>` |
       | 3. Runtime render in et | PASS / FAIL | `<vitest output line>` |

       ## Bundle Size (winner only)

       Main chunk gzip: `<N> bytes` (measured via `gzip -c dist/assets/index-*.js | wc -c`)

       ## Implications for Future Phases

       - Phase 5 (Auth): mounts `<I18nProvider i18n={i18n}>` (Lingui) OR `<IntlProvider locale=... messages=...>` (react-intl) at the App.tsx provider stack.
       - Phase 15 (i18n catalog gap-fill): populates en/et/ru catalogs; CI extract→merge→diff manifest guard belongs to Phase 15.
       - <if Lingui> Pitfall 1: pin `@lingui/swc-plugin` exactly to `<X.Y.Z>` (no caret); revisit on every Vite/SWC bump.
       - <if Lingui> Pitfall 2: when Phase 15 imports v2.0/v2.1 catalogs, run `lingui extract --convert-from=v5` once.
       - <if react-intl> AP-7: extraction runs via FormatJS CLI out-of-band; do NOT add Babel to the SWC pipeline.

       ## Spike Branch Artifacts (NOT MERGED)

       - Branch: `spike/i18n-decision` (HEAD: `<SPIKE_SHA>`)
       - Test fixture: `frontend2/src/spike-i18n.tsx` (lives on spike branch only)
       - Both candidates' install state captured in branch history; reproducible by `git checkout <SPIKE_SHA>~N`.
       ```

       Replace placeholders with actual results. Do NOT modify the scaffold's vite.config.ts or package.json on master in this task — Task 2 owns scaffold install.
  </action>
  <verify>
    <automated>
      test -f .planning/research/I18N-DECISION.md && \
      grep -qE 'Verdict' .planning/research/I18N-DECISION.md && \
      grep -qE 'Lingui v6|react-intl' .planning/research/I18N-DECISION.md && \
      grep -qE 'spike/i18n-decision' .planning/research/I18N-DECISION.md && \
      grep -qE 'PASS|FAIL' .planning/research/I18N-DECISION.md && \
      grep -qE 'Bundle Size' .planning/research/I18N-DECISION.md && \
      git tag -l 'spike/i18n-decision-evidence' | grep -q . && \
      git rev-parse --abbrev-ref HEAD | grep -q '^master$' && \
      git diff --quiet HEAD -- frontend2/package.json frontend2/vite.config.ts frontend2/vitest.config.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - .planning/research/I18N-DECISION.md exists
    - File contains literal `Verdict` AND a winner name (`Lingui v6` OR `react-intl`)
    - File contains the spike branch SHA and the tag `spike/i18n-decision`
    - File contains at least one `PASS` or `FAIL` cell (evidence table populated)
    - File contains a `Bundle Size` section (winner-only measurement)
    - Git tag `spike/i18n-decision-evidence` exists (`git tag -l` shows it)
    - Current branch is `master` (spike branch NOT merged)
    - frontend2/package.json, vite.config.ts, vitest.config.ts unchanged on master at end of Task 1 (Task 2 owns those edits)
  </acceptance_criteria>
  <done>Decision document is committed with evidence; spike branch retained as archaeology; master scaffold untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Install winner into main scaffold (package.json + vite + vitest + i18n.ts + locale scaffolds)</name>
  <files>
    frontend2/package.json,
    frontend2/bun.lockb,
    frontend2/vite.config.ts,
    frontend2/vitest.config.ts,
    frontend2/lingui.config.ts,
    frontend2/src/locales/en/messages.po,
    frontend2/src/locales/et/messages.po,
    frontend2/src/locales/ru/messages.po,
    frontend2/src/lib/i18n.ts
  </files>
  <read_first>
    .planning/research/I18N-DECISION.md (winner from Task 1 — drives every branch below),
    frontend2/package.json (current state on master after Plan 01),
    frontend2/vite.config.ts (current state on master — must keep proxy + tailwindcss + react plugins intact),
    frontend2/vitest.config.ts (current state on master — must keep e2e exclude + alias intact),
    .planning/phases/01-foundation-conflict-spikes/01-PATTERNS.md (Pattern C — Lingui SWC slot must match between vite.config.ts and vitest.config.ts; entries for lingui.config.ts)
  </read_first>
  <action>
    Read the winner from `.planning/research/I18N-DECISION.md` (the Verdict section). Branch on the winner.

    ### IF LINGUI v6 WON

    1. Install deps from master:
       ```
       cd frontend2
       bun add @lingui/core@^6.0.1 @lingui/react@^6.0.1
       LINGUI_SWC_VERSION=$(grep -oE '`@lingui/swc-plugin`[^`]*`[0-9.]+' .planning/research/I18N-DECISION.md | grep -oE '[0-9]+\.[0-9]+\.[0-9]+' | head -1)
       # If LINGUI_SWC_VERSION extraction fails, default to 6.0.0; verify in I18N-DECISION.md and adjust manually.
       bun add -d @lingui/cli@^6.0.1 @lingui/vite-plugin@^6.0.1
       bun add -d -E "@lingui/swc-plugin@${LINGUI_SWC_VERSION:-6.0.0}"   # -E pins exact (Pitfall 1)
       ```

    2. Add scripts to `frontend2/package.json`:
       ```json
       "i18n:extract": "lingui extract",
       "i18n:compile": "lingui compile"
       ```

    3. Update `frontend2/vite.config.ts` — add Lingui plugin slots (per Pattern C). Final shape:
       ```typescript
       import { defineConfig } from "vite";
       import react from "@vitejs/plugin-react-swc";
       import tailwindcss from "@tailwindcss/vite";
       import { lingui } from "@lingui/vite-plugin";
       import path from "path";

       export default defineConfig({
         plugins: [
           react({ plugins: [["@lingui/swc-plugin", {}]] }),
           tailwindcss(),
           lingui(),
         ],
         resolve: { alias: { "@": path.resolve(__dirname, "./src") } },
         server: {
           port: 5173,
           proxy: { "/api": { target: "http://localhost:8080", changeOrigin: true, secure: false } },
         },
         build: { sourcemap: true },
       });
       ```

    4. Update `frontend2/vitest.config.ts` — same SWC plugin slot:
       ```typescript
       react({ plugins: [["@lingui/swc-plugin", {}]] })
       ```
       Keep all other config (alias, exclude, environment) unchanged.

    5. Create `frontend2/lingui.config.ts`:
       ```typescript
       import { defineConfig } from "@lingui/cli";
       export default defineConfig({
         sourceLocale: "en",
         locales: ["en", "et", "ru"],
         catalogs: [
           { path: "<rootDir>/src/locales/{locale}/messages", include: ["src"] },
         ],
         format: "po",
       });
       ```

    6. Create empty catalogs (Phase 15 owns string population):
       ```
       frontend2/src/locales/en/messages.po
       frontend2/src/locales/et/messages.po
       frontend2/src/locales/ru/messages.po
       ```
       Each starts with the standard .po header:
       ```
       msgid ""
       msgstr ""
       "POT-Creation-Date: <YYYY-MM-DD>\n"
       "Language: <locale>\n"
       "Content-Type: text/plain; charset=utf-8\n"
       ```

    7. Create `frontend2/src/lib/i18n.ts`:
       ```typescript
       import { i18n } from "@lingui/core";

       export const defaultLocale = "en";
       export const locales = ["en", "et", "ru"] as const;
       export type Locale = (typeof locales)[number];

       export async function loadCatalog(locale: Locale): Promise<void> {
         const { messages } = await import(`../locales/${locale}/messages.po`);
         i18n.load(locale, messages);
         i18n.activate(locale);
       }

       export { i18n };
       ```
       Phase 5 mounts `<I18nProvider i18n={i18n}>` in App.tsx; Phase 15 fills catalogs.

    ### IF REACT-INTL WON

    1. Install deps:
       ```
       cd frontend2
       bun add react-intl@^6.0.3
       bun add -d "@formatjs/cli@^7"
       ```

    2. Add script to `frontend2/package.json`:
       ```json
       "i18n:extract": "formatjs extract 'src/**/*.{ts,tsx}' --out-file src/locales/en/messages.json"
       ```

    3. vite.config.ts and vitest.config.ts UNCHANGED (no plugin needed; AP-7 prohibits adding Babel).

    4. Do NOT create `lingui.config.ts`.

    5. Create empty catalogs:
       ```
       frontend2/src/locales/en/messages.json   → {}
       frontend2/src/locales/et/messages.json   → {}
       frontend2/src/locales/ru/messages.json   → {}
       ```

    6. Create `frontend2/src/lib/i18n.ts`:
       ```typescript
       export const defaultLocale = "en";
       export const locales = ["en", "et", "ru"] as const;
       export type Locale = (typeof locales)[number];

       export async function loadMessages(locale: Locale): Promise<Record<string, string>> {
         const mod = await import(`../locales/${locale}/messages.json`);
         return mod.default ?? mod;
       }
       ```
       Phase 5 mounts `<IntlProvider locale={locale} messages={messages}>` using `loadMessages`.

    ### COMMON (both winners)

    7. Verify the scaffold still builds and the CI guard still passes:
       ```
       cd frontend2 && bun run lint:tsc && bun run lint:imports && bun run build
       ```
       All three must exit 0. If `lint:imports` fails, the i18n install introduced a forbidden specifier (e.g., a Lingui dep transitively requiring `@formatjs/intl-relativetimeformat-sync` — extremely unlikely but possible). Diagnose and fix.

    Note: Plan 01-02 owns the integrated smoke script `scripts/verify-phase-01-scaffold.sh`. That script is exercised in Plan 01-02's own verification; it is intentionally NOT a gate for this plan because Plans 01-02 and 01-03 share Wave 2 (parallel execution — the script may not exist when this task runs). The three commands above (`lint:tsc`, `lint:imports`, `build`) fully cover the i18n install acceptance criteria.
  </action>
  <verify>
    <automated>
      cd frontend2 && \
      ( ( grep -q '"@lingui/core"' package.json && grep -q 'lingui()' vite.config.ts && \
          grep -qE '@lingui/swc-plugin' vite.config.ts && grep -qE '@lingui/swc-plugin' vitest.config.ts && \
          grep -q '"i18n:extract": "lingui extract"' package.json && \
          test -f lingui.config.ts && grep -q '"en", "et", "ru"' lingui.config.ts && \
          test -f src/locales/en/messages.po && test -f src/locales/et/messages.po && test -f src/locales/ru/messages.po && \
          grep -qE '"@lingui/swc-plugin": "[0-9]+\.[0-9]+\.[0-9]+"' package.json ) \
        || \
        ( grep -q '"react-intl"' package.json && ! grep -q '@lingui' vite.config.ts && \
          ! test -f lingui.config.ts && \
          test -f src/locales/en/messages.json && test -f src/locales/et/messages.json && test -f src/locales/ru/messages.json && \
          grep -q 'formatjs extract' package.json && \
          ! grep -q 'vite-plugin-babel' package.json ) ) && \
      test -f src/lib/i18n.ts && \
      grep -qE 'defaultLocale\s*=\s*"en"' src/lib/i18n.ts && \
      bun run lint:tsc && \
      bun run lint:imports && \
      bun run build
    </automated>
  </verify>
  <acceptance_criteria>
    - frontend2/src/lib/i18n.ts exists, exports `defaultLocale = "en"` and `locales = ["en", "et", "ru"]`
    - Locale catalogs exist for en, et, AND ru (winner-specific extension: .po if Lingui, .json if react-intl)
    - **If Lingui won:** package.json contains `"@lingui/core"` AND `"i18n:extract": "lingui extract"`; `@lingui/swc-plugin` is pinned EXACT (no caret — `"@lingui/swc-plugin": "X.Y.Z"`); vite.config.ts contains `lingui()` AND `@lingui/swc-plugin`; vitest.config.ts contains `@lingui/swc-plugin`; lingui.config.ts exists with `"en", "et", "ru"`
    - **If react-intl won:** package.json contains `"react-intl"` AND `"formatjs extract"` script; vite.config.ts does NOT contain `@lingui`; lingui.config.ts does NOT exist; package.json does NOT contain `vite-plugin-babel` (AP-7)
    - `cd frontend2 && bun run lint:tsc` exits 0
    - `cd frontend2 && bun run lint:imports` exits 0 (no new forbidden specifiers introduced by i18n deps)
    - `cd frontend2 && bun run build` exits 0
  </acceptance_criteria>
  <done>Spike winner installed in main scaffold; locale catalogs scaffolded for en/et/ru (empty until Phase 15); i18n runtime singleton in place ready for Phase 5 Provider mount; CI guards still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spike branch ↔ master branch | Spike installs untrusted dep variants on a throwaway branch; only the winner's locked install lands on master |
| `@lingui/swc-plugin` build-time native binary ↔ developer machine | If Lingui wins, a Rust-compiled WASM plugin executes at build time |
| FormatJS CLI ↔ source files | If react-intl wins, the CLI walks src/ at extract time; trusted as it ships from `@formatjs` org |
| Locale catalog files ↔ runtime | Phase 15 will fetch translations from translators; Phase 1 only reserves empty catalogs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Lingui SWC plugin executes untrusted build-time code | mitigate | Pin `@lingui/swc-plugin` EXACT (no caret) per Pitfall 1 + Task 2 step 1; Bun's lockfile records integrity hash; review the package's published source on first install (manual one-time review noted in SUMMARY) |
| T-03-02 | Tampering | Spike branch's `bun.lockb` carries platform-specific Lingui native binary into master | mitigate | Per Pitfall 8: spike branch is NEVER merged; Task 2 runs `bun add` fresh on master, regenerating the lockfile with the active platform's binary |
| T-03-03 | Information Disclosure | i18n catalog files committed before they're cleared of real translator emails | accept | Empty catalogs committed in Plan 03; Phase 15 owns population; translator metadata not yet in scope |
| T-03-04 | Tampering | An adversary opens a PR with `--convert-from=v5` that drops msgids during catalog migration | mitigate | If Lingui won, Pitfall 2 documented in I18N-DECISION.md; Phase 15 owns the conversion gate (extract→merge→diff manifest guard) |
| T-03-05 | Spoofing | Spike branch contains a half-working candidate that misleads the verdict | mitigate | Three-part empirical test (D-01) requires ALL three checks pass to be considered viable; tied results fall through to D-03 tiebreaker (Lingui wins on tie); both candidates' evidence is captured in I18N-DECISION.md for audit |
| T-03-06 | Tampering | react-intl path silently drags Babel into the SWC pipeline | mitigate | Task 2 step 1 (react-intl branch) explicitly does NOT install vite-plugin-babel; AP-7 documented; verification grep checks `! grep vite-plugin-babel package.json` |
| T-03-07 | Information Disclosure | Forbidden-imports guard regression — i18n install introduces an offline/sync transitive | mitigate | Task 2 step 7 runs `bun run lint:imports` after install; failure means a transitive dep matched the FORBIDDEN_SUBSTR `/(offline|sync)/i` regex (extremely unlikely but caught) |
</threat_model>

<verification>
End-to-end after Task 2:
```
cd frontend2 && bun run lint:tsc && bun run lint:imports && bun run build && cd ..
git rev-parse --abbrev-ref HEAD   # → master (spike branch not active)
git tag -l spike/i18n-decision-evidence | grep -q .
test -f .planning/research/I18N-DECISION.md
test -f frontend2/src/lib/i18n.ts
test -f frontend2/src/locales/en/messages.po -o -f frontend2/src/locales/en/messages.json
```

Note: Plan 01-02 owns `scripts/verify-phase-01-scaffold.sh` (the integrated Plan 01 + 02 + 03 health check). It is verified inside Plan 01-02 and is NOT a gate for this plan because Plans 01-02 and 01-03 are in the same wave (Wave 2) and execute in parallel — the script may not yet exist when Plan 01-03 runs its verify step.
</verification>

<success_criteria>
- I18N-DECISION.md committed on master with verdict + evidence + spike SHA + tag reference
- Spike branch (`spike/i18n-decision`) and tag (`spike/i18n-decision-evidence`) exist in git but are NOT merged to master
- Winner's deps are installed in `frontend2/package.json` with locked pins (Lingui SWC plugin exact-pinned per Pitfall 1; or react-intl without Babel contamination per AP-7)
- vite.config.ts + vitest.config.ts updated coherently if Lingui won (Pattern C parity)
- `frontend2/src/lib/i18n.ts` exists with `defaultLocale = "en"` and `locales = ["en", "et", "ru"]`
- Empty locale catalogs scaffolded for en + et + ru
- `bun run lint:tsc`, `bun run lint:imports`, `bun run build` all exit 0
- App.tsx is NOT modified (Phase 5 mounts I18nProvider; Phase 1 stays at placeholder shell)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-conflict-spikes/01-03-SUMMARY.md` recording:
- Spike winner (Lingui v6 OR react-intl)
- Spike branch SHA + tag name
- If Lingui won: exact `@lingui/swc-plugin` version pinned + Pitfall 1/2 reminders
- If react-intl won: confirmation no Babel was added (AP-7)
- Bundle-size delta vs Plan 01 baseline (`du -sh frontend2/dist/` before vs after)
- Reminder: Phase 5 mounts the i18n provider; Phase 15 fills catalogs
</output>
