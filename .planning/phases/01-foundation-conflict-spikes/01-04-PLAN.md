---
phase: 01-foundation-conflict-spikes
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/CARRY-FORWARD.md
  - .planning/STATE.md
autonomous: true
requirements: [FOUND-03, FOUND-05, FOUND-06]
must_haves:
  truths:
    - "CARRY-FORWARD.md enumerates exactly five port-verbatim items (auth flow, OAuth callback, format hooks, Playwright auth helper, grep guard) with v2.1 source SHAs cited for each"
    - "CARRY-FORWARD.md enumerates the four rebuild-from-scratch concepts (chrome, atoms, layout grid + tokens, provider stack composition) with target phase numbers"
    - "CARRY-FORWARD.md documents the two new backend endpoint specs from D-11 (capacity + activity) for Phase 13 consumption"
    - "STATE.md Decisions section includes locked entries for D-05..D-08 (mobile FAB scope) and D-09..D-11 (dashboard rollups) so downstream phases inherit the constraints"
  artifacts:
    - path: ".planning/research/CARRY-FORWARD.md"
      provides: "Audit document enumerating port-verbatim vs rebuild items + backend endpoint specs"
      min_lines: 60
      contains: "Port Verbatim"
    - path: ".planning/STATE.md"
      provides: "Updated Decisions section with v3.0 D-05..D-11 entries"
      contains: "v3.0"
  key_links:
    - from: ".planning/research/CARRY-FORWARD.md"
      to: "frontend2/src/lib/api.ts"
      via: "Port Verbatim row 1 references v2.1 SHA 4d4c233"
      pattern: "lib/api\\.ts"
    - from: ".planning/research/CARRY-FORWARD.md"
      to: "Phase 13 dashboard rollups"
      via: "Backend Endpoint Specs section"
      pattern: "/stats/capacity|/stats/activity"
---

<objective>
Write `.planning/research/CARRY-FORWARD.md` enumerating (1) the five port-verbatim items with v2.1 source SHAs (FOUND-03), (2) the four rebuild-from-scratch concepts with target phases, and (3) the Phase 13 backend endpoint specs from D-11 (FOUND-06). Update `.planning/STATE.md` Decisions section to lock D-05..D-11 (mobile FAB scope FOUND-05 + dashboard rollups FOUND-06) so all downstream v3.0 phases inherit them.

Purpose: This plan resolves three of the six FOUND-* requirements via planning artifacts only — no code. Closes FOUND-03 + FOUND-05 + FOUND-06. Phase 3 (Layout Primitives + Bottombar) cannot scope its breakpoint contract without D-05/D-06/D-08; Phase 13 (Dashboard) cannot scope its rollups without D-09/D-10/D-11.

Output: One new markdown file + one updated markdown file. No code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-conflict-spikes/01-CONTEXT.md
@.planning/phases/01-foundation-conflict-spikes/01-RESEARCH.md
@.planning/phases/01-foundation-conflict-spikes/01-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- This plan produces planning artifacts only. The "interface" is the document structure that downstream planners consume. -->

CARRY-FORWARD.md required sections (per FOUND-03 + 01-RESEARCH.md Pattern 5):

1. **Port Verbatim** table — exactly five rows, cite v2.1 source SHAs:
   | # | Item | Source (git show) | Destination | Notes |
   | 1 | Auth flow (cookie-JWT, single-flight 401 refresh) | `git show 3826d24:frontend2/src/lib/api.ts` + `git show 4d4c233:frontend2/src/lib/api.ts` (FormData edit) | frontend2/src/lib/api.ts (already ported in Plan 01-01) | Pitfall #10 — do NOT regress to localStorage Bearer |
   | 2 | OAuth callback handler | TBD by Phase 5 (search v2.1 archive: `.planning/milestones/v2.1-phases/`) | frontend2/src/features/auth/AuthCallbackPage.tsx | Phase 5 ports; Plan 01 reserves the path |
   | 3 | Format hooks (useDateFormat / useTimeFormat / useNumberFormat) | TBD by Phase 12 (search v2.1 archive) | frontend2/src/hooks/useDateFormat.ts (etc.) | Phase 12 ports; Plan 01 reserves the path |
   | 4 | Playwright auth helper | CLAUDE.md auth contract + `git show 5e77f98:frontend2/e2e/scan-lookup.spec.ts` (auth section) | frontend2/e2e/_helpers/auth.ts (refactor) | "/login" form contract; ^LOG IN$ exact match; cookie inheritance |
   | 5 | scripts/check-forbidden-imports.mjs | EXISTING — `/home/antti/Repos/Misc/home-warehouse-system/scripts/check-forbidden-imports.mjs` (verified working 2026-05-01) | unchanged path | Wired into CI by Plan 01-02 |

2. **Rebuild from Scratch** table — exactly four rows:
   | # | Concept | Why Rebuild | Phase |
   | 1 | Chrome (TopBar / Sidebar / Bottombar / PageHeader) | Sketch 005 fidelity is new; v2.1 chrome is partial | 3 |
   | 2 | Retro atoms (Panel / Button / Badge / Input / Table / Dialog / etc.) | Visual re-derivation per sketch 005 + new constraints from layout | 4 |
   | 3 | Layout grid + design tokens | Token system locked from sketches 001-005 | 2 |
   | 4 | Provider stack composition | Some providers new (Shortcuts, ToastProvider as sonner) | 6 |

3. **Backend Endpoint Specs (D-10/D-11)** — for Phase 13 dashboard rollups:
   - `GET /api/workspaces/{wsId}/stats/capacity` → `{ total_items: number, capacity_target: number | null }` (capacity_target = null until warehouse-capacity feature ships)
   - `GET /api/workspaces/{wsId}/stats/activity?days=14` → `{ days: Array<{ date: string (YYYY-MM-DD), count: number }> }` (zero-fill missing days; count = item-mutation events; UTC)
   - Both endpoints built in Phase 13 (or an adjacent backend task); Phase 1 only documents the contract.

4. **Out of Scope (Reaffirmed)** section — short list:
   - IndexedDB / Serwist / offline / sync* — CI grep guard enforced (Plan 01-02 wires the merge gate)
   - Lingui v5 macros — replaced by Plan 01-03 spike winner
   - v2.1 retro atom files — component-by-component re-derivation per Phase 2-4

STATE.md update: append the v3.0 decisions to the existing `### Decisions` section (preserve all v1.x..v2.2 entries). Add:
- v3.0: Mobile (<768px) → FAB only, no Bottombar (D-05)
- v3.0: Desktop (≥768px) → Bottombar only, no FAB (D-06)
- v3.0: FAB exposes context-aware radial menu — actions adapt per route (D-07)
- v3.0: Phase 3 BAR scope locked: Bottombar `≥768px`-only; FAB `<768px`-only; both share useShortcuts SSOT (D-08)
- v3.0: Dashboard HUD row ships in Phase 13 WITHOUT a feature flag (no prod env to flag against) (D-09)
- v3.0: Backend endpoint specs for HUD rollups documented in Phase 1 CARRY-FORWARD.md; endpoints built in Phase 13 (D-10)
- v3.0: HUD endpoints — `/api/workspaces/{wsId}/stats/capacity` and `/api/workspaces/{wsId}/stats/activity?days=14` (D-11)

Also update STATE.md `last_activity` and `Session Continuity` lines per the standard pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write CARRY-FORWARD.md with five-item port table + four-item rebuild table + D-11 endpoint specs</name>
  <files>
    .planning/research/CARRY-FORWARD.md
  </files>
  <read_first>
    .planning/phases/01-foundation-conflict-spikes/01-CONTEXT.md (D-09..D-11 for endpoint specs; FOUND-03 explicit list of port-verbatim items),
    .planning/phases/01-foundation-conflict-spikes/01-RESEARCH.md (Pattern 5 — verbatim CARRY-FORWARD.md layout template; Existing Code Insights — port-verbatim sources),
    .planning/phases/01-foundation-conflict-spikes/01-PATTERNS.md (.planning/research/CARRY-FORWARD.md entry — required sections + source-SHA discipline),
    CLAUDE.md (Playwright auth contract — load-bearing for row 4 of Port Verbatim)
  </read_first>
  <action>
    Create `.planning/research/CARRY-FORWARD.md` with the following exact sections. Use real SHAs from 01-PATTERNS.md (5cbde14, 3826d24, 4d4c233, 5e77f98, c570d9f) — do NOT invent placeholder SHAs.

    ```markdown
    # v3.0 Carry-Forward Audit

    **Date:** 2026-05-01
    **Wipe SHA:** <verify with `git log --oneline -- frontend2/ | grep -i 'wipe\|abandon\|reset' | head -3`>
    **v2.1 archive root:** `.planning/milestones/v2.1-phases/`
    **v2.2 abandoned phases:** `.planning/milestones/v2.2-phases-abandoned/`

    ## Purpose

    This document is the FOUND-03 deliverable. Downstream phase planners (Phase 2-17) read this audit to avoid re-deciding ported items and to know which v2.1 archive sources are canonical. Every "Port Verbatim" row cites a v2.1 source SHA executable via `git show <SHA>:<path>`.

    ## Port Verbatim (FOUND-03 — exactly five items)

    | # | Item | Source (git show) | Destination | Notes |
    |---|------|-------------------|-------------|-------|
    | 1 | Auth flow (cookie-JWT, single-flight 401 refresh, FormData multipart) | `git show 3826d24:frontend2/src/lib/api.ts` MERGED with `git show 4d4c233:frontend2/src/lib/api.ts` (FormData edit from Plan 56-01) | `frontend2/src/lib/api.ts` (already ported in Plan 01-01) | Pitfall #10 / AP-2 — do NOT regress to localStorage Bearer; locked invariants: `credentials: "include"` on every fetch; module-level `refreshPromise` singleton; `isFormData` Content-Type bypass |
    | 2 | OAuth callback handler | Search `.planning/milestones/v2.1-phases/` for the AuthCallbackPage; v2.1 plan 49-01 lineage. Concrete SHA TBD by Phase 5 planner via `git log --oneline -- frontend2/src/features/auth/AuthCallbackPage.tsx`. | `frontend2/src/features/auth/AuthCallbackPage.tsx` | Phase 5 ports; Plan 01 reserves the path. Carries Google + GitHub OAuth code-exchange flow (one-time Redis exchange per AUTH-03/04). |
    | 3 | Format hooks — `useDateFormat`, `useTimeFormat`, `useNumberFormat` | v1.6 Format Personalization milestone shipped these (Phases 30-34). v2.1 ported them; concrete SHAs TBD by Phase 12 planner. | `frontend2/src/hooks/useDateFormat.ts`, `useTimeFormat.ts`, `useNumberFormat.ts` | Phase 12 (Settings hub) ports; Plan 01 reserves the path. CI grep guard in Phase 15 forbids raw `Date.toString()` / `Number.toLocaleString()` in feature code. |
    | 4 | Playwright auth helper | CLAUDE.md project-root §E2E Tests (auth contract: `^LOG IN$` exact match + access_token cookie inheritance) + `git show 5e77f98:frontend2/e2e/scan-lookup.spec.ts` (auth seed section as reference for refactoring into a shared helper) | `frontend2/e2e/_helpers/auth.ts` (NEW shared helper, extracted on first E2E spec) | Specs that use this helper drop into `frontend2/e2e/*.spec.ts`. The contract is the helper's API surface; CLAUDE.md is the source of truth. |
    | 5 | `scripts/check-forbidden-imports.mjs` | EXISTING at `scripts/check-forbidden-imports.mjs` (verified working 2026-05-01 by reading source) | unchanged path (no move) | Wired into CI by Plan 01-02 (`.github/workflows/lint-frontend2.yml`). Self-tests at `scripts/__tests__/check-forbidden-imports.test.mjs`. Default scan root: `<repo>/frontend2/src`. Forbidden patterns: `^(?:idb|serwist|@serwist/.+)$/i` (exact) + `/(offline|sync)/i` (substr). |

    ## Rebuild from Scratch (FOUND-03 — exactly four concepts)

    | # | Concept | Why Rebuild | Target Phase |
    |---|---------|-------------|--------------|
    | 1 | Chrome — TopBar / Sidebar / Bottombar / PageHeader | Sketch 005 premium-terminal fidelity is new; v2.1 chrome was partial (no Bottombar; no `// GROUP` sidebar labels; no SESSION/LAST SYNC meta) | Phase 3 |
    | 2 | Retro atoms — Panel / Button / Badge / Input / Select / Combobox / Textarea / Checkbox / FileInput / FormField / Table family / Tabs / Dialog / ConfirmDialog / Toast / EmptyState / Pagination / StatusDot / HUD primitives | Visual re-derivation per sketch 005 + new constraints from Phase 3 layout (modal-stack ESC, status pills, tabular-nums, SSE live-dot in panel headers) | Phase 4 |
    | 3 | Layout grid + design tokens (palette, typography, scanline body overlay, radius=0 sharp corners, JetBrains Mono Variable) | Token system locked from sketches 001-005; v2.1 used a different (retro-cream) palette | Phase 2 |
    | 4 | Provider stack composition (IntlProvider + QueryClientProvider + AuthProvider + SSEProvider + ToastProvider + ShortcutsProvider + BrowserRouter in canonical order per PROV-01) | Some providers are new for v3.0 (ShortcutsProvider, SSEProvider with `useSSEStatus()` selector, ToastProvider mounted as sonner) | Phase 6 |

    ## Backend Endpoint Specs (D-10 / D-11 — for Phase 13 Dashboard HUD)

    Per locked decisions D-09..D-11 (no feature flag — there is no production environment), Phase 13 ships the HUD row directly. The two new backend endpoints are documented here so Phase 13 planning can scope them; the endpoints themselves are built as part of Phase 13 (or an adjacent backend task before Phase 13).

    ### `GET /api/workspaces/{wsId}/stats/capacity`

    **Response shape:**
    ```json
    { "total_items": 1234, "capacity_target": 5000 }
    ```
    or, when no warehouse-capacity feature exists yet:
    ```json
    { "total_items": 1234, "capacity_target": null }
    ```

    **Notes:**
    - `capacity_target = null` is the empty state until a warehouse-capacity feature ships (deferred to v3.1+).
    - Used by HUD capacity gauge (DASH-04, Phase 13).
    - Workspace-scoped per existing REST convention; ASVS V4 access control inherits from the rest of `/api/workspaces/{wsId}/*`.

    ### `GET /api/workspaces/{wsId}/stats/activity?days=14`

    **Response shape:**
    ```json
    { "days": [{ "date": "2026-04-18", "count": 12 }, { "date": "2026-04-19", "count": 7 }, ...] }
    ```

    **Notes:**
    - 14 days ending today (UTC).
    - Zero-fill missing days (a day with no events still appears with `count: 0`).
    - `count` = item-mutation events (create + update + archive). Definition refined by Phase 13 backend task.
    - Used by HUD 14-day activity sparkline (DASH-04, Phase 13). Hand-rolled SVG (no charting library — see Out of Scope).

    ## Out of Scope (Reaffirmed)

    - **IndexedDB / Serwist / offline / sync*** — CI grep guard at `scripts/check-forbidden-imports.mjs` enforces. Plan 01-02 wires the merge gate.
    - **Lingui v5 macros** — replaced by Plan 01-03 spike winner. See `.planning/research/I18N-DECISION.md` for verdict + evidence.
    - **v2.1 retro atom files** — component-by-component re-derivation per Phase 2-4.
    - **Mobile FAB on `≥768px` viewports** — locked by D-06 (FAB is mobile-only).
    - **Bottombar on `<768px` viewports** — locked by D-05 (Bottombar is desktop-only).
    - **Dashboard HUD feature flag** — D-09: ships unflagged (no prod env to gate against).
    - **Charting libraries (Recharts, Chart.js, Tremor)** — hand-rolled SVG for HUD per REQUIREMENTS.md Out of Scope.
    - **Animation libraries (motion, framer-motion)** — fight the locked 80ms CSS transitions.

    ## Reserved Env Vars

    These exist for downstream phases; Plan 01 reserves the names but does not yet consume them:
    - `E2E_USER`, `E2E_PASS` — Playwright seeder credentials per CLAUDE.md
    - `E2E_BASE_URL` — Playwright baseURL override
    - `TEST_DATABASE_URL` — Go integration test harness override (Phase 17 + future plan-65-11-style regression tests)

    ## Cross-References

    - Plan 01-01 — scaffold + lib/api.ts + lib/queryClient.ts ports (Port Verbatim row 1 + row 5)
    - Plan 01-02 — CI workflow wiring (Port Verbatim row 5; FOUND-02)
    - Plan 01-03 — i18n spike + decision + winner install (FOUND-04)
    - Plan 01-04 — this document (FOUND-03 + FOUND-05 + FOUND-06)

    ---

    *Audit completed: 2026-05-01*
    *Next read by: Phase 2 planner (tokens), Phase 3 planner (chrome), Phase 5 planner (auth + OAuth callback port), Phase 12 planner (format hooks port), Phase 13 planner (HUD endpoints).*
    ```

    **Verification before commit**: ensure all five Port Verbatim rows are present (numbered 1-5) and all four Rebuild rows are present (numbered 1-4); ensure both endpoint specs (`/stats/capacity` AND `/stats/activity`) appear with response shapes.

    Use the Write tool — do NOT use heredoc.
  </action>
  <verify>
    <automated>
      test -f .planning/research/CARRY-FORWARD.md && \
      grep -q '## Port Verbatim' .planning/research/CARRY-FORWARD.md && \
      grep -q '## Rebuild from Scratch' .planning/research/CARRY-FORWARD.md && \
      grep -q 'Auth flow' .planning/research/CARRY-FORWARD.md && \
      grep -q 'OAuth callback' .planning/research/CARRY-FORWARD.md && \
      grep -q 'Format hooks' .planning/research/CARRY-FORWARD.md && \
      grep -q 'Playwright auth helper' .planning/research/CARRY-FORWARD.md && \
      grep -q 'check-forbidden-imports.mjs' .planning/research/CARRY-FORWARD.md && \
      grep -qE 'Chrome.*TopBar|TopBar.*Sidebar' .planning/research/CARRY-FORWARD.md && \
      grep -q 'Retro atoms' .planning/research/CARRY-FORWARD.md && \
      grep -qE 'Layout grid.*design tokens' .planning/research/CARRY-FORWARD.md && \
      grep -q 'Provider stack' .planning/research/CARRY-FORWARD.md && \
      grep -q '/stats/capacity' .planning/research/CARRY-FORWARD.md && \
      grep -q '/stats/activity' .planning/research/CARRY-FORWARD.md && \
      grep -q '3826d24' .planning/research/CARRY-FORWARD.md && \
      grep -q '4d4c233' .planning/research/CARRY-FORWARD.md && \
      grep -q '5e77f98' .planning/research/CARRY-FORWARD.md && \
      grep -c '^| [1-5] |' .planning/research/CARRY-FORWARD.md | grep -qE '^9$|^[1-9][0-9]+$'
    </automated>
  </verify>
  <acceptance_criteria>
    - .planning/research/CARRY-FORWARD.md exists with at least 60 lines
    - File contains both required H2 headings: `## Port Verbatim` AND `## Rebuild from Scratch`
    - All five port-verbatim items are present (greppable substrings: `Auth flow`, `OAuth callback`, `Format hooks`, `Playwright auth helper`, `check-forbidden-imports.mjs`)
    - All four rebuild items are present (greppable: `Chrome`/`TopBar`, `Retro atoms`, `Layout grid`/`design tokens`, `Provider stack`)
    - Both backend endpoint specs documented (greppable: `/stats/capacity` AND `/stats/activity`)
    - Real v2.1 source SHAs cited (at minimum `3826d24`, `4d4c233`, `5e77f98` — three of the five Port Verbatim rows have explicit SHAs per 01-PATTERNS.md)
    - Numbered table rows (`| 1 |`, `| 2 |`, ...) total at least 9 across the two tables (5 port-verbatim + 4 rebuild)
  </acceptance_criteria>
  <done>CARRY-FORWARD.md is the canonical audit document; downstream planners can `git show <SHA>:<path>` to extract verbatim ports; Phase 13 has its endpoint contract.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update STATE.md Decisions section with v3.0 D-05..D-11 entries</name>
  <files>
    .planning/STATE.md
  </files>
  <read_first>
    .planning/STATE.md (full file — preserve all existing decisions and frontmatter; only append),
    .planning/phases/01-foundation-conflict-spikes/01-CONTEXT.md (D-05..D-11 verbatim source — must match wording exactly)
  </read_first>
  <action>
    Edit `.planning/STATE.md` to append seven new decision entries to the existing `### Decisions` section (NOT replace; append after the last v2.2 entry). Use Edit tool — find the line `- v2.2: Quick Capture included in v2.2 scope -- INT-QC-01..04 in Phase 69` (last v2.2 entry per current STATE.md) and append the seven v3.0 entries immediately after it.

    Append these exact lines (preserve indentation matching existing list — single-dash bullet, no nested):

    ```
    - v3.0: Mobile (<768px) renders FAB only, no Bottombar (D-05) -- locks Phase 3 BAR scope
    - v3.0: Desktop (>=768px) renders Bottombar only, no FAB (D-06) -- locks Phase 3 BAR scope
    - v3.0: FAB exposes context-aware radial menu -- actions adapt per route (D-07)
    - v3.0: Bottombar and FAB both consume useShortcuts context as single source of truth (D-08)
    - v3.0: Dashboard HUD row ships in Phase 13 WITHOUT feature flag -- no prod env to gate against (D-09)
    - v3.0: HUD backend endpoint specs documented in CARRY-FORWARD.md; endpoints built in Phase 13 (D-10)
    - v3.0: HUD endpoints -- GET /api/workspaces/{wsId}/stats/capacity + GET /api/workspaces/{wsId}/stats/activity?days=14 (D-11)
    ```

    Note: STATE.md uses `--` (double-dash) as the dash separator in existing entries; match the convention. Use ASCII `>=` and `<` rather than `≥`/`<` since existing v2.2 entries are ASCII-clean.

    Also update the frontmatter:
    - `status:` change from `Roadmap defined; Phase 1 unblocked` to `Phase 1 plans created; ready for execution`
    - `stopped_at:` change to `Phase 1 plans created`
    - `last_updated:` change to today's date in ISO 8601 format (use `date -u +%Y-%m-%dT%H:%M:%S.%3NZ` if available, else `2026-05-01T00:00:00Z`)
    - `last_activity:` change to `2026-05-01 -- Phase 1 plans created (4 plans across 2 waves; FOUND-01..06 covered)`

    Update the `Current Position` section similarly (the values mirror frontmatter).

    Append a `## Session Continuity` update line at the end of the file (do NOT replace existing italic update lines — append a new one):

    ```
    *Updated: 2026-05-01 -- Phase 1 planning complete. 4 plans cover FOUND-01 (scaffold), FOUND-02 (CI guard wiring), FOUND-03+05+06 (CARRY-FORWARD.md + STATE.md decisions), FOUND-04 (i18n spike + winner install). Wave 1: plans 01 + 04 parallel. Wave 2: plans 02 + 03 parallel (no file overlap). Spike for D-04 runs on throwaway branch never merged.*
    ```

    Do NOT modify the v2.2 abandoned-phase tables, the v3.0 phase overview table, or any other historical section.
  </action>
  <verify>
    <automated>
      grep -q 'v3.0: Mobile (<768px) renders FAB only' .planning/STATE.md && \
      grep -q 'v3.0: Desktop (>=768px) renders Bottombar only' .planning/STATE.md && \
      grep -q 'v3.0: FAB exposes context-aware radial menu' .planning/STATE.md && \
      grep -q 'v3.0: Bottombar and FAB both consume useShortcuts' .planning/STATE.md && \
      grep -q 'v3.0: Dashboard HUD row ships in Phase 13 WITHOUT feature flag' .planning/STATE.md && \
      grep -q 'v3.0: HUD backend endpoint specs documented' .planning/STATE.md && \
      grep -q 'v3.0: HUD endpoints' .planning/STATE.md && \
      grep -q '/stats/capacity' .planning/STATE.md && \
      grep -q '/stats/activity' .planning/STATE.md && \
      grep -q 'D-05' .planning/STATE.md && grep -q 'D-08' .planning/STATE.md && grep -q 'D-11' .planning/STATE.md && \
      grep -q 'v2.2: Quick Capture included' .planning/STATE.md && \
      grep -q 'Phase 1 plans created' .planning/STATE.md
    </automated>
  </verify>
  <acceptance_criteria>
    - .planning/STATE.md contains all seven new v3.0 decision lines (greppable individually for each)
    - Each new line includes its decision ID (D-05, D-06, D-07, D-08, D-09, D-10, D-11) — verified by greps for D-05, D-08, D-11 (sample)
    - Endpoint paths `/stats/capacity` and `/stats/activity` appear in STATE.md (the D-11 line)
    - The last existing v2.2 entry (`v2.2: Quick Capture included`) is still present (NOT removed during the append)
    - Frontmatter `status:` updated to indicate Phase 1 plans are created (greppable for `Phase 1 plans created`)
    - No deletions to the rest of STATE.md beyond the targeted updates
  </acceptance_criteria>
  <done>STATE.md Decisions section is the canonical SSOT for v3.0 locked decisions; downstream phases inherit D-05..D-11 without re-discussing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning artifact ↔ downstream phase planner | Future planners read this audit and inherit constraints; document drift = silent constraint violation |
| Decision document ↔ implementation | Implementers read STATE.md Decisions and assume they're authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Future PR silently edits CARRY-FORWARD.md to drop a port-verbatim row, opening the door to a re-implementation that drifts from v2.1 | mitigate | Each Port Verbatim row cites a v2.1 SHA via `git show <SHA>:<path>`; reviewers can verify the source still matches; SHA is immutable in git history |
| T-04-02 | Spoofing | Phase 13 planner cites a different endpoint shape than CARRY-FORWARD.md, leading to backend/frontend skew | mitigate | Endpoint shapes recorded inline with response JSON examples; Phase 13 plan must cite this document; CI for Phase 13 (Plan 01-02 grep guard does not catch this — Phase 13 planner responsibility) |
| T-04-03 | Tampering | An adversary edits STATE.md to remove D-05..D-08, allowing Phase 3 to ship FAB on desktop AND Bottombar on mobile (UX disaster but not a security breach) | accept | Solo-developer threat model; PR review by the maintainer is the control; D-05..D-11 also live in CARRY-FORWARD.md (defense in depth via redundant doc) |
| T-04-04 | Information Disclosure | CARRY-FORWARD.md inadvertently leaks an internal credential or PII via copy-paste from v2.1 archives | mitigate | Document is reviewed before commit; Pattern 5 layout is metadata-only (no actual catalog data, no env-var values); reserved env-var NAMES are listed (not values) |
| T-04-05 | Repudiation | "We don't know which v2.1 file is canonical for the auth flow" — discoverability | mitigate | Port Verbatim row 1 cites both `3826d24` (original) AND `4d4c233` (FormData merge); future planners can `git show` either |
</threat_model>

<verification>
1. CARRY-FORWARD.md contains all required sections (Port Verbatim + Rebuild + Backend Endpoint Specs + Out of Scope + Reserved Env Vars + Cross-References).
2. STATE.md contains all seven D-05..D-11 entries with correct decision IDs.
3. STATE.md frontmatter reflects Phase 1 plan-creation milestone.
4. Sanity check: `grep -c '^| [0-9] |' .planning/research/CARRY-FORWARD.md` returns at least 9 (5 port + 4 rebuild = 9 numbered rows minimum).
5. No collateral damage: `git diff .planning/STATE.md` should show only additions (no deletions of historical content).
</verification>

<success_criteria>
- `.planning/research/CARRY-FORWARD.md` exists, contains all five FOUND-03 port-verbatim items with v2.1 SHAs cited, all four rebuild concepts mapped to phases, and both D-11 backend endpoint specs documented
- `.planning/STATE.md` Decisions section appended with seven new v3.0 lines covering D-05 through D-11
- STATE.md frontmatter updated to reflect Phase 1 plan creation
- No v1.x..v2.2 historical content removed from STATE.md
- FOUND-03 + FOUND-05 + FOUND-06 are now closeable from a verification standpoint (the artifacts they require exist)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-conflict-spikes/01-04-SUMMARY.md` recording:
- Confirmation that all five port-verbatim rows + all four rebuild rows + both endpoint specs landed in CARRY-FORWARD.md
- Confirmation that all seven D-05..D-11 entries landed in STATE.md
- Note that the OAuth callback (row 2) and Format hooks (row 3) intentionally have placeholder source-SHAs because the precise v2.1 archive paths are deferred to Phase 5/12 planners (per the FOUND-03 contract — Phase 1 reserves the destination paths, downstream phases own the actual port).
- Reminder that the spike branch from Plan 01-03 is preserved as `spike/i18n-decision` + tag `spike/i18n-decision-evidence` (referenced in I18N-DECISION.md, not in CARRY-FORWARD.md).
</output>
