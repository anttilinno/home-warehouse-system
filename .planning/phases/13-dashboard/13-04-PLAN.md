---
phase: 13-dashboard
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend2/src/features/dashboard/components/HudRow.tsx
  - frontend2/src/features/dashboard/components/HudRow.test.tsx
otmf_note: "All-new files under features/dashboard/components/HudRow.*. DISJOINT from W1 siblings 13-01/02/03. Does NOT touch DashboardPage.tsx — Plan 13-05 (W2) imports HudRow and mounts it behind the flag check. The flag gate ALSO lives inside HudRow (renders null when flag off) so mounting it unconditionally in 13-05 is safe."
autonomous: true
requirements: [DASH-04]
must_haves:
  truths:
    - "With VITE_FEATURE_HUD_ROLLUPS unset/false, HudRow renders nothing (dashboard identical to today)"
    - "With VITE_FEATURE_HUD_ROLLUPS=true, HudRow renders a hand-rolled SVG capacity gauge + a 14-day sparkline + counts"
    - "No charting library is imported (hand-rolled SVG only)"
    - "The missing 14-day backend aggregate is rendered as an empty/'data pending' sparkline, not faked data"
  artifacts:
    - path: frontend2/src/features/dashboard/components/HudRow.tsx
      provides: "flag-gated HUD: SVG gauge + sparkline + counts from DashboardStats"
      contains: "VITE_FEATURE_HUD_ROLLUPS"
  key_links:
    - from: frontend2/src/features/dashboard/components/HudRow.tsx
      to: "import.meta.env.VITE_FEATURE_HUD_ROLLUPS"
      via: "early return null when the flag !== 'true' (default off)"
      pattern: "VITE_FEATURE_HUD_ROLLUPS"
    - from: frontend2/src/features/dashboard/components/HudRow.tsx
      to: "hand-rolled <svg> gauge + sparkline"
      via: "raw SVG path/polyline math — NO recharts/chart.js/d3 import"
      pattern: "<svg"
---

<objective>
The flag-gated HUD row (DASH-04): a hand-rolled SVG capacity gauge + a 14-day activity sparkline +
counts, gated behind `import.meta.env.VITE_FEATURE_HUD_ROLLUPS === "true"`, DEFAULT OFF. With the
flag unset the dashboard renders identically to today (HudRow returns null). NO charting library —
hand-rolled SVG only (POL-04 bundle budget; charting belongs to Phase 13b).

Data reality (VALIDATION-confirmed): `/analytics/activity` accepts `limit` only — there is NO
`days=14` aggregate and NO `capacity_target` in the backend. So:
- Counts come from `DashboardStats` (passed in as a prop from the page).
- The gauge needs a `capacity_target` the backend does not provide → use a client constant/stub and
  DOCUMENT it as data-pending (do NOT invent a backend endpoint this phase).
- The 14-day sparkline has no backend series → render an EMPTY / "data pending" sparkline (a flat
  baseline or a dashed placeholder), clearly labelled, rather than fabricating a series.

The CARRY-FORWARD backend-coordination items (`/activity?days=14`, `capacity_target`) are already
noted in the roadmap; this plan ships the UI flag-off and leaves the data seam honest.

Purpose: DASH-04 — the HUD row, flag-gated default off.
Output: HudRow (standalone — Plan 13-05 mounts it; the flag gate lives inside the component too).

Wave 1, DISJOINT from siblings 13-01/02/03 (all-new files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-dashboard/13-CONTEXT.md
@.planning/phases/13-dashboard/13-VALIDATION.md

# Flag precedent + the stats shape to bind:
@frontend2/src/features/dashboard/DashboardPage.tsx

<interfaces>
<!-- Verified from source this planning session. Use directly; no exploration needed. -->

Flag precedent (CONTEXT §Flag pattern): `import.meta.env.VITE_AUTHELIA_ENABLED === "true"` in
SocialLoginButtons.tsx. HUD gates on `import.meta.env.VITE_FEATURE_HUD_ROLLUPS === "true"`, default off.

DashboardStats (lib/types.ts) — fields available for counts: total_items, total_inventory,
active_loans, overdue_loans, low_stock_items, total_locations, total_containers, total_categories,
total_borrowers. HudRow takes `stats?: DashboardStats` as a prop (the page already holds it — no
duplicate fetch).

NO charting lib is installed and none may be added (grep-gate: assert no recharts/chart.js/d3 import).
Gauge + sparkline are raw <svg> with computed path/polyline coordinates. Atoms via @/components/retro
for the surrounding panel chrome. Strings via <Trans>/t.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HudRow — flag gate + SVG gauge + sparkline + counts</name>
  <files>frontend2/src/features/dashboard/components/HudRow.tsx, frontend2/src/features/dashboard/components/HudRow.test.tsx</files>
  <behavior>
    HudRow.test.tsx (render under lingui provider; control the flag by stubbing import.meta.env or
    via a `forceEnabled` test prop — pick the cleaner of the two; if stubbing env is awkward in the
    test runner, expose an internal `enabled` prop defaulting to the flag read so the test can force it):
    - flag OFF (default) → HudRow renders NOTHING (container.firstChild is null / no svg in DOM)
    - flag ON → an <svg> gauge is present AND a sparkline element is present AND the counts render
    - flag ON with no 14-day series → the sparkline shows the "data pending" / empty treatment
      (assert a placeholder label or a flat baseline, NOT fabricated points)
  </behavior>
  <action>
    HudRow.tsx: `export function HudRow({ stats }: { stats?: DashboardStats })`. First line:
    `const enabled = import.meta.env.VITE_FEATURE_HUD_ROLLUPS === "true";` (optionally allow a test
    override prop). `if (!enabled) return null;`. When enabled, render a Window/bevel panel row with
    three cells:
    (1) Capacity gauge — a hand-rolled <svg> arc/donut. Since there is no backend capacity_target,
        define a CLIENT constant (e.g. `CAPACITY_TARGET_PLACEHOLDER`) and compute the fill ratio from
        total_inventory / target; render a small "data pending" caption so it is honestly stubbed.
    (2) 14-day activity sparkline — a hand-rolled <svg> polyline. No backend series exists → render an
        empty/dashed baseline with a "data pending" caption (do NOT fabricate a series). If a
        client-derivable series later exists, the seam is the `series: number[]` prop — leave it empty []
        now.
    (3) Counts — a few key DashboardStats numbers (e.g. total_items, active_loans, low_stock_items) as
        mono figures with labels.
    NO recharts/chart.js/d3 import — raw SVG coordinate math only. Compose @/components/retro panel
    chrome; strings via <Trans>/t. Document the two backend-coordination gaps (capacity_target,
    /activity?days=14) in a top-of-file comment.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test src/features/dashboard/components/HudRow.test.tsx && ! grep -REn "from ['\"](recharts|chart\.js|d3)" src/features/dashboard/components/HudRow.tsx</automated>
  </verify>
  <done>HudRow renders null when flag off, gauge+sparkline+counts when on, no charting-lib import; sparkline honestly empty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time flag → UI surface | VITE_FEATURE_HUD_ROLLUPS controls whether the HUD compiles into the rendered tree; no runtime/user input crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Information Disclosure | fabricated capacity/sparkline misleading the operator | mitigate | gauge target is a labelled client placeholder + sparkline renders "data pending" — no invented data presented as real |
| T-13-09 | Tampering | a charting library sneaking into the bundle | mitigate | grep-gate asserts no recharts/chart.js/d3 import; hand-rolled SVG only (POL-04) |
| T-13-SC | Tampering | npm installs | mitigate | none — no packages installed (raw SVG) |
</threat_model>

<verification>
- `cd frontend2 && bunx tsc --noEmit` clean.
- `bun run test src/features/dashboard/components/HudRow.test.tsx` green (flag-off null, flag-on svg present).
- Grep-gate: no recharts/chart.js/d3 import in HudRow.
- Default build (flag unset) renders no HUD — dashboard identical to today.
</verification>

<success_criteria>
- DASH-04: HudRow gated on VITE_FEATURE_HUD_ROLLUPS (default off); hand-rolled SVG gauge + sparkline +
  counts when on; no charting library; missing data honestly rendered as "data pending".
</success_criteria>

<output>
Create `.planning/phases/13-dashboard/13-04-SUMMARY.md` when done (record the HudRow import path + props
and reaffirm the two backend-coordination gaps so Plan 13-05 mounts it and the carry-forward stays tracked).
</output>
