---
phase: 13-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend2/src/lib/api/pendingChanges.ts
  - frontend2/src/features/approvals/hooks/usePendingChangesQuery.ts
  - frontend2/src/features/approvals/components/PendingApprovalsPanel.tsx
  - frontend2/src/features/approvals/components/PendingApprovalsPanel.test.tsx
otmf_note: "All-new files under lib/api/pendingChanges.ts + features/approvals/*. DISJOINT from W1 siblings 13-01 (notifications + TopBar), 13-03 (system-alerts), 13-04 (HUD). Does NOT touch DashboardPage.tsx — Plan 13-05 (W2) imports PendingApprovalsPanel and mounts it."
autonomous: true
requirements: [DASH-03]
must_haves:
  truths:
    - "PendingApprovalsPanel shows the workspace pending-changes count for owners/admins"
    - "On a 403 (non-owner/admin) the panel degrades silently — no error spam, no count"
    - "The panel links to the approvals page (or to its source) when there are pending changes"
  artifacts:
    - path: frontend2/src/lib/api/pendingChanges.ts
      provides: "workspace-scoped pending-changes api (list with optional ?status=)"
      contains: "pendingChangesApi"
    - path: frontend2/src/features/approvals/hooks/usePendingChangesQuery.ts
      provides: "usePendingChangesQuery keyed [pending-changes, wsId] with retry:false (403-safe)"
      contains: "usePendingChangesQuery"
    - path: frontend2/src/features/approvals/components/PendingApprovalsPanel.tsx
      provides: "side-rail panel rendering the pending count; degrades on 403"
      contains: "PendingApprovalsPanel"
  key_links:
    - from: frontend2/src/features/approvals/hooks/usePendingChangesQuery.ts
      to: "GET /api/workspaces/{ws}/pending-changes"
      via: "get over pendingChangesApi.list(wsId), retry:false so a 403 settles fast"
      pattern: "retry: false"
    - from: frontend2/src/features/approvals/components/PendingApprovalsPanel.tsx
      to: "403 / forbidden detection"
      via: "query.isError on a 403 → render nothing (or neutral) instead of an error banner"
      pattern: "isError"
---

<objective>
The Pending Approvals side-rail panel (DASH-03, part 1 of 2): a workspace-scoped pending-changes
api + query hook + a panel that shows the pending count for owners/admins and degrades silently
on a 403 for everyone else.

`GET /api/workspaces/{ws}/pending-changes` is owner/admin-only — it returns 403 for non-admins
(verified this session: the handler calls `canReviewChanges` and returns
`Error403Forbidden` otherwise). The verified list envelope is
`{ changes: PendingChangeResponse[], total: number }` (NOTE the key is `changes`, NOT `items`).
The panel only needs `total` for the side-rail count; on 403 it renders nothing (no error spam —
ORCHESTRATOR resolution OQ2: side-rail card = workspace pending-changes for admins, degrade
silently for others). Query with `retry:false` so a 403 settles immediately.

Purpose: DASH-03 — the side rail's Pending Approvals card.
Output: pendingChanges api, usePendingChangesQuery, PendingApprovalsPanel (standalone — Plan 13-05
mounts it into the dashboard side rail).

Wave 1, DISJOINT from siblings 13-01/03/04 (all-new files; no shared file).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-dashboard/13-CONTEXT.md
@.planning/phases/13-dashboard/13-VALIDATION.md

# Templates to mirror EXACTLY:
@frontend2/src/lib/api/maintenance.ts
@frontend2/src/features/maintenance/hooks/useMaintenanceQuery.ts

<interfaces>
<!-- Verified from source this planning session. Use directly; no exploration needed. -->

lib/api helpers (lib/api.ts): `get<T>(endpoint)`. Workspace-scoped path:
`/workspaces/${ws}/pending-changes` (optional `?status=pending|approved|rejected`).

Verified backend list envelope (pendingchange/handler.go):
```
{ changes: PendingChangeResponse[], total: number }   // key is "changes" NOT "items"
```
PendingChangeResponse JSON tags (subset the panel may surface): id, workspace_id, requester_id,
requester_name, requester_email, entity_type, entity_id, action, status, created_at, …

wsId source: `useWorkspace().currentWorkspaceId` (the D-12 SSOT) — mirror useMaintenanceDueQuery.

Atoms via @/components/retro: Window (panel chrome), RetroBadge (the count), BevelButton/link.
All strings via @lingui <Trans>/t.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pendingChanges api + usePendingChangesQuery (403-safe)</name>
  <files>frontend2/src/lib/api/pendingChanges.ts, frontend2/src/features/approvals/hooks/usePendingChangesQuery.ts</files>
  <action>
    pendingChanges.ts: define `PendingChangeDTO` (id, workspace_id, requester_id, requester_name,
    requester_email, entity_type, entity_id, action, status, created_at — typed only for fields a
    consumer reads) and `pendingChangesApi.list(ws, opts?: { status?: string })` →
    get<{ changes: PendingChangeDTO[]; total: number }>(`/workspaces/${ws}/pending-changes` + optional
    `?status=`). NOTE the envelope key is `changes` (not `items`).
    usePendingChangesQuery.ts: `usePendingChangesQuery(opts?: { status?: string })` keyed
    `["pending-changes", wsId, opts?.status ?? "all"]`, `enabled: Boolean(wsId)`, `retry: false`
    (so a 403 settles immediately — non-admins must not retry-spam). Read wsId from
    `useWorkspace().currentWorkspaceId`. Return `{ total, isLoading, isError, isForbidden }` where
    `isForbidden` derives from the error being a 403 (inspect the thrown api error's status/shape —
    mirror however other hooks detect status; if the api error carries no status, treat any isError as
    "degrade silently" per OQ2). Expose `total = query.data?.total ?? 0`.
  </action>
  <verify>
    <automated>cd frontend2 && bunx tsc --noEmit -p tsconfig.json && grep -c "retry: false" src/features/approvals/hooks/usePendingChangesQuery.ts</automated>
  </verify>
  <done>api + hook compile; hook keyed ["pending-changes", wsId, …] with retry:false; exposes total + a forbidden/degrade signal.</done>
</task>

<task type="auto">
  <name>Task 2: PendingApprovalsPanel (count + silent 403 degrade)</name>
  <files>frontend2/src/features/approvals/components/PendingApprovalsPanel.tsx, frontend2/src/features/approvals/components/PendingApprovalsPanel.test.tsx</files>
  <behavior>
    PendingApprovalsPanel.test.tsx (MSW; render under QueryClient + Workspace + lingui providers):
    - admin fixture (200 with total=3) → panel renders titled "Pending approvals" + the count 3
    - total=0 fixture → panel renders a calm "Nothing pending" / "0" state (no error)
    - 403 fixture → panel renders NOTHING (or a neutral "—" per OQ2) — assert NO error banner text
    - loading → a mono "Loading…" placeholder, never a crash
  </behavior>
  <action>
    PendingApprovalsPanel.tsx: a Window-chrome side-rail panel (title <Trans>Pending approvals</Trans>).
    Read `usePendingChangesQuery()`. Branching: isForbidden (403) → return null (or a neutral "—" strip
    — pick the silent-degrade form; NO error banner, per OQ2). isLoading → a mono "Loading…" line.
    isError but NOT forbidden → also degrade silently (treat as null) — the dashboard must never spam.
    Otherwise render the `total` prominently (RetroBadge or a big mono number) with a label; when total>0
    render a link/BevelButton to the approvals page if a route exists (`/approvals` — if no such route
    yet, link to `/pending-changes` or leave the count non-linked and note the seam in the SUMMARY).
    All strings via <Trans>/t; compose only @/components/retro atoms. This component is STANDALONE —
    it does not import DashboardPage; Plan 13-05 mounts it in the side rail.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test src/features/approvals/components/PendingApprovalsPanel.test.tsx</automated>
  </verify>
  <done>Panel test green: admin sees the count; 403 degrades silently (no error banner); 0 renders calm.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend → /workspaces/{ws}/pending-changes | owner/admin-gated read; a non-admin crosses and is rejected 403 server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Elevation | non-admin reading pending changes | mitigate | server `canReviewChanges` returns 403; frontend never assumes a count — on 403 the panel renders nothing (no leak of existence/volume) |
| T-13-05 | DoS | retry storm on a 403 side-rail card | mitigate | `retry: false` on the query so a forbidden response settles once, no backoff loop |
| T-13-SC | Tampering | npm installs | mitigate | none — no packages installed |
</threat_model>

<verification>
- `cd frontend2 && bunx tsc --noEmit` clean.
- `bun run test src/features/approvals` green incl. the 403 silent-degrade assertion.
- Query uses `retry:false` and key `["pending-changes", wsId, …]`.
- Envelope decoded as `{ changes, total }` (NOT `items`).
</verification>

<success_criteria>
- DASH-03 (part): PendingApprovalsPanel shows the workspace pending count for admins.
- On 403 the panel degrades silently — no error UI.
</success_criteria>

<output>
Create `.planning/phases/13-dashboard/13-02-SUMMARY.md` when done (record the PendingApprovalsPanel
import path + props and whether an approvals route exists, so Plan 13-05 mounts it correctly).
</output>
