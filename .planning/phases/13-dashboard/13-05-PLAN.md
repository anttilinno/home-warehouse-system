---
phase: 13-dashboard
plan: 05
type: execute
wave: 2
depends_on: [13-02, 13-03, 13-04]
files_modified:
  - frontend2/src/features/dashboard/relativeTime.ts
  - frontend2/src/features/dashboard/relativeTime.test.ts
  - frontend2/src/features/dashboard/DashboardPage.tsx
  - frontend2/src/features/dashboard/DashboardPage.test.tsx
otmf_note: "DashboardPage.tsx is a SINGLE-WRITER file — this plan (W2) is its ONLY writer in Phase 13. It EXTENDS the existing 191-line page (does NOT rewrite). It consumes the W1 standalone components: DashboardSideRail/SystemAlertsPanel (13-03), PendingApprovalsPanel (13-02, via the rail), HudRow (13-04). relativeTime.ts is a NEW standalone util owned by this plan. No W2 sibling. TopBar.tsx is owned by Plan 13-01 — NOT touched here."
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05]
must_haves:
  truths:
    - "The activity table shows relative timestamps under 24h (Nm ago / Nh ago) and absolute date+time at/after 24h"
    - "The activity table has a Status pill column derived from `action` via the existing ACTION_BADGES map (RecentActivity has no status field)"
    - "The dashboard renders a right side rail (Pending Approvals + System Alerts) beside the main column, dropping below on narrow"
    - "The flag-gated HudRow is mounted (renders only when VITE_FEATURE_HUD_ROLLUPS=true)"
    - "The dashboard registers useShortcuts('dashboard', [N→/items/new, S→/scan, L→/loans]) with no render-loop"
    - "The four DASH-01 stat tiles still render token-correct (regression guard)"
  artifacts:
    - path: frontend2/src/features/dashboard/relativeTime.ts
      provides: "formatRelativeTime(iso): <1m / Nm ago / Nh ago under 24h, absolute date+time at/after 24h"
      contains: "formatRelativeTime"
    - path: frontend2/src/features/dashboard/DashboardPage.tsx
      provides: "extended page: relative-time + Status column + side rail + HUD mount + shortcuts"
      contains: "useShortcuts"
  key_links:
    - from: frontend2/src/features/dashboard/DashboardPage.tsx
      to: frontend2/src/features/dashboard/components/DashboardSideRail.tsx
      via: "2-column responsive grid mounting <DashboardSideRail/> as the right rail"
      pattern: "DashboardSideRail"
    - from: frontend2/src/features/dashboard/DashboardPage.tsx
      to: frontend2/src/features/dashboard/components/HudRow.tsx
      via: "mount <HudRow stats={s}/> (self-gates on the flag)"
      pattern: "HudRow"
    - from: frontend2/src/features/dashboard/DashboardPage.tsx
      to: frontend2/src/components/shortcuts/useShortcuts.ts
      via: "useShortcuts('dashboard', memoized bindings) with a stable navigate ref"
      pattern: "useShortcuts\\(\"dashboard\""
---

<objective>
Extend the EXISTING DashboardPage (191 lines — do NOT rewrite) to finish DASH-02, compose the
DASH-03 side rail, mount the DASH-04 HUD, register the DASH-05 shortcuts, and guard the DASH-01
tiles. This plan is the SINGLE WRITER of DashboardPage.tsx and the sole Wave 2 plan; it consumes the
standalone Wave 1 outputs (DashboardSideRail, SystemAlertsPanel, PendingApprovalsPanel, HudRow).

Scope of the page edit (surgical, minimal-diff over the current file):
1. DASH-02 finish — (a) a TRUE relative-time formatter (new `relativeTime.ts` util, unit-tested at
   boundaries) replacing the current same-day-only `formatActivityTime`; (b) a Status pill column
   DERIVED from `action` via the existing ACTION_BADGES map (VALIDATION: RecentActivity has NO status
   field — do not invent one; the pill IS the action-derived badge in a dedicated "Status" column);
   (c) an Actor column — ROADMAP success-criteria-2 + DASH-02 require Timestamp/Action/Entity/Actor/Status.
   RecentActivity has `user_id?` (optional UUID) but NO actor name; render Actor as the short user_id
   (first 8 chars, monospace) or "—" when absent. Do NOT fabricate a name or add a backend join this phase.
   Activity table columns become Time / Action / Entity / Actor / Status (keep entity_name visible inside
   the Entity cell as a secondary line so no data is lost).
2. DASH-03 compose — wrap the page body in a responsive 2-column grid: main column (tiles + activity)
   + a right rail `<DashboardSideRail/>` that drops below on narrow.
3. DASH-04 mount — render `<HudRow stats={s} />` between the tiles and the activity table; it self-gates
   on VITE_FEATURE_HUD_ROLLUPS (renders null when off → dashboard identical to today by default).
4. DASH-05 — `useShortcuts("dashboard", memoizedBindings)` for N→/items/new, S→/scan, L→/loans.
   LANDMINE (hit 4× this project): the bindings array MUST be `useMemo`'d and each action a stable
   `useCallback` over a stable `navigate` ref — never put `t`/a fresh object/`navigate` directly in
   deps, or the register effect render-loops.
5. DASH-01 guard — keep the four tiles exactly; add a regression assertion in the page test.

Purpose: finish the dashboard — activity parity (DASH-02), side rail (DASH-03), HUD (DASH-04),
shortcuts (DASH-05), tiles guard (DASH-01).
Output: relativeTime util + tests, extended DashboardPage + a page-level test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-dashboard/13-CONTEXT.md
@.planning/phases/13-dashboard/13-VALIDATION.md
@.planning/phases/13-dashboard/13-02-SUMMARY.md
@.planning/phases/13-dashboard/13-03-SUMMARY.md
@.planning/phases/13-dashboard/13-04-SUMMARY.md

# The file to EXTEND + the shortcut pattern to mirror EXACTLY:
@frontend2/src/features/dashboard/DashboardPage.tsx
@frontend2/src/features/items/ItemsListPage.tsx

<interfaces>
<!-- Verified from source this planning session. Use directly; no exploration needed. -->

Existing DashboardPage.tsx already holds: `useWorkspace()` → wsId + workspaces; `stats` query
(["dashboard", wsId]) → DashboardStats `s`; `activity` query (["activity", wsId]) → RecentActivity[];
the ACTION_BADGES map (CREATE→ok, UPDATE→warn, DELETE→danger, MOVE/LOAN/RETURN→info); the current
`formatActivityTime` (same-day only — REPLACE its under-24h branch with relative formatting); the
two stat-tile sections; the Recent-activity Window + RetroTable. EXTEND in place.

RecentActivity (lib/types.ts): { id, user_id?, action, entity_type, entity_id, entity_name?, …,
created_at } — NO status field, NO actor name. Status pill = ACTION_BADGES[action] in a new "Status"
column. Actor column = `user_id?.slice(0,8)` (monospace) or "—" (no name available; do not invent one).

W1 components to import + mount (from their SUMMARYs):
- DashboardSideRail — `@/features/dashboard/components/DashboardSideRail` (no required props)
- HudRow — `@/features/dashboard/components/HudRow` (prop `stats?: DashboardStats`)

Shortcut pattern (ItemsListPage.tsx ~150-167 — mirror EXACTLY):
```tsx
const navigate = useNavigate();
const goNew = useCallback(() => navigate("/items/new"), [navigate]);
// … one useCallback per target, all over the stable navigate
const routeShortcuts = useMemo(() => [
  { key: "N", label: tRef.current`New item`, action: goNew }, …
], [goNew, …]);            // stable deps only
useShortcuts("dashboard", routeShortcuts);
```
`tRef` = a ref holding `useLingui().t` so labels don't churn deps (Pitfall 6). Import `useShortcuts`
from `@/components/shortcuts`, `useNavigate` from `react-router`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: relativeTime formatter util + boundary tests (DASH-02 part a)</name>
  <files>frontend2/src/features/dashboard/relativeTime.ts, frontend2/src/features/dashboard/relativeTime.test.ts</files>
  <behavior>
    relativeTime.test.ts (control "now" via a fixed reference; pass `now` as an optional 2nd arg so the
    test is deterministic — `formatRelativeTime(iso, now?)`):
    - < 60s ago → "<1m" (or "just now" — pick one and assert it)
    - 5 minutes ago → "5m ago"
    - 59 minutes ago → "59m ago"  (still relative, under 24h)
    - 3 hours ago → "3h ago"
    - 23 hours ago → "23h ago"   (boundary: still relative)
    - 24 hours ago (and older) → an ABSOLUTE date+time string (assert it is NOT "ago")
  </behavior>
  <action>
    relativeTime.ts: `export function formatRelativeTime(iso: string, now: Date = new Date()): string`.
    Compute delta = now - new Date(iso). Under 60s → "<1m" (or "just now"). Under 60m → `${m}m ago`.
    Under 24h → `${h}h ago`. At/after 24h → an absolute `date.toLocaleString(i18n.locale, { day:"numeric",
    month:"short", hour:"2-digit", minute:"2-digit" })` (reuse the existing absolute branch shape from
    the current formatActivityTime). Keep it pure + locale-aware via `@/lib/i18n` `i18n.locale`. Guard
    against future/negative deltas (treat ≤0 as "<1m"/"just now").
  </action>
  <verify>
    <automated>cd frontend2 && bun run test src/features/dashboard/relativeTime.test.ts</automated>
  </verify>
  <done>Formatter test green at all boundaries (59m→ago, 23h→ago, 24h→absolute).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: extend DashboardPage — Status column, relative time, side rail, HUD, shortcuts (single-writer)</name>
  <files>frontend2/src/features/dashboard/DashboardPage.tsx, frontend2/src/features/dashboard/DashboardPage.test.tsx</files>
  <behavior>
    DashboardPage.test.tsx (render under QueryClient + Workspace + Router + ShortcutsProvider + lingui
    providers; MSW for dashboard/activity/expiring/maintenance-due/pending-changes):
    - four DASH-01 tiles render (Items / Loans / Overdue / Low stock) — regression guard
    - the activity table renders a Status column with an action-derived pill (e.g. a CREATE row → ok badge)
    - the activity table renders an Actor column: a row WITH user_id shows its short slug; a row WITHOUT shows "—"
    - activity timestamps use the relative formatter for a recent row (e.g. "5m ago" given a fresh fixture)
    - the side rail mounts (DashboardSideRail present) AND stacks Pending Approvals ABOVE System Alerts —
      assert both headings present and that "Pending approvals" appears before "System alerts" in DOM order
      (guards the rail composition order; 13-03's tsc+grep cannot, since its W1 worktree lacks 13-02's panel)
    - useShortcuts("dashboard", …) is registered: ShortcutsContext exposes keys N, S, L (mirror
      ItemsListPage.test.tsx's `useShortcutsContext().shortcuts.map(s=>s.key)` assertion)
    - NO render-loop: the test completes without exceeding render budget (a stable-deps regression guard)
  </behavior>
  <action>
    SINGLE-WRITER, minimal-diff EXTEND of the existing file (do NOT rewrite the 191 lines):
    (1) Replace the under-24h branch of `formatActivityTime` by importing + using `formatRelativeTime`
        from `./relativeTime` for the Time cell (drop the old same-day helper or have it delegate).
    (2) Add an "Actor" `<th>`/`<td>` column (`row.user_id ? row.user_id.slice(0,8) : "—"`, monospace)
        AND a "Status" `<th>`/`<td>` column: a StatusPill/RetroBadge whose variant is
        `ACTION_BADGES[row.action.toUpperCase()] ?? "neutral"` with a human label (the action text).
        Columns: Time / Action / Entity / Actor / Status (fold entity_name into the Entity cell so it
        stays visible). Keep it honest — status is action-derived and Actor is the raw user_id slug;
        RecentActivity has neither a status field nor an actor name.
    (3) Wrap the body in a responsive grid: main column (the two tile sections + HudRow + the activity
        Window) and a right `<DashboardSideRail/>`. On narrow the rail drops below (e.g.
        `grid grid-cols-1 lg:grid-cols-[1fr_320px] gap-sp-5`). Keep the `max-w-[1280px]` container.
    (4) Mount `<HudRow stats={s} />` between the tiles and the activity Window (it self-gates on the flag).
    (5) Register shortcuts: add `const navigate = useNavigate();` a `tRef` over `useLingui().t`, three
        `useCallback`s (goNew→/items/new, goScan→/scan, goLoans→/loans) over the stable `navigate`, a
        `useMemo` bindings array (keys N/S/L), and `useShortcuts("dashboard", bindings)`. Mirror
        ItemsListPage EXACTLY — stable deps only; NEVER put `t`/`navigate`/a fresh object in the effect
        deps (render-loop landmine).
    Preserve the existing workspaces empty-state, the stats/activity queries, and all DASH-01 tile markup
    unchanged. All new strings via <Trans>/t.
  </action>
  <verify>
    <automated>cd frontend2 && bunx tsc --noEmit && bun run test src/features/dashboard/DashboardPage.test.tsx</automated>
  </verify>
  <done>Page test green: 4 tiles guarded, Actor + Status columns + relative time render, side rail + HUD mount, N/S/L shortcuts registered, no render-loop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| activity payload → rendered table | server-supplied action/entity strings render into the TUI table; React escapes them — no raw HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-10 | Spoofing | a fabricated "status" implying server state | mitigate | the Status pill is explicitly DERIVED from `action` (ACTION_BADGES) — labelled as action-derived, never presented as an independent server status (RecentActivity has none) |
| T-13-11 | DoS | shortcut/effect render-loop | mitigate | bindings useMemo'd, actions useCallback over a stable navigate, t via ref — the page test guards against the loop (Landmine hit 4×) |
| T-13-SC | Tampering | npm installs | mitigate | none — no packages installed (composes existing atoms + W1 components) |
</threat_model>

<verification>
- `cd frontend2 && bunx tsc --noEmit` clean.
- `bun run test src/features/dashboard` green (relativeTime boundaries + page test).
- Phase gate (run before declaring done): `cd frontend2 && bun run lint:tsc && bun run test &&
  bun run build && bun run lint:imports` all green.
- DASH-01 tiles guarded; DASH-02 relative time + Status column; DASH-03 rail mounted; DASH-04 HUD
  self-gated; DASH-05 N/S/L shortcuts registered with no render-loop.
</verification>

<success_criteria>
- DASH-01: four stat tiles still render token-correct (regression guard green).
- DASH-02: activity table has relative-under-24h / absolute-after timestamps + an action-derived Status pill.
- DASH-03: dashboard mounts the side rail (Pending Approvals + System Alerts), responsive.
- DASH-04: HudRow mounted, flag-gated default off.
- DASH-05: useShortcuts("dashboard", [N/S/L]) registered, no render-loop.
</success_criteria>

<output>
Create `.planning/phases/13-dashboard/13-05-SUMMARY.md` when done (record the final activity column set,
the relativeTime contract, and the dashboard grid shape so the live E2E spec + verifier bind correctly).
</output>
