---
phase: 07-items-photos
plan: 07
type: execute
wave: 4
depends_on: ["07-03", "07-05", "07-06"]
files_modified:
  - frontend2/e2e/items.spec.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [ITEM-01, ITEM-05, ITEM-07]
must_haves:
  truths:
    - "A live-stack Playwright spec proves create → list → detail → archive → hidden → unarchive against the real backend"
    - "REQUIREMENTS.md ITEM-07 prose is corrected to jpeg/png/webp (HEIC dropped) with an auditable ORIGINAL/REVISED annotation"
    - "The dropped location filter chip is recorded as a deliberate deviation (category + archived only) with ORIGINAL/REVISED annotation"
    - "The backend G-65-01 integration test is untouched and still green"
  artifacts:
    - path: "frontend2/e2e/items.spec.ts"
      provides: "live item lifecycle E2E (chromium + firefox)"
      contains: "test("
    - path: ".planning/REQUIREMENTS.md"
      provides: "ITEM-07 HEIC correction + ITEM-01 location-chip deviation note"
      contains: "REVISED"
  key_links:
    - from: "frontend2/e2e/items.spec.ts"
      to: "real /login + /items flow"
      via: "page.request cookie-authed seeding"
      pattern: "page.request"
---

<objective>
Close the phase: a live-stack Playwright E2E for the item lifecycle (create → appears in list →
detail → archive → filtered out → unarchive), plus the two binding doc corrections — ITEM-07 HEIC
prose fix (jpeg/png/webp only) and the deliberately-dropped location filter chip — both with the
auditable ORIGINAL/REVISED annotation pattern.

Purpose: phase-gate E2E coverage (ITEM-01/05) + the upload accept-list correction (ITEM-07) +
honest requirements traceability for the scope deviations RESEARCH surfaced.
Output: items.spec.ts + annotated REQUIREMENTS.md + ROADMAP.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-items-photos/07-CONTEXT.md
@.planning/phases/07-items-photos/07-RESEARCH.md
@.planning/phases/07-items-photos/07-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- CLAUDE.md auth contract for Playwright (live stack must be running per the runbook): -->
<!-- /login → fill Email + Password labels → click submit /^log in$/i; access_token cookie inherited by page + page.request. -->
<!-- Seed via page.request.post("/api/workspaces/{wsId}/items", {...}) with a unique per-run name. -->
<!-- Existing spec frontend2/e2e/login-dashboard.spec.ts is the canonical pattern; chromium+firefox projects. -->
<!-- E2E_USER=seeder@test.local E2E_PASS=password123 (override via env). -->
<!-- Binding doc deviations (RESEARCH RESOLVED): -->
<!--   (1) Location filter chip DROPPED — backend GET /items has NO location_id param; category + archived chips only; Location stays a DISPLAY column. -->
<!--   (2) HEIC NOT accepted — backend AllowedMimeTypes = {jpeg,png,webp}; ITEM-07 prose says "JPEG/PNG/HEIC" and must be corrected. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live item-lifecycle Playwright spec</name>
  <files>frontend2/e2e/items.spec.ts</files>
  <read_first>frontend2/e2e/login-dashboard.spec.ts (canonical login + page.request pattern), CLAUDE.md (E2E runbook + auth contract)</read_first>
  <behavior>
    - Logs in via the real /login (cookie set for page + page.request).
    - Creates an item with a unique per-run name (`E2E-${Date.now()}`) via /items/new form OR cookie-authed page.request.post (whichever is cheaper for the assertion chain).
    - Asserts the new item appears in /items list.
    - Opens detail, asserts the name renders.
    - Archives it (titlebar/overflow), asserts it disappears from the default list.
    - Turns on the ARCHIVED facet, asserts it reappears; unarchives; asserts it returns to the default list.
    - Best-effort cleanup. Locale-agnostic assertions (EN or ET) where chrome text is involved.
    - Photo upload is NOT in the E2E (VALIDATION: cover via unit+MSW; manual residue noted) UNLESS a cheap file fixture is trivial — otherwise leave a comment pointing to the unit coverage + manual check.
  </behavior>
  <action>Write `frontend2/e2e/items.spec.ts` mirroring the login-dashboard.spec.ts structure (real login, cookie-inherited page.request, unique per-run name). Cover create → list → detail → archive → filtered-out → archived-facet-reveal → unarchive. In-plan gate is `npx playwright test --list` (proves the spec parses + is discovered); full live run is the phase verification step (requires the running stack per CLAUDE.md). Add a comment block citing the photo-upload manual residue (VALIDATION Manual-Only Verifications) and the still-standing by-barcode browser-spec gap (lands Phase 11).</action>
  <verify>
    <automated>cd frontend2 && npx playwright test --list e2e/items.spec.ts</automated>
  </verify>
  <done>items.spec.ts is discovered by both projects and parses; the lifecycle steps are present; manual residue + Phase-11 gap noted.</done>
</task>

<task type="auto">
  <name>Task 2: Doc corrections — HEIC + location chip (ORIGINAL/REVISED)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>.planning/REQUIREMENTS.md (ITEM-01, ITEM-07 lines), .planning/ROADMAP.md (Phase 7 success criteria 1 + 4)</read_first>
  <behavior>
    - REQUIREMENTS.md ITEM-07: the "JPEG/PNG/HEIC" prose gets a dated ORIGINAL/REVISED annotation pair — ORIGINAL preserves the original text verbatim; REVISED states "JPEG/PNG/WebP only — HEIC rejected server-side (AllowedMimeTypes); client accept-list excludes it" with the date + reason (07-RESEARCH Pitfall 2).
    - REQUIREMENTS.md ITEM-01 (+ ROADMAP Phase 7 Success Criterion 1): the location filter chip is annotated as DROPPED — REVISED notes "category + archived filter chips only; backend GET /items has no location_id param; Location remains a display column" with date + reason (07-RESEARCH OQ1). Do NOT delete the original text — wrap it in the ORIGINAL annotation.
    - Both edits follow the auditable-history pattern used by the Phase 65 D-06 / REQUIREMENTS:94 annotations (preserve original inside ORIGINAL, add dated REVISED).
  </behavior>
  <action>Apply the two ORIGINAL/REVISED annotation pairs to REQUIREMENTS.md (ITEM-07 HEIC→webp; ITEM-01 location-chip dropped) and mirror the location-chip deviation note into ROADMAP.md Phase 7 Success Criterion 1 + the photo criterion 4 (HEIC→WebP), preserving original text inside the ORIGINAL annotation and dating the REVISED note 2026-06-13 with the RESEARCH citation. Match the existing Phase-65 annotation style (the file already contains REVISED examples).</action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system && grep -c "REVISED" .planning/REQUIREMENTS.md | grep -vqE '^0$' && grep -iq "webp" .planning/REQUIREMENTS.md && grep -iq "location" .planning/ROADMAP.md && echo OK</automated>
  </verify>
  <done>ITEM-07 corrected to jpeg/png/webp and ITEM-01 location-chip recorded as a deliberate deviation, both with dated ORIGINAL/REVISED annotations; ROADMAP Phase 7 criteria mirror the changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E seed → real backend | test creates real rows in warehouse_dev via cookie-authed request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-20 | Tampering | E2E pollutes shared dev DB | mitigate | Unique per-run name (`E2E-${Date.now()}`) + best-effort cleanup; no fixed ids reused across runs. |
| T-07-21 | Repudiation | scope deviation hidden from traceability | mitigate | ORIGINAL/REVISED annotations make the dropped location chip + HEIC correction auditable in REQUIREMENTS.md + ROADMAP.md (never silently omitted). |
| T-07-22 | Tampering | accidental backend test regression | mitigate | This plan touches NO backend files — G-65-01 integration test (`-tags=integration`) stays green by construction; phase verification re-runs it. |
| T-07-SC | Tampering | npm installs | mitigate | @playwright/test already present; zero new installs; slopcheck N/A. |
</threat_model>

<verification>
- `cd frontend2 && npx playwright test --list e2e/items.spec.ts` discovers the spec.
- Phase gate (verification step, live stack): `E2E_USER=seeder@test.local E2E_PASS=password123 bun run test:e2e items.spec.ts` green.
- `grep REVISED .planning/REQUIREMENTS.md` shows the two new annotations.
- Backend `go test -tags=integration ./internal/domain/warehouse/item/...` UNCHANGED + green.
</verification>

<success_criteria>
- Live E2E covers the item lifecycle (ITEM-01/05).
- ITEM-07 HEIC prose corrected (jpeg/png/webp) with ORIGINAL/REVISED.
- Dropped location chip recorded as a deliberate deviation with ORIGINAL/REVISED.
- G-65-01 backend integration test untouched + green.
</success_criteria>

<output>
Create `.planning/phases/07-items-photos/07-07-SUMMARY.md` when done.
</output>
