---
phase: 07-items-photos
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend2/src/lib/utils/image.ts
  - frontend2/src/lib/utils/image.test.ts
autonomous: true
requirements: [ITEM-07]
must_haves:
  truths:
    - "compressImage resizes oversized images on a canvas and returns a File under the source dimensions"
    - "EXIF orientation is baked in via createImageBitmap so portrait phone photos are not sideways"
    - "Only jpeg/png/webp pass the client accept-check; HEIC and oversized files are rejected with a clear reason"
  artifacts:
    - path: "frontend2/src/lib/utils/image.ts"
      provides: "compressImage (EXIF-aware) + validateUploadFile (accept-list + 10MB)"
      exports: ["compressImage", "validateUploadFile"]
  key_links:
    - from: "frontend2/src/lib/utils/image.ts"
      to: "createImageBitmap"
      via: "imageOrientation:'from-image' before drawImage"
      pattern: "imageOrientation"
---

<objective>
Port the legacy canvas compression path from `frontend/lib/utils/image.ts` STRUCTURE and
ADD the EXIF-orientation fix the legacy port lacks, plus the client-side upload accept-list
(jpeg/png/webp only — NOT HEIC) and 10MB cap. Lib-free, no new deps.

Purpose: The photo pipeline (Plan 04) needs an EXIF-correct, size-bounded, accept-validated
File before it ever hits the network. This plan owns that pure utility in isolation so it
parallelizes with the API layer.
Output: `lib/utils/image.ts` with `compressImage` + `validateUploadFile` and a jsdom-aware test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-items-photos/07-RESEARCH.md
@.planning/phases/07-items-photos/07-VALIDATION.md
@frontend/lib/utils/image.ts

<interfaces>
<!-- Backend authority (defense-in-depth — client check is UX only): -->
<!-- AllowedMimeTypes = {image/jpeg, image/png, image/webp}; MaxFileSize = 10MB. HEIC is server-REJECTED. -->
<!-- RESEARCH Code Example: createImageBitmap(file, {imageOrientation:"from-image"}) bakes orientation; -->
<!-- then scale by maxDim/max(w,h), drawImage onto canvas, canvas.toBlob(type, quality), wrap in File. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: validateUploadFile (accept-list + size cap)</name>
  <files>frontend2/src/lib/utils/image.ts, frontend2/src/lib/utils/image.test.ts</files>
  <read_first>frontend/lib/utils/image.ts, .planning/phases/07-items-photos/07-RESEARCH.md (Pitfall 2)</read_first>
  <behavior>
    - validateUploadFile(file): returns {ok:true} | {ok:false, reason} where reason is one of the UI-SPEC copy strings.
    - Accept set = {"image/jpeg","image/png","image/webp"} ONLY. HEIC (image/heic / image/heif) → {ok:false, reason:"That file type isn't allowed."}.
    - file.size > 10*1024*1024 → {ok:false, reason:"File is too large (max 10.0 MB)."}.
    - Empty/zero-byte file → rejected with the type message (no MIME).
  </behavior>
  <action>Create `lib/utils/image.ts` exporting `validateUploadFile` with the accept-list `["image/jpeg","image/png","image/webp"]` and the 10MB constant (NO HEIC — Pitfall 2; the backend rejects it server-side). Return the exact UI-SPEC copy strings as the `reason`. Write `image.test.ts` cases for each accept type, HEIC rejection, oversize rejection. These are pure (no canvas) so they run in jsdom without shims.</action>
  <verify>
    <automated>cd frontend2 && bun run test src/lib/utils/image.test.ts -t validateUploadFile</automated>
  </verify>
  <done>jpeg/png/webp accepted; HEIC + oversize rejected with the exact UI-SPEC reason strings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: compressImage (canvas resize + EXIF orientation)</name>
  <files>frontend2/src/lib/utils/image.ts, frontend2/src/lib/utils/image.test.ts</files>
  <read_first>.planning/phases/07-items-photos/07-RESEARCH.md (Code Examples → EXIF-aware compress; Pitfall 3)</read_first>
  <behavior>
    - compressImage(file, maxDim=1600, quality=0.85): createImageBitmap(file,{imageOrientation:"from-image"}) → scale = min(1, maxDim/max(w,h)) → canvas w/h → drawImage → toBlob(type, quality) → new File. PNG stays image/png; everything else → image/jpeg.
    - bitmap.close() called after draw.
    - toBlob failure → rejects with Error("toBlob failed"); missing canvas ctx → Error("no canvas ctx").
    - Test strategy (jsdom lacks canvas): mock createImageBitmap + HTMLCanvasElement.toBlob (or document the happy-dom/node-canvas shim) so the orientation-call and scale-math are asserted WITHOUT a real raster. The dedicated "imageOrientation: 'from-image' is passed" assertion is the EXIF guard.
  </behavior>
  <action>Add `compressImage` to `lib/utils/image.ts` per the RESEARCH code example — `createImageBitmap(file, { imageOrientation: "from-image" })` is the load-bearing ADDITION (Pitfall 3; the legacy port has no orientation handling). Write `image.test.ts` cases mocking `globalThis.createImageBitmap` and `HTMLCanvasElement.prototype.toBlob`: assert createImageBitmap is called with `{imageOrientation:"from-image"}`, assert downscale math (e.g. 3200×2400 → 1600×1200 at maxDim 1600), assert PNG preserves type. Record the canvas-mock approach in the test file header (VALIDATION Wave 0 gap: jsdom canvas strategy).</action>
  <verify>
    <automated>cd frontend2 && bun run test src/lib/utils/image.test.ts</automated>
  </verify>
  <done>compressImage passes `imageOrientation:"from-image"`, downscales oversized dims correctly, preserves PNG; test documents the jsdom canvas-mock strategy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user file picker → upload pipeline | untrusted file bytes/type/size enter here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | DoS / Tampering | oversized / wrong-type upload | mitigate (client UX) + accept (server authoritative) | validateUploadFile enforces accept-list + 10MB as a UX pre-check; the backend `MaxFileSize`/`AllowedMimeTypes` is the real gate (defense in depth). image.test.ts proves HEIC + oversize are rejected client-side. |
| T-07-06 | Tampering | SVG upload (XSS via image) | accept (server authoritative) | SVG is absent from the accept-list and from the server's AllowedMimeTypes — never reaches storage. No client SVG path exists. |
| T-07-SC | Tampering | npm installs | mitigate | Zero new installs (native canvas + createImageBitmap only); slopcheck N/A. |
</threat_model>

<verification>
- `cd frontend2 && bun run test src/lib/utils/image.test.ts` green.
- `cd frontend2 && tsc -b --noEmit` clean.
</verification>

<success_criteria>
- compressImage is EXIF-aware (createImageBitmap orientation) — the CONTEXT-required ADDITION, not a bare legacy port.
- validateUploadFile rejects HEIC + oversize with exact UI-SPEC copy.
- No new dependencies; pure native browser APIs.
</success_criteria>

<output>
Create `.planning/phases/07-items-photos/07-02-SUMMARY.md` when done.
</output>
