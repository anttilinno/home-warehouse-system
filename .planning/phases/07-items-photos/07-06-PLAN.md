---
phase: 07-items-photos
plan: 06
type: execute
wave: 4
depends_on: ["07-01", "07-03", "07-04", "07-05"]
files_modified:
  - frontend2/src/features/items/ItemDetailPage.tsx
  - frontend2/src/features/items/ItemDetailPage.test.tsx
  - frontend2/src/features/items/components/LoanPanels.tsx
  - frontend2/src/features/items/components/ItemLabels.tsx
  - frontend2/src/features/items/components/InventoryPanelStub.tsx
  - frontend2/src/routes/index.tsx
autonomous: true
requirements: [ITEM-02]
must_haves:
  truths:
    - "Item detail renders all fields, a DETAILS/PHOTOS/HISTORY tab set, and a persistent side rail"
    - "The PHOTOS tab hosts the gallery + lightbox + upload entry from Plan 04"
    - "The active-loan panel partitions /items/{id}/loans client-side on is_active; history lists the rest"
    - "The labels row attaches/detaches existing labels; the inventory stub is a real named slot for 7b"
    - "Archive/restore/delete are reachable from the detail titlebar overflow menu"
  artifacts:
    - path: "frontend2/src/features/items/ItemDetailPage.tsx"
      provides: "mint Window, tabs, side rail, archive/delete, route wiring for new/detail/edit"
      min_lines: 90
    - path: "frontend2/src/features/items/components/InventoryPanelStub.tsx"
      provides: "named 7b slot placeholder (NOT absent — a real region)"
      contains: "Stock entries arrive in 7b"
  key_links:
    - from: "frontend2/src/features/items/ItemDetailPage.tsx"
      to: "loansApi.byItem partition"
      via: "active = is_active===true; history = rest"
      pattern: "is_active"
    - from: "frontend2/src/routes/index.tsx"
      to: "ItemDetailPage / ItemFormPage"
      via: "Routes items/new, items/:id, items/:id/edit"
      pattern: "items/:id"
---

<objective>
Build the item detail page (`/items/:id`): a mint `Window` titled with the item name, DETAILS/
PHOTOS/HISTORY tabs, a persistent side rail (active-loan panel + inventory STUB), the read-only
labels attach/detach row, titlebar archive/restore/delete, and the photo gallery+lightbox+upload
composed from Plan 04. Registers the remaining item routes (`/items/new`, `/items/:id`,
`/items/:id/edit`) since both the form (Plan 05) and detail pages mount here.

Purpose: ITEM-02 — full detail with gallery, active-loan + history panels, labels, 7b stub slot.
Output: ItemDetailPage + LoanPanels + ItemLabels + InventoryPanelStub + route registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-items-photos/07-UI-SPEC.md
@.planning/phases/07-items-photos/07-RESEARCH.md
@frontend2/src/routes/index.tsx

<interfaces>
From frontend2/src/lib/api/items.ts (Plan 01): itemsApi.get(wsId,id), archive/restore/del.
From frontend2/src/lib/api/loans.ts (Plan 01): `loansApi.byItem(wsId, itemId)` → {active, history} partitioned on is_active.
From frontend2/src/lib/api/labels.ts (Plan 01): getItemLabelIds, attach, detach, listWorkspaceLabels.
From frontend2/src/features/items/components (Plan 04):
```typescript
PhotoGallery({ wsId, itemId, photos, onOpenLightbox })
PhotoLightbox({ photos, index, onClose })
PhotoUpload({ wsId, itemId })   // dialog, opened from ⊕ ADD PHOTOS
```
From frontend2/src/features/items/ItemFormPage (Plan 05): default export ItemFormPage (used by /items/new + /items/:id/edit).
From frontend2/src/components/retro: Window (mint/pink/butter), RetroTabs, RetroBadge, StatusPill, RetroConfirmDialog, RetroEmptyState, BevelButton, FilterPopover (for the label-attach checklist).
Loan partition (RESEARCH OQ2): GET /items/{id}/loans returns ALL loans; active = is_active===true, history = rest. Key ["loans", wsId, "by-item", itemId].
Routes: literal routes before the wildcard (AP-1). /items already registered by Plan 03 — DO NOT duplicate it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LoanPanels + ItemLabels + InventoryPanelStub</name>
  <files>frontend2/src/features/items/components/LoanPanels.tsx, frontend2/src/features/items/components/ItemLabels.tsx, frontend2/src/features/items/components/InventoryPanelStub.tsx</files>
  <read_first>.planning/phases/07-items-photos/07-UI-SPEC.md (§2 Right column side rail, Labels row, Inventory STUB)</read_first>
  <behavior>
    - LoanPanels: reads loansApi.byItem (key ["loans", wsId, "by-item", itemId]); active panel = pink titlebar Window when on loan ("● On loan to {borrower}" text-accent-pink-deep + due date + disabled RETURN with hint "Loan actions arrive in Phase 8."), mint "● Available" when none. HISTORY list = the non-active loans (borrower · checkout · return-or-"— still out" · status pill); empty → RetroEmptyState "NO LOAN HISTORY".
    - ItemLabels: chips of attached labels (RetroBadge neutral, each with ✕ detach); ⊕ ADD LABEL opens a FilterPopover checklist of existing workspace labels (attach); empty workspace labels → "No labels yet — manage labels in Phase 10." Read-only label LIST (no label CRUD this phase) — attach/detach endpoints only.
    - InventoryPanelStub: a real named region — recessed bg-bg-panel-2 panel, eyebrow INVENTORY, ◇ glyph, body "Stock entries arrive in 7b." No CTA. Exists so 7b swaps contents without relayout (NOT hidden/absent).
  </behavior>
  <action>Build the three side-rail/labels components per UI-SPEC §2. LoanPanels partitions on is_active client-side (RESEARCH OQ2) — read-only (CRUD is Phase 8); the RETURN affordance is disabled with the Phase-8 hint. ItemLabels attaches/detaches via labelsApi against existing workspace labels only. InventoryPanelStub is a real slot with the exact stub copy. Test (MSW): on-loan item shows the pink active panel + borrower; available item shows mint "● Available"; history lists returned loans; attach/detach labels calls the api; stub renders "Stock entries arrive in 7b."</action>
  <verify>
    <automated>cd frontend2 && bun run test src/features/items/components/LoanPanels.test.tsx src/features/items/components/ItemLabels.test.tsx 2>/dev/null || cd frontend2 && bun run test src/features/items/components/</automated>
  </verify>
  <done>loan panels partition on is_active (read-only); labels attach/detach existing labels; inventory stub is a real named slot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ItemDetailPage (tabs, side rail, gallery, archive/delete)</name>
  <files>frontend2/src/features/items/ItemDetailPage.tsx, frontend2/src/features/items/ItemDetailPage.test.tsx</files>
  <read_first>.planning/phases/07-items-photos/07-UI-SPEC.md (§2 Item Detail + Copywriting Contract), Plan 04 photo component props</read_first>
  <behavior>
    - mint Window, titlebar = item name UPPERCASED (truncates). Titlebar actions: EDIT → /items/:id/edit + ↧ overflow (ARCHIVE/RESTORE, DELETE…).
    - Two-column at lg (grid-cols-[minmax(0,1fr)_320px]); left = RetroTabs DETAILS/PHOTOS/HISTORY; right = persistent side rail (LoanPanels active + InventoryPanelStub).
    - DETAILS tab: definition grid of fields (Name/Category/Location/Quantity mono/Barcode mono + COPY button/Description/Created+Updated) + ItemLabels row.
    - PHOTOS tab: PhotoGallery (photos from photosApi.list) + ⊕ ADD PHOTOS → PhotoUpload dialog; clicking a thumbnail opens PhotoLightbox at that index.
    - HISTORY tab: loan history list (from LoanPanels history data).
    - Archive/restore via itemsApi; delete → pink RetroConfirmDialog type-to-confirm (only when archived). On not-found → RetroEmptyState "ITEM NOT FOUND" + BACK TO ITEMS; on load error → "COULDN'T LOAD ITEM" + RETRY + retroToast.error.
  </behavior>
  <action>Build `ItemDetailPage.tsx` per UI-SPEC §2 composing RetroTabs + the side rail + Plan-04 gallery/lightbox/upload. Wire archive/restore/delete (delete archived-only, type-to-confirm). Use exact Copywriting Contract strings via lingui. Test (MSW): renders fields + DETAILS/PHOTOS/HISTORY tabs + side rail with the inventory stub; PHOTOS tab shows the gallery and opens the lightbox on thumbnail click; active-loan panel renders for an on-loan fixture; archive action calls itemsApi.archive; delete is gated to archived + type-to-confirm; not-found fixture shows ITEM NOT FOUND.</action>
  <verify>
    <automated>cd frontend2 && bun run test src/features/items/ItemDetailPage.test.tsx</automated>
  </verify>
  <done>detail renders all fields + gallery + lightbox + active-loan + history + labels + 7b stub slot; archive/restore/delete reachable; not-found/error states.</done>
</task>

<task type="auto">
  <name>Task 3: Register /items/new, /items/:id, /items/:id/edit routes</name>
  <files>frontend2/src/routes/index.tsx</files>
  <read_first>frontend2/src/routes/index.tsx (after Plan 03 added /items)</read_first>
  <action>In `routes/index.tsx` add three child routes under the authenticated AppShell branch, in literal-before-param order (AP-1 library mode): `items/new` → ItemFormPage (Plan 05), `items/:id` → ItemDetailPage, `items/:id/edit` → ItemFormPage. Place `items/new` BEFORE `items/:id` so the literal wins over the param. Import ItemFormPage (Plan 05) + ItemDetailPage. Do NOT touch the existing `items` (list) route Plan 03 registered.</action>
  <verify>
    <automated>cd frontend2 && bun run test src/features/items/ItemDetailPage.test.tsx && tsc -b --noEmit</automated>
  </verify>
  <done>/items/new + /items/:id + /items/:id/edit resolve correctly with literal-before-param ordering; the existing /items list route is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| label attach ids → server | label_id path params validated server-side for tenancy |
| barcode COPY → clipboard | user-initiated copy of own data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-17 | Information Disclosure | label attach cross-tenant | accept (backend authoritative) | attach/detach paths carry wsId + item id; backend validates the label belongs to the workspace (tenancy enforced server-side). |
| T-07-18 | Tampering | XSS via item name in titlebar / fields | mitigate | All fields render as React-escaped text (titlebar UPPERCASE via CSS, not HTML); no dangerouslySetInnerHTML. |
| T-07-19 | Information Disclosure | cross-tenant detail / loans read | accept (backend authoritative) | get + loans-by-item paths carry wsId; backend scopes by workspace_id. |
| T-07-SC | Tampering | npm installs | mitigate | Zero new installs; slopcheck N/A. |
</threat_model>

<verification>
- `cd frontend2 && bun run test src/features/items/` green (full items feature).
- `cd frontend2 && tsc -b --noEmit` clean; `bun run lint:imports` OK.
</verification>

<success_criteria>
- ITEM-02: detail with all fields, photo gallery (lightbox + arrow/ESC + primary + delete), active-loan panel, loan history panel, labels, and the 7b inventory stub slot.
- /items/new + /items/:id + /items/:id/edit all route correctly.
</success_criteria>

<output>
Create `.planning/phases/07-items-photos/07-06-SUMMARY.md` when done.
</output>
