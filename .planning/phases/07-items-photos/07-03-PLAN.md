---
phase: 07-items-photos
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - frontend2/src/features/items/ItemsListPage.tsx
  - frontend2/src/features/items/ItemsListPage.test.tsx
  - frontend2/src/features/items/hooks/useItemsQuery.ts
  - frontend2/src/features/items/hooks/useItemMutations.ts
  - frontend2/src/components/layout/Sidebar.tsx
  - frontend2/src/routes/index.tsx
autonomous: true
requirements: [ITEM-01, ITEM-05, ITEM-06, ITEM-10]
must_haves:
  truths:
    - "Items list renders 25/page from the MSW envelope with search, category + archived filter chips, and sort headers"
    - "All list state lives in the URL (?q&category&archived&sort&page) and deep-links restore it"
    - "Archived items are hidden by default and revealed by the Archived facet"
    - "Selecting rows surfaces bulk archive/delete; delete of non-archived is blocked"
    - "useShortcuts('items', [N,/,F]) is registered and the input-focus guard is respected"
  artifacts:
    - path: "frontend2/src/features/items/ItemsListPage.tsx"
      provides: "list window: FilterBar + SavedFilters + RetroTable + RetroPagination + bulk + export"
      min_lines: 80
    - path: "frontend2/src/features/items/hooks/useItemsQuery.ts"
      provides: "URL-param-driven items query keyed ['items', wsId, params]"
      contains: "[\"items\", wsId"
  key_links:
    - from: "frontend2/src/features/items/ItemsListPage.tsx"
      to: "itemsApi.list"
      via: "useItemsQuery → get list envelope"
      pattern: "useItemsQuery"
    - from: "frontend2/src/routes/index.tsx"
      to: "ItemsListPage"
      via: "Route path='items' under AppShell"
      pattern: "path=\"items\""
    - from: "frontend2/src/components/layout/Sidebar.tsx"
      to: "/items"
      via: "NavItem to='/items'"
      pattern: "to=\"/items\""
---

<objective>
Build the items list page (`/items`) — the make-or-break density surface (sketch 008): a
mint `Window` with FilterBar (search + category/archived facets), SavedFilters, the RetroTable
(sketch-008 column set), server-driven RetroPagination (25/page), URL-driven state, bulk
selection (Bottombar chips on desktop / BulkActionBar on mobile) for archive/delete, a CSV
export button, and `useShortcuts("items", [N,/,F])`. Wires the Sidebar Items entry on and
registers the `/items` route.

Purpose: ITEM-01/05/06/10 — browse, filter, sort, deep-link, archive/unarchive, type-to-confirm
delete (archived only), and keyboard shortcuts.
Output: ItemsListPage + query/mutation hooks + route registration + Sidebar enable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-items-photos/07-UI-SPEC.md
@.planning/phases/07-items-photos/07-RESEARCH.md
@frontend2/src/features/dashboard/DashboardPage.tsx
@frontend2/docs/sse-invalidation-contract.md

<interfaces>
From frontend2/src/lib/api/items.ts (Plan 01):
```typescript
itemsApi.list(wsId, { search, category_id, archived, sort, sort_dir, page, limit }): Promise<ItemListResponse>;
itemsApi.archive(wsId, id) / restore(wsId, id) / del(wsId, id): Promise<void>;
photosApi.exportCsv(wsId): Promise<void>;  // from lib/api/photos.ts
```
From frontend2/src/components/retro (Phase 4 atoms — shipped, do NOT reinvent):
```typescript
Window (titlebarVariant="mint"), BevelButton, RetroTable, RetroBadge, StatusPill
FilterBar, FilterPopover, BulkActionBar, SavedFilters, useSavedFilters
RetroPagination, useTableSelection, RetroEmptyState, RetroConfirmDialog
```
From frontend2/src/features/workspace/useWorkspace: `useWorkspace().currentWorkspaceId`
From frontend2/src/components/shortcuts/useShortcuts: `useShortcuts(id, Shortcut[])` — `Shortcut = {key,label,action,danger?}`; MEMOIZE bindings (Pitfall 3).
react-router v7: `useSearchParams`, `useNavigate`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useItemsQuery + useItemMutations (URL-driven, Phase 6 keys)</name>
  <files>frontend2/src/features/items/hooks/useItemsQuery.ts, frontend2/src/features/items/hooks/useItemMutations.ts</files>
  <read_first>frontend2/docs/sse-invalidation-contract.md, frontend2/src/features/dashboard/DashboardPage.tsx</read_first>
  <behavior>
    - useItemsQuery reads useSearchParams → {q, category, archived, sort, sort_dir, page} → queryKey `["items", wsId, params]` (Phase 6 contract — prefix-match invalidation; NEVER exact:true). enabled:!!wsId. limit fixed 25.
    - archived param: only sent when the Archived facet is on; default query excludes archived.
    - useItemMutations: archive/restore/delete each call itemsApi then `invalidateQueries({queryKey:["items", wsId]})` (prefix covers list + detail per the contract). Optimistic where cheap; revert on error with retroToast.error.
    - delete mutation refuses a non-archived id (guard before the call) — UI also blocks it, but the hook is defensive.
  </behavior>
  <action>Create `useItemsQuery.ts` (URL→params→TanStack query, key `["items", wsId, params]` per the SSE contract — assert in test the key prefix is exactly `["items", wsId]`). Create `useItemMutations.ts` with archive/restore/del mutations invalidating `["items", wsId]` (default prefix-match, NO exact:true — Anti-pattern). Test both with MSW + a QueryClient harness.</action>
  <verify>
    <automated>cd frontend2 && bun run test src/features/items/hooks/</automated>
  </verify>
  <done>query key is `["items", wsId, params]`; mutations invalidate the `["items", wsId]` prefix; archived defaults off.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ItemsListPage (table, filters, pagination, archived, shortcuts)</name>
  <files>frontend2/src/features/items/ItemsListPage.tsx, frontend2/src/features/items/ItemsListPage.test.tsx</files>
  <read_first>.planning/phases/07-items-photos/07-UI-SPEC.md (§1 Items List Page + Copywriting Contract), frontend2/src/routes/demo/DemoPage.tsx (useTableSelection + bulk-actions shortcut wiring example)</read_first>
  <behavior>
    - Renders a mint `Window` titled `ITEMS — {workspace}`; FilterBar (search→?q, CATEGORY▾ + ARCHIVED▾ facets→?category/?archived); SavedFilters row above FilterBar; RetroTable with the sketch-008 column set (select, thumb 26×26, Name, Category, Location, Qty mono, Status pill, actions); RetroPagination meta `page {n} of {m} · 25 / page`.
    - URL-driven: changing a filter resets ?page=1; sort headers toggle ?sort/?sort_dir with ↑/↓ glyph; deep-link (?q=foo&page=2) restores on mount.
    - Row click → /items/{id} (checkbox + action cells stopPropagation).
    - Archived hidden by default; ARCHIVED facet reveals; archived rows dim + ARCHIVED badge + EDIT→RESTORE; ↧ menu exposes DELETE… (type-to-confirm via RetroConfirmDialog — confirm disabled until item name typed; ITEM-06).
    - Bulk: useTableSelection drives selection; when selected.size>0 register a "bulk-actions" shortcut group (ARCHIVE/DELETE) so desktop surfaces Bottombar chips (no second desktop bar); mobile renders BulkActionBar. DELETE blocked (disabled + hint toast) unless the whole selection is archived.
    - CSV export button → photosApi.exportCsv(wsId).
    - useShortcuts("items", memoized [N→/items/new, /→focus search, F→toggle Archived facet]). Empty states per UI-SPEC (no items / no matches / archived-hidden hint).
  </behavior>
  <action>Build `ItemsListPage.tsx` composing the shipped atoms exactly per UI-SPEC §1 — invent no atoms. Drive every filter/sort/page through `useSearchParams` (Pattern 1). Wire `useShortcuts("items", ...)` with a MEMOIZED bindings array. Use `useTableSelection` for bulk; register bulk actions into the shortcuts SSOT only while a selection exists; mobile path renders `BulkActionBar`. Gate DELETE on all-archived. Wire `exportCsv`. Use the exact Copywriting Contract strings, wrapped in lingui `<Trans>`/`t` like DashboardPage. Write `ItemsListPage.test.tsx` (MSW): renders 25-row envelope; search input updates ?q; ARCHIVED facet toggles ?archived and reveals archived rows; sort header sets ?sort; deep-link ?page=2 restores; type-to-confirm delete enabled only after exact name; useShortcuts registers N//F.</action>
  <verify>
    <automated>cd frontend2 && bun run test src/features/items/ItemsListPage.test.tsx</automated>
  </verify>
  <done>list paginates/filters/sorts from URL; archived hidden→revealed; bulk archive/delete with archived-only delete guard; CSV export wired; N//F shortcuts registered.</done>
</task>

<task type="auto">
  <name>Task 3: Register /items route + enable Sidebar Items entry</name>
  <files>frontend2/src/routes/index.tsx, frontend2/src/components/layout/Sidebar.tsx</files>
  <read_first>frontend2/src/routes/index.tsx, frontend2/src/components/layout/Sidebar.tsx</read_first>
  <action>In `routes/index.tsx` add `<Route path="items" element={<ItemsListPage />} />` under the authenticated AppShell branch (literal route, before the wildcard — AP-1 library mode). Import ItemsListPage. In `Sidebar.tsx` give the INVENTORY-group Items `NavItem` a `to="/items"` prop so it renders as a live NavLink (it currently renders disabled with no `to`). Leave the `count={stats?.total_items}` binding intact. Do NOT register `/items/new`, `/items/:id`, `/items/:id/edit` here — those land with Plans 05/06 (this avoids a route-file conflict; those plans append their own routes in Wave 3).</action>
  <verify>
    <automated>cd frontend2 && bun run test src/features/items/ItemsListPage.test.tsx && tsc -b --noEmit</automated>
  </verify>
  <done>/items resolves to ItemsListPage under AppShell; Sidebar Items entry is an active NavLink to /items.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query params → list query | deep-link params are user-controllable input |
| selection → bulk delete | destructive op gated client-side, enforced server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Tampering | bulk DELETE on non-archived selection | mitigate | DELETE disabled + hint toast unless every selected id is archived; per-item delete also type-to-confirm (ITEM-06). Backend still authoritative. |
| T-07-08 | Information Disclosure | list query crossing workspaces | accept (backend authoritative) | key + path both carry wsId from useWorkspace; backend scopes by workspace_id. |
| T-07-09 | DoS | unbounded page param from deep link | accept | limit is fixed 25 client-side; backend clamps page/limit; an out-of-range page returns an empty envelope rendered as the filtered-empty state. |
</threat_model>

<verification>
- `cd frontend2 && bun run test src/features/items/` green.
- `cd frontend2 && tsc -b --noEmit` clean; `bun run lint:imports` OK.
</verification>

<success_criteria>
- ITEM-01: paginated 25/page list with search + category + archived chips + sort headers + URL deep-linking.
- ITEM-05: archived hidden by default, revealed by facet.
- ITEM-06: delete only on archived items, type-to-confirm.
- ITEM-10: useShortcuts("items", [N,/,F]) registered with the input-focus guard respected.
</success_criteria>

<output>
Create `.planning/phases/07-items-photos/07-03-SUMMARY.md` when done.
</output>
