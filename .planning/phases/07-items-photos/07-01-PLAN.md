---
phase: 07-items-photos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend2/src/lib/api.ts
  - frontend2/src/lib/types.ts
  - frontend2/src/lib/api.test.ts
  - frontend2/src/lib/api/url.ts
  - frontend2/src/lib/api/url.test.ts
  - frontend2/src/lib/api/items.ts
  - frontend2/src/lib/api/items.test.ts
  - frontend2/src/lib/api/photos.ts
  - frontend2/src/lib/api/photos.test.ts
  - frontend2/src/lib/api/loans.ts
  - frontend2/src/lib/api/labels.ts
  - frontend2/src/test/msw/handlers.ts
autonomous: true
requirements: [ITEM-09]
must_haves:
  truths:
    - "lookupByBarcode(wsId, code) returns the Item on 200 and null on 404"
    - "Absolute backend photo URLs are rewritten to /api-relative before any <img> consumer sees them"
    - "api.ts gains put + downloadBlob without regressing the cookie-JWT / single-flight-refresh / FormData-header invariants"
  artifacts:
    - path: "frontend2/src/lib/api/items.ts"
      provides: "itemsApi CRUD + archive/restore/delete + labels + lookupByBarcode"
      contains: "lookupByBarcode"
    - path: "frontend2/src/lib/api/photos.ts"
      provides: "photosApi list/upload/primary/caption/reorder/delete/bulk/zip/dup + URL rewrite"
      contains: "toProxyUrl"
    - path: "frontend2/src/lib/api/url.ts"
      provides: "toProxyUrl absolute→/api-relative rewrite"
      exports: ["toProxyUrl"]
    - path: "frontend2/src/lib/api.ts"
      provides: "put + downloadBlob helpers"
      contains: "export function put"
  key_links:
    - from: "frontend2/src/lib/api/items.ts"
      to: "/api/workspaces/{wsId}/items/by-barcode/{code}"
      via: "get<Item> with encodeURIComponent + 404→null"
      pattern: "by-barcode/\\$\\{encodeURIComponent"
    - from: "frontend2/src/lib/api/photos.ts"
      to: "toProxyUrl"
      via: "mapper rewrites url/thumbnail_url"
      pattern: "toProxyUrl\\("
---

<objective>
Build the typed API layer for items + photos over the existing `api.ts`, plus the two
load-bearing primitives every downstream plan depends on: the `put`/`downloadBlob`
additions to the locked `api.ts`, and the absolute→`/api`-relative photo URL rewrite.
This is the Wave 0 critical path — list, detail, forms, and the photo pipeline all
import from here.

Purpose: A single typed boundary (`itemsApi`/`photosApi`/`loansApi`/`labelsApi`) so feature
plans never hand-roll fetch calls, never see absolute backend URLs, and re-add the
`lookupByBarcode` helper (ITEM-09) per the locked Phase 65 pattern without touching the
green backend integration test.
Output: `lib/api/{items,photos,loans,labels,url}.ts` + tests, additive `api.ts` helpers,
Item/Photo/Loan/Label types, MSW handlers for all item/photo/loan/label/by-barcode routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-items-photos/07-RESEARCH.md
@.planning/phases/07-items-photos/07-VALIDATION.md
@frontend2/docs/sse-invalidation-contract.md

<interfaces>
<!-- Locked api.ts surface — EXTEND only, never regress the invariants in its header comment. -->
From frontend2/src/lib/api.ts:
```typescript
export class HttpError extends Error { constructor(status: number, message: string); status: number }
export function get<T>(endpoint: string): Promise<T>;
export function post<T>(endpoint: string, data?: unknown): Promise<T>;
export function postMultipart<T>(endpoint: string, form: FormData): Promise<T>;
export function patch<T>(endpoint: string, data: unknown): Promise<T>;
export function del<T = void>(endpoint: string): Promise<T>;
// MISSING (this plan adds): put<T>(endpoint, data), downloadBlob(endpoint, filename)
// BASE_URL = "/api"; every request carries credentials:"include"; FormData omits Content-Type.
```

Backend contracts (VERIFIED from source — paths are relative to /api/workspaces/{wsId}):
- GET    /items?search=&category_id=&archived=&sort=&sort_dir=&page=&limit=  → { items: ItemResponse[], total, page, total_pages, $schema? }
- GET    /items/by-barcode/{code}                                            → ItemResponse | 404
- GET    /items/{id}                                                         → ItemResponse
- POST   /items            (body: {sku, name, ...})                          → ItemResponse
- PATCH  /items/{id}       (omitted=unchanged, ""=clear string fields)       → ItemResponse
- POST   /items/{id}/archive                                                 → 204
- POST   /items/{id}/restore                                                 → 204
- DELETE /items/{id}                                                         → 204
- GET    /items/{id}/labels                                                  → { label_ids: string[] }
- POST   /items/{id}/labels/{label_id}                                       → 204
- DELETE /items/{id}/labels/{label_id}                                       → 204
- GET    /labels                                                             → { items: LabelResponse[] }
- GET    /items/{item_id}/photos/list                                        → PhotoResponse[]  (verify envelope shape live)
- GET    /photos/{id}                                                        → PhotoResponse
- PUT    /photos/{id}/primary                                               → 204
- PUT    /photos/{id}/caption  (body: {caption})                            → PhotoResponse
- PUT    /items/{item_id}/photos/order  (body: {photo_ids: string[]} — ALL ids required, else 400) → 204
- DELETE /photos/{id}                                                       → 204
- POST   /items/{item_id}/photos             (multipart, field "photo", optional "caption") → PhotoResponse
- POST   /items/{item_id}/photos/check-duplicate (multipart, field "photo")  → { has_duplicates, duplicates: DuplicateInfo[] }
- POST   /items/{item_id}/photos/bulk-delete  (body: {photo_ids})            → 204
- POST   /items/{item_id}/photos/bulk-caption (body: {updates: {photo_id, caption}[]}) → 204
- GET    /items/{item_id}/photos/download[?ids=uuid1,uuid2]                  → application/zip (blob)
- GET    /items/{item_id}/loans                                             → { items: LoanResponse[] }  (partition client-side on is_active)
- GET    /export/item?format=csv                                           → text/csv (blob)

ItemResponse key fields: id, workspace_id, sku, name, description?, category_id?, brand?, model?,
  serial_number?, manufacturer?, barcode?, is_insured?, is_archived?, min_stock_level, short_code,
  primary_photo_thumbnail_url? (ABSOLUTE), primary_photo_url? (ABSOLUTE), created_at, updated_at.
PhotoResponse: id, item_id, workspace_id, filename, file_size, mime_type, width, height,
  display_order, is_primary, caption?, url (ABSOLUTE), thumbnail_url (ABSOLUTE), thumbnail_status, created_at, updated_at.
DuplicateInfo: photo_id, item_id, filename, similarity_pct, thumbnail_url? (ABSOLUTE).
LabelResponse: id, workspace_id, name, color?, description?.
LoanResponse: has is_active, is_overdue, returned_at, borrower fields, due date (verify exact field names live before forms — read loan/handler.go).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Additive api.ts helpers + URL rewrite + types</name>
  <files>frontend2/src/lib/api.ts, frontend2/src/lib/api.test.ts, frontend2/src/lib/api/url.ts, frontend2/src/lib/api/url.test.ts, frontend2/src/lib/types.ts</files>
  <read_first>frontend2/src/lib/api.ts (locked invariants in header), frontend2/src/lib/types.ts</read_first>
  <behavior>
    - api.ts put&lt;T&gt;: PATCH-mirror but method PUT; JSON.stringify body; carries credentials:"include"; goes through the same 401 single-flight refresh path as request().
    - api.ts downloadBlob(endpoint, filename): fetch `/api{endpoint}` with credentials:"include"; on !ok throw HttpError(status); else blob → object URL → anchor click → revokeObjectURL.
    - INVARIANT-PRESERVATION (test the locked behaviors still hold): put with a FormData body would NOT happen (put is JSON-only); get/post/patch/del/postMultipart signatures unchanged; HttpError still thrown with status on non-ok.
    - toProxyUrl(absolute): `new URL(absolute)` → return `/api${u.pathname}${u.search}`; on parse failure (already relative) return input unchanged. Open-redirect guard: only the path+search are kept — scheme+host are DROPPED, so an attacker-controlled host can never be reached; the result always begins `/api/`.
  </behavior>
  <action>Add `export function put<T>(endpoint, data)` to api.ts mirroring `patch` (method "PUT"); add `export async function downloadBlob(endpoint, filename)` per RESEARCH Pattern 4 (credentials:"include", HttpError on non-ok, object-URL anchor click, revoke). Do NOT alter the existing request()/refresh/FormData logic — append only. Create `lib/api/url.ts` exporting `toProxyUrl` per RESEARCH Pattern 2 (drop scheme+host, keep path+search, prefix `/api`; catch→return input). In `types.ts` add exported interfaces `Item`, `Photo`, `Loan`, `Label`, `ItemListResponse`, `DuplicateInfo`, and a `DuplicateCheckResult` per the Interfaces block above — type only the fields read by this phase (Pitfall 7: do NOT assert exact key sets; ignore `$schema`). Write `api.test.ts` invariant-preservation cases and `url.test.ts` rewrite + open-redirect-guard cases.</action>
  <verify>
    <automated>cd frontend2 && bun run test src/lib/api.test.ts src/lib/api/url.test.ts</automated>
  </verify>
  <done>put + downloadBlob exported; api.test.ts proves locked invariants intact; toProxyUrl rewrites absolute localhost:8080 URLs to /api-relative and drops any foreign host; types compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: itemsApi (CRUD + archive/restore/delete + labels + lookupByBarcode)</name>
  <files>frontend2/src/lib/api/items.ts, frontend2/src/lib/api/items.test.ts, frontend2/src/lib/api/labels.ts, frontend2/src/lib/api/loans.ts</files>
  <read_first>frontend2/src/lib/api.ts (after Task 1), STATE.md Phase 65 lookupByBarcode history, backend/internal/domain/warehouse/loan/handler.go (LoanResponse field names)</read_first>
  <behavior>
    - lookupByBarcode(wsId, code): `get<Item>(/workspaces/${wsId}/items/by-barcode/${encodeURIComponent(code)})`; catch HttpError status===404 → return null; rethrow all other errors (so a 500 surfaces). NO client-side case normalization (server is case-sensitive authority — Phase 65 D-07).
    - list(wsId, params): builds query string from {search, category_id, archived, sort, sort_dir, page, limit}; omits empty params; returns ItemListResponse. Each item's primary_photo_thumbnail_url / primary_photo_url is rewritten via toProxyUrl when present.
    - create/update/get pass through; update is PATCH (omitted=unchanged, ""=clear per Pitfall 4 — the mapper does NOT inject defaults).
    - archive/restore/delete → POST .../archive, POST .../restore, del .../{id}.
    - labelsApi: getItemLabelIds, attach(itemId,labelId), detach(itemId,labelId), listWorkspaceLabels.
    - loansApi.byItem(wsId, itemId): GET /items/{itemId}/loans → { items }; helper partitions to {active: is_active===true, history: rest}.
  </behavior>
  <action>Create `itemsApi` in `lib/api/items.ts` with list/get/create/update/archive/restore/del/lookupByBarcode, importing get/post/patch/del/put + HttpError from `@/lib/api` and `toProxyUrl` from `@/lib/api/url`. Encode by-barcode with encodeURIComponent (path-injection guard, T below). Create `labelsApi` in `labels.ts` and `loansApi` in `loans.ts` (read-only; partition helper on is_active). Write `items.test.ts` mocking `global.fetch` (mirror the Phase 65 canonical fetch-mock pattern): URL shape `/items/by-barcode/...` NOT `?search=`, encodeURIComponent defense (`AB/CD 12`→`AB%2FCD%2012`), 200→Item, 404→null, 500→throws, primary_photo URL rewritten to `/api/...`.</action>
  <verify>
    <automated>cd frontend2 && bun run test src/lib/api/items.test.ts</automated>
  </verify>
  <done>lookupByBarcode 404→null + encodeURIComponent + 500-propagation proven; list rewrites primary-photo URLs; labels/loans helpers compile and export.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: photosApi (multipart + JSON ops + bulk + blob) + MSW handlers</name>
  <files>frontend2/src/lib/api/photos.ts, frontend2/src/lib/api/photos.test.ts, frontend2/src/test/msw/handlers.ts</files>
  <read_first>frontend2/src/lib/api/url.ts (Task 1), frontend2/src/test/msw/handlers.ts (append pattern), backend/internal/domain/warehouse/itemphoto/handler.go (route shapes)</read_first>
  <behavior>
    - list(wsId, itemId): GET /items/{itemId}/photos/list → PhotoResponse[]; mapper rewrites url + thumbnail_url via toProxyUrl on EVERY photo.
    - upload(wsId, itemId, file, caption?): FormData append "photo"=file (+ optional "caption"); postMultipart → PhotoResponse (url-rewritten). Field name MUST be "photo".
    - checkDuplicate(wsId, itemId, file): FormData "photo"=file; post → {has_duplicates, duplicates[]} with each duplicate.thumbnail_url rewritten.
    - setPrimary(put .../photos/{id}/primary), updateCaption(put .../caption), reorder(put .../photos/order body {photo_ids} — ALL ids), del(del /photos/{id}), bulkDelete(post bulk-delete {photo_ids}), bulkCaption(post bulk-caption {updates}).
    - downloadZip(wsId, itemId, ids?): downloadBlob(.../photos/download[?ids=...], `photos-${itemId}.zip`).
    - exportCsv(wsId): downloadBlob(/workspaces/${wsId}/export/item?format=csv, "item_export.csv").
  </behavior>
  <action>Create `photosApi` in `lib/api/photos.ts` covering every photo route in the Interfaces block, rewriting all absolute URLs via `toProxyUrl` at the mapper boundary (Pitfall 1). Upload + check-duplicate use FormData field name `photo`. Reorder sends the full ordered id list. Append MSW handlers to `src/test/msw/handlers.ts` for `/items`, `/items/:id`, `/items/by-barcode/:code`, `/items/:id/photos/list`, `/items/:id/loans`, `/labels`, `/items/:id/labels` returning contract-shaped JSON (include an ABSOLUTE `url`/`thumbnail_url` in photo fixtures so consumer tests exercise the rewrite). Write `photos.test.ts` proving: upload sends field `photo`; list rewrites every url+thumbnail_url to `/api/...`; reorder PUTs `{photo_ids}`; bulk shapes correct.</action>
  <verify>
    <automated>cd frontend2 && bun run test src/lib/api/photos.test.ts && bun run test src/test/msw</automated>
  </verify>
  <done>photosApi exports all ops; every absolute URL rewritten at boundary; FormData field `photo` asserted; MSW handlers serve item/photo/loan/label/by-barcode contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api (Vite proxy → :8080) | All item/photo requests; cookie-JWT crosses here |
| backend-emitted absolute URLs → `<img>` | Untrusted host string from PhotoResponse.url must be neutralized before render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | toProxyUrl rewrite | mitigate | Only `u.pathname + u.search` kept; scheme+host dropped → result always `/api/...`; a malicious BackendURL host can never be reached (open-redirect closed). url.test.ts asserts foreign-host input yields `/api`-prefixed output. |
| T-07-02 | Information Disclosure | by-barcode path param | mitigate | `encodeURIComponent(code)` before interpolation (path-injection guard); items.test.ts asserts `AB/CD 12`→`AB%2FCD%2012`. |
| T-07-03 | Information Disclosure | cross-tenant item/photo read | accept (backend authoritative) | Every path is `/workspaces/{wsId}/...`; backend `WHERE workspace_id=$1` is source of truth (G-65-01 integration test guards it — NOT touched by this plan). |
| T-07-04 | Spoofing | session on every request | mitigate | put/downloadBlob both carry `credentials:"include"` (cookie-JWT); never a token in a URL; api.test.ts invariant cases assert no Content-Type/auth regressions. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new installs this phase (RHF/zod confirmed present); slopcheck N/A. |
</threat_model>

<verification>
- `cd frontend2 && bun run test src/lib/` green.
- `cd frontend2 && bun run lint:tsc` (or `tsc -b --noEmit`) clean — new types compile.
- Backend `-tags=integration` item suite UNCHANGED and still green (no backend files touched).
</verification>

<success_criteria>
- ITEM-09 helper re-added per locked pattern (404→null, encodeURIComponent, case-sensitive, 500-propagation) with fetch-mock tests.
- api.ts put + downloadBlob added without regressing the locked invariants (proven by api.test.ts).
- Every absolute backend photo URL is rewritten to `/api`-relative at the mapper boundary.
- MSW handlers cover all item/photo/loan/label routes for downstream unit tests.
</success_criteria>

<output>
Create `.planning/phases/07-items-photos/07-01-SUMMARY.md` when done.
</output>
