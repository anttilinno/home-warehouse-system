---
phase: 10-taxonomy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend2/src/lib/api/category.ts
  - frontend2/src/lib/api/location.ts
  - frontend2/src/lib/api/container.ts
  - frontend2/src/lib/api/labels.ts
  - frontend2/src/lib/types.ts
  - frontend2/src/features/taxonomy/lib/buildTree.ts
  - frontend2/src/features/taxonomy/lib/buildTree.test.ts
  - frontend2/src/features/taxonomy/lib/safeSessionStorage.ts
  - frontend2/src/features/taxonomy/schema.ts
  - frontend2/src/features/taxonomy/schema.test.ts
  - frontend2/src/components/retro/data/RetroTree.tsx
  - frontend2/src/components/retro/data/RetroTree.test.tsx
  - frontend2/src/components/retro/data/index.ts
  - frontend2/src/test/msw/handlers.ts
autonomous: true
requirements: [TAX-01, TAX-02, TAX-03, TAX-04, TAX-05, TAX-06, TAX-07]
user_setup: []

must_haves:
  truths:
    - "buildTree nests flat rows by parent id and surfaces orphans at root"
    - "buildTree works for categories (parent_category_id) AND locations (parent_location)"
    - "RetroTree renders nested rows, toggles expand/collapse, and persists the expanded set to sessionStorage per storageKey"
    - "RetroTree is keyboard-navigable (role=tree/treeitem, arrow keys, aria-expanded)"
    - "category/location/container api modules expose list/get/create/update/archive/restore/del with correct envelopes; labels.ts is extended with full CRUD"
    - "MSW serves all four taxonomy domains + /items?category_id= + /inventory?container_id= with correct per-endpoint envelopes"
  artifacts:
    - path: "frontend2/src/features/taxonomy/lib/buildTree.ts"
      provides: "Generic flat→nested tree builder with depth + alpha sort"
      contains: "export function buildTree"
    - path: "frontend2/src/components/retro/data/RetroTree.tsx"
      provides: "Recursive tree atom with sessionStorage expand persistence + a11y"
      min_lines: 80
    - path: "frontend2/src/lib/api/category.ts"
      provides: "categoryApi with BARE {items} list"
      contains: "export const categoryApi"
    - path: "frontend2/src/lib/api/location.ts"
      provides: "locationApi with paginated list + /search + parent_location field"
      contains: "export const locationApi"
    - path: "frontend2/src/lib/api/container.ts"
      provides: "containerApi with paginated list + /search + location_id"
      contains: "export const containerApi"
    - path: "frontend2/src/lib/api/labels.ts"
      provides: "labelsApi extended with get/create/update/archive/restore/del"
      contains: "create:"
    - path: "frontend2/src/test/msw/handlers.ts"
      provides: "MSW handlers for taxonomy domains"
      contains: "/api/workspaces/:wsId/categories"
  key_links:
    - from: "frontend2/src/features/taxonomy/lib/buildTree.ts"
      to: "category.parent_category_id / location.parent_location"
      via: "parentIdOf accessor callback"
      pattern: "parentIdOf"
    - from: "frontend2/src/components/retro/data/RetroTree.tsx"
      to: "sessionStorage key taxonomy:tree:<tab>"
      via: "safeSessionStorage load/save of expanded id set"
      pattern: "taxonomy:tree"
---

<objective>
Build the Wave-0 foundation for the entire Taxonomy phase: the four api modules
(category/location/container net-new + labels.ts extended), the shared `buildTree`
util, the net-new recursive `RetroTree` atom with sessionStorage expand persistence,
the zod schemas, and the MSW handlers that every downstream tab test depends on.

Purpose: Downstream plans (page shell, the four tabs, E2E) all import these. They
must be correct and well-tested FIRST so tab plans have stable, typed dependencies.
Output: api modules, types, buildTree + test, RetroTree + test, schemas + test,
safeSessionStorage helper, MSW taxonomy handlers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-taxonomy/10-RESEARCH.md
@.planning/phases/10-taxonomy/10-UI-SPEC.md
@.planning/phases/10-taxonomy/10-VALIDATION.md

# Parity sources to mirror VERBATIM (read before writing):
@frontend2/src/lib/api/borrowers.ts
@frontend2/src/lib/api/labels.ts
@frontend2/src/components/retro/data/RetroTabs.tsx

<interfaces>
<!-- Backend contract (verified 2026-06-13, all under /workspaces/{wsId}). Envelope is load-bearing. -->
categories: GET /categories → BARE {items}; body {name, parent_category_id?, description?}; parent field = parent_category_id. NO item_count.
locations:  GET /locations?page&limit → PAGINATED {items,total,page,total_pages}; GET /locations/search?q=&limit → BARE {items}; body {name, parent_location?, description?, short_code?}; parent field = parent_location (NOT _id). has short_code.
containers: GET /containers?page&limit → PAGINATED {items,total,page,total_pages}; GET /containers/search?q=&limit → BARE {items}; body {name, location_id, description?, capacity?, short_code?}; flat (location_id). has short_code, capacity.
labels:     GET /labels → BARE {items}; body {name, color?, description?}; color *string hex ^#[0-9A-Fa-f]{6}$.
Lifecycle (all 4): POST /{domain}, GET /{id}, PATCH /{id}, POST /{id}/archive (204), POST /{id}/restore, DELETE /{id} (204).
Usage-count reads: GET /items?category_id={id}&limit=1 → PAGINATED, read .total. GET /inventory?container_id={id}&limit=1 → PAGINATED, read .total.

Existing Label type (lib/types.ts:182-189): {id, workspace_id, name, color?, description?} — ADD is_archived, created_at, updated_at.
HttpError (lib/api.ts:24): err instanceof HttpError && err.status === N.
api primitives: get/post/patch/del from "@/lib/api".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: api modules (category/location/container net-new, labels extended) + types</name>
  <files>frontend2/src/lib/api/category.ts, frontend2/src/lib/api/location.ts, frontend2/src/lib/api/container.ts, frontend2/src/lib/api/labels.ts, frontend2/src/lib/types.ts</files>
  <behavior>
    - category.ts list() returns {items: Category[]} (BARE — reading .total is a TYPE error)
    - location.ts list() returns {items, total, page, total_pages}; location.ts search() returns Location[] (BARE .items)
    - location.ts Location interface has `parent_location?: string` (NOT parent_location_id)
    - container.ts list() paginated; search() bare; Container has location_id, capacity?, short_code
    - all list/search reads clamp limit via Math.min(limit, 100)
    - labels.ts gains get/create/update/archive/restore/del WITHOUT removing existing getItemLabelIds/attach/detach/listWorkspaceLabels
    - Label type in lib/types.ts gains is_archived, created_at, updated_at
  </behavior>
  <action>
    Mirror lib/api/borrowers.ts verbatim for structure (MAX_LIMIT=100 const, clamped reads,
    typed BARE-vs-paginated returns, header comment citing handler.go envelopes).
    Create category.ts, location.ts, container.ts per the Backend contract in <interfaces>.
    CRITICAL field names: categories use parent_category_id; locations use parent_location (NO _id) —
    type Location.parent_location so a typo is a compile error (Pitfall 6).
    Type list returns precisely per envelope: categories list = {items} only; locations/containers
    list = {items,total,page,total_pages}; ALL /search = {items} then .then(r=>r.items) (Pitfall 2).
    location.search and container.search both encodeURIComponent(q) and clamp limit.
    Each api object exposes: list, get, create, update, archive(→post void), restore(→post void),
    del(→del void). For locations/containers list, accept (ws, page=1, limit=100).
    EXTEND labels.ts: append to the existing labelsApi object (do NOT rewrite the read/attach/detach
    helpers) — add CreateLabelBody {name; color?; description?}, UpdateLabelBody = Partial<…>, and
    get/create/update/archive/restore/del per RESEARCH OQ3 snippet. Color is hex string `^#[0-9A-Fa-f]{6}$`.
    In lib/types.ts, add is_archived: boolean; created_at: string; updated_at: string to the Label interface.
    Add a header comment to each new module citing the Pitfall-2 envelope discipline.
  </action>
  <verify>
    <automated>cd frontend2 && bunx tsc --noEmit 2>&1 | grep -E 'lib/api/(category|location|container|labels)|lib/types' ; test $? -eq 1</automated>
  </verify>
  <done>tsc clean for the api modules; category list typed {items} (no total), location.parent_location present, labels.ts retains existing helpers + adds CRUD, Label type has is_archived/created_at/updated_at.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: buildTree util + safeSessionStorage + zod schemas (with tests)</name>
  <files>frontend2/src/features/taxonomy/lib/buildTree.ts, frontend2/src/features/taxonomy/lib/buildTree.test.ts, frontend2/src/features/taxonomy/lib/safeSessionStorage.ts, frontend2/src/features/taxonomy/schema.ts, frontend2/src/features/taxonomy/schema.test.ts</files>
  <behavior>
    - buildTree(rows, parentIdOf) nests by parent id, sets depth, alpha-sorts each level by name
    - a row whose parent id is missing from the set surfaces at ROOT (orphan, Pitfall 7) — NOT dropped
    - works with categories case (c => c.parent_category_id) AND locations case (l => l.parent_location)
    - empty input → []
    - schema: categorySchema/locationSchema/containerSchema/labelSchema mirror server constraints
    - labelSchema.color regex = /^#[0-9A-Fa-f]{6}$/ (or empty/undefined when no-color)
    - location/container short_code (if exposed in form) regex ^[A-Za-z0-9]{4,8}$; container location_id required
    - safeSessionStorage.get/set wrap try/catch (private mode / quota) and JSON-(de)serialize string[]
  </behavior>
  <action>
    Port the RESEARCH buildTree snippet verbatim into buildTree.ts (TreeNode<T> {node, children, depth};
    Map-based two-pass; orphan→roots; walk() alpha-sort + depth). Generic over parentIdOf so the SAME
    util serves categories (parent_category_id) and locations (parent_location).
    Write buildTree.test.ts covering: 2-level nest, 3-level nest, orphan-at-root (parent id not in set),
    alpha sort per level, empty input, BOTH parent-field accessors.
    Create safeSessionStorage.ts: a tiny try/catch wrapper — getSet(key): string[] (parse JSON, [] on
    failure) and saveSet(key, ids: string[]). NO sessionStorage precedent exists in frontend2, so this is
    the canonical helper RetroTree consumes.
    Create schema.ts mirroring features/borrowers/schema.ts idiom (zod, lingui-friendly messages):
    categorySchema/locationSchema/containerSchema/labelSchema. name 1..255. color optional regex per server.
    parent fields optional (empty = root). containerSchema.location_id required (min 1). short_code optional.
    schema.test.ts: assert color regex accepts #b73348 and rejects #fff / red / #GGGGGG; name length bounds;
    container requires location_id.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test -- src/features/taxonomy/lib/buildTree.test.ts src/features/taxonomy/schema.test.ts</automated>
  </verify>
  <done>buildTree + schema tests green; orphan-at-root and both parent-field cases proven; color regex matches the server pattern exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RetroTree atom (recursive, sessionStorage expand, a11y) + thorough test</name>
  <files>frontend2/src/components/retro/data/RetroTree.tsx, frontend2/src/components/retro/data/RetroTree.test.tsx, frontend2/src/components/retro/data/index.ts</files>
  <behavior>
    - renders nested rows from TreeNode-shaped input; indentation by depth (INDENT_PX=20)
    - branch rows show ▾ (expanded) / ▸ (collapsed) focusable caret button with aria-expanded; leaves show · (non-interactive)
    - clicking caret OR row toggles expand; collapsed subtree rows are NOT in the DOM
    - expanded set loads from sessionStorage[storageKey] on mount and saves on every toggle
    - two RetroTrees with different storageKeys keep INDEPENDENT expand state
    - role=tree on container, role=treeitem + aria-level on rows, roving tabIndex; ↓/↑ move, → expand, ← collapse, Enter/Space toggle
    - item-count badge (RetroBadge neutral, mono "(N)") rendered when count>0, hidden at 0
    - archived row: text-fg-muted name + ARCHIVED RetroBadge + actions collapse to RESTORE
    - row actions EDIT / ⊕ add-child / ⌫ archive emitted via onEdit/onAddChild/onArchive; onRestore for archived; cluster stopPropagation
    - empty roots → renders emptyState prop
  </behavior>
  <action>
    Build RetroTree.tsx at components/retro/data/ (data family, alongside RetroTabs). Props per the
    UI-SPEC §NET-NEW RetroTreeProps: nodes (RetroTreeNode[] {id,name,itemCount,isArchived,children}),
    storageKey, onAddChild, onEdit, onArchive, onRestore, emptyState. Accept the buildTree TreeNode shape
    by mapping in the consumer OR define RetroTreeNode and let the tab adapt — pick RetroTreeNode (UI-SPEC
    contract) and document the adapter expectation.
    Expand state: const [expanded,setExpanded]=useState(()=>new Set(safeSessionStorage.getSet(storageKey)));
    persist in useEffect([expanded]) via saveSet(storageKey,[...expanded]). RENDER-LOOP guard: do NOT put
    non-stable values in deps; toggle via setExpanded(prev => …) functional updates.
    Indentation: depth * 20px (INDENT_PX const). 1px sand vertical guide (border-l border-[--border-sand])
    per level per UI-SPEC row anatomy. Caret: ▾/▸ focusable <button aria-expanded>; · leaf glyph (text-fg-faint).
    Count badge: RetroBadge variant="neutral" mono (N), hidden at 0. Archived: text-fg-muted + ARCHIVED badge +
    RESTORE action only (three-cue discipline). Row actions = compact BevelButtons (!px-[8px] !py-[2px] !text-[11px]),
    opacity-0 group-hover:opacity-100 group-focus-within:opacity-100; cluster onClick stopPropagation.
    A11y: role=tree/treeitem, aria-expanded on branches, aria-level, roving tabIndex (mirror RetroTabs
    tabIndex={isActive?0:-1} discipline); arrow-key handler ↑/↓/→/←/Enter/Space.
    Export RetroTree + RetroTreeNode + RetroTreeProps through data/index.ts → retro barrel.
    RetroTree.test.tsx (thorough — this is the central net-new atom): render+nest, expand/collapse toggles
    DOM presence, sessionStorage persists across remount, TWO storageKeys independent, keyboard nav (arrows
    + Enter), archived row shows ARCHIVED + RESTORE only, count hidden at 0, emptyState when no nodes,
    onEdit/onAddChild/onArchive fire with the right node, action cluster does not bubble a row-toggle.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test -- src/components/retro/data/RetroTree.test.tsx</automated>
  </verify>
  <done>RetroTree test green incl. sessionStorage persistence + two-key independence + keyboard nav + archived/RESTORE; exported through the data barrel.</done>
</task>

<task type="auto">
  <name>Task 4: MSW taxonomy handlers (single-writer addition to handlers.ts)</name>
  <files>frontend2/src/test/msw/handlers.ts</files>
  <action>
    Append taxonomy handlers to the existing handlers array (single-writer — only this plan touches
    handlers.ts this milestone). Mirror the existing borrowers/labels handler style.
    Add GET/POST/PATCH/archive/restore/DELETE for:
      /api/workspaces/:wsId/categories  → list returns BARE { items: [...] } with parent_category_id on rows
      /api/workspaces/:wsId/locations   → list returns PAGINATED { items, total, page, total_pages }, rows have parent_location
      /api/workspaces/:wsId/locations/search → BARE { items }
      /api/workspaces/:wsId/containers  → list PAGINATED, rows have location_id
      /api/workspaces/:wsId/containers/search → BARE { items }
      /api/workspaces/:wsId/labels      → extend existing GET to full CRUD (create/update/archive/restore/delete)
    Add usage-count reads (return PAGINATED with a `total`): handle the existing
      /api/workspaces/:wsId/items GET so ?category_id= returns { items:[], total: N } (configurable; default total 0)
      /api/workspaces/:wsId/inventory GET so ?container_id= returns { items:[], total: N }
    (If items/inventory list handlers already exist, extend them to honor the filter query param and surface a
    `total`; do NOT duplicate routes — specific-before-param ordering for /labels/:id etc.)
    archive/restore/delete return new HttpResponse(null,{status:204}); create returns 201 (categories) / 200 (others).
    Fixtures MUST use BARE {items} for categories/labels/search and {items,total,page,total_pages} for
    locations/containers (Pitfall 2) so tab tests exercise the real envelope split.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test 2>&1 | tail -20 ; bunx tsc --noEmit 2>&1 | grep -c 'handlers.ts' | grep -qx 0</automated>
  </verify>
  <done>handlers.ts compiles; existing full suite still green with new taxonomy + usage-count handlers; envelopes match the per-endpoint split.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→workspace-scoped API | every taxonomy read/write crosses `/workspaces/{wsId}` |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | cross-tenant taxonomy read | mitigate | every api call carries wsId from useWorkspace(); backend repos scope by workspace_id (verified prior phases) |
| T-10-02 | Tampering | label/category name rendered in tree | mitigate | React escapes by default; no dangerouslySetInnerHTML; color is regex-validated before use in style (schema.ts) |
| T-10-SC | Tampering | npm installs | accept | zero new installs (RESEARCH Package Legitimacy Gate: N/A) |
</threat_model>

<verification>
- tsc clean for all new files.
- buildTree.test.ts + schema.test.ts + RetroTree.test.tsx green.
- Full `bun run test` green after MSW additions (no regression).
</verification>

<success_criteria>
api modules + types + buildTree + RetroTree + schemas + safeSessionStorage + MSW handlers exist,
typecheck, and pass their tests. Downstream plans can import every symbol they need.
</success_criteria>

<output>
Create `.planning/phases/10-taxonomy/10-01-SUMMARY.md` when done (artifacts, exported symbols, MSW fixture shapes, any residues).
</output>
