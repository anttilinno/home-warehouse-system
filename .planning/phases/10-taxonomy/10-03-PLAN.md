---
phase: 10-taxonomy
plan: 03
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - frontend2/src/features/taxonomy/components/LocationsTab.tsx
  - frontend2/src/features/taxonomy/components/LocationsTab.test.tsx
  - frontend2/src/features/taxonomy/components/LocationFormDialog.tsx
  - frontend2/src/features/taxonomy/components/ContainersTab.tsx
  - frontend2/src/features/taxonomy/components/ContainersTab.test.tsx
  - frontend2/src/features/taxonomy/components/ContainerFormDialog.tsx
  - frontend2/src/features/taxonomy/components/SearchPicker.tsx
  - frontend2/src/features/taxonomy/hooks/useLocationsQuery.ts
  - frontend2/src/features/taxonomy/hooks/useLocationMutations.ts
  - frontend2/src/features/taxonomy/hooks/useLocationMutations.test.tsx
  - frontend2/src/features/taxonomy/hooks/useContainersQuery.ts
  - frontend2/src/features/taxonomy/hooks/useContainerMutations.ts
  - frontend2/src/features/taxonomy/hooks/useContainerMutations.test.tsx
  - frontend2/src/features/taxonomy/hooks/useTaxonomySearch.ts
autonomous: true
requirements: [TAX-03, TAX-04, TAX-05, TAX-06]
user_setup: []

must_haves:
  truths:
    - "Locations tab renders the location tree via RetroTree built from GET /locations?limit=100 (.items) keyed on parent_location (NOT _id)"
    - "Locations use ARCHIVE only (no delete button); archive flow mirrors categories"
    - "Containers tab renders containers GROUPED by location_id (client group-by), unresolved → (No location) group last"
    - "Deleting a container calls bare DELETE /containers/{id} and invalidates BOTH [containers, wsId] AND [inventory, wsId]"
    - "Container delete confirm warns 'will unassign from N items' (N from GET /inventory?container_id=&limit=1 total)"
    - "Container form Location field uses SearchPicker (RetroCombobox + /locations/search via useTaxonomySearch); shipped item/inventory/loan forms untouched"
  artifacts:
    - path: "frontend2/src/features/taxonomy/components/LocationsTab.tsx"
      provides: "location tree + archive CRUD (no delete)"
      min_lines: 50
    - path: "frontend2/src/features/taxonomy/components/ContainersTab.tsx"
      provides: "grouped-by-location list + delete-with-unassign"
      min_lines: 60
    - path: "frontend2/src/features/taxonomy/components/SearchPicker.tsx"
      provides: "RetroCombobox backed by /search (Taxonomy-only)"
      contains: "RetroCombobox"
    - path: "frontend2/src/features/taxonomy/hooks/useTaxonomySearch.ts"
      provides: "debounced /search query hook keyed [domain, wsId, search, q]"
      contains: "search"
    - path: "frontend2/src/features/taxonomy/hooks/useContainerMutations.ts"
      provides: "container mutations; del invalidates containers + inventory"
      contains: "[\"inventory\", wsId"
  key_links:
    - from: "frontend2/src/features/taxonomy/hooks/useLocationsQuery.ts"
      to: "buildTree(rows, l => l.parent_location)"
      via: "parent_location accessor (NOT _id — Pitfall 6)"
      pattern: "parent_location"
    - from: "frontend2/src/features/taxonomy/hooks/useContainerMutations.ts"
      to: "[containers, wsId] AND [inventory, wsId] caches"
      via: "del onSuccess double PREFIX-invalidate (FK SET NULL cascade)"
      pattern: "inventory"
    - from: "frontend2/src/features/taxonomy/components/SearchPicker.tsx"
      to: "GET /locations/search | /containers/search"
      via: "useTaxonomySearch(domain, q) → RetroComboboxOption[]"
      pattern: "/search"
---

<objective>
Implement the Locations tab (tree, ARCHIVE only) and the Containers tab
(grouped-by-location, DELETE with unassign-and-delete + dual cache invalidation),
plus the net-new SearchPicker (RetroCombobox + /search) used in the container form.
Slots into the W2 TaxonomyPage shell by replacing its locations/containers
placeholders. Runs in parallel with 10-04 (Labels) — disjoint files.

Purpose: TAX-03/04/05/06. Reuses the W1 RetroTree + buildTree (location case) and
the W2 useUsageCount (container case).
Output: Locations + Containers tabs + forms, SearchPicker, useTaxonomySearch, and
the four location/container query+mutation hooks.

CONSTRAINTS (do NOT violate): this plan MUST NOT edit routes/index.tsx (W2 single
writer), Sidebar.tsx (W2), or src/test/msw/handlers.ts (W1). Location/container
create+edit forms are INLINE RetroDialogs (no route) per the W2 form-routing decision.
Do NOT touch InventoryFormPage / MoveDialog / ItemFormPage / usePickerOptions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-taxonomy/10-RESEARCH.md
@.planning/phases/10-taxonomy/10-UI-SPEC.md
@.planning/phases/10-taxonomy/10-01-SUMMARY.md
@.planning/phases/10-taxonomy/10-02-SUMMARY.md

# Parity sources (read before writing):
@frontend2/src/components/retro/form/RetroCombobox.tsx
@frontend2/src/features/inventory/hooks/usePickerOptions.ts

<interfaces>
<!-- From W1/W2 (already shipped): -->
locationApi (lib/api/location.ts): list(ws,page,limit)→{items,total,page,total_pages}; search(ws,q,limit)→Location[]; get/create/update/archive/restore/del. Location.parent_location (NOT _id).
containerApi (lib/api/container.ts): list→paginated; search→Container[]; create/update/archive/restore/del. Container.location_id.
buildTree (features/taxonomy/lib/buildTree.ts): buildTree(rows, l=>l.parent_location).
RetroTree (components/retro): {nodes, storageKey, onAddChild, onEdit, onArchive, onRestore, emptyState}.
useUsageCount (features/taxonomy/hooks/useUsageCount.ts, from W2): fetchCount("container", id) → GET /inventory?container_id=&limit=1 .total.
locationSchema / containerSchema (features/taxonomy/schema.ts).
RetroCombobox (components/retro): {label, options:{value,label}[], value, onChange, error, disabled}.
RetroConfirmDialog, RetroDialog, RetroTable, RetroEmptyState (components/retro).
TaxonomyPage placeholders: data-testid="tab-locations-pending" / "tab-containers-pending" — replace by rendering LocationsTab / ContainersTab (W2 SUMMARY documents the swap point; if the page wires tabs by import, this plan edits ONLY the tab component files it owns — confirm the W2 wiring approach in 10-02-SUMMARY).
RENDER-LOOP guard: tRef; destructure .mutate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Locations tab (tree, ARCHIVE only) + LocationFormDialog + hooks</name>
  <files>frontend2/src/features/taxonomy/hooks/useLocationsQuery.ts, frontend2/src/features/taxonomy/hooks/useLocationMutations.ts, frontend2/src/features/taxonomy/hooks/useLocationMutations.test.tsx, frontend2/src/features/taxonomy/components/LocationsTab.tsx, frontend2/src/features/taxonomy/components/LocationsTab.test.tsx, frontend2/src/features/taxonomy/components/LocationFormDialog.tsx</files>
  <behavior>
    - useLocationsQuery keyed ["locations", wsId]; queryFn locationApi.list(ws,1,100).then(r=>r.items); tree=buildTree(rows, l=>l.parent_location)
    - useLocationMutations: create/update/archive/restore PREFIX-invalidate ["locations", wsId]; NO del exposed (locations archive-only, TAX-04)
    - LocationsTab renders RetroTree storageKey "taxonomy:tree:locations"; toolbar "⊕ ADD ROOT LOCATION"; add-child/edit/archive/restore
    - NO delete affordance anywhere for locations
    - archive confirm = plain butter "ARCHIVE LOCATION?" (locations have no client usage-count requirement — TAX-04 is archive only; keep the generic copy)
    - parent picker = RetroCombobox flattened locations excluding self+descendants, empty=root
    - empty/error states per UI-SPEC ("NO LOCATIONS YET" / "COULDN'T LOAD LOCATIONS")
  </behavior>
  <action>
    Mirror useCategoriesQuery/useCategoryMutations (W2) but for locations. CRITICAL: tree accessor is
    l => l.parent_location (NOT parent_location_id — Pitfall 6; the Location type makes a typo a compile error).
    List is PAGINATED so read .items from list(ws,1,100). Mutations PREFIX-invalidate ["locations", wsId];
    DO NOT expose del (TAX-04 archive-only; location delete is dangerous — CASCADE/RESTRICT). Toasts per UI-SPEC.
    LocationsTab mirrors CategoriesTab structure minus the usage-count fetch (archive is plain butter copy).
    storageKey "taxonomy:tree:locations". LocationFormDialog = INLINE RetroDialog (NOT a route) blue form,
    RHF+zod locationSchema, fields name / description / parent (RetroCombobox flattened, exclude self+descendants) /
    short_code optional. On submit create/update.mutate; dirty-guard butter discard.
    LocationsTab.test.tsx: tree nests via parent_location (assert a child renders under its parent); NO delete
    button present; archive uses plain copy + calls archive; empty/error states.
    useLocationMutations.test.tsx: create/update/archive/restore invalidate ["locations", wsId]; del is absent.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test -- src/features/taxonomy/components/LocationsTab.test.tsx src/features/taxonomy/hooks/useLocationMutations.test.tsx</automated>
  </verify>
  <done>Locations tab green: tree nests via parent_location, archive-only (no delete), PREFIX invalidation, empty/error states. Export name `LocationsTab` unchanged (TaxonomyPage imports it — single-writer, not re-edited in W3).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useTaxonomySearch + SearchPicker (RetroCombobox + /search, Taxonomy-only)</name>
  <files>frontend2/src/features/taxonomy/hooks/useTaxonomySearch.ts, frontend2/src/features/taxonomy/components/SearchPicker.tsx</files>
  <behavior>
    - useTaxonomySearch(domain:"locations"|"containers", query) debounces ~250ms, useQuery keyed [domain, wsId, "search", query]
    - calls locationApi.search / containerApi.search (limit clamped ≤100); returns RetroComboboxOption[] {value:id, label:name}
    - empty/short query → returns [] (or a sensible no-fetch state) without firing
    - SearchPicker composes RetroCombobox with the hook results; controlled value (selected id) + onChange(id); error + disabled passthrough
    - SearchPicker is imported ONLY by Taxonomy forms (never by shipped item/inventory/loan forms)
  </behavior>
  <action>
    useTaxonomySearch.ts: debounce the query string (250ms, a tiny useEffect+setTimeout or a small useDebounce),
    useQuery key [domain, wsId, "search", debouncedQuery] enabled !!wsId && debouncedQuery.length>0,
    queryFn → locationApi.search(ws, q) | containerApi.search(ws, q) (the api already returns the BARE .items),
    map → RetroComboboxOption[]. Keep keys PREFIXED by domain (Lock #4 so SSE/mutation invalidation covers them).
    SearchPicker.tsx per RESEARCH OQ4 SearchPickerProps: feed the hook results into RetroCombobox; track the
    typed query in local state; expose value/onChange for RHF Controller. Disabled + error passthrough.
    Add a header comment: "Taxonomy-only. Do NOT import into shipped item/inventory/loan forms (OQ4 RISK)."
  </action>
  <verify>
    <automated>cd frontend2 && bunx tsc --noEmit 2>&1 | grep -E 'SearchPicker|useTaxonomySearch' ; test $? -eq 1 && grep -rL 'SearchPicker' frontend2/src/features/inventory/InventoryFormPage.tsx >/dev/null</automated>
  </verify>
  <done>SearchPicker + useTaxonomySearch compile; debounced /search keyed [domain,wsId,"search",q]; NOT imported by shipped forms.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Containers tab (grouped-by-location) + ContainerFormDialog + delete-with-unassign</name>
  <files>frontend2/src/features/taxonomy/hooks/useContainersQuery.ts, frontend2/src/features/taxonomy/hooks/useContainerMutations.ts, frontend2/src/features/taxonomy/hooks/useContainerMutations.test.tsx, frontend2/src/features/taxonomy/components/ContainersTab.tsx, frontend2/src/features/taxonomy/components/ContainersTab.test.tsx, frontend2/src/features/taxonomy/components/ContainerFormDialog.tsx</files>
  <behavior>
    - useContainersQuery keyed ["containers", wsId]; list(ws,1,100).then(r=>r.items); also needs location names (reuse useLocationsQuery or a clamped locations read) to resolve group headers
    - ContainersTab groups containers by location_id (client group-by); groups sorted alpha by location name; unresolved/null location → "(No location)" group last
    - each group rendered as a RetroTable under a bg-bg-panel-2 group header; rows show name + count badge + EDIT + ⌫ DELETE
    - useContainerMutations: create/update PREFIX-invalidate ["containers", wsId]; del invalidates ["containers", wsId] AND ["inventory", wsId] (FK SET NULL cascade — OQ2)
    - delete confirm = pink dialog; BEFORE delete fetch count via useUsageCount("container", id); total>0 → "⚠ {name} holds {n} item(s)… will be unassigned…"; total=0 → plain pink; confirm → bare containerApi.del (NO ?force, NO 2nd call); 409 onError → conflict toast (defensive)
    - container form Location field = SearchPicker(domain="locations"), required; empty-source disabled hint
    - empty/error states ("NO CONTAINERS YET" / "COULDN'T LOAD CONTAINERS")
  </behavior>
  <action>
    useContainersQuery: key ["containers", wsId], list(ws,1,100).then(r=>r.items). For group headers, resolve
    location_id→name (consume useLocationsQuery rows or a clamped locationApi.list; do NOT fan out per row).
    ContainersTab: client group-by on location_id (Map), alpha-sort groups by resolved name, "(No location)"
    group sorts LAST (mirrors InventoryListPage unresolved-name discipline). Render each group = group header
    strip + a RetroTable of its containers. Row actions EDIT + ⌫ DELETE (containers use DELETE, not archive).
    useContainerMutations: mirror useCategoryMutations BUT del.onSuccess invalidates BOTH ["containers", wsId]
    AND ["inventory", wsId] (the FK SET NULLs inventory.container_id, so inventory caches must refetch — OQ2);
    del.onError maps 409 → conflict toast (defensive backstop, current service can't 409). Toasts per UI-SPEC.
    Delete UX = the TAX-06 cascade dialog: pink RetroConfirmDialog; on open fetch useUsageCount("container", id);
    total>0 → cascade copy with count + "they stay in their location"; total=0 → plain pink; confirm → del.mutate(id)
    (a single bare DELETE — NO flag/force/second call). NO type-to-confirm gate.
    ContainerFormDialog = INLINE RetroDialog (no route) blue form, RHF+zod containerSchema, fields name /
    location (SearchPicker domain="locations", REQUIRED) / description / capacity / short_code. Empty-source
    disabled state + "No locations yet — add one first." hint when no locations exist (InventoryFormPage pattern).
    Render-loop guard throughout.
    ContainersTab.test.tsx: containers render grouped under location headers (assert two groups + (No location));
    delete with items (MSW inventory total>0) shows the cascade count copy then calls bare del; delete invalidates
    inventory (assert via spy or refetch); empty/error states.
    useContainerMutations.test.tsx: del invalidates BOTH ["containers", wsId] AND ["inventory", wsId]; 409→conflict toast.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test -- src/features/taxonomy/components/ContainersTab.test.tsx src/features/taxonomy/hooks/useContainerMutations.test.tsx</automated>
  </verify>
  <done>Containers tab green: grouped-by-location with (No location) last, delete = bare DELETE invalidating containers+inventory, cascade count warning, SearchPicker location field, empty/error states. Export name `ContainersTab` unchanged (TaxonomyPage imports it — single-writer, not re-edited in W3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→workspace API | location/container reads/writes + /search + usage-count cross /workspaces/{wsId} |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering (data integrity in UI) | stale inventory cache after container delete (FK SET NULL) | mitigate | del invalidates BOTH ["containers",ws] AND ["inventory",ws] (OQ2) |
| T-10-06 | Information Disclosure | cross-tenant location/container search | mitigate | wsId from useWorkspace() on every /search; backend repo workspace scoping |
| T-10-07 | Tampering | location hard-delete (CASCADE/RESTRICT hazard) | mitigate | no delete affordance for locations; archive-only (TAX-04 / OQ6) |
| T-10-SC | Tampering | npm installs | accept | zero new installs |
</threat_model>

<verification>
- bun run test for LocationsTab / ContainersTab / location+container mutation tests green.
- tsc clean for all owned files; SearchPicker absent from shipped forms.
- No edits to routes/index.tsx, Sidebar.tsx, or handlers.ts (W1/W2 single-writer files).
</verification>

<success_criteria>
Locations tab = tree + archive-only (no delete, parent_location); Containers tab = grouped-by-location
+ bare-DELETE-with-unassign invalidating inventory + SearchPicker location field. Shipped forms untouched.
</success_criteria>

<output>
Create `.planning/phases/10-taxonomy/10-03-SUMMARY.md` when done (note how the tabs were swapped into the
W2 shell, and confirm no single-writer files were edited).
</output>
