---
phase: 10-taxonomy
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - frontend2/src/features/taxonomy/TaxonomyPage.tsx
  - frontend2/src/features/taxonomy/TaxonomyPage.test.tsx
  - frontend2/src/features/taxonomy/components/CategoriesTab.tsx
  - frontend2/src/features/taxonomy/components/CategoriesTab.test.tsx
  - frontend2/src/features/taxonomy/components/CategoryFormDialog.tsx
  - frontend2/src/features/taxonomy/components/LocationsTab.tsx
  - frontend2/src/features/taxonomy/components/ContainersTab.tsx
  - frontend2/src/features/taxonomy/components/LabelsTab.tsx
  - frontend2/src/features/taxonomy/hooks/useCategoriesQuery.ts
  - frontend2/src/features/taxonomy/hooks/useCategoryMutations.ts
  - frontend2/src/features/taxonomy/hooks/useCategoryMutations.test.tsx
  - frontend2/src/features/taxonomy/hooks/useUsageCount.ts
  - frontend2/src/routes/index.tsx
  - frontend2/src/components/layout/Sidebar.tsx
autonomous: true
requirements: [TAX-01, TAX-02]
user_setup: []

must_haves:
  truths:
    - "Navigating to /taxonomy renders a mint TAXONOMY Window with RetroTabs (categories/locations/containers/labels), categories default"
    - "?tab= round-trips: switching a tab updates the URL; loading /taxonomy?tab=locations selects the locations panel"
    - "Categories tab renders the category tree via RetroTree built from GET /categories (parent_category_id)"
    - "Creating/editing a category opens the form and PREFIX-invalidates [categories, wsId]"
    - "Archiving a category fires GET /items?category_id=&limit=1 first; when total>0 the confirm shows the item count (TAX-02), then POST archive"
    - "Sidebar Categories/Locations/Containers nav links route into /taxonomy?tab=…"
    - "Three W3 tab STUBS (Locations/Containers/Labels) exist so TaxonomyPage imports resolve"
  artifacts:
    - path: "frontend2/src/features/taxonomy/TaxonomyPage.tsx"
      provides: "?tab= + RetroTabs shell importing all four tabs"
      min_lines: 40
    - path: "frontend2/src/features/taxonomy/components/CategoriesTab.tsx"
      provides: "category tree + CRUD + archive-warning wiring"
      min_lines: 60
    - path: "frontend2/src/features/taxonomy/hooks/useCategoriesQuery.ts"
      provides: "categories query keyed [categories, wsId] + buildTree memo"
      contains: "[\"categories\", wsId]"
    - path: "frontend2/src/features/taxonomy/hooks/useCategoryMutations.ts"
      provides: "create/update/archive/restore with PREFIX invalidate"
      contains: "invalidateQueries"
    - path: "frontend2/src/features/taxonomy/hooks/useUsageCount.ts"
      provides: "shared usage-count fetcher (category items / container inventory .total)"
      contains: "category_id"
    - path: "frontend2/src/routes/index.tsx"
      provides: "taxonomy route + category form routes (literal-before-param)"
      contains: "path=\"taxonomy\""
  key_links:
    - from: "frontend2/src/features/taxonomy/TaxonomyPage.tsx"
      to: "?tab= URL"
      via: "useSearchParams round-trip (LoansListPage pattern)"
      pattern: "setSearchParams"
    - from: "frontend2/src/features/taxonomy/components/CategoriesTab.tsx"
      to: "GET /items?category_id={id}&limit=1 .total"
      via: "useUsageCount before archive confirm"
      pattern: "category_id"
    - from: "frontend2/src/features/taxonomy/hooks/useCategoryMutations.ts"
      to: "[categories, wsId] cache"
      via: "qc.invalidateQueries PREFIX (no exact)"
      pattern: "invalidateQueries"
---

<objective>
Build the Taxonomy page shell (one `/taxonomy` route, `?tab=` RetroTabs over
categories/locations/containers/labels) and fully implement the FIRST tab —
Categories — proving the tree + create/edit + the TAX-02 client-computed
usage-warning archive flow end to end. This plan is the SINGLE WRITER of
routes/index.tsx and Sidebar.tsx this milestone, and it creates the three W3 tab
STUBS so the page imports resolve.

Purpose: A working page shell + the hardest tab (tree + usage warning) de-risks
the parallel W3 tabs, which fill in the stubs without touching the page/route/nav.
Output: TaxonomyPage, CategoriesTab, CategoryFormDialog, category query/mutations
hooks, shared useUsageCount, route registration, Sidebar links, W3 tab stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-taxonomy/10-RESEARCH.md
@.planning/phases/10-taxonomy/10-UI-SPEC.md
@.planning/phases/10-taxonomy/10-01-SUMMARY.md

# Parity sources to mirror (read before writing):
@frontend2/src/features/loans/LoansListPage.tsx
@frontend2/src/features/borrowers/hooks/useBorrowerMutations.ts

<interfaces>
<!-- From 10-01 (Wave 1 — already shipped): -->
categoryApi (lib/api/category.ts): list→{items}, get, create, update, archive, restore, del.
buildTree (features/taxonomy/lib/buildTree.ts): buildTree(rows, r=>r.parent_category_id) → TreeNode<T>[].
RetroTree (components/retro): props {nodes:RetroTreeNode[], storageKey, onAddChild, onEdit, onArchive, onRestore, emptyState}; RetroTreeNode {id,name,itemCount,isArchived,children}.
categorySchema (features/taxonomy/schema.ts).
useUsageCount is created HERE (shared by containers W3): GET /items?category_id={id}&limit=1 → .total (PAGINATED).
RetroTabs (components/retro): controlled value + onChange + tabs RetroTab[].
useSearchParams round-trip: setSearchParams(prev => { const n=new URLSearchParams(prev); n.set("tab",id); return n; }).
useWorkspace(): { currentWorkspaceId, workspaces }.
RENDER-LOOP guard: const tRef=useRef(t); tRef.current=t; destructure mutation .mutate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useCategoriesQuery + useCategoryMutations + useUsageCount (shared)</name>
  <files>frontend2/src/features/taxonomy/hooks/useCategoriesQuery.ts, frontend2/src/features/taxonomy/hooks/useCategoryMutations.ts, frontend2/src/features/taxonomy/hooks/useCategoryMutations.test.tsx, frontend2/src/features/taxonomy/hooks/useUsageCount.ts</files>
  <behavior>
    - useCategoriesQuery keyed ["categories", wsId]; queryFn categoryApi.list(ws).then(r=>r.items); enabled !!wsId; retry false
    - exposes { rows, tree, isLoading, isError } where tree = buildTree(rows, c=>c.parent_category_id) memoized on rows
    - useCategoryMutations: create/update/archive/restore each PREFIX-invalidate ["categories", wsId] (no exact:true)
    - mutation success/error toasts per UI-SPEC copy; render-loop safe (returns whole mutations; page destructures .mutate)
    - useUsageCount returns an imperative fetchCount(kind:"category"|"container", id):Promise<number> reading the PAGINATED .total from GET /items?category_id= or /inventory?container_id= (limit=1)
  </behavior>
  <action>
    Mirror useBorrowerMutations.ts verbatim for shape (useWorkspace + qc + useLingui; invalidate =
    () => qc.invalidateQueries({queryKey:["categories", wsId as string]}) PREFIX, NO exact). create/update/
    archive/restore. Toasts: "Category created." / "Changes saved." / "{name} archived." / "{name} restored." /
    error "Couldn't save this category." (UI-SPEC §Toasts).
    useCategoriesQuery mirrors the RESEARCH Pattern-2 snippet: key ["categories", wsId], list().then(r=>r.items),
    tree via useMemo(()=>buildTree(data ?? [], c=>c.parent_category_id),[data]).
    useUsageCount.ts is SHARED with W3 containers — keep it domain-generic: a hook returning an imperative
    fetchCount(kind, id):Promise<number> that calls GET /items?category_id={id}&limit=1 (kind="category") or
    GET /inventory?container_id={id}&limit=1 (kind="container") via lib/api get, clamps limit, reads PAGINATED
    .total. Imperative (NOT a hook-per-row) to avoid fan-out — mirrors borrowers OQ7 no-fan-out.
    useCategoryMutations.test.tsx: assert create invalidates ["categories", wsId]; archive calls categoryApi.archive;
    error path toasts. Use MSW from 10-01.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test -- src/features/taxonomy/hooks/useCategoryMutations.test.tsx</automated>
  </verify>
  <done>category hooks green; PREFIX invalidation proven; useUsageCount reads .total with limit=1; no exact:true anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: TaxonomyPage shell (?tab= + RetroTabs) + W3 tab STUBS + route + Sidebar links</name>
  <files>frontend2/src/features/taxonomy/TaxonomyPage.tsx, frontend2/src/features/taxonomy/TaxonomyPage.test.tsx, frontend2/src/features/taxonomy/components/LocationsTab.tsx, frontend2/src/features/taxonomy/components/ContainersTab.tsx, frontend2/src/features/taxonomy/components/LabelsTab.tsx, frontend2/src/routes/index.tsx, frontend2/src/components/layout/Sidebar.tsx</files>
  <action>
    TaxonomyPage.tsx: one mint Window titled `TAXONOMY — ${workspaceName}` (INVENTORY title pattern).
    Derive tab = params.get("tab") ?? "categories"; unknown → categories. RetroTabs (controlled) with 4 tabs
    categories/locations/containers/labels; onChange uses the LoansListPage setSearchParams(prev=>…) round-trip.
    The page imports and renders ALL FOUR tab components by their stable paths: CategoriesTab (Task 3),
    LocationsTab / ContainersTab / LabelsTab. RENDER-LOOP guard: tRef pattern. Do NOT double-pad (RetroTabs owns p-sp-4).

    STUB the three W3 tab files NOW so the page imports resolve and the shell test passes:
      components/LocationsTab.tsx  → `export function LocationsTab(){ return <div data-testid="tab-locations-pending" />; }`
      components/ContainersTab.tsx → `export function ContainersTab(){ return <div data-testid="tab-containers-pending" />; }`
      components/LabelsTab.tsx     → `export function LabelsTab(){ return <div data-testid="tab-labels-pending" />; }`
    These stubs are REPLACED in-place by 10-03 (Locations/Containers) and 10-04 (Labels) — a legal cross-WAVE
    file handoff (W3 is a later wave). Because the page never changes when the stubs are filled in, NO same-wave
    plan re-edits TaxonomyPage.tsx.

    routes/index.tsx (SINGLE WRITER): import TaxonomyPage + CategoryFormDialog host. Add under the authenticated
    AppShell branch, literal-before-param:
      <Route path="taxonomy" element={<TaxonomyPage />} />
      <Route path="taxonomy/categories/new" element={<CategoryFormDialog mode="create" />} />
      <Route path="taxonomy/categories/:id/edit" element={<CategoryFormDialog mode="edit" />} />
    (literal `taxonomy/categories/new` ABOVE the `:id/edit` param route; keep above the `*` wildcard).
    Location/container forms are INLINE RetroDialogs (no route) — so routes/index.tsx is edited ONLY here.

    Sidebar.tsx (SINGLE WRITER — W3 does NOT touch it): wire all taxonomy nav links in this one edit —
    Categories `to="/taxonomy?tab=categories"`, Locations `to="/taxonomy?tab=locations"`, Containers
    `to="/taxonomy?tab=containers"`. Labels has no nav entry.

    TaxonomyPage.test.tsx: render at /taxonomy → categories content + 4-tab list; render at /taxonomy?tab=locations
    → the locations stub (testid tab-locations-pending) selected; clicking the Locations tab updates `?tab=`.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test -- src/features/taxonomy/TaxonomyPage.test.tsx && bunx tsc --noEmit 2>&1 | grep -E 'routes/index|Sidebar|TaxonomyPage|LocationsTab|ContainersTab|LabelsTab' ; test $? -eq 1</automated>
  </verify>
  <done>/taxonomy renders the mint shell + 4 tabs; ?tab= round-trips; three W3 tab STUBS created; route literal-before-param; Sidebar all-three taxonomy links wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CategoriesTab + CategoryFormDialog (tree + CRUD + TAX-02 archive warning)</name>
  <files>frontend2/src/features/taxonomy/components/CategoriesTab.tsx, frontend2/src/features/taxonomy/components/CategoriesTab.test.tsx, frontend2/src/features/taxonomy/components/CategoryFormDialog.tsx</files>
  <behavior>
    - CategoriesTab renders RetroTree from useCategoriesQuery().tree (adapted to RetroTreeNode; itemCount left 0 in tree, count fetched only on archive — no fan-out)
    - storageKey="taxonomy:tree:categories"
    - toolbar ⊕ ADD ROOT CATEGORY opens create form (parent empty); RetroTree onAddChild opens create with parent pre-set
    - onEdit opens edit form; onArchive opens the usage-warning confirm; onRestore calls restore mutation
    - archive confirm: BEFORE archiving, fetch count via useUsageCount fetchCount("category", id); total>0 → butter "Archive anyway" dialog with "⚠ {name} has {n} item(s)…"; total=0 → plain butter "Archive {RESOURCE}?"; confirm → archive.mutate
    - parent picker in the form = RetroCombobox of flattened categories, EXCLUDING self + descendants (no cycles), empty=root
    - empty roots → RetroEmptyState "NO CATEGORIES YET" with ADD ROOT CATEGORY action
    - load error → "COULDN'T LOAD CATEGORIES" + RETRY
  </behavior>
  <action>
    CategoriesTab.tsx: consume useCategoriesQuery + useCategoryMutations + useUsageCount. Map TreeNode→RetroTreeNode.
    Wire RetroTree (storageKey "taxonomy:tree:categories") onAddChild/onEdit/onArchive/onRestore + toolbar
    "⊕ ADD ROOT CATEGORY". Archive flow = the TAX-02 client warning: open RetroConfirmDialog (butter); on open,
    fetch the count via fetchCount("category", id); render count-aware copy (UI-SPEC §Destructive copy);
    confirm → archive.mutate(id) (archive proceeds regardless — advisory). Restore → restore.mutate(id).
    Category forms ARE routed (Task 2 registered /taxonomy/categories/new + /:id/edit) — navigate to them.
    CategoryFormDialog.tsx: blue Window form (InventoryFormPage idiom), RHF + zod (categorySchema), fields
    name / description / parent (RetroCombobox flattened categories excluding self+descendants via a
    flattenExcluding util — empty = root). On submit create/update.mutate then navigate to /taxonomy?tab=categories.
    Dirty-guard butter DISCARD CHANGES? on cancel. Render-loop guard throughout (tRef; destructure .mutate).
    CategoriesTab.test.tsx: tree renders nested rows from MSW; ⊕ ADD opens form; archiving a category with
    items (MSW total>0) shows the count copy then archives; archiving with 0 items shows plain copy; restore
    on an archived row calls restore; empty + error states render.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test -- src/features/taxonomy/components/CategoriesTab.test.tsx</automated>
  </verify>
  <done>Categories tab green: tree nests, CRUD opens/saves, TAX-02 warning shows the item count from /items?category_id= before a permissive archive, restore works, empty/error states render.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→workspace API | category reads/writes + usage-count read cross /workspaces/{wsId} |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering (UI integrity) | stale tree after archive | mitigate | PREFIX-invalidate ["categories", wsId] on every mutation (no exact:true) |
| T-10-04 | Information Disclosure | cross-tenant category access | mitigate | wsId from useWorkspace() on every call; backend repo workspace scoping |
| T-10-SC | Tampering | npm installs | accept | zero new installs |
</threat_model>

<verification>
- bun run test for the three taxonomy test files green.
- tsc clean for routes/index.tsx, Sidebar.tsx, TaxonomyPage, CategoriesTab, and the three stubs.
- Manual sanity: /taxonomy?tab=categories shows the tree; archive shows count when items assigned.
</verification>

<success_criteria>
The page shell renders with ?tab= routing; the Categories tab does full CRUD + the TAX-02
client-computed usage-warning archive; routes + Sidebar wired; W3 stubs in place; render-loop guards present.
</success_criteria>

<output>
Create `.planning/phases/10-taxonomy/10-02-SUMMARY.md` when done. Document: (1) the stub handoff —
LocationsTab/ContainersTab filled by 10-03, LabelsTab by 10-04, in-place, no page edit needed; (2) the
form-routing decision — category forms ARE routed; location/container/label forms are INLINE dialogs;
(3) useUsageCount's imperative fetchCount signature for the W3 container consumer.
</output>
