---
phase: 10-taxonomy
plan: 05
type: execute
wave: 4
depends_on: ["10-02", "10-03", "10-04"]
files_modified:
  - frontend2/e2e/taxonomy.spec.ts
autonomous: true
requirements: [TAX-01, TAX-02, TAX-03, TAX-04, TAX-05, TAX-06, TAX-07]
user_setup: []

must_haves:
  truths:
    - "A single live Playwright spec logs in ONCE (auth limiter) then exercises all four tabs against the real backend + Postgres"
    - "Category flow: create a category → it appears in the tree → archive it → the archive-warning confirm appears"
    - "Container flow: create a container under a location → it appears in the grouped-by-location list → delete it → unassign confirm appears"
    - "Label flow: create a label (with color) → it appears in the list → edit → delete"
    - "An in-plan discovery-list gate enumerates the live taxonomy routes/handlers before assertions"
  artifacts:
    - path: "frontend2/e2e/taxonomy.spec.ts"
      provides: "live smoke across categories/locations/containers/labels"
      min_lines: 80
  key_links:
    - from: "frontend2/e2e/taxonomy.spec.ts"
      to: "/taxonomy?tab= live page"
      via: "real login → cookie JWT → tab navigation + CRUD"
      pattern: "/taxonomy"
---

<objective>
A single live Playwright spec that smoke-tests the whole Taxonomy surface against
the real backend + Postgres: category create→tree→archive-warning, container
create→grouped→delete(unassign), and label CRUD. ONE login to respect the auth
rate limiter; an in-plan discovery-list gate confirms the live surface before asserting.

Purpose: Phase-gate E2E (10-VALIDATION §Phase gate). Proves the parity wiring end
to end past MSW.
Output: frontend2/e2e/taxonomy.spec.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-taxonomy/10-UI-SPEC.md
@.planning/phases/10-taxonomy/10-02-SUMMARY.md
@.planning/phases/10-taxonomy/10-03-SUMMARY.md
@.planning/phases/10-taxonomy/10-04-SUMMARY.md
@CLAUDE.md

# Parity source — the shipped E2E spec to mirror (login + proxy + cookie discipline):
@frontend2/e2e/login-dashboard.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: discovery-list gate — enumerate the live taxonomy surface</name>
  <files>frontend2/e2e/taxonomy.spec.ts</files>
  <action>
    Start the spec with an in-plan discovery gate (a `test.beforeAll` or a first `test`) that, after the single
    login, hits the live backend to confirm the four taxonomy domains respond before any UI assertion runs:
    via `page.request.get("/api/workspaces/{wsId}/categories")` (BARE {items}), `/locations` (paginated),
    `/containers` (paginated), `/labels` (BARE {items}). Assert each returns 200 and the EXPECTED envelope
    shape (categories/labels have no `total`; locations/containers DO). Resolve {wsId} from the logged-in
    session (the cookie context inherits — CLAUDE.md). If a domain 404s or the envelope is wrong, fail fast
    with a clear message (this is the gate). Log the discovered counts.
    Follow login-dashboard.spec.ts exactly for: real `/login → /` flow, the `/^log in$/i` submit button, the
    Vite `/api` rewrite assumption, and cookie inheritance for page.request. Read E2E_USER/E2E_PASS from env
    (defaults seeder@test.local / password123).
  </action>
  <verify>
    <automated>cd frontend2 && E2E_USER=${E2E_USER:-seeder@test.local} E2E_PASS=${E2E_PASS:-password123} bun run test:e2e -- taxonomy --grep "discovery" 2>&1 | tail -20</automated>
  </verify>
  <done>The discovery gate logs in once, confirms all four taxonomy domains respond with the correct per-endpoint envelope, fails fast otherwise.</done>
</task>

<task type="auto">
  <name>Task 2: category + container + label flows (single session)</name>
  <files>frontend2/e2e/taxonomy.spec.ts</files>
  <action>
    In the SAME spec file / SAME logged-in session (no second login — auth limiter, CLAUDE.md), add the three flows.
    Use a unique suffix (Date.now()) on created names so reruns don't collide; clean up created rows at the end
    where practical (archive/delete the ones created).

    CATEGORY (TAX-01/02): navigate /taxonomy (categories default). Click ⊕ ADD ROOT CATEGORY, fill name
    `E2E Cat {ts}`, submit. Assert the new node appears in the tree (role=tree / treeitem with the name).
    Click its ARCHIVE action → assert the butter archive confirm dialog appears (count copy when items assigned;
    here likely 0 items → plain copy). Confirm → assert the row reflects archived (ARCHIVED badge or removal).

    CONTAINER (TAX-05/06): go /taxonomy?tab=containers. Ensure at least one location exists (create one via
    /taxonomy?tab=locations ⊕ ADD ROOT LOCATION `E2E Loc {ts}` if needed). Click ⊕ ADD CONTAINER, pick the
    location via the SearchPicker (type to filter, select), fill name `E2E Box {ts}`, submit. Assert the
    container appears UNDER its location group header. Click ⌫ DELETE → assert the pink confirm with the
    unassign copy → confirm → assert the row is gone.

    LABEL (TAX-07): go /taxonomy?tab=labels. ⊕ ADD LABEL, fill name `E2E Label {ts}`, pick a color swatch,
    submit. Assert the row appears with a swatch + name. EDIT → change description → save → assert. Delete →
    pink confirm → confirm → assert gone.

    Assertions use UI-SPEC copy/glyph vocabulary and the locator discipline from login-dashboard.spec.ts
    (label/role-based, prefix-tolerant like the inventory specs). Keep all three flows in ONE test or serial
    tests sharing the one login context.
  </action>
  <verify>
    <automated>cd frontend2 && E2E_USER=${E2E_USER:-seeder@test.local} E2E_PASS=${E2E_PASS:-password123} bun run test:e2e -- taxonomy 2>&1 | tail -30</automated>
  </verify>
  <done>taxonomy.spec.ts green in isolation: one login; category create→tree→archive-warning; container create→grouped→delete(unassign); label create→edit→delete; created rows cleaned up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test client→live API | real cookie-JWT session crosses /api → :8080 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Denial of Service | auth rate limiter tripped by repeat logins | mitigate | exactly ONE login for the whole spec; run taxonomy spec in isolation (CLAUDE.md constraint) |
| T-10-SC | Tampering | npm installs | accept | zero new installs (playwright already present) |
</threat_model>

<verification>
- `bun run test:e2e -- taxonomy` green in isolation (auth limiter respected — single login).
- Discovery gate confirms live envelope split before UI assertions.
</verification>

<success_criteria>
A single live taxonomy spec proves the four-tab surface end to end against the real backend with
one login, covering the category archive-warning, container delete-unassign, and label CRUD flows.
</success_criteria>

<output>
Create `.planning/phases/10-taxonomy/10-05-SUMMARY.md` when done (spec coverage, any live-backend residues,
note the isolation/auth-limiter run requirement).
</output>
