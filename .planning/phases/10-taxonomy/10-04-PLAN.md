---
phase: 10-taxonomy
plan: 04
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - frontend2/src/features/taxonomy/components/LabelsTab.tsx
  - frontend2/src/features/taxonomy/components/LabelsTab.test.tsx
  - frontend2/src/features/taxonomy/components/LabelFormDialog.tsx
  - frontend2/src/features/taxonomy/components/ColorSwatchPicker.tsx
  - frontend2/src/features/taxonomy/hooks/useLabelsQuery.ts
  - frontend2/src/features/taxonomy/hooks/useLabelMutations.ts
  - frontend2/src/features/taxonomy/hooks/useLabelMutations.test.tsx
autonomous: true
requirements: [TAX-07]
user_setup: []

must_haves:
  truths:
    - "Labels tab renders a flat CRUD list from GET /labels (BARE {items}) with each row showing the color swatch + name"
    - "Creating/editing a label opens an inline RetroDialog with name + the 8-swatch on-palette color picker; selected swatch stores its hex"
    - "Label CRUD (create/update/archive/restore/delete) PREFIX-invalidates [labels, wsId]"
    - "Color swatch always renders a 1px ink border so a pale/white swatch stays visible (three-cue discipline)"
    - "Delete uses a plain pink RetroConfirmDialog; archive is the soft default per UI-SPEC"
  artifacts:
    - path: "frontend2/src/features/taxonomy/components/LabelsTab.tsx"
      provides: "label CRUD list (fills the W2 LabelsTab stub)"
      min_lines: 50
    - path: "frontend2/src/features/taxonomy/components/ColorSwatchPicker.tsx"
      provides: "8 on-palette swatches storing hex; selected → 2px ink ring + ✓"
      contains: "swatch"
    - path: "frontend2/src/features/taxonomy/hooks/useLabelMutations.ts"
      provides: "label create/update/archive/restore/del with PREFIX invalidate"
      contains: "[\"labels\", wsId"
  key_links:
    - from: "frontend2/src/features/taxonomy/hooks/useLabelsQuery.ts"
      to: "GET /labels BARE {items}"
      via: "labelsApi.listWorkspaceLabels keyed [labels, wsId]"
      pattern: "[\"labels\", wsId]"
    - from: "frontend2/src/features/taxonomy/components/LabelFormDialog.tsx"
      to: "Label.color hex"
      via: "ColorSwatchPicker selection (8 on-palette swatches)"
      pattern: "ColorSwatchPicker"
---

<objective>
Implement the Labels tab (TAX-07): a flat CRUD list of workspace labels with an
8-swatch on-palette color picker. Fills the W2 LabelsTab STUB in-place. Runs in
parallel with 10-03 (Locations/Containers) — fully disjoint files.

Purpose: TAX-07 label manager so item label-attach (Phase 7) has labels to manage.
Output: LabelsTab, LabelFormDialog (inline dialog), ColorSwatchPicker, label
query+mutation hooks.

CONSTRAINTS: MUST NOT edit routes/index.tsx, Sidebar.tsx, TaxonomyPage.tsx, or
handlers.ts (all W1/W2 single-writer files). The label form is an INLINE RetroDialog
(no route). labels.ts api was already extended in 10-01 — consume it, do not re-edit it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-taxonomy/10-RESEARCH.md
@.planning/phases/10-taxonomy/10-UI-SPEC.md
@.planning/phases/10-taxonomy/10-01-SUMMARY.md
@.planning/phases/10-taxonomy/10-02-SUMMARY.md

# Parity sources (read before writing):
@frontend2/src/features/borrowers/hooks/useBorrowerMutations.ts

<interfaces>
<!-- From 10-01 (Wave 1 — already shipped): -->
labelsApi (lib/api/labels.ts): listWorkspaceLabels(ws)→Label[]; get/create/update/archive/restore/del added in W1.
Label (lib/types.ts): {id, workspace_id, name, color?, description?, is_archived, created_at, updated_at}.
labelSchema (features/taxonomy/schema.ts): color regex ^#[0-9A-Fa-f]{6}$ (optional).
RetroDialog, RetroConfirmDialog, RetroTable, RetroEmptyState, RetroInput, RetroBadge, BevelButton, retroToast (components/retro).
LabelsTab STUB exists at components/LabelsTab.tsx (W2): replace its body in-place; TaxonomyPage already imports it.
8 on-palette swatch tokens (UI-SPEC §Label manager): --titlebar-blue, --titlebar-pink, --titlebar-mint, --titlebar-butter, --accent-blue-deep, --accent-pink-deep, --accent-mint-deep, --danger (#b73348).
RENDER-LOOP guard: tRef; destructure .mutate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useLabelsQuery + useLabelMutations (PREFIX invalidate)</name>
  <files>frontend2/src/features/taxonomy/hooks/useLabelsQuery.ts, frontend2/src/features/taxonomy/hooks/useLabelMutations.ts, frontend2/src/features/taxonomy/hooks/useLabelMutations.test.tsx</files>
  <behavior>
    - useLabelsQuery keyed ["labels", wsId]; queryFn labelsApi.listWorkspaceLabels(ws); enabled !!wsId; retry false; returns { rows, isLoading, isError }
    - useLabelMutations: create/update/archive/restore/del each PREFIX-invalidate ["labels", wsId] (no exact:true)
    - toasts per UI-SPEC (created/Changes saved/archived/restored/deleted; generic error)
    - render-loop safe (returns whole mutations; page destructures .mutate)
  </behavior>
  <action>
    Mirror useBorrowerMutations.ts for shape. useLabelsQuery: key ["labels", wsId], listWorkspaceLabels(ws).
    useLabelMutations: create/update/archive/restore/del; invalidate = qc.invalidateQueries({queryKey:["labels", wsId]})
    PREFIX (no exact). Toasts: "{name} created." / "Changes saved." / "{name} archived." / "{name} restored." /
    "{name} deleted." / error "Couldn't save this label." (and a delete-specific error).
    useLabelMutations.test.tsx: create/update/archive/restore/del invalidate ["labels", wsId]; error toasts. MSW from W1.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test -- src/features/taxonomy/hooks/useLabelMutations.test.tsx</automated>
  </verify>
  <done>label hooks green; full CRUD PREFIX-invalidates ["labels", wsId]; no exact:true.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ColorSwatchPicker (8 on-palette swatches storing hex)</name>
  <files>frontend2/src/features/taxonomy/components/ColorSwatchPicker.tsx</files>
  <behavior>
    - renders 8 fixed on-palette swatches + a "no color" option
    - each swatch is a focusable button with a 1px ink border; aria-label = the color name; aria-pressed when selected
    - selected swatch gets a 2px ink ring + ✓ glyph
    - value = hex string (or undefined/empty for no-color); onChange(hex|undefined)
    - keyboard operable (Tab between swatches, Enter/Space selects)
  </behavior>
  <action>
    Build ColorSwatchPicker.tsx from the UI-SPEC §Label manager 8-swatch set (the 8 token hexes + a neutral
    "none" swatch rendering bg-bg-panel-2). Store the resolved HEX value (not the token name) so the backend
    receives `#RRGGBB` matching its `^#[0-9A-Fa-f]{6}$` pattern. Each swatch: focusable <button> with a 1px
    border-border-ink (MANDATORY cue #3 — pale swatch stays visible), aria-label, aria-pressed; selected →
    2px ink ring + ✓. Controlled value/onChange for RHF Controller.
  </action>
  <verify>
    <automated>cd frontend2 && bunx tsc --noEmit 2>&1 | grep -E 'ColorSwatchPicker' ; test $? -eq 1</automated>
  </verify>
  <done>ColorSwatchPicker compiles; 8 swatches + none; selected shows ring + ✓; stores hex; 1px ink border on every swatch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: LabelsTab + LabelFormDialog (fills the W2 stub)</name>
  <files>frontend2/src/features/taxonomy/components/LabelsTab.tsx, frontend2/src/features/taxonomy/components/LabelsTab.test.tsx, frontend2/src/features/taxonomy/components/LabelFormDialog.tsx</files>
  <behavior>
    - LabelsTab replaces the stub body: flat list (RetroTable or rows) from useLabelsQuery; each row = [swatch 16×16 1px ink border][name semibold][description muted, optional]……[EDIT][⌫]
    - toolbar ⊕ ADD LABEL opens the inline LabelFormDialog (create); EDIT opens it (edit)
    - delete = plain pink RetroConfirmDialog; on confirm del.mutate; archived rows muted + ARCHIVED badge + RESTORE (three-cue), archive is the soft default
    - empty → RetroEmptyState "NO LABELS YET"; error → "COULDN'T LOAD LABELS" + RETRY
    - LabelFormDialog = inline RetroDialog: name (RetroInput) + ColorSwatchPicker + optional description; RHF+zod labelSchema; submit create/update.mutate; dirty-guard butter discard
  </behavior>
  <action>
    LabelsTab.tsx: REPLACE the W2 stub (`<div data-testid="tab-labels-pending" />`) with the real CRUD list —
    keep the export name `LabelsTab` (TaxonomyPage imports it). Consume useLabelsQuery + useLabelMutations.
    Row anatomy per UI-SPEC §Label manager: color swatch (16×16, 1px border-border-ink — MANDATORY), name 14px
    semibold, optional muted description, EDIT + ⌫ actions. Archived rows: text-fg-muted + ARCHIVED RetroBadge +
    RESTORE action (three-cue). Delete = plain pink RetroConfirmDialog ("Delete '{name}'?…", omit removal clause
    when n=0 — labels have no client attached-count fetch in this phase; use the no-count copy). Empty/error states.
    LabelFormDialog.tsx: inline RetroDialog (NOT a route). RHF + zod labelSchema. Fields: name (RetroInput),
    color (ColorSwatchPicker), description (optional). On submit create/update.mutate then close + toast.
    Dirty-guard butter DISCARD CHANGES?. Render-loop guard (tRef; destructure .mutate).
    LabelsTab.test.tsx: list renders rows w/ swatch + name from MSW; ⊕ ADD opens the dialog; create persists +
    invalidates; EDIT opens with values; delete opens pink confirm then calls del; empty + error states.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test -- src/features/taxonomy/components/LabelsTab.test.tsx</automated>
  </verify>
  <done>Labels tab green: CRUD list w/ swatch+name rows, 8-swatch color picker storing hex, create/edit/delete wired + PREFIX invalidation, archived RESTORE, empty/error states. Stub fully replaced, export name unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→workspace API | label reads/writes cross /workspaces/{wsId} |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Tampering | label name/color rendered in list | mitigate | React escapes names; color is regex-validated (labelSchema) before use in style; never dangerouslySetInnerHTML |
| T-10-09 | Information Disclosure | cross-tenant label access | mitigate | wsId from useWorkspace() on every call; backend repo workspace scoping |
| T-10-SC | Tampering | npm installs | accept | zero new installs |
</threat_model>

<verification>
- bun run test for useLabelMutations + LabelsTab green; ColorSwatchPicker compiles.
- tsc clean for all owned files.
- No edits to routes/index.tsx, Sidebar.tsx, TaxonomyPage.tsx, handlers.ts (W1/W2 single-writer).
- Disjoint from 10-03 (different files, same wave — Lock #7).
</verification>

<success_criteria>
Labels tab = full CRUD list with the 8-swatch on-palette color picker (hex stored), archive-soft +
delete, PREFIX invalidation, three-cue swatch discipline. W2 LabelsTab stub replaced in-place.
</success_criteria>

<output>
Create `.planning/phases/10-taxonomy/10-04-SUMMARY.md` when done (note the stub replacement + disjointness from 10-03).
</output>
