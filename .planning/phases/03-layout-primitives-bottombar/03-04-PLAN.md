---
phase: 03-layout-primitives-bottombar
plan: 04
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - frontend2/src/components/layout/Sidebar.tsx
  - frontend2/src/components/layout/Sidebar.test.tsx
  - frontend2/src/components/layout/TopBar.tsx
  - frontend2/src/components/layout/TopBar.test.tsx
  - frontend2/src/components/layout/PageHeader.tsx
  - frontend2/src/components/layout/PageHeader.test.tsx
autonomous: true
requirements: [SHELL-03, SHELL-04, SHELL-05]
must_haves:
  truths:
    - "Sidebar nav uses react-router NavLink so the current route gets aria-current=\"page\" + the active bevel (border-border-ink bg-titlebar-blue shadow-hard-ink)"
    - "Sidebar groups render OVERVIEW / INVENTORY / SYSTEM section labels; not-yet-built destinations render aria-disabled with a 'Not built yet' title"
    - "In rail mode (data-collapsed on an ancestor) nav labels/counts hide and only the glyph cell shows; an item with a count shows a 6px badge dot"
    - "TopBar renders the brand mark, a disabled workspace pill placeholder, an ONLINE dot + label, reserved disabled slots (bell, SSE), and a user pill whose menu contains a confirm-before Log out"
    - "PageHeader renders a route breadcrumb (ancestor › leaf) and a SESSION · LAST SYNC meta line"
  artifacts:
    - path: "frontend2/src/components/layout/Sidebar.tsx"
      provides: "Collapsible grouped Navigator with per-route active state + rail mode"
      exports: ["Sidebar"]
    - path: "frontend2/src/components/layout/TopBar.tsx"
      provides: "Brand + reserved slots + ONLINE dot + user pill/menu with logout confirm"
      exports: ["TopBar"]
    - path: "frontend2/src/components/layout/PageHeader.tsx"
      provides: "Breadcrumb + SESSION/LAST SYNC meta"
      exports: ["PageHeader"]
  key_links:
    - from: "Sidebar nav items"
      to: "react-router NavLink isActive"
      via: "NavLink render-prop sets active bevel + aria-current"
      pattern: "NavLink"
    - from: "TopBar user-menu logout"
      to: "modal stack (confirm dialog)"
      via: "confirm-before-logout, never bound to bare ESC"
      pattern: "useModalStack|ModalStack"
---

<objective>
Restyle/extend the existing `Sidebar.tsx` into a per-route-active, collapsible
Navigator, and build the `TopBar` and `PageHeader` chrome leaves. All visual
treatment re-anchors to retro-os per the BINDING UI-SPEC; ROADMAP's
premium-terminal vocabulary (`// OVERVIEW`, `// ROUTE`, amber, "HOME WAREHOUSE")
is stale — preserve the OBSERVABLE truth (group labels read as groups,
breadcrumb shows route) via the retro-os treatments.

TopBar reserves rendered-but-disabled slots for the workspace switcher (Phase 5),
notifications bell (Phase 13), and SSE indicator (Phase 6) so the chrome wires
once (Parity §4). The user-menu Log out is confirm-before via the modal stack —
never bound to bare ESC (BAR-05).

Purpose: The persistent chrome leaves that every authenticated route renders.
Output: Sidebar (extended) + TopBar + PageHeader + their tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-layout-primitives-bottombar/03-CONTEXT.md
@.planning/phases/03-layout-primitives-bottombar/03-RESEARCH.md
@.planning/phases/03-layout-primitives-bottombar/03-UI-SPEC.md

<interfaces>
EXISTING frontend2/src/components/layout/Sidebar.tsx (EXTEND, do not rewrite):
  - Already renders grouped Navigator (Overview/Inventory/System) in a plain-titlebar Window,
    glyph + label + count NavItems, user footer. Currently uses <Link active> prop.
  - SWAP the active branch to react-router <NavLink> with the isActive render-prop (SHELL-04
    wants per-route active). Keep the aria-disabled div for not-built routes.

EXISTING frontend2/src/components/BrandMark.tsx: <BrandMark/> = WAREHOUSE.SYS (use it; the
  ".SYS" suffix is the retro-os signature — UI-SPEC §TopBar Resolved: "HOME WAREHOUSE" is stale).

EXISTING frontend2/src/components/retro/Window.tsx: Window with titlebarVariant + actions slot
  (used for the Navigator titlebar + collapse toggle in the actions slot).

From Plan 02 (frontend2/src/components/modal): useModalStack(isOpen, onClose) + ModalStackProvider —
  the logout confirm dialog pushes onto the stack.

UI-SPEC bindings (read the full §TopBar, §Sidebar/Navigator, §PageHeader):
  - TopBar 40px, bg-bg-panel, border-b-2 border-border-ink; brand + workspace pill (disabled
    placeholder, aria-disabled, title "Switch workspace (Phase 5)") + ONLINE dot (bg-titlebar-mint
    live / bg-fg-faint offline) + ONLINE label + spacer + bell (disabled, title "Coming soon") +
    SSE slot ("sse: ● live" placeholder) + user pill (28px pink avatar + name); user menu = single
    "Log out" item (confirm-before) with placeholders for profile/settings/switch.
  - Sidebar group labels: 10px Plex Sans 700 uppercase tracking-[0.14em] text-fg-muted, dotted bottom rule.
    Active item: border-border-ink bg-titlebar-blue shadow-hard-ink + aria-current. Rail mode hides
    .nav-label/.nav-count; count item shows a 6px ink-bordered pastel badge dot top-right of the glyph cell.
    Collapse toggle (‹/›) sits in the Navigator titlebar actions slot.
  - PageHeader: bg-bg-panel-2 border-b-2 border-border-ink px-sp-3 py-sp-2. Breadcrumb = 11px Plex Sans
    700 uppercase tracking-[0.1em], ancestors text-fg-muted, leaf text-fg-ink, joined by ink "›".
    Meta line right-aligned font-mono 12px tabular-nums text-fg-muted: SESSION {hh:mm:ss} · LAST SYNC {hh:mm:ss}.
    LAST SYNC placeholder = "—" (em-dash) until Phase 6 (orchestrator resolution #2); slot/markup stays stable.

Copywriting contract (UI-SPEC §Copywriting): ONLINE/OFFLINE; logout confirm copy
  "Log out: End this session? You will need to sign in again." confirm "LOG OUT" (bg-danger-bg text-danger), cancel "STAY".
All user-facing strings via <Trans> (lingui macro) for i18n parity. React escapes user.full_name/email (no dangerouslySetInnerHTML).

JSDOM caveat: assert classes/attributes/aria, NOT computed layout or @media.
Tests need a router wrapper (MemoryRouter from react-router) since NavLink reads location.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sidebar — per-route NavLink active + rail-mode (SHELL-04)</name>
  <files>frontend2/src/components/layout/Sidebar.tsx, frontend2/src/components/layout/Sidebar.test.tsx</files>
  <read_first>
    - frontend2/src/components/layout/Sidebar.tsx (the existing component to extend)
    - .planning/phases/03-layout-primitives-bottombar/03-UI-SPEC.md §"Sidebar / Navigator" (group labels, nav states, collapse-to-rail, badge dot)
    - .planning/phases/03-layout-primitives-bottombar/03-RESEARCH.md "Active-route nav item" example + Pattern 2 (rail hides .nav-label/.nav-count)
  </read_first>
  <behavior>
    - Rendering at "/" gives the Dashboard nav item aria-current="page" and the active-bevel classes
    - Rendering at a different route does NOT mark Dashboard active
    - Group labels OVERVIEW / INVENTORY / SYSTEM render
    - A not-built nav item (no `to`) renders aria-disabled with title "Not built yet"
    - Nav labels/counts carry the .nav-label / .nav-count hook classes so CSS rail mode can hide them
    - A nav item with a count exposes a badge-dot element for rail mode (asserted by class/test-id)
  </behavior>
  <action>
    Extend `Sidebar.tsx`: replace the hardcoded `active` prop branch with react-router `NavLink`
    using the `isActive` render-prop to apply the active bevel
    (`border border-border-ink bg-titlebar-blue shadow-hard-ink` + `aria-current="page"`) vs the
    default/hover treatment. Keep the aria-disabled `<div>` for routes without a `to` (title "Not built yet").
    Add `.nav-label` / `.nav-count` className hooks on the label and count spans so the AppShell's
    `[data-collapsed]` CSS (Plan 06) hides them in rail mode. Add a 6px ink-bordered pastel badge-dot span
    (rendered for items with a count) for rail badge-dot mode. Add a collapse-toggle button (chevron ‹/›)
    rendered in the Navigator Window `actions` slot — it calls an `onToggleCollapse` prop (AppShell owns the
    boolean in Plan 06); accept `collapsed?: boolean` to set the chevron direction + aria-expanded.
    Keep all strings in `<Trans>`. Tokens only; no raw hex; no Silkscreen on nav text.
    Update/extend `Sidebar.test.tsx`: render inside MemoryRouter at "/" and at another path; assert
    aria-current + active classes, group labels, the disabled item, and the .nav-label/.nav-count/badge-dot hooks.
  </action>
  <verify>
    <automated>cd frontend2 && bunx vitest run src/components/layout/Sidebar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend2 && bunx vitest run src/components/layout/Sidebar.test.tsx` exits 0 (active-at-"/" + inactive-elsewhere + groups + disabled + rail hooks)
    - `grep -q 'NavLink' frontend2/src/components/layout/Sidebar.tsx`
    - `grep -q 'nav-label' frontend2/src/components/layout/Sidebar.tsx` and `grep -q 'nav-count' frontend2/src/components/layout/Sidebar.tsx`
    - `! grep -Eq '#[0-9a-fA-F]{6}' frontend2/src/components/layout/Sidebar.tsx`
    - `cd frontend2 && bun run lint:tsc` exits 0
  </acceptance_criteria>
  <done>Sidebar uses NavLink for per-route active state, renders groups + disabled items, and carries rail-mode + badge-dot hooks; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TopBar — brand + reserved slots + ONLINE dot + user menu logout confirm (SHELL-03, BAR-05)</name>
  <files>frontend2/src/components/layout/TopBar.tsx, frontend2/src/components/layout/TopBar.test.tsx</files>
  <read_first>
    - .planning/phases/03-layout-primitives-bottombar/03-UI-SPEC.md §"TopBar (SHELL-03)" + §Copywriting (logout confirm copy) + §Accessibility (banner landmark, focus-visible)
    - frontend2/src/components/BrandMark.tsx (use as-is)
    - frontend2/src/components/modal/index.ts (useModalStack for the confirm dialog — Plan 02)
    - frontend2/src/components/retro/BevelButton.tsx (bevel idiom for pills/buttons)
  </read_first>
  <behavior>
    - TopBar renders <header role="banner"> with the brand mark
    - Workspace pill renders disabled (aria-disabled, title naming Phase 5), no dropdown
    - ONLINE indicator renders a dot + "ONLINE" label by default; an `online={false}` prop renders "OFFLINE" + faint dot
    - Reserved bell + SSE slots render disabled placeholders (aria-disabled / "Coming soon")
    - User pill opens a menu (aria-haspopup, aria-expanded); the menu has a "Log out" item
    - Clicking "Log out" opens a confirm dialog (LOG OUT / STAY); ESC closes the confirm (via modal stack), it does NOT log out
    - Confirming triggers the passed onLogout callback; ESC alone never calls onLogout
  </behavior>
  <action>
    Create `TopBar.tsx` exporting `TopBar`. Props: `online?: boolean` (default true), `user?: {full_name; email}`,
    `onToggleDrawer?` (mobile hamburger — rendered, used by Plan 06), `onLogout: () => void`.
    Render `<header role="banner">` at the UI-SPEC chrome (40px, bg-bg-panel, border-b-2 border-border-ink,
    inset bevel, px-sp-4). Left→right per UI-SPEC: hamburger (mobile-only class `md:hidden`), 30×30 beveled
    brand square + `<BrandMark/>`, disabled workspace pill (aria-disabled, title "Switch workspace (Phase 5)"),
    ONLINE dot (`bg-titlebar-mint` live / `bg-fg-faint` offline) + label, `flex-1` spacer, disabled bell
    (title "Coming soon"), SSE placeholder slot (`sse: ● live` static), user pill (28px pink avatar +
    name). The user pill toggles a bevel-window dropdown (aria-haspopup="menu" aria-expanded) whose only
    enabled item is "Log out"; profile/settings/switch are disabled placeholders. "Log out" opens a confirm
    dialog using `useModalStack(open, close)` from Plan 02 with the UI-SPEC copy; confirm calls `onLogout`,
    cancel/ESC closes without logging out. focus-visible ink ring on all interactive chrome. Strings via <Trans>.
    Tokens only; no raw hex. React-escaped user text (no dangerouslySetInnerHTML).
    Add `TopBar.test.tsx`: banner landmark, disabled workspace pill, ONLINE/OFFLINE toggle, disabled bell/SSE,
    user-menu open, logout-opens-confirm, confirm-calls-onLogout, and ESC-closes-confirm-without-logout
    (wrap in ModalStackProvider).
  </action>
  <verify>
    <automated>cd frontend2 && bunx vitest run src/components/layout/TopBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend2 && bunx vitest run src/components/layout/TopBar.test.tsx` exits 0 including the "ESC closes confirm but does NOT call onLogout" assertion (BAR-05)
    - `grep -q 'role="banner"' frontend2/src/components/layout/TopBar.tsx`
    - `grep -q 'aria-disabled' frontend2/src/components/layout/TopBar.tsx` (reserved slots disabled)
    - `! grep -Eq '#[0-9a-fA-F]{6}' frontend2/src/components/layout/TopBar.tsx`
    - `cd frontend2 && bun run lint:tsc` exits 0 and `bun run lint:imports` exits 0
  </acceptance_criteria>
  <done>TopBar renders brand + disabled reserved slots + ONLINE dot + user menu whose logout is confirm-before and unreachable via bare ESC; tsc + import guard clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PageHeader — breadcrumb + SESSION/LAST SYNC meta (SHELL-05)</name>
  <files>frontend2/src/components/layout/PageHeader.tsx, frontend2/src/components/layout/PageHeader.test.tsx</files>
  <read_first>
    - .planning/phases/03-layout-primitives-bottombar/03-UI-SPEC.md §"PageHeader (SHELL-05)" (breadcrumb + meta line; LAST SYNC = "—" placeholder)
    - orchestrator resolution #2 (LAST SYNC renders "—" em-dash until Phase 6; slot stays stable)
    - frontend2/src/components/layout/Clock.tsx (Plan 03 — reuse the SESSION readout; do NOT add a second interval owner if shared)
  </read_first>
  <behavior>
    - PageHeader renders a breadcrumb: ancestor segments text-fg-muted, leaf text-fg-ink, joined by "›"
    - The breadcrumb reflects passed segments (e.g. ["OVERVIEW","DASHBOARD"] → "OVERVIEW › DASHBOARD")
    - The meta line renders a SESSION value and a LAST SYNC value
    - LAST SYNC renders "—" (em-dash) placeholder this phase; the markup/slot is present and stable
    - Meta values use font-mono tabular-nums
  </behavior>
  <action>
    Create `PageHeader.tsx` exporting `PageHeader`. Props: `segments: string[]` (breadcrumb path) and an
    optional `lastSync?` (defaults to the "—" em-dash placeholder per resolution #2). Render
    `bg-bg-panel-2 border-b-2 border-border-ink px-sp-3 py-sp-2`, a breadcrumb (11px Plex Sans 700 uppercase
    tracking-[0.1em]; ancestors `text-fg-muted`, leaf `text-fg-ink`; joined by an ink `›`), and a
    right-aligned meta line (`font-mono text-[12px] tabular-nums text-fg-muted`) showing
    `SESSION {clock}` · `LAST SYNC {lastSync}` with 11px uppercase keys. Render the SESSION value via the
    Plan 03 `<Clock/>` single-readout mode (reuse — do not spin a second timer pattern beyond the shared leaf).
    Keys via <Trans>. Tokens only; no raw hex.
    Add `PageHeader.test.tsx`: breadcrumb leaf/ancestor styling, the "—" LAST SYNC placeholder presence,
    and tabular-nums on the meta line.
  </action>
  <verify>
    <automated>cd frontend2 && bunx vitest run src/components/layout/PageHeader.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend2 && bunx vitest run src/components/layout/PageHeader.test.tsx` exits 0 (breadcrumb + LAST SYNC "—" placeholder + tabular-nums)
    - `grep -q '—' frontend2/src/components/layout/PageHeader.tsx` (em-dash LAST SYNC placeholder)
    - `! grep -Eq '#[0-9a-fA-F]{6}' frontend2/src/components/layout/PageHeader.tsx`
    - `cd frontend2 && bun run lint:tsc` exits 0
  </acceptance_criteria>
  <done>PageHeader renders the route breadcrumb + a stable SESSION/LAST SYNC meta line with the em-dash placeholder; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user/workspace data → DOM | user.full_name/email and workspace name render in TopBar/Sidebar as escaped text |
| user-menu logout action → session | logout is a destructive action; must be confirm-gated, never accidental |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering/XSS | TopBar/Sidebar identity text | mitigate | user.full_name/email rendered as escaped JSX text only; no dangerouslySetInnerHTML (existing Sidebar pattern preserved) |
| T-03-09 | Denial of Service (self-inflicted) | accidental logout | mitigate | Logout is confirm-before (LOG OUT/STAY) via modal stack; ESC closes the confirm without logging out (BAR-05 test) |
| T-03-10 | Information disclosure | PII in console | mitigate | Do NOT port legacy debug `console.log` of user/selection (03-RESEARCH.md V7); no PII logged |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this plan (Package Legitimacy Audit: N/A) |

Client-side chrome: no new endpoints, no auth changes (Phase 5). The disabled
workspace/bell/SSE slots enforce nothing — they are visual placeholders only.
</threat_model>

<verification>
- `cd frontend2 && bunx vitest run src/components/layout/Sidebar.test.tsx src/components/layout/TopBar.test.tsx src/components/layout/PageHeader.test.tsx` green
- `cd frontend2 && bun run lint:tsc` clean
- `cd frontend2 && bun run lint:imports` green
</verification>

<success_criteria>
- Sidebar marks the current route active via NavLink, groups nav, disables not-built routes, and supports rail/badge-dot mode
- TopBar shows brand + disabled reserved slots + ONLINE dot + a confirm-before logout unreachable via bare ESC
- PageHeader shows the route breadcrumb + a stable SESSION/LAST SYNC ("—") meta line
</success_criteria>

<output>
Create `.planning/phases/03-layout-primitives-bottombar/03-04-SUMMARY.md` when done
</output>
