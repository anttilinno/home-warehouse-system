---
phase: 03-layout-primitives-bottombar
plan: 06
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-03", "03-04", "03-05"]
files_modified:
  - frontend2/src/components/layout/AppShell.tsx
  - frontend2/src/components/layout/AppShell.test.tsx
  - frontend2/src/components/layout/MobileDrawer.tsx
  - frontend2/src/components/layout/MobileDrawer.test.tsx
  - frontend2/src/styles/globals.css
  - frontend2/src/App.tsx
  - frontend2/src/routes/index.tsx
  - frontend2/e2e/login-dashboard.spec.ts
autonomous: true
requirements: [SHELL-01, SHELL-02, SHELL-06, BAR-01]
must_haves:
  truths:
    - "AppShell renders a 2×3 CSS-Grid (topbar spans both columns, sidebar spans rows 2-3, bottombar in main column row 3) wrapping an <Outlet/> for route content"
    - "Toggling collapse flips a single data-collapsed attribute on the grid root — no JS layout/measure; CSS swaps the column track to the 60px rail"
    - "At <768px the grid is single-column, the sidebar becomes an off-canvas drawer (modal-stack ESC + scrim close), the Bottombar is not rendered, and the FAB renders"
    - "App.tsx mounts ShortcutsProvider + ModalStackProvider around the router; authenticated routes render inside AppShell"
    - "The E2E smoke logs in, sees the shell (TopBar + Sidebar + Bottombar), toggles collapse, opens F1 help, and closes it with ESC"
  artifacts:
    - path: "frontend2/src/components/layout/AppShell.tsx"
      provides: "2×3 grid root + data-collapsed + responsive composition of all chrome"
      exports: ["AppShell"]
    - path: "frontend2/src/components/layout/MobileDrawer.tsx"
      provides: "Off-canvas Navigator drawer (<768px) with focus trap + scrim + modal-stack ESC"
      exports: ["MobileDrawer"]
    - path: "frontend2/src/styles/globals.css"
      provides: "app-shell grid + [data-collapsed] rail CSS + rail label/count hide + reduced-motion"
      contains: "data-collapsed"
  key_links:
    - from: "AppShell grid root"
      to: "data-collapsed CSS in globals.css"
      via: "attribute selector swaps grid-template-columns; no JS measure"
      pattern: "data-collapsed"
    - from: "App.tsx"
      to: "ShortcutsProvider + ModalStackProvider"
      via: "providers wrap the router so chrome + routes share the SSOT and stack"
      pattern: "ShortcutsProvider"
    - from: "routes/index.tsx authenticated routes"
      to: "AppShell layout route"
      via: "RequireAuth content renders inside AppShell with an Outlet"
      pattern: "AppShell"
---

<objective>
Assemble the chrome: the 2×3 CSS-Grid **AppShell** (SHELL-01), the CSS-only
`data-collapsed` rail (SHELL-02, zero JS measurement), the responsive split
(SHELL-06 — drawer + FAB <768px, Bottombar ≥768px), the **MobileDrawer**, and
the provider/route wiring that mounts it all. Cap it with an extended Playwright
E2E shell smoke.

The Vite `/api` proxy rewrite is load-bearing — DO NOT touch vite.config.ts
(03-RESEARCH.md Pitfall 7). The existing login-dashboard E2E guards that contract.

Purpose: Turn the leaves (Plans 03–05) and providers (Plans 01–02) into the live
shell every authenticated route renders inside.
Output: AppShell + MobileDrawer + grid CSS + App.tsx + routes + extended E2E.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-layout-primitives-bottombar/03-CONTEXT.md
@.planning/phases/03-layout-primitives-bottombar/03-RESEARCH.md
@.planning/phases/03-layout-primitives-bottombar/03-UI-SPEC.md

<interfaces>
EXISTING frontend2/src/App.tsx (current order — APPEND, don't reorder):
  I18nProvider > BrowserRouter > QueryClientProvider > AppRoutes (+ DEV devtools).
  Add ShortcutsProvider (Plan 01) + ModalStackProvider (Plan 02). Per PROV-01 the
  canonical final order is Intl > Query > Auth > SSE > Toast > Shortcuts > Router, but
  Auth/SSE/Toast land in Phases 5/6 — for THIS phase mount Shortcuts + ModalStack so they
  wrap both the router chrome AND route content (both must share the SSOT/stack). Place
  ShortcutsProvider + ModalStackProvider INSIDE QueryClientProvider and AROUND BrowserRouter
  (or just inside it) so AppShell + routes both consume them; keep the existing providers'
  relative order intact so Phases 5/6 append cleanly.

EXISTING frontend2/src/routes/index.tsx: /login (public), / (RequireAuth → DashboardPage), * (placeholder).
  Restructure the authenticated branch into an AppShell LAYOUT route: <Route element={<RequireAuth><AppShell/></RequireAuth>}>
  with child routes (index → DashboardPage) rendered through AppShell's <Outlet/>. Keep /login public and
  the wildcard. DashboardPage currently renders its OWN Sidebar + header — once it lives inside AppShell,
  remove its duplicate Sidebar/header wrapper so the shell owns the chrome (the DashboardPage edit is in scope
  here as a wiring consequence; keep its stat windows + activity table content).

From Plans 03/04/05 (frontend2/src/components/layout): TopBar, Sidebar, PageHeader, Bottombar,
  Fab, F1HelpDialog, Clock, ShortcutChip. AppShell composes these.
From Plan 02: useModalStack (drawer + F1 dialog ESC ordering).

UI-SPEC bindings (read §"Layout — AppShell Grid", §"Sidebar mobile drawer", §Accessibility):
  - Grid: grid-template-areas "topbar topbar" / "sidebar main" / "sidebar bottombar";
    columns var(--sidebar-w) 1fr (232px; [data-collapsed] → 60px); rows 40px 1fr 36px; height 100dvh;
    main is the scroll container (overflow-y:auto; min-width:0 — load-bearing Silkscreen overflow guard).
    Transition grid-template-columns 160ms ease; @media (prefers-reduced-motion: reduce) → instant.
  - <768px: single column "topbar" / "main"; sidebar leaves grid → fixed off-canvas drawer
    (translateX(-100%)→0, bg-fg-ink/40 scrim); Bottombar NOT rendered (D-06); FAB renders (D-05);
    TopBar hamburger toggles the drawer.
  - Skip link "Skip to content" → main (id="main", tabIndex=-1), first focusable in DOM.
  Express the grid + [data-collapsed] + rail label/count hide as an @utility/@layer block in globals.css
  (consistent with existing .rtable / bevel utilities), OR Tailwind arbitrary grid utilities — Claude's
  discretion per CONTEXT (exact grid template values). The .nav-label/.nav-count hide rules target the
  Plan 04 hook classes.

MobileDrawer: the Navigator (Plan 04 Sidebar) rendered as a fixed overlay min(280px,86vw),
  role="dialog" aria-modal="true", focus-trapped, closed by scrim/ESC (useModalStack)/nav-selection,
  focus returns to the hamburger.

JSDOM caveat (03-RESEARCH.md Wave 0 Gaps): JSDOM does no layout / @media. Unit tests assert the
  data-collapsed ATTRIBUTE and the responsive CLASS contract (hidden md:flex / md:hidden), NOT pixel
  widths or media-query results. True breakpoint behavior → the Playwright viewport test.

Pitfall 7 (DO NOT regress): leave vite.config.ts `/api` rewrite untouched. The E2E guards it.
E2E runbook (CLAUDE.md): Postgres + backend(:8080) + dev(:5173) + seeder user; specs in e2e/*.spec.ts,
  chromium+firefox, no webServer. Auth contract: /login → Email+Password → submit /^log in$/i.
All strings via <Trans>. Tokens only; no raw hex.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AppShell 2×3 grid + data-collapsed CSS + MobileDrawer (SHELL-01/02/06)</name>
  <files>frontend2/src/components/layout/AppShell.tsx, frontend2/src/components/layout/AppShell.test.tsx, frontend2/src/components/layout/MobileDrawer.tsx, frontend2/src/components/layout/MobileDrawer.test.tsx, frontend2/src/styles/globals.css</files>
  <read_first>
    - .planning/phases/03-layout-primitives-bottombar/03-UI-SPEC.md §"Layout — AppShell Grid" + §"Sidebar mobile drawer" + §Accessibility (skip link)
    - .planning/phases/03-layout-primitives-bottombar/03-RESEARCH.md Pattern 1 + Pattern 2 (grid + CSS-only collapse) + Anti-pattern (no ResizeObserver)
    - frontend2/src/styles/globals.css (existing @utility / @layer idiom to extend)
    - frontend2/src/components/layout/{TopBar,Sidebar,PageHeader,Bottombar,Fab,F1HelpDialog}.tsx (compose these)
    - frontend2/src/components/modal/index.ts (useModalStack for the drawer)
  </read_first>
  <behavior>
    - AppShell renders TopBar, Sidebar, an <Outlet/> region, PageHeader, and the Bottombar within a grid root
    - The grid root carries data-collapsed reflecting collapse state; toggling the Sidebar's collapse control flips it
    - No ResizeObserver / offsetWidth / measured pixel width appears in AppShell (CSS-only collapse, SHELL-02)
    - globals.css defines .app-shell grid, [data-collapsed] rail columns, rail .nav-label/.nav-count hide, and a reduced-motion guard
    - A "Skip to content" link is the first focusable element and targets #main
    - MobileDrawer renders role="dialog" aria-modal, traps focus, and closes on scrim click / ESC (modal stack) / nav selection, restoring focus to the hamburger
    - The responsive class contract holds: Bottombar carries hidden md:flex, FAB carries md:hidden (asserted as strings)
  </behavior>
  <action>
    Create `MobileDrawer.tsx` exporting `MobileDrawer` (props: open, onClose, plus the Sidebar content/props):
    fixed off-canvas overlay min(280px,86vw), `role="dialog" aria-modal="true"`, `bg-fg-ink/40` scrim,
    translateX transition, focus trap + focus-restore to the invoker; `useModalStack(open, onClose)` for ESC;
    scrim click and a nav selection also close. Add `MobileDrawer.test.tsx` (dialog role, scrim-close,
    ESC-close via ModalStackProvider, focus trap basic).
    Create `AppShell.tsx` exporting `AppShell`: own a single `collapsed` boolean (useState) — the ONLY collapse
    state, no measurement. Render the `.app-shell` grid root with `data-collapsed={collapsed}`, composing
    TopBar (passing onToggleDrawer + onLogout), Sidebar (desktop, with onToggleCollapse + collapsed),
    PageHeader, `<main id="main" tabIndex={-1}>` containing the route `<Outlet/>`, and the Bottombar
    (`hidden md:flex`, onOpenHelp). Mount the Fab (`md:hidden`), the MobileDrawer (driven by a drawer-open
    boolean toggled by the TopBar hamburger), and the F1HelpDialog (open state toggled by Bottombar F1 + F1 key).
    Add the "Skip to content" visually-hidden-until-focus link as the first focusable element. Derive PageHeader
    breadcrumb segments from the current route (useLocation) with a small route→segments map (placeholder routes ok).
    Extend `globals.css` with the `.app-shell` grid + `[data-collapsed]` rail columns + rail label/count hide +
    `transition: grid-template-columns 160ms ease` + `@media (prefers-reduced-motion: reduce)` instant, mirroring
    the existing @utility/@layer style. Add `AppShell.test.tsx` (wrap in the providers + MemoryRouter): grid
    children render, toggling collapse flips data-collapsed, no measurement APIs used, Bottombar `hidden md:flex`
    + Fab `md:hidden` class contract, skip link present.
    Tokens only; no raw hex.
  </action>
  <verify>
    <automated>cd frontend2 && bunx vitest run src/components/layout/AppShell.test.tsx src/components/layout/MobileDrawer.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend2 && bunx vitest run src/components/layout/AppShell.test.tsx src/components/layout/MobileDrawer.test.tsx` exits 0 (data-collapsed toggle + responsive class contract + skip link + drawer dialog/scrim/ESC)
    - `grep -q 'data-collapsed' frontend2/src/components/layout/AppShell.tsx` and `grep -q 'data-collapsed' frontend2/src/styles/globals.css`
    - `! grep -Eq 'ResizeObserver|offsetWidth|getBoundingClientRect' frontend2/src/components/layout/AppShell.tsx` (CSS-only collapse — SHELL-02)
    - `grep -q 'grid-template-areas' frontend2/src/styles/globals.css` and `grep -q 'prefers-reduced-motion' frontend2/src/styles/globals.css`
    - `cd frontend2 && bun run lint:tsc` exits 0
  </acceptance_criteria>
  <done>AppShell renders the 2×3 grid with a CSS-only data-collapsed rail, a skip link, and the responsive drawer/FAB/Bottombar split; MobileDrawer closes via scrim/ESC; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Provider wiring + AppShell layout route + DashboardPage de-dup (App.tsx, routes)</name>
  <files>frontend2/src/App.tsx, frontend2/src/routes/index.tsx, frontend2/src/features/dashboard/DashboardPage.tsx</files>
  <read_first>
    - frontend2/src/App.tsx (current provider order — append, don't reorder)
    - frontend2/src/routes/index.tsx (route structure to restructure into a layout route)
    - frontend2/src/features/dashboard/DashboardPage.tsx (remove its duplicate Sidebar/header now the shell owns chrome)
    - frontend2/src/components/shortcuts/index.ts + frontend2/src/components/modal/index.ts (providers to mount)
  </read_first>
  <behavior>
    - App.tsx mounts ShortcutsProvider and ModalStackProvider so both AppShell chrome and route content share them
    - The existing provider relative order (Intl > Router > Query …) is preserved so Phases 5/6 append cleanly
    - The authenticated route renders DashboardPage inside AppShell via <Outlet/>; /login stays public; wildcard stays
    - DashboardPage no longer renders its own Sidebar/header wrapper (the shell owns chrome) but keeps its stat windows + activity table
    - The app still boots and the existing dashboard content renders under the shell
  </behavior>
  <action>
    Edit `App.tsx`: add `ShortcutsProvider` (Plan 01) and `ModalStackProvider` (Plan 02) around the router so
    both the AppShell chrome and the routed content consume the SSOT + modal stack; keep the existing providers'
    relative order intact (Phases 5/6 will insert Auth/SSE/Toast). Edit `routes/index.tsx`: convert the
    authenticated branch into an AppShell layout route — `<Route element={<RequireAuth><AppShell/></RequireAuth>}>`
    with an index child rendering `DashboardPage` through AppShell's `<Outlet/>`; keep `/login` public and the `*`
    placeholder. Edit `DashboardPage.tsx`: remove its own `<Sidebar/>` + brand header wrapper (the shell now owns
    chrome) and keep the stat windows + recent-activity table as the page body. Tokens only; no raw hex; strings
    stay in <Trans>. DO NOT touch vite.config.ts (Pitfall 7).
    Note: this is wiring/glue — no dedicated new test file is required for App.tsx; the AppShell unit test (Task 1)
    and the E2E (Task 3) cover the composition. Verify the full unit suite + build stay green.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test && bun run lint:tsc && bun run lint:imports && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend2 && bun run test` exits 0 (full unit suite green, including all Phase 3 specs)
    - `cd frontend2 && bun run build` exits 0 (tsc -b + vite build clean)
    - `cd frontend2 && bun run lint:imports` exits 0 (no idb/serwist/offline/sync/motion imports introduced)
    - `grep -q 'ShortcutsProvider' frontend2/src/App.tsx` and `grep -q 'ModalStackProvider' frontend2/src/App.tsx`
    - `grep -q 'AppShell' frontend2/src/routes/index.tsx`
    - `git diff --quiet frontend2/vite.config.ts` (proxy untouched — Pitfall 7)
  </acceptance_criteria>
  <done>Providers + AppShell layout route are wired; DashboardPage renders under the shell without duplicate chrome; full unit suite + build + import guard green; vite proxy untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the Playwright E2E shell smoke (SHELL-01/02 end-to-end)</name>
  <files>frontend2/e2e/login-dashboard.spec.ts</files>
  <read_first>
    - frontend2/e2e/login-dashboard.spec.ts (the spec to extend; existing login + redirect + wrong-password cases)
    - frontend2/CLAUDE.md §E2E Tests (runbook + auth contract: /login → Email+Password → /^log in$/i)
    - .planning/phases/03-layout-primitives-bottombar/03-RESEARCH.md Validation Architecture "SHELL-01..06 end-to-end" row + Pitfall 7
  </read_first>
  <action>
    Extend `frontend2/e2e/login-dashboard.spec.ts` with ONE shell-smoke test that logs in via the real
    `/login` flow (reusing the documented auth contract), then asserts the shell chrome is present (the
    TopBar banner / brand, the Sidebar Navigator, and the Bottombar shortcuts footer at desktop viewport),
    toggles the sidebar collapse control and asserts the grid root's `data-collapsed` attribute flips,
    opens the F1 help dialog (via the F1 chip or F1 key) and asserts the KEYBOARD SHORTCUTS dialog is visible,
    then presses ESC and asserts the dialog closes (and the app stays on the dashboard — ESC did not log out).
    Keep the existing three tests untouched (they guard the /api rewrite + auth). Do NOT add a webServer to
    playwright.config.ts (the spec expects the running dev stack per CLAUDE.md). This test runs against the
    live stack; in CI without the stack it is skipped/locally-run per the existing posture — note that in a
    comment. The unit suite + build (Task 2) is the phase's hard gate; this E2E is the end-to-end smoke.
  </action>
  <verify>
    <automated>cd frontend2 && npx playwright test --list e2e/login-dashboard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend2 && npx playwright test --list e2e/login-dashboard.spec.ts` lists the new shell-smoke test alongside the original three (parse/registration check that does not require the dev stack)
    - `grep -q 'data-collapsed' frontend2/e2e/login-dashboard.spec.ts` (collapse assertion present)
    - `grep -Eiq 'KEYBOARD SHORTCUTS|F1' frontend2/e2e/login-dashboard.spec.ts` (F1 help open/close asserted)
    - `git diff --quiet frontend2/playwright.config.ts` (no webServer added; config untouched)
    - When the dev stack is up: `cd frontend2 && E2E_USER=seeder@test.local E2E_PASS=password123 bun run test:e2e` passes the shell-smoke on chromium + firefox (manual/local per CLAUDE.md runbook)
  </acceptance_criteria>
  <done>The E2E spec gains a shell-smoke (login → shell renders → collapse toggles → F1 opens → ESC closes) without altering the proxy-guarding tests or the playwright config.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser viewport/route → shell composition | Route + viewport drive which chrome renders; no untrusted data crosses |
| /api proxy rewrite (vite) | Load-bearing rewrite binds cookies; must not regress |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-15 | Tampering | /api proxy rewrite regression | mitigate | vite.config.ts left untouched (Pitfall 7); existing login-dashboard E2E tests guard the rewrite + cookie-JWT login contract |
| T-03-16 | Denial of Service (self-inflicted) | ESC logout via drawer/dialog | mitigate | Drawer + F1 dialog push onto the modal stack; ESC pops the top only; E2E asserts ESC closes the dialog and stays on the dashboard (no logout) |
| T-03-17 | Resource exhaustion | layout thrash from JS-measured collapse | mitigate | CSS-only data-collapsed; AppShell uses no ResizeObserver/offsetWidth (asserted by grep gate) |
| T-03-18 | Information disclosure | PII logging in shell | mitigate | No console logging of user/session data ported from legacy shell (03-RESEARCH.md V7) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this plan; Package Legitimacy Audit: N/A (03-RESEARCH.md) |

Client-side chrome assembly: no new endpoints, no auth changes (RequireAuth
already gates the shell on the workspaces probe — not weakened here).
</threat_model>

<verification>
- `cd frontend2 && bun run test` (full unit suite, all Phase 3 specs) green
- `cd frontend2 && bun run build` clean
- `cd frontend2 && bun run lint:imports` green
- `git diff --quiet frontend2/vite.config.ts frontend2/playwright.config.ts` (proxy + e2e config untouched)
- E2E shell-smoke registered (`npx playwright test --list`); passes on the live dev stack locally
</verification>

<success_criteria>
- AppShell renders the 2×3 grid; collapse is a single CSS-only data-collapsed toggle with no JS measurement
- <768px renders the drawer + FAB and no Bottombar; ≥768px renders the Bottombar and no FAB (class contract + E2E)
- Providers + layout route mount the shell; the full unit suite + build are green; the vite proxy is untouched
- The E2E smoke proves login → shell → collapse → F1 open → ESC close end-to-end
</success_criteria>

<output>
Create `.planning/phases/03-layout-primitives-bottombar/03-06-SUMMARY.md` when done
</output>
