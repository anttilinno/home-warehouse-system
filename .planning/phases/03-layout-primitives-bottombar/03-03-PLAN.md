---
phase: 03-layout-primitives-bottombar
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - frontend2/src/components/layout/ShortcutChip.tsx
  - frontend2/src/components/layout/ShortcutChip.test.tsx
  - frontend2/src/components/layout/Clock.tsx
  - frontend2/src/components/layout/Clock.test.tsx
autonomous: true
requirements: [BAR-01, BAR-04, SHELL-05]
must_haves:
  truths:
    - "A ShortcutChip renders its [KEY] glyph cell + UPPERCASE label as a focusable <button> with aria-keyshortcuts set to its key"
    - "A danger chip renders danger styling (bg-danger-bg text-danger); the current/focused chip renders the blue accent face"
    - "Chip labels and key glyphs use Plex Sans/Plex Mono, never Silkscreen (hard rule 1)"
    - "The Clock leaf displays SESSION (elapsed since mount, hh:mm:ss) and LOCAL (wall clock) and advances each second"
    - "A clock tick re-renders ONLY the Clock leaf, not a parent shell wrapper"
  artifacts:
    - path: "frontend2/src/components/layout/ShortcutChip.tsx"
      provides: "The retro key-cap [KEY] LABEL chip (BAR-04)"
      exports: ["ShortcutChip"]
    - path: "frontend2/src/components/layout/Clock.tsx"
      provides: "Isolated single-interval SESSION/LOCAL clock leaf"
      exports: ["Clock"]
  key_links:
    - from: "ShortcutChip"
      to: "Shortcut type"
      via: "props typed from the shortcuts module"
      pattern: "from \"@/components/shortcuts\""
    - from: "Clock"
      to: "single setInterval"
      via: "one interval owned by this leaf, cleared on unmount"
      pattern: "setInterval"
---

<objective>
Build the two leaf chrome components the Bottombar, FAB, PageHeader, and F1
dialog all reuse: the retro key-cap **ShortcutChip** (BAR-04's re-anchored
`[KEY] LABEL`) and the **Clock** leaf (BAR-01/SHELL-05 SESSION/LOCAL readout).
The Clock is deliberately isolated so a 1s tick never re-renders the shell
(03-RESEARCH.md Pitfall 5).

Purpose: One chip component = visual consistency across Bottombar and F1 dialog.
One isolated Clock = no whole-shell re-render every second.
Output: ShortcutChip + Clock + their tests under `src/components/layout/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-layout-primitives-bottombar/03-CONTEXT.md
@.planning/phases/03-layout-primitives-bottombar/03-RESEARCH.md
@.planning/phases/03-layout-primitives-bottombar/03-UI-SPEC.md

<interfaces>
From frontend2/src/components/shortcuts/index.ts (Plan 01):
  interface Shortcut { key: string; label: string; action: () => void; danger?: boolean }

UI-SPEC §"Key-cap chip" (BINDING):
  Container: inline-flex items-center gap-sp-1 border-2 border-border-ink bg-bg-panel px-sp-2 py-[2px] bevel-raised-ink
  Key glyph: font-mono text-[12px] font-semibold in an inset cell (bg-bg-panel-2, border border-border-ink) e.g. N, F1
  Label: font-body text-[11px] font-bold uppercase tracking-[0.1em] text-fg-ink
  Hover: brightness-103. Press: active:translate-x-px active:translate-y-px active:bg-bg-pressed active:bevel-pressed
  Current/focused chip: bg-titlebar-blue face. danger:true → bg-danger-bg text-danger
  Chips are <button>, focusable, aria-keyshortcuts="{key}".

UI-SPEC §"SESSION / LOCAL clocks" (BINDING):
  Two font-mono text-[12px] tabular-nums readouts, each with an 11px uppercase Plex Sans key.
  SESSION = elapsed since mount (login-time source is Phase 5), hh:mm:ss.
  LOCAL = wall clock via toLocaleTimeString("et-EE").
  One setInterval (1s) updates both. tabular-nums prevents column jitter.

03-RESEARCH.md Pattern 6: interval + state MUST live in the leaf, not the shell.
Existing reference for bevel utilities: frontend2/src/components/retro/BevelButton.tsx
(bevel-raised-ink, active:* press idiom — reuse the same classes).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ShortcutChip key-cap component (BAR-04)</name>
  <files>frontend2/src/components/layout/ShortcutChip.tsx, frontend2/src/components/layout/ShortcutChip.test.tsx</files>
  <read_first>
    - .planning/phases/03-layout-primitives-bottombar/03-UI-SPEC.md §Color "Resolved: BAR-04 key-caps" + §Bottombar "Key-cap chip"
    - frontend2/src/components/retro/BevelButton.tsx (bevel/press class idiom to reuse)
    - frontend2/src/components/shortcuts/index.ts (Shortcut type)
    - .planning/phases/03-layout-primitives-bottombar/03-RESEARCH.md Pitfall 6 (Silkscreen never on small chrome)
  </read_first>
  <behavior>
    - ShortcutChip renders a <button> containing the key glyph and an uppercase label
    - The button has aria-keyshortcuts equal to the shortcut key
    - Clicking the chip invokes the shortcut action once
    - danger=true applies bg-danger-bg + text-danger classes
    - A `current`/focused prop applies bg-titlebar-blue face
    - Neither the glyph nor the label uses font-display (Silkscreen) — assert no font-display class
  </behavior>
  <action>
    Create `ShortcutChip.tsx` exporting `ShortcutChip`. Props: the Shortcut fields needed
    (`keyLabel`/`key`, `label`, `onActivate`, `danger?`, `current?`) — type from the imported
    `Shortcut` shape where practical. Render a focusable `<button type="button">` with the
    UI-SPEC key-cap chrome: inset key-glyph cell (font-mono 12px) + uppercase label
    (font-body 11px bold tracking-[0.1em] text-fg-ink), 2px ink border, `bevel-raised-ink`,
    the BevelButton press idiom (`active:translate-x-px active:translate-y-px active:bg-bg-pressed active:bevel-pressed`).
    `aria-keyshortcuts={key}`. `danger` → `bg-danger-bg text-danger`; `current` → `bg-titlebar-blue` face;
    default → `bg-bg-panel`. Wire onClick to call the action. Use ONLY Phase 2 token utilities; no raw hex;
    no font-display anywhere (Pitfall 6).
    Add `ShortcutChip.test.tsx` covering render, aria-keyshortcuts, click-fires-action, danger styling,
    current styling, and the no-font-display assertion.
  </action>
  <verify>
    <automated>cd frontend2 && bunx vitest run src/components/layout/ShortcutChip.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend2 && bunx vitest run src/components/layout/ShortcutChip.test.tsx` exits 0 (render + aria-keyshortcuts + click + danger + current + no-font-display)
    - `grep -q 'aria-keyshortcuts' frontend2/src/components/layout/ShortcutChip.tsx`
    - `grep -vq 'font-display' frontend2/src/components/layout/ShortcutChip.tsx` (no Silkscreen on chip — Pitfall 6)
    - `grep -Eiq 'bg-\[#|text-\[#|#[0-9a-f]{6}' frontend2/src/components/layout/ShortcutChip.tsx || true` then confirm NO raw hex: `! grep -Eq '#[0-9a-fA-F]{6}' frontend2/src/components/layout/ShortcutChip.tsx`
    - `cd frontend2 && bun run lint:tsc` exits 0
  </acceptance_criteria>
  <done>ShortcutChip renders the retro key-cap with aria-keyshortcuts, danger/current variants, fires its action, and uses no Silkscreen or raw hex; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Isolated Clock leaf (BAR-01, SHELL-05, Pitfall 5)</name>
  <files>frontend2/src/components/layout/Clock.tsx, frontend2/src/components/layout/Clock.test.tsx</files>
  <read_first>
    - .planning/phases/03-layout-primitives-bottombar/03-RESEARCH.md Pattern 6 + Pitfall 5 (clock isolation)
    - .planning/phases/03-layout-primitives-bottombar/03-UI-SPEC.md §"SESSION / LOCAL clocks"
    - frontend2/src/features/dashboard/DashboardPage.tsx (font-mono tabular-nums usage idiom)
  </read_first>
  <behavior>
    - Clock renders a SESSION key + hh:mm:ss value and a LOCAL key + hh:mm:ss value
    - Under fake timers, advancing 1s updates the rendered values (SESSION increments)
    - A parent component wrapping <Clock/> does NOT re-render on a clock tick (assert parent render-count stays 1 across a tick)
    - The clock's interval is cleared on unmount (no leaked timer)
  </behavior>
  <action>
    Create `Clock.tsx` exporting `Clock`. Own a SINGLE `setInterval(…, 1000)` inside this leaf
    (Pattern 6); state holds the mount start timestamp + current Date. SESSION = elapsed since
    mount formatted hh:mm:ss (zero-padded); LOCAL = `new Date().toLocaleTimeString("et-EE")`.
    Render both as `font-mono text-[12px] tabular-nums` values, each preceded by an 11px uppercase
    Plex Sans key (`SESSION`, `LOCAL`). Accept an optional prop to render a single readout (so the
    PageHeader can reuse the same SESSION timer) — keep it one component, parameterized. Clear the
    interval on unmount. Wrap user-facing key strings in `<Trans>` for i18n parity (lingui macro).
    Add `Clock.test.tsx` using vitest fake timers: render inside a parent spy component, advance timers
    1s, assert the SESSION value changed AND the parent render-count stayed at 1 (isolation proof,
    Pitfall 5), and assert the interval is cleared on unmount.
  </action>
  <verify>
    <automated>cd frontend2 && bunx vitest run src/components/layout/Clock.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend2 && bunx vitest run src/components/layout/Clock.test.tsx` exits 0 including the parent-render-count-stays-1 isolation assertion
    - `grep -q 'tabular-nums' frontend2/src/components/layout/Clock.tsx`
    - `grep -q 'clearInterval' frontend2/src/components/layout/Clock.tsx`
    - `! grep -Eq '#[0-9a-fA-F]{6}' frontend2/src/components/layout/Clock.tsx` (no raw hex)
    - `cd frontend2 && bun run lint:tsc` exits 0 and `bun run lint:imports` exits 0
  </acceptance_criteria>
  <done>Clock leaf shows SESSION/LOCAL, ticks each second, re-renders only itself (proven by render-count test), and clears its interval on unmount; tsc + import guard clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shortcut label/key data → DOM | Chip renders label/key strings; rendered as escaped JSX text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering/XSS | ShortcutChip label/key rendering | mitigate | Labels/keys rendered as escaped JSX text expressions; no dangerouslySetInnerHTML |
| T-03-07 | Resource exhaustion | Clock interval leak | mitigate | Single interval owned by the leaf, clearInterval on unmount; unmount-clears-timer test |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this plan (Package Legitimacy Audit: N/A) |

Client-side chrome only; no endpoints, no credentials, no untrusted parsing
beyond keyboard events handled in Plan 01.
</threat_model>

<verification>
- `cd frontend2 && bunx vitest run src/components/layout/ShortcutChip.test.tsx src/components/layout/Clock.test.tsx` green
- `cd frontend2 && bun run lint:tsc` clean
- `cd frontend2 && bun run lint:imports` green
</verification>

<success_criteria>
- ShortcutChip is the reusable retro key-cap (BAR-04), focusable with aria-keyshortcuts, danger/current variants, no Silkscreen
- Clock shows SESSION + LOCAL, ticks every second, and is isolated so the shell never re-renders on a tick
</success_criteria>

<output>
Create `.planning/phases/03-layout-primitives-bottombar/03-03-SUMMARY.md` when done
</output>
