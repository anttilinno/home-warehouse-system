---
phase: 03-layout-primitives-bottombar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend2/src/components/shortcuts/isEditableTarget.ts
  - frontend2/src/components/shortcuts/isEditableTarget.test.ts
  - frontend2/src/components/shortcuts/ShortcutsContext.tsx
  - frontend2/src/components/shortcuts/useShortcuts.ts
  - frontend2/src/components/shortcuts/useShortcuts.test.tsx
  - frontend2/src/components/shortcuts/index.ts
autonomous: true
requirements: [BAR-02, BAR-03, TUI-01]
must_haves:
  truths:
    - "isEditableTarget returns true for INPUT, TEXTAREA, SELECT, and contenteditable (incl. nested) targets, false for body/div"
    - "A single-letter shortcut does NOT fire while the user is typing in any of the four editable surfaces"
    - "useShortcuts(id, bindings) registers on mount and unregisters on unmount; the merged list is the union of all registered groups"
    - "Re-registering the same stable id replaces that group's bindings without unmount/remount flicker"
    - "A single document-level keydown listener owned by the provider dispatches a matching shortcut exactly once (no double-fire under StrictMode)"
  artifacts:
    - path: "frontend2/src/components/shortcuts/isEditableTarget.ts"
      provides: "Pure editable-target guard function"
      exports: ["isEditableTarget"]
    - path: "frontend2/src/components/shortcuts/ShortcutsContext.tsx"
      provides: "ShortcutsProvider SSOT + keyed registry + single keydown dispatcher"
      exports: ["ShortcutsProvider", "useShortcutsContext", "Shortcut"]
    - path: "frontend2/src/components/shortcuts/useShortcuts.ts"
      provides: "Route-facing register hook (caller id or useId() fallback)"
      exports: ["useShortcuts"]
  key_links:
    - from: "ShortcutsContext keydown dispatcher"
      to: "isEditableTarget"
      via: "guard call before matching any single-letter key"
      pattern: "isEditableTarget\\(e(vent)?\\.target\\)"
    - from: "useShortcuts hook"
      to: "ShortcutsContext register/unregister"
      via: "useEffect register on mount, return unregister"
      pattern: "return \\(\\) => unregister"
---

<objective>
Build the keyboard-shortcut single source of truth (SSOT) and its input-focus
guard — the spine both the Bottombar (Plan 05) and the FAB consume (D-08). This
is the FIRST keydown-wiring commit in the phase, so the `isEditableTarget` guard
and its four-surface regression suite ship HERE, before any chrome (Success
Criterion 3, Wave 0 mandate from 03-VALIDATION.md).

Purpose: A correct, leak-free, flicker-free shortcut registry. Every downstream
chrome surface reads from this one registry; no second action source exists.
Output: `src/components/shortcuts/` module — guard, provider, hook, tests, barrel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-layout-primitives-bottombar/03-CONTEXT.md
@.planning/phases/03-layout-primitives-bottombar/03-RESEARCH.md
@.planning/phases/03-layout-primitives-bottombar/03-VALIDATION.md

<interfaces>
<!-- Legacy reference (STRUCTURE only — strip "use client", restyle nothing here): -->
From frontend/components/layout/shortcuts-context.tsx (verbatim structural port):
  interface Shortcut { key: string; label: string; action: () => void; danger?: boolean }
  ShortcutsProvider holds Record<string, Shortcut[]> in state; register(id, s) sets
  groups[id]=s; unregister(id) deletes the key; shortcuts = Object.values(groups).flat().

From frontend/lib/hooks/use-keyboard-shortcuts.ts (guard + stale-closure pattern):
  isInputField(target): tagName === INPUT|TEXTAREA|SELECT (extend to contenteditable).
  shortcutsRef = useRef(shortcuts); effect keeps ref.current current so the single
  keydown handler reads live bindings WITHOUT being re-created each render.

Existing test setup: frontend2/src/test-utils.tsx imports @testing-library/jest-dom.
Vitest config: jsdom, globals, excludes **/e2e/**. Run a single file with
`cd frontend2 && bunx vitest run <path>`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: isEditableTarget guard + four-surface regression suite (Wave 0)</name>
  <files>frontend2/src/components/shortcuts/isEditableTarget.ts, frontend2/src/components/shortcuts/isEditableTarget.test.ts</files>
  <read_first>
    - frontend/lib/hooks/use-keyboard-shortcuts.ts (isInputField source of truth)
    - .planning/phases/03-layout-primitives-bottombar/03-CONTEXT.md §specifics (guard spec: tagName + isContentEditable + closest)
    - .planning/phases/03-layout-primitives-bottombar/03-RESEARCH.md Pattern 4 + Pitfall 1
    - frontend2/src/components/retro/__tests__/retro.test.tsx (vitest + RTL + user-event test idiom)
  </read_first>
  <behavior>
    - isEditableTarget(<input>) === true
    - isEditableTarget(<textarea>) === true
    - isEditableTarget(<select>) === true
    - isEditableTarget(element with contenteditable="true") === true
    - isEditableTarget(child node inside a contenteditable="true" ancestor) === true (via closest)
    - isEditableTarget(<div>) === false; isEditableTarget(document.body) === false; isEditableTarget(null) === false
  </behavior>
  <action>
    Create `isEditableTarget.ts` exporting a pure `isEditableTarget(target: EventTarget | null): boolean`.
    Guard: return false unless target is an HTMLElement; then true when tagName is
    INPUT, TEXTAREA, or SELECT, OR `target.isContentEditable`, OR
    `target.closest('[contenteditable="true"]') !== null` (covers nested rich-text per D-06 §specifics).
    Create `isEditableTarget.test.ts` asserting all four editable surfaces return true
    (mount real DOM nodes), plus the nested-contenteditable case via a child node, plus
    div/body/null returning false. This is the headline BAR-03 regression suite — it must
    exist and pass in this commit (first keydown-wiring commit of the phase).
  </action>
  <verify>
    <automated>cd frontend2 && bunx vitest run src/components/shortcuts/isEditableTarget.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend2 && bunx vitest run src/components/shortcuts/isEditableTarget.test.ts` exits 0 with ≥7 passing assertions (4 editable surfaces + nested + 3 negatives)
    - `grep -q "closest('\[contenteditable" frontend2/src/components/shortcuts/isEditableTarget.ts` (nested-contenteditable branch present)
    - `cd frontend2 && bun run lint:tsc` exits 0
  </acceptance_criteria>
  <done>Guard module + four-surface regression suite exist and pass; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ShortcutsProvider SSOT + single keydown dispatcher + useShortcuts hook</name>
  <files>frontend2/src/components/shortcuts/ShortcutsContext.tsx, frontend2/src/components/shortcuts/useShortcuts.ts, frontend2/src/components/shortcuts/useShortcuts.test.tsx, frontend2/src/components/shortcuts/index.ts</files>
  <read_first>
    - frontend/components/layout/shortcuts-context.tsx (verbatim structural port — strip "use client")
    - frontend/lib/hooks/use-keyboard-shortcuts.ts (shortcutsRef stale-closure pattern, Pitfall 3)
    - .planning/phases/03-layout-primitives-bottombar/03-RESEARCH.md Pattern 3 + Pitfall 2 (single-owner listener, StrictMode cleanup)
    - orchestrator resolution #3 (id source): caller-supplied id with useId() fallback
  </read_first>
  <behavior>
    - useShortcuts("items", [N]) then unmount: merged list contains N while mounted, empty after unmount
    - Two components register under distinct ids → merged list is the union of both groups
    - Re-rendering a component that re-registers the SAME id with new bindings replaces (not appends) that group
    - A document keydown for a registered single-letter key fires that shortcut's action EXACTLY ONCE
    - A document keydown while focus is in an <input> does NOT fire the action (guard wired into the dispatcher)
    - A keydown with metaKey/ctrlKey/altKey held does NOT fire single-letter shortcuts
  </behavior>
  <action>
    Create `ShortcutsContext.tsx`: port the legacy provider verbatim in structure —
    `Shortcut { key; label; action; danger? }`; `groups` state Record<string, Shortcut[]>;
    `register`/`unregister` useCallback; merged `shortcuts = useMemo(Object.values(groups).flat())`.
    Add the SINGLE document-level keydown dispatcher INSIDE the provider (Pitfall 2 — exactly one
    owner): keep a `shortcutsRef` synced via effect so the handler reads live bindings without
    re-subscribing each render; in the handler bail on meta/ctrl/alt, bail on `isEditableTarget(e.target)`,
    then match `s.key.toUpperCase() === e.key.toUpperCase()`, `preventDefault()`, run `action()`. Always
    return the removeEventListener cleanup (StrictMode no-op when correct). Export `ShortcutsProvider`,
    `useShortcutsContext` (throws outside provider), and the `Shortcut` type.
    Create `useShortcuts.ts`: `useShortcuts(id: string | undefined, bindings: Shortcut[])` — default the id
    from `useId()` when omitted (orchestrator resolution #3); effect registers on mount / when bindings or id
    change, returns `unregister`. Document that callers pass a stable route name for flicker-free re-registration
    (TUI-01); inline-array callers should useMemo (Pitfall 3) — note this in a comment.
    Create `index.ts` barrel re-exporting the guard, provider, context hook, useShortcuts, and Shortcut type.
    Add tests in `useShortcuts.test.tsx` covering every behavior above (render hook via RTL with the provider
    wrapper; assert action fires exactly once with a vi.fn; assert guard suppression by dispatching keydown
    from a focused input).
  </action>
  <verify>
    <automated>cd frontend2 && bunx vitest run src/components/shortcuts/useShortcuts.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend2 && bunx vitest run src/components/shortcuts/useShortcuts.test.tsx` exits 0 with all behaviors passing (register/unregister/merge/replace/fire-once/guard-suppress/modifier-bail)
    - `grep -q "isEditableTarget" frontend2/src/components/shortcuts/ShortcutsContext.tsx` (guard wired into dispatcher)
    - `grep -q "removeEventListener" frontend2/src/components/shortcuts/ShortcutsContext.tsx` (cleanup present — Pitfall 2)
    - `grep -q "useId" frontend2/src/components/shortcuts/useShortcuts.ts` (id fallback — resolution #3)
    - `cd frontend2 && bun run lint:tsc` exits 0 and `bun run lint:imports` exits 0
  </acceptance_criteria>
  <done>Provider, single dispatcher, and hook exist with the full behavior suite green; one listener owner; tsc + import guard clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM keyboard event → app | Untrusted keystroke targets cross into the dispatcher; `isEditableTarget` decides whether a single-letter shortcut may run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | keydown dispatcher | mitigate | Shortcut `action`s are app-defined closures registered by feature code, never built from event/URL data — no data-driven dispatch |
| T-03-02 | Denial of Service (self-inflicted) | single-letter shortcuts firing mid-typing | mitigate | `isEditableTarget` guard ships in this first keydown commit; four-surface regression suite proves suppression |
| T-03-03 | Resource exhaustion | leaked keydown listeners across StrictMode/route churn | mitigate | Exactly one listener owner (provider); effect always returns removeEventListener cleanup; fire-exactly-once test guards double-fire |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this plan (03-RESEARCH.md Package Legitimacy Audit: N/A); no install surface |

Phase 3 is client-side chrome: no new endpoints, no auth changes. The keyboard
handler never intercepts credentials; all rendering is through React (no
dangerouslySetInnerHTML), so no XSS sink is introduced.
</threat_model>

<verification>
- `cd frontend2 && bunx vitest run src/components/shortcuts/` green (both files)
- `cd frontend2 && bun run lint:tsc` clean
- `cd frontend2 && bun run lint:imports` green (no idb/serwist/offline/sync imports introduced)
</verification>

<success_criteria>
- isEditableTarget guard exists with a passing four-surface (+nested) regression suite
- ShortcutsProvider exposes a keyed registry with a single keydown dispatcher that fires matching shortcuts exactly once and suppresses while typing
- useShortcuts(id?, bindings) registers/unregisters with a useId() fallback; merged list is the union of registered groups
</success_criteria>

<output>
Create `.planning/phases/03-layout-primitives-bottombar/03-01-SUMMARY.md` when done
</output>
