---
phase: 58
plan: 04
type: execute
wave: 3
depends_on: ["58-03"]
files_modified:
  - frontend2/src/features/taxonomy/TaxonomyPage.tsx
  - frontend2/src/features/taxonomy/tree/TreeNode.tsx
  - frontend2/src/features/taxonomy/tree/TaxonomyTree.tsx
  - frontend2/src/features/taxonomy/tabs/CategoriesTab.tsx
  - frontend2/src/features/taxonomy/tabs/LocationsTab.tsx
  - frontend2/src/features/taxonomy/tabs/ContainersTab.tsx
  - frontend2/src/features/taxonomy/__tests__/TaxonomyPage.test.tsx
  - frontend2/src/features/taxonomy/__tests__/ContainersTab.test.tsx
  - frontend2/src/routes/index.tsx
  - frontend2/src/locales/en/messages.po
  - frontend2/src/locales/et/messages.po
autonomous: false
requirements: [TAX-01, TAX-02, TAX-03, TAX-04, TAX-05, TAX-06, TAX-07, TAX-08, TAX-09, TAX-10, TAX-11, TAX-12]
tags: [taxonomy, page, tabs, tree, route, i18n, human-verify]

must_haves:
  truths:
    - "Navigating to /taxonomy shows the TAXONOMY page with three tabs: CATEGORIES, LOCATIONS, CONTAINERS"
    - "URL hash #categories / #locations / #containers syncs with the active tab"
    - "CategoriesTab and LocationsTab render a hierarchical tree with 24px indent per depth, expand/collapse chevrons, and per-row Edit/Archive buttons"
    - "ContainersTab renders containers grouped by location with a location filter"
    - "Clicking + NEW CATEGORY opens the slide-over panel; successful create closes panel and refreshes tree"
    - "Clicking Archive on a row opens the archive-first dialog; Archive action soft-archives; delete permanently opens hard-delete dialog"
    - "Attempting to delete a category with children yields the 'Move or delete child nodes first.' toast"
    - "Empty states render RetroEmptyState with a primary CTA for each tab"
    - "All user-visible strings are in Lingui catalogs (EN and ET)"
  artifacts:
    - path: "frontend2/src/features/taxonomy/TaxonomyPage.tsx"
      provides: "/taxonomy route entry + RetroTabs wiring"
      exports: ["default TaxonomyPage"]
    - path: "frontend2/src/features/taxonomy/tree/TreeNode.tsx"
      provides: "Recursive tree row with chevron + actions"
      exports: ["TreeNode"]
    - path: "frontend2/src/features/taxonomy/tree/TaxonomyTree.tsx"
      provides: "Tree wrapper with keyboard nav"
      exports: ["TaxonomyTree"]
    - path: "frontend2/src/features/taxonomy/tabs/CategoriesTab.tsx"
      provides: "Categories tree tab body"
      exports: ["CategoriesTab"]
    - path: "frontend2/src/features/taxonomy/tabs/LocationsTab.tsx"
      provides: "Locations tree tab body"
      exports: ["LocationsTab"]
    - path: "frontend2/src/features/taxonomy/tabs/ContainersTab.tsx"
      provides: "Containers grouped-by-location tab body"
      exports: ["ContainersTab"]
  key_links:
    - from: "frontend2/src/routes/index.tsx"
      to: "TaxonomyPage"
      via: "<Route path='taxonomy' element={<TaxonomyPage />} /> inside authed AppShell"
      pattern: "path=\"taxonomy\""
    - from: "TaxonomyPage.tsx"
      to: "useHashTab"
      via: "tab <-> window.location.hash sync"
      pattern: "useHashTab"
    - from: "CategoriesTab.tsx"
      to: "EntityPanel + ArchiveDeleteFlow"
      via: "imperative refs opened from row actions / + NEW CATEGORY button"
      pattern: "entityPanelRef|archiveDeleteFlowRef"
---

<objective>
Ship the Taxonomy page: `/taxonomy` route + three tab bodies (Categories tree, Locations tree, Containers grouped) + the recursive TreeNode/TaxonomyTree components + integration tests + Lingui catalog extraction + a human-verify checkpoint covering all 12 TAX-* requirements end-to-end.

Purpose: Assemble Plans 01/02/03 into the user-visible feature. This plan composes pre-built primitives — it introduces zero new business logic beyond tree rendering, URL hash sync wiring, and tab orchestration.

Output: 6 source files, 2 integration tests, 1 route edit, EN+ET catalog entries, and a human checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/58-taxonomy-categories-locations-containers/58-CONTEXT.md
@.planning/phases/58-taxonomy-categories-locations-containers/58-UI-SPEC.md
@.planning/phases/58-taxonomy-categories-locations-containers/58-PATTERNS.md
@frontend2/src/pages/ApiDemoPage.tsx
@frontend2/src/components/retro/RetroTabs.tsx
@frontend2/src/components/retro/RetroEmptyState.tsx
@frontend2/src/routes/index.tsx
@frontend2/src/features/taxonomy/hooks/useHashTab.ts
@frontend2/src/features/taxonomy/hooks/useCategoriesTree.ts
@frontend2/src/features/taxonomy/hooks/useLocationsTree.ts
@frontend2/src/features/taxonomy/hooks/useContainersByLocation.ts
@frontend2/src/features/taxonomy/hooks/useCategoryMutations.ts
@frontend2/src/features/taxonomy/hooks/useLocationMutations.ts
@frontend2/src/features/taxonomy/hooks/useContainerMutations.ts
@frontend2/src/features/taxonomy/panel/EntityPanel.tsx
@frontend2/src/features/taxonomy/actions/ArchiveDeleteFlow.tsx
@frontend2/src/features/taxonomy/tree/buildTree.ts

<interfaces>
From `@/components/retro`:
```typescript
export function RetroTabs(props: {
  tabs: { key: string; label: string }[];
  activeTab: string;
  onTabChange: (key: string) => void;
}): JSX.Element;
export function RetroEmptyState(props: { heading: string; body: string; action?: ReactNode }): JSX.Element;
export function RetroPanel(props): JSX.Element;
export function RetroButton(props): JSX.Element;
export function RetroBadge(props): JSX.Element;
export function HazardStripe(): JSX.Element;
```

From `lucide-react`: `ChevronRight`, `ChevronDown`, `Pencil`, `Archive`, `Undo2`, `Plus`, `X`.

From `frontend2/src/routes/index.tsx` (existing structure):
```tsx
<Route element={<RequireAuth><AppShell /></RequireAuth>} errorElement={<ErrorBoundaryPage />}>
  <Route index element={<DashboardPage />} />
  <Route path="items" element={<ItemsPage />} />
  {/* add taxonomy route here */}
</Route>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TreeNode + TaxonomyTree (recursive rendering + keyboard nav)</name>
  <files>frontend2/src/features/taxonomy/tree/TreeNode.tsx, frontend2/src/features/taxonomy/tree/TaxonomyTree.tsx</files>
  <read_first>
    - .planning/phases/58-taxonomy-categories-locations-containers/58-UI-SPEC.md (Layout Contract — tree rendering; Typography — archived-node style; Color — amber rail on expanded row; Interaction Contracts — tree expand/collapse keyboard)
    - frontend2/src/features/taxonomy/tree/buildTree.ts (TreeNode<T> type)
    - frontend2/src/components/retro/RetroButton.tsx (icon button styling)
    - frontend2/src/components/retro/RetroBadge.tsx (ARCHIVED badge usage)
  </read_first>
  <action>
    **File 1: `frontend2/src/features/taxonomy/tree/TreeNode.tsx`**

    Recursive row component. Props:

    ```typescript
    export interface TreeNodeProps<T extends { id: string; name: string; archived_at?: string | null }> {
      node: TreeNode<T>;
      isExpanded: boolean;
      expandedIds: Set<string>;
      onToggle: (id: string) => void;
      onEdit: (item: T) => void;
      onArchive: (item: T) => void;
      onRestore: (item: T) => void;
      activeEditId?: string | null; // row being edited → render amber left rail
    }
    ```

    Rendering:
    - Row container: `<div role="treeitem" aria-expanded={hasChildren ? isExpanded : undefined} tabIndex={0}>` with `padding-inline-start: {depth * 24}px`, `min-height: 44px` mobile / `36px` lg, left border rail (2px amber) when `activeEditId === node.id`.
    - Expand chevron: `<button aria-label={isExpanded ? t\`Collapse ${name}\` : t\`Expand ${name}\`} min-h-[44px] min-w-[44px]>` showing `<ChevronDown>` or `<ChevronRight>` (16px inside), only rendered when `hasChildren`.
    - Name: `<span className={archived ? "line-through text-retro-gray" : ""}>` with `font-mono` for short_code in parentheses if present (locations/containers only). Use `font-sans` for node.name.
    - ARCHIVED badge: `<RetroBadge variant="neutral" className="font-mono">{t\`ARCHIVED\`}</RetroBadge>` when `node.archived_at`.
    - Action buttons container (icon-only on mobile, icon + label `≥lg`):
      - Edit: pencil + `EDIT` with `aria-label={t\`Edit ${name}\`}`, disabled when row is already being edited elsewhere
      - Archive (only when not archived): archive icon + `ARCHIVE` with `aria-label={t\`Archive ${name}\`}`
      - Restore (only when archived): Undo2 icon + `RESTORE` with `aria-label={t\`Restore ${name}\`}`
    - Recursive children: `{isExpanded && node.children.map((c) => <TreeNode ... />)}`

    **File 2: `frontend2/src/features/taxonomy/tree/TaxonomyTree.tsx`**

    Wrapper. Props:

    ```typescript
    export interface TaxonomyTreeProps<T> {
      roots: TreeNode<T>[];
      archivedRoots?: TreeNode<T>[]; // rendered under collapsed "ARCHIVED (N)" section when showArchived is off
      showArchived: boolean;
      onEdit, onArchive, onRestore: callbacks per TreeNode
      activeEditId?: string | null;
    }
    ```

    Responsibilities:
    - `<ul role="tree">` wrapper
    - Maintain local state `expandedIds: Set<string>` and `focusedId: string | null`
    - `onToggle(id)`: flip membership in expandedIds
    - Keyboard handler on the tree root:
      - ArrowDown: focus next visible row
      - ArrowUp: focus previous visible row
      - ArrowRight: expand focused (or move to first child if already expanded)
      - ArrowLeft: collapse focused (or move to parent if already collapsed)
      - Home: focus first visible row
      - End: focus last visible row
      - Enter: trigger `onEdit(focusedNode.item)`
    - Flatten visible rows helper: computes the linear visible order based on `expandedIds`
    - Render archived section: when `!showArchived` and `archivedRoots.length > 0`, render a single collapsible header row "`ARCHIVED (${N})`" at the bottom of the tree that expands to show archived roots and their descendants.

    Use the `node.depth` from `buildTree` output for `padding-inline-start`.
  </action>
  <verify>
    <automated>cd frontend2 && bun run tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^export function TreeNode" frontend2/src/features/taxonomy/tree/TreeNode.tsx` matches
    - `grep -E "^export function TaxonomyTree" frontend2/src/features/taxonomy/tree/TaxonomyTree.tsx` matches
    - `grep -q "role=\"treeitem\"" frontend2/src/features/taxonomy/tree/TreeNode.tsx`
    - `grep -q "role=\"tree\"" frontend2/src/features/taxonomy/tree/TaxonomyTree.tsx`
    - `grep -q "aria-expanded" frontend2/src/features/taxonomy/tree/TreeNode.tsx`
    - `grep -q "line-through" frontend2/src/features/taxonomy/tree/TreeNode.tsx`
    - `grep -q "ChevronDown" frontend2/src/features/taxonomy/tree/TreeNode.tsx`
    - `grep -q "ChevronRight" frontend2/src/features/taxonomy/tree/TreeNode.tsx`
    - `grep -q "ArrowDown" frontend2/src/features/taxonomy/tree/TaxonomyTree.tsx`
    - `grep -q "ArrowRight" frontend2/src/features/taxonomy/tree/TaxonomyTree.tsx`
    - `grep -q "ARCHIVED" frontend2/src/features/taxonomy/tree/TaxonomyTree.tsx`
    - `grep -q "depth \* 24" frontend2/src/features/taxonomy/tree/TreeNode.tsx`
    - `tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Recursive tree renders with correct 24px-per-depth indentation, chevron toggles, archived styling, and ARIA tree semantics; keyboard nav works per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Three tab bodies + TaxonomyPage + route wiring</name>
  <files>frontend2/src/features/taxonomy/tabs/CategoriesTab.tsx, frontend2/src/features/taxonomy/tabs/LocationsTab.tsx, frontend2/src/features/taxonomy/tabs/ContainersTab.tsx, frontend2/src/features/taxonomy/TaxonomyPage.tsx, frontend2/src/routes/index.tsx</files>
  <read_first>
    - frontend2/src/pages/ApiDemoPage.tsx (state cascade: loading / error / empty / success; full file)
    - frontend2/src/routes/index.tsx (exact current structure to locate the authed Route block)
    - .planning/phases/58-taxonomy-categories-locations-containers/58-UI-SPEC.md (Layout Contract — page shell + tab bodies; Empty states; Primary CTAs; Copywriting Contract for exact strings)
    - frontend2/src/features/taxonomy/panel/EntityPanel.tsx (EntityPanelHandle — imperative open(mode, node))
    - frontend2/src/features/taxonomy/actions/ArchiveDeleteFlow.tsx (ArchiveDeleteFlowHandle — imperative open())
    - frontend2/src/components/retro/RetroTabs.tsx (tabs prop shape)
    - frontend2/src/components/retro/RetroEmptyState.tsx (heading/body/action props)
  </read_first>
  <action>
    **Tab body common pattern** (apply to all three tabs):

    ```tsx
    import { useRef, useState, useMemo } from "react";
    import { useLingui } from "@lingui/react/macro";
    import { RetroPanel, RetroButton, RetroEmptyState, HazardStripe } from "@/components/retro";
    import { Plus } from "lucide-react";
    import { EntityPanel, type EntityPanelHandle } from "../panel/EntityPanel";
    import { ArchiveDeleteFlow, type ArchiveDeleteFlowHandle } from "../actions/ArchiveDeleteFlow";
    // state cascade: authLoading / workspaceId / query.isPending / isError / isSuccess empty / isSuccess data
    ```

    **File 1: `frontend2/src/features/taxonomy/tabs/CategoriesTab.tsx`**

    - Local state: `showArchived: boolean` (default false)
    - Call `useCategoriesTree(showArchived)`
    - Derive `parentOptions` from `items` (all categories, filtered to exclude self+descendants when editing — passed through to EntityPanel via a state `{ editingId: string | null }`; compute excluded set via `collectDescendantIds` when `editingId` is set)
    - Refs: `entityPanelRef: EntityPanelHandle`, `archiveFlowRef: ArchiveDeleteFlowHandle`, state `{ archiveTarget: Category | null }`
    - Header: toggle `Show archived ({archivedCount})` on the left + `+ NEW CATEGORY` button on the right (amber)
    - Empty state: `<RetroEmptyState heading={t\`NO CATEGORIES YET\`} body={t\`Create your first category to start organizing items.\`} action={<RetroButton variant="primary" onClick={() => entityPanelRef.current?.open("create")}>{t\`+ NEW CATEGORY\`}</RetroButton>} />`
    - Loading: `<RetroPanel><p>{t\`Loading…\`}</p></RetroPanel>`
    - Error: `<RetroPanel style={{ borderColor: "var(--color-retro-red)" }}><HazardStripe className="mb-md" /><p>{t\`Could not load categories.\`}</p><RetroButton onClick={() => query.refetch()}>{t\`Retry\`}</RetroButton></RetroPanel>`
    - Success: render `<TaxonomyTree roots={tree} ... />` + mount `<EntityPanel kind="category" ref={entityPanelRef} parentOptions={parentOptions} />` + mount `<ArchiveDeleteFlow ref={archiveFlowRef} entityKind="category" nodeName={archiveTarget?.name ?? ""} onArchive={() => archiveMutation.mutateAsync(archiveTarget!.id)} onDelete={() => deleteMutation.mutateAsync(archiveTarget!.id)} />`
    - Row actions wire:
      - `onEdit` → `setEditingId(node.id); entityPanelRef.current?.open("edit", node)`
      - `onArchive` → `setArchiveTarget(node); archiveFlowRef.current?.open()`
      - `onRestore` → `restoreMutation.mutate(node.id)` (no dialog — direct call)
    - Import `useArchiveCategory`, `useDeleteCategory`, `useRestoreCategory` from `../hooks/useCategoryMutations`

    **File 2: `frontend2/src/features/taxonomy/tabs/LocationsTab.tsx`**

    Same structure as CategoriesTab but:
    - Use `useLocationsTree`, `useArchiveLocation`, `useDeleteLocation`, `useRestoreLocation`
    - EntityPanel `kind="location"`, `parentOptions` derived from locations
    - CTA: `+ NEW LOCATION`; empty: `NO LOCATIONS YET` / `Create your first location to describe where items live.`
    - TaxonomyTree: show `short_code` in mono next to name when present

    **File 3: `frontend2/src/features/taxonomy/tabs/ContainersTab.tsx`**

    Different structure — grouped list, not a tree:
    - Local state: `filterLocationId: string | null` (from a top-of-tab `RetroCombobox` location filter)
    - Call `useContainersByLocation({ locationId: filterLocationId ?? undefined, showArchived })` + `useLocationsTree(false)` to build location labels
    - Render: for each location that has containers (or all locations if filter is null and groups is non-empty), render a group header `<h3 className="uppercase tracking-wider font-sans text-[14px] font-semibold">{location.name}</h3>` followed by a card/panel containing each container as a row: `GAR-001  Red bin  [edit][archive]` (short_code mono, name sans)
    - CTA: `+ NEW CONTAINER`; disabled with helper `Create a location first.` when locations array is empty
    - Empty state: heading `NO CONTAINERS YET`, body `Containers belong to a location. Create a location first, then add containers inside it.`
    - `locationOptions` derived from `locationsTree.items` and passed to `EntityPanel kind="container"`

    **File 4: `frontend2/src/features/taxonomy/TaxonomyPage.tsx`**

    ```tsx
    import { useLingui } from "@lingui/react/macro";
    import { RetroTabs } from "@/components/retro";
    import { useHashTab } from "./hooks/useHashTab";
    import { CategoriesTab } from "./tabs/CategoriesTab";
    import { LocationsTab } from "./tabs/LocationsTab";
    import { ContainersTab } from "./tabs/ContainersTab";

    const TAB_KEYS = ["categories", "locations", "containers"] as const;
    type TabKey = typeof TAB_KEYS[number];

    export default function TaxonomyPage() {
      const { t } = useLingui();
      const [tab, setTab] = useHashTab<TabKey>("categories", TAB_KEYS);

      return (
        <div className="flex flex-col gap-lg">
          <h1 className="text-[20px] font-semibold uppercase tracking-wider">{t`TAXONOMY`}</h1>
          <RetroTabs
            tabs={[
              { key: "categories", label: t`CATEGORIES` },
              { key: "locations", label: t`LOCATIONS` },
              { key: "containers", label: t`CONTAINERS` },
            ]}
            activeTab={tab}
            onTabChange={(k) => setTab(k as TabKey)}
          />
          <div role="tabpanel" aria-labelledby={`tab-${tab}`}>
            {tab === "categories" && <CategoriesTab />}
            {tab === "locations" && <LocationsTab />}
            {tab === "containers" && <ContainersTab />}
          </div>
        </div>
      );
    }
    ```

    **File 5: `frontend2/src/routes/index.tsx`** (MODIFY)

    Inside the authed `AppShell` route block, add:

    ```tsx
    <Route path="taxonomy" element={<TaxonomyPage />} />
    ```

    Add the import at the top: `import TaxonomyPage from "@/features/taxonomy/TaxonomyPage";`

    Place the new Route immediately after the existing `items` route (or after `index` if `items` is not present yet). Do NOT wrap with a separate RequireAuth — inherit the parent.
  </action>
  <verify>
    <automated>cd frontend2 && bun run tsc --noEmit && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^export function CategoriesTab" frontend2/src/features/taxonomy/tabs/CategoriesTab.tsx` matches
    - `grep -E "^export function LocationsTab" frontend2/src/features/taxonomy/tabs/LocationsTab.tsx` matches
    - `grep -E "^export function ContainersTab" frontend2/src/features/taxonomy/tabs/ContainersTab.tsx` matches
    - `grep -q "export default function TaxonomyPage" frontend2/src/features/taxonomy/TaxonomyPage.tsx`
    - `grep -q "useHashTab" frontend2/src/features/taxonomy/TaxonomyPage.tsx`
    - `grep -q "RetroTabs" frontend2/src/features/taxonomy/TaxonomyPage.tsx`
    - `grep -q "useCategoriesTree" frontend2/src/features/taxonomy/tabs/CategoriesTab.tsx`
    - `grep -q "useLocationsTree" frontend2/src/features/taxonomy/tabs/LocationsTab.tsx`
    - `grep -q "useContainersByLocation" frontend2/src/features/taxonomy/tabs/ContainersTab.tsx`
    - `grep -q "+ NEW CATEGORY" frontend2/src/features/taxonomy/tabs/CategoriesTab.tsx`
    - `grep -q "+ NEW LOCATION" frontend2/src/features/taxonomy/tabs/LocationsTab.tsx`
    - `grep -q "+ NEW CONTAINER" frontend2/src/features/taxonomy/tabs/ContainersTab.tsx`
    - `grep -q "NO CATEGORIES YET" frontend2/src/features/taxonomy/tabs/CategoriesTab.tsx`
    - `grep -q "path=\"taxonomy\"" frontend2/src/routes/index.tsx`
    - `grep -q "TaxonomyPage" frontend2/src/routes/index.tsx`
    - `tsc --noEmit` exits 0
    - `bun run build` exits 0
  </acceptance_criteria>
  <done>/taxonomy route resolves to TaxonomyPage with three working tabs; each tab shows loading/error/empty/success states; New/Edit opens EntityPanel; Archive opens ArchiveDeleteFlow; Restore fires directly; build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Integration tests + Lingui catalog extraction</name>
  <files>frontend2/src/features/taxonomy/__tests__/TaxonomyPage.test.tsx, frontend2/src/features/taxonomy/__tests__/ContainersTab.test.tsx, frontend2/src/locales/en/messages.po, frontend2/src/locales/et/messages.po</files>
  <read_first>
    - frontend2/src/features/taxonomy/__tests__/fixtures.ts (Plan 01 renderWithProviders + setupDialogMocks + makeCategory/makeLocation/makeContainer)
    - frontend2/src/components/retro/__tests__/RetroConfirmDialog.test.tsx (i18n + dialog mock pattern)
    - frontend2/src/locales/en/messages.po (existing catalog format — head of file)
    - frontend2/package.json (find the lingui:extract script name)
  </read_first>
  <action>
    **File 1: `frontend2/src/features/taxonomy/__tests__/TaxonomyPage.test.tsx`**

    Use `renderWithProviders` + `setupDialogMocks`. Mock `categoriesApi.list`, `locationsApi.list`, `containersApi.list` via `vi.mock("@/lib/api/categories")`, etc. Tests:

    1. Renders with default hash → CATEGORIES tab visible, CategoriesTab loading state shown initially.
    2. Click LOCATIONS tab → window.location.hash becomes `#locations`; LocationsTab visible.
    3. Hash change from external source (`window.location.hash = "#containers"; dispatchEvent(new HashChangeEvent("hashchange"))`) → ContainersTab visible.
    4. Empty categories → renders `NO CATEGORIES YET` heading + `+ NEW CATEGORY` action.

    **File 2: `frontend2/src/features/taxonomy/__tests__/ContainersTab.test.tsx`**

    Mock `containersApi.list` + `locationsApi.list`. Tests:

    1. With 2 locations and 3 containers (2 in loc A, 1 in loc B), renders two group headers and 3 container rows.
    2. With zero containers + zero locations → empty state with disabled `+ NEW CONTAINER` button and helper `Create a location first.`
    3. Click `+ NEW CONTAINER` when locations exist → EntityPanel opens (assert panel title `NEW CONTAINER` visible).

    **File 3: Lingui catalog extraction**

    Run the extract script (consult `frontend2/package.json` for the exact name, likely `bun run lingui:extract` or `bun run i18n:extract`). Verify new keys land in both `src/locales/en/messages.po` and `src/locales/et/messages.po`. Must include (at minimum): `TAXONOMY`, `CATEGORIES`, `LOCATIONS`, `CONTAINERS`, `+ NEW CATEGORY`, `+ NEW LOCATION`, `+ NEW CONTAINER`, `NEW CATEGORY`, `EDIT CATEGORY`, `NEW LOCATION`, `EDIT LOCATION`, `NEW CONTAINER`, `EDIT CONTAINER`, `CREATE CATEGORY`, `SAVE CATEGORY`, `CREATE LOCATION`, `SAVE LOCATION`, `CREATE CONTAINER`, `SAVE CONTAINER`, `← BACK`, `WORKING…`, `CONFIRM ARCHIVE`, `CONFIRM DELETE`, `HIDES FROM PICKERS`, `delete permanently`, `DISCARD CHANGES?`, `DISCARD`, `ARCHIVED`, `NO CATEGORIES YET`, `NO LOCATIONS YET`, `NO CONTAINERS YET`, `Move or delete child nodes first.`, `Category created.`, `Category saved.`, `Category archived.`, `Category restored.`, `Category deleted.`, plus Location/Container equivalents.

    Fill in ET translations. If an exact translation is uncertain, mirror the existing ET catalog style (consult `et/messages.po` for tone). DO NOT leave `msgstr ""` for Phase 58 keys — supply an Estonian translation for each.

    If the extract script auto-orders keys and produces diff noise in unrelated entries, accept the full diff (lingui is authoritative).
  </action>
  <verify>
    <automated>cd frontend2 && bun run test -- --run src/features/taxonomy/ && bun run lingui:compile 2>&1 | tee /tmp/lingui.log && grep -q "NO CATEGORIES YET" frontend2/src/locales/en/messages.po && grep -q "NO CATEGORIES YET" frontend2/src/locales/et/messages.po</automated>
  </verify>
  <acceptance_criteria>
    - TaxonomyPage.test.tsx has >= 4 `it(` blocks
    - ContainersTab.test.tsx has >= 3 `it(` blocks
    - All Phase 58 taxonomy tests pass (`bun run test -- --run src/features/taxonomy/` exits 0)
    - `grep -q "TAXONOMY" frontend2/src/locales/en/messages.po`
    - `grep -q "NO CATEGORIES YET" frontend2/src/locales/en/messages.po`
    - `grep -q "NO CATEGORIES YET" frontend2/src/locales/et/messages.po`
    - `grep -q "Move or delete child nodes first" frontend2/src/locales/en/messages.po`
    - `grep -q "Move or delete child nodes first" frontend2/src/locales/et/messages.po`
    - `grep -q "delete permanently" frontend2/src/locales/en/messages.po`
    - No empty `msgstr ""` lines for the new Phase 58 keys in `et/messages.po` (grep `msgstr ""` followed by a blank line after the new msgids — manually verify the new keys have non-empty ET translations)
    - `bun run lingui:compile` exits 0
  </acceptance_criteria>
  <done>Integration tests pass; Lingui catalogs contain all Phase 58 user-visible strings in EN + ET with non-empty translations.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — end-to-end /taxonomy flows</name>
  <files>n/a — manual verification only; no file modifications</files>
  <action>Pause automation. Present the "how-to-verify" steps to the user and wait for the resume signal. Do not proceed to SUMMARY until user types "approved" or equivalent.</action>
  <verify>
    <automated>echo "manual checkpoint — awaiting human verification"</automated>
  </verify>
  <done>User has typed "approved" (or equivalent acceptance) indicating all 18 end-to-end checks pass against the running app.</done>
  <what-built>
    A `/taxonomy` page with three tabs (Categories, Locations, Containers), hierarchical tree CRUD, slide-over panel create/edit, archive-first delete flow, and 409 cascade handling — all satisfying TAX-01 through TAX-12 end-to-end against the real Go backend.
  </what-built>
  <how-to-verify>
    Prerequisites: `cd frontend2 && bun run dev` is running (Vite dev server proxies to Go backend). Log in to the app, then navigate to `http://localhost:5173/taxonomy`.

    **CATEGORIES tab (TAX-01..TAX-04):**
    1. Verify TAX-01: URL hash is `#categories` on arrival. The tab body shows `TAXONOMY` page title, tab bar with three tabs, and either a tree or `NO CATEGORIES YET` empty state. If tree has children, children are indented 24px per depth with chevrons.
    2. Verify TAX-02: Click `+ NEW CATEGORY` (amber, top-right). Slide-over panel docks from the right in ~150ms. Title says `NEW CATEGORY`. Type a name only (e.g., "Tools"), click `CREATE CATEGORY`. Panel closes. Toast "Category created." appears. Tree refreshes to show the new row.
    3. Verify TAX-02 (parent): Create a second category with an existing category as parent via the `PARENT CATEGORY` combobox. After save, it appears indented under the parent.
    4. Verify TAX-03: Click `EDIT` on a row. Panel opens with title `EDIT CATEGORY` and pre-filled values. Parent combobox excludes the node being edited and its descendants. Change the name, click `SAVE CATEGORY`. Panel closes; toast "Category saved."; tree updates.
    5. Verify TAX-04 (archive): Click `ARCHIVE` on a leaf row. Archive-first dialog appears with amber `ARCHIVE CATEGORY` button, orange `HIDES FROM PICKERS` badge, and `delete permanently` link below. Click `ARCHIVE CATEGORY`. Dialog closes; toast "Category archived."; row now shows strikethrough + `ARCHIVED` badge.
    6. Verify TAX-04 (restore): Click `RESTORE` on the archived row. Toast "Category restored."; strikethrough gone.
    7. Verify TAX-04 (409 cascade): Create a parent category with a child. Click `ARCHIVE` on the parent, then click `delete permanently` link → hard-delete dialog with hazard stripes and red `DELETE CATEGORY`. Click it. Expected: both dialogs close; toast "Move or delete child nodes first." appears. Tree unchanged.
    8. Verify TAX-04 (hard-delete leaf): Find a leaf with no children. Open archive dialog → click `delete permanently` → click `DELETE CATEGORY`. Toast "Category deleted."; row gone.

    **LOCATIONS tab (TAX-05..TAX-08):**
    9. Click `LOCATIONS` tab. URL hash becomes `#locations`. Repeat the create flow (TAX-06): name + optional short_code (verify auto-fill: type "Garage Shelf 1" in Name, watch SHORT CODE field auto-populate with `GAR-NNN` after ~300ms; manually edit short code and confirm auto-fill stops). Save.
    10. TAX-07: Edit a location; verify parent picker excludes self+descendants.
    11. TAX-08: Archive → restore → delete flow matches TAX-04 (locations have no 409 cascade; delete succeeds without toast warning).

    **CONTAINERS tab (TAX-09..TAX-12):**
    12. Click `CONTAINERS` tab. URL hash becomes `#containers`. TAX-09: Containers render grouped by location with UPPERCASE location headers. TAX-10: `+ NEW CONTAINER` opens panel; location picker is required; short_code auto-fills same as location. TAX-11: Edit flow works. TAX-12: Archive/restore/delete flow works.
    13. Empty-containers edge case: If no locations exist yet, `+ NEW CONTAINER` is disabled with helper text `Create a location first.`

    **Cross-cutting:**
    14. Hash deep-link: Paste `http://localhost:5173/taxonomy#locations` into a new tab; the Locations tab is active on arrival.
    15. Unsaved-changes guard: Open `+ NEW CATEGORY`, type something, then press Esc. A `DISCARD CHANGES?` dialog appears. Click `← BACK` to return. Type more, press Esc, click `DISCARD`. Panel closes, data discarded.
    16. i18n: Switch app locale to Estonian (via settings). All taxonomy strings render in Estonian.
    17. Motion: Toggle OS setting "Reduce motion" (or DevTools emulation). Slide-over now switches in instantly; tests still pass.
    18. Keyboard nav: Focus a tree row; ArrowDown/Up moves focus; ArrowRight expands; Enter opens edit panel.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 18 checks pass. Otherwise list the failing check numbers with observed vs. expected behavior.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL hash -> active tab | User-controlled hash read into state; restricted to allowlist (`["categories", "locations", "containers"]`) |
| route -> RequireAuth | /taxonomy nests inside authed AppShell; unauthed users are redirected upstream |
| tab body -> mutation hooks | All mutations go through Plan 02 hooks which enforce zod-validated input + workspaceId guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-14 | Spoofing | /taxonomy route | mitigate | Route inherits RequireAuth from parent AppShell; unauthenticated GET redirects to /login (existing v2.0 infrastructure) |
| T-58-15 | Tampering | URL hash | mitigate | useHashTab allowlist restricts to `["categories", "locations", "containers"]`; invalid values fall back to default |
| T-58-16 | Information Disclosure | Toast copy | mitigate | All error toasts use sanitized Lingui strings, no raw backend messages surfaced |
| T-58-17 | Cross-Site Scripting | Tree node.name rendering | mitigate | React default text-content escaping applies; no `dangerouslySetInnerHTML` in any Phase 58 file |
| T-58-18 | Denial of Service | Tree render on large taxonomies | accept | `useMemo(buildTree)` caps cost at O(n) per query refetch; taxonomy size bounded to <100 nodes realistically (D-02) |
| T-58-19 | Spoofing | Hash-based tab key injection | mitigate | useHashTab explicitly validates against allowlist before `history.replaceState` or state update |
</threat_model>

<verification>
- `cd frontend2 && bun run tsc --noEmit` exits 0
- `cd frontend2 && bun run build` exits 0
- `cd frontend2 && bun run test -- --run src/features/taxonomy/` shows all Phase 58 tests passing
- `cd frontend2 && bun run lingui:compile` exits 0
- Human checkpoint: all 18 end-to-end scenarios pass
</verification>

<success_criteria>
- /taxonomy route resolves to TaxonomyPage
- All 12 TAX-* requirements verified in human checkpoint
- EN + ET catalogs complete with no empty msgstr entries for Phase 58 keys
- No new runtime dependencies
- No imports of `idb`, `serwist`, `*offline*`, `*sync*` (Phase 56 CI guard continues to pass)
</success_criteria>

<output>
After completion, create `.planning/phases/58-taxonomy-categories-locations-containers/58-04-SUMMARY.md`
</output>
