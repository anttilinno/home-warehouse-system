---
phase: 43-backend-schema-and-needs-review-api
plan: 02
type: execute
wave: 2
depends_on:
  - 43-01
files_modified:
  - backend/internal/domain/warehouse/item/handler.go
  - backend/internal/domain/sync/types.go
  - backend/internal/domain/sync/service.go
  - backend/internal/domain/warehouse/item/handler_test.go
  - backend/internal/domain/warehouse/item/service_test.go
  - backend/internal/domain/warehouse/item/entity_test.go
autonomous: true
requirements:
  - COMP-02
  - COMP-03

must_haves:
  truths:
    - "GET /items?needs_review=true returns only items with needs_review=true"
    - "POST /items with needs_review=true creates an item flagged for review"
    - "PATCH /items/{id} with needs_review=false clears the review flag"
    - "Item list API response includes needs_review field"
    - "Sync delta endpoint includes needs_review in ItemSyncData"
    - "Handler tests verify the filter, create, and update paths for needs_review"
  artifacts:
    - path: "backend/internal/domain/warehouse/item/handler.go"
      provides: "needs_review query parameter filter and field in request/response types"
      contains: "NeedsReview"
    - path: "backend/internal/domain/sync/types.go"
      provides: "NeedsReview field in ItemSyncData"
      contains: "NeedsReview"
    - path: "backend/internal/domain/sync/service.go"
      provides: "needs_review mapped in mapItems"
      contains: "NeedsReview"
    - path: "backend/internal/domain/warehouse/item/handler_test.go"
      provides: "Tests for needs_review filter and field handling"
      contains: "NeedsReview"
  key_links:
    - from: "backend/internal/domain/warehouse/item/handler.go"
      to: "backend/internal/domain/warehouse/item/service.go"
      via: "ListNeedingReview called when needs_review=true query param"
      pattern: "svc\\.ListNeedingReview"
    - from: "backend/internal/domain/sync/service.go"
      to: "backend/internal/domain/warehouse/item/entity.go"
      via: "NeedsReview() getter called in mapItems"
      pattern: "item\\.NeedsReview\\(\\)"
---

<objective>
Expose the needs_review field through the HTTP API (create, update, list with filter) and sync endpoint, then add comprehensive tests for all new functionality.

Purpose: Complete the API contract so the frontend (Phase 44+) can create items with needs_review=true, filter the item list, and mark items as reviewed. Also ensure the PWA offline cache receives the field via sync.

Output: Updated handler with filter support, sync endpoint with needs_review field, and tests covering entity, service, and handler layers.
</objective>

<execution_context>
@/home/antti/.claude/get-shit-done/workflows/execute-plan.md
@/home/antti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/43-backend-schema-and-needs-review-api/43-RESEARCH.md
@.planning/phases/43-backend-schema-and-needs-review-api/43-01-SUMMARY.md

Key existing files to study for patterns:
@backend/internal/domain/warehouse/item/handler.go
@backend/internal/domain/warehouse/item/handler_test.go
@backend/internal/domain/warehouse/item/service_test.go
@backend/internal/domain/warehouse/item/entity_test.go
@backend/internal/domain/sync/types.go
@backend/internal/domain/sync/service.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Handler API and sync endpoint updates</name>
  <files>
    backend/internal/domain/warehouse/item/handler.go
    backend/internal/domain/sync/types.go
    backend/internal/domain/sync/service.go
  </files>
  <action>
1. **handler.go** -- Update request/response types and list logic:

   a. Add `NeedsReview *bool` to `ListItemsInput` struct:
      ```go
      NeedsReview *bool `query:"needs_review,omitempty" doc:"Filter by needs_review status"`
      ```

   b. Add `NeedsReview *bool` to `CreateItemRequest` body struct (the request type used for POST /items). Follow the exact pattern of how `IsInsured` is declared in the same struct.

   c. Add `NeedsReview *bool` to `UpdateItemRequest` body struct (used for PATCH /items/{id}). Same pattern as IsInsured.

   d. Add `NeedsReview *bool` to the item response struct (the type returned in API responses, e.g., `ItemResponse` or equivalent). Follow existing boolean field pattern.

   e. Update `toItemResponse()` helper (or equivalent mapping function): map `item.NeedsReview()` to the response field.

   f. Update the list handler logic: when `input.NeedsReview != nil && *input.NeedsReview`, call `svc.ListNeedingReview(ctx, workspaceID, page, limit)` instead of `svc.List(ctx, workspaceID, page, limit)`. The response format (items array + pagination) stays the same.

   g. Update the create handler: pass `NeedsReview` from the request to `service.CreateInput`.

   h. Update the update handler: pass `NeedsReview` from the request to `service.UpdateInput`.

2. **sync/types.go** -- Add NeedsReview to ItemSyncData:
   ```go
   NeedsReview bool `json:"needs_review"`
   ```
   Position it near the other boolean fields (IsInsured, IsArchived, LifetimeWarranty).

3. **sync/service.go** -- Update `mapItems()` function:
   Add the NeedsReview field mapping. The entity returns `*bool`, but ItemSyncData uses `bool`. Use the same `boolPtrToBool()` helper (or equivalent dereference pattern) already used for other boolean fields in that function.

IMPORTANT: Study the existing handler.go carefully to understand the exact struct names and mapping patterns. The research provides the conceptual pattern but struct names may differ from the examples.
  </action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system/backend && go build ./... 2>&1 | head -20</automated>
  </verify>
  <done>
    - ListItemsInput has NeedsReview query parameter
    - Create and update request types include NeedsReview field
    - Item response type includes NeedsReview field
    - List handler branches to ListNeedingReview when needs_review=true
    - Sync ItemSyncData includes NeedsReview field
    - mapItems maps NeedsReview from entity to sync data
    - Backend compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Tests for entity, service, and handler</name>
  <files>
    backend/internal/domain/warehouse/item/entity_test.go
    backend/internal/domain/warehouse/item/service_test.go
    backend/internal/domain/warehouse/item/handler_test.go
  </files>
  <action>
1. **entity_test.go** -- Add tests following existing test patterns:
   - `TestItem_NewItem_DefaultsNeedsReviewToFalse`: Create item with NewItem(), assert NeedsReview() returns &false.
   - `TestItem_SetNeedsReview`: Create item, call SetNeedsReview(true), assert NeedsReview() returns &true. Call SetNeedsReview(false), assert returns &false.
   - `TestItem_Update_WithNeedsReview`: Create item, call Update() with NeedsReview=&true in the input, assert the field changed. Test with NeedsReview=nil to verify it stays unchanged.

2. **service_test.go** -- Add tests following existing mock/test patterns:
   - `TestService_Create_WithNeedsReview`: Mock the repository Save, call Create with NeedsReview=&true in CreateInput, verify the saved item has NeedsReview=true.
   - `TestService_ListNeedingReview`: Mock FindNeedingReview to return test items, call ListNeedingReview, verify it returns the correct items and count.

3. **handler_test.go** -- Add tests following existing handler test patterns:
   - **Update the mock service** (or interface mock): Add `ListNeedingReview` method to the mock, matching the service interface.
   - `TestListItems_FilterByNeedsReview`: Send GET /items?needs_review=true, verify the mock's ListNeedingReview was called (not List), and response contains the expected items.
   - `TestCreateItem_WithNeedsReview`: Send POST /items with needs_review=true in body, verify the created item response includes needs_review=true.
   - `TestUpdateItem_ClearNeedsReview`: Send PATCH /items/{id} with needs_review=false, verify the response shows needs_review=false.

CRITICAL: Read the existing test files first to understand:
- How mocks are structured (generated mocks vs hand-written)
- How test HTTP requests are constructed
- How assertions are written
- What test helpers exist

Follow the exact patterns. Do NOT introduce new test utilities or assertion patterns. If the mock service interface needs a new method, add it in the same style as existing mock methods.
  </action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system/backend && go test ./internal/domain/warehouse/item/... -count=1 -run "NeedsReview|Needs" -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    - Entity tests verify default value, setter, and Update with NeedsReview
    - Service tests verify Create with NeedsReview and ListNeedingReview
    - Handler tests verify filter query parameter, create with field, update with field
    - Mock service updated with ListNeedingReview method
    - All NeedsReview tests pass
    - No existing tests broken (`go test ./internal/domain/warehouse/item/...` all pass)
  </done>
</task>

</tasks>

<verification>
1. `cd backend && go build ./...` -- compiles cleanly
2. `cd backend && go test ./internal/domain/warehouse/item/... -count=1 -v` -- all tests pass
3. `cd backend && go test ./internal/domain/sync/... -count=1 -v` -- sync tests pass (if any)
4. `cd backend && go vet ./...` -- no warnings
5. Grep `NeedsReview` across handler.go, sync/types.go, sync/service.go to confirm field is threaded through
</verification>

<success_criteria>
- GET /items?needs_review=true filters to only needs-review items (COMP-02)
- PATCH /items/{id} with needs_review=false clears the flag (COMP-03)
- POST /items with needs_review=true flags the item (COMP-01 API layer)
- All API responses include needs_review field
- Sync endpoint includes needs_review in ItemSyncData
- All new tests pass, no existing tests broken
- Full backend compiles and vets cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/43-backend-schema-and-needs-review-api/43-02-SUMMARY.md`
</output>
