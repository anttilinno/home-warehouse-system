---
phase: 43-backend-schema-and-needs-review-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/db/migrations/002_add_needs_review.sql
  - backend/db/queries/items.sql
  - backend/internal/infra/queries/
  - backend/internal/domain/warehouse/item/entity.go
  - backend/internal/domain/warehouse/item/service.go
  - backend/internal/domain/warehouse/item/repository.go
  - backend/internal/infra/postgres/item_repository.go
  - backend/internal/testutil/factory/item.go
autonomous: true
requirements:
  - COMP-01

must_haves:
  truths:
    - "Items table has a needs_review boolean column that defaults to false"
    - "Item entity exposes NeedsReview getter and SetNeedsReview setter"
    - "Item can be created with needs_review=true via CreateInput"
    - "Item can be updated to set needs_review=false via UpdateInput"
    - "Repository persists needs_review on both create and update paths"
    - "FindNeedingReview repository method returns only items with needs_review=true"
  artifacts:
    - path: "backend/db/migrations/002_add_needs_review.sql"
      provides: "Schema migration adding needs_review column"
      contains: "needs_review boolean DEFAULT false"
    - path: "backend/internal/domain/warehouse/item/entity.go"
      provides: "needsReview field on Item struct"
      contains: "needsReview"
    - path: "backend/internal/domain/warehouse/item/service.go"
      provides: "NeedsReview in CreateInput and UpdateInput, ListNeedingReview method"
      contains: "ListNeedingReview"
    - path: "backend/internal/domain/warehouse/item/repository.go"
      provides: "FindNeedingReview in Repository interface"
      contains: "FindNeedingReview"
    - path: "backend/internal/infra/postgres/item_repository.go"
      provides: "FindNeedingReview implementation and needs_review in Save"
      contains: "FindNeedingReview"
  key_links:
    - from: "backend/internal/infra/postgres/item_repository.go"
      to: "backend/internal/infra/queries/"
      via: "sqlc generated types in Save and FindNeedingReview"
      pattern: "queries\\.CreateItemParams|queries\\.ListItemsNeedingReview"
    - from: "backend/internal/domain/warehouse/item/service.go"
      to: "backend/internal/domain/warehouse/item/repository.go"
      via: "ListNeedingReview calls FindNeedingReview"
      pattern: "repo\\.FindNeedingReview"
---

<objective>
Add the needs_review boolean column to the items table and thread it through the entire data layer: migration, sqlc queries, domain entity, service, repository interface, repository implementation, and test factory.

Purpose: Establish the data foundation that the API layer (Plan 02) will expose. After this plan, the backend can persist and query needs_review but the HTTP handler does not yet expose it.

Output: Migration file, updated sqlc queries with regenerated Go code, updated entity/service/repository with NeedsReview support, and factory helper for tests.
</objective>

<execution_context>
@/home/antti/.claude/get-shit-done/workflows/execute-plan.md
@/home/antti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/43-backend-schema-and-needs-review-api/43-RESEARCH.md

Key existing files to study for patterns:
@backend/db/migrations/001_initial_schema.sql
@backend/db/queries/items.sql
@backend/internal/domain/warehouse/item/entity.go
@backend/internal/domain/warehouse/item/service.go
@backend/internal/domain/warehouse/item/repository.go
@backend/internal/infra/postgres/item_repository.go
@backend/internal/testutil/factory/item.go
@backend/sqlc.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migration and sqlc query updates</name>
  <files>
    backend/db/migrations/002_add_needs_review.sql
    backend/db/queries/items.sql
    backend/internal/infra/queries/ (regenerated)
  </files>
  <action>
1. Create `backend/db/migrations/002_add_needs_review.sql` using dbmate format:
   ```sql
   -- migrate:up
   ALTER TABLE warehouse.items ADD COLUMN needs_review boolean DEFAULT false;

   -- migrate:down
   ALTER TABLE warehouse.items DROP COLUMN needs_review;
   ```

2. Update `backend/db/queries/items.sql`:
   - Add `needs_review` to the `CreateItem` INSERT column list and VALUES. It becomes the next positional parameter after the current last one. Follow the exact column ordering from the research (see 43-RESEARCH.md "sqlc Query: CreateItem with needs_review").
   - Add `needs_review` to the `UpdateItem` SET clause. Follow the exact pattern from research.
   - Add two new queries at the end of the file:
     ```sql
     -- name: ListItemsNeedingReview :many
     SELECT * FROM warehouse.items
     WHERE workspace_id = $1 AND needs_review = true AND is_archived = false
     ORDER BY updated_at DESC
     LIMIT $2 OFFSET $3;

     -- name: CountItemsNeedingReview :one
     SELECT COUNT(*) FROM warehouse.items
     WHERE workspace_id = $1 AND needs_review = true AND is_archived = false;
     ```

3. Run `sqlc generate` from the `backend/` directory to regenerate Go types. The generated `queries.WarehouseItem` struct will now include `NeedsReview *bool` (because of `emit_pointers_for_null_types: true` in sqlc.yaml). New generated functions: `ListItemsNeedingReview`, `CountItemsNeedingReview`.

4. Run the migration against the dev database: use `mise run db:migrate` or equivalent dbmate command from the project root.

IMPORTANT: Check the existing CreateItem and UpdateItem queries carefully before modifying -- count exact parameter positions. The research examples show the expected final state but parameter numbering must match the actual current query.
  </action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system/backend && sqlc compile 2>&1 | head -20</automated>
  </verify>
  <done>
    - Migration file exists with correct dbmate format
    - CreateItem query includes needs_review parameter
    - UpdateItem query includes needs_review in SET clause
    - ListItemsNeedingReview and CountItemsNeedingReview queries exist
    - sqlc generate succeeds without errors
    - Generated Go code includes NeedsReview field on WarehouseItem struct
  </done>
</task>

<task type="auto">
  <name>Task 2: Domain entity, service, repository, and factory updates</name>
  <files>
    backend/internal/domain/warehouse/item/entity.go
    backend/internal/domain/warehouse/item/service.go
    backend/internal/domain/warehouse/item/repository.go
    backend/internal/infra/postgres/item_repository.go
    backend/internal/testutil/factory/item.go
  </files>
  <action>
1. **entity.go** -- Follow the exact `is_insured`/`is_archived` pattern:
   - Add `needsReview *bool` field to the `Item` struct
   - In `NewItem()`: initialize `needsReview` to `&falseVal` (same pattern as existing booleans, declare `falseVal := false`)
   - Add getter: `func (i *Item) NeedsReview() *bool { return i.needsReview }`
   - Add setter: `func (i *Item) SetNeedsReview(v bool) { i.needsReview = &v; i.updatedAt = time.Now() }`
   - Update `Reconstruct()`: add `needsReview *bool` parameter (BEFORE createdAt/updatedAt which are always last), assign to struct field
   - Update `Update()` method: if input `NeedsReview` is non-nil, call `SetNeedsReview(*input.NeedsReview)`

2. **service.go**:
   - Add `NeedsReview *bool` field to `CreateInput` struct
   - Add `NeedsReview *bool` field to `UpdateInput` struct
   - In `Create()` method: after creating the item, if `input.NeedsReview != nil && *input.NeedsReview`, call `item.SetNeedsReview(true)`
   - Add `ListNeedingReview(ctx, workspaceID, page, limit)` method that calls `s.repo.FindNeedingReview(ctx, workspaceID, page, limit)` and returns `([]*Item, int, error)` matching the existing `List()` signature pattern

3. **repository.go** -- Add to the Repository interface:
   ```go
   FindNeedingReview(ctx context.Context, workspaceID uuid.UUID, page, limit int) ([]*Item, int, error)
   ```
   This follows the exact same signature as `FindByWorkspace`.

4. **item_repository.go** (infra/postgres):
   - Update `rowToItem()`: pass `row.NeedsReview` to the `Reconstruct()` call. Position it correctly (before `row.CreatedAt.Time, row.UpdatedAt.Time`).
   - Update `Save()` CREATE branch: add `NeedsReview` to `queries.CreateItemParams`, mapping from `item.NeedsReview()`. Use `derefBool()` or direct dereference matching the existing `IsInsured` pattern.
   - Update `Save()` UPDATE branch: add `NeedsReview` to `queries.UpdateItemParams`, mapping from `item.NeedsReview()`.
   - Implement `FindNeedingReview(ctx, workspaceID, page, limit)`:
     - Call generated `q.ListItemsNeedingReview(ctx, queries.ListItemsNeedingReviewParams{WorkspaceID: workspaceID, Limit: int32(limit), Offset: int32((page-1)*limit)})`
     - Call generated `q.CountItemsNeedingReview(ctx, workspaceID)` for the total count
     - Map rows using `rowToItem()`, return items slice, int(count), nil

5. **factory/item.go** -- Add factory option:
   ```go
   func WithItemNeedsReview(v bool) func(*item.Item) {
       return func(i *item.Item) {
           i.SetNeedsReview(v)
       }
   }
   ```
   Follow the exact pattern of `WithItemIsInsured` or similar existing factory options.

CRITICAL: After updating `Reconstruct()` signature, grep for ALL call sites of `item.Reconstruct(` in the codebase. The infra repository `rowToItem` is the primary one, but verify there are no others (e.g., in test helpers or import workers). All call sites must be updated with the new parameter.

PITFALL: The `Save()` method has SEPARATE create and update branches. Both must include `NeedsReview`. Missing it in either branch causes silent data loss on that path.
  </action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system/backend && go build ./... 2>&1 | head -30</automated>
  </verify>
  <done>
    - Item entity has needsReview field with getter, setter, and Reconstruct support
    - Service has NeedsReview in CreateInput/UpdateInput and ListNeedingReview method
    - Repository interface includes FindNeedingReview
    - Infra repository implements FindNeedingReview with count, and Save includes needs_review in both create and update paths
    - Factory has WithItemNeedsReview option
    - `go build ./...` succeeds with zero errors
  </done>
</task>

</tasks>

<verification>
1. `cd backend && sqlc compile` -- no errors (queries valid against schema)
2. `cd backend && go build ./...` -- no errors (all types aligned)
3. `cd backend && go vet ./...` -- no warnings
4. Grep for `Reconstruct(` and verify all call sites include needsReview parameter
5. Grep for `NeedsReview` in generated queries package to confirm sqlc output
</verification>

<success_criteria>
- Migration file creates needs_review boolean column with DEFAULT false
- sqlc queries updated and regenerated without errors
- Item entity fully supports needsReview (field, getter, setter, Reconstruct, Update)
- Service supports NeedsReview in CreateInput/UpdateInput and has ListNeedingReview method
- Repository interface and implementation support FindNeedingReview with pagination and count
- Factory has WithItemNeedsReview option for tests
- Full backend compiles cleanly with `go build ./...`
</success_criteria>

<output>
After completion, create `.planning/phases/43-backend-schema-and-needs-review-api/43-01-SUMMARY.md`
</output>
