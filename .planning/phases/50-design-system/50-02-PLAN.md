---
phase: 50-design-system
plan: 02
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - frontend2/src/components/retro/RetroCard.tsx
  - frontend2/src/components/retro/RetroDialog.tsx
  - frontend2/src/components/retro/RetroTable.tsx
  - frontend2/src/components/retro/RetroTabs.tsx
  - frontend2/src/components/retro/RetroBadge.tsx
  - frontend2/src/components/retro/index.ts
  - frontend2/src/components/retro/__tests__/RetroCard.test.tsx
  - frontend2/src/components/retro/__tests__/RetroDialog.test.tsx
  - frontend2/src/components/retro/__tests__/RetroTable.test.tsx
  - frontend2/src/components/retro/__tests__/RetroTabs.test.tsx
  - frontend2/src/components/retro/__tests__/RetroBadge.test.tsx
autonomous: true
requirements:
  - DS-04
  - DS-05
  - DS-06
  - DS-07
  - DS-09

must_haves:
  truths:
    - "RetroCard renders as a lightweight container with thick border, cream background, and raised shadow"
    - "RetroDialog opens as a native modal with backdrop, hazard stripe header, and close button"
    - "RetroTable renders header row with dark background and alternating row colors"
    - "RetroTabs renders file-folder tab bar where active tab merges with content panel below"
    - "RetroBadge renders inline colored labels with 5 variants"
  artifacts:
    - path: "frontend2/src/components/retro/RetroCard.tsx"
      provides: "Lightweight content container"
      exports: ["RetroCard"]
    - path: "frontend2/src/components/retro/RetroDialog.tsx"
      provides: "Modal dialog with imperative open/close API"
      exports: ["RetroDialog", "RetroDialogHandle"]
    - path: "frontend2/src/components/retro/RetroTable.tsx"
      provides: "Data table with retro styling"
      exports: ["RetroTable"]
    - path: "frontend2/src/components/retro/RetroTabs.tsx"
      provides: "File-folder tab navigation"
      exports: ["RetroTabs"]
    - path: "frontend2/src/components/retro/RetroBadge.tsx"
      provides: "Inline status badge with color variants"
      exports: ["RetroBadge"]
  key_links:
    - from: "frontend2/src/components/retro/RetroDialog.tsx"
      to: "native <dialog> element"
      via: "showModal() / close() imperative API"
      pattern: "useImperativeHandle"
    - from: "frontend2/src/components/retro/RetroDialog.tsx"
      to: "frontend2/src/components/retro/HazardStripe.tsx"
      via: "import for hazard stripe at top of dialog"
      pattern: "import.*HazardStripe"
---

<objective>
Build the five secondary retro UI primitives: RetroCard (DS-04), RetroDialog (DS-05), RetroTable (DS-06), RetroTabs (DS-07), and RetroBadge (DS-09). These components complete the design system vocabulary alongside the core primitives from Plan 01.

Purpose: Complete the component set so the demo page and auth refactor can use the full design system.
Output: Five component files with tests, updated barrel export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-design-system/50-CONTEXT.md
@.planning/phases/50-design-system/50-RESEARCH.md
@.planning/phases/50-design-system/50-UI-SPEC.md
@frontend2/src/styles/globals.css
@frontend2/src/features/auth/AuthPage.tsx
@frontend2/src/test-utils.tsx

<interfaces>
<!-- From Plan 01 (will exist when this plan executes) -->
From frontend2/src/components/retro/index.ts:
  export { RetroButton } from "./RetroButton";
  export { RetroPanel } from "./RetroPanel";
  export { RetroInput } from "./RetroInput";
  export { HazardStripe } from "./HazardStripe";

From frontend2/src/components/retro/HazardStripe.tsx:
  export function HazardStripe({ height, className }: { height?: number; className?: string })

<!-- Design tokens (same as Plan 01) -->
Colors: retro-cream (#F5F0E1), retro-charcoal (#2A2A2A), retro-ink (#1A1A1A), retro-amber (#D4A017), retro-red (#CC3333), retro-green (#4A7C4A), retro-blue (#3366AA), retro-gray (#8B8B8B)
Borders: border-retro-thick (3px), border-retro-extra-thick (4px)
Shadows: shadow-retro-raised (2px 2px 0px #1A1A1A), shadow-retro-pressed (inset 2px 2px 0px rgba(0,0,0,0.3))

<!-- Existing tab pattern to extract (AuthPage.tsx) -->
Tab base: min-w-[120px] h-[36px] text-[14px] font-bold uppercase border-retro-thick border-retro-ink cursor-pointer
Active: bg-retro-cream border-b-0
Inactive: bg-retro-gray
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build RetroCard, RetroBadge, RetroTabs components with tests</name>
  <files>
    frontend2/src/components/retro/RetroCard.tsx,
    frontend2/src/components/retro/RetroBadge.tsx,
    frontend2/src/components/retro/RetroTabs.tsx,
    frontend2/src/components/retro/__tests__/RetroCard.test.tsx,
    frontend2/src/components/retro/__tests__/RetroBadge.test.tsx,
    frontend2/src/components/retro/__tests__/RetroTabs.test.tsx
  </files>
  <read_first>
    frontend2/src/styles/globals.css,
    frontend2/src/components/retro/RetroPanel.tsx,
    frontend2/src/components/retro/RetroButton.tsx,
    frontend2/src/components/retro/index.ts,
    frontend2/src/features/auth/AuthPage.tsx,
    frontend2/src/test-utils.tsx,
    .planning/phases/50-design-system/50-UI-SPEC.md
  </read_first>
  <behavior>
    RetroCard:
    - Renders a div with border-retro-thick border-retro-ink bg-retro-cream shadow-retro-raised p-md
    - Forwards ref to div, merges className, renders children
    - Simpler than RetroPanel: no close button, no hazard stripe, smaller padding (p-md vs p-lg)

    RetroBadge:
    - Renders an inline-flex span with border-retro-thick border-retro-ink text-[12px] font-bold uppercase py-[2px] px-sm leading-[1.2]
    - 5 variants: neutral (bg-retro-gray text-white), success (bg-retro-green text-white), danger (bg-retro-red text-white), warning (bg-retro-amber text-retro-ink), info (bg-retro-blue text-white)
    - Default variant: neutral
    - Merges className, renders children

    RetroTabs:
    - Renders a controlled tab bar (no panel content -- just the tabs row)
    - Props: tabs: { key: string; label: string }[], activeTab: string, onTabChange: (key: string) => void, className?: string
    - Tab bar is a flex div
    - Each tab: button with min-w-[120px] h-[36px] text-[14px] font-bold uppercase border-retro-thick border-retro-ink cursor-pointer
    - Active tab: bg-retro-cream border-b-0
    - Inactive tab: bg-retro-gray
    - focus-visible: outline-2 outline-offset-2 outline-retro-amber
    - Clicking inactive tab calls onTabChange(key)
  </behavior>
  <action>
    **RetroCard.tsx** — Create using `forwardRef<HTMLDivElement, RetroCardProps>`:
    - Interface: `interface RetroCardProps { className?: string; children: ReactNode }`
    - Classes: `border-retro-thick border-retro-ink bg-retro-cream shadow-retro-raised p-md ${className || ""}`
    - Forward ref, render children
    - `RetroCard.displayName = "RetroCard"`

    **RetroBadge.tsx** — Simple component (no need for forwardRef since badges are non-interactive):
    - Interface: `interface RetroBadgeProps { variant?: "neutral" | "success" | "danger" | "warning" | "info"; className?: string; children: ReactNode }`
    - Variant map: `{ neutral: "bg-retro-gray text-white", success: "bg-retro-green text-white", danger: "bg-retro-red text-white", warning: "bg-retro-amber text-retro-ink", info: "bg-retro-blue text-white" }`
    - Base classes: `inline-flex items-center border-retro-thick border-retro-ink text-[12px] font-bold uppercase py-[2px] px-sm leading-[1.2]`
    - Render as `<span>`

    **RetroTabs.tsx** — Controlled component:
    - Interface: `interface RetroTabsProps { tabs: { key: string; label: string }[]; activeTab: string; onTabChange: (key: string) => void; className?: string }`
    - Tab base classes: `min-w-[120px] h-[36px] text-[14px] font-bold uppercase border-retro-thick border-retro-ink cursor-pointer outline-2 outline-offset-2 outline-transparent focus-visible:outline-retro-amber`
    - Active: append `bg-retro-cream border-b-0`
    - Inactive: append `bg-retro-gray`
    - Container: `<div className={"flex " + (className || "")}>`
    - Each tab: `<button type="button" key={tab.key} onClick={() => onTabChange(tab.key)} className={...}>{tab.label}</button>`

    **Tests:**
    - RetroCard.test.tsx: renders children, applies border and shadow classes, forwards ref, merges className
    - RetroBadge.test.tsx: renders children text, default variant is neutral (bg-retro-gray), each variant applies correct background color class, merges className
    - RetroTabs.test.tsx: renders all tab labels, active tab has bg-retro-cream and border-b-0, inactive tab has bg-retro-gray, clicking tab calls onTabChange with key
  </action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system/frontend2 && bunx vitest run src/components/retro/__tests__/RetroCard.test.tsx src/components/retro/__tests__/RetroBadge.test.tsx src/components/retro/__tests__/RetroTabs.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - RetroCard.tsx exports `RetroCard`, contains `forwardRef<HTMLDivElement`, contains `p-md` and `shadow-retro-raised`
    - RetroBadge.tsx exports `RetroBadge`, contains all 5 variant strings: `bg-retro-gray`, `bg-retro-green`, `bg-retro-red`, `bg-retro-amber`, `bg-retro-blue`
    - RetroBadge.tsx contains `text-[12px]` and `font-bold` and `uppercase`
    - RetroTabs.tsx exports `RetroTabs`, contains `min-w-[120px]` and `h-[36px]` and `border-b-0`
    - RetroTabs.tsx contains `onTabChange` callback in props interface
    - All 3 test files pass: exit code 0
  </acceptance_criteria>
  <done>RetroCard, RetroBadge (5 variants), and RetroTabs (file-folder style) are implemented with passing tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build RetroDialog, RetroTable components with tests; update barrel export</name>
  <files>
    frontend2/src/components/retro/RetroDialog.tsx,
    frontend2/src/components/retro/RetroTable.tsx,
    frontend2/src/components/retro/__tests__/RetroDialog.test.tsx,
    frontend2/src/components/retro/__tests__/RetroTable.test.tsx,
    frontend2/src/components/retro/index.ts
  </files>
  <read_first>
    frontend2/src/styles/globals.css,
    frontend2/src/components/retro/RetroPanel.tsx,
    frontend2/src/components/retro/HazardStripe.tsx,
    frontend2/src/components/retro/index.ts,
    frontend2/src/test-utils.tsx,
    .planning/phases/50-design-system/50-UI-SPEC.md
  </read_first>
  <behavior>
    RetroDialog:
    - Uses native <dialog> element with showModal() for focus trapping and backdrop
    - Imperative API via useImperativeHandle: { open(): void; close(): void }
    - Exported handle type: RetroDialogHandle
    - Border: border-retro-extra-thick (4px) border-retro-ink
    - Background: bg-retro-cream, shadow-retro-raised, p-0 (content handles its own padding)
    - Backdrop: backdrop:bg-black/50
    - Max width: max-w-[480px] w-full mx-auto
    - Always shows HazardStripe at top
    - Always shows red X close button at top-right (same pattern as RetroPanel close)
    - Escape key closes (native dialog behavior)
    - onClose callback fires when dialog closes
    - Renders children below hazard stripe

    RetroTable:
    - Props: columns: { key: string; header: string; className?: string }[], data: Record<string, ReactNode>[], className?: string
    - Wrapped in overflow-x-auto div for mobile
    - Table: w-full border-collapse, border-retro-thick border-retro-ink
    - Header: bg-retro-charcoal, text white text-[14px] font-bold uppercase font-sans, cells py-sm px-md, border-retro-thick border-retro-ink
    - Rows: alternating bg-retro-cream (odd) and white (even)
    - Data cells: font-mono text-[14px], py-sm px-md, border-retro-thick border-retro-ink
  </behavior>
  <action>
    **RetroDialog.tsx** — Create using `forwardRef<RetroDialogHandle, RetroDialogProps>`:
    - Import `HazardStripe` from `./HazardStripe`
    - Interface: `interface RetroDialogProps { onClose?: () => void; children: ReactNode }`
    - Export: `export interface RetroDialogHandle { open: () => void; close: () => void }`
    - Internal `dialogRef = useRef<HTMLDialogElement>(null)`
    - `useImperativeHandle(ref, () => ({ open: () => dialogRef.current?.showModal(), close: () => dialogRef.current?.close() }))`
    - Dialog element classes: `bg-retro-cream border-retro-extra-thick border-retro-ink shadow-retro-raised p-0 max-w-[480px] w-full backdrop:bg-black/50`
    - Inside dialog, render a relative container div with `p-lg pt-md`:
      - Close button (same as RetroPanel): `<button type="button" onClick={() => dialogRef.current?.close()} aria-label="Close" className="absolute top-sm right-sm w-[24px] h-[24px] bg-retro-red border-retro-thick border-retro-ink flex items-center justify-center text-white text-[12px] font-bold leading-none cursor-pointer hover:brightness-110">X</button>`
      - `<HazardStripe className="mb-md" />`
      - `{children}`
    - Set `onClose` on the `<dialog>` element to fire the callback
    - `RetroDialog.displayName = "RetroDialog"`

    Note: jsdom does not implement `HTMLDialogElement.showModal()`. In tests, mock it:
    ```typescript
    HTMLDialogElement.prototype.showModal = vi.fn(function(this: HTMLDialogElement) { this.setAttribute("open", ""); });
    HTMLDialogElement.prototype.close = vi.fn(function(this: HTMLDialogElement) { this.removeAttribute("open"); });
    ```

    **RetroTable.tsx** — Presentational component:
    - Interface: `interface RetroTableColumn { key: string; header: string; className?: string }`
    - Interface: `interface RetroTableProps { columns: RetroTableColumn[]; data: Record<string, ReactNode>[]; className?: string }`
    - Outer div: `overflow-x-auto ${className || ""}`
    - Table: `w-full border-collapse`
    - Thead: `<tr>` with `<th>` for each column: `bg-retro-charcoal text-white text-[14px] font-bold uppercase font-sans py-sm px-md border-retro-thick border-retro-ink text-left`
    - Tbody: `<tr>` for each data row: odd rows get `bg-retro-cream`, even rows get `bg-white`
    - Each `<td>`: `font-mono text-[14px] text-retro-ink py-sm px-md border-retro-thick border-retro-ink ${column.className || ""}`
    - Cell content: `data[rowIndex][column.key]`

    **Update index.ts** — Add exports:
    ```
    export { RetroCard } from "./RetroCard";
    export { RetroDialog } from "./RetroDialog";
    export type { RetroDialogHandle } from "./RetroDialog";
    export { RetroTable } from "./RetroTable";
    export { RetroTabs } from "./RetroTabs";
    export { RetroBadge } from "./RetroBadge";
    ```
    (Keep the existing 4 exports from Plan 01, append these 5 new ones plus the type export.)

    **Tests:**
    - RetroDialog.test.tsx: Add showModal/close mocks in beforeEach. Test: renders dialog element, open() calls showModal, close() calls native close, renders hazard stripe (bg-hazard-stripe class in dialog), renders close button with aria-label="Close", renders children
    - RetroTable.test.tsx: Test with sample columns=[{key:"name",header:"Name"},{key:"status",header:"Status"}] and data=[{name:"Item A",status:"Active"}]. Verify: header cells render "Name" and "Status" text, data cells render "Item A" and "Active", table wrapped in overflow-x-auto, header has bg-retro-charcoal class, data cells have font-mono class
  </action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system/frontend2 && bunx vitest run src/components/retro/__tests__/RetroDialog.test.tsx src/components/retro/__tests__/RetroTable.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - RetroDialog.tsx exports `RetroDialog` and `RetroDialogHandle`
    - RetroDialog.tsx contains `useImperativeHandle` and `showModal` and `border-retro-extra-thick`
    - RetroDialog.tsx contains `aria-label="Close"` and `bg-retro-red` and `backdrop:bg-black/50`
    - RetroDialog.tsx imports `HazardStripe`
    - RetroTable.tsx exports `RetroTable`
    - RetroTable.tsx contains `overflow-x-auto` and `bg-retro-charcoal` and `font-mono` and `border-collapse`
    - index.ts exports all 9 components: RetroButton, RetroPanel, RetroInput, HazardStripe, RetroCard, RetroDialog, RetroTable, RetroTabs, RetroBadge
    - index.ts exports `RetroDialogHandle` type
    - All tests pass: `cd frontend2 && bunx vitest run src/components/retro/__tests__/` exits 0
  </acceptance_criteria>
  <done>RetroDialog works with imperative open/close API via native dialog element; RetroTable renders header and alternating rows; barrel export includes all 9 components; all tests green</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** 1
**Threats identified:**
- RetroDialog must not prevent Escape key closing (native `<dialog>` handles this automatically) -- mitigated by using native dialog element without intercepting keydown events
- RetroTable renders ReactNode cell content: if used with user-generated content, React's default JSX escaping prevents XSS -- accept (React handles escaping by default)
**Residual risk:** Low -- pure UI components with no data processing or network access
</threat_model>

<verification>
All component unit tests pass: `cd frontend2 && bunx vitest run src/components/retro/__tests__/`
TypeScript compiles: `cd frontend2 && bunx tsc --noEmit`
Build succeeds: `cd frontend2 && bun run build`
</verification>

<success_criteria>
- Five additional retro components (RetroCard, RetroDialog, RetroTable, RetroTabs, RetroBadge) fully implemented
- RetroDialog uses native `<dialog>` with imperative open/close API
- RetroTable renders alternating rows with overflow-x-auto wrapper for mobile
- RetroTabs renders file-folder tab bar with active/inactive states
- RetroBadge has 5 color variants (neutral, success, danger, warning, info)
- Barrel export includes all 9 components plus RetroDialogHandle type
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/50-design-system/50-02-SUMMARY.md`
</output>
