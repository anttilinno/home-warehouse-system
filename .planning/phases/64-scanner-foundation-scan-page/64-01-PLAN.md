---
phase: 64
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend2/package.json
  - frontend2/src/components/retro/HazardStripe.tsx
autonomous: true
requirements: []
requirements_addressed: []
must_haves:
  truths:
    - "@yudiel/react-qr-scanner@2.5.1 is installed in frontend2/node_modules"
    - "uuid@^13.0.0 is installed in frontend2/node_modules"
    - "@types/uuid@^11.0.0 is installed in frontend2/node_modules (devDependency)"
    - "ios-haptics is NOT in frontend2/package.json (D-17 compliance)"
    - "HazardStripe accepts variant='yellow' | 'red' prop; default is 'yellow'"
    - "bun run lint:imports passes against the full frontend2/src tree"
  artifacts:
    - path: "frontend2/package.json"
      provides: "Runtime dep declarations for scanner library + UUID; devDep for @types/uuid"
      contains: "@yudiel/react-qr-scanner"
    - path: "frontend2/src/components/retro/HazardStripe.tsx"
      provides: "HazardStripe atom with variant prop (yellow default, red for library-init-fail panel)"
      exports: ["HazardStripe"]
  key_links:
    - from: "frontend2/package.json"
      to: "frontend2/bun.lock"
      via: "bun install recording integrity hashes for @yudiel/react-qr-scanner, uuid, @types/uuid"
      pattern: "@yudiel/react-qr-scanner"
---

<objective>
Wave 0 scaffolding plan (half 1 of 2): add three new JS dependencies to frontend2/package.json and extend the existing `HazardStripe` retro atom with a `variant` prop so the Phase 64 `ScanErrorPanel` library-init-fail variant can render a red hazard stripe per UI-SPEC §Color. Blocks Waves 1-3 because downstream code imports from these deps and prop.

Purpose: Installing deps and wiring the HazardStripe variant must happen before any scanner code is written (lib/scanner, hooks, components all import from `@yudiel/react-qr-scanner` or depend on HazardStripe variant accepting `"red"`).

Output: package.json with three new dep entries + frontend2/bun.lock updated; HazardStripe.tsx accepts `variant?: "yellow" | "red"` (default "yellow"); file count under `frontend2/src/components/retro/` unchanged (UI-SPEC Acceptance Gate #4 respected).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-scanner-foundation-scan-page/64-CONTEXT.md
@.planning/phases/64-scanner-foundation-scan-page/64-UI-SPEC.md
@.planning/phases/64-scanner-foundation-scan-page/64-RESEARCH.md
@.planning/phases/64-scanner-foundation-scan-page/64-PATTERNS.md
@frontend2/package.json
@frontend2/src/components/retro/HazardStripe.tsx
@frontend2/src/components/retro/index.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add scanner + uuid runtime deps (no ios-haptics per D-17)</name>
  <read_first>
    - frontend2/package.json (current deps block — lines 39-51 target)
    - .planning/phases/64-scanner-foundation-scan-page/64-CONTEXT.md §Post-Discuss Amendments (D-17 locks ios-haptics OUT of Phase 64)
    - .planning/phases/64-scanner-foundation-scan-page/64-PATTERNS.md §37 package.json edit (lines 1247-1270)
    - .planning/phases/64-scanner-foundation-scan-page/64-RESEARCH.md §Installation + §Version verification (lines 184-215)
  </read_first>
  <files>frontend2/package.json</files>
  <action>
    Run from the `frontend2/` directory (shell `cd frontend2` first):

    1. `bun add @yudiel/react-qr-scanner@2.5.1` — exact pin, NO caret. Per RESEARCH.md §Core and CONTEXT.md D-01 lock.
    2. `bun add uuid@^13.0.0`
    3. `bun add -d @types/uuid@^11.0.0` — npm registry flags this as deprecated; install anyway per CONTEXT.md lock (parity with `/frontend`). The deprecation warning is expected and acceptable.

    Do NOT run `bun add ios-haptics` — D-17 explicitly removes it from Phase 64 scope. If any tooling auto-suggests it, decline.

    After installation:
    - `dependencies` block must contain EXACTLY: `"@yudiel/react-qr-scanner": "2.5.1"` (no caret, no tilde) and `"uuid": "^13.0.0"` alongside existing deps.
    - `devDependencies` block must contain `"@types/uuid": "^11.0.0"` alongside existing devDeps.
    - `frontend2/bun.lock` is updated and committed in the same commit as package.json.

    Verify transitive footprint matches RESEARCH.md §Transitive dependencies: `bun pm ls | grep -E "(barcode-detector|zxing-wasm|webrtc-adapter|sdp)"` should report `barcode-detector@3.0.8`, `zxing-wasm@2.2.4` (NOT 3.0.2), `webrtc-adapter@9.0.3`, `sdp@^3.2.0`.
  </action>
  <verify>
    <automated>cd frontend2 && grep -q '"@yudiel/react-qr-scanner": "2.5.1"' package.json && grep -q '"uuid": "\^13.0.0"' package.json && grep -q '"@types/uuid": "\^11.0.0"' package.json && ! grep -q "ios-haptics" package.json && ls node_modules/@yudiel/react-qr-scanner/package.json && ls node_modules/uuid/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"@yudiel/react-qr-scanner": "2.5.1"' frontend2/package.json` outputs `1` (exact version, no caret)
    - `grep -c '"uuid": "\^13.0.0"' frontend2/package.json` outputs `1`
    - `grep -c '"@types/uuid": "\^11.0.0"' frontend2/package.json` outputs `1`
    - `grep -c "ios-haptics" frontend2/package.json` outputs `0` (D-17 compliance)
    - `frontend2/node_modules/@yudiel/react-qr-scanner/package.json` exists
    - `frontend2/node_modules/barcode-detector/package.json` exists (transitive)
    - `frontend2/node_modules/zxing-wasm/package.json` exists (transitive)
    - `frontend2/bun.lock` diff is non-empty (lockfile updated with new integrity hashes)
    - `cd frontend2 && bun run lint:imports` exits 0 (guardrail: no new forbidden imports introduced by the dep additions themselves; scanned only against `frontend2/src/`)
  </acceptance_criteria>
  <done>
    package.json has exactly three new entries (2 dep + 1 devDep). bun.lock updated. ios-haptics is absent. Forbidden-imports lint still green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add variant prop to HazardStripe retro atom</name>
  <read_first>
    - frontend2/src/components/retro/HazardStripe.tsx (current impl — single class, no variant)
    - frontend2/src/components/retro/__tests__/HazardStripe.test.tsx (existing test — MUST be kept green; add cases for new variant)
    - .planning/phases/64-scanner-foundation-scan-page/64-UI-SPEC.md §Color (lines 122-132) — red vs yellow disposition per error panel
    - .planning/phases/64-scanner-foundation-scan-page/64-PATTERNS.md §Retro Component Fit Audit (lines 909-928) — confirms this is legal under UI-SPEC Acceptance Gate #4 because file count is unchanged
    - .planning/phases/64-scanner-foundation-scan-page/64-RESEARCH.md §Discrepancies Flagged #3 (lines 1307) — confirms the option a recommendation
    - frontend2/src/styles/globals.css (to confirm `bg-hazard-stripe` token + confirm a red hazard stripe utility or fallback color)
  </read_first>
  <behavior>
    - Test 1: default render (no `variant` prop) retains existing yellow hazard stripe visual (backward-compat — the existing test must still pass without modification to its expectations)
    - Test 2: `variant="yellow"` renders with the same yellow stripe class / styling as the default
    - Test 3: `variant="red"` renders with a red background stripe (test by asserting presence of a `bg-retro-red` class OR by asserting the rendered element carries a distinct `data-variant="red"` attribute — implementation chooses one)
    - Test 4: `className` prop still merges correctly with any variant (no override of user classes)
  </behavior>
  <files>
    frontend2/src/components/retro/HazardStripe.tsx, frontend2/src/components/retro/__tests__/HazardStripe.test.tsx
  </files>
  <action>
    RED → GREEN → REFACTOR against the `<behavior>` block above.

    Implementation target (~5 LOC delta per RESEARCH.md):

    ```tsx
    // frontend2/src/components/retro/HazardStripe.tsx
    import type { HTMLAttributes } from "react";

    interface HazardStripeProps extends HTMLAttributes<HTMLDivElement> {
      variant?: "yellow" | "red";
    }

    export function HazardStripe({
      variant = "yellow",
      className = "",
      ...props
    }: HazardStripeProps) {
      const variantClass =
        variant === "red" ? "bg-retro-red" : "bg-hazard-stripe";
      return (
        <div
          data-variant={variant}
          className={`h-md border-b-retro-thick border-retro-ink ${variantClass} ${className}`}
          {...props}
        />
      );
    }
    ```

    Adjust the exact class concatenation to match the CURRENT HazardStripe.tsx structure read in `<read_first>` — only change what's needed to introduce the variant. Do NOT add new files. Do NOT rename. The `frontend2/src/components/retro/` directory file count MUST stay the same (count before = count after) to honor UI-SPEC Acceptance Gate #4.

    The existing public barrel `frontend2/src/components/retro/index.ts` already exports `HazardStripe`; no barrel change needed. Verify.

    Write the 3 new test cases for variant="yellow", variant="red", and variant="red" + custom className into `frontend2/src/components/retro/__tests__/HazardStripe.test.tsx` BEFORE implementing. Run `cd frontend2 && bun run test -- HazardStripe` and confirm tests fail (RED). Then implement. Confirm tests pass (GREEN). No refactor required unless readability demands it.

    Do NOT regress the existing test assertions — read them first and keep their expectations intact.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test -- HazardStripe --run</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend2 && bun run test -- HazardStripe --run` exits 0 with at least 3 passing test cases (or the prior count plus 3) covering variant="yellow", variant="red", variant + className merge
    - `grep -c 'variant\?: "yellow" | "red"' frontend2/src/components/retro/HazardStripe.tsx` outputs at least `1` (or the TS-equivalent single-quote form — at least one match of the union type)
    - `grep -c 'variant = "yellow"' frontend2/src/components/retro/HazardStripe.tsx` outputs at least `1` (default value)
    - `grep -c 'bg-retro-red' frontend2/src/components/retro/HazardStripe.tsx` outputs at least `1` (red branch applied)
    - File count under `frontend2/src/components/retro/` unchanged: `ls frontend2/src/components/retro/*.tsx | wc -l` returns the same count it did pre-task (UI-SPEC Acceptance Gate #4)
    - `cd frontend2 && bun run test --run` exits 0 (full suite stays green — no regressions from the variant edit)
  </acceptance_criteria>
  <done>
    HazardStripe accepts `variant?: "yellow" | "red"` with `"yellow"` default. Existing call sites still render yellow (backward-compat). ScanErrorPanel can later import `HazardStripe` from `@/components/retro` barrel and pass `variant="red"` for the library-init-fail panel. Full suite is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | Supply-chain: pinned exact version for @yudiel/react-qr-scanner; caret for uuid/@types/uuid |
| component public API (HazardStripe props) → internal class concat | Prop injection via `className` passthrough |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01 | Tampering | npm dep (@yudiel/react-qr-scanner@2.5.1) | mitigate | Exact version pin (no caret) + bun.lock integrity hashes; legacy `/frontend` has shipped same version in production since v1.9; monitor GitHub Advisory DB |
| T-64-02 | Tampering | transitive zxing-wasm@2.2.4 | mitigate | Integrity recorded in bun.lock; flagged in RESEARCH.md §Security Domain; WASM binary fetched on demand — no static pre-execution |
| T-64-03 | Information Disclosure | @types/uuid deprecation warning | accept | npm-reported deprecation (uuid v13 ships own types); install anyway per CONTEXT.md lock. No runtime impact. |
| T-64-04 | Tampering | HazardStripe className prop passthrough | accept | Existing atom already spreads `...props` onto `<div>` — adding `variant` does not expand the attack surface |

</threat_model>

<verification>
- `cd frontend2 && bun run test --run` is green
- `cd frontend2 && bun run lint:imports` is green
- `cd frontend2 && bun run build` still builds (should pass; no new source files yet — just deps)
- `ls frontend2/src/components/retro/*.tsx | wc -l` count unchanged vs pre-plan
- `cat frontend2/package.json | jq '.dependencies | keys | map(select(. == "ios-haptics")) | length'` returns `0` (D-17)
</verification>

<success_criteria>
- Three deps added (@yudiel/react-qr-scanner@2.5.1, uuid@^13.0.0, @types/uuid@^11.0.0 as devDep); ios-haptics absent (D-17)
- HazardStripe accepts `variant?: "yellow" | "red"`; default yellow; red branch renders `bg-retro-red`
- File count in `frontend2/src/components/retro/` unchanged (Acceptance Gate #4)
- Full Vitest suite green; lint:imports green
</success_criteria>

<output>
After completion, create `.planning/phases/64-scanner-foundation-scan-page/64-01-SUMMARY.md` recording: exact versions in package.json after bun add, bun.lock integrity delta summary (`git diff --stat frontend2/bun.lock`), HazardStripe variant API shape, and confirmation that D-17 is honored.
</output>
