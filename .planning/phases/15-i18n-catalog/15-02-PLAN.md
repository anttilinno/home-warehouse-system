---
phase: 15-i18n-catalog
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-i18n-format.mjs
  - scripts/check-i18n-catalog.mjs
  - scripts/__tests__/check-i18n-format.test.mjs
  - scripts/__tests__/check-i18n-catalog.test.mjs
  - scripts/__tests__/fixtures-i18n-format/clean.tsx
  - scripts/__tests__/fixtures-i18n-format/offender-tolocale.tsx
  - scripts/__tests__/fixtures-i18n-format/ignored.tsx
  - scripts/__tests__/fixtures-i18n-catalog/en.po
  - scripts/__tests__/fixtures-i18n-catalog/et-complete.po
  - scripts/__tests__/fixtures-i18n-catalog/et-empty.po
  - scripts/__tests__/fixtures-i18n-catalog/ru-missing-msgid.po
  - frontend2/package.json
  - .github/workflows/lint-frontend2.yml
autonomous: true
requirements: [I18N-01, I18N-03]

must_haves:
  truths:
    - "check-i18n-format.mjs fails on un-ignored raw toLocale*/Date.toString in features+components, passes when ignored/under lib/format/test"
    - "check-i18n-catalog.mjs fails when msgid sets diverge across en/et/ru or any et/ru msgstr is empty, passes on a complete tree"
    - "check-i18n-catalog.mjs supports --only <locale> for single-locale verification"
    - "Both guards ship node --test self-tests with fixtures"
    - "package.json has lint:i18n and lint:i18n:format script entries"
    - "lint-frontend2.yml runs both guards as jobs with paths covering the new scripts"
  artifacts:
    - path: "scripts/check-i18n-format.mjs"
      provides: "Grep guard for raw locale-formatting in feature/component code"
    - path: "scripts/check-i18n-catalog.mjs"
      provides: "po msgid-parity + empty-msgstr manifest guard (+ --only flag)"
    - path: "scripts/__tests__/check-i18n-format.test.mjs"
      provides: "Self-test proving fail-on-offender + pass-on-ignored"
    - path: "scripts/__tests__/check-i18n-catalog.test.mjs"
      provides: "Self-test proving fail-on-divergence + fail-on-empty"
  key_links:
    - from: ".github/workflows/lint-frontend2.yml"
      to: "scripts/check-i18n-format.mjs + check-i18n-catalog.mjs"
      via: "new jobs running the guards + their self-tests"
      pattern: "check-i18n-(format|catalog)\\.mjs"
    - from: "frontend2/package.json"
      to: "the guard scripts (repo-root ../scripts)"
      via: "lint:i18n / lint:i18n:format script entries"
      pattern: "lint:i18n"
---

<objective>
Build the two CI guards that ENFORCE the i18n contract going forward, plus their
self-tests, the package.json script entries, and the CI workflow wiring.

- `scripts/check-i18n-format.mjs` (D-4): fails on un-ignored raw locale formatting
  in `frontend2/src/features` + `frontend2/src/components` (the enforcement layer
  behind 15-01's render-site routing).
- `scripts/check-i18n-catalog.mjs` (D-5): the extract→merge→diff manifest guard —
  fails when en/et/ru msgid sets diverge OR any et/ru msgstr is empty (the live
  enforcement of I18N-01).

Purpose: lock in I18N-01 + I18N-03 so regressions are caught in CI.

Output: two `.mjs` guards, two `__tests__` self-tests with fixtures, two new
package.json scripts, two new CI jobs + `paths` entries.

This plan touches NO `frontend2/src/**` feature code and NO `src/locales/**` po
files (single-writer D-6). It owns BOTH guards, BOTH script entries, and the CI.

CRITICAL placement: existing guards live at REPO-ROOT `scripts/` (NOT
`frontend2/scripts/`), and `lint:imports` already invokes `node ../scripts/...`
from inside frontend2. Mirror that EXACTLY: new guards go in repo-root `scripts/`,
package.json scripts use `node ../scripts/check-i18n-*.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Absolute cwds:
- repo root: `/home/antti/Repos/Misc/home-warehouse-system`
- frontend: `/home/antti/Repos/Misc/home-warehouse-system/frontend2`
Run `bun install --frozen-lockfile` (in frontend2) once if node_modules is cold.
</execution_context>

<context>
@.planning/phases/15-i18n-catalog/15-CONTEXT.md
@.planning/phases/15-i18n-catalog/15-VALIDATION.md

<interfaces>
<!-- Mirror this EXACT guard+self-test pattern. -->
From scripts/check-forbidden-imports.mjs:
  - resolves REPO_ROOT via fileURLToPath(import.meta.url) + "..".
  - accepts an optional CLI arg = scan root (override for tests).
  - walks .ts/.tsx/.js/.jsx/.mjs/.cjs, skips node_modules.
  - collects offenders[], prints to stderr, process.exit(1) if any, else exit 0 + OK log.

From scripts/__tests__/check-forbidden-imports.test.mjs:
  - node:test + node:assert/strict + spawnSync.
  - SCRIPT = resolve(__dirname,"..","check-...mjs"); FIXTURES dir alongside.
  - asserts r.status===1 + stderr matches offender; asserts safe fixtures NOT flagged;
    asserts real frontend2/src passes (status 0).

po file shape (from frontend2/src/locales/<l>/messages.po):
  - header block: `msgid ""` / `msgstr ""` + folded "...\n" lines (SKIP this entry).
  - each entry: optional `#: source` comments, then `msgid "..."`, then `msgstr "..."`.
  - multi-line msgid/msgstr fold onto continuation `"..."` lines.
  - ICU placeholders appear inside msgids, e.g. {0}, {n}, {workspaceName}, %.
-->

D-4 format-guard spec:
  Scan frontend2/src/features + frontend2/src/components.
  FAIL on a line containing any of: `.toLocaleDateString(`, `.toLocaleTimeString(`,
  `.toLocaleString(`, or a Date `.toString()` (date-context .toString — match the
  literal `Date` ... `.toString()` form per D-4; keep the regex conservative to
  avoid flagging unrelated .toString()).
  EXCEPT: lines tagged `// i18n-format-ignore`, anything under `src/lib/format/`,
  and `*.test.*` files. Must NOT flag `Intl.NumberFormat` (money.ts).

D-5 catalog-guard spec:
  Parse the 3 po files (en/et/ru). FAIL if msgid sets differ across the three
  (report orphaned + missing per locale) OR any et/ru msgstr is empty (excluding
  the header entry). Clear diff report to stderr. Exit 0 on a complete, parity tree.
  ALSO accept a `--only <locale>` flag: when present, check ONLY that locale's
  empty-msgstr coverage + its msgid parity against en, ignoring the other locale.
  This lets each Wave-2 translation plan verify its OWN locale before the sibling
  locale has landed (et and ru plans run in parallel).
</interfaces>

@scripts/check-forbidden-imports.mjs
@scripts/__tests__/check-forbidden-imports.test.mjs
@.github/workflows/lint-frontend2.yml
@frontend2/package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: check-i18n-format.mjs + self-test + fixtures</name>
  <files>scripts/check-i18n-format.mjs, scripts/__tests__/check-i18n-format.test.mjs, scripts/__tests__/fixtures-i18n-format/clean.tsx, scripts/__tests__/fixtures-i18n-format/offender-tolocale.tsx, scripts/__tests__/fixtures-i18n-format/ignored.tsx</files>
  <behavior>
    - clean.tsx (uses a useDateFormat hook, no raw toLocale) is NOT flagged.
    - offender-tolocale.tsx (a line `new Date(x).toLocaleString()`) is flagged, exit 1.
    - ignored.tsx (a `.toLocaleTimeString(...) // i18n-format-ignore` line) is NOT flagged.
    - real frontend2/src/features + src/components exit 0 (proves 15-01 left it clean;
      this is the cross-plan link, kept as a self-test like the forbidden-imports one).
    - an `Intl.NumberFormat(...)` line is NOT flagged.
  </behavior>
  <action>Mirror check-forbidden-imports.mjs structure. Default scan roots: `frontend2/src/features` and `frontend2/src/components` (resolved from REPO_ROOT). Accept an optional CLI arg to override the scan root (for the self-test fixtures). Walk source files; for each line, skip if it contains `// i18n-format-ignore`; skip files under `src/lib/format/` and matching `*.test.*`. Flag a line matching the locale-format pattern `.toLocaleDateString(` / `.toLocaleTimeString(` / `.toLocaleString(` or a Date `.toString()` form (per D-4 — keep the Date.toString match narrow: require a Date token context, or document that bare `.toString()` is intentionally NOT matched to avoid false positives; choose the conservative interpretation and note it in a comment). Do NOT match `Intl.NumberFormat`. Collect offenders as `file:line: ...`, exit 1 with report, else log OK + exit 0. Write 3 fixtures + the node:test self-test FIRST (RED — guard not yet written), then implement to GREEN.</action>
  <verify>
    <automated>node --test /home/antti/Repos/Misc/home-warehouse-system/scripts/__tests__/check-i18n-format.test.mjs</automated>
  </verify>
  <done>Self-test green: offender flagged (exit 1), ignored/clean/Intl not flagged. (Real-tree `check-i18n-format.mjs` against frontend2 exits 0 only AFTER 15-01 merges — orchestrator confirms on the merged tree, NOT here.)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: check-i18n-catalog.mjs + self-test + fixtures</name>
  <files>scripts/check-i18n-catalog.mjs, scripts/__tests__/check-i18n-catalog.test.mjs, scripts/__tests__/fixtures-i18n-catalog/en.po, scripts/__tests__/fixtures-i18n-catalog/et-complete.po, scripts/__tests__/fixtures-i18n-catalog/et-empty.po, scripts/__tests__/fixtures-i18n-catalog/ru-missing-msgid.po</files>
  <behavior>
    - {en, et-complete, ru(parity+filled)} exits 0.
    - {en, et-empty (an entry with msgstr "")} exits 1, report names the empty msgid + locale.
    - {en, et-complete, ru-missing-msgid (drops one msgid)} exits 1, report names the missing/orphaned msgid + locale.
    - header entry (msgid "" / msgstr "") is NOT treated as an empty-translation failure.
    - `--only et` against {en, et-complete} exits 0; `--only et` against {en, et-empty} exits 1.
  </behavior>
  <action>Write a minimal po parser (no new deps): read each po file, split into entries on blank lines, skip the header entry (the one whose msgid is the empty string), extract msgid/msgstr (handle the multi-line continuation fold by concatenating the quoted segments). Build the msgid SET per locale. Default targets: the 3 real po files under `frontend2/src/locales/{en,et,ru}/messages.po` (resolved from REPO_ROOT); accept an optional CLI arg = a directory of `<locale>.po` fixtures for the self-test. FAIL (exit 1) if: en/et/ru msgid sets are not identical (print per-locale missing + orphaned lists) OR any et/ru entry has an empty msgstr (print `LOCALE: empty msgstr for <msgid>`). Else log a parity+coverage OK summary and exit 0. ADD a `--only <locale>` flag (parse from argv): when set, restrict the empty-msgstr + parity check to that single locale vs en, ignoring the third locale entirely (this is what the Wave-2 translation plans call to verify their own locale pre-merge). Optionally also assert ICU placeholder parity between en msgid and et/ru msgstr — implement if cheap, else leave a `// TODO stretch` and rely on translators (per landmine note). Write fixtures + node:test self-test FIRST (RED), then implement to GREEN. NOTE: at authoring time the REAL et/ru po files are still empty shells, so do NOT run the guard against the real tree in the self-test — the self-test runs against fixtures only. The orchestrator runs it against the real tree AFTER Wave 2 fills the translations.</action>
  <verify>
    <automated>node --test /home/antti/Repos/Misc/home-warehouse-system/scripts/__tests__/check-i18n-catalog.test.mjs</automated>
  </verify>
  <done>Self-test green: complete fixture set exits 0; empty-msgstr fixture exits 1 naming the msgid; missing-msgid fixture exits 1 naming the divergence; header entry never counts as empty; --only et behaves per behavior block.</done>
</task>

<task type="auto">
  <name>Task 3: package.json scripts + CI workflow wiring</name>
  <files>frontend2/package.json, .github/workflows/lint-frontend2.yml</files>
  <action>In `frontend2/package.json` scripts, add (mirroring the `lint:imports` repo-root invocation):
  - `"lint:i18n:format": "node ../scripts/check-i18n-format.mjs"`
  - `"lint:i18n": "node ../scripts/check-i18n-catalog.mjs"`
  Do NOT add them to `prebuild` (keep build fast; CI runs them as jobs). In `.github/workflows/lint-frontend2.yml`:
  - Extend BOTH the pull_request and push `paths` lists with: `scripts/check-i18n-format.mjs`, `scripts/check-i18n-catalog.mjs`, `scripts/__tests__/check-i18n-format.test.mjs`, `scripts/__tests__/check-i18n-catalog.test.mjs`.
  - Add two new jobs mirroring the `forbidden-imports` job shape (checkout + setup-bun): `i18n-format-guard` running `node scripts/check-i18n-format.mjs` then `node --test scripts/__tests__/check-i18n-format.test.mjs`; and `i18n-catalog-guard` running `node --test scripts/__tests__/check-i18n-catalog.test.mjs` then `node scripts/check-i18n-catalog.mjs` (the real-tree run goes GREEN after Wave 2 fills et/ru — add a YAML comment noting the self-test always runs, the real-tree run passes once translations land).
  - Validate the YAML parses (eyeball indentation against the existing jobs; if a linter is available, run it).</action>
  <verify>
    <!-- Do NOT run `bun run lint:i18n:format` in this isolated Wave-1 verify: 15-01's
         render-site routing is NOT merged yet, so the real-tree format guard exits 1
         BY DESIGN. Self-tests run fine in isolation; the real-tree guard is the
         orchestrator's post-merge gate (see <done>). -->
    <automated>node --test /home/antti/Repos/Misc/home-warehouse-system/scripts/__tests__/check-i18n-format.test.mjs /home/antti/Repos/Misc/home-warehouse-system/scripts/__tests__/check-i18n-catalog.test.mjs</automated>
  </verify>
  <done>package.json has lint:i18n + lint:i18n:format; lint:i18n:format passes against the merged frontend2 tree (needs 15-01 merged; orchestrator confirms on merged tree); CI has both guard jobs + extended paths; both self-tests green.</done>
</task>

</tasks>

<verification>
- `node --test scripts/__tests__/check-i18n-format.test.mjs` — green.
- `node --test scripts/__tests__/check-i18n-catalog.test.mjs` — green.
- `cd frontend2 && bun run lint:i18n:format` — exit 0 against the MERGED tree (depends on 15-01 routing all render sites; orchestrator confirms GREEN on the merged tree per the 14b post-merge-semantic-conflict lesson).
- `cd frontend2 && bun run lint:tsc && bun run test && bun run build && bun run lint:imports` — green (this plan adds no frontend src; run to confirm no accidental breakage).
- The catalog guard against the REAL tree (`node scripts/check-i18n-catalog.mjs`) is EXPECTED to fail until Wave 2 fills et/ru — by design; orchestrator runs it green after 15-03/15-04 land.
</verification>

<success_criteria>
- scripts/check-i18n-format.mjs + check-i18n-catalog.mjs exist, mirror the forbidden-imports pattern.
- check-i18n-catalog.mjs supports `--only <locale>`.
- Both have node --test self-tests with fixtures proving fail-on-offender + pass-on-clean.
- package.json: lint:i18n + lint:i18n:format entries.
- lint-frontend2.yml: two new guard jobs + paths cover the new scripts/tests.
- No frontend2/src or src/locales edits.
</success_criteria>

<output>
Create `.planning/phases/15-i18n-catalog/15-02-SUMMARY.md` when done, then commit:
`git add -A && git commit` with message `feat(15-02): i18n CI guards + scripts + workflow (I18N-01/03)` plus the Co-Authored-By trailer. Atomic commits per task if practical. Do NOT edit STATE.md or ROADMAP.md.
</output>
