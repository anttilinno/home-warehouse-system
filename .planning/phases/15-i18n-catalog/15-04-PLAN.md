---
phase: 15-i18n-catalog
plan: 04
type: execute
wave: 2
depends_on: [15-01, 15-02]
files_modified:
  - frontend2/src/locales/ru/messages.po
autonomous: true
requirements: [I18N-01]

must_haves:
  truths:
    - "Every non-header entry in locales/ru/messages.po has a non-empty msgstr"
    - "Russian translations are correct technical-UI Russian (lifted from legacy ru.json where the English source matches, translated natively otherwise)"
    - "Every ICU placeholder present in a msgid is preserved verbatim in its ru msgstr"
    - "No msgid or source-comment lines were edited — only msgstr values filled"
  artifacts:
    - path: "frontend2/src/locales/ru/messages.po"
      provides: "Complete Russian catalog (~1010 msgstr filled)"
      contains: "Language: ru"
  key_links:
    - from: "frontend2/src/locales/ru/messages.po"
      to: "check-i18n-catalog.mjs"
      via: "no empty msgstr, msgid parity with en/et"
      pattern: "Language: ru"
---

<objective>
Fill the Russian catalog: every empty msgstr in
`frontend2/src/locales/ru/messages.po` gets a correct technical-UI Russian
translation.

Purpose: I18N-01 requires en/et/ru complete with matching msgid sets and no empty
translations. Russian must read as correct, idiomatic technical-UI Russian.

Output: a fully-translated `locales/ru/messages.po` (~1010 entries).

SINGLE-WRITER (D-8): this plan owns ONLY `locales/ru/messages.po`. The et plan
(15-03) owns et. No other file is touched.

REFRESHED-SHELL CONTRACT (D-7): the orchestrator runs `bun run i18n:extract`
BETWEEN Wave 1 and Wave 2 and commits the refreshed catalogs (en becomes ~1010
source messages; et/ru become ~1010 EMPTY shells with parity msgids). You operate
on THAT refreshed ru shell. Fill msgstr values ONLY. NEVER add, remove, reorder,
or edit a msgid or a source-reference comment line — doing so breaks msgid parity
and the catalog guard. If a msgid is hard to translate, still provide the best
Russian rendering; never leave it empty and never alter the id.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Absolute cwds:
- repo root: `/home/antti/Repos/Misc/home-warehouse-system`
- frontend: `/home/antti/Repos/Misc/home-warehouse-system/frontend2`
Run `bun install --frozen-lockfile` (in frontend2) once if node_modules is cold.
</execution_context>

<context>
@.planning/phases/15-i18n-catalog/15-CONTEXT.md
@.planning/phases/15-i18n-catalog/15-VALIDATION.md

Translation reference (LIFT, don't invent — see 15-CONTEXT "Translation reference"):
@frontend/messages/en.json
@frontend/messages/ru.json

Strategy: legacy next-intl messages are keyed by nested key (e.g. `nav.features`),
NOT by English source. Build an English-value to ru-value map by walking en.json and
ru.json in parallel (identical key shape, 38 top-level groups, ~1472 leaf keys each).
For each Lingui msgid (msgid = English source string), if an exact English-value
match exists in that map, lift the ru translation. Otherwise translate natively in
correct, idiomatic technical-UI Russian for an inventory/warehouse application.
Overlap is partial — UI context differs — so expect to translate a meaningful
remainder by hand.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build English to Russian lift map and fill exact matches</name>
  <files>frontend2/src/locales/ru/messages.po</files>
  <action>From repo root, walk `frontend/messages/en.json` + `frontend/messages/ru.json` in parallel (identical key trees) to build a flat map from each English leaf value to its Russian leaf value. Parse `frontend2/src/locales/ru/messages.po` (the refreshed empty shell). For each entry whose msgstr is empty, if the msgid (English source) exactly matches a key in the lift map, write the Russian value as the msgstr. PRESERVE ICU placeholders verbatim — when a msgid contains `{0}`, `{n}`, `{workspaceName}`, `%`, etc., the Russian msgstr must contain the SAME placeholder tokens. If a legacy value uses a different placeholder syntax, reconcile to the Lingui msgid's exact tokens rather than blindly lifting. Edit ONLY msgstr lines; leave msgid + source-comment lines byte-identical, and preserve header + entry order exactly. Match the Lingui .po long-string fold the extractor produces. Implement as a careful read-transform-write of the po file. A throwaway node script is fine; if you write one, place it under /tmp so it never lands in the repo.</action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system && node scripts/check-i18n-catalog.mjs --only ru || echo "expected partial — remaining empties handled in Task 2"</automated>
  </verify>
  <done>Every msgid with a legacy English-value match has its ru msgstr filled; ru msgid count equals en (parity intact); no msgid or comment lines changed.</done>
</task>

<task type="auto">
  <name>Task 2: Translate the remainder natively to zero empty ru msgstr</name>
  <files>frontend2/src/locales/ru/messages.po</files>
  <action>Translate every still-empty ru msgstr by hand into correct, idiomatic technical-UI Russian appropriate for a warehouse/inventory application (buttons, dialogs, table headers, validation messages, empty states). Keep terminology consistent across entries (pick one Russian term per domain noun — item, location, container, loan, workspace — and reuse it; prefer standard software-localization conventions). Preserve ICU placeholders verbatim in every msgstr. Do not touch msgid or source-comment lines. This plan's success is verified per-locale via check-i18n-catalog.mjs --only ru (the et locale, owned by 15-03, runs in parallel and may still be empty pre-merge — that is NOT this plan's failure). The --only ru run must exit 0: zero empty ru msgstr and full ru/en msgid parity.</action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system && node scripts/check-i18n-catalog.mjs --only ru</automated>
  </verify>
  <done>Zero empty ru msgstr (header excluded); ICU placeholders preserved; ru/en msgid parity intact; full frontend gate green.</done>
</task>

</tasks>

<verification>
Run from repo root unless noted:
- `node scripts/check-i18n-catalog.mjs --only ru` — exit 0 (this plan's primary gate).
- After 15-03 (et) ALSO lands and the orchestrator merges both Wave-2 plans,
  `node scripts/check-i18n-catalog.mjs` (full, all three locales) exits 0.
- `cd frontend2 && bun run lint:tsc && bun run test && bun run build && bun run lint:imports` — green (po changes are build-time transformed; build must still succeed).
- Spot-check ICU-placeholder msgids (e.g. `{0} created.`, `{0} — loaned to {1}.`) to confirm the ru msgstr keeps the exact `{0}`/`{1}` tokens.
</verification>

<success_criteria>
- locales/ru/messages.po: every non-header entry has a non-empty Russian msgstr.
- ru/en msgid sets identical (parity); no msgid or source-comment edits.
- ICU placeholders preserved verbatim.
- No file other than locales/ru/messages.po modified.
- `check-i18n-catalog.mjs --only ru` exits 0; frontend gate green.
</success_criteria>

<output>
Create `.planning/phases/15-i18n-catalog/15-04-SUMMARY.md` when done, then commit:
`git add frontend2/src/locales/ru/messages.po && git commit` with message
`feat(15-04): fill Russian catalog (I18N-01)` plus the Co-Authored-By trailer.
Do NOT edit STATE.md or ROADMAP.md, and do NOT stage any other file.
</output>
