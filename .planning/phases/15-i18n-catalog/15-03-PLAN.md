---
phase: 15-i18n-catalog
plan: 03
type: execute
wave: 2
depends_on: [15-01, 15-02]
files_modified:
  - frontend2/src/locales/et/messages.po
autonomous: true
requirements: [I18N-01]

must_haves:
  truths:
    - "Every non-header entry in locales/et/messages.po has a non-empty msgstr"
    - "Estonian translations are native-quality (lifted from legacy et.json where the English source matches, translated natively otherwise)"
    - "Every ICU placeholder present in a msgid is preserved verbatim in its et msgstr"
    - "No msgid or source-comment lines were edited — only msgstr values filled"
  artifacts:
    - path: "frontend2/src/locales/et/messages.po"
      provides: "Complete Estonian catalog (~1010 msgstr filled)"
      contains: "Language: et"
  key_links:
    - from: "frontend2/src/locales/et/messages.po"
      to: "check-i18n-catalog.mjs"
      via: "no empty msgstr, msgid parity with en"
      pattern: "Language: et"
---

<objective>
Fill the Estonian catalog: every empty msgstr in
`frontend2/src/locales/et/messages.po` gets a native-quality Estonian translation.

Purpose: I18N-01 requires en/et/ru complete with matching msgid sets and no empty
translations; et is the user's native language, so the quality bar is high.

Output: a fully-translated `locales/et/messages.po` (~1010 entries).

SINGLE-WRITER (D-8): this plan owns ONLY `locales/et/messages.po`. The ru plan
(15-04) owns ru. No other file is touched.

REFRESHED-SHELL CONTRACT (D-7): the orchestrator runs `bun run i18n:extract`
BETWEEN Wave 1 and Wave 2 and commits the refreshed catalogs (en becomes ~1010
source messages; et/ru become ~1010 EMPTY shells with parity msgids). You operate
on THAT refreshed et shell. Fill msgstr values ONLY. NEVER add, remove, reorder,
or edit a msgid or a source-reference comment line — doing so breaks msgid parity
and the catalog guard. If a msgid is hard to translate, still provide the best
Estonian rendering; never leave it empty and never alter the id.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Absolute cwds:
- repo root: `/home/antti/Repos/Misc/home-warehouse-system`
- frontend: `/home/antti/Repos/Misc/home-warehouse-system/frontend2`
Run `bun install --frozen-lockfile` (in frontend2) once if node_modules is cold.
</execution_context>

<context>
@.planning/phases/15-i18n-catalog/15-CONTEXT.md
@.planning/phases/15-i18n-catalog/15-VALIDATION.md

Translation reference (LIFT, don't invent — see 15-CONTEXT "Translation reference"):
@frontend/messages/en.json
@frontend/messages/et.json

Strategy: legacy next-intl messages are keyed by nested key (e.g. `nav.features`),
NOT by English source. Build an English-value to et-value map by walking en.json and
et.json in parallel (identical key shape, 38 top-level groups, ~1472 leaf keys each).
For each Lingui msgid (msgid = English source string), if an exact English-value
match exists in that map, lift the et translation. Otherwise translate natively in
correct, idiomatic Estonian for a technical inventory/warehouse UI. Overlap is
partial — UI context differs — so expect to translate a meaningful remainder by hand.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build English to Estonian lift map and fill exact matches</name>
  <files>frontend2/src/locales/et/messages.po</files>
  <action>From repo root, walk `frontend/messages/en.json` + `frontend/messages/et.json` in parallel (identical key trees) to build a flat map from each English leaf value to its Estonian leaf value. Parse `frontend2/src/locales/et/messages.po` (the refreshed empty shell). For each entry whose msgstr is empty, if the msgid (English source) exactly matches a key in the lift map, write the Estonian value as the msgstr. PRESERVE ICU placeholders verbatim — when a msgid contains `{0}`, `{n}`, `{workspaceName}`, `%`, etc., the Estonian msgstr must contain the SAME placeholder tokens. If a legacy value uses a different placeholder syntax, reconcile to the Lingui msgid's exact tokens rather than blindly lifting. Edit ONLY msgstr lines; leave msgid + source-comment lines byte-identical, and preserve header + entry order exactly. Match the Lingui .po long-string fold the extractor produces. Implement as a careful read-transform-write of the po file. A throwaway node script is fine; if you write one, place it under /tmp so it never lands in the repo.</action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system && node scripts/check-i18n-catalog.mjs --only et || echo "expected partial — remaining empties handled in Task 2"</automated>
  </verify>
  <done>Every msgid with a legacy English-value match has its et msgstr filled; et msgid count equals en (parity intact); no msgid or comment lines changed.</done>
</task>

<task type="auto">
  <name>Task 2: Translate the remainder natively to zero empty et msgstr</name>
  <files>frontend2/src/locales/et/messages.po</files>
  <action>Translate every still-empty et msgstr by hand into correct, idiomatic Estonian appropriate for a technical warehouse/inventory UI (buttons, dialogs, table headers, validation messages, empty states). Keep terminology consistent across entries (pick one Estonian term per domain noun — item, location, container, loan, workspace — and reuse it). Preserve ICU placeholders verbatim in every msgstr. Do not touch msgid or source-comment lines. This plan's success is verified per-locale via `check-i18n-catalog.mjs --only et` (the ru locale, owned by 15-04, runs in parallel and may still be empty pre-merge — that is NOT this plan's failure). The `--only et` run must exit 0: zero empty et msgstr and full et/en msgid parity.</action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system && node scripts/check-i18n-catalog.mjs --only et</automated>
  </verify>
  <done>Zero empty et msgstr (header excluded); ICU placeholders preserved; et/en msgid parity intact; full frontend gate green.</done>
</task>

</tasks>

<verification>
Run from repo root unless noted:
- `node scripts/check-i18n-catalog.mjs --only et` — exit 0 (this plan's primary gate).
- After 15-04 (ru) ALSO lands and the orchestrator merges both Wave-2 plans,
  `node scripts/check-i18n-catalog.mjs` (full, all three locales) exits 0.
- `cd frontend2 && bun run lint:tsc && bun run test && bun run build && bun run lint:imports` — green (po changes are build-time transformed; build must still succeed).
- Spot-check ICU-placeholder msgids (e.g. `{0} created.`, `{0} — loaned to {1}.`) to confirm the et msgstr keeps the exact `{0}`/`{1}` tokens.
</verification>

<success_criteria>
- locales/et/messages.po: every non-header entry has a non-empty Estonian msgstr.
- et/en msgid sets identical (parity); no msgid or source-comment edits.
- ICU placeholders preserved verbatim.
- No file other than locales/et/messages.po modified.
- `check-i18n-catalog.mjs --only et` exits 0; frontend gate green.
</success_criteria>

<output>
Create `.planning/phases/15-i18n-catalog/15-03-SUMMARY.md` when done, then commit:
`git add frontend2/src/locales/et/messages.po && git commit` with message
`feat(15-03): fill Estonian catalog (I18N-01)` plus the Co-Authored-By trailer.
Do NOT edit STATE.md or ROADMAP.md, and do NOT stage any other file.
</output>
