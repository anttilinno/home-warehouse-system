---
phase: 12-settings
plan: 07
type: execute
wave: 3
depends_on: ["12-02", "12-03", "12-04", "12-06"]
files_modified:
  - frontend2/e2e/settings.spec.ts
autonomous: true
requirements: [SETT-01, SETT-02, SETT-05, SETT-10]
must_haves:
  truths:
    - "A live browser logs in ONCE, opens /settings, and sees the grouped-row landing"
    - "Navigating to Profile, editing the name, and saving persists against the real backend"
    - "Switching language persists and the UI reflects the change"
    - "Members page lists members, adds one by email, and removes one against the real backend"
  artifacts:
    - path: frontend2/e2e/settings.spec.ts
      provides: "live Playwright settings spec (landing → profile save; language; members)"
      contains: "settings"
  key_links:
    - from: frontend2/e2e/settings.spec.ts
      to: "real backend :8080 + Postgres via the /api proxy rewrite"
      via: "page.goto('/login') one login; page navigation + page.request for seeding"
      pattern: "getByLabel\\(\"Email\"\\)"
---

<objective>
A live Playwright settings spec that crosses the real HTTP boundary (per the CLAUDE.md §E2E runbook:
backend :8080 + Postgres + Vite :5173 with the load-bearing /api rewrite). It proves the hub end to
end against real endpoints: landing → Profile save; Language switch persists; Members list + add-by-
email + remove.

Critical constraint (CLAUDE.md + VALIDATION phase gate): **log in exactly ONCE** for the whole spec —
the backend auth limiter rate-limits repeated logins, which is why the full E2E suite is flaky when
each test re-authenticates. Use a single `test.beforeAll`/serial login (shared browser context) or one
`test.describe.serial` with a single login step, and reuse the authenticated cookie across the
sub-flows. Include the discovery-list as an in-plan gate: enumerate the new spec before running so the
orchestrator sees it is wired.

This is the live spec; the per-page MSW unit tests (Plans 12-03..12-06) are the fast inner loop. This
spec is the outer-loop parity proof.

Purpose: prove the settings hub works against the real stack with one authentication.
Output: frontend2/e2e/settings.spec.ts, discovered and (when the live stack is up) green.

Wave 3 — runs after the foundation (12-02), Profile (12-03), Language (12-04) and Members (12-06) land.
Parallel with the Members unit plan only in scheduling; it depends on Members shipping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/12-settings/12-UI-SPEC.md
@frontend2/e2e/login-dashboard.spec.ts

<interfaces>
<!-- Live-stack contract (CLAUDE.md §E2E). Mirror login-dashboard.spec.ts exactly. -->
Login: page.goto("/login"); getByLabel("Email").fill(E2E_USER); getByLabel("Password").fill(E2E_PASS);
  getByRole("button", { name: /^log in$/i }).click(); await expect(page).toHaveURL("/").
  E2E_USER default "seeder@test.local", E2E_PASS default "password123" (env-overridable).
After login the context holds the access_token cookie; both page and page.request inherit it (no manual
  token plumbing). API-only seeding: page.request.post("/api/workspaces/{wsId}/...").
playwright.config.ts: two projects (chromium+firefox), baseURL http://localhost:5173, NO webServer —
  the live stack must be up. Run: `cd frontend2 && E2E_USER=... E2E_PASS=... bun run test:e2e`.
Routes (from 12-02): /settings (landing), /settings/profile, /settings/language, /settings/members.
Landing rows are <Link>s with text labels + trailing chevron (no leading icons).
Profile: "Full name" + "Email" labels, primary "Save changes" (DOM "Save changes"; CSS uppercases).
Language: a select (English/Eesti/Русский); success toast "Language updated."
Members: a table (name/email/role) + add form (placeholder "user@email…" + role select + "Add") + per-row
  Remove (pink confirm "Remove"). add-by-email needs an EXISTING registered user's email — seed/choose a
  second known user, or assert the 404 path for an unregistered email if a second seed user is not
  guaranteed (document which in the summary; prefer seeding a second user via page.request if the API
  allows, else assert the unregistered-email error toast for determinism).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live settings spec (one login → landing, profile save, language, members)</name>
  <files>frontend2/e2e/settings.spec.ts</files>
  <action>
    Create `frontend2/e2e/settings.spec.ts` mirroring login-dashboard.spec.ts. Use a single
    `test.describe.serial("Settings hub", ...)` with ONE login performed once (a serial beforeAll that
    logs in on a shared page, or the first serial test logging in and subsequent tests reusing the same
    page) — do NOT re-login per test (auth limiter; CLAUDE.md). Sub-flows:
    (1) Landing — goto /settings (NOT a redirect to /settings/security); assert the grouped rows render
        (e.g. links for Profile, Language, Members, Data & Storage visible) and that clicking Profile
        navigates to /settings/profile.
    (2) Profile save — on /settings/profile, read the current "Full name", fill a new deterministic
        value (e.g. append a timestamp suffix), click "Save changes", assert a success toast/visible
        confirmation, reload the page, and assert the new name persisted (real backend round-trip). Reset
        the name back to the original at the end to keep the seed user stable (or use a value that is safe
        to leave).
    (3) Language — goto /settings/language, switch the select to Eesti (et), assert the success toast
        "Language updated.", reload, assert the select still shows et (persisted via GetMe). Switch back
        to English to leave the seed user in a known state.
    (4) Members — goto /settings/members, assert the list shows the seeder user with an email + role and
        a "YOU" badge on the own row. Add-by-email: if a second registered user is seedable/known, add
        them by email, assert the row appears, then remove them (pink confirm "Remove") and assert the row
        disappears. If a second user is NOT guaranteed, instead add an unregistered email and assert the
        "No registered user with that email" error surfaces (deterministic without seeding) — pick the
        approach that is reliable on this stack and document it in the summary.
    Use env E2E_USER/E2E_PASS with the documented defaults. Keep selectors resilient (getByRole/
    getByLabel/getByText, field-verb prefixes) per the recent E2E locator-loosening commits.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test:e2e -- --list e2e/settings.spec.ts</automated>
  </verify>
  <done>settings.spec.ts is discovered by Playwright (the --list gate enumerates its tests); one-login serial structure in place.</done>
</task>

<task type="auto">
  <name>Task 2: Run the spec against the live stack (if up) and record the result</name>
  <files>frontend2/e2e/settings.spec.ts</files>
  <action>
    Attempt a live run on chromium only (single project to halve auth-limiter pressure):
    `cd frontend2 && bun run test:e2e -- --project=chromium e2e/settings.spec.ts`. If the live stack is
    up (backend :8080 + Postgres + dev :5173 per CLAUDE.md), the spec must pass; fix any selector/flow
    mismatches against the shipped pages. If the stack is NOT up in this environment, the orchestrator's
    phase gate runs it isolated — record in the summary that the spec is discovered + structurally valid
    and note the exact run command + any env (E2E_USER/E2E_PASS) needed. Do NOT loosen assertions to make
    a red test green; if a real bug surfaces, log it as a residue for gap closure.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test:e2e -- --list e2e/settings.spec.ts</automated>
  </verify>
  <done>Live run attempted; pass recorded OR (stack down) discovery confirmed + run command documented; no assertions weakened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| live browser→real backend | the spec authenticates and mutates real workspace state (name, language, membership) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-15 | DoS (test infra) | repeated logins tripping the auth limiter | mitigate | exactly ONE login per spec (serial, shared context); single-project live run |
| T-12-16 | Tampering (test data) | spec mutating the seed user/workspace | mitigate | reset name + language to original; remove any added member; deterministic add/remove pairing |
| T-12-SC | Tampering | npm installs | mitigate | zero new packages; uses the installed @playwright/test |
</threat_model>

<verification>
- `cd frontend2 && bun run test:e2e -- --list e2e/settings.spec.ts` enumerates the new spec.
- Live (if stack up): `bun run test:e2e -- --project=chromium e2e/settings.spec.ts` green.
- Exactly one login in the spec (grep: a single `getByLabel("Email")` fill or one login helper call).
</verification>

<success_criteria>
- A live spec proves landing → profile save (persisted) → language switch (persisted) → members
  list/add-by-email/remove, with ONE authentication.
- Discovered by Playwright; green against the live stack or run-command documented for the gate.
</success_criteria>

<output>
Create `.planning/phases/12-settings/12-07-SUMMARY.md` when done (record the live run result or the
discovery+command, and the members add/remove approach chosen).
</output>
