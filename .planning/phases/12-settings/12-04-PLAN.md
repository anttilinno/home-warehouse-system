---
phase: 12-settings
plan: 04
type: execute
wave: 2
depends_on: ["12-02"]
files_modified:
  - frontend2/src/features/settings/LanguagePage.tsx
  - frontend2/src/features/settings/LanguagePage.test.tsx
  - frontend2/src/features/settings/RegionalFormatsPage.tsx
  - frontend2/src/features/settings/RegionalFormatsPage.test.tsx
  - frontend2/src/features/settings/NotificationsPage.tsx
  - frontend2/src/features/settings/NotificationsPage.test.tsx
autonomous: true
requirements: [SETT-05, SETT-06, SETT-07]
must_haves:
  truths:
    - "Changing language PATCHes {language} then calls loadCatalog(locale) exactly once"
    - "Regional formats save sends only changed fields; thousand==decimal is rejected inline"
    - "Notifications save sends the full notification_preferences map (RetroCheckbox, no toggle atom)"
    - "All three subpages read current values from ['me'] and invalidate ['me'] on save"
  artifacts:
    - path: frontend2/src/features/settings/LanguagePage.tsx
      provides: "language select → PATCH preferences + loadCatalog"
      contains: "loadCatalog"
    - path: frontend2/src/features/settings/RegionalFormatsPage.tsx
      provides: "date/time/thousand/decimal selects + live preview + separator-conflict guard"
      contains: "thousand_separator"
    - path: frontend2/src/features/settings/NotificationsPage.tsx
      provides: "RetroCheckbox rows persisting notification_preferences map"
      contains: "notification_preferences"
  key_links:
    - from: frontend2/src/features/settings/LanguagePage.tsx
      to: "PATCH /users/me/preferences {language} → loadCatalog"
      via: "mutation onSuccess awaits loadCatalog(locale) once (event handler, not render)"
      pattern: "loadCatalog"
    - from: frontend2/src/features/settings/RegionalFormatsPage.tsx
      to: "PATCH /users/me/preferences (partial)"
      via: "settingsApi.updatePreferences(dirtyFields)"
      pattern: "updatePreferences"
---

<objective>
The three preferences subpages, all writing the single `PATCH /users/me/preferences` (partial,
omitempty) and reading current values from `["me"]`. Replaces placeholder files pre-registered by
Plan 12-02; creates NO routes; does NOT touch SettingsLayout/routes.

- **LanguagePage (SETT-05)**: a RetroSelect (English/Eesti/Русский → en/et/ru endonyms). On change:
  PATCH {language} FIRST, THEN `loadCatalog(locale)` exactly once in the mutation onSuccess (event
  handler — NEVER in render/effect, Pitfall 4 render-loop guard). Toast "Language updated."; invalidate
  ["me"]. et/ru catalogs are stubbed until Phase 15 — untranslated msgids fall back to source (parity).
- **RegionalFormatsPage (SETT-06)**: four RetroSelects (date/time/thousand/decimal) + a live preview
  strip recomputed locally on change (NO format read-hooks — Phase 15 owns consumption; resolved OQ3).
  Partial PATCH (only dirty fields). zod refinement: thousand_separator == decimal_separator → inline
  danger field error "Thousand and decimal separators must be different." (mirrors backend 400
  entity.go:252). Save → toast + invalidate ["me"].
- **NotificationsPage (SETT-07)**: RetroCheckbox rows (NO RetroToggle — it does not exist) for the
  notification_preferences map. Default ON when a key is absent (opt-out, matches job-side default-true).
  Save sends the FULL map (the map replaces wholesale — Pitfall 2). Toast "Notification settings saved.";
  invalidate ["me"].

Purpose: parity preferences write surface (read/format consumption is Phase 15).
Output: three component files + tests, all green.

Wave 2, parallel with 12-03 and 12-05 (disjoint files). Depends on 12-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-settings/12-RESEARCH.md
@.planning/phases/12-settings/12-UI-SPEC.md
@frontend2/src/features/settings/SecurityPage.tsx
@frontend2/src/lib/i18n.ts

<interfaces>
<!-- Bind to Plan 12-02 outputs (12-02-SUMMARY). Verified: -->
settingsApi.updatePreferences(b: Partial<Preferences>) → Promise<User>   // PATCH /users/me/preferences
settingsApi.getMe() → Promise<User>
Preferences = { date_format; time_format; thousand_separator; decimal_separator; language; theme: string;
  notification_preferences: Record<string, boolean> }
i18n.ts: locales=["en","et","ru"], type Locale, loadCatalog(locale: Locale): Promise<void> (load+activate).
Mutation idiom: SecurityPage.tsx:71-77; RHF+zod idiom: SecurityPage.tsx:203-249.
RetroSelect + RetroCheckbox from @/components/retro (form subdir, re-exported from top barrel —
  verify exact import names against src/components/retro/index.ts). NO RetroToggle.
Test provider stack + freshClient: see AccountsPage.test.tsx.

UI-SPEC option sets (UX choice — backend stores free strings; [ASSUMED] A1/A2, confirm if Phase 15 differs):
  date_format: YYYY-MM-DD, DD/MM/YYYY, MM/DD/YYYY, DD.MM.YYYY
  time_format: 24h (HH:mm), 12h (h:mm AM/PM)   — store the token the option carries
  thousand_separator: Space ( ), Comma (,), Period (.), None
  decimal_separator: Period (.), Comma (,)
  notification keys (5): loan_alerts, expiry_alerts, maintenance_alerts, low_stock, workspace_activity
Copy: Language helper "Changes apply immediately."/toast "Language updated."; Formats CTA "Save changes",
  error "Thousand and decimal separators must be different."; Notifications intro "Email & in-app alerts
  for:"/toast "Notification settings saved."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LanguagePage (PATCH language then loadCatalog once)</name>
  <files>frontend2/src/features/settings/LanguagePage.tsx, frontend2/src/features/settings/LanguagePage.test.tsx</files>
  <behavior>
    - Select pre-fills from ["me"].language; changing it triggers PATCH {language} then loadCatalog.
    - loadCatalog is called exactly once per change (in onSuccess, not render) — no render loop.
    - Success → toast + invalidate ["me"].
  </behavior>
  <action>
    Build `LanguagePage`: one blue `Window` "LANGUAGE". A RetroSelect of {English→en, Eesti→et,
    Русский→ru}, value from ["me"].language (default "en"). On change, run a mutation
    settingsApi.updatePreferences({ language }); in onSuccess (an event handler — Pitfall 4) `await
    loadCatalog(language as Locale)` exactly once, then invalidate ["me"], then toast "Language updated."
    loadCatalog failure is non-fatal (catch + log, mirror main.tsx). Helper line "Changes apply
    immediately." Write the test: spy/mock loadCatalog (vi.mock the i18n module or inject), select "Eesti",
    assert the PATCH body is { language: "et" } AND loadCatalog was called once with "et" AFTER the PATCH
    resolved (ordering), and ["me"] invalidated.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test -- src/features/settings/LanguagePage.test.tsx</automated>
  </verify>
  <done>language PATCH then loadCatalog once; toast+invalidate; no render loop; test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RegionalFormatsPage (4 selects, live preview, separator-conflict guard)</name>
  <files>frontend2/src/features/settings/RegionalFormatsPage.tsx, frontend2/src/features/settings/RegionalFormatsPage.test.tsx</files>
  <behavior>
    - Selects pre-fill from ["me"]; changing only the date format PATCHes only { date_format }.
    - Choosing identical thousand/decimal separators blocks save with an inline field error.
    - The preview strip recomputes on each change BEFORE save (reflects pending, not saved, state).
    - Success → toast + invalidate ["me"].
  </behavior>
  <action>
    Build `RegionalFormatsPage`: one blue `Window` "REGIONAL FORMATS". RHF+zod with four RetroSelects
    (date_format, time_format, thousand_separator, decimal_separator) using the option sets in the
    interfaces block — each option's VALUE is the token persisted to the backend. zod `.refine` (or
    superRefine) rejecting thousand_separator === decimal_separator with message "Thousand and decimal
    separators must be different." attached to the thousand_separator field (inline danger treatment on
    both selects when conflicting). A live preview strip (`bg-bg-panel-2`, mono 12px tabular-nums)
    computed LOCALLY from the current RHF values (watch()) showing a sample date/time/number — recompute
    on change, do NOT call the backend for the preview, do NOT import any format read-hook (none exist;
    Phase 15). Submit builds the PATCH body from dirtyFields ONLY (partial). settingsApi.updatePreferences
    → toast "Saved."/"Changes saved." + invalidate ["me"]. ONE primary submit "Save changes". Write the
    test: change only date_format, submit, assert PATCH body has date_format and NOT the others
    (request-capture via server.use); set thousand=decimal, assert the inline error renders and the
    submit does not PATCH; assert the preview text changes when a select changes.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test -- src/features/settings/RegionalFormatsPage.test.tsx</automated>
  </verify>
  <done>partial PATCH; separator-conflict inline error blocks save; live preview recomputes; test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: NotificationsPage (RetroCheckbox rows, full-map PATCH)</name>
  <files>frontend2/src/features/settings/NotificationsPage.tsx, frontend2/src/features/settings/NotificationsPage.test.tsx</files>
  <behavior>
    - Five RetroCheckbox rows; absent key defaults to ON (opt-out).
    - Toggling one and saving sends the FULL notification_preferences map (all five keys).
    - Success → toast + invalidate ["me"]; uses RetroCheckbox, never a toggle atom.
  </behavior>
  <action>
    Build `NotificationsPage`: one blue `Window` "NOTIFICATIONS", intro "Email & in-app alerts for:".
    Five `RetroCheckbox` rows keyed loan_alerts, expiry_alerts, maintenance_alerts, low_stock,
    workspace_activity with bold category labels + muted parentheticals (copy per UI-SPEC §7). Initial
    checked state = ["me"].notification_preferences[key] ?? true (opt-out default). Save builds the FULL
    map of all five keys (not just changed — the map replaces wholesale, Pitfall 2) and calls
    settingsApi.updatePreferences({ notification_preferences: fullMap }) → toast "Notification settings
    saved." + invalidate ["me"]. ONE primary submit "Save changes". Do NOT import RetroToggle (it does
    not exist). Write the test: render with an ME map missing keys (assert all checked by default),
    uncheck "Low stock", submit, assert the PATCH body's notification_preferences contains ALL five keys
    with low_stock=false and the rest true (request-capture).
  </action>
  <verify>
    <automated>cd frontend2 && bun run test -- src/features/settings/NotificationsPage.test.tsx</automated>
  </verify>
  <done>opt-out defaults; full-map PATCH on save; RetroCheckbox only; toast+invalidate; test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser→/api | preferences PATCH crosses the cookie-JWT boundary; values are non-privileged user prefs |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | Tampering | separator conflict producing unparseable formats | mitigate | zod refine client-side + backend 400 (entity.go:252) is authoritative |
| T-12-09 | DoS (self) | language render-loop from activation in render | mitigate | loadCatalog only in mutation onSuccess (event), never render/effect (Pitfall 4) |
| T-12-SC | Tampering | npm installs | mitigate | zero new packages; lint:imports gate (no idb/serwist/sync*) |
</threat_model>

<verification>
- `cd frontend2 && bun run test -- src/features/settings/LanguagePage.test.tsx src/features/settings/RegionalFormatsPage.test.tsx src/features/settings/NotificationsPage.test.tsx` green.
- `cd frontend2 && bun run tsc --noEmit && bun run lint:imports` clean.
</verification>

<success_criteria>
- Language: PATCH then loadCatalog once. Regional: partial PATCH + separator guard + live preview.
  Notifications: full-map PATCH via RetroCheckbox. All read/invalidate ["me"].
- No edits to SettingsLayout.tsx or routes/index.tsx.
</success_criteria>

<output>
Create `.planning/phases/12-settings/12-04-SUMMARY.md` when done (record the exact option token
values chosen for date/time/separators so Phase 15 read-hooks bind to the same strings — A1/A2).
</output>
