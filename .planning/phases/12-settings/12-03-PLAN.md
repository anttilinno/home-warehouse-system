---
phase: 12-settings
plan: 03
type: execute
wave: 2
depends_on: ["12-02"]
files_modified:
  - frontend2/src/features/settings/ProfilePage.tsx
  - frontend2/src/features/settings/ProfilePage.test.tsx
  - frontend2/src/features/settings/AvatarUploader.tsx
  - frontend2/src/features/settings/AvatarUploader.test.tsx
  - frontend2/src/features/settings/AppearancePage.tsx
  - frontend2/src/features/settings/AppearancePage.test.tsx
autonomous: true
requirements: [SETT-02, SETT-04, SETT-11]
must_haves:
  truths:
    - "User edits name + email and saves; only changed fields PATCH /users/me"
    - "User uploads an avatar (multipart field 'avatar'); preview updates without a stale-cache image"
    - "User removes the avatar via a pink confirm → DELETE /users/me/avatar"
    - "Appearance shows a single Light theme marked CURRENT plus a light-only backlog note; no dark toggle"
  artifacts:
    - path: frontend2/src/features/settings/ProfilePage.tsx
      provides: "RHF+zod name/email form + avatar block"
      min_lines: 40
    - path: frontend2/src/features/settings/AvatarUploader.tsx
      provides: "dedicated single-file multipart avatar uploader (NOT PhotoUpload)"
      contains: "avatar"
    - path: frontend2/src/features/settings/AppearancePage.tsx
      provides: "light-only theme card + butter backlog note"
      contains: "backlog"
  key_links:
    - from: frontend2/src/features/settings/ProfilePage.tsx
      to: settingsApi.updateMe / uploadAvatar / deleteAvatar
      via: "useMutation → invalidateQueries(['me'])"
      pattern: "invalidateQueries"
    - from: frontend2/src/features/settings/AvatarUploader.tsx
      to: "POST /users/me/avatar (field 'avatar')"
      via: "settingsApi.uploadAvatar(file)"
      pattern: "uploadAvatar"
---

<objective>
Profile (SETT-02) + Appearance (SETT-04/SETT-11) subpages. Replaces the placeholder component files
pre-registered by Plan 12-02; this plan creates NO routes and does NOT touch SettingsLayout/routes.

- **ProfilePage**: one blue `Window` "PROFILE". RHF+zod name/email form (mirror SecurityPage), partial
  PATCH /users/me sending only dirty fields (CONTEXT constraint 3); email conflict (409/400) → inline
  danger band (SecurityPage `wrongCurrent` pattern). Avatar block on top.
- **AvatarUploader**: a DEDICATED single-file multipart uploader (NOT PhotoUpload — that carries item
  queue/dup-check/gallery dead weight). 150×150 preview with a 1px ink frame; no-avatar state shows a
  placeholder with Silkscreen initials (reuse the AccountsPage ProviderTile initials idiom). Upload via
  settingsApi.uploadAvatar (field `avatar`); Remove via pink RetroConfirmDialog → deleteAvatar. Cache-
  bust the `<img>` after success (`?v=${me.dataUpdatedAt}`); invalidate `["me"]` on any avatar mutation.
- **AppearancePage**: one blue `Window` "APPEARANCE". A single selected "Light" theme card (titlebar-blue
  fill + ink border + ◉/✓ glyph + RetroBadge "CURRENT" — three non-color cues) and a butter
  `role="note"` band "Light only — a dark theme is on the backlog." NO dark option, NO multi-theme
  switcher, NO write (presentational only — resolved OQ-R3). SETT-11 wins over SETT-04's stale prose.

Purpose: parity profile management + the explicit light-only appearance note.
Output: three component files + tests, all green.

Wave 2, parallel with 12-04 and 12-05 (disjoint files). Depends on 12-02 (settingsApi, types, MSW).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-settings/12-RESEARCH.md
@.planning/phases/12-settings/12-UI-SPEC.md
@frontend2/src/features/settings/SecurityPage.tsx
@frontend2/src/features/settings/AccountsPage.tsx
@frontend2/src/components/retro/form/RetroFileInput.tsx

<interfaces>
<!-- Bind to Plan 12-02's outputs (see 12-02-SUMMARY). Verified signatures: -->
settingsApi.updateMe({ full_name?, email? }) → Promise<User>
settingsApi.uploadAvatar(file: File) → Promise<User>   // appends FormData field "avatar"
settingsApi.deleteAvatar() → Promise<void>
User (extended): { id, email, full_name, avatar_url: string|null, has_password, ...prefs }
RHF+zod idiom: SecurityPage.tsx:203-249 (zodResolver, handleSubmit, isSubmitting, inline error band).
Mutation idiom: SecurityPage.tsx:71-77 (mutationFn → onSuccess toast + invalidateQueries(["me"]),
  onError branch on HttpError.status for 400/409 inline band).
RetroFileInput props: accept, maxSize, multiple={false} (file primitive at form/RetroFileInput.tsx).
ProviderTile initials idiom: AccountsPage.tsx:44-53 (Silkscreen, border-border-ink, place-items-center).
Test provider stack + freshClient: AccountsPage.test.tsx (I18nProvider+MemoryRouter+QueryClientProvider+
  ModalStackProvider+RetroToaster; server.use(...) overrides).
Copy (UI-SPEC copywriting contract): primary CTA "Save changes"; avatar helper "PNG/JPG · max 5 MB";
  avatar actions "Change photo…" / "Remove"; remove-avatar confirm "Remove photo? Your profile will
  show your initials instead." (confirm label "Remove", pink); email error "That email is already in
  use." (409) / "Couldn't save. Try again."; Appearance "Light" + badge "CURRENT"; backlog note
  "Light only — a dark theme is on the backlog." (butter role="note").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AvatarUploader (dedicated multipart, 150×150, remove confirm)</name>
  <files>frontend2/src/features/settings/AvatarUploader.tsx, frontend2/src/features/settings/AvatarUploader.test.tsx</files>
  <behavior>
    - Selecting a file calls settingsApi.uploadAvatar with that file; on success invalidates ["me"]
      and the preview reflects the new avatar_url (cache-busted).
    - With no avatar_url, renders a 150×150 initials placeholder (no broken <img>).
    - Remove opens a pink confirm; confirming calls deleteAvatar and invalidates ["me"].
    - accept = image/jpeg,image/png,image/webp; multiple=false; maxSize mirrors the 2MB server cap.
  </behavior>
  <action>
    Build `AvatarUploader` (props: the current `avatar_url: string|null`, the user's `full_name` for
    initials, and the `dataUpdatedAt` from the ["me"] query for cache-busting — OR read ["me"] itself
    via useQuery; pick the cleaner shape and document it in the summary). Render a 150×150 box:
    `border border-border-ink object-cover` `<img>` when avatar_url present (src =
    `${avatar_url}?v=${dataUpdatedAt}` to defeat the stable-URL cache, Pitfall 7), else a
    `bg-bg-panel-2 border border-border-ink` placeholder with centered Silkscreen initials (ProviderTile
    idiom). A `RetroFileInput` (accept="image/jpeg,image/png,image/webp", maxSize=2*1024*1024,
    multiple={false}) labelled "Change photo…" + helper "PNG/JPG · max 5 MB". Upload mutation:
    settingsApi.uploadAvatar(file) → onSuccess invalidate ["me"] + toast; onError danger toast. Remove
    button → pink RetroConfirmDialog ("Remove photo? Your profile will show your initials instead.",
    confirm "Remove") → deleteAvatar mutation → invalidate ["me"]. Do NOT import PhotoUpload. Write the
    test: mock settingsApi (or use MSW handlers from 12-02) to assert the FormData carries field "avatar"
    on upload (spy on uploadAvatar), the initials render with no avatar, and the remove confirm fires
    deleteAvatar.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test -- src/features/settings/AvatarUploader.test.tsx</automated>
  </verify>
  <done>Upload sends field "avatar"; initials placeholder when empty; remove confirm → delete; ["me"] invalidated; test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ProfilePage (name/email RHF+zod, partial PATCH, avatar block)</name>
  <files>frontend2/src/features/settings/ProfilePage.tsx, frontend2/src/features/settings/ProfilePage.test.tsx</files>
  <behavior>
    - Form pre-fills from ["me"]; submitting only a changed name PATCHes { full_name } (NOT email).
    - zod: full_name min 1, email valid email.
    - 409/400 on email conflict → inline danger band "That email is already in use." (no toast-only).
    - Successful save → toast + invalidate ["me"].
    - Renders the AvatarUploader at the top.
  </behavior>
  <action>
    Build `ProfilePage`: read ["me"] (settingsApi.getMe), one blue `Window` "PROFILE" centered
    `max-w-[560px]` inside the 720 column. Top: `<AvatarUploader .../>`. RHF+zod form (mirror
    SecurityPage password form structure): fields Full name (RetroInput) + Email (RetroInput
    type="email", mono). Build the PATCH body from dirtyFields ONLY (RHF formState.dirtyFields) so an
    untouched email is never sent (CONTEXT constraint 3 / Pitfall 2). Mutation settingsApi.updateMe →
    onSuccess toast "Saved." + invalidate ["me"]; onError: if HttpError 409 or 400 → set an inline
    danger band "That email is already in use." (SecurityPage band pattern), else toast "Couldn't save.
    Try again." ONE primary submit "Save changes", right-aligned. Write the test: MSW ME has a name;
    change only the name field, submit, assert the PATCH /users/me request body contains full_name and
    NOT email (intercept via server.use capturing the request JSON); assert a 409 handler surfaces the
    inline band.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test -- src/features/settings/ProfilePage.test.tsx</automated>
  </verify>
  <done>Partial PATCH (only dirty fields); 409 inline band; toast+invalidate on success; avatar block present; test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AppearancePage (light-only, no dark, presentational)</name>
  <files>frontend2/src/features/settings/AppearancePage.tsx, frontend2/src/features/settings/AppearancePage.test.tsx</files>
  <behavior>
    - Renders exactly one theme card "Light" marked CURRENT (selected treatment + badge + glyph).
    - Renders the butter backlog note "Light only — a dark theme is on the backlog." with role="note".
    - No second selectable theme; no dark toggle; no PATCH fires (presentational).
  </behavior>
  <action>
    Build `AppearancePage`: one blue `Window` "APPEARANCE". A single theme card with the locked selected
    treatment (`bg-titlebar-blue` fill, `border-2 border-border-ink`, bevel-pressed), a `◉`/`✓` glyph,
    label "Light", AND a `RetroBadge variant="info"` "CURRENT" (three non-color cues), plus subtext
    "Retro OS Pastel — the only theme." Below: a `role="note"` butter band (PasswordCard note pattern)
    with copy "Light only — a dark theme is on the backlog." Render NO dark option and NO toggle; fire
    NO mutation (OQ-R3 presentational). Write the test: assert exactly one theme card, the "CURRENT"
    badge present, the backlog note present, and assert NO element matching a dark-theme option/toggle
    exists (e.g. queryByText(/dark/i) on a control returns null aside from the backlog note text).
  </action>
  <verify>
    <automated>cd frontend2 && bun run test -- src/features/settings/AppearancePage.test.tsx</automated>
  </verify>
  <done>Single Light/CURRENT card + butter backlog note; no dark control; no PATCH; test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser→/api | name/email PATCH and avatar multipart cross the cookie-JWT boundary |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Tampering/DoS | avatar upload size/type | mitigate | client mirrors server cap: accept allowlist + maxSize=2MB on RetroFileInput; server is authoritative (handler.go:659-665) |
| T-12-07 | Spoofing | email change to a taken address | mitigate | server 409/400 on conflict; client surfaces inline band; zod email format pre-check |
| T-12-SC | Tampering | npm installs | mitigate | zero new packages; lint:imports gate (no idb/serwist/sync*) |
</threat_model>

<verification>
- `cd frontend2 && bun run test -- src/features/settings/ProfilePage.test.tsx src/features/settings/AvatarUploader.test.tsx src/features/settings/AppearancePage.test.tsx` green.
- `cd frontend2 && bun run tsc --noEmit && bun run lint:imports` clean.
</verification>

<success_criteria>
- Name/email edit with partial PATCH; avatar upload (field "avatar") + remove; ["me"] invalidated.
- Appearance shows light-only with explicit backlog note; no dark theme anywhere.
- No edits to SettingsLayout.tsx or routes/index.tsx.
</success_criteria>

<output>
Create `.planning/phases/12-settings/12-03-SUMMARY.md` when done.
</output>
