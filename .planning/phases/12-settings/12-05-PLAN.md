---
phase: 12-settings
plan: 05
type: execute
wave: 2
depends_on: ["12-02"]
files_modified:
  - frontend2/src/features/settings/DataStoragePage.tsx
  - frontend2/src/features/settings/DataStoragePage.test.tsx
autonomous: true
requirements: [SETT-09]
must_haves:
  truths:
    - "Clear cached data calls queryClient.clear() behind a butter confirm (no backend call)"
    - "Export downloads the real GET /export/workspace blob; the button is gated on admin/owner role"
    - "Import is a pointer to the Phase-14 imports surface (no inline restore here)"
    - "No offline-storage surface; no idb/serwist/sync* imports"
  artifacts:
    - path: frontend2/src/features/settings/DataStoragePage.tsx
      provides: "clear-cache + export-download + import-pointer"
      contains: "queryClient.clear"
  key_links:
    - from: frontend2/src/features/settings/DataStoragePage.tsx
      to: "GET /workspaces/{wsId}/export/workspace"
      via: "settingsApi.exportWorkspace(wsId, format) → downloadBlob"
      pattern: "exportWorkspace"
    - from: frontend2/src/features/settings/DataStoragePage.tsx
      to: "useWorkspace().currentWorkspaceId + Workspace.role"
      via: "wsId for export; role gate for the export button"
      pattern: "currentWorkspaceId"
---

<objective>
Data & Storage subpage (SETT-09). Replaces the placeholder file pre-registered by Plan 12-02; creates
NO routes; does NOT touch SettingsLayout/routes. Online-only — NO offline-storage surface (CI grep
guard forbids idb/serwist/sync*).

Three grouped actions in one blue `Window` "DATA & STORAGE":
1. **Clear cached data** — `queryClient.clear()` ONLY (client-only, CONTEXT constraint 5) behind a
   butter `RetroConfirmDialog`. Toast "Cached data cleared." NO backend call.
2. **Export** — "Download workspace backup" → `settingsApi.exportWorkspace(wsId, "xlsx")`
   (real `GET /workspaces/{wsId}/export/workspace?format=xlsx` via downloadBlob — verified endpoint,
   OQ5). wsId from `useWorkspace().currentWorkspaceId`. The endpoint is admin-gated server-side
   (requireAdminRole 403, handler.go:227) — GATE the button on the workspace role
   (admin/owner) and still handle a 403 defensively (Pitfall 8).
3. **Import** — a pointer row "Go to imports →" linking to the Phase-14 imports surface (resolved
   A3: pointer, not inline restore). If the imports route does not exist on this branch, render as a
   disabled butter "COMING SOON" badge instead of a dead link.

Purpose: parity data-management surface, bounded to clear-cache + real export + import pointer.
Output: one component file + test, green.

Wave 2, parallel with 12-03 and 12-04 (disjoint files). Depends on 12-02 (settingsApi.exportWorkspace).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-settings/12-RESEARCH.md
@.planning/phases/12-settings/12-UI-SPEC.md
@frontend2/src/features/settings/AccountsPage.tsx
@frontend2/src/features/workspace/useWorkspace.ts

<interfaces>
<!-- Bind to Plan 12-02 outputs (12-02-SUMMARY). Verified: -->
settingsApi.exportWorkspace(wsId: string, format?: "xlsx"|"json") → Promise<void>  // downloadBlob
useWorkspace(): { currentWorkspaceId: string|null, ... }  (src/features/workspace/useWorkspace.ts)
Workspace.role: string (lib/types.ts:35) — gate export on "owner"|"admin". The current workspace's
  role is available via the WorkspaceProvider (read currentWorkspaceId, find the matching workspace's
  role — verify the provider exposes the workspaces array / current role; confirm in 12-02-SUMMARY or
  WorkspaceProvider.tsx and document the chosen accessor).
queryClient: useQueryClient() → .clear().
RetroConfirmDialog (butter context for non-destructive confirm), BevelButton, RetroBadge, Window,
  retroToast from @/components/retro. Link from react-router for the imports pointer.
Test provider stack: see AccountsPage.test.tsx; the WorkspaceProvider must wrap the page in the test
  (or mock useWorkspace) to supply currentWorkspaceId + role.
Copy (UI-SPEC): clear CTA "Clear cached data" → confirm "Clear cached data? Your inventory data stays
  safe — this only clears locally cached lists." → toast "Cached data cleared."; export button
  "Export…"/"Download workspace backup"; import pointer "Go to imports →".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DataStoragePage (clear-cache + export + import pointer)</name>
  <files>frontend2/src/features/settings/DataStoragePage.tsx, frontend2/src/features/settings/DataStoragePage.test.tsx</files>
  <behavior>
    - Clear button opens a butter confirm; confirming calls queryClient.clear() (no fetch) + toast.
    - Export button (visible only to admin/owner) calls settingsApi.exportWorkspace(currentWorkspaceId, "xlsx").
    - For a non-admin role, the export button is hidden/disabled with an explanatory note.
    - Import renders a pointer to the imports route (or a disabled COMING SOON badge if absent).
    - No idb/serwist/sync* imports anywhere in the file.
  </behavior>
  <action>
    Build `DataStoragePage`: one blue `Window` "DATA & STORAGE" with three sections separated by
    `border-b border-table-rule`. (1) CACHED DATA: copy + a "Clear cached data" BevelButton →
    butter RetroConfirmDialog (copy per interfaces) → onConfirm `queryClient.clear()` (useQueryClient)
    + toast "Cached data cleared." (NO network). (2) EXPORT: read useWorkspace() for currentWorkspaceId
    and the current workspace role; show the "Download workspace backup"/"Export…" BevelButton ONLY when
    role is owner/admin (else render a muted note that export needs admin rights) — on click call
    settingsApi.exportWorkspace(wsId, "xlsx") inside a mutation; onError (incl. 403) → danger toast
    "Couldn't export. Try again." (defense in depth, Pitfall 8); guard against a null wsId. (3) IMPORT:
    a react-router Link "Go to imports →" to the Phase-14 imports path IF that route exists on this
    branch; otherwise a non-link row with a disabled butter RetroBadge "COMING SOON". Determine which at
    implementation time by checking routes/index.tsx for an imports route; document the choice in the
    summary. Do NOT build an inline restore flow (A3). Write the test: mock useWorkspace to supply an
    admin role + wsId; assert the clear confirm calls queryClient.clear (spy) and fires NO fetch; assert
    the export button calls exportWorkspace with the wsId; re-render with a viewer role and assert the
    export action is hidden/disabled. Assert (grep-style in the test or a source check) no offline import.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test -- src/features/settings/DataStoragePage.test.tsx && grep -nE "idb|serwist|sync[A-Z]" frontend2/src/features/settings/DataStoragePage.tsx; test $? -ne 0</automated>
  </verify>
  <done>clear-cache (client-only) + admin-gated real export + import pointer; no offline imports; test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser→/api | workspace export blob crosses the cookie-JWT boundary; admin-gated server-side |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 | Information Disclosure | non-admin triggering a full workspace export | mitigate | server requireAdminRole 403 is authoritative; client gates the button on workspace role + handles 403 |
| T-12-11 | Tampering | offline-storage drift | mitigate | online-only: clear-cache = queryClient.clear() only; verify gate forbids idb/serwist/sync* |
| T-12-SC | Tampering | npm installs | mitigate | zero new packages; lint:imports gate |
</threat_model>

<verification>
- `cd frontend2 && bun run test -- src/features/settings/DataStoragePage.test.tsx` green.
- `grep -nE "idb|serwist|sync[A-Z]" frontend2/src/features/settings/DataStoragePage.tsx` returns nothing.
- `cd frontend2 && bun run tsc --noEmit && bun run lint:imports` clean.
</verification>

<success_criteria>
- Clear cache is client-only; export hits the real /export/workspace and is admin-gated; import is a pointer.
- No offline-storage surface or imports.
- No edits to SettingsLayout.tsx or routes/index.tsx.
</success_criteria>

<output>
Create `.planning/phases/12-settings/12-05-SUMMARY.md` when done (record whether the imports route
exists on this branch and the import-pointer treatment chosen).
</output>
