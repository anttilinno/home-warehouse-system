---
phase: 12-settings
plan: 06
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - frontend2/src/features/settings/MembersPage.tsx
  - frontend2/src/features/settings/MembersPage.test.tsx
autonomous: true
requirements: [SETT-10]
must_haves:
  truths:
    - "Members list shows each member's name, email, and role (enriched by the 12-01 backend)"
    - "An admin changes a member's role via a select → PATCH /members/{user_id}"
    - "An admin removes a member via a pink confirm → DELETE /members/{user_id}"
    - "An admin adds a member by email → POST /members {email, role}; unregistered email → 404 surfaced"
    - "The current user's own row is tagged YOU; own-role-change and self-remove are disabled (server-authoritative)"
  artifacts:
    - path: frontend2/src/features/settings/MembersPage.tsx
      provides: "members table + role select + remove confirm + add-by-email"
      min_lines: 60
  key_links:
    - from: frontend2/src/features/settings/MembersPage.tsx
      to: "GET/POST/PATCH/DELETE /workspaces/{wsId}/members"
      via: "settingsApi.listMembers/addMemberByEmail/updateMemberRole/removeMember"
      pattern: "addMemberByEmail"
    - from: frontend2/src/features/settings/MembersPage.tsx
      to: "['members', wsId] query + ['me'] for own-row detection"
      via: "useQuery keyed on wsId; compare member.user_id to me.id"
      pattern: "\\[\"members\""
---

<objective>
Members subpage (SETT-10). Replaces the placeholder file pre-registered by Plan 12-02; creates NO
routes; does NOT touch SettingsLayout/routes. Consumes the parity-true backend from Plan 12-01
(enriched MemberResponse with email/full_name + email-accepting POST /members).

One blue `Window` "MEMBERS":
- **List**: `["members", wsId]` → a RetroTable (mirror SecurityPage SessionsCard) with NAME / EMAIL /
  ROLE / actions columns. NAME + EMAIL come from the 12-01 enrichment; EMAIL in mono. Empty →
  RetroEmptyState ("No members yet").
- **Role change**: per-row RetroSelect (owner/admin/member/viewer — verified enum) → PATCH
  /members/{user_id}. Disabled on the current user's own row (mirrors ErrCannotChangeOwnRole 400).
- **Remove**: per-row Remove → pink RetroConfirmDialog ("Remove {name}? They'll lose access
  immediately.") → DELETE /members/{user_id}. Hidden on the own row; the server last-owner guard (400)
  is surfaced as a danger toast if the client guard misses (defense in depth, AccountsPage 409 idiom).
- **Add by email**: a footer-strip form (email input + role select + "Add") → POST /members
  {email, role}. A 404 ("no registered user with that email") → inline danger band / toast "No
  registered user with that email." Success → toast + invalidate ["members", wsId].
- **Own row**: tagged with a RetroBadge "YOU"; role select + remove disabled/hidden for self.

The server is authoritative for all guards; the client mirrors them for UX only and surfaces the
400/404 as a toast/band when it misses.

Purpose: parity multi-user member management.
Output: one component file + test, green.

Wave 3 — runs after BOTH the backend members plan (12-01, for enriched fields + email-add) and the
foundation (12-02, for settingsApi.* + the Member type + the pre-registered route). Parallel with the
E2E plan (12-07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-settings/12-RESEARCH.md
@.planning/phases/12-settings/12-UI-SPEC.md
@.planning/phases/12-settings/12-01-SUMMARY.md
@.planning/phases/12-settings/12-02-SUMMARY.md
@frontend2/src/features/settings/SecurityPage.tsx
@frontend2/src/features/settings/AccountsPage.tsx

<interfaces>
<!-- Bind to 12-01-SUMMARY (final backend shapes) + 12-02-SUMMARY (settingsApi + Member type). -->
settingsApi.listMembers(wsId) → Promise<{ items: Member[] }>
settingsApi.addMemberByEmail(wsId, { email, role }) → Promise<Member>   // POST /members {email, role}
settingsApi.updateMemberRole(wsId, userId, role) → Promise<Member>      // PATCH /members/{user_id}
settingsApi.removeMember(wsId, userId) → Promise<void>                  // DELETE /members/{user_id}
Member = { id; workspace_id; user_id; role; email?; full_name?; invited_by?; created_at; updated_at }
  (email/full_name populated by 12-01 list enrichment — confirm names against 12-01-SUMMARY)
useWorkspace(): { currentWorkspaceId: string|null, ... }
settingsApi.getMe() → User (use me.id to detect the own row)
Role enum (verified backend): "owner" | "admin" | "member" | "viewer".
Server guards (authoritative — mirror client-side for UX, surface on miss):
  own-role-change → 400; last-owner removal → 400; add unregistered email → 404; already-member → 400.
HttpError(status) from @/lib/api to branch on 400/404.
RetroTable, RetroSelect, RetroBadge, RetroConfirmDialog, RetroEmptyState, RetroInput, BevelButton,
  retroToast, Window from @/components/retro.
Test provider stack: see AccountsPage.test.tsx; wrap with WorkspaceProvider (or mock useWorkspace)
  for currentWorkspaceId. Use the 12-02 MSW members handlers and server.use(...) overrides for the
  404/400 cases.
Copy (UI-SPEC §10): add CTA "Add", placeholder "user@email…"; empty "No members yet" / "Invite someone
  to share this workspace."; remove confirm title "Remove {name}", body "Remove {name} from this
  workspace? They'll lose access immediately.", confirm "Remove" (pink); own badge "YOU".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MembersPage list + role-change + remove (server-authoritative guards)</name>
  <files>frontend2/src/features/settings/MembersPage.tsx, frontend2/src/features/settings/MembersPage.test.tsx</files>
  <behavior>
    - List renders name/email/role per member from ["members", wsId]; empty → RetroEmptyState.
    - Role select changes PATCH /members/{user_id}; own row's select is disabled.
    - Remove fires a pink confirm → DELETE /members/{user_id}; own row has no remove.
    - A 400 on remove (last owner) surfaces a danger toast (client guard miss → server authoritative).
    - All mutations invalidate ["members", wsId].
  </behavior>
  <action>
    Build the read+mutate core of `MembersPage`: one blue `Window` "MEMBERS". useWorkspace() for wsId
    (guard null), useQuery(["members", wsId], () => settingsApi.listMembers(wsId)), and useQuery(["me"]).
    Render a RetroTable: NAME (full_name, fallback to a truncated user_id if absent), EMAIL (mono),
    ROLE (a per-row RetroSelect of owner/admin/member/viewer), and an actions cell. Tag the row whose
    user_id === me.id with a RetroBadge "YOU"; disable that row's role select and hide its Remove.
    Role-change mutation: settingsApi.updateMemberRole(wsId, userId, role) → onSuccess invalidate
    ["members", wsId] + toast; onError if HttpError 400 → danger toast (e.g. "Couldn't change role.")
    else generic. Remove flow: a pink RetroConfirmDialog (copy per interfaces) → settingsApi.removeMember
    → onSuccess invalidate + toast; onError 400 (last owner) → danger toast "Can't remove the last
    owner." Empty list → RetroEmptyState. Write the test (this task): MSW returns two members incl. the
    current user; assert name/email/role render; assert the own row shows "YOU" and has a disabled role
    select + no remove; change the other member's role and assert the PATCH; remove the other member,
    confirm, assert the DELETE; override DELETE with a 400 and assert the danger toast.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test -- src/features/settings/MembersPage.test.tsx -t "list|role|remove"</automated>
  </verify>
  <done>list shows name/email/role; own-row guarded; role-change PATCH + remove DELETE with 400 surfaced; invalidates; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MembersPage add-by-email (POST /members {email, role}, 404 surfaced)</name>
  <files>frontend2/src/features/settings/MembersPage.tsx, frontend2/src/features/settings/MembersPage.test.tsx</files>
  <behavior>
    - Add form (email input + role select + Add) POSTs {email, role}; success adds + invalidates + toast.
    - An unregistered email (404) surfaces "No registered user with that email." (inline band or toast).
    - An already-member email (400) surfaces a clear message; the form does not silently no-op.
  </behavior>
  <action>
    Add the footer add-member strip to `MembersPage` (bg-bg-panel-2, border-t-2): a small RHF+zod form
    (email: zod email; role: RetroSelect default "member") with an "Add" primary submit + placeholder
    "user@email…". On submit, settingsApi.addMemberByEmail(wsId, { email, role }) →
    onSuccess: invalidate ["members", wsId], reset the form, toast (e.g. "Member added.");
    onError: if HttpError 404 → inline danger band / toast "No registered user with that email."; if 400
    (ErrAlreadyMember) → "That user is already a member."; else generic "Couldn't add member. Try again."
    Extend `MembersPage.test.tsx`: type an email + submit, assert POST body { email, role }; override POST
    with a 404 and assert the "No registered user with that email" message renders; override with a 400
    and assert the already-member message.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test -- src/features/settings/MembersPage.test.tsx -t "add"</automated>
  </verify>
  <done>add-by-email POSTs {email, role}; 404 and 400 surfaced with distinct copy; invalidates on success; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→workspace API | role changes / removals / add-by-email cross the cookie-JWT boundary; admin-gated server-side |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-12 | Elevation | self-promoting via own-row role change | mitigate | own-row select disabled (client) + server ErrCannotChangeOwnRole 400 (authoritative) |
| T-12-13 | DoS | removing the last owner (workspace lockout) | mitigate | server last-owner 400 surfaced as danger toast; client mirror best-effort |
| T-12-14 | Information Disclosure | add-by-email existence oracle | accept | 404 reveals registration state to an authenticated workspace admin only (T-12-02) |
| T-12-SC | Tampering | npm installs | mitigate | zero new packages; lint:imports gate |
</threat_model>

<verification>
- `cd frontend2 && bun run test -- src/features/settings/MembersPage.test.tsx` green (list, role, remove, add 404/400).
- `cd frontend2 && bun run tsc --noEmit && bun run lint:imports` clean.
- Binds to the FINAL field names in 12-01-SUMMARY (email/full_name) and 12-02-SUMMARY (settingsApi.addMemberByEmail).
</verification>

<success_criteria>
- Members list shows name/email/role; role-change + remove + add-by-email all wired to the real endpoints.
- Own-row and last-owner guards mirrored client-side and surfaced server-side on miss.
- Unregistered-email add → 404 surfaced.
- No edits to SettingsLayout.tsx or routes/index.tsx.
</success_criteria>

<output>
Create `.planning/phases/12-settings/12-06-SUMMARY.md` when done.
</output>
