---
phase: 11-scan
plan: 02
type: execute
wave: 1
depends_on: ["11-01"]
files_modified:
  - frontend2/src/lib/scanner/feedback.ts
  - frontend2/src/lib/scanner/scan-history.ts
  - frontend2/src/lib/scanner/init-polyfill.ts
  - frontend2/src/lib/scanner/types.ts
  - frontend2/src/lib/scanner/index.ts
  - frontend2/src/lib/scanner/feedback.test.ts
  - frontend2/src/lib/scanner/scan-history.test.ts
  - frontend2/src/lib/api/barcode.ts
  - frontend2/src/lib/api/barcode.test.ts
autonomous: true
requirements: [SCAN-02, SCAN-03, SCAN-06, SCAN-07, SCAN-10]
must_haves:
  truths:
    - "Successful-scan feedback produces an AudioContext oscillator beep + a vibrate fallback, with a reduced-motion-aware visual hook"
    - "Scan history persists last-10 deduped codes to localStorage key hws-scan-history and survives malformed legacy data"
    - "Clearing history empties hws-scan-history"
    - "barcodeApi.lookup(code) GETs the GLOBAL /barcode/{code} with encodeURIComponent and returns the product shape"
    - "The supported-format set is exactly QR/UPC-A/EAN-13/Code128"
  artifacts:
    - path: "frontend2/src/lib/scanner/feedback.ts"
      provides: "playSuccessBeep/playErrorBeep (singleton AudioContext) + vibrate helper"
      exports: ["playSuccessBeep", "playErrorBeep"]
    - path: "frontend2/src/lib/scanner/scan-history.ts"
      provides: "get/add/clear scan history, last-10, dedup, malformed-tolerant"
      contains: "hws-scan-history"
    - path: "frontend2/src/lib/scanner/types.ts"
      provides: "SUPPORTED_FORMATS, ScanHistoryEntry, BarcodeFormat re-export"
      contains: "SUPPORTED_FORMATS"
    - path: "frontend2/src/lib/scanner/init-polyfill.ts"
      provides: "barcode-detector/polyfill side-effect registration"
    - path: "frontend2/src/lib/api/barcode.ts"
      provides: "barcodeApi.lookup(code) → ProductResponse"
      exports: ["barcodeApi", "ProductResponse"]
  key_links:
    - from: "frontend2/src/lib/api/barcode.ts"
      to: "/barcode/{code}"
      via: "get(`/barcode/${encodeURIComponent(code)}`)"
      pattern: "barcode/\\$\\{encodeURIComponent"
    - from: "frontend2/src/lib/scanner/scan-history.ts"
      to: "localStorage"
      via: "hws-scan-history key, slice(0,10)"
      pattern: "hws-scan-history"
---

<objective>
1:1 port of the four legacy `lib/scanner/*` modules into `frontend2/src/lib/scanner/`
plus the new typed `barcodeApi`. Pure logic + browser-API utilities, fully unit-
testable with the Wave-0 mocks. NO UI, NO React — disjoint from every other Wave-1/2/3
plan's files.

Purpose: every scanner behavior (beep, vibrate, history persistence, format set,
UPC product lookup) lives in proven, tested modules that the hooks (11-03) and
components (11-04/05) consume. Mirrors the shipped legacy /frontend scanner (parity).
Output: lib/scanner/{feedback,scan-history,init-polyfill,types,index}.ts + lib/api/barcode.ts + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-scan/11-RESEARCH.md
@.planning/phases/11-scan/11-01-SUMMARY.md

<interfaces>
<!-- Legacy parity sources (RESEARCH cites file:line) + existing api helper. -->

api helper (frontend2/src/lib/api.ts):
  export function get<T>(endpoint: string): Promise<T>   // prepends BASE_URL="/api"
  export class HttpError extends Error { status: number }

Legacy feedback.ts (frontend/lib/scanner/feedback.ts:69-119): singleton
AudioContext, sine oscillator; success = 880Hz/100ms/0.25, error = 300Hz/200ms/0.3;
`resume()` ONLY inside a real user gesture (do NOT resume in module scope); a raw
`navigator.vibrate` path exists independently of ios-haptics.

Legacy scan-history.ts (frontend/lib/scanner/scan-history.ts:40-45): key
`hws-scan-history`, entry shape `{ code, format, entityType, entityId?, entityName?, timestamp }`,
dedup by code, slice(0,10), validator filters malformed entries (legacy data is safe).

Backend GET /api/barcode/{barcode} (VERIFIED handler.go:42-49) — GLOBAL, NOT
ws-scoped; param {barcode} minLen 8 maxLen 14:
  { barcode: string, name: string /* "" when not found */, brand?: string,
    category?: string, image_url?: string, found: boolean }

barcode-detector exposes `BarcodeFormat` type + a `barcode-detector/polyfill`
side-effect import.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port types + init-polyfill</name>
  <files>frontend2/src/lib/scanner/types.ts, frontend2/src/lib/scanner/init-polyfill.ts, frontend2/src/lib/scanner/index.ts</files>
  <behavior>
    - SUPPORTED_FORMATS is exactly ['qr_code','upc_a','ean_13','code_128'] (SCAN-02 subset; binding override 3).
    - ScanHistoryEntry type matches the legacy shape { code, format, entityType, entityId?, entityName?, timestamp }.
    - importing init-polyfill registers the barcode-detector polyfill (side-effect) without throwing under jsdom.
  </behavior>
  <action>Create types.ts exporting `SUPPORTED_FORMATS` (the 4-format tuple), `ScanHistoryEntry`, and re-export `BarcodeFormat` from barcode-detector. Create init-polyfill.ts as a side-effect module importing `barcode-detector/polyfill` (registers BarcodeDetector for Safari/Firefox — RESEARCH Don't-Hand-Roll). Create index.ts barrel re-exporting feedback, scan-history, types. Keep format names lowercase enum strings exactly as the lib expects (`formats` prop, NOT enabledFormats).</action>
  <verify>
    <automated>cd frontend2 && bun install --frozen-lockfile && grep -q "qr_code" src/lib/scanner/types.ts && grep -q "code_128" src/lib/scanner/types.ts && bun run typecheck 2>&1 | tail -3</automated>
  </verify>
  <done>SUPPORTED_FORMATS = the 4-format subset; ScanHistoryEntry + BarcodeFormat exported; init-polyfill imports the polyfill; barrel exports cleanly; typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port feedback.ts + scan-history.ts (with tests)</name>
  <files>frontend2/src/lib/scanner/feedback.ts, frontend2/src/lib/scanner/scan-history.ts, frontend2/src/lib/scanner/feedback.test.ts, frontend2/src/lib/scanner/scan-history.test.ts</files>
  <behavior>
    - feedback: playSuccessBeep() creates/uses a singleton AudioContext oscillator (880Hz); playErrorBeep() 300Hz; a vibrate helper calls navigator.vibrate when present; module does NOT resume AudioContext at import (iOS — Pitfall 4); a primeAudio() (or equivalent) resumes the ctx, intended to be called from a pointerdown gesture.
    - scan-history: getScanHistory() reads + validates hws-scan-history (drops malformed entries, never throws on bad JSON); addToScanHistory(entry) dedups by code and caps at 10 (slice(0,10)); clearScanHistory() removes the key; updateScanHistory(code, item|null) refines the matched entry.
  </behavior>
  <action>Port feedback.ts 1:1 from legacy (singleton AudioContext, sine oscillator, success 880/100/0.25, error 300/200/0.3) plus the raw navigator.vibrate path; expose a primeAudio()/resume helper for the pointerdown unlock — do NOT resume at module load. Port scan-history.ts 1:1: key `hws-scan-history`, dedup-by-code, slice(0,10) (10 per CONTEXT/UI-SPEC, roadmap ×10 over the ×20 parity note), validator-filtered reads tolerant of stale legacy data. Haptics via ios-haptics is wired in the 11-03 hook (useScanFeedback), NOT here — this module owns beep + raw vibrate only. Write vitest tests: feedback (oscillator invoked via the Wave-0 AudioContext-friendly env or a mocked AudioContext; vibrate called) and scan-history (dedup, cap-10, clear, malformed-tolerant) — SCAN-03 / SCAN-06 / SCAN-07 test-map entries.</action>
  <verify>
    <automated>cd frontend2 && bun run test -- src/lib/scanner/feedback src/lib/scanner/scan-history 2>&1 | tail -8</automated>
  </verify>
  <done>feedback + scan-history ported; tests prove beep+vibrate, dedup, cap-10, clear, malformed-tolerance; no module-scope AudioContext.resume.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: barcodeApi.lookup + test</name>
  <files>frontend2/src/lib/api/barcode.ts, frontend2/src/lib/api/barcode.test.ts</files>
  <behavior>
    - barcodeApi.lookup(code) GETs `/barcode/${encodeURIComponent(code)}` (GLOBAL route) and resolves to ProductResponse { barcode, name, brand?, category?, image_url?, found }.
    - A code containing path-injection chars is encoded (encodeURIComponent) before interpolation (T-07-02 / Pitfall 5).
    - found:false is returned faithfully (caller suppresses the banner).
  </behavior>
  <action>Create lib/api/barcode.ts: export `ProductResponse` interface + `barcodeApi.lookup(code)` calling the existing `get<ProductResponse>` against `/barcode/${encodeURIComponent(code)}` (RESEARCH barcodeApi snippet). This route is GLOBAL (not /workspaces) — do NOT prefix with wsId. Write a vitest test using the Wave-0 MSW `/api/barcode/:barcode` handler asserting: found path returns name/brand/found:true; not-found path returns found:false; a code with `../` is encoded in the request URL.</action>
  <verify>
    <automated>cd frontend2 && grep -q "encodeURIComponent" src/lib/api/barcode.ts && bun run test -- src/lib/api/barcode 2>&1 | tail -6</automated>
  </verify>
  <done>barcodeApi.lookup hits the global encoded route; test covers found/not-found + encoding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanned code → backend path | User-controlled code interpolated into a URL |
| localStorage → app | Possibly-stale/malformed persisted history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Tampering | barcodeApi.lookup path | mitigate | encodeURIComponent(code) before interpolation (Pitfall 5 / T-07-02); test asserts encoding. |
| T-11-03 | Tampering | scan-history read | mitigate | Validator filters malformed entries; JSON.parse wrapped — never throws on stale legacy data. |
</threat_model>

<verification>
- `bun install --frozen-lockfile` (no lockfile drift — foundation owns the lockfile).
- `bun run test -- src/lib/scanner src/lib/api/barcode` green.
- `bun run typecheck` clean.
</verification>

<success_criteria>
Four scanner lib modules + barcodeApi ported and tested: format subset exact, beep+vibrate
fire, history deduped/capped-10/clear/malformed-tolerant, barcode lookup encoded + shape-correct.
</success_criteria>

<output>
Create `.planning/phases/11-scan/11-02-SUMMARY.md` — record exact exported names
(playSuccessBeep/primeAudio/getScanHistory/addToScanHistory/clearScanHistory/
updateScanHistory/barcodeApi/SUPPORTED_FORMATS) so 11-03/04/05 import without re-discovery.
</output>
