---
phase: 11-scan
plan: 03
type: execute
wave: 2
depends_on: ["11-02"]
files_modified:
  - frontend2/src/features/scan/useScanHistory.ts
  - frontend2/src/features/scan/useScanFeedback.ts
  - frontend2/src/features/scan/useTorch.ts
  - frontend2/src/features/scan/useScanResolve.ts
  - frontend2/src/features/scan/useScanHistory.test.ts
  - frontend2/src/features/scan/useScanFeedback.test.ts
  - frontend2/src/features/scan/useTorch.test.ts
  - frontend2/src/features/scan/useScanResolve.test.ts
autonomous: true
requirements: [SCAN-03, SCAN-04, SCAN-06, SCAN-08, SCAN-11]
must_haves:
  truths:
    - "useScanResolve exposes ONE handleResolveCode(code, format) that live-scan, manual, and history-tap all call"
    - "handleResolveCode pauses (prop, not unmount), drives a useQuery(['item-by-barcode',wsId,code]) banner, records history, and fires feedback"
    - "useScanFeedback fires beep + ios-haptics/vibrate, reduced-motion-aware on the visual flash"
    - "useTorch returns supported=false on iOS and probes getCapabilities().torch on Android"
    - "useScanHistory is a React wrapper over lib/scanner/scan-history with no render loop"
  artifacts:
    - path: "frontend2/src/features/scan/useScanResolve.ts"
      provides: "The single post-scan funnel + 4-state banner query"
      exports: ["useScanResolve"]
    - path: "frontend2/src/features/scan/useScanFeedback.ts"
      provides: "beep + haptic + visual-flash trigger"
      exports: ["useScanFeedback"]
    - path: "frontend2/src/features/scan/useTorch.ts"
      provides: "torch capability probe + toggle boolean"
      exports: ["useTorch"]
    - path: "frontend2/src/features/scan/useScanHistory.ts"
      provides: "history list + add/clear/refire bridge"
      exports: ["useScanHistory"]
  key_links:
    - from: "frontend2/src/features/scan/useScanResolve.ts"
      to: "itemsApi.lookupByBarcode"
      via: "useQuery item-by-barcode"
      pattern: "item-by-barcode"
    - from: "frontend2/src/features/scan/useScanResolve.ts"
      to: "primitive query status deps"
      via: "effect depends on lookup.status/code (NOT history array)"
      pattern: "lookup.status"
---

<objective>
The React hook layer that turns the 11-02 lib modules + existing itemsApi into the
post-scan state machine. Four hooks: the single resolve funnel (the heart of OQ7),
feedback, torch, and a history bridge. Pure hooks, fully testable with renderHook +
the Wave-0 mocks. Disjoint from 11-04/05/07 (no shared files).

Purpose: the ONE handleResolveCode funnel (binding override 7) guarantees live-scan,
manual-entry, and history-tap behave identically; the 4-state banner is derived from
TanStack query status, not hand-managed (Pattern 3), with the render-loop guard
(primitive deps — Pitfall 6 / Phase 65 D-22).
Output: useScanResolve, useScanFeedback, useTorch, useScanHistory + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-scan/11-RESEARCH.md
@.planning/phases/11-scan/11-02-SUMMARY.md

<interfaces>
<!-- Existing surfaces these hooks consume — do NOT re-explore. -->

itemsApi (frontend2/src/lib/api/items.ts):
  lookupByBarcode(wsId: string, code: string): Promise<Item | null>
    // 404 → null; other errors rethrow; encodeURIComponent already applied.

useWorkspace (frontend2/src/features/workspace/useWorkspace.ts):
  const { currentWorkspaceId } = useWorkspace();   // string | undefined

From 11-02 (see 11-02-SUMMARY for exact names):
  playSuccessBeep/playErrorBeep, primeAudio (resume on gesture),
  getScanHistory/addToScanHistory/clearScanHistory/updateScanHistory,
  SUPPORTED_FORMATS.

ios-haptics: `import { haptic, supportsHaptics } from "ios-haptics"` —
  haptic()/haptic.confirm()/haptic.error(); supportsHaptics abstracts the
  iOS-17.4+-vs-navigator.vibrate split (no UA branching needed — RESEARCH Pattern 5).

Torch probe (RESEARCH Pattern 4 / OQ2): UA-gate iOS (`/iPad|iPhone|iPod/` → false),
else getUserMedia({video:{facingMode:'environment'}}) → track.getCapabilities().torch
→ stop the probe. Apply is lib-managed via components.torch in 11-04.

Query keys (CONTEXT 4): ["item-by-barcode", wsId, code].
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useScanResolve — the single funnel + 4-state banner query</name>
  <files>frontend2/src/features/scan/useScanResolve.ts, frontend2/src/features/scan/useScanResolve.test.ts</files>
  <behavior>
    - handleResolveCode(code, format): no-op on empty code; sets paused=true; sets banner {code, format} which enables a useQuery(["item-by-barcode", wsId, code]); fires feedback; adds the code to scan history.
    - lookup.status maps to banner state: pending→LOADING, success+Item→MATCH, success+null→NOT-FOUND, error→ERROR.
    - a resume()/clearBanner() sets paused=false and clears the banner (Back to Scan).
    - the effect that refines history depends on PRIMITIVE lookup.status/lookup.data/banner.code — NOT the history array (render-loop guard, Pitfall 6).
    - re-resolving the SAME code re-fires the lookup (staleTime:0) so a history re-tap behaves like a fresh scan.
  </behavior>
  <action>Implement useScanResolve per RESEARCH Pattern 3: state `banner:{code,format}|null` + `paused:boolean`; `lookup = useQuery({ queryKey:["item-by-barcode", wsId, banner?.code], queryFn:()=>itemsApi.lookupByBarcode(wsId, banner.code), enabled:Boolean(wsId && banner?.code), staleTime:0 })`. `handleResolveCode = useCallback((code,format)=>{ if(!code) return; setPaused(true); setBanner({code,format}); feedback(); addToScanHistory({...}); }, [...])`. Expose `{ handleResolveCode, paused, setPaused, banner, lookup, resume }`. On lookup settle, refine the history entry via updateScanHistory keyed on primitive deps. Pause is PROP-DRIVEN — this hook never unmounts the scanner (binding override 1). Take `feedback` as a param/dep (from useScanFeedback) so the funnel stays testable. Write renderHook tests: one funnel drives all 4 states from the MSW item-by-barcode handler; empty code is a no-op; re-resolve same code re-fires; assert NO infinite re-render (render count bounded).</action>
  <verify>
    <automated>cd frontend2 && bun install --frozen-lockfile && bun run test -- src/features/scan/useScanResolve 2>&1 | tail -8</automated>
  </verify>
  <done>handleResolveCode is the sole funnel; 4 banner states derive from query status; primitive-dep effect; same-code re-fire works; no render loop.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useScanFeedback + useScanHistory</name>
  <files>frontend2/src/features/scan/useScanFeedback.ts, frontend2/src/features/scan/useScanHistory.ts, frontend2/src/features/scan/useScanFeedback.test.ts, frontend2/src/features/scan/useScanHistory.test.ts</files>
  <behavior>
    - useScanFeedback: success() fires playSuccessBeep + haptic.confirm()/vibrate + a visual-flash signal; error() fires playErrorBeep + haptic.error(); exposes a primeAudio() to call on pointerdown; under prefers-reduced-motion the visual-flash signal is the static-checkmark variant (no animation) — the hook EXPOSES reduced-motion so the component renders the static variant.
    - useScanHistory: returns { entries, add, clear, refire } bridging lib/scanner/scan-history; reads on mount + after mutations; does NOT cause a render loop (state-backed, not re-reading every render).
  </behavior>
  <action>useScanFeedback: wrap 11-02 beep + ios-haptics (supportsHaptics gate; no UA branching — RESEARCH Pattern 5) + a flash trigger; read `window.matchMedia('(prefers-reduced-motion: reduce)')` and expose it so the component picks the static-checkmark fallback (UI-SPEC Surface 2 motion contract). Expose primeAudio (Pitfall 4 — resume in pointerdown). useScanHistory: state-backed list synced from getScanHistory(), with add/clear/refire helpers; refire(entry) returns the stored code for handleResolveCode (the component wires it to the funnel). Tests: success/error fire beep+haptic; reduced-motion flag honored; history add/clear/refire reflect localStorage; SCAN-03/06 test-map.</action>
  <verify>
    <automated>cd frontend2 && bun run test -- src/features/scan/useScanFeedback src/features/scan/useScanHistory 2>&1 | tail -8</automated>
  </verify>
  <done>feedback fires beep+haptic+flash with reduced-motion awareness; history hook bridges add/clear/refire without a render loop.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: useTorch — capability probe + toggle</name>
  <files>frontend2/src/features/scan/useTorch.ts, frontend2/src/features/scan/useTorch.test.ts</files>
  <behavior>
    - On iOS (UA matches /iPad|iPhone|iPod/) supported=false WITHOUT probing (auto-hide, SCAN-04).
    - On non-iOS, probes getUserMedia → track.getCapabilities().torch, stops the probe stream, sets supported accordingly.
    - exposes { supported, enabled, toggle } where `enabled` is the boolean fed into the scanner lib's components.torch in 11-04 (lib-managed apply, RESEARCH Pattern 4 approach 1).
  </behavior>
  <action>Implement useTorch per RESEARCH Pattern 4: UA-gate iOS first (return supported:false, no probe); else async probe getUserMedia({video:{facingMode:'environment'}}), read track.getCapabilities().torch, stop ALL probe tracks (release), set supported. Manage `enabled` boolean + toggle. Apply is lib-managed (the component passes `components={{ torch: supported && enabled }}`) — this hook does NOT itself call applyConstraints (the probe-race makes the lib path more reliable on Android — RESEARCH OQ2). Tests use the Wave-0 getUserMedia stub (controllable getCapabilities().torch): iOS UA → supported false + no getUserMedia call; Android torch:true → supported true; torch:false → supported false; toggle flips enabled.</action>
  <verify>
    <automated>cd frontend2 && bun run test -- src/features/scan/useTorch 2>&1 | tail -8</automated>
  </verify>
  <done>iOS auto-hides (no probe); Android probes capabilities and releases the stream; toggle works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanned code → lookup query | User-controlled code drives the item lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering | item-by-barcode lookup | mitigate | lookupByBarcode already encodeURIComponents the code (items.ts:94); the funnel passes the raw code through that boundary, never building paths itself. |
| T-11-05 | DoS | render-loop / runaway query | mitigate | Effect deps are primitive status/code (Pitfall 6); useQuery is keyed + enabled-gated; test asserts bounded render count. |
</threat_model>

<verification>
- `bun install --frozen-lockfile`.
- `bun run test -- src/features/scan/useScan src/features/scan/useTorch` green.
- `bun run typecheck` clean.
</verification>

<success_criteria>
One funnel drives the 4-state banner query; feedback fires beep+haptic+flash (reduced-motion
aware); torch probes/auto-hides correctly; history bridge add/clear/refire; no render loops.
</success_criteria>

<output>
Create `.planning/phases/11-scan/11-03-SUMMARY.md` — record each hook's return shape
(handleResolveCode/paused/banner/lookup/resume; success/error/primeAudio/reducedMotion;
supported/enabled/toggle; entries/add/clear/refire) for 11-06's ScanPage wiring.
</output>
