---
phase: 11-scan
plan: 08
type: execute
wave: 4
depends_on: ["11-06", "11-07"]
files_modified:
  - frontend2/src/locales/en/messages.po
  - frontend2/src/locales/et/messages.po
  - frontend2/src/locales/ru/messages.po
  - frontend2/e2e/scan-lookup.spec.ts
autonomous: false
requirements: [SCAN-05, SCAN-08, SCAN-09]
must_haves:
  truths:
    - "All Phase 11 call-site strings are extracted into the en/et/ru catalogs (no untranslated literals)"
    - "A Playwright spec drives the MANUAL-entry path against the real backend: manual code → MATCH banner and → NOT-FOUND banner"
    - "The G-65-01 by-barcode guard is re-established (browser-level barcode lookup coverage)"
  artifacts:
    - path: "frontend2/e2e/scan-lookup.spec.ts"
      provides: "MANUAL-entry → MATCH + NOT-FOUND banner E2E (G-65-01 re-add)"
      contains: "MANUAL"
    - path: "frontend2/src/locales/en/messages.po"
      provides: "Extracted EN catalog incl. Phase 11 strings"
  key_links:
    - from: "frontend2/e2e/scan-lookup.spec.ts"
      to: "/scan MANUAL tab → lookupByBarcode"
      via: "real backend manual-entry lookup"
      pattern: "scan"
---

<objective>
Phase-close plan: extract the Lingui catalogs (EN/ET/RU) so every Phase 11 string is
translatable, and re-add the by-barcode Playwright spec that was wiped with the v2.2
frontend2 (standing gap G-65-01). The spec drives the MANUAL-entry path — a real
camera cannot be driven in CI, so it types codes into the Manual tab and asserts the
MATCH and NOT-FOUND banners against the REAL backend (binding override: manual code →
banners against real backend; ONE login; discovery-list gate).

Purpose: closes the Validation phase-gate items (Lingui clean + live by-barcode E2E)
and restores the long-standing browser-level barcode-lookup guard.
Output: extracted catalogs + frontend2/e2e/scan-lookup.spec.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-scan/11-VALIDATION.md
@.planning/phases/11-scan/11-06-SUMMARY.md
@.planning/phases/11-scan/11-07-SUMMARY.md

<interfaces>
<!-- E2E runbook (CLAUDE.md) + existing spec conventions. -->

Lingui: `bun run i18n:extract` updates src/locales/{en,et,ru}/messages.po from all
<Trans>/t`` call sites. EN is the source; ET/RU get new msgids (translate or leave
empty per existing catalog convention).

E2E (CLAUDE.md "Add a new spec"): files in frontend2/e2e/*.spec.ts run in chromium +
firefox. baseURL http://localhost:5173 (Vite proxy /api → :8080 with the load-bearing
/api rewrite). Auth contract: /login → fill Email + Password labels → click submit
matching /^log in$/i. After login the browser holds access_token cookie; page.request
inherits it. API-only seeding via page.request.post("/api/workspaces/{wsId}/...").
E2E_USER=seeder@test.local E2E_PASS=password123 (override via env).

The Phase 65 scan-lookup spec (wiped) prior shape (CLAUDE.md): login → seed item →
/scan → MANUAL tab → MATCHED banner. The full-suite auth rate-limit constraint
(recent commit c8c3a2f0) means: do ONE login and reuse the session; do not re-login
per assertion.

ScanPage (11-06): /scan, MANUAL tab (?tab=manual), ManualBarcodeEntry RetroInput
(label ENTER CODE) + LOOK UP CODE button; banner words MATCH / NOT FOUND.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Lingui catalogs (EN/ET/RU)</name>
  <files>frontend2/src/locales/en/messages.po, frontend2/src/locales/et/messages.po, frontend2/src/locales/ru/messages.po</files>
  <action>Run `bun run i18n:extract` from frontend2. This sweeps all Phase 11 <Trans>/t`` call sites (ScanPage, the scan components, ClaimPage, the new ItemFormPage strings, copy from UI-SPEC Copywriting Contract) into the catalogs. Confirm EN gains the new msgids (LOOK UP CODE, Point the camera…, CAMERA BLOCKED, ENTER CODE, RECENT SCANS, NO SCANS YET, banner words, MATCHED ITEM, VIEW ITEM/LOAN/UNARCHIVE/MARK REVIEWED/BACK TO SCAN, PRODUCT FOUND, USE NAME/USE ALL/DISMISS, CLAIM ITEM, CODE NOT FOUND, CLEAR HISTORY?, etc. — all already wrapped by Waves 2-3). Leave ET/RU translations per the existing catalog convention (empty msgstr is acceptable; do NOT hand-fake translations). Verify no Phase 11 call site renders a raw literal (extract would surface it).</action>
  <verify>
    <automated>cd frontend2 && bun run i18n:extract 2>&1 | tail -5 && grep -q "Look up code" src/locales/en/messages.po && echo CATALOGS_OK</automated>
  </verify>
  <done>en/et/ru catalogs regenerated; Phase 11 msgids present in EN; no untranslated literals at call sites.</done>
</task>

<task type="auto">
  <name>Task 2: Re-add the by-barcode Playwright spec (G-65-01)</name>
  <files>frontend2/e2e/scan-lookup.spec.ts</files>
  <action>Create frontend2/e2e/scan-lookup.spec.ts. ONE login at the top (reuse the login-dashboard.spec auth recipe; honor the rate-limit constraint — single login, reuse the session/cookie). Then, via page.request (cookie inherited), seed ONE item with a known barcode into the current workspace (POST /api/workspaces/{wsId}/items) so a MATCH exists — a discovery/list step gates that the item is present before asserting. Navigate to /scan, activate the MANUAL tab (click the MANUAL tab or go to /scan?tab=manual), and in ManualBarcodeEntry: (a) type the seeded barcode → LOOK UP CODE → assert the MATCH banner shows (word MATCH + the item name); (b) type a guaranteed-absent code (e.g. a random 13-digit string) → LOOK UP CODE → assert the NOT-FOUND banner (word NOT FOUND + a CREATE WITH CODE affordance). Do NOT attempt to drive a real camera — manual-entry is the CI-drivable path (binding override / CLAUDE.md note). Locators: prefer label/role + the field-verb prefix discipline used in the loosened inventory spec (recent commit bf694bba). Spec runs in chromium + firefox (config default).</action>
  <verify>
    <automated>cd frontend2 && grep -q "MANUAL\|manual" e2e/scan-lookup.spec.ts && grep -qi "not found\|NOT-FOUND\|NOT FOUND" e2e/scan-lookup.spec.ts && npx playwright test --list e2e/scan-lookup.spec.ts 2>&1 | tail -5</automated>
  </verify>
  <done>scan-lookup.spec.ts does ONE login, seeds a matching item (gated by a discovery step), and asserts both MATCH and NOT-FOUND banners via the MANUAL tab against the real backend.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: live by-barcode E2E (isolated run)</name>
  <action>Human runs the new scan-lookup spec in isolation against the local stack (Postgres + backend + Vite + seeder user) to avoid the suite-wide auth rate-limit, and confirms MATCH + NOT-FOUND on chromium + firefox.</action>
  <what-built>The live by-barcode E2E spec. It needs Postgres + the backend (8080) + the Vite dev server (5173) + a seeder user, per the CLAUDE.md runbook. The auth limiter makes the full E2E suite flaky when run together, so this spec is verified in isolation.</what-built>
  <how-to-verify>
    1. Ensure Postgres up (`docker compose up -d postgres`), backend running (`cd backend && go run ./cmd/server/main.go`, port 8080), and Vite (`cd frontend2 && bun run dev`, port 5173).
    2. Ensure the seeder user exists (E2E_USER=seeder@test.local / E2E_PASS=password123).
    3. Run ONLY this spec to avoid the suite-wide auth rate-limit:
       `cd frontend2 && E2E_USER=seeder@test.local E2E_PASS=password123 bun run test:e2e -- scan-lookup`
    4. Confirm both projects (chromium + firefox) pass: MATCH banner for the seeded code, NOT-FOUND banner for the random code.
  </how-to-verify>
  <resume-signal>Type "approved" if the isolated spec passes on both browsers, or paste failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E seeding → real backend | Test creates real data via the authenticated API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-17 | Spoofing | E2E auth rate-limit | mitigate | ONE login per spec run + session reuse (recent c8c3a2f0 constraint); spec run in isolation at the gate. |
| T-11-18 | Info Disclosure | seeded test data | accept | Seeds into the seeder user's dev workspace only; cross-tenant barcode scoping is guarded by the Phase 65 backend integration test. |
</threat_model>

<verification>
- `bun run i18n:extract` clean; EN catalog has the Phase 11 msgids.
- `npx playwright test --list e2e/scan-lookup.spec.ts` lists the spec.
- Isolated live run green on chromium + firefox (human-verify checkpoint).
- Full phase gate (orchestrator): tsc clean, `bun run test` green, `bun run build`, `bun run lint:imports` OK.
</verification>

<success_criteria>
All Phase 11 strings extracted into en/et/ru; the by-barcode Playwright spec re-added (G-65-01),
driving the MANUAL-entry path to MATCH + NOT-FOUND banners against the real backend, green in isolation.
</success_criteria>

<output>
Create `.planning/phases/11-scan/11-08-SUMMARY.md` — note the catalog deltas and the
spec's coverage (manual path only; camera/torch/haptic remain device-only human-UAT residues).
</output>
