---
phase: 11-scan
plan: 06
type: execute
wave: 3
depends_on: ["11-03", "11-04", "11-05", "11-07"]
files_modified:
  - frontend2/src/features/scan/ScanPage.tsx
  - frontend2/src/features/scan/ScanPage.test.tsx
  - frontend2/src/routes/index.tsx
  - frontend2/src/components/layout/Sidebar.tsx
  - frontend2/src/features/items/ItemFormPage.tsx
  - frontend2/src/features/items/ItemFormPage.test.tsx
autonomous: true
requirements: [SCAN-01, SCAN-05, SCAN-06, SCAN-07, SCAN-08, SCAN-09, SCAN-10, SCAN-11, SCAN-12]
must_haves:
  truths:
    - "ScanPage mounts BarcodeScanner ONCE in a persistent sibling layer; tab switches toggle CSS visibility, never unmount the scanner"
    - "Live scan, manual submit, and history tap all funnel through the single handleResolveCode"
    - "The 4-state banner + quick-action overlay render OVER the always-mounted scanner"
    - "/scan (lazy) and /claim/:code (under RequireAuth) routes resolve; the Scan nav item is enabled"
    - "ItemFormPage reads ?name= and ?brand= so SCAN-10 USE ALL prefills name+brand"
  artifacts:
    - path: "frontend2/src/features/scan/ScanPage.tsx"
      provides: "Scan route orchestration: persistent scanner + tabs + banner + overlay"
      exports: ["ScanPage"]
    - path: "frontend2/src/routes/index.tsx"
      provides: "/scan (lazy) + /claim/:code routes"
      contains: "claim/:code"
    - path: "frontend2/src/components/layout/Sidebar.tsx"
      provides: "Enabled Scan nav item (to=/scan)"
      contains: "/scan"
    - path: "frontend2/src/features/items/ItemFormPage.tsx"
      provides: "?name=/?brand= prefill consumption (SCAN-10 USE ALL)"
      contains: "searchParams.get(\"name\")"
  key_links:
    - from: "frontend2/src/features/scan/ScanPage.tsx"
      to: "BarcodeScanner (persistent sibling)"
      via: "CSS visibility toggle, NOT a RetroTabs panel child"
      pattern: "BarcodeScanner"
    - from: "frontend2/src/routes/index.tsx"
      to: "ScanPage / ClaimPage"
      via: "literal/param routes before the wildcard"
      pattern: "path=\"scan\""
---

<objective>
The integration plan. ScanPage assembles the persistent-scanner architecture
(binding override 1 / RESEARCH Pattern 2): BarcodeScanner is hoisted OUT of RetroTabs
into an always-mounted sibling layer with CSS-visibility toggling; the tabs +
4-state banner + quick-action overlay render on top. It wires the 11-03 hooks
(funnel/feedback/torch/history) to the 11-04/05 components. Then it owns the three
SINGLE-WRITER files: routes/index.tsx (+/scan lazy, +/claim/:code), Sidebar.tsx
(enable Scan nav), ItemFormPage.tsx (read ?name=/?brand= for SCAN-10 USE ALL).

Purpose: this is where the scanner becomes a working route. Because routes/index.tsx,
Sidebar.tsx, and ItemFormPage.tsx are single-writer, this plan is alone in Wave 3 and
imports everything built in Wave 2 (incl. ClaimPage from 11-07).
Output: ScanPage + the three wired single-writer files + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-scan/11-RESEARCH.md
@.planning/phases/11-scan/11-UI-SPEC.md
@.planning/phases/11-scan/11-03-SUMMARY.md
@.planning/phases/11-scan/11-04-SUMMARY.md
@.planning/phases/11-scan/11-05-SUMMARY.md
@.planning/phases/11-scan/11-07-SUMMARY.md

<interfaces>
<!-- From Wave 2 (see each SUMMARY for exact names) + existing single-writer files. -->

Hooks (11-03): useScanResolve → { handleResolveCode, paused, setPaused, banner, lookup, resume };
  useScanFeedback → { success, error, primeAudio, reducedMotion };
  useTorch → { supported, enabled, toggle };
  useScanHistory → { entries, add, clear, refire }.

Components (11-04/05, barrel @/components/scan): BarcodeScanner { paused, onDecode,
  onError, torchSupported, torchEnabled }; ScanViewfinderOverlay; ScanTorchToggle
  { supported, enabled, onToggle }; ManualBarcodeEntry { onSubmit }; ScanResultBanner
  { status, code, item, onOpenActions, onRetry }; QuickActionMenu { item, onClose };
  ScanHistoryList { entries, onSelect, onClear }; UpcSuggestionBanner { code, onUse, onDismiss }.

ClaimPage (11-07): export ClaimPage; route /claim/:code under RequireAuth.

RetroTabs (frontend2/src/components/retro/data/RetroTabs.tsx): renders ONLY active.content
  ({active && <tabpanel>}) — Pitfall 1. The scanner CANNOT be a tab panel child.

routes/index.tsx (frontend2/src/routes/index.tsx): library-mode RR7. Authenticated
  branch is the RequireAuth+AppShell layout route with child routes. Literal/param
  routes go BEFORE the `*` wildcard (Pitfall 7). React.lazy pattern exists in App.tsx
  (`const X = lazy(() => import(...))`) — lazy ScanPage so the scanner chunk (11-01
  manualChunks) loads only on /scan.

Sidebar.tsx (frontend2/src/components/layout/Sidebar.tsx:149): a DISABLED Scan NavItem
  (`<NavItem glyph="⌗" label={<Trans>Scan</Trans>} />`) — add `to="/scan"`.

ItemFormPage.tsx (frontend2/src/features/items/ItemFormPage.tsx:80): reads
  `searchParams.get("barcode")` → prefillBarcode (FROM SCAN badge). It does NOT yet read
  ?name=/?brand=. The RHF defaultValues object is built from prefill. The form has a
  `name` field (register("name")); brand is NOT a current form field (schema.ts has
  sku/name/description/barcode/category/location/minStock) — so USE ALL applies NAME
  via ?name= reliably; if brand is wired, it flows to the create mutation only if a
  brand input exists. Wire ?name= (and ?brand= passthrough where the form/mutation
  accept it) without inventing a brand form field.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ScanPage — persistent scanner + tabs + banner + overlay</name>
  <files>frontend2/src/features/scan/ScanPage.tsx, frontend2/src/features/scan/ScanPage.test.tsx</files>
  <behavior>
    - BarcodeScanner is rendered ONCE as a persistent sibling (NOT inside a RetroTabs panel); switching tabs toggles its wrapper's CSS visibility (hidden class), it is NEVER unmounted.
    - activeTab ∈ {scan,manual,history} via the ?tab= URL sync (LoansListPage recipe); RetroTabs hosts ONLY the Manual + History overlay panels (the scan tab's panel is empty/spacer — the real scanner is the sibling).
    - handleResolveCode is the single funnel: live decode (BarcodeScanner.onDecode), ManualBarcodeEntry.onSubmit, and ScanHistoryList row tap all call it.
    - The ScanResultBanner renders over the scanner from useScanResolve's lookup state; MATCH → opening QuickActionMenu (RetroDialog, camera stays mounted); BACK TO SCAN → resume (paused=false).
    - A pointerdown primer on the page wrapper calls primeAudio once (iOS AudioContext unlock, Pitfall 4).
    - Camera onError (NotAllowedError / no device) → RetroEmptyState CAMERA BLOCKED + SWITCH TO MANUAL (activates manual tab); Manual + History stay usable.
    - The torch toggle renders only when useTorch.supported.
  </behavior>
  <action>Implement ScanPage per RESEARCH Pattern 2 + UI-SPEC Surface 1. Wire useScanResolve (feeding it useScanFeedback.success/error as the feedback fn), useTorch, useScanHistory. Structure: a `<Window title="SCAN" titlebarVariant="blue" onPointerDown={primeAudio}>` containing (a) a PERSISTENT `<div className={activeTab==='scan' ? '' : 'hidden'}>` holding BarcodeScanner (paused from useScanResolve, onDecode=handleResolveCode, torchSupported/torchEnabled from useTorch) + ScanViewfinderOverlay + ScanTorchToggle + ScanResultBanner + (banner==='match' && open) QuickActionMenu; (b) a RetroTabs whose tabs are scan(content:null spacer), manual(ManualBarcodeEntry onSubmit=handleResolveCode), history(ScanHistoryList entries/onSelect=(code)=>handleResolveCode(code,'history')/onClear=clear). Use ?tab= URL sync (reuse LoansListPage setTab/useSearchParams). The scanner node MUST be the sibling, never a tab child (Pitfall 1). NOT-FOUND banner already links to /items/new?barcode= (built in 11-05). Camera-blocked → RetroEmptyState. Test with the scanner-mock + MSW: (1) the SAME scanner DOM node persists across a scan→manual→scan tab switch (assert not remounted — e.g. a stable test id / single mount count); (2) firing a decode → LOADING then MATCH banner → QuickActionMenu opens; (3) manual submit funnels identically; (4) history tap re-fires; (5) NotAllowedError → CAMERA BLOCKED + SWITCH TO MANUAL. (SCAN-01/05/06/07/08 test-map.)</action>
  <verify>
    <automated>cd frontend2 && bun install --frozen-lockfile && bun run test -- src/features/scan/ScanPage 2>&1 | tail -10</automated>
  </verify>
  <done>Scanner mounts once + persists across tab switches; one funnel for scan/manual/history; banner+overlay render over the scanner; pointerdown audio primer; camera-blocked degrade.</done>
</task>

<task type="auto">
  <name>Task 2: Wire routes (/scan lazy, /claim/:code) + enable Scan nav</name>
  <files>frontend2/src/routes/index.tsx, frontend2/src/components/layout/Sidebar.tsx</files>
  <action>routes/index.tsx (SINGLE-WRITER — binding override 10): import ScanPage via React.lazy (`const ScanPage = lazy(() => import("@/features/scan/ScanPage").then(m => ({ default: m.ScanPage })))`) and ClaimPage (lazy or static) from 11-07. Add `<Route path="scan" element={<Suspense fallback={...}><ScanPage/></Suspense>} />` and `<Route path="claim/:code" element={<ClaimPage/>} />` INSIDE the RequireAuth+AppShell layout branch (both login-gated; SCAN-12 login-required) and BEFORE the `*` wildcard (Pitfall 7). Wrap the lazy ScanPage in Suspense (App.tsx lazy idiom). Sidebar.tsx (SINGLE-WRITER): add `to="/scan"` to the disabled Scan NavItem at line 149 (enable it). Do NOT touch any other route or nav item.</action>
  <verify>
    <automated>cd frontend2 && grep -q 'path="scan"' src/routes/index.tsx && grep -q 'claim/:code' src/routes/index.tsx && grep -q 'lazy' src/routes/index.tsx && grep -q 'to="/scan"' src/components/layout/Sidebar.tsx && bun run typecheck 2>&1 | tail -3</automated>
  </verify>
  <done>/scan (lazy, Suspense) + /claim/:code routes registered under RequireAuth before the wildcard; Scan nav enabled; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ItemFormPage reads ?name= / ?brand= (SCAN-10 USE ALL)</name>
  <files>frontend2/src/features/items/ItemFormPage.tsx, frontend2/src/features/items/ItemFormPage.test.tsx</files>
  <behavior>
    - On create (not edit), ?name= prefills the Name field in addition to the existing ?barcode= → barcode field.
    - ?brand= is consumed where the form/create-mutation accepts brand; if there is no brand form field, brand is passed through to the create mutation payload (the mutation already maps brand) without breaking the form — name prefill is the guaranteed USE-ALL effect.
    - Edit mode ignores ?name=/?brand= (prefill is create-only, as ?barcode= already is).
  </behavior>
  <action>ItemFormPage.tsx (SINGLE-WRITER — binding override 5): extend the existing prefill block (line ~80, where `prefillBarcode = searchParams.get("barcode")`) to also read `searchParams.get("name")` and `searchParams.get("brand")` (create-only). Fold prefillName into the RHF defaultValues alongside barcode (the form HAS a name field — register("name")). For brand: the items feature schema has no brand field today, so do NOT invent a UI field — instead pass the prefilled brand into the create mutation payload via useItemFormMutations (which already references brand) OR, if cleaner, add brand to defaultValues only if a brand field is later present; the RELIABLE USE-ALL contract is name prefill. Keep the FROM SCAN badge logic intact. Values are React-controlled (XSS-safe, T-07-14). Test (MemoryRouter at /items/new?barcode=...&name=...&brand=...): Name field shows the prefilled name; barcode still prefills + FROM SCAN badge; edit mode ignores the params. (SCAN-10 USE ALL test-map.)</action>
  <verify>
    <automated>cd frontend2 && grep -q 'searchParams.get("name")' src/features/items/ItemFormPage.tsx && bun run test -- src/features/items/ItemFormPage 2>&1 | tail -8</automated>
  </verify>
  <done>Create-mode ItemFormPage prefills Name from ?name= (and threads ?brand= to the create payload); barcode/FROM-SCAN intact; edit unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ?name=/?brand= query → form | External product metadata into a controlled form |
| /scan + /claim/:code | Auth-gated routes |
| camera persistence | iOS PWA permission must not re-prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-13 | Tampering | ?name=/?brand= prefill | mitigate | Values bound into React-controlled inputs (auto-escaped, T-07-14); zod length bounds apply on submit. |
| T-11-14 | Info Disclosure | iOS camera re-prompt on tab switch | mitigate | Scanner is a persistent CSS-toggled sibling, never a RetroTabs panel child (Pitfall 1); test asserts no remount across tab switches. |
| T-11-15 | EoP | unauth /scan or /claim access | mitigate | Both routes inside the RequireAuth+AppShell branch; unauth → /login?next= (existing). |
| T-11-16 | DoS | scanner bytes on every page | mitigate | ScanPage is React.lazy → scanner manualChunk (11-01) loads only on /scan. |
</threat_model>

<verification>
- `bun install --frozen-lockfile`.
- `bun run test -- src/features/scan/ScanPage src/features/items/ItemFormPage` green.
- `bun run typecheck` clean; `bun run lint:imports` OK.
- `bun run build` succeeds (scanner emitted as its own chunk).
</verification>

<success_criteria>
ScanPage = single-mount scanner + one funnel + banner/overlay on top + audio primer +
camera-blocked degrade; /scan (lazy) + /claim/:code routes under RequireAuth; Scan nav enabled;
ItemFormPage consumes ?name=/?brand= for USE ALL. Build green with a dedicated scanner chunk.
</success_criteria>

<output>
Create `.planning/phases/11-scan/11-06-SUMMARY.md` — note the persistent-scanner mount
proof, the funnel wiring, the route registrations, and the ItemFormPage param additions.
</output>
