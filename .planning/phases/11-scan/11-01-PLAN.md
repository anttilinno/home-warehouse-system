---
phase: 11-scan
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend2/package.json
  - frontend2/bun.lockb
  - frontend2/vite.config.ts
  - frontend2/src/lib/types.ts
  - frontend2/src/test/setup.ts
  - frontend2/src/test/msw/handlers.ts
  - frontend2/src/test/scanner-mock.ts
autonomous: false
requirements: [SCAN-02, SCAN-10, SCAN-11]
must_haves:
  truths:
    - "The three scanner deps resolve at their exact pins and the lockfile is committed"
    - "`bun install --frozen-lockfile` succeeds for every later plan"
    - "Unit tests can mount components that import @yudiel/react-qr-scanner without a real camera"
    - "The frontend2 Item type carries needs_review so SCAN-11 gating type-checks"
  artifacts:
    - path: "frontend2/package.json"
      provides: "Exact pins for @yudiel/react-qr-scanner@2.5.1, barcode-detector@3.0.8, ios-haptics@0.1.4"
      contains: "@yudiel/react-qr-scanner"
    - path: "frontend2/bun.lockb"
      provides: "Frozen lockfile for all later plans"
    - path: "frontend2/src/test/scanner-mock.ts"
      provides: "vi.mock stub for @yudiel/react-qr-scanner that fires onScan on a test trigger"
    - path: "frontend2/src/lib/types.ts"
      provides: "needs_review?: boolean on Item"
      contains: "needs_review"
  key_links:
    - from: "frontend2/vite.config.ts"
      to: "scanner chunk"
      via: "rollupOptions.output.manualChunks"
      pattern: "manualChunks"
    - from: "frontend2/src/test/msw/handlers.ts"
      to: "/api/barcode/:barcode"
      via: "http.get handler"
      pattern: "barcode/:barcode"
---

<objective>
FOUNDATION plan for Phase 11 (Scan). This is the SINGLE non-frozen installer: it
adds the three exact-pinned scanner dependencies, commits the lockfile, opens the
scanner WASM manual-chunk slot in vite.config.ts, adds the test-infra camera /
BarcodeDetector mocks + a reusable `@yudiel/react-qr-scanner` mock + an MSW
`GET /api/barcode/:barcode` handler, and adds `needs_review` to the frontend2
`Item` type (types.ts is single-writer; foundation owns it — binding override 8/10).

Purpose: every other Phase 11 plan branches from this merged base and uses
`bun install --frozen-lockfile`. Without the deps + mocks in place, no later plan
can install or unit-test the scanner surface.
Output: deps pinned + lockfile committed, vite chunk slot wired, test mocks +
barcode MSW handler, Item.needs_review.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-scan/11-CONTEXT.md
@.planning/phases/11-scan/11-RESEARCH.md
@.planning/phases/11-scan/11-VALIDATION.md

<interfaces>
<!-- Existing test-infra patterns the executor extends — do NOT re-explore. -->

frontend2/src/test/setup.ts already registers the MSW server and defines
`MockEventSource` (the camera/BarcodeDetector mocks must follow the SAME
pattern — a static class/registry installed on the global, reset in afterEach).

frontend2/src/test/msw/handlers.ts: shared MSW handlers. The api.ts client
prepends `BASE_URL = "/api"`, so handlers match at `/api/...`. Per-test override
via `server.use(...)`.

frontend2/src/lib/types.ts Item (lines 113-135): id, workspace_id, sku, name,
description?, category_id?, brand?, model?, serial_number?, manufacturer?,
barcode?, is_insured?, is_archived?, min_stock_level, short_code,
primary_photo_thumbnail_url?, primary_photo_url?, created_at, updated_at.
Backend by-barcode handler serializes NeedsReview (handler.go:659) → add
`needs_review?: boolean` to this interface.

frontend2/vite.config.ts: the manualChunks slot is reserved (commented) at the
`build` block — `// rollupOptions: { output: { manualChunks: { /* scanner */ } } }`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Package legitimacy gate (pre-install)</name>
  <action>Human verifies the three scanner packages on npmjs.com before the non-frozen install (defense-in-depth — slopcheck unavailable in research; ios-haptics is young/single-maintainer). Do not run `bun add` until approved.</action>
  <what-built>Pre-install package-legitimacy gate for the three scanner deps. slopcheck was NOT available during research (RESEARCH Package Legitimacy Audit); ios-haptics is a young single-maintainer package. Per the package legitimacy gate, the human verifies the packages before the non-frozen install.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@yudiel/react-qr-scanner — confirm version 2.5.1 exists, repo github.com/yudielcurbelo/react-qr-scanner.
    2. Open https://www.npmjs.com/package/barcode-detector — confirm 3.0.8 exists (transitive dep of the scanner; bundled).
    3. Open https://www.npmjs.com/package/ios-haptics — confirm 0.1.4 exists, repo github.com/tijnjh/ios-haptics. Note: young/single-maintainer; it ships in the legacy /frontend production app (parity-locked).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the non-frozen install, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Add the three scanner deps (NON-frozen) + commit lockfile</name>
  <files>frontend2/package.json, frontend2/bun.lockb</files>
  <action>From the frontend2 directory run `bun add @yudiel/react-qr-scanner@2.5.1 ios-haptics@0.1.4 barcode-detector@3.0.8` — EXACT pins, NO caret, per binding override 2 + SCAN-02. This is the ONLY plan permitted to run `bun add` / a non-frozen install; it writes package.json + bun.lockb. Do NOT pass --frozen-lockfile here (Pitfall 8). barcode-detector arrives transitively at 3.0.8 via the scanner lib, but it is added EXPLICITLY at the same pin because 11-02 imports `barcode-detector/polyfill` and its `BarcodeFormat` type directly. After install, verify package.json shows all three at the exact pins (no `^`). Commit package.json + bun.lockb in this plan's commit.</action>
  <verify>
    <automated>cd frontend2 && grep -q '"@yudiel/react-qr-scanner": "2.5.1"' package.json && grep -q '"ios-haptics": "0.1.4"' package.json && grep -q '"barcode-detector": "3.0.8"' package.json && echo PINS_OK</automated>
  </verify>
  <done>package.json carries all three deps at exact pins; bun.lockb updated; `bun install --frozen-lockfile` (re-run) succeeds with no lockfile drift.</done>
</task>

<task type="auto">
  <name>Task 2: Open the scanner manualChunks slot in vite.config.ts</name>
  <files>frontend2/vite.config.ts</files>
  <action>Uncomment / fill the reserved `rollupOptions.output.manualChunks` slot in the `build` block so the scanner stack (zxing-wasm pulled by barcode-detector + @yudiel/react-qr-scanner) is emitted as its own lazy chunk rather than inflating the main bundle. Add a manualChunks function (or map) routing module ids containing `@yudiel/react-qr-scanner`, `barcode-detector`, or `zxing-wasm` into a `scanner` chunk. Keep the existing `sourcemap: false`. This pairs with 11-06's `React.lazy` of the ScanPage (Pattern 5) so the scanner bytes never load until /scan is visited.</action>
  <verify>
    <automated>cd frontend2 && grep -v '^[[:space:]]*//' vite.config.ts | grep -c manualChunks | grep -qv '^0$' && echo CHUNK_SLOT_OK</automated>
  </verify>
  <done>vite.config.ts has an active (non-comment) manualChunks rule routing scanner deps to a dedicated chunk.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test-infra mocks — camera, BarcodeDetector, scanner lib, barcode MSW</name>
  <files>frontend2/src/test/setup.ts, frontend2/src/test/scanner-mock.ts, frontend2/src/test/msw/handlers.ts</files>
  <behavior>
    - A test that calls `navigator.mediaDevices.getUserMedia({video:...})` resolves to a fake MediaStream whose video track exposes `getCapabilities()` returning `{ torch: false }` by default (overridable) and a `stop()` no-op.
    - `globalThis.BarcodeDetector` is defined (stub class) so feature code that probes it does not throw.
    - The scanner-mock module exports a `vi.mock`-able stub `Scanner` component (default + named export `Scanner`, plus the `IDetectedBarcode` type re-export) that renders a test hook (e.g. a button / exposed callback) to fire `onScan([{ rawValue, format }])` on demand and does NOT touch real getUserMedia.
    - MSW `GET /api/barcode/:barcode` returns `{ barcode, name, brand, found: true }` for a "found" fixture code and `{ barcode, name: "", found: false }` for a not-found fixture code.
  </behavior>
  <action>In setup.ts add, mirroring the existing MockEventSource pattern: (a) a `navigator.mediaDevices.getUserMedia` stub returning a fake stream with a controllable `getCapabilities().torch`; (b) a `globalThis.BarcodeDetector` stub class; reset overrides in afterEach. Create `src/test/scanner-mock.ts` exporting a stub `Scanner` (matching @yudiel/react-qr-scanner's `{ onScan, onError, paused, formats, ... }` prop surface — see RESEARCH OQ1) plus a test trigger so component tests can simulate a decode; the v2.2 archived 64-02 plan did exactly this. In msw/handlers.ts add the `/api/barcode/:barcode` handler (found + not-found cases) per the verified response shape `{ barcode, name, brand?, category?, image_url?, found }` (RESEARCH Pattern 6 / OQ5). Do NOT call `bun add` for any test util — these are local modules + existing vitest/MSW.</action>
  <verify>
    <automated>cd frontend2 && bun install --frozen-lockfile && bun run test -- src/test 2>&1 | tail -5; grep -q "getUserMedia" src/test/setup.ts && grep -q "BarcodeDetector" src/test/setup.ts && grep -q "barcode/:barcode" src/test/msw/handlers.ts && echo MOCKS_OK</automated>
  </verify>
  <done>setup.ts stubs getUserMedia + BarcodeDetector; scanner-mock.ts exports a usable Scanner stub; handlers.ts serves /api/barcode/:barcode (found + not-found); existing suite still green under frozen install.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add needs_review to the frontend2 Item type</name>
  <files>frontend2/src/lib/types.ts</files>
  <behavior>
    - `Item.needs_review` is an optional boolean (backend serializes it via the by-barcode ItemResponse; absent on older payloads → undefined).
    - Existing Item consumers still type-check (purely additive).
  </behavior>
  <action>Add `needs_review?: boolean;` to the `Item` interface (lines 113-135) with a one-line comment: backend serializes NeedsReview (handler.go:659); v3.0 type was trimmed and lacked it (Pitfall 3). This is the single-writer touch of types.ts for Phase 11 — owning it here keeps 11-05's QuickActionMenu and 11-06's ScanPage from needing a serial dependency on a type edit (binding override 8). Do NOT add active-loan fields — active-loan state is derived via loansApi.byItem, not a flag.</action>
  <verify>
    <automated>cd frontend2 && grep -q "needs_review" src/lib/types.ts && bun run typecheck 2>&1 | tail -3</automated>
  </verify>
  <done>Item carries needs_review?: boolean; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → lockfile | Third-party package code enters the build |
| test mocks → suite | Mocks must not leak into production bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-SC | Tampering | bun add (scanner deps) | mitigate | Blocking human-verify checkpoint (Task above) verifies all three on npmjs.com before the non-frozen install; exact pins (no caret) prevent silent version drift; lockfile committed and frozen thereafter. |
| T-11-01 | Tampering | test mocks bleeding to prod | accept | Mocks live under src/test/** which Vite excludes from the app bundle; no app import path references them. |
</threat_model>

<verification>
- `bun install --frozen-lockfile` succeeds (lockfile consistent).
- `bun run typecheck` clean (needs_review additive).
- `bun run test -- src/test` green (mocks load).
- vite.config.ts manualChunks active (non-comment).
</verification>

<success_criteria>
Three deps pinned + lockfile committed; scanner manualChunks slot active; camera +
BarcodeDetector + Scanner-lib mocks + barcode MSW handler in place; Item.needs_review
added; full frozen install + typecheck + src/test suite green.
</success_criteria>

<output>
Create `.planning/phases/11-scan/11-01-SUMMARY.md` when done — record the exact
installed pins, the manualChunks shape, and the mock module export names so later
plans import them without re-discovery.
</output>
