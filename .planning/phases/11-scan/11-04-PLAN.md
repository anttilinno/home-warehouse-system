---
phase: 11-scan
plan: 04
type: execute
wave: 2
depends_on: ["11-02"]
files_modified:
  - frontend2/src/components/scan/BarcodeScanner.tsx
  - frontend2/src/components/scan/ScanViewfinderOverlay.tsx
  - frontend2/src/components/scan/ScanTorchToggle.tsx
  - frontend2/src/components/scan/BarcodeScanner.test.tsx
  - frontend2/src/components/scan/ScanTorchToggle.test.tsx
autonomous: true
requirements: [SCAN-01, SCAN-02, SCAN-04]
must_haves:
  truths:
    - "BarcodeScanner wraps <Scanner> with prop-driven paused (never unmount) and the 4-format subset"
    - "BarcodeScanner fires onDecode(rawValue, format) only when not paused and codes non-empty (double-fire guard)"
    - "ScanTorchToggle renders ONLY when torch is supported; three-cue OFF/ON state"
    - "The viewfinder reticle is aria-hidden corner brackets, not a full box"
  artifacts:
    - path: "frontend2/src/components/scan/BarcodeScanner.tsx"
      provides: "Retro <Scanner> wrapper, prop-driven pause, format subset, lib-managed torch"
      exports: ["BarcodeScanner"]
    - path: "frontend2/src/components/scan/ScanTorchToggle.tsx"
      provides: "44x44 retro torch button, three-cue state, supported-gated"
      exports: ["ScanTorchToggle"]
    - path: "frontend2/src/components/scan/ScanViewfinderOverlay.tsx"
      provides: "corner-bracket reticle + aim hint over the ink scrim"
      exports: ["ScanViewfinderOverlay"]
  key_links:
    - from: "frontend2/src/components/scan/BarcodeScanner.tsx"
      to: "@yudiel/react-qr-scanner Scanner"
      via: "paused + formats props"
      pattern: "paused"
---

<objective>
The camera-facing retro components: the `<Scanner>` wrapper (prop-driven pause,
format subset, lib-managed torch), the corner-bracket viewfinder overlay, and the
retro torch toggle. Disjoint from 11-03/05/07 (the result/manual/history components
are a separate plan to keep same-wave files disjoint).

Purpose: BarcodeScanner is the ONE node that must mount once and stay mounted
(binding override 1); it exposes a clean onDecode + paused contract so 11-06's
ScanPage hoists it into a persistent always-mounted sibling layer (NOT inside a
RetroTabs panel — RetroTabs unmounts inactive panels, Pitfall 1).
Output: BarcodeScanner, ScanViewfinderOverlay, ScanTorchToggle + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-scan/11-RESEARCH.md
@.planning/phases/11-scan/11-UI-SPEC.md
@.planning/phases/11-scan/11-02-SUMMARY.md

<interfaces>
<!-- Scanner lib props (RESEARCH OQ1, VERIFIED README + legacy) + retro atoms. -->

@yudiel/react-qr-scanner (pin 2.5.1):
  import { Scanner, type IDetectedBarcode } from "@yudiel/react-qr-scanner";
  Props: onScan:(IDetectedBarcode[])=>void (required), onError?, paused?,
    formats?: BarcodeFormat[] (omit=all), scanDelay? (use 200), allowMultiple? (false),
    sound? (set false — we own audio), components?:{ torch?, finder?, ... },
    constraints?: MediaTrackConstraints, styles?, classNames?.
  IDetectedBarcode: { rawValue: string; format: string; boundingBox; cornerPoints }.
  The lib mounts its OWN <video> (handles playsInline) — do NOT inject one.

Test stub: src/test/scanner-mock.ts exports a Scanner stub with a test trigger to
fire onScan([{rawValue, format}]) — vi.mock("@yudiel/react-qr-scanner") with it.

From 11-02: SUPPORTED_FORMATS (the 4-format tuple).

Retro atoms (@/components/retro): BevelButton (variant, glyph), RetroBadge.
Tokens: bg-fg-ink/55..70 scrims, bg-titlebar-butter (torch ON), 2px white
(--bevel-light) corner brackets. Touch floor: torch button min-h-[44px] min-w-[44px].
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BarcodeScanner wrapper</name>
  <files>frontend2/src/components/scan/BarcodeScanner.tsx, frontend2/src/components/scan/BarcodeScanner.test.tsx</files>
  <behavior>
    - Renders <Scanner> with formats=SUPPORTED_FORMATS, scanDelay=200, allowMultiple=false, sound=false, components={{ finder:false, torch: torchSupported && torchEnabled }}, constraints facingMode environment.
    - paused is a pass-through prop (prop-driven, NEVER unmount; binding override 1).
    - onScan handler: if codes.length===0 OR paused → return (double-fire / render-loop guard); else call onDecode(codes[0].rawValue, codes[0].format).
    - onError forwards to an onError prop (so 11-06 can render the camera-blocked state on NotAllowedError).
  </behavior>
  <action>Implement per RESEARCH Pattern 1. Props: `{ paused, onDecode, onError, torchSupported, torchEnabled }`. Wire onScan with the empty/paused guard before calling onDecode. Pass `formats={SUPPORTED_FORMATS}` (the 4-format subset — binding override 3 / SCAN-02), `paused={paused}`, `components={{ finder:false, torch: torchSupported && torchEnabled }}` (lib-managed torch apply — RESEARCH Pattern 4 approach 1), `sound={false}`, `constraints={{ facingMode:{ideal:'environment'}, width:{ideal:1280}, height:{ideal:720} }}`, container/video styles (objectFit cover, fill). Do NOT inject a <video>. Test with `vi.mock("@yudiel/react-qr-scanner", () => scannerMock)`: firing the stub's onScan with a code calls onDecode; firing while paused does NOT; empty array does NOT; assert formats prop = the 4-format subset; assert the component is NOT unmounted across a paused toggle (re-render, not remount).</action>
  <verify>
    <automated>cd frontend2 && bun install --frozen-lockfile && bun run test -- src/components/scan/BarcodeScanner 2>&1 | tail -8</automated>
  </verify>
  <done>Scanner wired with the 4-format subset + prop-driven pause + lib-managed torch; onDecode fires only when active+non-empty; no unmount on pause.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ScanTorchToggle</name>
  <files>frontend2/src/components/scan/ScanTorchToggle.tsx, frontend2/src/components/scan/ScanTorchToggle.test.tsx</files>
  <behavior>
    - When supported=false the component renders NOTHING (no disabled ghost — iOS auto-hide, SCAN-04).
    - When supported=true renders a BevelButton ≥44x44; OFF = ⚡ + cap TORCH + neutral bevel; ON = ⚡ + cap TORCH ON + butter fill + pressed bevel (three cues: color+word+glyph).
    - clicking calls onToggle.
  </behavior>
  <action>Implement per UI-SPEC Surface 1 torch spec. Props `{ supported, enabled, onToggle }`. Early-return null when !supported. BevelButton with `min-h-[44px] min-w-[44px]`, glyph ⚡, cap text TORCH / TORCH ON, butter fill (`bg-titlebar-butter` ink text) + pressed bevel when enabled. aria-pressed reflects enabled. All copy wrapped in <Trans>/t`` (Lingui — consumer owns copy). Test: supported=false → renders null; supported=true OFF → TORCH word + not pressed; ON → TORCH ON + aria-pressed; click → onToggle.</action>
  <verify>
    <automated>cd frontend2 && bun run test -- src/components/scan/ScanTorchToggle 2>&1 | tail -6</automated>
  </verify>
  <done>Torch toggle hidden when unsupported; three-cue OFF/ON; onToggle fires; ≥44x44.</done>
</task>

<task type="auto">
  <name>Task 3: ScanViewfinderOverlay</name>
  <files>frontend2/src/components/scan/ScanViewfinderOverlay.tsx</files>
  <action>Implement per UI-SPEC Surface 1 reticle spec: four 2px white (`--bevel-light`) corner brackets framing a centered ~70%-width region (NOT a full box — must not mask the code), plus the aim-hint line `Point the camera at a barcode or QR code.` on a `bg-fg-ink/55` scrim with white text (AA holds). The brackets are `aria-hidden` decoration. Static under prefers-reduced-motion (it is static anyway). Copy wrapped in <Trans>. This is a presentational overlay (no logic) — no dedicated test file; it is exercised by 11-06's ScanPage test.</action>
  <verify>
    <automated>cd frontend2 && grep -q "aria-hidden" src/components/scan/ScanViewfinderOverlay.tsx && bun run typecheck 2>&1 | tail -3</automated>
  </verify>
  <done>Corner-bracket reticle (aria-hidden) + aim hint over ink scrim; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| camera stream → decode | Device camera pixels decoded client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Info Disclosure | iOS camera re-prompt | mitigate | paused is prop-driven; component never unmounts / never track.stop() on decode (Pitfall 1/Pattern 1); test asserts no unmount on pause toggle. |
| T-11-07 | Tampering | decoded rawValue rendered | mitigate | rawValue is forwarded as a string only; React auto-escapes when rendered in 11-05 banner; never dangerouslySetInnerHTML. |
</threat_model>

<verification>
- `bun install --frozen-lockfile`.
- `bun run test -- src/components/scan/BarcodeScanner src/components/scan/ScanTorchToggle` green.
- `bun run typecheck` clean.
</verification>

<success_criteria>
BarcodeScanner mounts <Scanner> with the format subset + prop-driven pause + lib-managed
torch and a double-fire guard; torch toggle auto-hides on unsupported and shows three-cue
state; viewfinder reticle is non-masking corner brackets.
</success_criteria>

<output>
Create `.planning/phases/11-scan/11-04-SUMMARY.md` — record BarcodeScanner/ScanTorchToggle/
ScanViewfinderOverlay prop contracts so 11-06 wires the persistent layer without re-discovery.
</output>
