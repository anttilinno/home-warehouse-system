---
phase: 11-scan
plan: 05
type: execute
wave: 2
depends_on: ["11-02"]
files_modified:
  - frontend2/src/components/scan/ManualBarcodeEntry.tsx
  - frontend2/src/components/scan/ScanResultBanner.tsx
  - frontend2/src/components/scan/QuickActionMenu.tsx
  - frontend2/src/components/scan/ScanHistoryList.tsx
  - frontend2/src/components/scan/UpcSuggestionBanner.tsx
  - frontend2/src/components/scan/index.ts
  - frontend2/src/components/scan/ManualBarcodeEntry.test.tsx
  - frontend2/src/components/scan/ScanResultBanner.test.tsx
  - frontend2/src/components/scan/QuickActionMenu.test.tsx
  - frontend2/src/components/scan/ScanHistoryList.test.tsx
  - frontend2/src/components/scan/UpcSuggestionBanner.test.tsx
  - frontend2/src/features/items/hooks/useMarkReviewedItem.ts
  - frontend2/src/features/items/hooks/useMarkReviewedItem.test.ts
autonomous: true
requirements: [SCAN-05, SCAN-06, SCAN-07, SCAN-08, SCAN-09, SCAN-10, SCAN-11]
must_haves:
  truths:
    - "ScanResultBanner renders the correct one of LOADING/MATCH/NOT-FOUND/ERROR with a reduced-motion-aware blinking cursor"
    - "NOT-FOUND offers CREATE WITH CODE → /items/new?barcode=<encodeURIComponent(code)>"
    - "QuickActionMenu shows View always, hides Loan when active loan, shows Unarchive iff is_archived, Mark Reviewed iff needs_review"
    - "ManualBarcodeEntry funnels a trimmed non-empty code into onSubmit(code,'manual')"
    - "ScanHistoryList rows re-fire the funnel and CLEAR HISTORY confirms before clearing"
    - "UpcSuggestionBanner gates on /^\\d{8,14}$/ + found and emits USE / USE ALL / DISMISS"
  artifacts:
    - path: "frontend2/src/components/scan/ScanResultBanner.tsx"
      provides: "4-state banner driven by query status"
      exports: ["ScanResultBanner"]
    - path: "frontend2/src/components/scan/QuickActionMenu.tsx"
      provides: "state-adaptive quick-action overlay"
      exports: ["QuickActionMenu"]
    - path: "frontend2/src/components/scan/UpcSuggestionBanner.tsx"
      provides: "USE/USE ALL/DISMISS product-prefill banner"
      exports: ["UpcSuggestionBanner"]
    - path: "frontend2/src/features/items/hooks/useMarkReviewedItem.ts"
      provides: "PATCH {needs_review:false} mutation"
      exports: ["useMarkReviewedItem"]
  key_links:
    - from: "frontend2/src/components/scan/ScanResultBanner.tsx"
      to: "/items/new?barcode="
      via: "NOT-FOUND CREATE WITH CODE link, encodeURIComponent"
      pattern: "items/new\\?barcode="
    - from: "frontend2/src/components/scan/QuickActionMenu.tsx"
      to: "loansApi.byItem active count"
      via: "Loan visibility gate"
      pattern: "active"
---

<objective>
The result/manual/history retro components: the 4-state banner, the state-adaptive
quick-action overlay, manual entry, history list, and the UPC suggestion banner —
plus the `useMarkReviewedItem` PATCH mutation that backs the Mark-Reviewed action.
Disjoint from 11-04 (camera components) so both run same-wave.

Purpose: these are the overlay surfaces that render ON TOP of the always-mounted
scanner. All three entry points (manual submit, history tap, banner CTAs) speak the
funnel contract from 11-03. The backend PATCH update accepts `needs_review`
(handler.go:419 patchOrCurrent + handler.go:765 input field) so Mark Reviewed IS in
scope (binding override 8 — items update CAN clear needs_review).
Output: ManualBarcodeEntry, ScanResultBanner, QuickActionMenu, ScanHistoryList,
UpcSuggestionBanner, barrel, useMarkReviewedItem + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-scan/11-RESEARCH.md
@.planning/phases/11-scan/11-UI-SPEC.md
@.planning/phases/11-scan/11-02-SUMMARY.md

<interfaces>
<!-- Existing surfaces + atoms these components consume. -->

Item (frontend2/src/lib/types.ts): id, name, barcode?, is_archived?,
  needs_review? (added in 11-01), short_code, ...

loansApi (frontend2/src/lib/api/loans.ts):
  byItem(wsId, itemId): Promise<{ active: Loan[]; history: Loan[] }>
  // active.length > 0 → item is on an active loan (RESEARCH OQ7 / A4).

itemsApi (frontend2/src/lib/api/items.ts):
  restore(wsId, id): Promise<void>          // Unarchive
  update(wsId, id, data): Promise<Item>     // PATCH; { needs_review: false } clears it.

Retro atoms (@/components/retro): Window, BevelButton (variant primary/mint/danger/neutral,
  glyph), RetroInput (mono), RetroBadge (variant info), StatusPill (variant ok/warn/info/danger),
  RetroDialog (focus-trap/ESC/scrim — for the quick-action overlay so the camera stays mounted),
  RetroConfirmDialog (pink/danger — clear-history confirm), RetroEmptyState (empty history),
  retroToast. RetroTable hover/stripe idiom for history rows.

Query keys (CONTEXT 4): ["item-by-barcode", wsId, code], ["barcode", wsId, code],
  ["loans", wsId, "by-item", id].

Banner state map: pending→LOADING(info,◌), Item→MATCH(ok,✓,mint), null→NOT-FOUND(warn,✕,butter),
  error→ERROR(danger,✕). Blinking cursor reuses the status-blink steps() convention with a
  prefers-reduced-motion `animation:none` fallback (hold solid).

UPC gate: /^\d{8,14}$/ before calling barcodeApi.lookup; found:false → suppress banner.
NOT-FOUND CTA: /items/new?barcode=<encodeURIComponent(code)>.
USE ALL extends the create URL with &name=&brand= (11-06 wires ItemFormPage to READ them).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ScanResultBanner + ManualBarcodeEntry + ScanHistoryList</name>
  <files>frontend2/src/components/scan/ScanResultBanner.tsx, frontend2/src/components/scan/ManualBarcodeEntry.tsx, frontend2/src/components/scan/ScanHistoryList.tsx, frontend2/src/components/scan/ScanResultBanner.test.tsx, frontend2/src/components/scan/ManualBarcodeEntry.test.tsx, frontend2/src/components/scan/ScanHistoryList.test.tsx</files>
  <behavior>
    - ScanResultBanner: given a status prop (loading|match|not-found|error) + code + optional item, renders the matching state with three cues (StatusPill variant + glyph + Silkscreen word), code in mono; LOADING shows a blinking cursor that holds SOLID under prefers-reduced-motion; MATCH shows item.name + a ▸ ACTIONS button (calls onOpenActions); NOT-FOUND shows ⊕ CREATE WITH CODE linking to /items/new?barcode=<encodeURIComponent(code)>; ERROR shows ↻ TRY AGAIN (calls onRetry).
    - ManualBarcodeEntry: a <form> with a mono RetroInput (label ENTER CODE) + LOOK UP CODE BevelButton(primary); submit trims, no-ops when blank (button disabled while blank), calls onSubmit(code,'manual') and clears.
    - ScanHistoryList: renders entries as full-width <button> rows (RetroTable hover idiom), mono code + relative time + ▸; row click → onSelect(code) (re-fire); a CLEAR HISTORY danger button (disabled when empty) opens RetroConfirmDialog (pink) → onClear; empty → RetroEmptyState (◇ NO SCANS YET).
  </behavior>
  <action>Implement all three per UI-SPEC Surfaces 5/3/4. ScanResultBanner is PRESENTATIONAL — it takes status/code/item/handlers, does NOT own the query (11-03's useScanResolve owns it). Add the `.scan-cursor--blink` keyframe in globals.css next to status-blink (steps(1,end) + `@media (prefers-reduced-motion: reduce){ animation:none }`) — this is the ONE globals.css touch for the banner; declare it. encodeURIComponent the code in the NOT-FOUND link (SCAN-09 / Pitfall 5). All copy via <Trans>/t``. Tests: banner renders each of the 4 states with correct word+glyph+CTA and the NOT-FOUND href; reduced-motion cursor holds solid (assert no animation class or static variant); manual entry trims+no-ops-blank+funnels 'manual'; history rows re-fire onSelect, CLEAR confirms then clears, empty shows empty state. (SCAN-05/06/07/08/09 test-map.)</action>
  <verify>
    <automated>cd frontend2 && bun install --frozen-lockfile && bun run test -- src/components/scan/ScanResultBanner src/components/scan/ManualBarcodeEntry src/components/scan/ScanHistoryList 2>&1 | tail -10</automated>
  </verify>
  <done>Banner = 4 states w/ reduced-motion cursor + encoded NOT-FOUND link; manual funnels 'manual'; history re-fires + confirm-clears + empty state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: QuickActionMenu + useMarkReviewedItem</name>
  <files>frontend2/src/components/scan/QuickActionMenu.tsx, frontend2/src/features/items/hooks/useMarkReviewedItem.ts, frontend2/src/components/scan/QuickActionMenu.test.tsx, frontend2/src/features/items/hooks/useMarkReviewedItem.test.ts</files>
  <behavior>
    - useMarkReviewedItem: a useMutation calling itemsApi.update(wsId, id, { needs_review: false }); invalidates the item + by-barcode queries; toast on success.
    - QuickActionMenu (rendered as a RetroDialog so the camera stays mounted): VIEW ITEM always (→ /items/:id); LOAN visible only when NOT is_archived AND NOT hasActiveLoan (→ /loans/new?itemId=<encodeURIComponent(id)>); UNARCHIVE only when is_archived (→ itemsApi.restore); MARK REVIEWED only when needs_review (→ useMarkReviewedItem); BACK TO SCAN always (close + resume).
    - hasActiveLoan derives from loansApi.byItem(wsId, item.id).active.length>0; while that query loads, LOAN is HIDDEN (fail-safe — never offer a duplicate loan, UI-SPEC Surface 6).
  </behavior>
  <action>Implement per UI-SPEC Surface 6 + RESEARCH quick-action gating snippet. QuickActionMenu props `{ item, onClose }`; fire `useQuery(["loans", wsId, "by-item", item.id], () => loansApi.byItem(wsId, item.id))` on open; compute `hasActiveLoan = (query.data?.active.length ?? 0) > 0 || query.isPending` (pending → treat as has-loan so LOAN hides until known). Build the action list with the gates above. Loan target `/loans/new?itemId=${encodeURIComponent(item.id)}` (inventory_id contract — Pitfall 2; the form auto-selects the single matching entry). MARK REVIEWED calls useMarkReviewedItem; on success toast + close. Render inside RetroDialog (focus-trap/ESC/scrim — keeps camera mounted underneath). useMarkReviewedItem: useMutation → itemsApi.update(wsId,id,{needs_review:false}) → invalidate ["items",...] + ["item-by-barcode",...] + retroToast.success. Backend confirmed: PATCH accepts needs_review (handler.go:419/765) — this is NOT a dead button. Tests: actions list for {plain}, {archived}, {needs_review}, {active loan} combinations; LOAN hidden while byItem pending; MARK REVIEWED fires the PATCH with {needs_review:false}; mutation invalidates + toasts. (SCAN-11 test-map.)</action>
  <verify>
    <automated>cd frontend2 && bun run test -- src/components/scan/QuickActionMenu src/features/items/hooks/useMarkReviewedItem 2>&1 | tail -10</automated>
  </verify>
  <done>Quick-action gating correct for all 4 state combinations; LOAN fail-safe-hidden while loans load; Mark Reviewed PATCHes needs_review:false + invalidates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: UpcSuggestionBanner + scan barrel</name>
  <files>frontend2/src/components/scan/UpcSuggestionBanner.tsx, frontend2/src/components/scan/index.ts, frontend2/src/components/scan/UpcSuggestionBanner.test.tsx</files>
  <behavior>
    - Renders nothing unless the code matches /^\d{8,14}$/.
    - When matched, fires barcodeApi.lookup via useQuery(["barcode", wsId, code]); when product.found, renders the info banner with `{name} — {brand}` + a `via {source}` chip and three buttons.
    - found:false (or non-numeric) → renders nothing (suppress, never a dead banner).
    - USE → onUse({name}); USE ALL → onUse({name, brand}); DISMISS → onDismiss.
  </behavior>
  <action>Implement per UI-SPEC SCAN-10 banner + RESEARCH Pattern 6. Props `{ code, onUse, onDismiss }` (this banner lives on the item-create form, NOT /scan — 11-06 mounts it in ItemFormPage). Gate on `/^\d{8,14}$/` BEFORE the query (enabled flag); query `["barcode", wsId, code]` → barcodeApi.lookup; suppress on !found. USE = name only; USE ALL = name + brand; DISMISS = hide. BevelButton: USE ALL primary, USE/DISMISS neutral; RetroBadge info PRODUCT FOUND. Copy via <Trans>. Create the components/scan/index.ts barrel exporting BarcodeScanner, ScanViewfinderOverlay, ScanTorchToggle (from 11-04) AND ManualBarcodeEntry, ScanResultBanner, QuickActionMenu, ScanHistoryList, UpcSuggestionBanner (this plan). NOTE: 11-04 also writes its three components but NOT the barrel — this plan owns components/scan/index.ts as the single barrel writer; reference the 11-04 component names from its SUMMARY. Test: non-numeric/short code → null; matched + found → banner with name/brand + 3 buttons; found:false → null; USE emits {name}; USE ALL emits {name,brand}; DISMISS emits.</action>
  <verify>
    <automated>cd frontend2 && bun run test -- src/components/scan/UpcSuggestionBanner 2>&1 | tail -6 && bun run typecheck 2>&1 | tail -3</automated>
  </verify>
  <done>UPC banner gates on /^\d{8,14}$/ + found, emits USE/USE ALL/DISMISS, suppresses otherwise; scan barrel exports all 8 components; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanned code → create/loan URLs | User-controlled code in navigation targets |
| product metadata → form prefill | External (OpenFoodFacts) data rendered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Tampering | NOT-FOUND / Loan nav targets | mitigate | encodeURIComponent on code (items/new?barcode=) and id (loans/new?itemId=) — Pitfall 5; tests assert encoded hrefs. |
| T-11-09 | Tampering | product name/brand rendered | mitigate | Rendered as React text (auto-escaped); never dangerouslySetInnerHTML; USE/USE ALL pass values through controlled inputs (11-06). |
| T-11-10 | EoP | dead Mark-Reviewed button | mitigate | Backend PATCH accepts needs_review (verified handler.go:419/765) — action is real; gated on needs_review so it never shows when not applicable. |
</threat_model>

<verification>
- `bun install --frozen-lockfile`.
- `bun run test -- src/components/scan src/features/items/hooks/useMarkReviewedItem` green.
- `bun run typecheck` clean.
</verification>

<success_criteria>
4-state banner (reduced-motion cursor) + encoded NOT-FOUND link; state-adaptive quick-action
overlay with fail-safe Loan gating + working Mark Reviewed; manual + history funnels; UPC
suggestion banner gated on /^\d{8,14}$/ + found; scan barrel complete.
</success_criteria>

<output>
Create `.planning/phases/11-scan/11-05-SUMMARY.md` — record each component's prop contract +
the barrel export list + useMarkReviewedItem signature for 11-06's ScanPage + ItemFormPage wiring.
</output>
