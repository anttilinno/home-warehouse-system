---
phase: 11-scan
plan: 07
type: execute
wave: 2
depends_on: ["11-02"]
files_modified:
  - frontend2/src/features/scan/ClaimPage.tsx
  - frontend2/src/features/scan/ClaimPage.test.tsx
autonomous: true
requirements: [SCAN-12]
must_haves:
  truths:
    - "/claim/:code resolves :code via lookupByBarcode: MATCH → item detail/quick actions; 404 → create-item-with-barcode (PORT LEGACY create-entity)"
    - "ClaimPage is login-gated content (rendered under RequireAuth via the route 11-06 adds)"
    - "Three load states: RESOLVING / RESOLVED / UNRESOLVABLE; no new backend endpoint"
  artifacts:
    - path: "frontend2/src/features/scan/ClaimPage.tsx"
      provides: "The /claim/:code create-entity claim flow (parity)"
      exports: ["ClaimPage"]
  key_links:
    - from: "frontend2/src/features/scan/ClaimPage.tsx"
      to: "itemsApi.lookupByBarcode"
      via: "resolve :code"
      pattern: "lookupByBarcode"
    - from: "frontend2/src/features/scan/ClaimPage.tsx"
      to: "/items/new?barcode="
      via: "404 create-entity, encodeURIComponent"
      pattern: "items/new\\?barcode="
---

<objective>
The `/claim/:code` page component — a PORT of the shipped legacy create-entity claim
flow (USER DECISION, binding override 9: NO new backend, NO claim-as-loan). It reads
`:code`, resolves it via the existing `itemsApi.lookupByBarcode`, and either routes to
the matched item (with quick actions) or offers create-item-with-this-barcode for an
unmatched code. The route itself is added by 11-06 (single-writer routes/index.tsx) —
this plan only builds the page component so it can be imported there.

Purpose: SCAN-12 parity. The CONTEXT/roadmap "claim-as-loan" text is superseded by the
parity-true USER DECISION (RESEARCH OQ6 / CONTEXT resolved OQ). There is NO JSON resolve
endpoint and the legacy /claim/[code] is a create-new-ENTITY page — this mirrors it.
Output: ClaimPage.tsx + test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-scan/11-CONTEXT.md
@.planning/phases/11-scan/11-RESEARCH.md
@.planning/phases/11-scan/11-UI-SPEC.md
@.planning/phases/11-scan/11-02-SUMMARY.md

<interfaces>
<!-- Existing surfaces ClaimPage consumes. SCAN-12 = PORT LEGACY create-entity. -->

itemsApi.lookupByBarcode(wsId, code): Promise<Item | null>
  // 404 → null (UNRESOLVABLE → create-entity); Item → MATCH.

useWorkspace(): { currentWorkspaceId }
react-router: useParams() → { code }; useNavigate(); Link.

Retro atoms (@/components/retro): Window (titlebar mint — positive action),
  RetroEmptyState (◎ CODE NOT FOUND + BACK TO SCAN → /scan), BevelButton, retroToast.

USER DECISION (binding override 9): SCAN-12 = PORT LEGACY create-entity:
  /claim/:code (login required) → lookupByBarcode(code):
    MATCH  → item detail / quick actions (route to /items/:id, or reuse QuickActionMenu)
    404    → create-item form with the barcode prefilled (reuse /items/new?barcode=)
  Do NOT build a claim-as-loan flow or a new resolve endpoint. encodeURIComponent the code.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ClaimPage create-entity flow</name>
  <files>frontend2/src/features/scan/ClaimPage.tsx, frontend2/src/features/scan/ClaimPage.test.tsx</files>
  <behavior>
    - Reads :code from the route; while resolving shows a RESOLVING state (Resolving code… line, reuse .retro-progress).
    - lookupByBarcode(code) MATCH → routes to the matched item (navigate to /items/:id, or render the item with a VIEW ITEM action) — RESOLVED state.
    - 404 → UNRESOLVABLE create-entity: a Window (mint) offering CREATE ITEM WITH THIS CODE → /items/new?barcode=<encodeURIComponent(code)> (parity create-entity), plus a RetroEmptyState fallback path (◎ CODE NOT FOUND / BACK TO SCAN → /scan) for genuinely empty cases per UI-SPEC.
    - On lookup error → persistent retroToast.error + an in-page role="alert" danger banner.
    - encodeURIComponent the code on every path/URL use (Pitfall 5).
  </behavior>
  <action>Implement ClaimPage per the USER DECISION (binding override 9 — PORT LEGACY create-entity, NOT claim-as-loan). useParams → code; useQuery(["item-by-barcode", wsId, code]) → itemsApi.lookupByBarcode. Three states: RESOLVING (.retro-progress), RESOLVED (Item found → navigate /items/:id OR render item summary + VIEW ITEM), UNRESOLVABLE (404/null → create-entity: BevelButton CREATE ITEM WITH THIS CODE → /items/new?barcode=${encodeURIComponent(code)}). Window titlebar mint (positive). Error → retroToast.error (Infinity default) + role="alert" banner (ItemFormPage root-error idiom). Login gating is enforced by the route 11-06 adds under RequireAuth — do NOT re-implement auth here. Do NOT call loansApi.create, do NOT add any backend route. Reuse QuickActionMenu (from 11-05) for the MATCH case if a richer surface is desired, else a simple VIEW ITEM link. Copy via <Trans>/t``. Test (MemoryRouter at /claim/<code> + MSW): MATCH code → navigates / shows the item; not-found code → CREATE ITEM WITH THIS CODE links to /items/new?barcode=<encoded>; error → alert + toast; code is encoded in the create link.</action>
  <verify>
    <automated>cd frontend2 && bun install --frozen-lockfile && grep -q "lookupByBarcode" src/features/scan/ClaimPage.tsx && grep -q "encodeURIComponent" src/features/scan/ClaimPage.tsx && bun run test -- src/features/scan/ClaimPage 2>&1 | tail -8</automated>
  </verify>
  <done>ClaimPage resolves :code via lookupByBarcode; MATCH → item; 404 → create-entity with encoded barcode prefill; error handled; NO new backend / NO claim-as-loan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| :code path param → lookup/create URLs | User-controlled route param resolved + interpolated |
| unauthenticated visitor → /claim/:code | Must be login-gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Tampering | :code in lookup + create link | mitigate | lookupByBarcode encodeURIComponents the code; ClaimPage encodeURIComponents it again in the /items/new?barcode= link (Pitfall 5); test asserts encoding. |
| T-11-12 | EoP | unauth access to /claim/:code | mitigate | Route is registered under the RequireAuth AppShell branch (11-06); unauth → bounced to /login?next= (existing RequireAuth contract). |
</threat_model>

<verification>
- `bun install --frozen-lockfile`.
- `bun run test -- src/features/scan/ClaimPage` green.
- `bun run typecheck` clean.
</verification>

<success_criteria>
/claim/:code (component) resolves via lookupByBarcode, routes a MATCH to the item, offers
create-entity-with-barcode on 404, handles errors — parity port, no new backend, no loan flow.
</success_criteria>

<output>
Create `.planning/phases/11-scan/11-07-SUMMARY.md` — record the ClaimPage export +
its route expectation (`/claim/:code` under RequireAuth) so 11-06 wires the route.
</output>
