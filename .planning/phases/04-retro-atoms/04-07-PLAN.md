---
phase: 04-retro-atoms
plan: 07
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03", "04-04", "04-05", "04-06"]
files_modified:
  - frontend2/src/routes/demo/DemoPage.tsx
  - frontend2/src/routes/demo/DemoPage.test.tsx
  - frontend2/src/routes/index.tsx
  - frontend2/src/components/layout/Sidebar.tsx
  - frontend2/src/components/retro/overlay/escStack.test.tsx
autonomous: false
requirements: [TUI-02, TUI-03, TUI-04, TUI-06, ATOM-FB-01, ATOM-FB-02, ATOM-FB-03, ATOM-FB-04]
user_setup: []

must_haves:
  truths:
    - "/demo renders every atom family (form, overlay, feedback, data, filter) inside AppShell, registered as an unlisted/DEV-gated route + Sidebar SYSTEM link"
    - "The escStack integration test proves a stacked dialog → drawer → menu pops the TOPMOST overlay first on ESC, and never fires logout while any overlay is open (TUI-02 composition proof)"
    - "Bulk-action chips surface in the Bottombar when a /demo RetroTable selection is non-empty (Success Criterion 5)"
  artifacts:
    - path: "frontend2/src/routes/demo/DemoPage.tsx"
      provides: "Visual-review surface rendering every Phase 4 atom (Success Criterion 1)"
      min_lines: 80
    - path: "frontend2/src/components/retro/overlay/escStack.test.tsx"
      provides: "TUI-02 integration proof across dialog→drawer→menu stack"
      exports: []
  key_links:
    - from: "frontend2/src/routes/index.tsx"
      to: "DemoPage"
      via: "child route under the AppShell layout route"
      pattern: "demo"
    - from: "frontend2/src/routes/demo/DemoPage.tsx"
      to: "useTableSelection + useShortcuts"
      via: "bulk-action group registered for Bottombar chips"
      pattern: "useShortcuts"
---

<objective>
Land the `/demo` review surface (Success Criterion 1 — renders every atom family inside AppShell) and the TUI-02 composition proof: an `escStack` integration test that stacks a real dialog → drawer → menu and asserts ESC pops the TOPMOST overlay first while logout never fires. Wire a /demo RetroTable selection to `useShortcuts("bulk-actions", …)` so Bottombar chips appear on selection (Success Criterion 5 proven end-to-end). Register `/demo` as an unlisted/DEV-gated route under AppShell with a Sidebar SYSTEM link.

Purpose: This is the integration playground where the composition of all earlier-plan atoms is exercised against the live Phase 3 modal-stack + shortcuts providers. Phase 3 unit-tested the providers; this plan proves the composition (TUI-02 across heterogeneous overlay types — the one test Phase 3 could not write).
Output: `routes/demo/DemoPage.tsx` + smoke test, route + Sidebar registration, `escStack.test.tsx` integration spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-retro-atoms/04-UI-SPEC.md
@.planning/phases/04-retro-atoms/04-RESEARCH.md
@.planning/phases/04-retro-atoms/04-01-SUMMARY.md
@.planning/phases/04-retro-atoms/04-02-SUMMARY.md
@.planning/phases/04-retro-atoms/04-03-SUMMARY.md
@.planning/phases/04-retro-atoms/04-04-SUMMARY.md
@.planning/phases/04-retro-atoms/04-05-SUMMARY.md
@.planning/phases/04-retro-atoms/04-06-SUMMARY.md

<interfaces>
<!-- Route + Sidebar structure to extend (verified). -->

From frontend2/src/routes/index.tsx — the AppShell LAYOUT route; add /demo as a child:
```typescript
<Route element={<RequireAuth><AppShell /></RequireAuth>}>
  <Route index element={<DashboardPage />} />
  {/* add: <Route path="demo" element={<DemoPage />} /> — gate behind import.meta.env.DEV if desired */}
</Route>
```

From frontend2/src/components/layout/Sidebar.tsx — the SYSTEM group has NavItems (Scan, Settings); add a DEV-gated demo link:
```typescript
// <NavItem glyph="…" label={<Trans>Demo</Trans>} to="/demo" />  // under SYSTEM, DEV-only
```

From frontend2/src/components/shortcuts/useShortcuts.ts — register the bulk-action group (memoize bindings, Pitfall 3):
```typescript
useShortcuts("bulk-actions", bulkActions); // empty array → nothing registered → no chips
```

From frontend2/src/components/layout/F1HelpDialog.test.tsx — provider harness shape for the escStack test:
`<I18nProvider><ShortcutsProvider><ModalStackProvider>…</ModalStackProvider></ShortcutsProvider></I18nProvider>`,
plus the LogoutOnEscape sentinel that must stay silent while any overlay is open.

All atoms import from the single `@/components/retro` barrel (RetroDialog, RetroConfirmDialog, Popover, RetroSelect, RetroCombobox, RetroTextarea, RetroCheckbox, RetroFileInput, RetroFormField, RetroToaster/retroToast, RetroStatusDot, StatusPill, RetroEmptyState, RetroTable, useTableSelection, RetroPagination, RetroTabs, FilterBar, FilterPopover, BulkActionBar, SavedFilters).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DemoPage (every atom family) + route + Sidebar registration</name>
  <files>frontend2/src/routes/demo/DemoPage.tsx, frontend2/src/routes/demo/DemoPage.test.tsx, frontend2/src/routes/index.tsx, frontend2/src/components/layout/Sidebar.tsx</files>
  <read_first>
    - .planning/phases/04-retro-atoms/04-UI-SPEC.md § "/demo Page Layout" (section order, titlebar moods, captions) + § "Open Questions RESOLVED" (unlisted/DEV-gated)
    - frontend2/src/routes/index.tsx (route registration site)
    - frontend2/src/components/layout/Sidebar.tsx (SYSTEM group NavItem pattern)
  </read_first>
  <action>
    Build `DemoPage` per UI-SPEC § /demo Page Layout: one `Window` section per atom family with the semantic titlebar mood (FORM ATOMS blue, OVERLAY ATOMS blue, FEEDBACK ATOMS mint, DATA ATOMS mint, FILTER ATOMS butter). Render every atom with its key states and a 14px `text-fg-muted` caption naming it:
    - FORM: RetroSelect, RetroCombobox, RetroTextarea, RetroCheckbox (all 3 states incl. indeterminate), RetroFileInput, RetroFormField (default/hint/error).
    - OVERLAY: buttons launching RetroDialog (each titlebar variant), RetroConfirmDialog, a Menu/Popover.
    - FEEDBACK: toast launchers (all 4 types via retroToast), RetroEmptyState (no-data + filtered), RetroStatusDot (live/idle/error), StatusPills (OK/WARN/INFO/DANGER).
    - DATA: a RetroTable wired to useTableSelection (range-select + header select-all indeterminate + tabular-nums numeric column), RetroPagination, RetroTabs.
    - FILTER: FilterBar, FilterPopover, BulkActionBar, SavedFilters.
    Mount `RetroToaster` on the page (or rely on AppShell once Phase 6 mounts it — for the demo, mount it locally so toasts render here). Section rhythm `gap-sp-6`, within-section `gap-sp-4`.
    Register `/demo` as a child route under the AppShell layout route in routes/index.tsx (gate behind `import.meta.env.DEV` so it does not ship as a user route). Add a DEV-gated `<NavItem to="/demo" label={<Trans>Demo</Trans>}>` under the Sidebar SYSTEM group.
    DemoPage.test.tsx: a smoke render test asserting each family section heading renders and at least one atom from each family mounts without crashing (render inside the AppShell/provider harness or a minimal provider wrapper).
  </action>
  <verify>
    <automated>cd frontend2 && bun run test src/routes/demo/DemoPage.test.tsx && bun run lint:tsc</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test src/routes/demo/DemoPage.test.tsx` passes (all five family sections render).
    - `grep -c "path=\"demo\"\|path='demo'" frontend2/src/routes/index.tsx` ≥ 1.
    - `grep -c "/demo" frontend2/src/components/layout/Sidebar.tsx` ≥ 1.
    - `grep -c "import.meta.env.DEV" frontend2/src/routes/index.tsx frontend2/src/components/layout/Sidebar.tsx` ≥ 1 (DEV-gated).
    - `bun run lint:tsc` exits 0.
  </acceptance_criteria>
  <done>/demo renders every atom family, registered as an unlisted/DEV-gated route + Sidebar link (Success Criterion 1).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: escStack TUI-02 integration test (dialog → drawer → menu) + bulk-action chip proof</name>
  <files>frontend2/src/components/retro/overlay/escStack.test.tsx</files>
  <read_first>
    - frontend2/src/components/layout/F1HelpDialog.test.tsx (LogoutOnEscape sentinel + provider harness to model on)
    - frontend2/src/components/modal/ModalStack.test.tsx (the provider-level stack tests Phase 3 already wrote — this plan proves the COMPOSITION, not the provider)
    - .planning/phases/04-retro-atoms/04-UI-SPEC.md § "Accessibility → ESC discipline"
  </read_first>
  <behavior>
    - Test: mount a real RetroDialog, then open a drawer (MobileDrawer or a second RetroDialog standing in for the drawer), then open a Popover menu — all stacked open simultaneously inside one ModalStackProvider. Pressing ESC pops the MENU first (topmost), a second ESC pops the DRAWER, a third pops the DIALOG — topmost-first ordering asserted at each step.
    - Test: a LogoutOnEscape sentinel listener is mounted; assert it NEVER fires while any overlay is open (logout unreachable on bare ESC — TUI-02 "never logs out while a modal is open").
    - Test: after all overlays close, the stack is empty (balanced push/pop) and a subsequent ESC reaches no overlay (no crash, no double-pop).
    - Test (Success Criterion 5 proof): render a RetroTable + useTableSelection inside the ShortcutsProvider, select rows so a `useShortcuts("bulk-actions", …)` group registers, assert the merged shortcuts context exposes the bulk-action group (chips would surface in the Bottombar); clearing selection unregisters it.
  </behavior>
  <action>
    Write `escStack.test.tsx` as the TUI-02 composition proof. Use the F1HelpDialog.test harness shape (`I18nProvider > ShortcutsProvider > ModalStackProvider`). Build a small host component that opens three heterogeneous overlays (RetroDialog as the dialog, a second RetroDialog or MobileDrawer as the drawer, a Popover as the menu) in sequence, and a LogoutOnEscape sentinel. Drive ESC with `userEvent.keyboard("{Escape}")` and assert topmost-first pop ordering via which overlay's content disappears at each step, plus the sentinel silence. Then, in a separate describe block, mount a RetroTable + useTableSelection, select a range, and assert the bulk-actions shortcut group is present in the merged shortcuts context (read it via a probe component using `useShortcutsContext`), and that clearing selection removes it. Memoize the bulk-action bindings (Pitfall 3) so the register effect does not churn.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test src/components/retro/overlay/escStack.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test src/components/retro/overlay/escStack.test.tsx` passes: topmost-first ESC pop across dialog→drawer→menu, LogoutOnEscape never fires while open, balanced stack after close, bulk-action group registers/unregisters with selection.
    - `grep -c "Escape" frontend2/src/components/retro/overlay/escStack.test.tsx` ≥ 1.
    - `grep -c "bulk-actions" frontend2/src/components/retro/overlay/escStack.test.tsx` ≥ 1.
  </acceptance_criteria>
  <done>TUI-02 composition proven across stacked heterogeneous overlays; Success Criterion 5 bulk-chip surfacing proven.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: /demo visual review vs sketches 006-008</name>
  <what-built>
    The /demo page now renders every Phase 4 atom family with retro-os pastel chrome. Automated specs cover behavior + ARIA; the visual fidelity (chrome, spacing, fonts, toast skin radius-0, blink feel, combobox keyboard feel) is the one thing RTL/jsdom cannot assert.
  </what-built>
  <how-to-verify>
    1. Start the dev stack: `cd frontend2 && bun run dev`; log in; navigate to `/demo`.
    2. Eyeball each family section against sketches 006-008 (`.planning/sketches/`): window chrome, pinstriped titlebars, bevels, Silkscreen ≥16px titles only, Plex body/data, radius-0 everywhere except 2px chips/pills.
    3. Toasts: fire all 4 types — confirm mini-Window chrome with NO rounded corners, semantic titlebar stripes (mint/blue/butter/pink), stacking, hover-pause, and that the danger/ERROR toast does NOT auto-dismiss.
    4. StatusDot: confirm the live dot blinks hard (step-end) and goes solid under OS "reduce motion".
    5. Combobox: arrow/type/select keyboard flow feels right; selected option shows ✓; ESC closes.
    6. Table: Shift+Click range-selects; header checkbox shows the indeterminate dash on partial selection; numeric column is tabular-aligned.
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keyboard → stacked overlays | ESC must pop topmost-first and never reach logout |
| /demo route | a DEV-only review surface, not a shipped user route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-ESC | Elevation/DoS | stacked overlay ESC composition | mitigate | escStack integration test proves topmost-first pop + logout-unreachable across heterogeneous overlays (TUI-02 composition proof) |
| T-04-07-DEMO | Information disclosure | /demo route exposure | accept | DEV-gated route + Sidebar link (import.meta.env.DEV); presentational atoms only, no real data or endpoints |
| T-04-07-XSS | Tampering | demo atom labels | accept | All demo content is static/JSX text, auto-escaped; no dangerouslySetInnerHTML |
</threat_model>

<verification>
- `cd frontend2 && bun run test` (FULL suite) green.
- `cd frontend2 && bun run build && bun run lint:imports && bun run lint:tsc` all exit 0 (phase gate per 04-VALIDATION.md).
- Visual /demo review approved (Task 3).
</verification>

<success_criteria>
- /demo renders every atom family (Success Criterion 1), DEV-gated route + Sidebar link.
- escStack proves TUI-02 across dialog→drawer→menu; logout never fires while open (Success Criterion 2).
- Bulk-action chips surface in the Bottombar on selection (Success Criterion 5).
- Full suite + build + lint green.
</success_criteria>

<output>
Create `.planning/phases/04-retro-atoms/04-07-SUMMARY.md` when done. Record the final full-suite test count and the cumulative sonner gzip bundle delta vs the Phase 3 baseline.
</output>
