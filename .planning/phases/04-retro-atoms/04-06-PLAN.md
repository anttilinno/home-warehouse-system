---
phase: 04-retro-atoms
plan: 06
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-04"]
files_modified:
  - frontend2/src/components/retro/filters/useSavedFilters.ts
  - frontend2/src/components/retro/filters/useSavedFilters.test.ts
  - frontend2/src/components/retro/filters/FilterBar.tsx
  - frontend2/src/components/retro/filters/FilterBar.test.tsx
  - frontend2/src/components/retro/filters/FilterPopover.tsx
  - frontend2/src/components/retro/filters/FilterPopover.test.tsx
  - frontend2/src/components/retro/filters/BulkActionBar.tsx
  - frontend2/src/components/retro/filters/BulkActionBar.test.tsx
  - frontend2/src/components/retro/filters/SavedFilters.tsx
  - frontend2/src/components/retro/filters/SavedFilters.test.tsx
  - frontend2/src/components/retro/filters/index.ts
autonomous: true
requirements: [ATOM-FB-01, ATOM-FB-02, ATOM-FB-03, ATOM-FB-04]
user_setup: []

must_haves:
  truths:
    - "FilterBar renders a recessed toolbar: search input + per-facet ▾ triggers + count + primary CTA + removable active-filter chips with CLEAR ALL"
    - "FilterPopover anchors the Plan 04-01 Popover under a FilterBar trigger with a RetroCheckbox facet checklist; ESC closes via useModalStack"
    - "BulkActionBar renders the inline mobile selection surface ({n} SELECTED + actions + ✕ CLEAR); destructive actions route through RetroConfirmDialog"
    - "useSavedFilters persists presets to localStorage (saveFilter/applyFilter/setAsDefault/deleteFilter) with try/catch JSON.parse tolerating malformed data by resetting to []"
    - "SavedFilters renders preset chips + a ▾ PRESETS menu with SAVE CURRENT… and per-preset delete (→ RetroConfirmDialog)"
  artifacts:
    - path: "frontend2/src/components/retro/filters/FilterBar.tsx"
      provides: "ATOM-FB-01 recessed filter toolbar"
      exports: ["FilterBar"]
    - path: "frontend2/src/components/retro/filters/FilterPopover.tsx"
      provides: "ATOM-FB-02 facet checklist popover"
      exports: ["FilterPopover"]
    - path: "frontend2/src/components/retro/filters/BulkActionBar.tsx"
      provides: "ATOM-FB-03 inline mobile bulk-action surface"
      exports: ["BulkActionBar"]
    - path: "frontend2/src/components/retro/filters/useSavedFilters.ts"
      provides: "ATOM-FB-04 localStorage preset hook"
      exports: ["useSavedFilters", "SavedFilter"]
    - path: "frontend2/src/components/retro/filters/SavedFilters.tsx"
      provides: "ATOM-FB-04 preset chips + menu"
      exports: ["SavedFilters"]
  key_links:
    - from: "frontend2/src/components/retro/filters/useSavedFilters.ts"
      to: "localStorage"
      via: "try/catch JSON.parse, reset to [] on malformed"
      pattern: "JSON.parse"
    - from: "frontend2/src/components/retro/filters/FilterPopover.tsx"
      to: "retro/overlay/Popover"
      via: "anchored facet popover via useModalStack"
      pattern: "Popover"
---

<objective>
Build the four filter atoms (ATOM-FB-01..04) once, here, so Phases 7/8/14 consume them instead of three ad-hoc copies: `FilterBar` (recessed toolbar + active-filter chips), `FilterPopover` (facet checklist on the Plan 04-01 Popover), `BulkActionBar` (inline mobile selection surface — desktop bulk actions surface via the Bottombar per the locked decision, NOT a second desktop bar), and `SavedFilters` (preset chips + ▾ PRESETS menu) backed by `useSavedFilters` (localStorage, ported from the legacy contract with `any`→`unknown`).

Purpose: These are the last atoms; they compose the overlay (Popover, RetroConfirmDialog), form (RetroCheckbox, RetroInput), and feedback (RetroBadge chips) primitives from earlier plans. SavedFilters persistence is localStorage-only this phase (server prefs deferred to Phase 12); the contract is forward-compatible. Security: SavedFilters names/values render as text nodes (no dangerouslySetInnerHTML), and the localStorage read is try/catch-wrapped (Pitfall — malformed payload resets to []).
Output: `retro/filters/{useSavedFilters.ts, FilterBar, FilterPopover, BulkActionBar, SavedFilters}.tsx` + tests + subdir barrel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-retro-atoms/04-UI-SPEC.md
@.planning/phases/04-retro-atoms/04-RESEARCH.md
@.planning/phases/04-retro-atoms/04-01-SUMMARY.md
@.planning/phases/04-retro-atoms/04-02-SUMMARY.md
@.planning/phases/04-retro-atoms/04-04-SUMMARY.md

<interfaces>
<!-- Consumed primitives from earlier Phase 4 plans (all via @/components/retro barrel). -->

From retro/overlay (Plan 04-01):
```typescript
// Popover({ open, onClose, anchorRef, role, children }) — chromeless utility, ESC via useModalStack.
// RetroConfirmDialog({ open, onClose, onConfirm, title?, titlebarVariant?, confirmVariant?, ... })
//   — pink titlebar default, focus-on-cancel; destructive delete confirms route here.
```
From retro/form (Plan 04-04): `RetroCheckbox`, `RetroInput`, `RetroFormField`.
From retro core: `RetroBadge` (chip chrome), `BevelButton`.

Legacy contracts to port (RESEARCH § Code Examples):
```typescript
// useSavedFilters — port from frontend/lib/hooks/use-saved-filters.ts, retype any→unknown
export interface SavedFilter { id: string; name: string; filters: Record<string, unknown>; createdAt: string; isDefault?: boolean; }
// useSavedFilters({ storageKey, onApplyFilter }) →
//   { savedFilters, saveFilter, deleteFilter, updateFilter, applyFilter, setAsDefault, getDefaultFilter }
// Storage shape = array of SavedFilter under storageKey (forward-compatible with Phase 12 server prefs).
// FilterBar legacy contract: { filterChips, onRemoveFilter, onClearAll }
// BulkActionBar legacy contract: { selectedCount, onClear, children }
```

UI-SPEC § "Filter Atoms" (BINDING chrome/copy):
- FilterBar: `flex items-center gap-sp-2 p-sp-3 bg-bg-panel-2`; search RetroInput w-[260px] placeholder `Filter items…`; facet BevelButtons `CATEGORY ▾`; spacer; mono count `{n} items`; primary CTA `+ ADD ITEM`; active chips `bg-titlebar-blue` + ink ✕; `CLEAR ALL` text button.
- FilterPopover: Popover (chromeless) anchored under a trigger; RetroCheckbox checklist; multi-select keeps popover open per toggle; ESC closes via useModalStack.
- BulkActionBar (inline mobile): `bg-bg-panel-2 border-2 border-border-ink bevel-raised p-sp-3 flex items-center gap-sp-2`; left `{n} SELECTED` RetroBadge (blue); destructive BevelButtons variant="danger" → RetroConfirmDialog; `✕ CLEAR`; `role="toolbar" aria-label="Bulk actions"` + aria-live polite count.
- SavedFilters: preset chips (RetroBadge, active = blue, aria-pressed); `▾ PRESETS` BevelButton opens a Menu/Popover listing presets + `SAVE CURRENT…` (opens a small RetroDialog with a RetroInput name field) + per-preset delete (danger menu item → RetroConfirmDialog); empty menu row `No saved filters yet.`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useSavedFilters (localStorage) + SavedFilters</name>
  <files>frontend2/src/components/retro/filters/useSavedFilters.ts, frontend2/src/components/retro/filters/useSavedFilters.test.ts, frontend2/src/components/retro/filters/SavedFilters.tsx, frontend2/src/components/retro/filters/SavedFilters.test.tsx</files>
  <read_first>
    - .planning/phases/04-retro-atoms/04-RESEARCH.md § "Code Examples → SavedFilters localStorage contract" + § "Security Domain"
    - .planning/phases/04-retro-atoms/04-UI-SPEC.md § "Filter Atoms → SavedFilters" + § "Copywriting Contract"
    - frontend/lib/hooks/use-saved-filters.ts (legacy source to port; retype any→unknown)
  </read_first>
  <behavior>
    useSavedFilters:
    - Test: saveFilter persists a `{id,name,filters,createdAt}` preset to localStorage under storageKey; reading back returns it.
    - Test: applyFilter calls the onApplyFilter callback with the preset's filters.
    - Test: setAsDefault marks one preset isDefault; getDefaultFilter returns it; saving a new default clears the prior.
    - Test: deleteFilter removes a preset.
    - Test (security/robustness): a malformed/corrupted localStorage payload (non-JSON or wrong shape) is tolerated — the hook resets to `[]` without throwing (try/catch JSON.parse).
    SavedFilters:
    - Test: renders preset chips as `<button>`s; the active preset has aria-pressed="true".
    - Test: ▾ PRESETS opens a menu listing presets + a `SAVE CURRENT…` item; empty state shows `No saved filters yet.`
    - Test: per-preset delete opens a RetroConfirmDialog before removing.
  </behavior>
  <action>
    Port `useSavedFilters({ storageKey, onApplyFilter })` from the legacy hook, retyping `any`→`unknown` and `filters: Record<string, unknown>`. Wrap the localStorage read in try/catch — on parse error or non-array shape, reset to `[]` (do NOT throw; treat parsed data as untrusted per Security Domain V5). Return `{ savedFilters, saveFilter, deleteFilter, updateFilter, applyFilter, setAsDefault, getDefaultFilter }`. Storage shape = array of SavedFilter under storageKey (forward-compatible with Phase 12 server prefs).
    Build `SavedFilters` per UI-SPEC: preset chips (RetroBadge chrome, active = blue + aria-pressed) + a `▾ PRESETS` BevelButton opening a Menu/Popover (the Plan 04-01 Popover) listing presets + `SAVE CURRENT…` (opens a small RetroDialog with a RetroInput name field) + per-preset delete (danger item → RetroConfirmDialog). Empty menu row `No saved filters yet.` Render all preset names/values as TEXT nodes (never dangerouslySetInnerHTML). Copy via `<Trans>`/`t`.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test src/components/retro/filters/useSavedFilters.test.ts src/components/retro/filters/SavedFilters.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both specs pass including the malformed-localStorage reset-to-[] test.
    - `grep -c "JSON.parse" frontend2/src/components/retro/filters/useSavedFilters.ts` ≥ 1 and it is inside a try/catch (the reset-to-[] test enforces this).
    - `grep -c "dangerouslySetInnerHTML" frontend2/src/components/retro/filters/SavedFilters.tsx` = 0.
    - `bun run lint:tsc` exits 0.
  </acceptance_criteria>
  <done>localStorage preset hook (malformed-tolerant) + SavedFilters UI exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FilterBar + FilterPopover + BulkActionBar + filters barrel</name>
  <files>frontend2/src/components/retro/filters/FilterBar.tsx, frontend2/src/components/retro/filters/FilterBar.test.tsx, frontend2/src/components/retro/filters/FilterPopover.tsx, frontend2/src/components/retro/filters/FilterPopover.test.tsx, frontend2/src/components/retro/filters/BulkActionBar.tsx, frontend2/src/components/retro/filters/BulkActionBar.test.tsx, frontend2/src/components/retro/filters/index.ts</files>
  <read_first>
    - .planning/phases/04-retro-atoms/04-UI-SPEC.md § "Filter Atoms → FilterBar / FilterPopover / BulkActionBar" + § "Copywriting Contract"
    - .planning/phases/04-retro-atoms/04-RESEARCH.md § "Architecture Patterns → Pattern 3" (bulk-action surfacing via Bottombar; BulkActionBar is the inline mobile surface)
    - frontend/components/ui/{filter-bar,filter-popover,bulk-action-bar}.tsx (legacy contracts)
  </read_first>
  <behavior>
    FilterBar:
    - Test: renders the search RetroInput, a facet trigger per facet (label + ▾), the mono `{n} items` count, the primary CTA, and active-filter chips with an ink ✕ that calls onRemoveFilter; CLEAR ALL calls onClearAll.
    FilterPopover:
    - Test: a facet trigger opens the Popover with a RetroCheckbox checklist of facet values; toggling a checkbox keeps the popover open and calls onChange; ESC closes via the modal stack (no local document ESC listener).
    BulkActionBar:
    - Test: renders `role="toolbar" aria-label="Bulk actions"` with `{n} SELECTED` count (aria-live polite), the action children, and `✕ CLEAR` calling onClear.
    - Test: a destructive action routes through RetroConfirmDialog (clicking it opens the confirm, not an immediate destructive call).
  </behavior>
  <action>
    Build `FilterBar` per UI-SPEC (recessed `bg-bg-panel-2` toolbar): search RetroInput (`type="search"`, w-[260px], placeholder `Filter items…`), facet BevelButtons (`CATEGORY ▾` etc.) each wired to open a FilterPopover, `flex-1` spacer, mono `{n} items` count, primary CTA BevelButton (`+ ADD ITEM`), and active-filter chips (RetroBadge `bg-titlebar-blue` + ink ✕ → onRemoveFilter) + a `CLEAR ALL` text button → onClearAll. Props follow the legacy contract `{ filterChips, onRemoveFilter, onClearAll, ... }` extended with search + facets + CTA slots.
    Build `FilterPopover` on the Plan 04-01 `Popover` (chromeless utility) anchored under a trigger: a RetroCheckbox checklist (or radio set) of facet values + optional APPLY/CLEAR footer; multi-select keeps the popover open on toggle; ESC via useModalStack.
    Build `BulkActionBar` as the inline mobile/contextual surface (desktop bulk actions surface via the Bottombar SSOT per the locked decision — this atom is NOT a second desktop bar): `bg-bg-panel-2 border-2 border-border-ink bevel-raised p-sp-3 flex` strip, left `{n} SELECTED` RetroBadge (blue), action BevelButton children (destructive ones variant="danger" routed through RetroConfirmDialog before executing), `✕ CLEAR` → onClear. `role="toolbar" aria-label="Bulk actions"`; count `aria-live="polite"`. Copy via `<Trans>`/`t`.
    Add `retro/filters/index.ts` exporting FilterBar, FilterPopover, BulkActionBar, SavedFilters, useSavedFilters + SavedFilter type.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test src/components/retro/filters/ && bun run lint:tsc</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test src/components/retro/filters/` passes (all four filter atoms + the hook).
    - `grep -c "Popover" frontend2/src/components/retro/filters/FilterPopover.tsx` ≥ 1 (built on the shared primitive, not a bespoke overlay).
    - `grep -c "role=\"toolbar\"" frontend2/src/components/retro/filters/BulkActionBar.tsx` ≥ 1.
    - `bun run lint:tsc` exits 0.
  </acceptance_criteria>
  <done>All four ATOM-FB atoms exist on the shared primitives; filters subdir barrel complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → app (SavedFilters) | untrusted persisted JSON re-enters the app |
| user-supplied preset/filter text → DOM | rendered into chips and menu rows |
| user keyboard → filter popover | ESC must stay within the modal stack |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-LS | DoS (client) / Tampering | useSavedFilters localStorage read | mitigate | try/catch JSON.parse; malformed/oversized payload resets to [] (test-enforced); treat parsed shape as untrusted (ASVS V5) |
| T-04-06-XSS | Tampering / Elevation | SavedFilters names + FilterBar chip labels rendered | mitigate | Render as text nodes (JSX auto-escape); grep gate forbids dangerouslySetInnerHTML in SavedFilters |
| T-04-06-ESC | Elevation/DoS | FilterPopover / SavedFilters menu ESC | mitigate | ESC via Popover→useModalStack only; no local document ESC listener (preserves TUI-02) |
| T-04-06-DEL | (safety) | destructive bulk/preset actions | mitigate | Destructive actions route through RetroConfirmDialog (pink, focus-on-cancel) before executing |
</threat_model>

<verification>
- `cd frontend2 && bun run test src/components/retro/filters/` green.
- `bun run lint:tsc` + `bun run lint:imports` exit 0.
</verification>

<success_criteria>
- FilterBar/FilterPopover/BulkActionBar/SavedFilters (ATOM-FB-01..04) exist on the shared primitives.
- useSavedFilters localStorage round-trips and tolerates malformed data.
</success_criteria>

<output>
Create `.planning/phases/04-retro-atoms/04-06-SUMMARY.md` when done.
</output>
