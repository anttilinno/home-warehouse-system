---
phase: 04-retro-atoms
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend2/package.json
  - frontend2/src/components/retro/overlay/RetroDialog.tsx
  - frontend2/src/components/retro/overlay/RetroDialog.test.tsx
  - frontend2/src/components/retro/overlay/RetroConfirmDialog.tsx
  - frontend2/src/components/retro/overlay/RetroConfirmDialog.test.tsx
  - frontend2/src/components/retro/overlay/Popover.tsx
  - frontend2/src/components/retro/overlay/Popover.test.tsx
  - frontend2/src/components/retro/overlay/index.ts
  - frontend2/src/components/retro/index.ts
  - frontend2/src/components/layout/F1HelpDialog.tsx
autonomous: false
requirements: [TUI-02]
user_setup: []

must_haves:
  truths:
    - "RetroDialog renders a centered Window over a scrim, traps focus, restores focus to the invoker on close, and pops via useModalStack on ESC (never a local document listener)"
    - "RetroConfirmDialog renders a pink-titlebar destructive confirm with focus defaulting to Cancel"
    - "Popover renders an anchored chromeless-utility floating panel that closes via useModalStack on ESC"
    - "F1HelpDialog is refactored to consume RetroDialog and still passes its existing LogoutOnEscape never-logs-out test"
    - "sonner@2.0.7 is installed at the exact pinned version after a human-verify legitimacy gate"
  artifacts:
    - path: "frontend2/src/components/retro/overlay/RetroDialog.tsx"
      provides: "Generalized overlay primitive (scrim + Window + useModalStack + focus-trap + restore)"
      exports: ["RetroDialog"]
    - path: "frontend2/src/components/retro/overlay/RetroConfirmDialog.tsx"
      provides: "Destructive/decision confirm preset of RetroDialog"
      exports: ["RetroConfirmDialog"]
    - path: "frontend2/src/components/retro/overlay/Popover.tsx"
      provides: "Anchored chromeless-utility floating panel primitive (combobox/select/filter consume it)"
      exports: ["Popover"]
    - path: "frontend2/package.json"
      provides: "sonner@2.0.7 exact-pinned dependency"
      contains: "\"sonner\": \"2.0.7\""
  key_links:
    - from: "frontend2/src/components/retro/overlay/RetroDialog.tsx"
      to: "@/components/modal useModalStack"
      via: "ESC arbiter registration"
      pattern: "useModalStack\\("
    - from: "frontend2/src/components/layout/F1HelpDialog.tsx"
      to: "RetroDialog"
      via: "overlay composition"
      pattern: "RetroDialog"
---

<objective>
Establish the Phase 4 overlay foundation: install sonner (the one new dependency, gated by a legitimacy checkpoint), extract the proven F1HelpDialog overlay recipe into a reusable `RetroDialog` primitive, build the `RetroConfirmDialog` preset and the anchored `Popover` primitive on top of it, then refactor `F1HelpDialog` to consume `RetroDialog`. Every other overlay in the phase (combobox listbox, select, filter popover, saved-filters menu) builds on these primitives so focus-trap and ESC behavior are written ONCE (Pitfall 5).

Purpose: TUI-02 ESC discipline is already solved by Phase 3's capture-phase `ModalStackProvider`; this plan ensures every new overlay routes ESC through it via a single shared primitive, never a per-overlay document listener (the logout-race anti-pattern).
Output: `retro/overlay/{RetroDialog,RetroConfirmDialog,Popover}.tsx` + tests, refactored `F1HelpDialog`, sonner pinned, root barrel aggregating all five Phase 4 subdirs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-retro-atoms/04-UI-SPEC.md
@.planning/phases/04-retro-atoms/04-RESEARCH.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

From frontend2/src/components/modal/useModalStack.ts:
```typescript
// while isOpen, pushes onClose onto the capture-phase ESC stack; pops on close/unmount.
export function useModalStack(isOpen: boolean, onClose: () => void): void;
```

From frontend2/src/components/retro/Window.tsx:
```typescript
export type TitlebarVariant = "blue" | "pink" | "mint" | "butter" | "plain";
export interface WindowProps {
  title?: ReactNode;            // omit → chromeless panel (no titlebar)
  titlebarVariant?: TitlebarVariant;
  actions?: ReactNode;          // right-aligned titlebar slot (close box goes here)
  className?: string;
  bodyClassName?: string;       // "" for flush content
  children: ReactNode;
}
```

From frontend2/src/components/retro/BevelButton.tsx:
```typescript
export type BevelButtonVariant = "neutral" | "primary" | "mint" | "danger";
export interface BevelButtonProps extends ComponentPropsWithRef<"button"> { variant?: BevelButtonVariant; }
```

The PROVEN overlay recipe to generalize lives in frontend2/src/components/layout/F1HelpDialog.tsx:
- scrim div `fixed inset-0 z-40 flex items-center justify-center bg-fg-ink/40 p-sp-4`, onClick={onClose}
- inner `role="dialog" aria-modal="true" aria-labelledby={titleId} tabIndex={-1} className="w-[min(520px,92vw)] outline-none"`, onClick stopPropagation
- `useModalStack(open, onClose)` (no local document ESC listener)
- focus effect: stores `document.activeElement` as invoker, focuses dialogRef on open, Tab focus-trap via `querySelectorAll('button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])')`, restores invoker focus on cleanup
- renders children inside a `<Window title={<span id={titleId}>…</span>} titlebarVariant="blue">`

Test harness pattern (from F1HelpDialog.test.tsx): wrap in
`<I18nProvider i18n={i18n}><ShortcutsProvider><ModalStackProvider>…</ModalStackProvider></ShortcutsProvider></I18nProvider>`.
The `LogoutOnEscape` harness in that file proves "never logs out while open" — reuse its shape.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: sonner legitimacy gate + exact-pin install</name>
  <what-built>
    sonner@2.0.7 is the ONLY new dependency this phase (orchestrator-approved toast engine, skinned to UI-SPEC visuals in Plan 04-05). RESEARCH tagged it [ASSUMED] because slopcheck was unavailable; the package-legitimacy gate requires a human verify before install.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/sonner — confirm: maintainer is emilkowalski, weekly downloads in the millions, repo github.com/emilkowalski/sonner, latest 2.x line.
    2. Confirm version 2.0.7 exists (Versions tab) and has zero runtime dependencies.
    3. Confirm no postinstall script concern (this is a well-known UI lib).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with `cd frontend2 && bun add sonner@2.0.7` (exact pin, no caret), or describe concerns.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract RetroDialog + build Popover and RetroConfirmDialog</name>
  <files>frontend2/src/components/retro/overlay/RetroDialog.tsx, frontend2/src/components/retro/overlay/RetroDialog.test.tsx, frontend2/src/components/retro/overlay/Popover.tsx, frontend2/src/components/retro/overlay/Popover.test.tsx, frontend2/src/components/retro/overlay/RetroConfirmDialog.tsx, frontend2/src/components/retro/overlay/RetroConfirmDialog.test.tsx, frontend2/src/components/retro/overlay/index.ts</files>
  <read_first>
    - frontend2/src/components/layout/F1HelpDialog.tsx (the recipe to generalize — copy the scrim/focus-trap/useModalStack structure verbatim)
    - frontend2/src/components/layout/F1HelpDialog.test.tsx (test harness + LogoutOnEscape shape)
    - .planning/phases/04-retro-atoms/04-UI-SPEC.md § "Overlay Family" (RetroDialog, RetroConfirmDialog, Menu/Popover dimensions + titlebar semantics + ARIA)
    - frontend2/src/components/retro/Window.tsx (chrome host — titlebar `actions` slot holds the close box)
  </read_first>
  <behavior>
    RetroDialog:
    - Test: open=false renders nothing; open=true renders role="dialog" aria-modal="true" with aria-labelledby pointing at the titlebar title id.
    - Test: on open, focus moves into the dialog; on close, focus restores to the element that was focused before open (invoker restore).
    - Test: Tab from the last focusable wraps to the first; Shift+Tab from the first wraps to the last (focus trap).
    - Test: ESC (dispatched through the ModalStackProvider in the harness) calls onClose; there is NO `document.addEventListener("keydown", …)` for ESC inside RetroDialog (grep gate: only the Tab-trap keydown listener may exist on the dialog node, never a document-level ESC).
    - Test: a close box rendered in the Window titlebar `actions` slot with aria-label="Close" calls onClose on click.
    - Test: titlebarVariant prop forwards to Window (default "blue").
    Popover:
    - Test: closed renders nothing; open renders a floating panel (role="menu" or role="listbox" per a `role` prop) with the chromeless-utility chrome (bg-bg-panel border-2 border-border-ink bevel-raised, z-40).
    - Test: ESC closes via useModalStack (no local document listener); tap-outside (click on the backdrop sentinel) calls onClose.
    - Test: opening moves focus into the panel; closing restores focus to the trigger element passed via an anchor ref.
    RetroConfirmDialog:
    - Test: renders with titlebarVariant="pink" by default (danger semantic).
    - Test: focus defaults to the Cancel button (not Confirm) on open.
    - Test: confirm button uses BevelButton variant="danger"; clicking confirm calls onConfirm, clicking cancel calls onCancel/onClose.
    - Test: width contract `min(420px,92vw)` (narrower than RetroDialog's `min(520px,92vw)`).
  </behavior>
  <action>
    Create `RetroDialog` by generalizing the F1HelpDialog recipe (scrim + inner dialog div + `useModalStack(open, onClose)` + Tab focus-trap + invoker focus-restore + `<Window>` host). Props: `{ open, onClose, title, titlebarVariant?, actions?, footer?, width?, children }`. Render an interactive close box (`✕`, aria-label="Close") into the Window `actions` slot wired to onClose — same path ESC takes. Width default `min(520px,92vw)`; body `p-sp-4` scrollable if tall; optional right-aligned `footer` action row with `border-t-2 border-border-ink pt-sp-3` per UI-SPEC. Do NOT add any document-level ESC listener — all ESC flows through useModalStack (TUI-02 LOCKED).
    Create `Popover` as the anchored chromeless-utility sibling: props `{ open, onClose, anchorRef, role, children }`. Surface `bg-bg-panel border-2 border-border-ink bevel-raised min-w-[160px] z-40`, no pinstriped titlebar. Anchor under the trigger (flip up/down to stay in viewport — basic top/bottom flip acceptable this phase). `useModalStack(open, onClose)` for ESC; backdrop-click / tap-outside closes; move focus into the panel on open, restore to `anchorRef.current` on close. Used by RetroCombobox (Plan 04-04) and the filter atoms (Plan 04-06).
    Create `RetroConfirmDialog` as a thin preset of RetroDialog: `titlebarVariant="pink"` default, body sentence + optional `text-danger` consequence line, footer = confirm BevelButton (variant="danger", consumer-named verb) + cancel BevelButton (variant="neutral"). Autofocus Cancel via an effect/ref after open. Width `min(420px,92vw)`. Per UI-SPEC, a non-destructive decision variant may pass `titlebarVariant="butter"` + a neutral confirm — expose `titlebarVariant` and `confirmVariant` props so the discard-changes idiom is expressible.
    Use `<Trans>`/`t` for any literal UI text (e.g. the close-box aria-label via `t`). Add `retro/overlay/index.ts` barrel exporting RetroDialog, RetroConfirmDialog, Popover and their prop types.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test src/components/retro/overlay/ && bun run lint:tsc</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test src/components/retro/overlay/` passes (RetroDialog + Popover + RetroConfirmDialog specs green).
    - `grep -rn "addEventListener(\"keydown\"" frontend2/src/components/retro/overlay/ | grep -v Tab` returns no ESC document listener (only the Tab focus-trap keydown is allowed; ESC is exclusively via useModalStack).
    - `grep -c "useModalStack" frontend2/src/components/retro/overlay/RetroDialog.tsx` ≥ 1 and same for Popover.tsx.
    - `bun run lint:tsc` exits 0.
  </acceptance_criteria>
  <done>Three overlay primitives exist, all routing ESC through useModalStack, all with focus-trap + restore, all green under vitest + tsc.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor F1HelpDialog onto RetroDialog + aggregate root barrel</name>
  <files>frontend2/src/components/layout/F1HelpDialog.tsx, frontend2/src/components/retro/index.ts</files>
  <read_first>
    - frontend2/src/components/layout/F1HelpDialog.tsx (current inline overlay implementation to replace)
    - frontend2/src/components/layout/F1HelpDialog.test.tsx (the LogoutOnEscape + focus-trap tests that MUST stay green)
    - frontend2/src/components/retro/index.ts (current barrel — add subdir re-exports)
  </read_first>
  <action>
    Replace F1HelpDialog's inline scrim/dialog/focus-trap/useModalStack block with `<RetroDialog open={open} onClose={onClose} title={<Trans>KEYBOARD SHORTCUTS</Trans>} titlebarVariant="blue">`, keeping the Group/Row body and the SINGLE F1/"?" keydown toggle owner (that listener stays in F1HelpDialog — it is the F1 toggle owner, NOT an ESC listener, so it does not violate TUI-02). The existing F1HelpDialog.test.tsx must pass unchanged — do not edit it; if a query breaks, adjust the component to preserve the rendered structure (dialog role, aria-labelledby, focus behavior, LogoutOnEscape silence).
    Update `retro/index.ts` to re-export every Phase 4 subdir barrel: add `export * from "./overlay";` now, plus `export * from "./form";`, `export * from "./feedback";`, `export * from "./data";`, `export * from "./filters";` (the latter four resolve once their owning plans land in this wave / Wave 2-3; add them now so downstream imports use the single `@/components/retro` barrel per the locked v2.0 convention). Keep the existing named exports (Window, BevelButton, RetroInput, RetroBadge, StatCard, RetroTable).
  </action>
  <verify>
    <automated>cd frontend2 && bun run test src/components/layout/F1HelpDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - F1HelpDialog.test.tsx passes with zero edits to the test file (LogoutOnEscape, focus-trap, focus-restore all green).
    - `grep -c "RetroDialog" frontend2/src/components/layout/F1HelpDialog.tsx` ≥ 1.
    - `grep -c "export \* from \"./overlay\"" frontend2/src/components/retro/index.ts` = 1.
  </acceptance_criteria>
  <done>F1HelpDialog consumes RetroDialog; existing overlay tests green; root barrel aggregates the five Phase 4 subdirs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | new dependency (sonner) installed into the bundle |
| user keyboard → overlay stack | ESC must not escape the modal stack and reach a logout/global handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-SC | Tampering | `bun add sonner` (npm install) | mitigate | Blocking human-verify checkpoint (Task 1) against npmjs.com/package/sonner before install; exact-pin 2.0.7 (no caret) |
| T-04-01-ESC | Elevation/DoS | RetroDialog / Popover ESC handling | mitigate | No per-overlay document ESC listener; ESC routed exclusively through Phase 3 capture-phase `useModalStack` (grep gate in Task 2 acceptance); preserves TUI-02 "never logs out while a modal is open" |
| T-04-01-FOCUS | a11y/safety | RetroDialog focus trap | mitigate | Focus trap + invoker restore inherited from the proven F1HelpDialog recipe; covered by RTL specs |
</threat_model>

<verification>
- `cd frontend2 && bun run test src/components/retro/overlay/ && bun run test src/components/layout/F1HelpDialog.test.tsx` all green.
- `bun run lint:tsc` and `bun run lint:imports` exit 0.
- `grep '"sonner": "2.0.7"' frontend2/package.json` matches (exact pin).
- Note the sonner gzip bundle delta in SUMMARY (orchestrator install acceptance).
</verification>

<success_criteria>
- RetroDialog, RetroConfirmDialog, Popover exist and route ESC through useModalStack only.
- F1HelpDialog refactored onto RetroDialog with its existing tests passing unedited.
- sonner@2.0.7 exact-pinned after legitimacy gate.
- Root `@/components/retro` barrel aggregates all five Phase 4 subdirs.
</success_criteria>

<output>
Create `.planning/phases/04-retro-atoms/04-01-SUMMARY.md` when done. Record the sonner gzip bundle delta.
</output>
