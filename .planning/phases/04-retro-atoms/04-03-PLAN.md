---
phase: 04-retro-atoms
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend2/src/components/retro/feedback/RetroStatusDot.tsx
  - frontend2/src/components/retro/feedback/RetroStatusDot.test.tsx
  - frontend2/src/components/retro/feedback/RetroEmptyState.tsx
  - frontend2/src/components/retro/feedback/RetroEmptyState.test.tsx
  - frontend2/src/components/retro/feedback/StatusPill.tsx
  - frontend2/src/components/retro/feedback/StatusPill.test.tsx
  - frontend2/src/components/retro/feedback/index.ts
  - frontend2/src/styles/globals.css
autonomous: true
requirements: [TUI-03, TUI-04]
user_setup: []

must_haves:
  truths:
    - "RetroStatusDot renders `sse: ● <state>` mono text + an 8px square ink-bordered dot; live = mint dot with step-end blink, idle = faint, error = danger; props-driven with NO SSE import"
    - "The status-blink keyframe respects prefers-reduced-motion (dot stays solid mint, no animation)"
    - "StatusPill renders OK/WARN/INFO/DANGER variants with the locked token fills and ink text (no new color tokens)"
    - "RetroEmptyState renders eyebrow/glyph/heading/body/action per UI-SPEC copy"
  artifacts:
    - path: "frontend2/src/components/retro/feedback/RetroStatusDot.tsx"
      provides: "TUI-03 SSE-state panel-header indicator (visual primitive only)"
      exports: ["RetroStatusDot"]
    - path: "frontend2/src/components/retro/feedback/StatusPill.tsx"
      provides: "TUI-04 OK/WARN/INFO/DANGER status pills on RetroBadge chrome"
      exports: ["StatusPill"]
    - path: "frontend2/src/components/retro/feedback/RetroEmptyState.tsx"
      provides: "Empty-list/filtered-results placeholder"
      exports: ["RetroEmptyState"]
  key_links:
    - from: "frontend2/src/components/retro/feedback/RetroStatusDot.tsx"
      to: "globals.css .status-dot--live"
      via: "step-end blink keyframe class"
      pattern: "status-dot--live"
---

<objective>
Build the feedback-visuals family (no overlays, no sonner): `RetroStatusDot` (TUI-03 — the `sse: ● live` panel-header indicator, a DUMB prop-driven primitive with zero SSE coupling — Phase 6 feeds it later), `StatusPill` (TUI-04 — OK/WARN/INFO/DANGER on the shipped RetroBadge chrome, locked token fills, ink text), and `RetroEmptyState`. Owns the single `globals.css` edit this wave (the `status-blink` keyframe + reduced-motion guard).

Purpose: TUI-03 invites premature SSE wiring (Pitfall 6) — this plan ships the dot as a pure visual that takes a `state` prop only. TUI-04 must reuse existing token pairs (no new contrast pairings — UI-SPEC § Accessibility "NO NEW pairings").
Output: `retro/feedback/{RetroStatusDot,StatusPill,RetroEmptyState}.tsx` + tests + subdir barrel + the blink keyframe in globals.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-retro-atoms/04-UI-SPEC.md
@.planning/phases/04-retro-atoms/04-RESEARCH.md

<interfaces>
<!-- Extracted from codebase + UI-SPEC. -->

From frontend2/src/components/retro/RetroBadge.tsx (StatusPill builds on this chrome — reuse, do not fork):
```typescript
export type RetroBadgeVariant = "neutral" | "ok" | "warn" | "danger" | "info";
// pastel fill + 1px ink border + 2px radius + 11px UPPERCASE INK text.
export interface RetroBadgeProps { variant?: RetroBadgeVariant; className?: string; children: ReactNode; }
```

UI-SPEC RetroStatusDot contract:
- `font-mono text-[12px] text-fg-muted` key (`sse:`) + 8px square ink-bordered dot + optional state word.
- live = `bg-titlebar-mint` + blink; idle/offline = `bg-fg-faint`; error = `bg-danger`.
- state word colors (text on panel/cream, deep companions): live `text-accent-mint-deep`, offline `text-fg-muted`, error `text-danger`.
- props: `state: "live" | "idle" | "error"` (NO SSE hook import).

UI-SPEC StatusPill: variant = fill token only; text always `text-fg-ink`. OK=`bg-ok-bg`, WARN=`bg-warn-bg`, INFO=`bg-info-bg`, DANGER=`bg-danger-bg`. All four pairings already pass tokens.test.ts (no new pairing).

UI-SPEC RetroEmptyState copy (Copywriting Contract): no-data heading `NOTHING HERE YET` / body `This list is empty. Add your first item to get started.`; filtered heading `NO MATCHES` / body `No items match these filters. Clear a filter or adjust your search.`

The reduced-motion guard idiom already exists in globals.css (`@media (prefers-reduced-motion: reduce)` block near line 181) — mirror it for the blink keyframe.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RetroStatusDot (TUI-03) + status-blink keyframe</name>
  <files>frontend2/src/components/retro/feedback/RetroStatusDot.tsx, frontend2/src/components/retro/feedback/RetroStatusDot.test.tsx, frontend2/src/styles/globals.css</files>
  <read_first>
    - .planning/phases/04-retro-atoms/04-UI-SPEC.md § "Feedback Family → RetroStatusDot" (dot scale, colors, blink keyframe, reduced-motion)
    - .planning/phases/04-retro-atoms/04-RESEARCH.md § "Pitfall 6" (no SSE coupling)
    - frontend2/src/styles/globals.css (the existing prefers-reduced-motion block to mirror)
  </read_first>
  <behavior>
    - Test: state="live" renders the `sse:` mono key, an 8px square dot with `bg-titlebar-mint` and the `status-dot--live` class, and the state word `live` (text-accent-mint-deep).
    - Test: state="idle" renders a faint dot (`bg-fg-faint`), word `offline`, and NO blink class.
    - Test: state="error" renders a danger dot (`bg-danger`), word `error` (text-danger).
    - Test: the component does NOT import any SSE/useSSE hook (grep gate: no `useSSE` import in the file).
    - Test: dot is `aria-hidden` (decorative); the state word carries the meaning.
  </behavior>
  <action>
    Build `RetroStatusDot({ state }: { state: "live" | "idle" | "error" })` exactly per UI-SPEC: `font-mono text-[12px] text-fg-muted` `sse:` key + an 8px (`h-2 w-2` / fixed 8px) radius-0 ink-bordered dot + the state word. Map state→{dot fill, word, word color, blink}. Apply `status-dot--live` class only when live. Mark the dot `aria-hidden="true"`. Use `<Trans>`/`t` for the state words. Do NOT import any SSE hook — props only (Pitfall 6).
    Append to globals.css (in the @layer components or top-level, matching existing convention):
    `@keyframes status-blink { 50% { opacity: 0; } }` and `.status-dot--live { animation: status-blink 1.4s steps(1, end) infinite; }`, plus a `@media (prefers-reduced-motion: reduce) { .status-dot--live { animation: none; } }` guard so the dot stays solid mint.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test src/components/retro/feedback/RetroStatusDot.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test src/components/retro/feedback/RetroStatusDot.test.tsx` passes.
    - `grep -c "useSSE\|sseStatus\|EventSource" frontend2/src/components/retro/feedback/RetroStatusDot.tsx` = 0 (no SSE coupling).
    - `grep -c "status-blink" frontend2/src/styles/globals.css` ≥ 1 and `grep -c "prefers-reduced-motion" frontend2/src/styles/globals.css` ≥ 1 (the new guard or the existing block covers .status-dot--live).
  </acceptance_criteria>
  <done>RetroStatusDot is a prop-driven visual primitive with a reduced-motion-safe blink and zero SSE import.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StatusPill (TUI-04) + RetroEmptyState</name>
  <files>frontend2/src/components/retro/feedback/StatusPill.tsx, frontend2/src/components/retro/feedback/StatusPill.test.tsx, frontend2/src/components/retro/feedback/RetroEmptyState.tsx, frontend2/src/components/retro/feedback/RetroEmptyState.test.tsx, frontend2/src/components/retro/feedback/index.ts</files>
  <read_first>
    - .planning/phases/04-retro-atoms/04-UI-SPEC.md § "Color → Status pill variants" + § "Feedback Family → RetroEmptyState" + § "Copywriting Contract"
    - frontend2/src/components/retro/RetroBadge.tsx (StatusPill wraps this chrome)
    - frontend2/src/components/retro/BevelButton.tsx (EmptyState action CTA)
  </read_first>
  <behavior>
    StatusPill:
    - Test: variant="ok" renders `bg-ok-bg` + `text-fg-ink`; "warn" → `bg-warn-bg`; "info" → `bg-info-bg`; "danger" → `bg-danger-bg`. All carry ink text (no deep-companion text inside a filled pill).
    - Test: the consumer-supplied label text renders (e.g. `OK`, `OVERDUE`).
    - Test: built on RetroBadge chrome (radius-2 chip, ink border) — assert the chip class is present.
    RetroEmptyState:
    - Test: renders eyebrow (optional), glyph thumb, heading (Silkscreen uppercase), body, optional action BevelButton.
    - Test: default no-data copy `NOTHING HERE YET` renders when given those props; a filtered variant renders `NO MATCHES`.
    - Test: clicking the action calls its onClick.
  </behavior>
  <action>
    Build `StatusPill({ variant, children }: { variant: "ok"|"warn"|"info"|"danger"; children: ReactNode })` as a thin preset over RetroBadge — pass `variant` straight through to RetroBadge (which already maps ok/warn/info/danger to the locked fills with ink text). No new tokens, no deep-companion text inside the fill (UI-SPEC "pastel fills carry ink text ONLY"). This satisfies TUI-04 pills.
    Build `RetroEmptyState({ eyebrow?, glyph?, heading, body, action? })` per UI-SPEC: centered `flex flex-col items-center gap-sp-2 py-sp-5 px-sp-4 text-center`, optional 10px uppercase eyebrow, a 32px glyph in a 1px ink-bordered square thumb (`text-fg-faint` — the sanctioned faint use), 16px Silkscreen uppercase heading, 14px `text-fg-muted` body, optional `BevelButton` (primary) action. Copy via `<Trans>`. Add `retro/feedback/index.ts` exporting RetroStatusDot, StatusPill, RetroEmptyState + types.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test src/components/retro/feedback/ && bun run lint:tsc</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test src/components/retro/feedback/` passes (StatusPill + EmptyState + StatusDot).
    - `grep -c "text-fg-ink" frontend2/src/components/retro/feedback/StatusPill.tsx` — pill text is ink (or inherited from RetroBadge which is ink); no `*-deep` text class inside StatusPill fill.
    - `bun run lint:tsc` exits 0.
  </acceptance_criteria>
  <done>StatusPill (4 variants, ink text, no new tokens) + RetroEmptyState exist; feedback subdir barrel complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer text → pill/empty-state labels | rendered as text nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-SSE | Information disclosure / coupling | RetroStatusDot | mitigate | Prop-driven only; grep gate forbids any SSE/EventSource import (Pitfall 6) — no premature live-state coupling |
| T-04-03-XSS | Tampering | pill / empty-state / status-word text | accept | React JSX auto-escapes; no dangerouslySetInnerHTML; labels are Lingui/text nodes |
| T-04-03-TOKEN | (design integrity) | StatusPill fills | mitigate | Reuse existing ok/warn/info/danger token pairs only; no new contrast pairing (UI-SPEC NO NEW pairings) |
</threat_model>

<verification>
- `cd frontend2 && bun run test src/components/retro/feedback/` green.
- `bun run lint:tsc` + `bun run lint:imports` exit 0.
- (Manual, /demo in Plan 04-06/07) eyeball blink + reduced-motion.
</verification>

<success_criteria>
- RetroStatusDot prop-driven, reduced-motion-safe, no SSE import (TUI-03).
- StatusPill OK/WARN/INFO/DANGER on locked tokens, ink text (TUI-04).
- RetroEmptyState matches UI-SPEC copy + chrome.
</success_criteria>

<output>
Create `.planning/phases/04-retro-atoms/04-03-SUMMARY.md` when done.
</output>
