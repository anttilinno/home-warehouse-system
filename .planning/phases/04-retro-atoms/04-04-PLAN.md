---
phase: 04-retro-atoms
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend2/src/components/retro/form/RetroFormField.tsx
  - frontend2/src/components/retro/form/RetroFormField.test.tsx
  - frontend2/src/components/retro/form/RetroSelect.tsx
  - frontend2/src/components/retro/form/RetroSelect.test.tsx
  - frontend2/src/components/retro/form/RetroTextarea.tsx
  - frontend2/src/components/retro/form/RetroTextarea.test.tsx
  - frontend2/src/components/retro/form/RetroCheckbox.tsx
  - frontend2/src/components/retro/form/RetroCheckbox.test.tsx
  - frontend2/src/components/retro/form/RetroFileInput.tsx
  - frontend2/src/components/retro/form/RetroFileInput.test.tsx
  - frontend2/src/components/retro/form/RetroCombobox.tsx
  - frontend2/src/components/retro/form/RetroCombobox.test.tsx
  - frontend2/src/components/retro/form/index.ts
autonomous: true
requirements: [ATOM-FB-02]
user_setup: []

must_haves:
  truths:
    - "RetroFormField wraps any control with a 12px uppercase label, optional hint, and a ✕-prefixed danger error wired via aria-describedby + aria-invalid"
    - "RetroSelect is a skinned NATIVE <select> (not a custom listbox) with the ▾ glyph affordance and shared field states"
    - "RetroCombobox is a hand-rolled editable combobox using aria-activedescendant (virtual focus stays on the input), arrow keys move the active descendant, Enter commits, Esc closes via useModalStack"
    - "RetroCheckbox is a System-7 square box (16x16) driven by an sr-only native checkbox, with an indeterminate dash state for header select-all"
    - "RetroFileInput emits File[] via click-to-browse + basic drag-drop, never reading file contents"
  artifacts:
    - path: "frontend2/src/components/retro/form/RetroCombobox.tsx"
      provides: "aria-activedescendant editable combobox on RetroInput + Popover"
      exports: ["RetroCombobox"]
    - path: "frontend2/src/components/retro/form/RetroFormField.tsx"
      provides: "label/hint/error wrapper for Select/Combobox/Textarea/FileInput"
      exports: ["RetroFormField"]
    - path: "frontend2/src/components/retro/form/RetroFileInput.tsx"
      provides: "File[] emitter (click + drag-drop), no content read"
      exports: ["RetroFileInput"]
  key_links:
    - from: "frontend2/src/components/retro/form/RetroCombobox.tsx"
      to: "retro/overlay/Popover + useModalStack"
      via: "listbox popover routed through the shared ESC arbiter"
      pattern: "Popover"
    - from: "frontend2/src/components/retro/form/RetroCombobox.tsx"
      to: "active option id"
      via: "aria-activedescendant virtual focus"
      pattern: "aria-activedescendant"
---

<objective>
Build the form family: `RetroFormField` (label+hint+error wrapper generalizing the inline RetroInput layout for Select/Combobox/Textarea/FileInput), `RetroSelect` (skinned native `<select>` per orchestrator arbitration — bulletproof a11y, no focus-trap risk), `RetroTextarea`, `RetroCheckbox` (System-7 square box + indeterminate for header select-all), `RetroFileInput` (click + basic drag-drop emitting `File[]`), and `RetroCombobox` (hand-rolled editable combobox on `RetroInput` + the Plan 04-01 `Popover`, using aria-activedescendant virtual focus per W3C APG).

Purpose: Atoms stay presentational and RHF-compatible (forwardRef, name/onChange) — feature pages wire them with `register()`; validation lives at the RHF+zod layer, atoms just render the `error` string. The combobox is the one accessibility-critical hand-roll (Pitfall 2 — keep DOM focus on the input, only move aria-activedescendant).
Output: `retro/form/{RetroFormField,RetroSelect,RetroTextarea,RetroCheckbox,RetroFileInput,RetroCombobox}.tsx` + tests + subdir barrel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-retro-atoms/04-UI-SPEC.md
@.planning/phases/04-retro-atoms/04-RESEARCH.md
@.planning/phases/04-retro-atoms/04-01-SUMMARY.md

<interfaces>
<!-- Extracted from codebase + Plan 04-01 outputs. -->

From frontend2/src/components/retro/RetroInput.tsx (the field chrome to mirror; RetroCombobox uses it as the text trigger):
```typescript
export interface RetroInputProps extends ComponentPropsWithRef<"input"> {
  label: ReactNode;
  error?: ReactNode;   // flips to danger treatment
  mono?: boolean;
}
// Sunken bevel field; focus ring focus:outline-3 focus:outline-titlebar-blue;
// error → border-danger bg-danger-bg + aria-invalid + aria-describedby.
```

From frontend2/src/components/retro/overlay/Popover.tsx (Plan 04-01 — combobox listbox renders through this so ESC goes via useModalStack):
```typescript
// Popover({ open, onClose, anchorRef, role, children }) — chromeless-utility floating panel,
// ESC via useModalStack, focus-in on open / restore-to-anchor on close.
```

Shared field states (UI-SPEC § Form Family):
- Default: `border-2 border-border-ink bg-bg-panel bevel-sunken text-fg-ink`
- Focus-visible: `focus-visible:outline focus-visible:outline-3 focus-visible:outline-offset-1 focus-visible:outline-titlebar-blue`
- Error: `border-danger bg-danger-bg` + `aria-invalid="true"` + `aria-describedby` → error id; error text 12px font-semibold text-danger, ✕-prefixed.
- Disabled: `opacity-50 cursor-not-allowed`; faint text allowed (the one place).

Combobox APG contract (RESEARCH § Pattern 4 + Pitfall 2): input `role="combobox" aria-expanded aria-controls aria-activedescendant`; `<ul role="listbox">` options `role="option" aria-selected`; arrows move aria-activedescendant (DOM focus STAYS on input); Enter commits; Esc closes via useModalStack; scrollIntoView the active option; each option a stable id. Empty filter → one muted `No matches.` row.

Checkbox (UI-SPEC): 16x16 box, `<input type="checkbox" className="sr-only">` drives state, visual box aria-hidden; checked = `bg-titlebar-blue bevel-pressed` + ink ✓; indeterminate = ink `–` dash + `bg-bg-panel-2`; label is the click target (≥24px hit area).
FileInput (orchestrator arbitration: click + basic drop emitting File[]): sunken dashed drop zone + BROWSE… BevelButton, dragover state, selected-files list with mono filename + tabular-nums size + ✕ remove. Emits File[] only; does NOT read file contents (upload is Phase 7/14b).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RetroFormField + RetroSelect + RetroTextarea + RetroCheckbox + RetroFileInput</name>
  <files>frontend2/src/components/retro/form/RetroFormField.tsx, frontend2/src/components/retro/form/RetroFormField.test.tsx, frontend2/src/components/retro/form/RetroSelect.tsx, frontend2/src/components/retro/form/RetroSelect.test.tsx, frontend2/src/components/retro/form/RetroTextarea.tsx, frontend2/src/components/retro/form/RetroTextarea.test.tsx, frontend2/src/components/retro/form/RetroCheckbox.tsx, frontend2/src/components/retro/form/RetroCheckbox.test.tsx, frontend2/src/components/retro/form/RetroFileInput.tsx, frontend2/src/components/retro/form/RetroFileInput.test.tsx</files>
  <read_first>
    - .planning/phases/04-retro-atoms/04-UI-SPEC.md § "Form Family" (every atom's exact chrome, states, ARIA, copy)
    - frontend2/src/components/retro/RetroInput.tsx (chrome + label + error wiring to mirror)
    - frontend2/src/components/retro/BevelButton.tsx (FileInput BROWSE… button)
  </read_first>
  <behavior>
    RetroFormField:
    - Test: renders label (12px uppercase, htmlFor→control id), the control as children, a hint when no error, and the ✕-prefixed danger error (replacing the hint) when error is set; error id is referenced by aria-describedby on the control.
    - Test: required marker `*` (ink) renders after the label when required.
    RetroSelect:
    - Test: renders a native `<select>` (NOT a custom listbox — assert a real select element) with the ▾ affordance glyph and the sunken field chrome; forwardRef + name/onChange pass through (RHF-compatible).
    - Test: error state flips to danger treatment + aria-invalid.
    RetroTextarea:
    - Test: renders a `<textarea>` with min-h, resize-y, mono prop toggling font; forwardRef + RHF props pass through; error flips danger.
    RetroCheckbox:
    - Test: an sr-only native checkbox drives state; the visual box is aria-hidden; checked shows ✓ + pressed bevel; clicking the label toggles; `indeterminate` prop shows the `–` dash.
    RetroFileInput:
    - Test: clicking BROWSE… opens the hidden native file input; selecting files calls onChange with a `File[]`; drop on the zone emits File[]; a ✕ remove row removes a file; the component never calls `.text()`/`FileReader` (grep gate — no content read).
    - Test: dragover toggles the highlight state class.
  </behavior>
  <action>
    Build all five per UI-SPEC § Form Family. RetroFormField is the composition wrapper (vertical stack, gap-sp-1): label / control slot / hint-or-error. Generate an id, pass it to the child control via render-prop or `htmlFor`+`id` convention, and wire aria-describedby/aria-invalid through the error.
    RetroSelect = skinned native `<select>` (orchestrator arbitration; do NOT build a custom listbox here — that lives in RetroCombobox) + `▾` glyph (`absolute right`, pointer-events-none, ink) + shared field chrome; forwardRef.
    RetroTextarea = sunken multi-line field (`min-h-[88px] resize-y`), `mono` prop, forwardRef.
    RetroCheckbox = 16x16 System-7 box driven by an `sr-only` native checkbox; visual box aria-hidden; checked/indeterminate treatments; whole-label click target.
    RetroFileInput = sunken dashed drop zone + BROWSE… BevelButton triggering a hidden native file input; dragover highlight (`border-titlebar-blue bg-info-bg`); selected-files list (mono filename + tabular-nums size + ✕ remove BevelButton). Emit `File[]` via onChange; reject wrong-type/oversized files into a RetroFormField error; NEVER read file contents (no FileReader/.text()). Use `<Trans>`/`t` for all copy (BROWSE…, Drop files here, or, Release to add, error strings).
  </action>
  <verify>
    <automated>cd frontend2 && bun run test src/components/retro/form/RetroFormField.test.tsx src/components/retro/form/RetroSelect.test.tsx src/components/retro/form/RetroTextarea.test.tsx src/components/retro/form/RetroCheckbox.test.tsx src/components/retro/form/RetroFileInput.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The five specs pass.
    - `grep -c "<select" frontend2/src/components/retro/form/RetroSelect.tsx` ≥ 1 (native select, not a div listbox).
    - `grep -c "FileReader\|\.text()\|readAsText\|readAsArrayBuffer" frontend2/src/components/retro/form/RetroFileInput.tsx` = 0 (emits File[] only, no content read).
    - `bun run lint:tsc` exits 0.
  </acceptance_criteria>
  <done>FormField + Select + Textarea + Checkbox + FileInput render per UI-SPEC, RHF-compatible, FileInput content-read-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RetroCombobox (aria-activedescendant) + form barrel</name>
  <files>frontend2/src/components/retro/form/RetroCombobox.tsx, frontend2/src/components/retro/form/RetroCombobox.test.tsx, frontend2/src/components/retro/form/index.ts</files>
  <read_first>
    - .planning/phases/04-retro-atoms/04-UI-SPEC.md § "Form Family → RetroCombobox" (listbox chrome, keyboard, ARIA, empty copy)
    - .planning/phases/04-retro-atoms/04-RESEARCH.md § "Pattern 4" + "Pitfall 2" (aria-activedescendant, virtual focus)
    - frontend2/src/components/retro/overlay/Popover.tsx (Plan 04-01 — the floating panel host)
    - frontend2/src/components/retro/RetroInput.tsx (the text trigger)
  </read_first>
  <behavior>
    - Test: typing filters the option list; the input has `role="combobox" aria-expanded aria-controls`; the listbox is `role="listbox"` with `role="option" aria-selected` rows.
    - Test: ArrowDown/ArrowUp move `aria-activedescendant` to the next/prev option id WITHOUT moving DOM focus off the input (assert document.activeElement stays the input).
    - Test: Enter commits the active option (calls onChange with its value, closes the listbox, sets the input display).
    - Test: Escape closes the listbox via the modal stack (the harness ModalStackProvider), NOT a local document ESC listener (grep gate).
    - Test: empty filter renders a single muted `No matches.` row.
    - Test: the selected option carries a ✓ glyph; active/hover option carries `bg-titlebar-blue`.
  </behavior>
  <action>
    Build `RetroCombobox` hand-rolled on a `RetroInput` text trigger + the Plan 04-01 `Popover` listbox. Keep DOM focus on the input at all times; arrow keys update a virtual highlight by setting `aria-activedescendant` to the active option's stable id; `scrollIntoView` the active option (Pitfall 2). Wire `role="combobox" aria-expanded aria-controls aria-activedescendant` on the input and `role="listbox"`/`role="option" aria-selected` on the panel. Enter commits, Tab commits + closes, Esc closes via the Popover's useModalStack (no own document listener). Empty filter → one `text-fg-muted text-[12px]` `No matches.` row. Selected option shows ✓; active/hover = `bg-titlebar-blue`. Props: controlled `{ value, onChange, options: {value,label}[], label, error?, placeholder? }`; RHF-compatible. Copy via `<Trans>`/`t`.
    Add `retro/form/index.ts` exporting RetroFormField, RetroSelect, RetroTextarea, RetroCheckbox, RetroFileInput, RetroCombobox + their prop types.
  </action>
  <verify>
    <automated>cd frontend2 && bun run test src/components/retro/form/RetroCombobox.test.tsx && bun run lint:tsc</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test src/components/retro/form/RetroCombobox.test.tsx` passes including the "DOM focus stays on input while arrows move activedescendant" test.
    - `grep -c "aria-activedescendant" frontend2/src/components/retro/form/RetroCombobox.tsx` ≥ 1.
    - `grep -rn "addEventListener(\"keydown\"" frontend2/src/components/retro/form/RetroCombobox.tsx | grep -i "Escape\|document" ` returns nothing (ESC via Popover/useModalStack only).
    - `bun run lint:tsc` exits 0.
  </acceptance_criteria>
  <done>RetroCombobox uses virtual focus per W3C APG, ESC via the stack; form subdir barrel complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user file selection → RetroFileInput | files enter the app as File[] handles |
| user keyboard → combobox listbox | ESC must stay within the modal stack |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-FILE | Tampering / Info disclosure | RetroFileInput | mitigate | Emits File[] handles only; never reads content (grep gate forbids FileReader/.text); server-validated upload is Phase 7/14b |
| T-04-04-ESC | Elevation/DoS | RetroCombobox listbox ESC | mitigate | ESC routed through Popover→useModalStack; no local document ESC listener (grep gate) — preserves TUI-02 |
| T-04-04-A11Y | a11y/safety | combobox virtual focus | mitigate | aria-activedescendant per W3C APG; DOM focus stays on input; scrollIntoView active option (Pitfall 2) — covered by RTL spec |
| T-04-04-XSS | Tampering | option/field labels | accept | React JSX auto-escapes; no dangerouslySetInnerHTML |
</threat_model>

<verification>
- `cd frontend2 && bun run test src/components/retro/form/` green.
- `bun run lint:tsc` + `bun run lint:imports` exit 0.
</verification>

<success_criteria>
- All six form atoms render per UI-SPEC, RHF-compatible.
- RetroSelect = native select; RetroCombobox = aria-activedescendant hand-roll; both ESC via the stack.
- RetroFileInput emits File[] with no content read.
</success_criteria>

<output>
Create `.planning/phases/04-retro-atoms/04-04-SUMMARY.md` when done.
</output>
