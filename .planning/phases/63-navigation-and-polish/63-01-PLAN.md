---
phase: 63-navigation-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend2/src/components/layout/Sidebar.tsx
  - frontend2/src/features/taxonomy/tabs/LocationsTab.tsx
  - frontend2/locales/en/messages.po
  - frontend2/locales/et/messages.po
autonomous: false
requirements:
  - NAV-01
  - NAV-02
tags:
  - navigation
  - i18n
  - ui-polish
  - frontend2

must_haves:
  truths:
    - "User sees six sidebar NavLinks in this exact order: DASHBOARD, ITEMS, LOANS, BORROWERS, TAXONOMY, SETTINGS."
    - "SETTINGS is visually pushed to the bottom of the sidebar flex column via an mt-auto spacer (no horizontal divider, no flat equal-weight list)."
    - "No /scan NavLink is present in the sidebar (scanning is deferred to v2.2)."
    - "Dashboard ADD ITEM card routes to /items and renders ItemsListPage (not 404, not DemoPage); VIEW LOANS card routes to /loans and renders LoansListPage."
    - "All six v2.1 list pages render RetroEmptyState with copy matching the UI-SPEC empty state table verbatim: Items (3 variants), Loans (3 tab variants), Borrowers (1), Taxonomy categories/locations/containers (3)."
    - "LocationsTab empty state body reads exactly: 'Create your first location to start placing items.' (rewritten from the old 'describe where items live' string)."
    - "Every msgid present in frontend2/locales/en/messages.po has a non-empty, non-'[ET] …' placeholder msgstr in frontend2/locales/et/messages.po."
    - "ET translation for 'BORROWERS' is 'LAENAJAD' and for 'TAXONOMY' is 'TAKSONOOMIA'."
    - "Compiled Lingui bundles under frontend2/locales/et/ are regenerated after translations so the running UI serves the new strings."
  artifacts:
    - path: "frontend2/src/components/layout/Sidebar.tsx"
      provides: "Six NavLinks in order (Dashboard, Items, Loans, Borrowers, Taxonomy, Settings) with mt-auto spacer before Settings"
      contains: "to=\"/borrowers\""
    - path: "frontend2/src/components/layout/Sidebar.tsx"
      provides: "Taxonomy NavLink"
      contains: "to=\"/taxonomy\""
    - path: "frontend2/src/components/layout/Sidebar.tsx"
      provides: "Flex column spacer pushing Settings to the bottom"
      contains: "mt-auto"
    - path: "frontend2/src/features/taxonomy/tabs/LocationsTab.tsx"
      provides: "Locations empty state with UI-SPEC body copy"
      contains: "Create your first location to start placing items."
    - path: "frontend2/locales/en/messages.po"
      provides: "EN source catalog with extracted BORROWERS and TAXONOMY msgids"
      contains: "msgid \"BORROWERS\""
    - path: "frontend2/locales/et/messages.po"
      provides: "ET catalog with LAENAJAD/TAKSONOOMIA and zero empty msgstr entries for introduced strings"
      contains: "msgstr \"LAENAJAD\""
  key_links:
    - from: "frontend2/src/components/layout/Sidebar.tsx"
      to: "/borrowers route (BorrowersListPage)"
      via: "NavLink to=\"/borrowers\""
      pattern: "to=\"/borrowers\""
    - from: "frontend2/src/components/layout/Sidebar.tsx"
      to: "/taxonomy route (TaxonomyPage)"
      via: "NavLink to=\"/taxonomy\""
      pattern: "to=\"/taxonomy\""
    - from: "frontend2/src/features/dashboard/QuickActionCards.tsx"
      to: "/items + /loans list pages"
      via: "<Link to=...>"
      pattern: "to: \"/items\"|to: \"/loans\""
    - from: "frontend2/locales/et/messages.po"
      to: "Running UI at /, /items, /loans, /borrowers, /taxonomy when locale=et"
      via: "bun run i18n:compile output consumed by Lingui runtime"
      pattern: "msgstr \"[A-ZÄÖÜÕ]"
---

<objective>
Wire the retro sidebar to every v2.1 section (add BORROWERS and TAXONOMY NavLinks in the UI-SPEC order with an mt-auto spacer before SETTINGS), verify the dashboard QuickActionCards route to the real Items and Loans list pages, bring the LocationsTab empty state body in line with the UI-SPEC copy table, and close the Estonian catalog gap for every string introduced across phases 56–62 plus the two new sidebar labels.

Purpose: Close out v2.1 by delivering NAV-01 (sidebar parity) and NAV-02 (dashboard card wiring), and make the Estonian locale fully usable across every v2.1 surface. No new components, no new routes, no new backend work — this phase is pure wiring and copy polish.

Output: Modified Sidebar.tsx (6 NavLinks + mt-auto spacer), modified LocationsTab.tsx (rewritten empty state body), regenerated EN catalog (extract pass), fully populated ET catalog (zero empty msgstr entries introduced by v2.1), recompiled Lingui bundles, and a recorded human-verify checkpoint confirming the dashboard cards land on the real list pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-navigation-and-polish/63-CONTEXT.md
@.planning/phases/63-navigation-and-polish/63-UI-SPEC.md
@.planning/phases/63-navigation-and-polish/63-PATTERNS.md

<interfaces>
<!-- Key patterns and strings executors need. Extracted from the codebase — do not re-explore. -->

From frontend2/src/components/layout/Sidebar.tsx (current state before edit):
```tsx
import { NavLink } from "react-router";
import { useLingui } from "@lingui/react/macro";

interface SidebarProps {
  className?: string;
  onNavClick?: () => void;
}

const navItemBase =
  "w-full text-left px-md py-sm font-bold uppercase text-[14px] border-retro-thick border-retro-ink cursor-pointer outline-2 outline-offset-2 outline-transparent focus-visible:outline-retro-amber";
const navItemDefault =
  "bg-retro-cream text-retro-ink shadow-retro-raised hover:bg-retro-amber";
const navItemActive =
  "bg-retro-amber text-retro-ink shadow-retro-pressed";

export function Sidebar({ className, onNavClick }: SidebarProps) {
  const { t } = useLingui();

  return (
    <nav
      aria-label="Main navigation"
      className={`flex flex-col gap-sm ${className || ""}`}
    >
      {/* 4 existing NavLinks: DASHBOARD (to="/" end), ITEMS (to="/items"), LOANS (to="/loans"), SETTINGS (to="/settings") */}
    </nav>
  );
}
```

From frontend2/src/features/dashboard/QuickActionCards.tsx (current routes, do NOT modify):
```tsx
const actions = [
  { labelKey: "ADD ITEM", to: "/items" },
  { labelKey: "SCAN BARCODE", to: "/scan" },
  { labelKey: "VIEW LOANS", to: "/loans" },
] as const;
```

From frontend2/src/components/retro/RetroEmptyState.tsx (consumer API):
```tsx
interface RetroEmptyStateProps {
  title: string;       // UPPERCASE
  body?: ReactNode;    // sentence-case
  action?: ReactNode;  // optional — informational-only empty states omit this
  showHazardStripe?: boolean;
  className?: string;
}
```

From frontend2/package.json scripts (Lingui commands used by this plan):
```
"i18n:extract": "lingui extract"
"i18n:compile": "lingui compile"
"test": "vitest run"
"lint:imports": "node ../scripts/check-forbidden-imports.mjs src"
```
Run all i18n commands from within `frontend2/` using `bun run i18n:extract` / `bun run i18n:compile` (NOT `bun lingui …` directly).

UI-SPEC Empty State Copy Table (single source of truth — rewrite code to match, not the other way around):

| Surface                                     | Title                 | Body                                                                                  | Action                       |
|---------------------------------------------|-----------------------|---------------------------------------------------------------------------------------|------------------------------|
| /items — no data                            | NO ITEMS YET          | Create your first item to start tracking inventory.                                   | + NEW ITEM                   |
| /items — filter, 0 matches                  | NO MATCHES            | No items match your filters. Clear them to see all items.                             | CLEAR FILTERS (neutral)      |
| /items — all archived, toggle off           | NO ACTIVE ITEMS       | All items are currently archived. Toggle "Show archived" to view them.                | + NEW ITEM                   |
| /loans tab=active                           | NO ACTIVE LOANS       | Nothing is currently out on loan.                                                     | + NEW LOAN                   |
| /loans tab=overdue                          | NO OVERDUE LOANS      | Nothing is past its due date. Nice.                                                   | (none)                       |
| /loans tab=history                          | NO LOAN HISTORY       | Returned loans will appear here.                                                      | (none)                       |
| /borrowers                                  | NO BORROWERS YET      | Create your first borrower to start tracking loans.                                   | + NEW BORROWER               |
| /taxonomy tab=categories                    | NO CATEGORIES YET     | Create your first category to start organizing items.                                 | + NEW CATEGORY               |
| /taxonomy tab=locations                     | NO LOCATIONS YET      | Create your first location to start placing items.                                    | + NEW LOCATION               |
| /taxonomy tab=containers                    | NO CONTAINERS YET     | Containers belong to a location. Create a location first, then add containers inside it. | + NEW CONTAINER (disabled when !hasLocations) |

Pre-flight audit result (from 63-PATTERNS.md): 9 of 10 surfaces already match verbatim. Only LocationsTab.tsx deviates — its body currently reads `"Create your first location to describe where items live."` and must be rewritten to `"Create your first location to start placing items."`

Sidebar NavLink label translations (EN → ET, UPPERCASE, both from UI-SPEC):

| EN msgid     | ET msgstr      |
|--------------|----------------|
| DASHBOARD    | TÖÖLAUD        |
| ITEMS        | ESEMED         |
| LOANS        | LAENUTUSED     |
| BORROWERS    | LAENAJAD       |  (NEW in Phase 63)
| TAXONOMY     | TAKSONOOMIA    |  (NEW in Phase 63 — replace existing "[ET] TAXONOMY" placeholder)
| SETTINGS     | SEADED         |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Sidebar — add BORROWERS + TAXONOMY NavLinks with mt-auto spacer</name>
  <files>frontend2/src/components/layout/Sidebar.tsx</files>
  <read_first>
    - frontend2/src/components/layout/Sidebar.tsx (current state — 63 lines, 4 NavLinks)
    - .planning/phases/63-navigation-and-polish/63-UI-SPEC.md (Sidebar NavLinks table, accessibility contract)
    - .planning/phases/63-navigation-and-polish/63-PATTERNS.md (Sidebar section — copy-verbatim NavLink pattern and mt-auto spacer placement)
    - .planning/phases/63-navigation-and-polish/63-CONTEXT.md (D-01, D-02, D-03 — locked decisions for this task)
  </read_first>
  <action>
    Edit frontend2/src/components/layout/Sidebar.tsx. Inside the existing `<nav aria-label="Main navigation" className={`flex flex-col gap-sm ${className || ""}`}>` element, leave the DASHBOARD, ITEMS, and LOANS NavLinks exactly as they are, then insert TWO new NavLinks after LOANS and a spacer div before SETTINGS so the final children order is:

    1. DASHBOARD (existing — `to="/"` with `end`, label `{t`DASHBOARD`}`)
    2. ITEMS (existing — `to="/items"`, label `{t`ITEMS`}`)
    3. LOANS (existing — `to="/loans"`, label `{t`LOANS`}`)
    4. BORROWERS (NEW — `to="/borrowers"`, label `{t`BORROWERS`}`)
    5. TAXONOMY (NEW — `to="/taxonomy"`, label `{t`TAXONOMY`}`)
    6. `<div className="mt-auto" />` (NEW spacer — pushes SETTINGS to the bottom of the flex column; per D-02 this replaces a horizontal divider and a flat equal-weight list)
    7. SETTINGS (existing — `to="/settings"`, label `{t`SETTINGS`}`)

    For both new NavLinks, copy the exact pattern used by the LOANS NavLink (lines 43-51 of the current file) — same className callback `({ isActive }) => `${navItemBase} ${isActive ? navItemActive : navItemDefault}``, same `onClick={onNavClick}`, same UPPERCASE label wrapped in the `t` tagged template from `useLingui()`. Do NOT redeclare `navItemBase`/`navItemDefault`/`navItemActive`, do NOT add new CSS classes or tokens, do NOT add `end` to any NavLink other than DASHBOARD, do NOT add a `/scan` NavLink (per D-03 — scanning is deferred to v2.2 and reached only from the dashboard quick-action card).

    Do NOT modify the `<nav>` element's className — the existing `flex flex-col gap-sm` is the layout that makes `mt-auto` on the spacer push SETTINGS to the bottom.

    After saving the file, run `cd frontend2 && bun run lint` and `cd frontend2 && bun run test` to confirm no regressions.
  </action>
  <verify>
    <automated>cd frontend2 &amp;&amp; grep -n 'to="/borrowers"' src/components/layout/Sidebar.tsx &amp;&amp; grep -n 'to="/taxonomy"' src/components/layout/Sidebar.tsx &amp;&amp; grep -n 'className="mt-auto"' src/components/layout/Sidebar.tsx &amp;&amp; grep -c 'NavLink' src/components/layout/Sidebar.tsx &amp;&amp; bun run lint &amp;&amp; bun run test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'NavLink' frontend2/src/components/layout/Sidebar.tsx` returns at least 13 (1 import + 6 opening tags + 6 closing tags = 13 occurrences minimum; the exact count may be higher if NavLink is also referenced in comments).
    - `grep -n 'to="/borrowers"' frontend2/src/components/layout/Sidebar.tsx` returns exactly one match.
    - `grep -n 'to="/taxonomy"' frontend2/src/components/layout/Sidebar.tsx` returns exactly one match.
    - `grep -n 'to="/scan"' frontend2/src/components/layout/Sidebar.tsx` returns zero matches (no /scan NavLink per D-03).
    - `grep -n 'className="mt-auto"' frontend2/src/components/layout/Sidebar.tsx` returns exactly one match, and the matching line is positioned AFTER the `to="/taxonomy"` NavLink and BEFORE the `to="/settings"` NavLink in source order.
    - `grep -n 'to=' frontend2/src/components/layout/Sidebar.tsx` returns six lines in this exact vertical order: `to="/"`, `to="/items"`, `to="/loans"`, `to="/borrowers"`, `to="/taxonomy"`, `to="/settings"`.
    - `grep -n '{t`BORROWERS`}' frontend2/src/components/layout/Sidebar.tsx` returns one match.
    - `grep -n '{t`TAXONOMY`}' frontend2/src/components/layout/Sidebar.tsx` returns one match.
    - `grep -n 'navItemBase =\|navItemDefault =\|navItemActive =' frontend2/src/components/layout/Sidebar.tsx` returns exactly 3 lines (constants not duplicated).
    - `cd frontend2 && bun run lint` exits 0.
    - `cd frontend2 && bun run test` exits 0.
  </acceptance_criteria>
  <done>
    Sidebar.tsx contains six NavLinks in the UI-SPEC order with BORROWERS and TAXONOMY linking to their respective routes, a single `<div className="mt-auto" />` spacer sits between TAXONOMY and SETTINGS, no `/scan` NavLink exists, the navItem class constants are declared exactly once, and lint + tests pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: LocationsTab — rewrite empty-state body to match UI-SPEC + re-confirm other 9 empty states</name>
  <files>frontend2/src/features/taxonomy/tabs/LocationsTab.tsx</files>
  <read_first>
    - frontend2/src/features/taxonomy/tabs/LocationsTab.tsx (current state — body on line 138 deviates)
    - frontend2/src/features/taxonomy/tabs/CategoriesTab.tsx (sibling analog — confirmed matching UI-SPEC, use as pattern reference)
    - frontend2/src/features/taxonomy/tabs/ContainersTab.tsx (sibling — must stay unchanged, confirmed matching)
    - frontend2/src/features/items/ItemsListPage.tsx (confirmed matching, no edit needed — verify only)
    - frontend2/src/features/loans/LoansListPage.tsx (confirmed matching, no edit needed — verify only)
    - frontend2/src/features/borrowers/BorrowersListPage.tsx (confirmed matching, no edit needed — verify only)
    - .planning/phases/63-navigation-and-polish/63-UI-SPEC.md (Empty State Parity table — authoritative copy)
    - .planning/phases/63-navigation-and-polish/63-PATTERNS.md (Pre-flight: Empty State Audit Summary — delta table)
    - .planning/phases/63-navigation-and-polish/63-CONTEXT.md (D-05 — audit rule: UI-SPEC wins over code)
  </read_first>
  <action>
    Single source edit: in frontend2/src/features/taxonomy/tabs/LocationsTab.tsx, locate the `RetroEmptyState` block (approximately lines 135-145) and change the `body` prop from:

    ```tsx
    body={t`Create your first location to describe where items live.`}
    ```

    to exactly:

    ```tsx
    body={t`Create your first location to start placing items.`}
    ```

    Leave the surrounding conditional guard (`{workspaceId && query.isSuccess && allItems.length === 0 && (`), the `title={t`NO LOCATIONS YET`}` prop, the `action` prop with the `+ NEW LOCATION` RetroButton, and all imports unchanged. No other edits to this file.

    Then, WITHOUT editing them, read the nine other empty-state blocks listed below and confirm their `title` and `body` strings match the UI-SPEC Empty State Copy Table verbatim (per D-05, UI-SPEC wins — if any deviation is found that was not pre-identified in 63-PATTERNS.md, stop and escalate):

    1. frontend2/src/features/items/ItemsListPage.tsx — three RetroEmptyState variants around lines 255-299 (NO MATCHES / NO ACTIVE ITEMS / NO ITEMS YET)
    2. frontend2/src/features/loans/LoansListPage.tsx — three tab variants around lines 145-176 (NO ACTIVE LOANS / NO OVERDUE LOANS / NO LOAN HISTORY)
    3. frontend2/src/features/borrowers/BorrowersListPage.tsx — single variant around lines 194-204 (NO BORROWERS YET)
    4. frontend2/src/features/taxonomy/tabs/CategoriesTab.tsx — single variant around lines 133-143 (NO CATEGORIES YET)
    5. frontend2/src/features/taxonomy/tabs/ContainersTab.tsx — single variant around lines 165-179 (NO CONTAINERS YET)

    The 63-PATTERNS.md pre-flight audit declared all nine match — this task re-confirms nothing has drifted since and records the result in the SUMMARY.

    After the LocationsTab edit, run `cd frontend2 && bun run lint && bun run test` to confirm nothing regressed.
  </action>
  <verify>
    <automated>cd frontend2 &amp;&amp; grep -n 'Create your first location to start placing items.' src/features/taxonomy/tabs/LocationsTab.tsx &amp;&amp; ! grep -n 'Create your first location to describe where items live.' src/features/taxonomy/tabs/LocationsTab.tsx &amp;&amp; grep -n 'Create your first item to start tracking inventory.' src/features/items/ItemsListPage.tsx &amp;&amp; grep -n 'No items match your filters. Clear them to see all items.' src/features/items/ItemsListPage.tsx &amp;&amp; grep -n 'All items are currently archived. Toggle ' src/features/items/ItemsListPage.tsx &amp;&amp; grep -n 'Nothing is currently out on loan.' src/features/loans/LoansListPage.tsx &amp;&amp; grep -n 'Nothing is past its due date. Nice.' src/features/loans/LoansListPage.tsx &amp;&amp; grep -n 'Returned loans will appear here.' src/features/loans/LoansListPage.tsx &amp;&amp; grep -n 'Create your first borrower to start tracking loans.' src/features/borrowers/BorrowersListPage.tsx &amp;&amp; grep -n 'Create your first category to start organizing items.' src/features/taxonomy/tabs/CategoriesTab.tsx &amp;&amp; grep -n 'Containers belong to a location. Create a location first, then add containers inside it.' src/features/taxonomy/tabs/ContainersTab.tsx &amp;&amp; bun run lint &amp;&amp; bun run test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Create your first location to start placing items.' frontend2/src/features/taxonomy/tabs/LocationsTab.tsx` returns exactly 1.
    - `grep -c 'Create your first location to describe where items live.' frontend2/src/features/taxonomy/tabs/LocationsTab.tsx` returns 0.
    - `grep -c 'NO LOCATIONS YET' frontend2/src/features/taxonomy/tabs/LocationsTab.tsx` returns at least 1 (title unchanged).
    - `grep -c '+ NEW LOCATION' frontend2/src/features/taxonomy/tabs/LocationsTab.tsx` returns at least 1 (action label unchanged).
    - The following literal strings are each grep-matched exactly one or more times in their respective source files (audit confirmation, NO edits required):
      - `Create your first item to start tracking inventory.` in `frontend2/src/features/items/ItemsListPage.tsx`
      - `No items match your filters. Clear them to see all items.` in `frontend2/src/features/items/ItemsListPage.tsx`
      - `All items are currently archived. Toggle "Show archived" to view them.` in `frontend2/src/features/items/ItemsListPage.tsx`
      - `Nothing is currently out on loan.` in `frontend2/src/features/loans/LoansListPage.tsx`
      - `Nothing is past its due date. Nice.` in `frontend2/src/features/loans/LoansListPage.tsx`
      - `Returned loans will appear here.` in `frontend2/src/features/loans/LoansListPage.tsx`
      - `Create your first borrower to start tracking loans.` in `frontend2/src/features/borrowers/BorrowersListPage.tsx`
      - `Create your first category to start organizing items.` in `frontend2/src/features/taxonomy/tabs/CategoriesTab.tsx`
      - `Containers belong to a location. Create a location first, then add containers inside it.` in `frontend2/src/features/taxonomy/tabs/ContainersTab.tsx`
    - `cd frontend2 && bun run lint` exits 0.
    - `cd frontend2 && bun run test` exits 0.
    - The SUMMARY for this plan lists each audited surface with "confirmed verbatim match" (9 surfaces) and one "rewrote body" entry (LocationsTab).
  </acceptance_criteria>
  <done>
    LocationsTab.tsx body reads exactly `Create your first location to start placing items.`, the old body string is gone, the other nine empty-state surfaces are verified to still match UI-SPEC copy verbatim, and lint + tests pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Lingui extract + full ET catalog gap-fill + compile</name>
  <files>frontend2/locales/en/messages.po, frontend2/locales/et/messages.po</files>
  <read_first>
    - frontend2/locales/en/messages.po (EN source catalog — authoritative msgid list)
    - frontend2/locales/et/messages.po (ET target catalog — currently has ~215 empty msgstr entries plus several "[ET] …" placeholder strings)
    - frontend2/package.json (confirm scripts: `i18n:extract`, `i18n:compile`)
    - frontend2/lingui.config.ts (confirm extraction config scans `src` and emits to `locales/{locale}/messages`)
    - .planning/phases/63-navigation-and-polish/63-UI-SPEC.md (i18n Catalog Contract + Sidebar NavLinks table — LAENAJAD / TAKSONOOMIA translations)
    - .planning/phases/63-navigation-and-polish/63-PATTERNS.md (i18n operation order: edit LocationsTab first, then Sidebar, then extract, then fill, then compile)
    - .planning/phases/63-navigation-and-polish/63-CONTEXT.md (D-06 — full v2.1 ET gap fill is canonical for "catalogs complete")
  </read_first>
  <action>
    Prerequisite: Tasks 1 and 2 must already be merged into working tree (Sidebar has BORROWERS + TAXONOMY NavLinks, LocationsTab has the rewritten body) so that extraction picks up the new msgids.

    Step 1 — run extraction:
    From the `frontend2/` directory, run `bun run i18n:extract`. This regenerates `frontend2/locales/en/messages.po` and `frontend2/locales/et/messages.po` with current msgids, marks orphaned entries (e.g. the old `"Create your first location to describe where items live."`) as obsolete (`#, obsolete` or removed by the extractor per the project's Lingui config), and creates new `msgstr ""` entries for any newly added strings.

    Step 2 — open `frontend2/locales/et/messages.po` and fill every empty or placeholder msgstr with a real Estonian translation. "Empty or placeholder" means any of: `msgstr ""` with no translation, `msgstr "[ET] …"` bracketed-placeholder translations (legacy artifacts), or `msgstr "[et] …"` variants. Required specific translations (from UI-SPEC):

    | msgid                                                                                          | required Estonian msgstr |
    |------------------------------------------------------------------------------------------------|--------------------------|
    | `BORROWERS`                                                                                    | `LAENAJAD`               |
    | `TAXONOMY`                                                                                     | `TAKSONOOMIA`            |
    | `DASHBOARD`                                                                                    | `TÖÖLAUD` (confirm if already translated — do not overwrite a correct value) |
    | `ITEMS`                                                                                        | `ESEMED` (confirm)       |
    | `LOANS`                                                                                        | `LAENUTUSED` (confirm)   |
    | `SETTINGS`                                                                                     | `SEADED` (confirm)       |
    | `+ NEW ITEM`                                                                                   | `+ UUS ESE`              |
    | `+ NEW LOAN`                                                                                   | `+ UUS LAENUTUS`         |
    | `+ NEW BORROWER`                                                                               | `+ UUS LAENAJA`          |
    | `+ NEW CATEGORY`                                                                               | `+ UUS KATEGOORIA`       |
    | `+ NEW LOCATION`                                                                               | `+ UUS ASUKOHT`          |
    | `+ NEW CONTAINER`                                                                              | `+ UUS KONTEINER`        |
    | `NO ITEMS YET`                                                                                 | `ESEMEID POLE VEEL`      |
    | `NO MATCHES`                                                                                   | `VASTEID POLE`           |
    | `NO ACTIVE ITEMS`                                                                              | `AKTIIVSEID ESEMEID POLE` |
    | `NO ACTIVE LOANS`                                                                              | `AKTIIVSEID LAENUTUSI POLE` |
    | `NO OVERDUE LOANS`                                                                             | `ÜLETATUD TÄHTAJAGA LAENUTUSI POLE` |
    | `NO LOAN HISTORY`                                                                              | `LAENUTUSAJALUGU POLE`   |
    | `NO BORROWERS YET`                                                                             | `LAENAJAID POLE VEEL`    |
    | `NO CATEGORIES YET`                                                                            | `KATEGOORIAID POLE VEEL` |
    | `NO LOCATIONS YET`                                                                             | `ASUKOHTI POLE VEEL`     |
    | `NO CONTAINERS YET`                                                                            | `KONTEINEREID POLE VEEL` |
    | `Create your first item to start tracking inventory.`                                          | `Lisa oma esimene ese, et alustada laoseisu jälgimist.` |
    | `No items match your filters. Clear them to see all items.`                                    | `Ükski ese ei vasta filtritele. Tühjenda filtrid, et näha kõiki esemeid.` |
    | `All items are currently archived. Toggle "Show archived" to view them.`                       | `Kõik esemed on arhiveeritud. Lülita sisse „Näita arhiveeritud", et neid näha.` |
    | `Nothing is currently out on loan.`                                                            | `Praegu pole midagi laenutatud.` |
    | `Nothing is past its due date. Nice.`                                                          | `Ükski laenutus pole tähtaega ületanud. Tubli.` |
    | `Returned loans will appear here.`                                                             | `Tagastatud laenutused ilmuvad siia.` |
    | `Create your first borrower to start tracking loans.`                                          | `Lisa oma esimene laenaja, et alustada laenutuste jälgimist.` |
    | `Create your first category to start organizing items.`                                        | `Lisa oma esimene kategooria, et alustada esemete korrastamist.` |
    | `Create your first location to start placing items.`                                           | `Lisa oma esimene asukoht, et hakata esemeid paigutama.` |
    | `Containers belong to a location. Create a location first, then add containers inside it.`     | `Konteinerid kuuluvad asukoha juurde. Loo esmalt asukoht ja lisa seejärel konteinerid.` |
    | `CLEAR FILTERS`                                                                                | `TÜHJENDA FILTRID`       |

    For EVERY OTHER empty/placeholder msgstr in ET catalog (the remaining ones not tabulated above — roughly 180+ entries spanning forms, table headers, toasts, confirm dialog copy, etc. introduced across phases 56–62), write an idiomatic Estonian translation following the tone conventions used by the already-translated entries in the same catalog. Short UPPERCASE labels (table headers like `NAME`, `SKU`, `STATUS`, `ACTIONS`) translate to UPPERCASE Estonian equivalents; sentence-case body copy translates to sentence-case Estonian; placeholders like `"+372 555 0101"` or `"0123456789012"` are purely example inputs and must stay verbatim (do NOT translate numeric/example placeholders).

    Rule for known-good ET entries: if an entry already has a correct Estonian translation that is NOT a `[ET] …` bracketed placeholder and is NOT empty, LEAVE IT AS-IS. Do not rewrite working translations.

    Rule for obsolete entries: if extraction marks an entry `#, obsolete`, leave it marked obsolete — do not fill it. The `grep` check in acceptance criteria counts non-obsolete empty msgstrs only if the extractor leaves obsolete blocks in the file; if the extractor deletes obsolete blocks entirely, this is a non-issue.

    Preserve the header block (`msgid ""` / `msgstr ""` at the top of each .po file — those are PO metadata, not translations) exactly as extraction produces it.

    Step 3 — run compilation:
    From the `frontend2/` directory, run `bun run i18n:compile`. This regenerates the compiled message bundles under `frontend2/locales/en/` and `frontend2/locales/et/` (typically `messages.ts` or `messages.mjs` files per Lingui v5 defaults) so the running UI serves the new translations.

    Step 4 — final verification:
    Run `cd frontend2 && grep -n 'msgstr ""' locales/et/messages.po` and confirm only the first occurrence (the header `msgid ""` / `msgstr ""` PO metadata line, around line 1-2) remains. If any non-header `msgstr ""` is still present AND its preceding msgid is not inside a `#, obsolete` block, fill it. Also run `cd frontend2 && grep -n '^msgstr "\[ET\]' locales/et/messages.po` — must return zero matches (all `[ET] …` placeholders replaced with real translations).

    Then run `cd frontend2 && bun run lint && bun run test` to confirm nothing regressed.

    Notes:
    - Do NOT modify `lingui.config.ts`.
    - Do NOT edit `frontend2/locales/en/messages.po` manually — only the extractor writes it.
    - Do NOT translate bracketed interpolation placeholders like `{name}`, `{count}`, or `{0}` — keep them verbatim inside the translated string.
  </action>
  <verify>
    <automated>cd frontend2 &amp;&amp; bun run i18n:extract &amp;&amp; grep -n '^msgid "BORROWERS"' locales/et/messages.po &amp;&amp; grep -A 1 '^msgid "BORROWERS"' locales/et/messages.po | grep -c 'msgstr "LAENAJAD"' &amp;&amp; grep -A 1 '^msgid "TAXONOMY"' locales/et/messages.po | grep -c 'msgstr "TAKSONOOMIA"' &amp;&amp; test $(grep -c '^msgstr ""' locales/et/messages.po) -le 1 &amp;&amp; test $(grep -c '^msgstr "\[ET\]' locales/et/messages.po) -eq 0 &amp;&amp; bun run i18n:compile &amp;&amp; bun run lint &amp;&amp; bun run test</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend2 && bun run i18n:extract` exits 0.
    - `grep -A 1 '^msgid "BORROWERS"' frontend2/locales/et/messages.po` shows `msgstr "LAENAJAD"` on the line immediately after the msgid.
    - `grep -A 1 '^msgid "TAXONOMY"' frontend2/locales/et/messages.po` shows `msgstr "TAKSONOOMIA"` (replacing the previous `[ET] TAXONOMY` placeholder).
    - `grep -c '^msgstr ""' frontend2/locales/et/messages.po` returns a value ≤ 1 (only the PO header metadata empty msgstr is allowed; all others must be filled).
    - `grep -c '^msgstr "\[ET\]' frontend2/locales/et/messages.po` returns 0 (no bracketed `[ET] …` placeholders remain).
    - `grep -A 1 '^msgid "NO BORROWERS YET"' frontend2/locales/et/messages.po` shows `msgstr "LAENAJAID POLE VEEL"`.
    - `grep -A 1 '^msgid "Create your first location to start placing items."' frontend2/locales/et/messages.po` shows `msgstr "Lisa oma esimene asukoht, et hakata esemeid paigutama."`.
    - `grep -c '^msgid "Create your first location to describe where items live."' frontend2/locales/et/messages.po` returns 0 (orphaned msgid removed or marked obsolete and stripped by extractor).
    - `cd frontend2 && bun run i18n:compile` exits 0, producing compiled bundles whose mtime is newer than the .po files.
    - `cd frontend2 && bun run lint` exits 0.
    - `cd frontend2 && bun run test` exits 0.
  </acceptance_criteria>
  <done>
    `bun run i18n:extract` has been run, all new v2.1 msgids are in the EN catalog, the ET catalog has zero non-header empty msgstr entries and zero `[ET] …` placeholder entries, the two new sidebar labels translate to `LAENAJAD` / `TAKSONOOMIA`, compiled bundles are regenerated, and lint + tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify — sidebar order, dashboard card routing, ET locale, empty states</name>
  <files>frontend2/src/components/layout/Sidebar.tsx, frontend2/src/features/dashboard/QuickActionCards.tsx, frontend2/src/features/taxonomy/tabs/LocationsTab.tsx, frontend2/locales/et/messages.po</files>
  <read_first>
    - .planning/phases/63-navigation-and-polish/63-UI-SPEC.md (visual + copy contract against which the human verifies)
    - .planning/phases/63-navigation-and-polish/63-CONTEXT.md (D-01 through D-07 — user decisions being verified)
  </read_first>
  <what-built>
    - Sidebar renders six NavLinks in order (DASHBOARD, ITEMS, LOANS, BORROWERS, TAXONOMY, SETTINGS) with SETTINGS pushed to the bottom via mt-auto spacer.
    - Dashboard QuickActionCards ADD ITEM → /items and VIEW LOANS → /loans (unchanged code, verification only per D-04).
    - LocationsTab empty state body rewritten to UI-SPEC copy.
    - Estonian catalog fully populated for all v2.1 surfaces; LAENAJAD / TAKSONOOMIA sidebar labels present.
  </what-built>
  <action>
    Pause execution for human visual verification. Present the `<how-to-verify>` checklist to the user and wait for their `approved` signal (or a list of failing steps) before proceeding to close out the plan. No code changes in this task — this is a pure checkpoint.
  </action>
  <how-to-verify>
    Start the dev server: `cd frontend2 && bun run dev`. Log in and then:

    1. **Sidebar order (desktop ≥ lg):**
       - Visit `http://localhost:5173/` — the sidebar must show six NavLinks in this exact vertical order: DASHBOARD, ITEMS, LOANS, BORROWERS, TAXONOMY, (gap), SETTINGS.
       - SETTINGS must be visually separated from TAXONOMY by blank space (the mt-auto spacer), NOT by a horizontal divider line, NOT in a flat equal-weight list.
       - Click BORROWERS — URL becomes `/borrowers`, BorrowersListPage renders (either list or `NO BORROWERS YET` empty state). The BORROWERS NavLink turns amber (active state).
       - Click TAXONOMY — URL becomes `/taxonomy`, TaxonomyPage renders (tabbed page: Categories / Locations / Containers). The TAXONOMY NavLink turns amber.
       - Click each NavLink once in order and confirm active-state amber background follows the active route.

    2. **Sidebar order (mobile, viewport ≤ md, e.g. Chrome DevTools iPhone 12 preset):**
       - Open the hamburger drawer — same six NavLinks appear in the same order with the same mt-auto spacer behavior (Sidebar.tsx is reused by AppShell mobile drawer per 63-UI-SPEC).
       - Tap any NavLink — drawer closes (onNavClick fires) and route navigates.

    3. **Dashboard card routing (NAV-02 verification):**
       - Visit `http://localhost:5173/` — three QuickActionCards visible: ADD ITEM, SCAN BARCODE, VIEW LOANS.
       - Click ADD ITEM — URL becomes `/items` and ItemsListPage renders (NOT 404, NOT DemoPage, NOT a placeholder). Confirm you see either the items table with paginated rows or the `NO ITEMS YET` empty state.
       - Back to dashboard. Click VIEW LOANS — URL becomes `/loans` and LoansListPage renders (tabbed Active/Overdue/History). NOT 404, NOT a placeholder.
       - SCAN BARCODE is out of scope (v2.2) — do not click unless curious; confirm visually it routes to `/scan`.

    4. **Empty states (audit confirmation):**
       - Visit `/taxonomy`, click the Locations tab — if no locations exist, the empty-state body must read exactly: `Create your first location to start placing items.` (NOT the old `...to describe where items live.`). The title must read `NO LOCATIONS YET` and the action button `+ NEW LOCATION`.
       - Briefly sanity-check the other visible empty states if reachable (Items with no data, Borrowers with no data, Taxonomy Categories/Containers tabs). Copy should match the UI-SPEC table verbatim.

    5. **Estonian locale (i18n gap-fill confirmation):**
       - Switch language to Estonian via Settings → Language → Eesti (or whichever control exists).
       - Open the sidebar — labels read: TÖÖLAUD, ESEMED, LAENUTUSED, LAENAJAD, TAKSONOOMIA, SEADED (in that order). Specifically confirm LAENAJAD and TAKSONOOMIA (the two new labels) are rendered in Estonian — NOT as `BORROWERS` or `TAXONOMY` or `[ET] TAXONOMY`.
       - Visit `/borrowers` and `/taxonomy` → Locations tab. Confirm page headings, empty states, and button labels are rendered in Estonian with no English fallback and no bracketed `[ET] …` placeholders visible.
       - Navigate to at least two other v2.1 pages (e.g. /items, /loans) and spot-check that strings introduced in phases 56–62 are rendered in Estonian.

    6. **Compiled bundle served:**
       - Open DevTools → Network, reload with language = et, confirm the compiled locale bundle is served (typical path `/locales/et/messages.mjs` or similar per Lingui v5 defaults) with HTTP 200 and non-empty body.

    If any step fails — screenshot the failure and describe which acceptance criterion did not hold. If all steps pass, the phase is ready for retrospective and milestone close-out.
  </how-to-verify>
  <verify>
    <automated>echo "Manual checkpoint — awaiting human approval signal. See how-to-verify steps."</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar DOM order (inspected via DevTools or visible order) is: DASHBOARD → ITEMS → LOANS → BORROWERS → TAXONOMY → (spacer) → SETTINGS.
    - Clicking BORROWERS navigates to `/borrowers` and BorrowersListPage renders.
    - Clicking TAXONOMY navigates to `/taxonomy` and TaxonomyPage renders.
    - No `/scan` NavLink appears in the sidebar.
    - SETTINGS sits at the bottom of the sidebar with blank space above it (no visible divider line).
    - Clicking dashboard ADD ITEM lands on `/items` (ItemsListPage), VIEW LOANS on `/loans` (LoansListPage), neither 404 nor DemoPage.
    - LocationsTab empty state body renders `Create your first location to start placing items.` (or its ET translation when locale=et).
    - With locale=et, sidebar labels are TÖÖLAUD / ESEMED / LAENUTUSED / LAENAJAD / TAKSONOOMIA / SEADED.
    - With locale=et, no visible `[ET] …` bracketed placeholder strings appear on any v2.1 page reached during verification.
    - User types `approved` (or returns a list of failures with screenshots).
  </acceptance_criteria>
  <done>
    Human tester has signed off with `approved`, or all reported failures have been remediated and re-verified. Outcome (approved / remediated / deferred) is recorded in the plan SUMMARY.
  </done>
  <resume-signal>Type "approved" if all verification steps pass, or describe the failing step(s) with which acceptance criterion was violated.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ frontend bundle | User-controlled input (route navigation, language toggle) traverses only client-side; no new boundaries introduced. |
| frontend ↔ backend API | Unchanged — Phase 63 introduces no new API calls and no new auth scopes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | T (Tampering) | `Sidebar.tsx` NavLinks | accept | NavLinks use react-router `<NavLink>` with hardcoded `to="…"` string literals — no user input reaches `to`; no interpolation. Existing SPA routing already validated in v2.0. |
| T-63-02 | I (Information Disclosure) | `messages.po` ET translations | accept | Catalog contains only UI copy (no secrets, no PII, no tokens). Translations are shipped in the public frontend bundle by design. |
| T-63-03 | E (Elevation of Privilege) | Dashboard QuickActionCards routing to `/items` and `/loans` | accept | No privilege change — both routes are already behind the existing auth guard (AuthContext, unchanged). This plan only adds NavLinks; route guards are inherited. |
| T-63-04 | D (Denial of Service) | Lingui extract/compile build step | mitigate | `bun run i18n:compile` is part of the build chain; if it fails, the prebuild `bun run lint:imports` runs first and lint-breaks early. Acceptance criteria require compile to exit 0 before task 3 completes. |
| T-63-05 | S (Spoofing) | ET translations containing HTML-like characters | mitigate | All `t\`…\`` template-literal usages are auto-escaped by Lingui at render time. Translations in this plan are plain Estonian text; no HTML, no `<script>`, no MDX. Code review during extract confirms. |
</threat_model>

<verification>
## Phase-Level Verification

Run from `frontend2/`:
1. `bun run lint` — ESLint clean.
2. `bun run lint:imports` — forbidden-imports guard clean (no idb/serwist/offline/sync leakage; unchanged by this phase).
3. `bun run test` — vitest suite green.
4. `bun run i18n:extract` — no extractor errors, catalogs up-to-date.
5. `bun run i18n:compile` — compiled bundles regenerated.
6. `grep -c '^msgstr ""' frontend2/locales/et/messages.po` ≤ 1.
7. `grep -c '^msgstr "\[ET\]' frontend2/locales/et/messages.po` == 0.
8. Manual checkpoint (Task 4) signed off.
</verification>

<success_criteria>
## Phase 63 Success (NAV-01 + NAV-02)

- [ ] Sidebar renders six NavLinks in UI-SPEC order: DASHBOARD → ITEMS → LOANS → BORROWERS → TAXONOMY → SETTINGS.
- [ ] SETTINGS is pushed to the bottom via a single `<div className="mt-auto" />` spacer (no horizontal divider, no flat list, no new CSS tokens).
- [ ] No `/scan` NavLink in the sidebar.
- [ ] Dashboard ADD ITEM card navigates to `/items` (real ItemsListPage).
- [ ] Dashboard VIEW LOANS card navigates to `/loans` (real LoansListPage).
- [ ] LocationsTab empty state body reads `Create your first location to start placing items.` (UI-SPEC copy).
- [ ] All ten empty-state surfaces match the UI-SPEC copy table verbatim.
- [ ] `frontend2/locales/et/messages.po` has zero non-header empty msgstr entries and zero `[ET] …` placeholder entries.
- [ ] Sidebar labels in ET are TÖÖLAUD / ESEMED / LAENUTUSED / LAENAJAD / TAKSONOOMIA / SEADED.
- [ ] Compiled Lingui bundles regenerated via `bun run i18n:compile`.
- [ ] `bun run lint` + `bun run test` pass.
- [ ] Human-verify checkpoint (Task 4) recorded as approved.
</success_criteria>

<output>
After completion, create `.planning/phases/63-navigation-and-polish/63-01-SUMMARY.md` documenting:
- Final children order of Sidebar.tsx `<nav>`.
- Exact diff for LocationsTab.tsx body prop.
- Count of ET catalog entries filled (before vs after).
- List of all ten empty-state surfaces with "confirmed verbatim match" or "rewrote body".
- Evidence that `/items` and `/loans` dashboard cards resolve to real v2.1 list pages (screenshot references or route-rendered component names).
- Human-verify checkpoint outcome.
</output>
