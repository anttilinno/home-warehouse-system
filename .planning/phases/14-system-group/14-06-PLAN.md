---
phase: 14-system-group
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend2/src/features/sync-history/SyncHistoryPage.tsx
  - frontend2/src/features/sync-history/SyncHistoryPage.test.tsx
otmf_note: "Wave 1, DISJOINT from siblings. Greenfield: owns ONLY features/sync-history/ (a static informational page — no api module, no hook, no network call). No shared file. Touches NO route/Sidebar file — wiring is Wave-2 (14-08). CRITICAL: this page must NOT import any `sync*` module (FOUND-02 / lint:imports CI guard) — it renders static copy only."
autonomous: true
requirements: [SYS-03]
must_haves:
  truths:
    - "SYS-03: /sync-history renders an HONEST informational/empty state — this workspace runs online-only, so there are no background sync events to show"
    - "The page makes NO network call (no /sync/delta consumption — that is a data pull, not an event log) and fabricates NO events/timestamps/statuses"
    - "The page imports NO sync engine / `sync*` module (FOUND-02 CI guard); lint:imports stays green"
  artifacts:
    - path: frontend2/src/features/sync-history/SyncHistoryPage.tsx
      provides: "the /sync-history informational page (static online-only empty state)"
      contains: "SyncHistoryPage"
  key_links:
    - from: frontend2/src/features/sync-history/SyncHistoryPage.tsx
      to: "(none — static informational page, no api binding)"
      via: "renders the online-only RetroEmptyState; no fetch"
      pattern: "RetroEmptyState"
---

<objective>
SYS-03 — the /sync-history page. ROADMAP success-criteria-3 asks for "past sync events with timestamps,
status, and error details", but this is a VESTIGIAL offline-era concept: v3.0 is ONLINE-ONLY and there is
NO sync-event-history backend (the only sync route is GET /sync/delta — a DATA delta-pull, not an event
log). OQ2 is resolved: ship an HONEST informational/empty-state page that explains the workspace runs
online-only and there are no background sync events — do NOT fabricate events, do NOT consume /sync/delta,
do NOT import any sync engine (FOUND-02 CI-blocks `sync*` imports).

This is the smallest plan in the phase: one static page + one render test. No api module, no hook, no
network call. It exists so the `// SYSTEM` nav group is complete and the route is not a dead PlaceholderShell.

Verified surface (CONTEXT + VALIDATION, orchestrator-verified 2026-06-13):
- ONLY `GET /sync/delta?modified_since=<RFC3339>` exists (a data pull). NO event-history endpoint exists.
- FOUND-02: any `sync*` import is CI-blocked (lint:imports). This page must render static copy ONLY.

Document the page as a parity-vestigial residue and flag it (in the SUMMARY) as a roadmap de-scope candidate.

Purpose: SYS-03 — an honest online-only sync-history surface.
Output: SyncHistoryPage (static) + a render test.

Wave 1, DISJOINT (greenfield, no api). Wiring is Wave 2 (14-08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-system-group/14-CONTEXT.md
@.planning/phases/14-system-group/14-VALIDATION.md

# The Window + RetroEmptyState shell to mirror (read for the atom signatures only):
@frontend2/src/features/loans/LoansListPage.tsx

<interfaces>
<!-- Verified from source this planning session. Use directly; no exploration needed. -->

retro atoms (`@/components/retro`): `Window` (title + titlebarVariant), `RetroEmptyState`
({ eyebrow, glyph, heading, body }). No workspace hook strictly required (the page is static), but you MAY
read useWorkspace() for the title — keep it side-effect-free; do NOT fetch.

i18n: all strings via `<Trans>` / `useLingui().t` (@lingui/react/macro).

FORBIDDEN imports: anything matching `sync*` (no SyncEngine, no /sync/delta client, no offline cache).
The page must compose ONLY retro atoms + lingui — verify with lint:imports.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SyncHistoryPage (static online-only informational state) + render test</name>
  <files>frontend2/src/features/sync-history/SyncHistoryPage.tsx, frontend2/src/features/sync-history/SyncHistoryPage.test.tsx</files>
  <action>
    Build SyncHistoryPage as a static informational page: outer max-w container + `<Window
    title={t\`SYNC HISTORY\`} titlebarVariant="plain">` wrapping a `<RetroEmptyState>` with an honest
    online-only message — eyebrow <Trans>Sync</Trans>, a neutral glyph (e.g. "⇄" or "○"), heading
    <Trans>ONLINE ONLY</Trans>, body <Trans>This workspace runs online-only — changes save directly to the
    server, so there are no background sync events to show.</Trans>. NO fetch, NO hook that fires a request,
    NO `sync*` import. All strings via <Trans>/t. Add a short doc comment marking the page a parity-vestigial
    residue (SYS-03) and a roadmap de-scope candidate.
    SyncHistoryPage.test.tsx (render under Router + lingui; NO MSW needed):
    - the page renders the online-only informational heading/body (assert the "online-only" copy is present)
    - the page makes NO network request (assert no fetch is issued — e.g. a fetch spy is never called, or
      simply that no MSW handler is required and the render is synchronous)
  </action>
  <verify>
    <automated>cd frontend2 && bun run test src/features/sync-history/SyncHistoryPage.test.tsx && bun run lint:imports</automated>
  </verify>
  <done>Render test green: the online-only informational state renders, no network call fires; lint:imports green (no sync* import).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | the page is static informational copy — it crosses no trust boundary, issues no request, and renders no server data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-17 | Spoofing | fabricated sync events implying server state that does not exist | mitigate | the page fabricates NO events — it renders an honest "online-only, no sync events" informational state; it never invents timestamps/statuses or consumes /sync/delta (which is a data pull, not history) |
| T-14-18 | Tampering | importing an offline sync engine (FOUND-02 CI guard) | mitigate | the page imports ONLY retro atoms + lingui; no `sync*` module; lint:imports asserts the guard stays green |
| T-14-SC | Tampering | npm installs | mitigate | none — installs NO packages (composes existing retro atoms + lingui only) |
</threat_model>

<verification>
- `cd frontend2 && bun run lint:tsc` clean (NOT bare `tsc`).
- `bun run test src/features/sync-history` green; the page issues no network call.
- `bun run lint:imports` green — NO `sync*` import anywhere in the page.
</verification>

<success_criteria>
- SYS-03: /sync-history renders an honest online-only informational/empty state with no fabricated events,
  no /sync/delta consumption, and no sync-engine import; documented as a parity-vestigial / de-scope candidate.
</success_criteria>

<output>
Create `.planning/phases/14-system-group/14-06-SUMMARY.md` when done (record the SyncHistoryPage export name
so 14-08's route wiring binds to a verified selector, and restate the SYS-03 roadmap de-scope-candidate flag).
</output>
