---
phase: 14-system-group
plan: 08
type: execute
wave: 2
depends_on: [14-01, 14-02, 14-03, 14-04, 14-05, 14-06, 14-07]
files_modified:
  - frontend2/src/routes/index.tsx
  - frontend2/src/components/layout/Sidebar.tsx
otmf_note: "Wave 2. SINGLE-WRITER of BOTH routes/index.tsx AND components/layout/Sidebar.tsx — this plan is their ONLY writer in Phase 14. DISJOINT from the other Wave-2 plan 14-07 (which owns InventoryListPage.tsx + inventoryCsv.ts — zero file overlap). depends_on the six Wave-1 feature plans because it IMPORTS their page component exports (ApprovalsPage, MyChangesPage, WishlistPage, DeclutterPage, ImportsPage, SyncHistoryPage) — those modules must exist for tsc to pass (the same lazy-import-must-exist discipline as the Phase-12 settings route table). Mounts every new route + adds the // SYSTEM nav group items + wires the still-disabled Settings NavItem."
autonomous: true
requirements: [SYS-01, SYS-02, SYS-03, SYS-04, WISH-01, WISH-02, DECL-01, DECL-02]
must_haves:
  truths:
    - "All six new pages are reachable: /approvals, /my-changes, /sync-history, /imports, /wishlist, /declutter each mount their real page (NOT PlaceholderShell) under the authenticated AppShell layout"
    - "The Sidebar // SYSTEM group gains nav items for the new pages, and the previously-disabled Settings NavItem is wired to /settings"
    - "Literal routes precede the wildcard (AP-1 library mode); the new routes sit inside the RequireAuth/AppShell branch like every sibling"
    - "Phase 13's PendingApprovalsPanel 'Review' link now resolves to the real /approvals page"
  artifacts:
    - path: frontend2/src/routes/index.tsx
      provides: "the six new authenticated routes wired to their Wave-1 page components"
      contains: "approvals"
    - path: frontend2/src/components/layout/Sidebar.tsx
      provides: "the // SYSTEM nav group with the new pages + the wired Settings NavItem"
      contains: "/wishlist"
  key_links:
    - from: frontend2/src/routes/index.tsx
      to: frontend2/src/features/approvals/ApprovalsPage.tsx
      via: "<Route path=\"approvals\" element={<ApprovalsPage/>} /> under the AppShell layout"
      pattern: "approvals"
    - from: frontend2/src/components/layout/Sidebar.tsx
      to: "/approvals"
      via: "a NavItem with to=\"/approvals\" in the System group"
      pattern: "to=\"/approvals\""
---

<objective>
The Wave-2 wiring plan: mount every Phase-14 page in the route table and surface them in the Sidebar
`// SYSTEM` group. This is the SINGLE WRITER of routes/index.tsx + Sidebar.tsx in Phase 14. It depends on
the six Wave-1 feature plans because it imports their page exports — tsc requires those modules to exist
(the Phase-12 settings-route-table discipline).

New authenticated routes (inside the existing RequireAuth + AppShell layout branch, literal segments before
the `*` wildcard — AP-1 library mode):
- `/approvals`  → ApprovalsPage   (14-01, SYS-01) — the target of Phase 13's PendingApprovalsPanel "Review" link
- `/my-changes` → MyChangesPage   (14-02, SYS-02)
- `/sync-history` → SyncHistoryPage (14-06, SYS-03)
- `/imports`    → ImportsPage      (14-05, SYS-04)
- `/wishlist`   → WishlistPage     (14-03, WISH-01/02)
- `/declutter`  → DeclutterPage    (14-04, DECL-01/02)

Sidebar (`components/layout/Sidebar.tsx`): the `// SYSTEM` group currently holds Scan (wired), Settings
(DISABLED — no `to`), and the DEV-only Demo item. Add NavItems for the six new pages and WIRE the Settings
NavItem to `/settings` (CONTEXT: folding the still-unwired Settings nav here is sanctioned). Keep the
glyph + count idiom; pick distinct retro glyphs. Approvals MAY carry a count if a cheap source exists —
but do NOT add a new query here just for a badge (keep wiring side-effect-free); count is optional.

Lazy vs eager: the Phase-14 pages are light (tables + forms, no heavy chart/scanner chunk), so import them
EAGERLY like ItemsListPage (NOT React.lazy). Only /scan + /analytics stay lazy (their heavy chunks). Do
NOT convert existing routes.

Purpose: make all eight Phase-14 requirements reachable from the router + nav.
Output: the six routes + the SYSTEM nav items + the wired Settings nav.

Wave 2, SINGLE-WRITER of routes/index.tsx + Sidebar.tsx, DISJOINT from 14-07 (list pages).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-system-group/14-CONTEXT.md
@.planning/phases/14-system-group/14-VALIDATION.md

# The two single-writer files to EXTEND:
@frontend2/src/routes/index.tsx
@frontend2/src/components/layout/Sidebar.tsx

# The Wave-1 SUMMARYs (read for the EXACT exported page component names + route paths):
@.planning/phases/14-system-group/14-01-SUMMARY.md
@.planning/phases/14-system-group/14-02-SUMMARY.md
@.planning/phases/14-system-group/14-03-SUMMARY.md
@.planning/phases/14-system-group/14-04-SUMMARY.md
@.planning/phases/14-system-group/14-05-SUMMARY.md
@.planning/phases/14-system-group/14-06-SUMMARY.md

<interfaces>
<!-- Verified from source this planning session. Use directly; no exploration needed. -->

routes/index.tsx: an `AppRoutes()` `<Routes>` tree. The authenticated branch is a layout `<Route
element={<RequireAuth><AppShell/></RequireAuth>}>` whose children are the app pages; the LAST top-level
route is `<Route path="*" element={<PlaceholderShell/>} />`. Add the six new `<Route path="…"
element={<Page/>} />` INSIDE the authenticated layout branch (siblings of `items`, `loans`, `analytics`,
`settings`), eagerly imported at the top like ItemsListPage. Each path is a literal segment (no params),
so ordering vs the wildcard is the only constraint — they must precede `*` (they already will, being inside
the layout branch above it).

Sidebar.tsx: `NavGroup` + `NavItem` ({ glyph, label, count?, to? }). A NavItem WITH `to` is an active
NavLink; WITHOUT `to` it renders disabled. The `// SYSTEM` group is the third NavGroup (around line 148).
Add the six new NavItems there and change the Settings NavItem from `<NavItem glyph="⚙" label={<Trans>
Settings</Trans>} />` to `<NavItem glyph="⚙" label={<Trans>Settings</Trans>} to="/settings" />`. All labels
via <Trans>.

EXACT page export names + paths: READ the six Wave-1 SUMMARYs (each records its page component export name
and route path). Use those verbatim — do NOT guess the export identifiers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: mount the six new routes in routes/index.tsx (single-writer)</name>
  <files>frontend2/src/routes/index.tsx</files>
  <action>
    SINGLE-WRITER, minimal-diff extension (do NOT rewrite the file):
    - add eager top-level imports for the six page components, using the EXACT export names from the Wave-1
      SUMMARYs: `import { ApprovalsPage } from "@/features/approvals/ApprovalsPage";` and similarly
      MyChangesPage (@/features/my-changes/MyChangesPage), WishlistPage (@/features/wishlist/WishlistPage),
      DeclutterPage (@/features/declutter/DeclutterPage), ImportsPage (@/features/imports/ImportsPage),
      SyncHistoryPage (@/features/sync-history/SyncHistoryPage). (Confirm each path/name against its SUMMARY.)
    - inside the authenticated `<Route element={<RequireAuth><AppShell/></RequireAuth>}>` branch (siblings of
      the existing `items`/`loans`/`analytics`/`settings` routes, ABOVE the closing of that branch), add:
      `<Route path="approvals" element={<ApprovalsPage />} />`, `<Route path="my-changes" element={<
      MyChangesPage />} />`, `<Route path="sync-history" element={<SyncHistoryPage />} />`, `<Route
      path="imports" element={<ImportsPage />} />`, `<Route path="wishlist" element={<WishlistPage />} />`,
      `<Route path="declutter" element={<DeclutterPage />} />`. All are literal segments (no param-route
      ordering hazard); they sit inside the layout branch so they already precede the `*` wildcard.
    - leave /scan + /analytics lazy; do NOT touch the settings block or any existing route.
  </action>
  <verify>
    <automated>cd frontend2 && bun run lint:tsc</automated>
  </verify>
  <done>tsc clean: all six page imports resolve and the six routes are registered inside the authenticated AppShell branch.</done>
</task>

<task type="auto">
  <name>Task 2: add the // SYSTEM nav items + wire Settings in Sidebar.tsx (single-writer)</name>
  <files>frontend2/src/components/layout/Sidebar.tsx</files>
  <action>
    SINGLE-WRITER, minimal-diff extension (do NOT rewrite the file):
    - in the `// SYSTEM` NavGroup (third group, ~line 148), add six NavItems with distinct glyphs and
      <Trans> labels, each with a `to`: Approvals (to="/approvals"), My Changes (to="/my-changes"), Wishlist
      (to="/wishlist"), Declutter (to="/declutter"), Imports (to="/imports"), Sync History (to="/sync-history").
      Place them logically (e.g. Approvals + My Changes near the top of System, then Wishlist + Declutter,
      then Imports + Sync History) — keep Scan + Settings + the DEV Demo item.
    - WIRE the Settings NavItem: change `<NavItem glyph="⚙" label={<Trans>Settings</Trans>} />` to
      `<NavItem glyph="⚙" label={<Trans>Settings</Trans>} to="/settings" />`.
    - optional: give Approvals a `count` ONLY if a count is already trivially in scope — do NOT add a new
      query/hook here (wiring stays side-effect-free). Counts are optional.
    - all labels via <Trans>. Do NOT change the NavGroup/NavItem components or the Overview/Inventory groups.
  </action>
  <verify>
    <automated>cd frontend2 && bun run lint:tsc && bun run test src/components/layout 2>/dev/null; cd frontend2 && bun run lint:tsc</automated>
  </verify>
  <done>tsc clean: the System group renders six new wired NavItems + a now-wired Settings NavItem; existing groups unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated user → new routes | every new route sits inside the RequireAuth + AppShell layout branch — an unauth user is redirected to /login (the routes inherit the existing gate; no new public surface is added) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-21 | Elevation of Privilege | a new route accidentally placed OUTSIDE the auth branch (public) | mitigate | all six routes are added INSIDE the existing `<Route element={<RequireAuth><AppShell/></RequireAuth>}>` branch (siblings of items/loans/settings), inheriting the auth gate; tsc + the literal-before-wildcard placement guard against a leaked public route |
| T-14-22 | DoS | a lazy-import typo breaking the whole route tree at runtime | mitigate | the pages are imported EAGERLY (static imports), so a wrong path/name fails at tsc/build time, not at runtime; lint:tsc is the gate |
| T-14-SC | Tampering | npm installs | mitigate | none — installs NO packages (wires existing components into existing route/nav files) |
</threat_model>

<verification>
- `cd frontend2 && bun run lint:tsc` clean (NOT bare `tsc`).
- Phase gate (run before declaring done): `cd frontend2 && bun run lint:tsc && bun run test && bun run build &&
  bun run lint:imports` all green.
- Live E2E sanity: navigating to /approvals and /wishlist renders their real pages (not PlaceholderShell);
  the Sidebar System group shows the new wired items + a wired Settings nav.
- All six routes sit inside the authenticated AppShell branch; Settings nav is now wired.
</verification>

<success_criteria>
- All eight Phase-14 requirements are reachable: /approvals, /my-changes, /sync-history, /imports, /wishlist,
  /declutter mount their real pages, and the Sidebar // SYSTEM group + wired Settings nav surface them.
- Phase 13's PendingApprovalsPanel "Review" link resolves to the real /approvals page.
</success_criteria>

<output>
Create `.planning/phases/14-system-group/14-08-SUMMARY.md` when done (record the final route paths + the
Sidebar System-group nav set + the now-wired Settings nav, and confirm the live-E2E /approvals + /wishlist
navigation renders the real pages — the phase-gate evidence for the verifier).
</output>
