---
phase: 14-system-group
plan: 07
type: execute
wave: 2
depends_on: []
files_modified:
  - frontend2/src/features/inventory/inventoryCsv.ts
  - frontend2/src/features/inventory/inventoryCsv.test.ts
  - frontend2/src/features/inventory/InventoryListPage.tsx
otmf_note: "Wave 2. SINGLE-WRITER of InventoryListPage.tsx — this plan is its ONLY writer in Phase 14. DISJOINT from the other Wave-2 plan 14-08 (which owns routes/index.tsx + Sidebar.tsx — zero file overlap). Owns a NEW inventoryCsv.ts (mirrors loanCsv's escaping; does NOT import or edit loanCsv.ts). Does NOT touch ItemsListPage.tsx (items export already shipped — photosApi.exportCsv → /export/item) or LoansListPage.tsx (loans already has its client CSV export button). Could share Wave 2 with 14-08 safely (no shared file), but is kept as its own plan for single-writer clarity."
autonomous: true
requirements: [SYS-04]
must_haves:
  truths:
    - "Parity §4: the inventory list page gains an export button — a client-built, formula-injection-safe CSV of the current inventory rows (mirroring loanCsv), downloaded via an object URL"
    - "The items list export (server /export/item via photosApi.exportCsv) and the loans list export (client loanCsv) ALREADY exist — this plan does NOT touch those pages and documents them as already-satisfied parity"
    - "Inventory uses a CLIENT CSV (not a server /export/inventory) because the backend importexport EntityType set is item|location|container|category|label|company|borrower — there is NO /export/inventory or /export/loan endpoint (verified types.go); a server button would 400"
  artifacts:
    - path: frontend2/src/features/inventory/inventoryCsv.ts
      provides: "inventoryToCsvBlob(rows) — formula-injection-safe client CSV of inventory rows"
      contains: "inventoryToCsvBlob"
    - path: frontend2/src/features/inventory/InventoryListPage.tsx
      provides: "the inventory list with an added EXPORT CSV button wired to inventoryToCsvBlob"
      contains: "inventoryToCsvBlob"
  key_links:
    - from: frontend2/src/features/inventory/InventoryListPage.tsx
      to: frontend2/src/features/inventory/inventoryCsv.ts
      via: "EXPORT CSV button → inventoryToCsvBlob(visible) → triggerCsvDownload"
      pattern: "inventoryToCsvBlob"
---

<objective>
Parity §4 — per-entity export buttons on the list pages. Audit + close the ONE gap:
- ITEMS: already done — ItemsListPage's `onExport` calls `photosApi.exportCsv(wsId)` → the server
  `GET /export/item?format=csv` blob (verified photos.ts:128). NO work, do NOT touch the page.
- LOANS: already done — LoansListPage has a client CSV export button (loanCsv → triggerCsvDownload).
  NO work, do NOT touch the page.
- INVENTORY: MISSING — InventoryListPage has NO export button. This plan adds one.

Why a CLIENT CSV for inventory (not a server button): the backend importexport EntityType set is
`item, location, container, category, label, company, borrower` (verified importexport/types.go:31-37,
IsValid). There is NO `/export/inventory` (nor `/export/loan`) endpoint — a server export button for
inventory would hit a 400 invalid-entity-type. So inventory mirrors the loans approach: a client-built,
formula-injection-safe CSV from the already-fetched rows, downloaded via an object URL (no token in a URL).

This plan is the SINGLE WRITER of InventoryListPage.tsx in Phase 14 and is DISJOINT from the other Wave-2
plan 14-08 (routes/index.tsx + Sidebar.tsx) — zero file overlap.

Purpose: complete parity §4 by adding the missing inventory list export.
Output: a NEW inventoryCsv.ts (mirrors loanCsv) + the export button on InventoryListPage + the CSV test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-system-group/14-CONTEXT.md
@.planning/phases/14-system-group/14-VALIDATION.md

# The CSV escaping + download pattern to MIRROR (copy escapeCell/toRow/triggerCsvDownload, do NOT import):
@frontend2/src/features/loans/loanCsv.ts
@frontend2/src/features/loans/LoansListPage.tsx
# The page to EXTEND (single-writer) + its existing FilterBar primaryAction region:
@frontend2/src/features/inventory/InventoryListPage.tsx
# Already-satisfied parity references (do NOT edit — read to confirm the export already exists):
@frontend2/src/features/items/ItemsListPage.tsx
@frontend2/src/lib/api/photos.ts

<interfaces>
<!-- Verified from source this planning session. Use directly; no exploration needed. -->

loanCsv pattern (`@/features/loans/loanCsv`): `escapeCell(value)` (formula-injection-safe: leading
=,+,-,@,\t,\r → `'` prefix, quotes doubled, cell wrapped), `toRow(cells)`, `triggerCsvDownload(blob,
filename)` (object-URL anchor click). COPY these verbatim into inventoryCsv.ts — do NOT import (keeps
loans + inventory CSV builders single-writer-clean).

InventoryListPage: already imports `BevelButton` + `FilterBar` (line 6,11). The FilterBar's
`primaryAction` currently holds only the "⊕ ADD ENTRY" BevelButton (lines 331-335). Add an EXPORT CSV
BevelButton beside it (wrap both in a `<span className="flex items-center gap-sp-2">` like LoansListPage's
primaryAction). `visible` / the filtered row list + the inventory row type are already in scope — reuse them.

money (`@/lib/utils/money`): `formatCents(cents, currency?)` — null-safe; for CSV prefer the raw cents
number for round-trip fidelity (document), guarding null as "".

retroToast (`@/components/retro`): `retroToast.error(t\`Nothing to export.\`)` on an empty export.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: inventoryCsv.ts (formula-injection-safe client CSV) + test</name>
  <files>frontend2/src/features/inventory/inventoryCsv.ts, frontend2/src/features/inventory/inventoryCsv.test.ts</files>
  <behavior>
    inventoryCsv.test.ts:
    - inventoryToCsvBlob([row]) builds a text/csv Blob: header row + one row per inventory entry
    - a cell beginning with "=" (e.g. an item name "=cmd") is prefixed with `'` (formula-injection guard)
    - empty input → a header-only Blob
  </behavior>
  <action>
    inventoryCsv.ts: COPY escapeCell + INJECTION_PREFIXES + toRow + triggerCsvDownload verbatim from
    loanCsv.ts. Define an inventory HEADER (e.g. item, sku, location, container, quantity, condition,
    status, purchase_price, currency) matching the inventory row fields already rendered in
    InventoryListPage (read the page's table cells to pick the exact fields/getters). Export
    `inventoryToCsvBlob(rows): Blob` mapping each row to escaped cells (purchase_price as raw cents for
    fidelity; null → ""). Empty input → header-only Blob. Type the param off the inventory row type the page
    already uses (import the same type the page imports — do NOT invent a new shape).
  </action>
  <verify>
    <automated>cd frontend2 && bun run test src/features/inventory/inventoryCsv.test.ts</automated>
  </verify>
  <done>CSV test green: header + rows; a leading "=" cell is `'`-prefixed; empty → header-only Blob.</done>
</task>

<task type="auto">
  <name>Task 2: add the EXPORT CSV button to InventoryListPage (single-writer, minimal diff)</name>
  <files>frontend2/src/features/inventory/InventoryListPage.tsx</files>
  <action>
    SINGLE-WRITER, minimal-diff extension (do NOT rewrite the page):
    - import `{ inventoryToCsvBlob, triggerCsvDownload }` from `./inventoryCsv` and `retroToast` from
      `@/components/retro` (if not already imported).
    - add an `exportCsv` `useCallback` (mirror LoansListPage lines 106-112): if the filtered/visible row
      list is empty → `retroToast.error(tRef.current\`Nothing to export.\`)` and return (use the page's
      existing tRef if present; if the page has no tRef yet, use the in-scope `t` — but if you add a
      shortcut do NOT regress the render-loop guard); else `triggerCsvDownload(inventoryToCsvBlob(visible),
      "inventory.csv")`. Deps = the visible-rows variable only (stable).
    - in the FilterBar `primaryAction` (lines 331-335), wrap the existing "⊕ ADD ENTRY" BevelButton and a
      NEW `<BevelButton onClick={exportCsv} disabled={<empty>} aria-disabled={…}><Trans>⤓ EXPORT CSV</Trans>
      </BevelButton>` in a `<span className="flex items-center gap-sp-2">` (mirror LoansListPage's
      primaryAction composition exactly). The export button comes BEFORE the mint ADD button.
    - all new strings via <Trans>/t. Do NOT add a route shortcut for export this plan (keep the diff small
      and render-loop-safe) unless the page already has a stable shortcut memo to extend.
    Preserve every existing inventory query/filter/table behavior unchanged.
  </action>
  <verify>
    <automated>cd frontend2 && bun run lint:tsc && bun run test src/features/inventory/InventoryListPage.test.tsx</automated>
  </verify>
  <done>tsc clean + the existing InventoryListPage test still green; the EXPORT CSV button renders in the FilterBar primaryAction and is disabled on an empty list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inventory rows → client CSV (formula-evaluating sink) | item/sku/location/condition cells flow into a spreadsheet app; every cell is formula-injection-escaped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-19 | Tampering | CSV/formula injection via item or location names in the inventory export | mitigate | inventoryCsv mirrors loanCsv's escapeCell — leading =,+,-,@,\t,\r get a `'` prefix, quotes doubled, cell wrapped; the CSV test asserts the guard (T-08-CSV parity) |
| T-14-20 | Information Disclosure | a token riding a download URL | mitigate | the CSV is built in-memory from already-fetched rows and streamed via an object URL — no network download link, no token in a URL (T-08-TOKEN parity). Inventory uses CLIENT CSV (no server /export/inventory exists) |
| T-14-SC | Tampering | npm installs | mitigate | none — installs NO packages (composes the existing loanCsv pattern + retro atoms) |
</threat_model>

<verification>
- `cd frontend2 && bun run lint:tsc` clean (NOT bare `tsc`).
- `bun run test src/features/inventory/inventoryCsv.test.ts src/features/inventory/InventoryListPage.test.tsx` green.
- The inventory export is a CLIENT CSV (no server /export/inventory); items (server) + loans (client) exports
  confirmed already present and untouched.
</verification>

<success_criteria>
- Parity §4: the inventory list page has an export button (formula-injection-safe client CSV via object URL);
  items + loans list exports already exist and are documented as already-satisfied (not re-touched).
</success_criteria>

<output>
Create `.planning/phases/14-system-group/14-07-SUMMARY.md` when done (record that inventory now exports via a
client CSV, that items/loans were already covered, and the rationale for client-vs-server export per entity —
no /export/inventory or /export/loan endpoint exists).
</output>
