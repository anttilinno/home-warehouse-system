---
phase: 09-borrowers
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - frontend2/src/features/borrowers/BorrowersListPage.tsx
  - frontend2/src/features/borrowers/BorrowersListPage.test.tsx
  - frontend2/src/components/layout/Sidebar.tsx
  - frontend2/src/routes/index.tsx
autonomous: true
requirements: [BORR-01]
must_haves:
  truths:
    - "Navigating to /borrowers renders a mint Window list with FilterBar + RetroTable + RetroPagination"
    - "Typing in the search box client-filters the loaded rows (no /borrowers/search call)"
    - "RetroPagination pages through the client-computed pageCount (PER_PAGE=25)"
    - "The NEW BORROWER CTA and the N shortcut navigate to /borrowers/new"
    - "Clicking a row navigates to /borrowers/:id; the row actions cell stops propagation"
    - "The Sidebar Borrowers entry is enabled and links to /borrowers"
    - "The /borrowers list route is registered in routes/index.tsx"
  artifacts:
    - path: "frontend2/src/features/borrowers/BorrowersListPage.tsx"
      provides: "BORR-01 list surface (search + table + pager + CTA)"
      contains: "useBorrowersQuery"
      min_lines: 80
    - path: "frontend2/src/features/borrowers/BorrowersListPage.test.tsx"
      provides: "BORR-01 component coverage"
      contains: "BorrowersListPage"
  key_links:
    - from: "frontend2/src/features/borrowers/BorrowersListPage.tsx"
      to: "useBorrowersQuery"
      via: "rows/page/pageCount"
      pattern: "useBorrowersQuery"
    - from: "frontend2/src/routes/index.tsx"
      to: "BorrowersListPage"
      via: "Route path=borrowers"
      pattern: "path=\"borrowers\""
    - from: "frontend2/src/components/layout/Sidebar.tsx"
      to: "/borrowers"
      via: "NavItem to prop"
      pattern: "to=\"/borrowers\""
---

<objective>
Ship the BORR-01 borrower list page: a mint Window with a FilterBar (client
search + count + NEW BORROWER CTA), a RetroTable (Name / Contact / Loans / row
actions), and a RetroPagination fed a client-computed pageCount. Enable the
Sidebar Borrowers nav and register the `/borrowers` list route.

Purpose: This is the browse surface — a near-clone of `LoansListPage` /
`InventoryListPage`.

Output: `BorrowersListPage.tsx` (+ test), Sidebar nav flip, the `/borrowers`
list route registration.

SHARED-FILE OWNERSHIP (Phase-8 BLOCKER 1 — single-writer/serialize rule):
- `routes/index.tsx` is shared with plan 09-03. THIS plan (Wave 2) registers
  ONLY `<Route path="borrowers" element={<BorrowersListPage />} />`. Plan 09-03
  (Wave 3, LATER) registers `/borrowers/new`, `/borrowers/:id`,
  `/borrowers/:id/edit` alongside the pages those routes point to (so each route
  is compilable at its own gate, and the literal `borrowers/new` is registered
  ABOVE the `:id` param per AP-1). The two edits merge SEQUENTIALLY across waves
  — never two parallel worktrees on routes/index.tsx.
- `Sidebar.tsx` is touched ONLY by this plan (the single nav flip).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-borrowers/09-RESEARCH.md
@.planning/phases/09-borrowers/09-UI-SPEC.md
@.planning/phases/09-borrowers/09-VALIDATION.md
@.planning/phases/09-borrowers/09-01-SUMMARY.md

# Mirror these shipped files exactly (parity)
@frontend2/src/features/loans/LoansListPage.tsx
@frontend2/src/features/inventory/InventoryListPage.tsx
@frontend2/src/features/loans/LoansListPage.test.tsx
@frontend2/src/routes/index.tsx
@frontend2/src/components/layout/Sidebar.tsx

<interfaces>
<!-- From plan 09-01 (this wave's dependency) + shipped atoms. -->

From features/borrowers/hooks/useBorrowersQuery.ts (09-01):
  export const BORROWERS_PER_PAGE = 25;
  export function useBorrowersQuery(search: string): { all, isLoading, isError, page, pageCount, rows }
  // rows = current page slice post-search; page already clamped to pageCount.

From test/msw/borrowerHandlers.ts (09-01): borrowerHandlers — add via server.use in the test (NOT global), per loans convention.

Retro atoms (all from @/components/retro): Window (titlebarVariant, actions), BevelButton (variant), RetroTable, RetroPagination (page, pageCount, perPage, onPageChange), FilterBar (searchValue, onSearchChange, searchPlaceholder, facets, itemCount, filterChips, onRemoveFilter, onClearAll, primaryAction), RetroBadge (variant), RetroEmptyState (eyebrow, glyph, heading, body, action), useShortcuts from @/components/shortcuts.

Sidebar.tsx line ~145: <NavItem glyph="☺" label={<Trans>Borrowers</Trans>} count={stats?.total_borrowers} /> — add to="/borrowers" (this is the SINGLE nav change; glyph + count already wired).

routes/index.tsx: library-mode RR7, literal-before-param (AP-1). Loans precedent at lines ~69-73.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BorrowersListPage (search + table + pager + CTA)</name>
  <files>frontend2/src/features/borrowers/BorrowersListPage.tsx, frontend2/src/features/borrowers/BorrowersListPage.test.tsx</files>
  <behavior>
    - Renders a row per borrower returned by the (mocked) list endpoint
    - Typing in the search box filters rows by name substring (client-side; no /borrowers/search request fires)
    - With >25 borrowers, RetroPagination renders and clicking page 2 shows the next slice
    - Clicking NEW BORROWER navigates to /borrowers/new
    - Empty data → NO BORROWERS empty state with ADD BORROWER action; empty search match → NO MATCHES with CLEAR ALL
    - Row click navigates to /borrowers/:id; the actions cell EDIT button does NOT trigger the row navigation (stopPropagation)
  </behavior>
  <action>Create `BorrowersListPage.tsx` cloning `LoansListPage.tsx` density. Mint Window title={t`BORROWERS — ${workspaceName}`} titlebarVariant="mint". Local `search` state; call useBorrowersQuery(search) for { rows, page, pageCount, isLoading, isError }. FilterBar: searchPlaceholder={t`Filter borrowers…`}, itemCount (mono "{n} borrowers"), facets={[]} (borrowers have no enum facets — 09-UI-SPEC §Surface 1), filterChips reflecting an active search with CLEAR ALL, primaryAction = <BevelButton variant="mint" onClick={goNew}>⊕ NEW BORROWER</BevelButton>. RetroTable 4 columns per 09-UI-SPEC §Surface 1: Name (font-semibold), Contact (email ?? phone ?? muted "—", font-mono), Loans (muted "—" placeholder — NO per-row count / NO N+1 fan-out, OQ7 binding override #2/§Data Strategy), and an aria-hidden actions <th> with a per-row EDIT BevelButton (neutral) that calls e.stopPropagation() and navigates to /borrowers/:id/edit. Whole <tr> cursor-pointer, onClick → navigate(`/borrowers/${b.id}`). RetroPagination fed page/pageCount/perPage={BORROWERS_PER_PAGE}, onPageChange writes ?page to the URL (mirror InventoryListPage feed). Empty/loading/error states + copy verbatim from 09-UI-SPEC §Surface 1 (NO BORROWERS / NO MATCHES / Loading… / Couldn't load borrowers). RENDER-LOOP GUARD (constraint 5, hit 4×): const tRef = useRef(t); tRef.current = t; read tRef.current inside the useShortcuts memo; deps are STABLE callbacks only, NEVER `t`. Shortcuts: N → goNew (navigate /borrowers/new), / → focus search input. Then `BorrowersListPage.test.tsx` mirroring LoansListPage.test.tsx: render inside the test providers + router, server.use(...borrowerHandlers) (per-test, not global), assert the behavior cases above (rows render, search filters with NO /borrowers/search request, pager pages, CTA navigates, empty/no-match states).</action>
  <verify>
    <automated>cd frontend2 && bun run test BorrowersListPage --run</automated>
  </verify>
  <done>List renders rows, client search filters (no /search call), pager pages, CTA + N navigate to /borrowers/new, row click → detail, empty/no-match states render; render-loop guard in place.</done>
</task>

<task type="auto">
  <name>Task 2: Enable Sidebar nav + register /borrowers list route</name>
  <files>frontend2/src/components/layout/Sidebar.tsx, frontend2/src/routes/index.tsx</files>
  <action>Two edits, both narrow.

1. `Sidebar.tsx` (~line 145): add `to="/borrowers"` to the existing Borrowers NavItem (glyph="☺", count={stats?.total_borrowers} already present). This is the ONLY nav edit in the phase.

2. `routes/index.tsx`: import { BorrowersListPage } from "@/features/borrowers/BorrowersListPage". Inside the authenticated AppShell branch, register EXACTLY ONE route: `<Route path="borrowers" element={<BorrowersListPage />} />`. Add a short comment, mirroring the loans block (~lines 69-73), noting that the borrower form/detail routes (`borrowers/new`, `borrowers/:id/edit`, `borrowers/:id`) are added by plan 09-03 with `borrowers/new` registered ABOVE the `:id` param per AP-1 (literal-before-param). DO NOT register `/borrowers/new`, `/borrowers/:id`, or `/borrowers/:id/edit` here — those routes (and the pages they point to) belong to plan 09-03 (later wave; this shared file merges sequentially). DO NOT import BorrowerFormPage or BorrowerDetailPage here (they do not exist yet — that would break tsc at this gate).</action>
  <verify>
    <automated>cd frontend2 && bun run lint:tsc 2>&1 | grep -v '^#' | grep -iE 'routes/index|Sidebar|error TS' || echo "tsc clean for routes + sidebar"</automated>
  </verify>
  <done>Sidebar Borrowers links to /borrowers; routes/index.tsx registers ONLY the /borrowers list route; no form/detail imports; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Search string is client-only (never sent); list read is workspace-scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Information Disclosure | List read | mitigate | Workspace-scoped list via useBorrowersQuery (wsId from useWorkspace); no client-supplied ws. |
| T-09-04 | Tampering | Borrower name rendering | mitigate | React escapes by default; no dangerouslySetInnerHTML (mirrors LoansListPage). |
| T-09-SC | Tampering | npm installs | accept | Zero new packages this phase (RESEARCH §Package Legitimacy Audit). |
</threat_model>

<verification>
- `bun run test BorrowersListPage --run` green
- `bun run lint:tsc` clean for routes/index.tsx + Sidebar.tsx
- Phase gate (09-03 E2E): /borrowers reachable from the sidebar; NEW BORROWER → /borrowers/new
</verification>

<success_criteria>
- /borrowers renders the mint list (search + table + pager + CTA)
- Search client-filters (no /borrowers/search call); pager uses client pageCount (PER_PAGE=25)
- NEW BORROWER + N navigate to /borrowers/new; row click → /borrowers/:id
- Sidebar Borrowers entry enabled (to="/borrowers")
- ONLY the /borrowers list route registered here; render-loop guard mirrored from InventoryListPage
</success_criteria>

<output>
Create `.planning/phases/09-borrowers/09-02-SUMMARY.md` when done.
</output>
