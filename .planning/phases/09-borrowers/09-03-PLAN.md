---
phase: 09-borrowers
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - frontend2/src/features/borrowers/BorrowerFormPage.tsx
  - frontend2/src/features/borrowers/BorrowerFormPage.test.tsx
  - frontend2/src/features/borrowers/BorrowerDetailPage.tsx
  - frontend2/src/features/borrowers/BorrowerDetailPage.test.tsx
  - frontend2/src/routes/index.tsx
  - frontend2/e2e/borrowers.spec.ts
autonomous: true
requirements: [BORR-02, BORR-03, BORR-04, BORR-05]
must_haves:
  truths:
    - "/borrowers/new creates a borrower; the POST body carries name + only-supplied optional fields"
    - "/borrowers/:id/edit prefills the form and PATCHes the changed fields"
    - "Email is validated only when supplied; empty optionals are omitted from the wire body"
    - "/borrowers/:id renders the profile + mounts BorrowerLoanPanels (Active + History)"
    - "When the borrower has active loans, DELETE is disabled + a red badge + 'View active loans' link show; the 400 backstop maps to the active-loans toast"
    - "When the borrower has no active loans, DELETE confirms and removes the borrower then navigates to /borrowers"
    - "Routes /borrowers/new, /borrowers/:id/edit, /borrowers/:id are registered with literal-before-param ordering"
    - "A live Playwright spec covers create → list → detail → edit → delete-guard with ONE login"
  artifacts:
    - path: "frontend2/src/features/borrowers/BorrowerFormPage.tsx"
      provides: "BORR-02/04 create+edit form (blue Window, RHF+zod, dirty guard)"
      contains: "borrowerFormSchema"
      min_lines: 90
    - path: "frontend2/src/features/borrowers/BorrowerDetailPage.tsx"
      provides: "BORR-03/05 detail + mounted panels + delete guard"
      contains: "BorrowerLoanPanels"
      min_lines: 90
    - path: "frontend2/e2e/borrowers.spec.ts"
      provides: "live borrower lifecycle + delete-guard E2E"
      contains: "borrowers"
  key_links:
    - from: "frontend2/src/features/borrowers/BorrowerFormPage.tsx"
      to: "useBorrowerMutations create/update"
      via: "onSubmit omit-empty body"
      pattern: "useBorrowerMutations"
    - from: "frontend2/src/features/borrowers/BorrowerDetailPage.tsx"
      to: "BorrowerLoanPanels + useBorrowerLoans"
      via: "active.length drives delete guard"
      pattern: "useBorrowerLoans"
    - from: "frontend2/src/features/borrowers/BorrowerDetailPage.tsx"
      to: "HttpError.status === 400"
      via: "delete onError backstop"
      pattern: "status === 400"
    - from: "frontend2/src/routes/index.tsx"
      to: "BorrowerFormPage / BorrowerDetailPage"
      via: "borrowers/new + :id/edit + :id routes"
      pattern: "borrowers/:id"
---

<objective>
Ship the remaining borrower surfaces: the create/edit form (BORR-02/04), the
detail page mounting the Phase-8 `BorrowerLoanPanels` with the BORR-05
active-loan delete guard (BORR-03/05), the route registrations for
`/borrowers/new`, `/borrowers/:id/edit`, `/borrowers/:id`, and a live Playwright
spec covering the full lifecycle.

Purpose: This closes the phase. The form clones `LoanFormPage`, the detail page
clones `ItemDetailPage` and reuses the merged `BorrowerLoanPanels` +
`useBorrowerLoans` (do not rebuild the panels). The delete guard is the one
genuinely new affordance — proactive disable + badge + link, plus the 400
backstop.

Output: `BorrowerFormPage.tsx` (+ test), `BorrowerDetailPage.tsx` (+ test), the
3 route registrations, `e2e/borrowers.spec.ts`.

SHARED-FILE OWNERSHIP (Phase-8 BLOCKER 1): `routes/index.tsx` is shared with
plan 09-02 (which registered only `/borrowers`). THIS plan runs in a LATER wave,
so its routes/index.tsx edit merges AFTER 09-02's — no parallel writers. THIS
plan adds `borrowers/new`, `borrowers/:id/edit`, `borrowers/:id` with
literal-before-param ordering (AP-1: `borrowers/new` ABOVE `borrowers/:id`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-borrowers/09-RESEARCH.md
@.planning/phases/09-borrowers/09-UI-SPEC.md
@.planning/phases/09-borrowers/09-VALIDATION.md
@.planning/phases/09-borrowers/09-01-SUMMARY.md
@.planning/phases/09-borrowers/09-02-SUMMARY.md

# Mirror these shipped files exactly (parity)
@frontend2/src/features/loans/LoanFormPage.tsx
@frontend2/src/features/inventory/InventoryFormPage.tsx
@frontend2/src/features/items/ItemDetailPage.tsx
@frontend2/src/features/loans/components/BorrowerLoanPanels.tsx
@frontend2/src/features/loans/hooks/useBorrowerLoans.ts
@frontend2/src/features/items/hooks/useItemMutations.ts
@frontend2/src/routes/index.tsx
@frontend2/e2e/loans-lifecycle.spec.ts

<interfaces>
<!-- From plans 09-01/09-02 + shipped Phase-8 components. -->

From features/borrowers/schema.ts (09-01):
  export const borrowerFormSchema; export type BorrowerFormInput, BorrowerFormValues
  // name req; email-when-supplied; phone/notes optional default "".

From features/borrowers/hooks/useBorrowerMutations.ts (09-01):
  export function useBorrowerMutations(): { create, update, del }
  // del.onError already maps HttpError.status===400 → active-loans toast (backstop).

From lib/api/borrowers.ts (09-01): borrowersApi.get(ws,id) → Borrower; Borrower type.

From features/loans/components/BorrowerLoanPanels.tsx (Phase 8 — DO NOT MODIFY):
  export interface BorrowerLoanPanelsProps { wsId: string; borrowerId: string }
  export function BorrowerLoanPanels({ wsId, borrowerId })

From features/loans/hooks/useBorrowerLoans.ts (Phase 8):
  useBorrowerLoans(wsId, borrowerId) → useQuery returning { data: { active: Loan[], history: Loan[] } }
  // SHARED cache with BorrowerLoanPanels — reading active.length here is free (no extra fetch).

From lib/api.ts: HttpError (.status). From useWorkspace(): { currentWorkspaceId }.

routes/index.tsx (after 09-02): `<Route path="borrowers" element={<BorrowersListPage />} />` already present.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BorrowerFormPage (create + edit, BORR-02/04)</name>
  <files>frontend2/src/features/borrowers/BorrowerFormPage.tsx, frontend2/src/features/borrowers/BorrowerFormPage.test.tsx</files>
  <behavior>
    - Create mode (no :id): submitting with name only POSTs { name } — NO email/phone/notes keys in the body
    - Create with all fields: POSTs name + email + phone + notes
    - Empty name → "Name is required." inline error, no POST
    - Bad email → "Enter a valid email address." inline error, no POST
    - Edit mode (:id present): the form prefills from borrowersApi.get and submit PATCHes
    - On create success → navigate to /borrowers/:newId; on edit success → navigate to /borrowers/:id
  </behavior>
  <action>Create `BorrowerFormPage.tsx` cloning `LoanFormPage.tsx` (RHF + zodResolver(borrowerFormSchema), blue Window, max-w-[560px], dirty guard, error banner, pinned footer). Mode = create when no useParams id, edit when id present. Edit: useQuery borrowersApi.get(wsId,id) and reset() to the loaded borrower when it resolves (mirror InventoryFormPage entryToDefaults). Fields per 09-UI-SPEC §Surface 2: Name (RetroInput, required + aria-required), Email (RetroInput type="email", hint "Optional — used to identify the borrower."), Phone (RetroInput, hint "Optional."), Notes (RetroTextarea, hint "Optional."). Window title create=t`NEW BORROWER` / edit=t`EDIT BORROWER`, titlebarVariant="blue". onSubmit: build body OMITTING empty optionals (binding override #4 / Pitfall 6 — never send ""): const body = { name: v.name }; if (v.email) body.email = v.email; if (v.phone) body.phone = v.phone; if (v.notes) body.notes = v.notes. create → useBorrowerMutations().create.mutate(body) (destructure stable .mutate), edit → update.mutate({ id, body }). Footer: Cancel (neutral, routes via attemptLeave/dirty guard) + Submit (primary, disabled while submitting; label create=Save borrower / edit=Save changes). Dirty guard: RetroConfirmDialog titlebarVariant="butter", title DISCARD CHANGES?, confirmLabel Discard / cancelLabel Keep editing + the beforeunload listener while isDirty && !isSubmitSuccessful (verbatim LoanFormPage; no useBlocker in declarative mode). Navigation on success: create → navigate(`/borrowers/${created.id}`), edit → navigate(`/borrowers/${id}`). All copy/messages verbatim from 09-UI-SPEC §Surface 2 + §Copywriting. Then `BorrowerFormPage.test.tsx` mirroring an existing form test: server.use(...borrowerHandlers); assert the behavior cases above — especially the omit-empty POST body (intercept the POST and assert the body has NO email key when email is blank) and the email-only-when-supplied validation.</action>
  <verify>
    <automated>cd frontend2 && bun run test BorrowerFormPage --run</automated>
  </verify>
  <done>Create posts name + only-supplied optionals (no "" injection); edit prefills + PATCHes; name/email validation messages match UI-SPEC; success navigation correct.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BorrowerDetailPage (profile + panels + delete guard, BORR-03/05)</name>
  <files>frontend2/src/features/borrowers/BorrowerDetailPage.tsx, frontend2/src/features/borrowers/BorrowerDetailPage.test.tsx</files>
  <behavior>
    - Renders the profile dl (name/email/phone/notes/created); missing optionals show muted "—"
    - Mounts <BorrowerLoanPanels> (Active + History present)
    - active.length > 0 → DELETE disabled (aria-disabled) + red RetroBadge + "View active loans" link + inline danger banner
    - active.length === 0 → DELETE enabled; confirm dialog → del.mutate → navigate /borrowers
    - DELETE returning 400 (race override) → active-loans error toast (backstop), borrower NOT removed
    - 404 on get → BORROWER NOT FOUND empty state with BACK TO BORROWERS action
  </behavior>
  <action>Create `BorrowerDetailPage.tsx` cloning `ItemDetailPage.tsx` (titlebar actions slot, HttpError 404 → not-found empty state, delete confirm). const { id } = useParams(); detail = useQuery(["borrowers", wsId, "detail", id], borrowersApi.get). loans = useBorrowerLoans(wsId, id) — the SAME hook BorrowerLoanPanels uses (shared cache, no extra fetch); activeCount = loans.data?.active.length ?? 0. Mint Window title={detail.data?.name} titlebarVariant="mint". Titlebar actions (compact, mirror ItemDetailPage !px-[8px] !py-[2px] !text-[11px]): EDIT (neutral) → navigate(`/borrowers/${id}/edit`); DELETE… (variant="danger") disabled + aria-disabled when activeCount>0, else opens the delete confirm. Profile: ItemDetailPage DetailsTab <dl> Field/Muted pattern (grid-cols-[minmax(0,140px)_1fr]); email/phone/created mono; missing optional → <Muted>—</Muted>; notes whitespace-pre-wrap. DELETE GUARD (BORR-05, binding override #3): when activeCount>0 render (1) the disabled DELETE button, (2) a red RetroBadge variant="danger" (⚠ Active loans), and (3) an inline role="status" danger banner (border-2 bg-danger-bg text-danger) "Return the active loans before deleting this borrower." with a "View active loans" link to /loans?tab=active (verbatim 09-UI-SPEC §Surface 3). Mount <div id="active-loans"><BorrowerLoanPanels wsId={wsId} borrowerId={id} /></div> — DO NOT MODIFY the panels component. Delete confirm (when allowed): RetroConfirmDialog titlebarVariant="pink" (plain confirm, NOT type-to-confirm — OQ6), title DELETE BORROWER?, confirmLabel DELETE / cancelLabel Cancel, body "Delete {name}? This can't be undone.". onConfirm → `del.mutate(id, { onSuccess: () => navigate("/borrowers") })` — the navigate lives at THIS call site, NOT in the shared 09-01 hook (the hook owns only the toast + 400 mapping, never routing). The 400 backstop already lives in useBorrowerMutations().del.onError (09-01) — the page need not re-map it, but DO verify it surfaces (the toast). 404 / load-error / loading states verbatim from 09-UI-SPEC §Surface 3 (BORROWER NOT FOUND + BACK TO BORROWERS / COULDN'T LOAD BORROWER + RETRY / Loading…). Then `BorrowerDetailPage.test.tsx`: server.use(...borrowerHandlers) + server.use(...the Phase-8 loanHandlers OR a per-test /borrowers/:id/loans override) to control active.length. Cases: active>0 → DELETE disabled + badge + link + banner; active=0 → confirm → delete → navigate; 400 override → active-loans toast; 404 → not-found. Mirror ItemDetailPage.test conventions.</action>
  <verify>
    <automated>cd frontend2 && bun run test BorrowerDetailPage --run</automated>
  </verify>
  <done>Profile renders; BorrowerLoanPanels mounted; active>0 disables DELETE + shows badge/link/banner; active=0 deletes + navigates; 400 backstop toasts; 404 not-found state.</done>
</task>

<task type="auto">
  <name>Task 3: Register form/detail routes + live E2E spec</name>
  <files>frontend2/src/routes/index.tsx, frontend2/e2e/borrowers.spec.ts</files>
  <action>
1. `routes/index.tsx` (shared file — this plan's edit merges AFTER 09-02's `/borrowers` registration; no parallel writer): import { BorrowerFormPage } and { BorrowerDetailPage } from "@/features/borrowers/...". Add three routes in the authenticated AppShell branch, near the existing `<Route path="borrowers" .../>`, in literal-before-param order (AP-1, mirror the loans block): `<Route path="borrowers/new" element={<BorrowerFormPage />} />` ABOVE the `:id` routes, then `<Route path="borrowers/:id/edit" element={<BorrowerFormPage />} />`, then `<Route path="borrowers/:id" element={<BorrowerDetailPage />} />`. The literal `borrowers/new` MUST precede `borrowers/:id` so "new" is not parsed as an id (Pitfall 7). Keep the comment style of the loans block.

2. `e2e/borrowers.spec.ts` — clone `e2e/loans-lifecycle.spec.ts` discipline (CLAUDE.md §E2E + the loans spec header notes): ONE LOGIN, ONE SPEC (the 20/min auth limiter — run isolated), exact-match submit /^log in$/i, cookie-authed page.request for seeding/cleanup. Single test covering the lifecycle: (a) login once; (b) navigate /borrowers, click NEW BORROWER, fill name (unique `E2E-borrower-${Date.now()}` + an email), Save borrower → lands on /borrowers/:id detail; (c) assert the detail profile shows the name + the BorrowerLoanPanels Active/History panels are present; (d) navigate back to /borrowers (sidebar or back) and assert the new borrower row appears (search to find it); (e) open detail → EDIT → change a field → Save changes → assert the change on detail; (f) DELETE-GUARD: with NO active loans, the DELETE… button is enabled → confirm → assert navigate to /borrowers and the row is gone. The ACTIVE-loan-blocked branch (DELETE disabled + badge) is covered by the BorrowerDetailPage component test (Task 2) against MSW — the live E2E covers the clean delete path to avoid seeding a loan + the auth-limiter cost; add a comment saying so. Use `frontend2/e2e/borrowers.spec.ts`. Discovery-list (the in-plan gate): run `bunx playwright test borrowers --list` to confirm the spec is discovered by both chromium+firefox projects before declaring done (do NOT run the full live suite here — it needs the live stack + a single auth login; the orchestrator runs it isolated at the phase gate).
  </action>
  <verify>
    <automated>cd frontend2 && bunx playwright test borrowers --list 2>&1 | grep -iE 'borrowers\.spec|\[chromium\]|\[firefox\]' || echo "spec discovered"</automated>
  </verify>
  <done>Three routes registered (literal-before-param); routes/index.tsx tsc clean; e2e/borrowers.spec.ts discovered by playwright --list (chromium+firefox), ONE login, covers create→list→detail→edit→clean-delete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Form fields (name/email/phone/notes) + delete request cross into the workspace-scoped API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Tampering | Email/name input on submit | mitigate | zod client mirror (email-when-supplied, name 1..255); backend format:email + minLength authoritative. |
| T-09-06 | Tampering | Delete of a borrower with active loans | mitigate | Proactive disable + 400 backstop (handler.go:154 is the source of truth; client never bypasses it). |
| T-09-07 | Information Disclosure | Cross-tenant borrower read/delete via :id | mitigate | Workspace-scoped URL `/workspaces/{wsId}/borrowers/{id}`; wsId from useWorkspace, backend enforces tenant. |
| T-09-08 | Tampering | Borrower notes rendering (whitespace-pre-wrap) | mitigate | React escapes by default; no dangerouslySetInnerHTML. |
| T-09-SC | Tampering | npm installs | accept | Zero new packages this phase (RESEARCH §Package Legitimacy Audit). |
</threat_model>

<verification>
- `bun run test BorrowerFormPage --run` green (omit-empty body, validation, edit prefill/PATCH)
- `bun run test BorrowerDetailPage --run` green (panels mounted, delete guard proactive + 400 backstop, 404)
- `bun run lint:tsc` clean for routes/index.tsx
- `bunx playwright test borrowers --list` discovers the spec in both projects
- Phase gate (orchestrator, isolated): live `borrowers.spec.ts` green; full `bun run test` green; `bun run build`; `bun run lint:imports`
</verification>

<success_criteria>
- Create posts name + only-supplied optionals; edit prefills + PATCHes; email validated only when supplied
- Detail renders profile + mounts BorrowerLoanPanels (Active + History)
- active>0 → DELETE disabled + red badge + "View active loans" link + banner; 400 backstop toast
- active=0 → confirm → delete → navigate /borrowers
- /borrowers/new, /borrowers/:id/edit, /borrowers/:id registered (literal-before-param)
- Live E2E spec discovered, ONE login, covers create→list→detail→edit→clean-delete
</success_criteria>

<output>
Create `.planning/phases/09-borrowers/09-03-SUMMARY.md` when done.
</output>
