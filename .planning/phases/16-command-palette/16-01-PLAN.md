---
phase: 16-command-palette
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend2/package.json
  - frontend2/bun.lock
  - frontend2/vite.config.ts
autonomous: false
requirements: [TUI-05]
user_setup: []

must_haves:
  truths:
    - "cmdk@1.1.1 and tinykeys@4.0.0 are dependencies of frontend2 and resolve cleanly on React 19 with no peer warnings"
    - "@radix-ui/react-dialog is hoisted in bun.lock (pulled transitively by cmdk) — no separate install"
    - "vite's manualChunks function routes BOTH cmdk AND @radix-ui/react-dialog into a `palette` chunk, mirroring the scanner/charts isolate"
    - "command-score is NOT a direct dependency (vendored inside cmdk)"
  artifacts:
    - path: frontend2/package.json
      provides: "cmdk + tinykeys in dependencies; command-score absent"
      contains: "cmdk"
    - path: frontend2/bun.lock
      provides: "updated lockfile pinning cmdk@1.1.1, tinykeys@4.0.0, and the hoisted @radix-ui/react-dialog tree"
      contains: "cmdk"
    - path: frontend2/vite.config.ts
      provides: "manualChunks branch: cmdk / @radix-ui/react-dialog -> \"palette\""
      contains: "palette"
  key_links:
    - from: frontend2/vite.config.ts
      to: cmdk
      via: "manualChunks (id) => id.includes('cmdk') || id.includes('@radix-ui/react-dialog') ? 'palette' : ..."
      pattern: "\"palette\""
---

<objective>
The dependency-and-chunk foundation for the command palette (TUI-05). Add `cmdk@1.1.1` +
`tinykeys@4.0.0` to frontend2, run a NORMAL `bun install` to update + commit `bun.lock`, and add a
`palette` manualChunks branch to `vite.config.ts` that isolates BOTH `cmdk` AND
`@radix-ui/react-dialog` (cmdk hard-imports radix-dialog at module top level — OVERRIDE B — so without
the explicit rule radix-dialog can leak into the entry chunk).

This is the ONLY Wave-1 plan and the ONLY plan that runs a non-frozen `bun install`. Every downstream
plan runs `bun install --frozen-lockfile`, so the lockfile MUST land here first. This plan owns two of
the milestone single-writer files (`package.json`+`bun.lock`, `vite.config.ts`) — no other Phase-16
plan touches them.

Purpose: TUI-05 / POL-04 — make cmdk + tinykeys available and pre-wire the lazy `palette` chunk so the
~15-20KB gz of cmdk + radix-dialog never inflates the entry bundle.
Output: updated package.json + bun.lock; the `palette` manualChunks branch in vite.config.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-command-palette/16-CONTEXT.md
@.planning/phases/16-command-palette/16-RESEARCH.md

# The single-writer file to edit + the manualChunks idiom to mirror EXACTLY:
@frontend2/vite.config.ts

<interfaces>
<!-- Verified from source this planning session. Use directly; no exploration needed. -->

vite.config.ts — the manualChunks FUNCTION (lines 55-94, Vite 8/rolldown — MUST stay a function
`(id) => string | undefined`, NOT a record). The existing body has a `scannerModules` array + a
`scanner` return, then a `chartModules` array + a `charts` return, then `return undefined`. ADD a
parallel `paletteModules` array `["cmdk", "@radix-ui/react-dialog"]` + an
`if (paletteModules.some((mod) => id.includes(mod))) return "palette";` branch BEFORE
`return undefined`, mirroring the scanner/charts blocks (same data-structure-as-membership style + an
explanatory comment noting TUI-05 / POL-04 + OVERRIDE B: cmdk hard-imports @radix-ui/react-dialog at
module top level so radix-dialog MUST be co-located in the `palette` chunk or it leaks into entry).

Verified dep facts (16-RESEARCH):
- cmdk latest = 1.1.1, peer `react: ^18 || ^19 || ^19.0.0-rc` — clean on React 19.2, no postinstall.
- tinykeys latest = 4.0.0, ESM-first, no postinstall.
- command-score is VENDORED inside cmdk — do NOT `bun add` it.
- @radix-ui/react-dialog arrives transitively via cmdk — do NOT add it directly; confirm it hoists.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package-legitimacy gate (cmdk + tinykeys)</name>
  <action>Verify cmdk + tinykeys on npmjs.com (see how-to-verify) before any install; this is a BLOCKING-HUMAN gate because slopcheck was unavailable so both packages are [ASSUMED].</action>
  <what-built>NOT YET BUILT — this is the package-legitimacy gate that MUST clear BEFORE the install
  task runs. 16-RESEARCH's Package Legitimacy Audit could not run slopcheck in-environment, so both
  packages are `[ASSUMED]` per protocol and require human verification before install.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/cmdk — confirm: latest is 1.1.1, source repo
       github.com/pacocoursey/cmdk, ~40M weekly downloads, no postinstall script. (Already shipped in
       the sibling legacy `frontend/` — strong trust signal.)
    2. Open https://www.npmjs.com/package/tinykeys — confirm: latest is 4.0.0, source repo
       github.com/jamiebuilds/tinykeys, ~200K weekly downloads, no postinstall script.
    3. Confirm `command-score` will NOT be installed (it is vendored inside cmdk).
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the `bun add` in Task 2, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add cmdk + tinykeys; update + commit bun.lock</name>
  <files>frontend2/package.json, frontend2/bun.lock</files>
  <action>
    From the explicit cwd `/home/antti/Repos/Misc/home-warehouse-system/frontend2`, run a NORMAL
    (non-frozen) install — this is the ONE plan in Phase 16 that updates the lockfile:
    `cd /home/antti/Repos/Misc/home-warehouse-system/frontend2 && bun add cmdk@1.1.1 tinykeys@4.0.0`.
    This pins both into `dependencies` and rewrites `bun.lock`. Do NOT add `command-score`
    (vendored in cmdk) and do NOT add `@radix-ui/react-dialog` (arrives transitively via cmdk).
    After install, CONFIRM @radix-ui/react-dialog is hoisted in `bun.lock` (it must be present for the
    palette chunk gate downstream) — grep the lockfile for it; if it is somehow NOT resolved, stop and
    surface (cmdk's hard top-level import would otherwise fail at build).
    Commit package.json + bun.lock together.
  </action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system/frontend2 && grep -q '"cmdk"' package.json && grep -q '"tinykeys"' package.json && ! grep -q '"command-score"' package.json && grep -q 'cmdk' bun.lock && grep -q '@radix-ui/react-dialog' bun.lock</automated>
  </verify>
  <done>cmdk@1.1.1 + tinykeys@4.0.0 in package.json dependencies; command-score absent; bun.lock updated and resolves cmdk + the hoisted @radix-ui/react-dialog tree.</done>
</task>

<task type="auto">
  <name>Task 3: Add the `palette` manualChunks branch (cmdk + radix-dialog)</name>
  <files>frontend2/vite.config.ts</files>
  <action>
    Inside the existing `manualChunks` function (mirror the scanner + charts blocks), add a
    `paletteModules` array `["cmdk", "@radix-ui/react-dialog"]` and an
    `if (paletteModules.some((mod) => id.includes(mod))) return "palette";` branch placed AFTER the
    charts branch and BEFORE `return undefined`. Keep the data-structure-as-membership style + add a
    comment noting: Phase 16 (Command Palette) TUI-05/POL-04 — isolate cmdk (~5KB gz) + the
    @radix-ui/react-dialog tree (~10-15KB gz) into a lazy `palette` chunk so the entry bundle carries
    ZERO palette bytes; cmdk hard-imports @radix-ui/react-dialog at module top level (OVERRIDE B,
    non-tree-shakable) so radix-dialog is co-located here or it leaks into entry. Do NOT convert the
    function to a record (rolldown requires the function form). Do NOT touch the scanner/charts branches.
    NOTE: the `palette` chunk stays empty until Plan 16-02 ships the lazy palette body that imports cmdk
    (rolldown no-ops an unreachable rule) — that is expected; the chunk materializes once 16-03 wires the
    lazy import. tsc/build must stay green now.
  </action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system/frontend2 && bun install --frozen-lockfile && grep -q '"palette"' vite.config.ts && grep -q '@radix-ui/react-dialog' vite.config.ts && bun run build</automated>
  </verify>
  <done>vite.config.ts manualChunks returns "palette" for cmdk + @radix-ui/react-dialog; `bun run build` green; the function form is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → frontend2 dependency tree | two new packages enter the supply chain; the install gate verifies legitimacy before they land |
| build pipeline → shipped bundle | the manualChunks rule decides whether cmdk + radix-dialog land in the always-loaded entry chunk vs the lazy palette chunk (POL-04 budget surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-SC | Tampering | npm installs (cmdk, tinykeys) | mitigate | slopcheck unavailable in-env → both ASSUMED; Task 1 is a BLOCKING-HUMAN legitimacy gate verifying npmjs.com (downloads, source repo, no postinstall) before `bun add`. cmdk already ships in the sibling legacy `frontend/`. command-score not installed (vendored). |
| T-16-01 | DoS | cmdk + radix-dialog bytes leaking into the entry bundle (every route pays ~15-20KB gz) | mitigate | the `palette` manualChunks branch isolates BOTH cmdk AND @radix-ui/react-dialog (OVERRIDE B) into a lazy chunk; Plan 16-03's bundle gate proves the entry chunk is palette-free |
</threat_model>

<verification>
- `cd frontend2 && bun run lint:tsc` clean (no new source yet — deps + config only).
- `cd frontend2 && bun run build` green; the function-form manualChunks preserved.
- package.json has cmdk + tinykeys, NOT command-score; bun.lock resolves cmdk + hoisted @radix-ui/react-dialog.
- `cd frontend2 && bun run lint:imports` clean (FOUND-02 — `cmdk`/`tinykeys`/`palette` are SAFE substrings; never `sync`/`idb`/`offline`).
</verification>

<success_criteria>
- TUI-05 foundation: cmdk@1.1.1 + tinykeys@4.0.0 installed (legitimacy-gated), command-score NOT installed.
- POL-04 pre-wire: vite manualChunks routes cmdk + @radix-ui/react-dialog into a `palette` chunk; build green.
- bun.lock committed so downstream plans can run `bun install --frozen-lockfile`.
</success_criteria>

<output>
Create `.planning/phases/16-command-palette/16-01-SUMMARY.md` when done (record the exact pinned
versions, confirm @radix-ui/react-dialog hoisted in bun.lock, and the final `paletteModules` membership
list so the 16-03 bundle gate binds to verified module ids).
</output>
