---
phase: 16-command-palette
plan: 03
type: execute
wave: 3
depends_on: [16-01, 16-02]
files_modified:
  - frontend2/src/components/layout/AppShell.tsx
  - frontend2/e2e/command-palette.spec.ts
autonomous: true
requirements: [TUI-05]
user_setup: []

must_haves:
  truths:
    - "ShellChrome owns [paletteOpen, setPaletteOpen], calls usePaletteChord(() => setPaletteOpen(true)) mounted once, and renders the React.lazy CommandPalette in a Suspense boundary EXACTLY like the helpOpen + <F1HelpDialog> pattern"
    - "the palette body is React.lazy(() => import('@/features/command-palette')) so cmdk + radix-dialog load only on first open (stay in the palette chunk, out of entry)"
    - "after `bun run build`, cmdk AND @radix-ui/react-dialog appear ONLY in palette-*.js; the entry index-*.js is cmdk-free (POL-04 bundle gate)"
    - "an E2E spec opens the palette via Meta+k AND F2, filters, Arrow+Enter navigates, ESC closes, and entity-search→navigate works against the real backend"
  artifacts:
    - path: frontend2/src/components/layout/AppShell.tsx
      provides: "paletteOpen state + usePaletteChord mount + lazy CommandPalette in Suspense (mirrors helpOpen/F1HelpDialog)"
      contains: "usePaletteChord"
    - path: frontend2/e2e/command-palette.spec.ts
      provides: "chromium E2E: open via Meta+k & F2, filter, Arrow+Enter nav, ESC close, entity-search→navigate"
      contains: "command-palette"
  key_links:
    - from: frontend2/src/components/layout/AppShell.tsx
      to: frontend2/src/features/command-palette/index.ts
      via: "const CommandPalette = lazy(() => import('@/features/command-palette')); usePaletteChord(() => setPaletteOpen(true))"
      pattern: "lazy\\(\\(\\) => import\\(\"@/features/command-palette\""
    - from: frontend2/src/components/layout/AppShell.tsx
      to: setPaletteOpen
      via: "usePaletteChord(() => setPaletteOpen(true)) — stable setter, mount-once"
      pattern: "usePaletteChord\\("
---

<objective>
The WIRING plan — the sole Phase-16 writer of `AppShell.tsx` (a milestone single-writer file, so this
plan is ALONE in its wave for that file). Mount the palette in `ShellChrome` exactly like the existing
`helpOpen` + `<F1HelpDialog>` pattern: own `[paletteOpen, setPaletteOpen]`, call
`usePaletteChord(() => setPaletteOpen(true))` (the tinykeys owner from 16-02, stays in the main bundle),
and render the React.lazy `CommandPalette` body in a `<Suspense>` boundary (so cmdk + radix-dialog load
only on first open, into the `palette` chunk 16-01 pre-wired). Then add the chromium E2E spec and PROVE
the POL-04 bundle gate (cmdk + @radix-ui/react-dialog absent from the entry chunk, present only in
`palette-*.js`).

Depends on 16-02 (the `@/features/command-palette` default export + `usePaletteChord` must exist for the
lazy import + the mount to tsc-check) and 16-01 (cmdk/tinykeys installed + the `palette` manualChunk so
the build gate has a chunk to assert against). DISJOINT from both: it touches only AppShell.tsx (no
other plan edits it) + a new E2E spec file.

Purpose: TUI-05 wiring + POL-04 bundle gate + the live E2E proof.
Output: the ShellChrome palette mount, the chromium E2E spec, and the build-gate evidence.

RENDER-LOOP LANDMINE: `usePaletteChord` already encapsulates the mount-once `useEffect([])` + openRef
(16-02). Pass it the STABLE `setPaletteOpen` setter via an arrow — do NOT wrap in a fresh memoized
object or add it to any dep array here. Mirror how `helpOpen`'s setters are threaded (plain inline
arrows).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-command-palette/16-CONTEXT.md
@.planning/phases/16-command-palette/16-VALIDATION.md

# The single-writer file to edit + the lazy/Suspense + helpOpen idioms to mirror EXACTLY:
@frontend2/src/components/layout/AppShell.tsx
@frontend2/e2e/login-dashboard.spec.ts

<interfaces>
<!-- Verified from source this planning session. Use directly; no exploration needed. -->

16-02 export surface (from .../command-palette/index.ts — confirm exact names against 16-02-SUMMARY):
  export { CommandPalette as default } from "./CommandPalette"   // React.lazy target: import("@/features/command-palette")
  export { usePaletteChord } from "./usePaletteChord"
  CommandPalette props: { open: boolean; onClose: () => void }
  usePaletteChord(open: () => void): void   // mount-once tinykeys $mod+k + F2 owner; stays in main bundle
  root has data-testid="command-palette" (E2E hook; confirm exact value in 16-02-SUMMARY)

AppShell.tsx — the helpOpen pattern to mirror EXACTLY (verified lines):
  - ShellChrome (a function component INSIDE WorkspaceProvider + SSEProvider) declares state at the top:
      const [helpOpen, setHelpOpen] = useState(false);   // add a sibling: const [paletteOpen, setPaletteOpen] = useState(false);
  - it renders <F1HelpDialog open={helpOpen} onClose={() => setHelpOpen(false)} onToggle={…} /> near the
    bottom (lines 148-153). Add the palette mount as a sibling there.
  - imports `useState` (line 1), Trans, the layout atoms; add the lazy + Suspense imports + usePaletteChord.

The mount (mirror the routes/index.tsx /scan lazy idiom + the helpOpen render slot):
  import { lazy, Suspense, useMemo, useState } from "react";   // add lazy, Suspense to the existing import
  import { usePaletteChord } from "@/features/command-palette";
  const CommandPalette = lazy(() => import("@/features/command-palette"));  // default export = CommandPalette
  // inside ShellChrome:
  usePaletteChord(() => setPaletteOpen(true));   // tinykeys owner, stays in main bundle
  // near the F1HelpDialog render slot:
  {paletteOpen && (
    <Suspense fallback={null}>
      <CommandPalette open={paletteOpen} onClose={() => setPaletteOpen(false)} />
    </Suspense>
  )}

E2E (mirror login-dashboard.spec.ts auth flow; chromium project; CLAUDE.md run contract):
  - login via /login (Email + Password labels → submit /^log in$/i), then on / press the chord.
  - playwright key syntax: page.keyboard.press('Meta+k') (and a SEPARATE assertion for 'F2').
  - assert the data-testid="command-palette" overlay is visible; type a route name → row visible;
    ArrowDown → Enter → URL changes; press Escape → overlay hidden; (entity) seed/find an item by name,
    type it → entity row → Enter → lands on /items/{id}.
  - baseURL defaults to http://localhost:5173; E2E_USER/E2E_PASS env (seeder@test.local / password123).

POL-04 bundle gate (mirror the 13b-05 recharts gate): after `bun run build`, grep dist/assets/*.js —
  cmdk + @radix-ui/react-dialog markers appear ONLY in a palette-*.js (or the lazy palette chunk),
  NEVER in the entry index-*.js. The largest always-loaded entry chunk must be cmdk-free.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount the lazy palette + chord owner in ShellChrome</name>
  <files>frontend2/src/components/layout/AppShell.tsx</files>
  <action>
    SINGLE-WRITER edit — this plan is the sole Phase-16 writer of AppShell.tsx. Mirror the helpOpen +
    F1HelpDialog pattern EXACTLY:
    1. Extend the React import to include `lazy, Suspense` (alongside the existing `useMemo, useState`).
    2. Add `import { usePaletteChord } from "@/features/command-palette";` and
       `const CommandPalette = lazy(() => import("@/features/command-palette"));` at module scope (next
       to the other top-level imports). Short comment: the palette body is React.lazy so cmdk +
       @radix-ui/react-dialog land in the `palette` chunk (16-01) and load only on first open — POL-04;
       the tiny tinykeys chord owner (usePaletteChord) stays in main. Mirrors the /scan lazy idiom.
    3. Inside `ShellChrome`, add `const [paletteOpen, setPaletteOpen] = useState(false);` as a sibling of
       `helpOpen`, and call `usePaletteChord(() => setPaletteOpen(true));` (stable inline arrow — do NOT
       memoize/wrap; usePaletteChord already mounts once internally).
    4. Near the `<F1HelpDialog … />` render slot, add the gated lazy mount:
       `{paletteOpen && (<Suspense fallback={null}><CommandPalette open={paletteOpen} onClose={() => setPaletteOpen(false)} /></Suspense>)}`.
    Touch nothing else in AppShell.tsx (no changes to collapsed/drawer/helpOpen/SSE wiring). Minimal diff.
  </action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system/frontend2 && bun install --frozen-lockfile && grep -q 'usePaletteChord' src/components/layout/AppShell.tsx && grep -q 'import("@/features/command-palette")' src/components/layout/AppShell.tsx && bun run lint:tsc</automated>
  </verify>
  <done>ShellChrome owns paletteOpen, calls usePaletteChord(() => setPaletteOpen(true)), and renders the React.lazy CommandPalette in a Suspense boundary mirroring helpOpen/F1HelpDialog; tsc -b clean; no unrelated AppShell churn.</done>
</task>

<task type="auto">
  <name>Task 2: POL-04 bundle gate — cmdk + radix-dialog absent from entry chunk</name>
  <files>frontend2/src/components/layout/AppShell.tsx</files>
  <action>
    NO source edit in this task (AppShell.tsx is in the file list only because Task 1 owns it; this task
    may need a one-line tweak ONLY if the build reveals the lazy import isn't actually splitting — e.g. a
    static re-import path that defeats the chunk). Run the build and PROVE the POL-04 gate (mirror the
    13b-05 recharts gate):
    1. `cd /home/antti/Repos/Misc/home-warehouse-system/frontend2 && bun run build` (clean).
    2. Identify the entry chunk (the largest always-loaded `dist/assets/index-*.js`) and the
       `palette-*.js` chunk. Assert: a cmdk marker AND an @radix-ui/react-dialog marker appear ONLY in
       the palette chunk (or the lazy palette page chunk), NEVER in the entry chunk. Practical assertion:
       `grep -lR cmdk dist/assets/*.js` returns the palette chunk and NOT index-*.js; likewise for the
       radix-dialog marker. Confirm the entry chunk gzip is ~unchanged vs pre-palette (document
       before/after — VALIDATION expects the main chunk to carry ZERO palette bytes).
    3. If cmdk/radix-dialog bytes leak into the entry chunk, the lazy boundary is defeated (likely a
       stray static import of the palette body somewhere) — fix the import path so ONLY the
       `lazy(() => import(...))` path reaches cmdk, and rebuild until the grep is clean. If the leak is a
       manualChunks membership gap, coordinate (16-01 owns vite.config.ts — flag it; do NOT edit
       vite.config.ts here, it is 16-01's single-writer file).
    Record the gate evidence (which chunk holds cmdk + radix-dialog; entry-chunk gzip before/after) in
    the SUMMARY. Do NOT declare the plan done until the grep proves zero palette bytes in the entry chunk.
  </action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system/frontend2 && bun run build && test -n "$(grep -lR cmdk dist/assets/*.js)" && ! grep -lR cmdk dist/assets/index-*.js</automated>
  </verify>
  <done>Build green; cmdk + @radix-ui/react-dialog appear in a palette-*.js chunk but NOT in the entry index-*.js; entry-chunk gzip ~unchanged vs pre-palette (documented).</done>
</task>

<task type="auto">
  <name>Task 3: Chromium E2E spec — open/filter/navigate/ESC/entity-search</name>
  <files>frontend2/e2e/command-palette.spec.ts</files>
  <action>
    Add `frontend2/e2e/command-palette.spec.ts` (chromium project; mirror login-dashboard.spec.ts's auth
    flow + the CLAUDE.md auth contract: /login → Email + Password labels → submit /^log in$/i; cookie
    JWT inherited by page + page.request). After landing on `/`:
    1. OPEN — `page.keyboard.press('Meta+k')` → assert the `data-testid="command-palette"` overlay is
       visible; close it; assert `page.keyboard.press('F2')` ALSO opens it (both chords, SC1).
    2. FILTER — type a route name (e.g. "Borrowers") → assert the matching row is visible/highlighted (SC2).
    3. NAVIGATE — ArrowDown then Enter → assert the URL changed to the selected route (SC3).
    4. ESC — open → `page.keyboard.press('Escape')` → assert the overlay is hidden and the underlying
       route is intact (SC3, modal-stack pop).
    5. ENTITY SEARCH (§4 parity) — seed/find a known item (use `page.request.post`/existing seed helper
       per CLAUDE.md, or pick an item known to exist in the seeded dev DB), open the palette, type its
       name → assert an entity result row appears → Enter → assert the URL is `/items/{id}` (SC4).
    Scope to the chromium project (16-VALIDATION: chromium). Honor the 20/min auth limiter — one login
    per spec; restart backend on mass auth-fail. baseURL + E2E_USER/E2E_PASS per CLAUDE.md.
  </action>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system/frontend2 && test -f e2e/command-palette.spec.ts && bunx playwright test --list e2e/command-palette.spec.ts --project=chromium</automated>
  </verify>
  <done>command-palette.spec.ts exists and is discovered by the chromium project; it covers open-via-Meta+k-AND-F2, filter, Arrow+Enter navigate, ESC close, and entity-search→navigate. (Live green run is the phase gate, run with the dev stack up per CLAUDE.md.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build pipeline → shipped bundle | the React.lazy boundary + the `palette` manualChunk decide whether cmdk + radix-dialog land in the always-loaded entry chunk (POL-04 budget surface) |
| nav → ShellChrome mount | the palette mounts inside the authenticated WorkspaceProvider/SSEProvider scope — it never renders for an unauthenticated session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06 | DoS | cmdk + radix-dialog (~15-20KB gz) leaking into the entry bundle | mitigate | the body is React.lazy(() => import('@/features/command-palette')) so it loads only on first open; Task 2's build gate greps dist to PROVE the entry chunk carries zero palette bytes (POL-04 hard gate) — the plan is not done until that assertion is green |
| T-16-07 | Elevation of Privilege | palette mounting / querying for an unauthenticated session | mitigate | mounted in ShellChrome, which renders only under RequireAuth + WorkspaceProvider; the chord owner is mounted in the same authed subtree, so there is no unauth palette surface |
| T-16-SC | Tampering | npm installs | mitigate | none — this plan installs NO packages; it runs `bun install --frozen-lockfile` against the lockfile 16-01 committed behind its legitimacy gate. |
</threat_model>

<verification>
- `cd frontend2 && bun run lint:tsc` clean (the lazy import resolves the 16-02 default export + usePaletteChord).
- `cd frontend2 && bun run build` green; POL-04 gate: `grep -lR cmdk dist/assets/*.js` → palette chunk only; entry index-*.js cmdk-free; @radix-ui/react-dialog likewise.
- `cd frontend2 && bunx playwright test --list e2e/command-palette.spec.ts --project=chromium` discovers the spec.
- Phase gate (dev stack up per CLAUDE.md): full vitest + `E2E_USER=seeder@test.local E2E_PASS=password123 bun run test:e2e e2e/command-palette.spec.ts` green.
- Render-loop guard: usePaletteChord (16-02) mounts once; setPaletteOpen passed as a stable inline arrow, not memoized/dep-tracked here.
</verification>

<success_criteria>
- TUI-05 wiring: ShellChrome mounts the React.lazy CommandPalette + the usePaletteChord chord owner mirroring the helpOpen/F1HelpDialog pattern; ⌘K/Ctrl+K/F2 open the palette; ESC closes it.
- POL-04: cmdk + @radix-ui/react-dialog appear ONLY in palette-*.js; the entry index-*.js is palette-free (build-grep proven); entry-chunk gzip ~unchanged.
- §4 parity + SC1-4: the chromium E2E spec covers open (Meta+k AND F2) / filter / Arrow+Enter navigate / ESC / entity-search→navigate.
</success_criteria>

<output>
Create `.planning/phases/16-command-palette/16-03-SUMMARY.md` when done (record the ShellChrome mount
diff, the POL-04 gate evidence — which chunk holds cmdk + radix-dialog + entry-chunk gzip before/after —
and the E2E spec coverage map SC1-4, so the gsd-verifier binds to verified facts).
</output>
