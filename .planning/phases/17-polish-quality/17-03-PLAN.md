---
phase: 17-polish-quality
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - frontend2/e2e/responsive.spec.ts
autonomous: true
requirements: [POL-05]
must_haves:
  truths:
    - "responsive spec loads / and /items at 320/360/768/1024/1440 px"
    - "at every viewport there is no horizontal overflow (scrollWidth <= clientWidth + tolerance)"
    - "at <768 the mobile Fab is visible and the Sidebar is hidden; at >=768 the Sidebar is visible"
    - "dashboard screenshots at all 5 breakpoints are captured as Playwright artifacts"
  artifacts:
    - path: "frontend2/e2e/responsive.spec.ts"
      provides: "5-breakpoint structural responsive matrix + dashboard screenshots"
      contains: "1440"
  key_links:
    - from: "frontend2/e2e/responsive.spec.ts"
      to: "AppShell responsive contract"
      via: "viewport-driven visibility of Sidebar vs Fab"
      pattern: "setViewportSize"
---

<objective>
Verify the mobile breakpoint matrix. Satisfies **POL-05** (mobile breakpoints at
320/360/768/1024/1440 px + visual diff vs sketch 006). One new Playwright spec
asserts the structural responsive contract and captures dashboard screenshots;
the pixel diff vs sketch 006 is a documented human-eye residue (OQ-5), not
asserted here.

Purpose: Catch responsive regressions (horizontal overflow, wrong nav surface)
automatically. Output: `frontend2/e2e/responsive.spec.ts`.

BRANCH NOTE: Wave 3 — branch off the post-17-02 HEAD. Runs in parallel with
17-04 (disjoint files: this plan owns ONLY `e2e/responsive.spec.ts`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-polish-quality/17-CONTEXT.md
@.planning/phases/17-polish-quality/17-VALIDATION.md

<!-- MODEL THE AUTH/SEED PATTERN on the palette spec login() helper. -->
@frontend2/e2e/command-palette.spec.ts
@frontend2/playwright.config.ts

<interfaces>
<!-- CLAUDE.md auth contract: /login -> getByLabel Email/Password -> -->
<!--   submit /^log in$/i -> toHaveURL("/"). ONE login per test. -->
<!-- VERIFIED AppShell responsive contract (orchestrator grep of -->
<!--   frontend2/src/components/layout/): the breakpoint is Tailwind `md` = 768px. -->
<!--   .app-sidebar  -> class `hidden md:block`  => HIDDEN <768, VISIBLE >=768 -->
<!--   Fab           -> class `md:hidden`        => VISIBLE <768, HIDDEN >=768 -->
<!--   Bottombar     -> class `hidden md:flex`   => desktop-only (>=768) -->
<!--   Sidebar <nav> carries aria-label="Primary"; Fab toggle aria-label="Quick actions". -->
<!-- IMPORTANT: assert Fab-visible / Sidebar-hidden at <768 and Sidebar-visible -->
<!--   at >=768. Do NOT assert "Bottombar visible <768" — Bottombar is DESKTOP-only -->
<!--   here; the Fab is the mobile counterpart. -->
<!-- Breakpoints (POL-05): 320, 360, 768, 1024, 1440 px. 768 is the boundary -->
<!--   itself => Tailwind `md` (min-width:768px) is ACTIVE at exactly 768 (desktop -->
<!--   surface). So: 320/360 => mobile (Fab); 768/1024/1440 => desktop (Sidebar). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: responsive.spec.ts — 5-breakpoint structural matrix + screenshots</name>
  <files>frontend2/e2e/responsive.spec.ts</files>
  <action>
Write `frontend2/e2e/responsive.spec.ts` (chromium is the hard gate —
17-VALIDATION runs `bun run test:e2e e2e/responsive.spec.ts`).
- `login(page)` helper copied from the palette spec (ONE login per test; 20/min
  auth limiter — prefer a single test that logs in once then loops viewports, or
  a `beforeEach` if split, but keep total logins minimal).
- Define the breakpoints array: 320, 360, 768, 1024, 1440 (widths; pick a tall
  enough height, e.g. 900).
- For each viewport: `page.setViewportSize({ width, height })`, then load `/`
  (dashboard) AND `/items`. After each load, wait for a stable shell selector.
  Assertions per viewport per route:
  1. NO horizontal overflow:
     `page.evaluate(() => document.documentElement.scrollWidth <=
      document.documentElement.clientWidth + TOL)` with a small TOL (e.g. 2px) for
     sub-pixel rounding. Assert true.
  2. Nav-surface contract (use the VERIFIED contract from the interfaces block):
     - width < 768  => the mobile Fab (aria-label "Quick actions", or its
       md:hidden container) is VISIBLE and the Sidebar (aria-label "Primary",
       `.app-sidebar`) is HIDDEN.
     - width >= 768 => the Sidebar is VISIBLE.
     Use `toBeVisible()` / `toBeHidden()` on stable locators (prefer the
     aria-label nav / Fab button; fall back to the `.app-sidebar` container).
- Screenshot the DASHBOARD at each breakpoint into the test artifacts:
  `await page.screenshot({ path: \`test-results/dashboard-\${width}.png\`,
   fullPage: true })`. These are POL-05 artifacts for the human visual-diff
   residue (OQ-5) — they are CAPTURED, not pixel-asserted.
- Add a top-of-file comment: the pixel diff vs `006-retro-os-dashboard` is a
  documented human-eye residue (logged to FINAL-REVIEW-CHECKLIST per OQ-5); this
  spec asserts only the STRUCTURAL contract.

CONSTRAINT: do not name files/dirs with `sync`/`idb`/`offline` substrings.
Commit: `test(17-03): responsive 5-breakpoint matrix + dashboard screenshots (POL-05)`.
  </action>
  <verify>
    <automated>grep -q "320" frontend2/e2e/responsive.spec.ts && grep -q "1440" frontend2/e2e/responsive.spec.ts && grep -q "setViewportSize" frontend2/e2e/responsive.spec.ts && grep -q "scrollWidth" frontend2/e2e/responsive.spec.ts && echo ok</automated>
    <human-check>cd frontend2 && bun run test:e2e e2e/responsive.spec.ts (chromium, live :5173/:8080) — 5 breakpoints pass, no overflow, Sidebar/Fab swap at 768, 5 dashboard screenshots in test-results/</human-check>
  </verify>
  <done>Spec covers all 5 breakpoints on / and /items, asserts no horizontal overflow + the verified Sidebar/Fab swap at 768, and writes 5 dashboard screenshots to test-results/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser E2E -> live backend | spec logs in against real :8080 with a real cookie |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-01 | Information disclosure | screenshots may capture seeded/dev data | accept | dev DB only; artifacts live in test-results/, gitignored CI scratch |
| T-17-03-SC | Tampering | no new packages installed | accept | zero new deps; nothing to audit |
</threat_model>

<verification>
- `bun run test:e2e e2e/responsive.spec.ts` (chromium) passes at all 5
  breakpoints: no horizontal overflow, Sidebar/Fab swap at 768, screenshots
  written.
- No other file touched (disjoint from 17-04's docs/test).
</verification>

<success_criteria>
POL-05 satisfied: 5-breakpoint structural contract asserted + dashboard
screenshots captured; pixel visual-diff residue logged to FINAL-REVIEW-CHECKLIST.
</success_criteria>

<output>
Create `.planning/phases/17-polish-quality/17-03-SUMMARY.md` when done.
Declare every file edited. Do NOT edit STATE.md or ROADMAP.md.
</output>
