---
phase: 17-polish-quality
plan: 04
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - .planning/phases/17-polish-quality/COVERAGE-MATRIX.md
  - .planning/phases/17-polish-quality/ENDPOINT-DIFF.md
autonomous: true
requirements: [POL-01, POL-06]
must_haves:
  truths:
    - "COVERAGE-MATRIX.md maps every HTTP-crossing flow (per domain) to its E2E spec + its Go integration test"
    - "any flow with ZERO real-backend coverage is flagged in the matrix"
    - "ENDPOINT-DIFF.md records the POL-06 endpoint coverage diff + a checklist of all ~40 routes"
    - "a genuine zero-coverage flow gets a minimal disjoint gap-fill test, OR the matrix documents 'no gaps'"
  artifacts:
    - path: ".planning/phases/17-polish-quality/COVERAGE-MATRIX.md"
      provides: "flow -> E2E + Go integration test coverage matrix"
      contains: "POL-01"
    - path: ".planning/phases/17-polish-quality/ENDPOINT-DIFF.md"
      provides: "endpoint coverage diff + route checklist"
      contains: "POL-06"
  key_links:
    - from: ".planning/phases/17-polish-quality/COVERAGE-MATRIX.md"
      to: "frontend2/e2e/*.spec.ts + backend *_test.go"
      via: "per-flow mapping rows"
      pattern: "\\.spec\\.ts"
---

<objective>
Close out the parity verification gate. Satisfies **POL-01** (every HTTP-crossing
flow has >=1 real-backend test — E2E + tagged Go integration) and **POL-06**
(parity gate: route checklist + endpoint coverage diff). Produces two committed
phase-dir docs; writes a gap-fill test ONLY if the matrix finds a genuine
zero-coverage flow (OQ-3).

Purpose: Prove v3.0 parity is complete and every cross-HTTP flow is tested, and
record the residual (documented-as-deferred) endpoint deltas. Output:
`COVERAGE-MATRIX.md`, `ENDPOINT-DIFF.md`, and at most 1-2 disjoint gap-fill tests.

BRANCH NOTE: Wave 3 — branch off the post-17-02 HEAD. Runs in parallel with 17-03
(disjoint files: this plan owns the two phase-dir docs + any NEW disjoint test
file with a unique name).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-polish-quality/17-CONTEXT.md
@.planning/phases/17-polish-quality/17-VALIDATION.md

<interfaces>
<!-- Existing E2E specs (frontend2/e2e/, 15 files): analytics, attachments-paperless, -->
<!--   auth, borrowers, command-palette, inventory, items, loans-lifecycle, -->
<!--   login-dashboard, repairs-maintenance, scan-lookup, settings, sse-online, -->
<!--   system-group, taxonomy. PLUS 17-02/17-03 add a11y-sweep, keyboard-nav, responsive. -->
<!-- Existing Go integration tests (48 _test.go, //go:build integration): -->
<!--   item/handler, attachment/handler+storage, loan, maintenance, pendingchange, -->
<!--   wishlist, import, auth, warehouse, permission, multitenant, cross_workspace_fk, -->
<!--   approval_pipeline, all postgres repos, etc. Run: -->
<!--   TEST_DATABASE_URL=postgresql://wh:wh@localhost:5432/warehouse_test \ -->
<!--     go test -tags=integration ./... -->
<!-- Endpoint coverage diff (17-CONTEXT §Endpoint coverage diff) — PARITY ESSENTIALLY -->
<!--   COMPLETE. Residual non-feature-gap deltas (all documented, not blockers): -->
<!--   - /analytics/{borrowers,categories,locations,conditions,statuses,loans} per-metric -->
<!--       => SUPERSEDED by /analytics/dashboard + /summary + /loans/monthly + /out-of-stock -->
<!--   - /locations/{id}/breadcrumb => SUPERSEDED (taxonomy builds tree client-side) -->
<!--   - /inventory/{id}/loans, /inventory/available/{itemId}, /inventory/total-quantity/{itemId} -->
<!--       => niche legacy queries, unused by v3.0 screens => DEFERRED backlog (also in 17-VALIDATION residues) -->
<!--   - /items/search?query= => covered by command-palette useEntitySearch /{domain}/search (confirm) -->
<!--   - /paperless/settings DELETE => minor optional (GET/PATCH present) -->
<!-- Legacy files ported elsewhere: auth.ts->features/auth+settings; item-photos->photos; -->
<!--   repair-logs->repairs+repairPhotos+repairAttachments; search->command-palette; -->
<!--   workspace-backup->settings export/import; importexport->importJobs+settings. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: COVERAGE-MATRIX.md — flow -> E2E + Go integration (POL-01)</name>
  <files>.planning/phases/17-polish-quality/COVERAGE-MATRIX.md</files>
  <action>
Enumerate every HTTP-crossing flow per domain and map each to (a) its Playwright
E2E spec file and (b) its tagged Go integration test. Build the inventory by
inspection, NOT by guessing:
- List E2E specs: `ls frontend2/e2e/*.spec.ts` (include the 17-02/17-03 additions
  if present at this HEAD; if not yet merged, reference them by name as
  cross-domain a11y/keyboard/responsive coverage).
- List Go integration tests: `grep -rl "go:build integration" backend --include=*_test.go`
  and note each domain (item, attachment, loan, maintenance, pendingchange,
  wishlist, import, auth, warehouse, permission, multitenant, cross_workspace_fk,
  approval_pipeline, postgres repos).
- Identify the cross-HTTP flows per domain (the user-facing actions that cross the
  `/api` boundary): auth/login, items CRUD + by-barcode, inventory + expiring +
  maintenance-due, loans lifecycle, borrowers, taxonomy, scan-lookup, analytics
  dashboard, attachments/paperless, approvals/pending-changes, wishlist, imports,
  settings (export/import, members, security), SSE/online presence,
  command-palette entity search.

Write `COVERAGE-MATRIX.md` with a table:
`| Domain | Flow (HTTP-crossing) | E2E spec | Go integration test | Coverage |`
where Coverage is one of: BOTH / E2E-only / Go-only / **NONE (gap)**. Reference
real filenames. Add a header explaining the POL-01 standard (every flow needs
>=1 real-backend test; the Phase-65-11 pattern). At the bottom add a "Gaps" list:
every flow scored NONE. If there are no NONE rows, state explicitly
"**No zero-coverage flows found.**"

Do NOT re-test already-covered flows (OQ-3 over-build risk). Note any flow that is
E2E-only or Go-only but where the single test is sufficient (a flow needs >=1
real-backend test, not necessarily both layers) — only NONE is a true gap.
Commit: `docs(17-04): COVERAGE-MATRIX flow->test mapping (POL-01)`.
  </action>
  <verify>
    <automated>test -f .planning/phases/17-polish-quality/COVERAGE-MATRIX.md && grep -q "POL-01" .planning/phases/17-polish-quality/COVERAGE-MATRIX.md && grep -qi "gap" .planning/phases/17-polish-quality/COVERAGE-MATRIX.md && echo ok</automated>
  </verify>
  <done>COVERAGE-MATRIX.md maps every cross-HTTP flow to its E2E + Go tests with a BOTH/E2E-only/Go-only/NONE column and an explicit gaps section (or "no gaps").</done>
</task>

<task type="auto">
  <name>Task 2: ENDPOINT-DIFF.md — endpoint coverage diff + route checklist (POL-06)</name>
  <files>.planning/phases/17-polish-quality/ENDPOINT-DIFF.md</files>
  <action>
Write `ENDPOINT-DIFF.md` recording the POL-06 endpoint coverage diff from
17-CONTEXT §"Endpoint coverage diff" (do NOT re-derive — it is orchestrator-
verified; transcribe and structure it):
- Lead with the conclusion: parity essentially complete; the investigator's
  first-pass "33 gaps" were almost all false positives (list the confirmed-present
  set: auth/oauth/sessions/password/can-delete/workspaces/analytics/labels/
  inventory-by-*/notifications-pagination).
- A "Residual deltas" table (legacy endpoint -> frontend2 status) for the 5
  documented deltas: per-metric /analytics/* (superseded), /locations/{id}/breadcrumb
  (superseded), the 3 niche /inventory/* queries (deferred backlog),
  /items/search?query= (covered by command-palette /{domain}/search — CONFIRM by
  grepping frontend2/src/lib/api or the palette search hook), /paperless/settings
  DELETE (minor optional).
- A "Legacy files ported elsewhere" list (auth.ts, item-photos.ts, repair-logs.ts,
  search.ts, workspace-backup, importexport.ts) mapping each to its frontend2 home.
- A "Route checklist" of ALL ~40 frontend2 routes (the full set from 17-CONTEXT
  §Routes to sweep, including the :id detail routes, /demo, /claim/:code,
  /settings subpages) as a checkbox list, each marked covered by the a11y sweep /
  an E2E spec / human-only. Derive the route list from the React Router config:
  `grep -rn "path:" frontend2/src` (or the routes file) to confirm the live set,
  reconcile against 17-CONTEXT, and note any discrepancy.
- Close with the POL-06 residues that go to FINAL-REVIEW-CHECKLIST (per
  17-VALIDATION): one-week dogfooding before legacy frontend/ retirement; the 3
  niche inventory endpoints deferred to backlog; the e2e-frontend2.yml first-PR
  validation residue.

Commit: `docs(17-04): ENDPOINT-DIFF parity gate + route checklist (POL-06)`.
  </action>
  <verify>
    <automated>test -f .planning/phases/17-polish-quality/ENDPOINT-DIFF.md && grep -q "POL-06" .planning/phases/17-polish-quality/ENDPOINT-DIFF.md && grep -qi "breadcrumb" .planning/phases/17-polish-quality/ENDPOINT-DIFF.md && grep -qi "checklist" .planning/phases/17-polish-quality/ENDPOINT-DIFF.md && echo ok</automated>
  </verify>
  <done>ENDPOINT-DIFF.md records parity-complete conclusion, the 5 residual deltas, ported-files map, a checklist of all ~40 routes, and the POL-06 residues.</done>
</task>

<task type="auto">
  <name>Task 3: Gap-fill ONLY for a genuine zero-coverage flow (OQ-3)</name>
  <files>(conditional) one disjoint new test file — name TBD by the gap</files>
  <action>
GATED on Task 1's matrix. If COVERAGE-MATRIX.md found ZERO genuine NONE-coverage
flows, do NOTHING here except record "no gaps to fill" in the SUMMARY — do not
write a test (OQ-3 explicitly warns against over-building; 15 E2E + 48 Go tests
already exist, expect <=1-2 genuine gaps).

If a genuine zero-coverage HTTP-crossing flow EXISTS, add the MINIMAL test to
close it, in a NEW disjoint file (never edit an existing spec/test — single-writer
discipline; this plan must not collide with 17-03):
- If browser-driven: a new `frontend2/e2e/<flow>.spec.ts` (chromium), using the
  CLAUDE.md auth contract + page.request seeding (palette-spec pattern).
- If a server contract: a new tagged Go integration test
  (`//go:build integration`) under the relevant `backend/internal/.../...` package,
  following the Phase-65-11 pattern (real Postgres via tests/testdb harness, run
  with `-tags=integration` + TEST_DATABASE_URL=...warehouse_test).
- Keep it to the single missing flow. Update COVERAGE-MATRIX.md's gap row to
  reflect the new coverage.

CONSTRAINT: do not name the file/dir with `sync`/`idb`/`offline` substrings
(FOUND-02 / lint:imports). If a Go test is added,
`cd backend && go build ./... && go test -tags=integration <pkg>` must pass.
Commit (only if a test was added): `test(17-04): gap-fill <flow> real-backend coverage (POL-01)`.
  </action>
  <verify>
    <automated>grep -qi "gap" .planning/phases/17-polish-quality/COVERAGE-MATRIX.md && echo "matrix gaps section present"</automated>
    <human-check>If a gap-fill test was added, run it: chromium spec via bun run test:e2e, OR Go via go test -tags=integration. If "no gaps", confirm SUMMARY records that.</human-check>
  </verify>
  <done>Either a single disjoint gap-fill test closes the one genuine zero-coverage flow (and the matrix row is updated), OR the SUMMARY records "no gaps to fill".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| docs only (Tasks 1-2) | no runtime trust boundary; phase-dir markdown |
| conditional gap-fill test (Task 3) | if added, browser/Go test crosses the real /api or DB boundary |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04-01 | Repudiation | matrix overclaims coverage (marks a flow covered that is not) | mitigate | every row references a REAL filename verified by ls/grep; NONE rows explicit |
| T-17-04-02 | Information disclosure | ENDPOINT-DIFF leaks internal route shape | accept | planning doc in private .planning/; no secrets |
| T-17-04-SC | Tampering | conditional Go/E2E gap-fill may pull a dep | mitigate | gap-fill uses ONLY existing test harnesses (tests/testdb, @playwright/test); no new package — if one is somehow needed, STOP and flag (no audit table) |
</threat_model>

<verification>
- `COVERAGE-MATRIX.md` and `ENDPOINT-DIFF.md` exist in the phase dir and are
  committed.
- Every HTTP-crossing flow in the matrix maps to an E2E or Go integration test;
  gaps either filled (disjoint new test) or logged deferred-with-reason.
- If a gap-fill test was added: it passes (chromium spec or
  `go test -tags=integration`).
- No existing spec/test edited; disjoint from 17-03.
</verification>

<success_criteria>
POL-01 + POL-06 satisfied: committed coverage matrix mapping flows->tests (gaps
filled or deferred-with-reason) + committed endpoint-diff parity gate with a
~40-route checklist; over-build avoided (gap-fill only where genuinely zero).
</success_criteria>

<output>
Create `.planning/phases/17-polish-quality/17-04-SUMMARY.md` when done.
Declare every file edited (incl. any gap-fill test). Do NOT edit STATE.md or
ROADMAP.md.
</output>
