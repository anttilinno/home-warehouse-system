---
phase: 17-polish-quality
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - frontend2/package.json
  - frontend2/bun.lock
  - frontend2/e2e/a11y-sweep.spec.ts
  - frontend2/e2e/keyboard-nav.spec.ts
  - frontend2/src/styles/globals.css
  - .github/workflows/e2e-frontend2.yml
autonomous: true
requirements: [POL-02, POL-03]
must_haves:
  truths:
    - "@axe-core/playwright is a committed devDependency with bun.lock updated"
    - "a11y-sweep spec logs in once and runs axe across the static-path route subset + one seeded detail route with zero serious/critical violations"
    - "keyboard-nav spec proves focus-visible reachability, ESC-closes-modal, and no keyboard trap"
    - "a single global :focus-visible fallback exists in globals.css without removing per-component rules"
    - "a best-effort e2e-frontend2.yml workflow exists, flagged as needing first-PR validation"
  artifacts:
    - path: "frontend2/e2e/a11y-sweep.spec.ts"
      provides: "axe sweep across swept routes"
    - path: "frontend2/e2e/keyboard-nav.spec.ts"
      provides: "keyboard nav + focus-visible + ESC + no-trap assertions"
    - path: "frontend2/src/styles/globals.css"
      provides: "global :focus-visible fallback ring"
      contains: "focus-visible"
    - path: ".github/workflows/e2e-frontend2.yml"
      provides: "best-effort browser-E2E CI scaffold"
  key_links:
    - from: "frontend2/e2e/a11y-sweep.spec.ts"
      to: "@axe-core/playwright"
      via: "AxeBuilder import"
      pattern: "@axe-core/playwright"
    - from: "frontend2/e2e/keyboard-nav.spec.ts"
      to: "command palette"
      via: "ControlOrMeta+k then Escape"
      pattern: "ControlOrMeta\\+k"
---

<objective>
Add accessibility + keyboard-navigation coverage. Satisfies **POL-02** (axe
sweep: no contrast / focus-visible / touch-target / aria-label violations) and
**POL-03** (tab/keyboard audit: keyboard-navigable, focus-visible not focus, no
traps). Adds `@axe-core/playwright`, two new Playwright specs, a single global
`:focus-visible` safety-net rule, and a best-effort CI workflow.

Purpose: Make a11y + keyboard correctness a repeatable gate, not a one-off manual
check. Output: the dep + bun.lock, `e2e/a11y-sweep.spec.ts`,
`e2e/keyboard-nav.spec.ts`, a globals.css fallback rule, and a flagged
`e2e-frontend2.yml`.

BRANCH NOTE: Wave 2 — branch off the post-17-01 HEAD so the package.json
devDependency append merges cleanly over 17-01's `lint:bundle` scripts edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-polish-quality/17-CONTEXT.md
@.planning/phases/17-polish-quality/17-VALIDATION.md

<!-- MODEL THE SPEC AUTH/PALETTE PATTERN on the existing palette spec: -->
<!--   login() helper, ControlOrMeta+k chord, data-testid="command-palette", -->
<!--   ESC pops the modal without route change. ONE login per test (20/min limiter). -->
@frontend2/e2e/command-palette.spec.ts
@frontend2/playwright.config.ts
@frontend2/package.json
@.github/workflows/lint-frontend2.yml
@frontend2/src/styles/globals.css

<interfaces>
<!-- CLAUDE.md auth contract: /login -> getByLabel("Email")/("Password") -> -->
<!--   getByRole("button",{name:/^log in$/i}) -> toHaveURL("/"). cookie inherited -->
<!--   by page.request. ONE login per test. -->
<!-- Palette overlay testid: [data-testid="command-palette"]; open chord -->
<!--   ControlOrMeta+k ($mod = Ctrl on Linux CI), close = Escape (no route change). -->
<!-- Existing focus token (per-component, 12 files): -->
<!--   focus-visible:outline-2 outline-border-ink outline-offset-2; --border-ink = #26262e. -->
<!-- Responsive shell contract (verified by orchestrator grep): -->
<!--   .app-sidebar = `hidden md:block` (visible >=768), Fab = `md:hidden` (visible <768), -->
<!--   Bottombar = `hidden md:flex` (desktop). Sidebar nav has aria-label="Primary"; -->
<!--   Fab toggle has aria-label="Quick actions". -->
<!-- Routes to sweep (17-CONTEXT §Routes to sweep), STATIC-PATH SUBSET (no :id, -->
<!--   no /demo, no /claim/:code): -->
<!--   public  /login /register /auth/callback -->
<!--   app     / /items /items/new /inventory /inventory/new /inventory/expiring -->
<!--           /maintenance/due /loans /loans/new /borrowers /borrowers/new -->
<!--           /taxonomy /scan /analytics /approvals /my-changes /sync-history -->
<!--           /imports /wishlist /declutter /settings (+ subpages /settings/security -->
<!--           /accounts /profile /appearance /language /formats /notifications -->
<!--           /data /members /paperless) -->
<!--   PLUS one seeded detail route (e.g. /items/{id} from a page.request-seeded item). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @axe-core/playwright dep + global :focus-visible fallback</name>
  <files>frontend2/package.json, frontend2/bun.lock, frontend2/src/styles/globals.css</files>
  <action>
1. Add `@axe-core/playwright` (latest 4.x) to `frontend2` devDependencies. Run
   `cd frontend2 && bun add -d @axe-core/playwright@^4` so bun.lock is updated,
   THEN commit BOTH package.json and bun.lock (17-VALIDATION requires
   `bun install --frozen-lockfile` to stay green — the lockfile MUST be in sync).
   Do not add any other dep.
2. Append ONE global `:focus-visible` fallback rule to the END of
   `frontend2/src/styles/globals.css` (surgical — do NOT remove or alter the
   per-component focus rules in the 12 atom/layout files; this is a safety net for
   any element missing an explicit rule, OQ-4). The rule: a `*:focus-visible`
   selector with `outline: 2px solid` using `var(--border-ink)` (match the
   per-component token; you MAY use `color-mix` only if it degrades to a solid
   `--border-ink`) and `outline-offset: 2px`. Add a one-line comment tagging it
   `POL-03 / OQ-4 global focus-visible fallback`. Respect the existing
   `@layer`/`@media` structure conventions at file end if one applies; otherwise a
   bare top-level rule at file end is fine.

Verify typecheck (the canonical command — bare `tsc --noEmit` exits 0 spuriously
on this project-references repo): `cd frontend2 && bun run lint:tsc`.
Commit: `feat(17-02): @axe-core/playwright dep + global focus-visible fallback (POL-02/03)`.
  </action>
  <verify>
    <automated>cd frontend2 && bun install --frozen-lockfile && bun run lint:tsc && grep -c "focus-visible" src/styles/globals.css</automated>
  </verify>
  <done>@axe-core/playwright in devDependencies; bun.lock in sync (frozen install passes); globals.css ends with a global :focus-visible rule; per-component rules untouched; lint:tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: a11y-sweep.spec.ts (POL-02)</name>
  <files>frontend2/e2e/a11y-sweep.spec.ts</files>
  <action>
Write `frontend2/e2e/a11y-sweep.spec.ts` (chromium project is the hard gate —
17-VALIDATION runs `bun run test:e2e e2e/a11y-sweep.spec.ts` on chromium).
- Reuse the CLAUDE.md auth contract via a `login(page)` helper copied from the
  palette spec (getByLabel Email/Password -> submit `/^log in$/i` ->
  toHaveURL("/")). Log in ONCE per test (20/min auth limiter): structure as ONE
  test that logs in then loops the app routes; sweep the three public routes
  (`/login`, `/register`, `/auth/callback`) without login (separate test or before
  login) — but keep total logins minimal.
- Import `AxeBuilder` from `@axe-core/playwright`.
- Iterate the STATIC-PATH route subset from the interfaces block. For each route:
  `page.goto(route)`, wait for network idle / a stable shell selector, then run
  `new AxeBuilder({ page }).withTags(["wcag2a","wcag2aa"]).analyze()`. Cover the
  four POL-02 concerns explicitly: enable `target-size` (wcag22, not in the base
  tags) via `.options({ rules: { 'target-size': { enabled: true } } })`; the base
  tags already cover color-contrast, focus-order/visible-related rules, and
  aria/label/button-name/image-alt.
- ALSO sweep ONE seeded detail route: seed an item via
  `page.request.post("/api/workspaces/{wsId}/items", ...)` (discover wsId via
  `GET /api/users/me/workspaces` exactly as the palette spec does) and goto
  `/items/{id}`.
- COLLECT violations across ALL routes (do not bail on the first) and assert ZERO
  with `impact` of `serious` or `critical` (filter `results.violations` by impact).
  On failure, build a per-route report string (route + rule id + node count) in
  the assertion message so the failure is actionable.

CONSTRAINT: do not name helpers/files with `sync`/`idb`/`offline` substrings.
Commit: `test(17-02): axe a11y sweep across swept routes (POL-02)`.
  </action>
  <verify>
    <automated>grep -q "@axe-core/playwright" frontend2/e2e/a11y-sweep.spec.ts && grep -q "withTags" frontend2/e2e/a11y-sweep.spec.ts && echo ok</automated>
    <human-check>cd frontend2 && bun run test:e2e e2e/a11y-sweep.spec.ts (chromium, live :5173/:8080) — zero serious/critical violations</human-check>
  </verify>
  <done>Spec logs in once, sweeps the static-path subset + one seeded detail route, asserts zero serious/critical axe violations with a per-route report on failure.</done>
</task>

<task type="auto">
  <name>Task 3: keyboard-nav.spec.ts (POL-03) + best-effort e2e-frontend2.yml</name>
  <files>frontend2/e2e/keyboard-nav.spec.ts, .github/workflows/e2e-frontend2.yml</files>
  <action>
A) Write `frontend2/e2e/keyboard-nav.spec.ts` (chromium):
- `login(page)` helper (one login per test, palette-spec pattern).
- Test "focus-visible reachable": from the dashboard, press Tab a bounded number
  of times; assert the active element changes and that the focused element shows a
  visible focus ring — via `page.evaluate` read `getComputedStyle(document.
  activeElement)` and assert a non-`none`/non-zero `outlineStyle`/`outlineWidth`
  (the global fallback OR a per-component rule guarantees this). Keep assertions
  chromium-stable (avoid Firefox outline quirks — chromium is the gate).
- Test "ESC closes a modal, no trap": open the command palette with
  `ControlOrMeta+k`, assert `[data-testid="command-palette"]` visible, press
  `Escape`, assert it is hidden AND the route is unchanged (no logout / no nav).
  Then assert Tab does NOT trap: while NOT in a modal, Tab repeatedly and confirm
  focus eventually leaves any single widget (active element id/tag changes across
  presses; never stuck on one element for N consecutive Tabs).
- Reuse the data-testid + chord constants from the palette spec.

B) Write `.github/workflows/e2e-frontend2.yml` — a NEW best-effort workflow
(17-CONTEXT OQ-1; do NOT edit lint-frontend2.yml — 17-01 owns it):
- HEADER COMMENT at top: state plainly this workflow is BEST-EFFORT scaffolding
  that needs first-real-PR validation; the orchestrator cannot execute GitHub
  Actions so it is NOT claimed green (per the "verification is REAL" rule).
- Trigger: pull_request + push on master with `paths: frontend2/**` and
  `.github/workflows/e2e-frontend2.yml`.
- One job: ubuntu-latest with a `postgres` service container (warehouse_test DB,
  wh/wh credentials per the CLAUDE.md runbook), checkout, setup-go (Go version
  from go.mod / .mise.toml), setup-bun (1.3.13), apply migrations then start the
  Go backend (`cd backend && go run ./cmd/server/main.go &` — reference the
  migrate binary in backend/cmd/), seed the E2E user, then
  `cd frontend2 && bun install --frozen-lockfile && bun run build && bun run
  preview &` (set E2E_BASE_URL to the preview port) OR `bun run dev &`, install
  playwright chromium (`bunx playwright install --with-deps chromium`), then run
  the a11y + keyboard specs (responsive lands in 17-03; reference it but the file
  may not exist yet at this plan's HEAD — list it in a comment as a follow-on).
  Mark the spec step `continue-on-error: true` (best-effort) so a first-run
  failure does not block — the LOCAL spec run is the HARD gate.

CONSTRAINT: do not name files/dirs with `sync`/`idb`/`offline` substrings.
Commit: `test(17-02): keyboard-nav spec + best-effort e2e-frontend2.yml (POL-03/OQ-1)`.
  </action>
  <verify>
    <automated>grep -q "ControlOrMeta+k" frontend2/e2e/keyboard-nav.spec.ts && grep -q "Escape" frontend2/e2e/keyboard-nav.spec.ts && node -e "require('fs').readFileSync('.github/workflows/e2e-frontend2.yml','utf8').includes('best-effort')||process.exit(1)" && echo ok</automated>
    <human-check>cd frontend2 && bun run test:e2e e2e/keyboard-nav.spec.ts (chromium, live :5173/:8080) — focus-visible reachable, ESC closes palette, no trap</human-check>
  </verify>
  <done>keyboard-nav spec asserts focus-visible + ESC-closes-modal + no-trap; e2e-frontend2.yml is a new best-effort workflow flagged for first-PR validation; lint-frontend2.yml untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser E2E -> live backend | specs hit real :8080 with a real auth cookie; seed via page.request |
| new devDependency -> build/CI | @axe-core/playwright enters the supply chain |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-01 | Information disclosure | a11y spec seeds an item in the dev DB | accept | dev/test DB only; seed uses a unique throwaway name like the palette spec |
| T-17-02-02 | Tampering | global focus-visible rule overrides per-component rings | mitigate | rule is a `*:focus-visible` fallback only; per-component rules retained and take precedence by specificity |
| T-17-02-SC | Tampering | npm install of @axe-core/playwright | mitigate | see Package Legitimacy gate below — verified-legit official Deque package |
</threat_model>

<package_legitimacy>
`@axe-core/playwright` — official Deque Systems integration of axe-core for
Playwright (scope `@axe-core`, same org as `axe-core`). Status: **[LEGIT]** —
widely used, published by Deque. If the orchestrator's RESEARCH.md lacks a
Package Legitimacy table entry, the executor MUST verify at
`npmjs.com/package/@axe-core/playwright` (publisher = dequelabs, weekly downloads
in the hundreds of thousands) before `bun add`. Not a `[SLOP]`/`[SUS]` package;
no blocking human checkpoint required, but record the verification in the SUMMARY.
</package_legitimacy>

<verification>
- `cd frontend2 && bun install --frozen-lockfile` passes (lockfile in sync).
- `cd frontend2 && bun run lint:tsc` green.
- Live (17-VALIDATION, orchestrator-run on chromium):
  `bun run test:e2e e2e/a11y-sweep.spec.ts` — zero serious/critical violations.
  `bun run test:e2e e2e/keyboard-nav.spec.ts` — focus-visible + ESC + no-trap.
- globals.css contains a global `:focus-visible` fallback; per-component rules
  intact (grep the 12 component files still have their focus-visible tokens).
- lint-frontend2.yml is UNCHANGED by this plan (17-01 owns it).
</verification>

<success_criteria>
POL-02 + POL-03 satisfied: axe sweep clean across swept routes + one seeded detail
route; keyboard nav proves focus-visible reachability, ESC-closes-modal, no trap;
global focus fallback in place; best-effort CI workflow committed and flagged.
</success_criteria>

<output>
Create `.planning/phases/17-polish-quality/17-02-SUMMARY.md` when done.
Declare every file edited (incl. bun.lock). Do NOT edit STATE.md or ROADMAP.md.
</output>
