---
phase: 02-tokens-type-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend2/src/styles/globals.css
  - frontend2/src/styles/tokens.css
autonomous: true
requirements: [TOKEN-01, TOKEN-02, TOKEN-03, TOKEN-04]
must_haves:
  truths:
    - "Running `bun run build` compiles and emits the retro-os utility classes (bg-bg-panel, text-fg-ink, bg-titlebar-mint, gap-sp-2, rounded-chip, shadow-hard-ink)"
    - "Running the contrast test asserts every retro-os text pair meets WCAG AA (>=4.5:1), 16/16 green"
    - "Corners are square everywhere — Tailwind's default rounded-* utilities resolve to 0 — except badges/chips, which keep a 2px radius (rounded-chip)"
    - "The font-display (FOUT) behavior of @fontsource is an explicit, documented decision, not an accident"
  artifacts:
    - path: "frontend2/src/styles/tokens.css"
      provides: "retro-os :root token block + @theme inline utility mapping + radius defaults zeroed"
      contains: "@theme inline"
    - path: "frontend2/src/styles/globals.css"
      provides: "body globals + radius-0 strategy + named font-display decision comment"
      contains: "border-radius"
  key_links:
    - from: "frontend2/src/styles/globals.css"
      to: "frontend2/src/styles/tokens.css"
      via: "@import './tokens.css'"
      pattern: "@import\\s+[\"']\\./tokens\\.css[\"']"
    - from: "frontend2/src/styles/tokens.css"
      to: "Tailwind v4 compiler"
      via: "@theme inline maps :root vars to --color-*/--spacing-sp-*/--radius-*"
      pattern: "--color-bg-panel:\\s*var\\(--bg-panel\\)"
---

<objective>
Verify the already-committed retro-os token implementation (commit 19f038e) satisfies TOKEN-01, TOKEN-02, and TOKEN-04, then close the one real gap in TOKEN-03: there is no global radius-0 reset. Also name the @fontsource `font-display: swap` (FOUT) behavior as an explicit decision.

Purpose: Phase 2 is verification + gap-closure, NOT greenfield re-porting. tokens.css, globals.css, tokens.test.ts, and the three @fontsource packages already exist and pass (build green, 16/16 contrast). This plan asserts that against the requirements and adds the missing radius-0 reset so no atom renders a curved corner in Phase 4+.

Output: A confirmed-green build + contrast test, a radius-0 global strategy in globals.css/tokens.css with a 2px badge/chip exception (design hard rule 4), and a documented font-display decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tokens-type-system/02-CONTEXT.md
@.planning/phases/02-tokens-type-system/02-RESEARCH.md
@.planning/phases/02-tokens-type-system/02-VALIDATION.md

<interfaces>
<!-- Already-committed files the executor verifies/edits. Read them; do not re-port. -->

frontend2/src/styles/tokens.css (committed in 19f038e):
- `:root { ... }` — retro-os palette ported verbatim from retro-os.css: surfaces (--bg-desktop/panel/panel-2/pressed), ink (--fg-ink/muted/faint), pastel fills (--titlebar-blue/pink/mint/butter), deep companions (--accent-blue-deep/pink-deep/mint-deep, --warn-deep), status (--danger/danger-bg/warn-bg/ok-bg/info-bg), bevel (--border-ink, --bevel-light/shade, --shadow-hard, --shadow-hard-ink, --table-rule, --table-stripe), type (--font-display/body/mono), spacing (--sp-1..6), radius (--radius-chip: 2px).
- `@theme inline { ... }` — maps each token to a Tailwind theme var: --color-* (e.g. --color-bg-panel: var(--bg-panel)), --spacing-sp-1..6, --font-display/body/mono (DELIBERATE literals, not var() — see lines 106-115 comment), --shadow-hard, --shadow-hard-ink, --radius-none: 0, --radius-chip: 2px.

frontend2/src/styles/globals.css (committed in 19f038e):
- @import './tokens.css'; @import 'tailwindcss'; then 7 @fontsource imports (silkscreen 400/700, ibm-plex-sans 400/600/700, ibm-plex-mono 400/600).
- body: background-color var(--bg-desktop), radial-gradient dot-dither, color var(--fg-ink), font-family var(--font-body), 14px/1.5.
- @media (prefers-contrast: more) { :root { --fg-faint: var(--fg-muted); } }
- @utility bevel-raised / bevel-raised-ink / bevel-pressed / bevel-sunken / pinstripes.
- @layer components { .rtable ... } table chrome.
- NO global `border-radius: 0` reset exists (THE GAP).

frontend2/src/styles/tokens.test.ts (committed, 16/16 green) — pure WCAG AA formula over hex literals parsed from tokens.css. Do NOT edit in this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify TOKEN-01, TOKEN-02, TOKEN-04 against the committed implementation</name>
  <files>frontend2/src/styles/tokens.css (read-only verification), frontend2/src/styles/tokens.test.ts (read-only verification)</files>
  <read_first>
    - frontend2/src/styles/tokens.css (the :root block + @theme inline block being asserted)
    - frontend2/src/styles/tokens.test.ts (the contrast guard being run)
    - .planning/sketches/themes/retro-os.css (the verbatim-port source-of-truth for TOKEN-01 value equality)
    - .planning/phases/02-tokens-type-system/02-RESEARCH.md (TOKEN-01/02/04 rows + Pitfall 4 + Pitfall 5)
  </read_first>
  <action>
    This is a verification task over already-committed files — assert, do not re-port or edit token values. From `frontend2/`:
    (1) TOKEN-01 (verbatim port): confirm tokens.css `:root` token values match `.planning/sketches/themes/retro-os.css` line-for-line. The two intentional derived tokens `--table-rule: #e7ddca` and `--table-stripe: #fcf8f0` are promoted from the source's inline `.rtable` hexes (Pitfall 5) — confirm those hexes match the source and note them as intentional derived tokens, NOT a verbatim violation. Confirm the `--font-*` and `--shadow-*` literals are duplicated in BOTH `:root` and `@theme inline` (Pitfall 4) and the duplicated copies are identical strings.
    (2) TOKEN-02 (utilities emitted): run `bun run build` and grep the compiled CSS at `dist/assets/index-*.css` for the utility classes `.bg-bg-panel`, `.text-fg-ink`, `.bg-titlebar-mint`, `.gap-sp-2`, `.rounded-chip`, `.shadow-hard-ink`. All must be present. Confirm the mapping uses `@theme inline` (not plain `@theme`) — grep tokens.css for the literal `@theme inline`.
    (3) TOKEN-04 (WCAG AA): run `bun run test src/styles/tokens.test.ts` — must report 16 passing assertions (15 pairs + 1 black/white sanity), all >=4.5:1.
    Record each assertion's pass/fail in the SUMMARY. Make NO edits in this task.
  </action>
  <acceptance_criteria>
    - `cd frontend2 && bun run build` exits 0 and `dist/assets/index-*.css` contains all of: `.bg-bg-panel`, `.text-fg-ink`, `.bg-titlebar-mint`, `.gap-sp-2`, `.rounded-chip`, `.shadow-hard-ink`
    - `cd frontend2 && bun run test src/styles/tokens.test.ts` exits 0 with 16 passing assertions
    - `grep -c '@theme inline' frontend2/src/styles/tokens.css` returns 1
    - tokens.css `--table-rule` and `--table-stripe` values (`#e7ddca`, `#fcf8f0`) match the inline `.rtable` hexes in `.planning/sketches/themes/retro-os.css`; noted as intentional derived tokens in SUMMARY
    - SUMMARY records TOKEN-01 = verified (verbatim + 2 documented derived tokens), TOKEN-02 = verified (6 utilities emitted), TOKEN-04 = verified (16/16 AA)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend2 && bun run test src/styles/tokens.test.ts && bun run build && grep -ql '.bg-bg-panel' dist/assets/index-*.css && grep -ql '.rounded-chip' dist/assets/index-*.css && grep -ql '.shadow-hard-ink' dist/assets/index-*.css && echo VERIFY_OK</automated>
  </verify>
  <done>Build green; 6 named utilities present in compiled CSS; contrast test 16/16; verbatim-port + derived-token findings recorded in SUMMARY. No files edited.</done>
</task>

<task type="auto">
  <name>Task 2: Close the radius-0 gap (TOKEN-03) — global reset + 2px badge exception</name>
  <files>frontend2/src/styles/globals.css, frontend2/src/styles/tokens.css</files>
  <read_first>
    - frontend2/src/styles/globals.css (the file gaining the radius reset; currently has NO border-radius rule)
    - frontend2/src/styles/tokens.css (has --radius-none: 0 + --radius-chip: 2px in @theme inline already)
    - .planning/phases/02-tokens-type-system/02-RESEARCH.md (Pitfall 1 — radius-0 reset missing; Open Question 2 — recommends adding it now)
    - .claude/skills/sketch-findings-home-warehouse-system/SKILL.md (Hard Rule 4: "Radii: 0 everywhere except badges (2px)")
    - .planning/phases/02-tokens-type-system/02-CONTEXT.md (D-06 discretion: radius strategy is Claude's call — global reset + badge exception is an explicit option)
  </read_first>
  <action>
    Implement the radius-0 strategy that satisfies design hard rule 4 ("Radii: 0 everywhere except badges 2px"). The `@theme inline` radius tokens (`--radius-none: 0`, `--radius-chip: 2px`) already exist in tokens.css, but there is NO global reset, so Tailwind v4's own default `--radius-*` scale (`rounded-sm`..`rounded-3xl`) still emits curved corners.

    Choose strategy (a) — zero out Tailwind's default radius scale in tokens.css `@theme inline` so the `rounded-sm`/`rounded-md`/`rounded-lg`/`rounded-xl`/`rounded-2xl`/`rounded-3xl`/`rounded` utilities all collapse to 0, while keeping `--radius-chip: 2px` for `rounded-chip` (badges). In the `@theme inline` block add `--radius-sm: 0; --radius-md: 0; --radius-lg: 0; --radius-xl: 0; --radius-2xl: 0; --radius-3xl: 0;` (override Tailwind's defaults to 0). Keep the existing `--radius-none: 0` and `--radius-chip: 2px`.

    THEN, as belt-and-suspenders for raw `border-radius` set outside Tailwind utilities, add a single global reset to globals.css: a `*, ::before, ::after { border-radius: 0; }` rule, immediately followed by a 2px badge/chip exception. Since no atoms ship until Phase 4, scope the exception to the design-system badge contract: add `.badge { border-radius: var(--radius-chip); }` (matches the retro-os.css `.badge` 2px rule and Phase 4's badge atom). Add a one-line comment above the reset citing design hard rule 4.

    Do NOT touch the `--font-*`/`--shadow-*` duplicated literals (Pitfall 4 — out of scope here). Do NOT change any color/spacing token values.
  </action>
  <acceptance_criteria>
    - `frontend2/src/styles/globals.css` contains a global selector setting `border-radius: 0` (grep `border-radius:\s*0` matches a `*`/`::before`/`::after` reset rule)
    - `frontend2/src/styles/globals.css` contains a badge/chip exception setting `border-radius: var(--radius-chip)` (or `2px`) for `.badge`
    - `frontend2/src/styles/tokens.css` `@theme inline` zeroes Tailwind's default radius scale: `grep -E 'radius-(sm|md|lg|xl|2xl|3xl):\s*0' frontend2/src/styles/tokens.css` returns >= 6 matches
    - `--radius-chip: 2px` is still present in tokens.css `@theme inline`
    - `cd frontend2 && bun run build` exits 0; compiled `dist/assets/index-*.css` still contains `.rounded-chip`; and the `.rounded-sm`/`.rounded-lg` utilities, if emitted, resolve to `border-radius: 0` (or `var(--radius-sm)` which is now 0)
    - `cd frontend2 && bun run test src/styles/tokens.test.ts` still exits 0 with 16 passing assertions (radius change must not regress contrast)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend2 && grep -Eq 'border-radius:\s*0' src/styles/globals.css && grep -q 'var(--radius-chip)' src/styles/globals.css && [ "$(grep -Ec 'radius-(sm|md|lg|xl|2xl|3xl):\s*0' src/styles/tokens.css)" -ge 6 ] && bun run test src/styles/tokens.test.ts && bun run build && echo RADIUS_OK</automated>
  </verify>
  <done>globals.css has a global border-radius:0 reset + .badge 2px exception; tokens.css zeroes Tailwind's default radius scale while keeping rounded-chip at 2px; build green; contrast still 16/16.</done>
</task>

<task type="auto">
  <name>Task 3: Name the font-display (FOUT) decision explicitly (TOKEN-03 polish)</name>
  <files>frontend2/src/styles/globals.css</files>
  <read_first>
    - frontend2/src/styles/globals.css (the 7 @fontsource @import lines + body font-family)
    - .planning/phases/02-tokens-type-system/02-RESEARCH.md (Pitfall 2 — @fontsource ships font-display: swap = FOUT; Assumptions Log A1)
    - .planning/phases/02-tokens-type-system/02-CONTEXT.md (priority "avoid flash-of-fallback" nuance)
    - .claude/skills/sketch-findings-home-warehouse-system/SKILL.md (Hard Rule 1: Silkscreen is display-only >=16px uppercase — the face most affected by swap)
  </read_first>
  <action>
    CONTEXT asks to "avoid flash-of-fallback," but @fontsource ships `font-display: swap` (FOUT — fallback paints then swaps). This is a polish decision that must be NAMED, not silently left.

    Decision to implement: ACCEPT `swap` for IBM Plex Sans/Mono body+data (metrics are close to system-ui/ui-monospace fallbacks, FOUT is acceptable and avoids invisible text), and OVERRIDE only Silkscreen — the pixel display face whose swap is visually jarring against any system fallback — to `font-display: optional` so display headings either show Silkscreen immediately (if cached/fast) or stay in fallback without a late jarring swap. Implement by adding two `@font-face` re-declarations in globals.css (one per Silkscreen weight 400 + 700) that re-point `src` at the same `@fontsource/silkscreen` woff2 assets but set `font-display: optional`; place them AFTER the `@import "@fontsource/silkscreen/..."` lines so they override. Use the Silkscreen `family: "Silkscreen"` matching `--font-display`. Add a comment block above documenting the decision (accept swap for Plex, optional for Silkscreen, and why).

    If re-declaring `@font-face` against the fontsource woff2 path proves brittle in this Vite setup (asset URLs are hashed at build), FALL BACK to the lighter form: keep `swap` for all faces and add a documented comment in globals.css stating the FOUT decision was reviewed and `swap` accepted as the named outcome (CONTEXT "avoid flash" applies to body, where Plex's close metrics make swap acceptable). Either way the OUTCOME is a documented, intentional font-display decision — not silence. Record which path was taken in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `frontend2/src/styles/globals.css` contains a comment block naming the font-display / FOUT decision (mentions both `swap` and the Silkscreen treatment)
    - Either: globals.css contains `font-display: optional` in a Silkscreen `@font-face` re-declaration; OR globals.css contains a comment explicitly accepting `swap` as the reviewed decision — `grep -E 'font-display' frontend2/src/styles/globals.css` matches at least once OR a `FOUT`/`font-display` decision comment is present
    - `cd frontend2 && bun run build` exits 0 (fonts still resolve; no broken @font-face)
    - SUMMARY records which path was taken (override vs accept) and the rationale
  </acceptance_criteria>
  <verify>
    <automated>cd frontend2 && grep -Eqi 'font-display|FOUT' src/styles/globals.css && bun run build && echo FONTDISPLAY_OK</automated>
  </verify>
  <done>globals.css documents the font-display decision (Silkscreen override to optional, or named acceptance of swap); build green; rationale in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Phase 2 has no untrusted-input boundary. Static design tokens + build-time CSS + a unit test over hardcoded hex literals. No user input, no auth, no data flow, no network at runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | @fontsource font packages (supply chain) | accept | No new packages installed this plan (all three pre-existing in 19f038e, pinned in package.json + lockfile, woff2 subsets inspected on disk in 02-RESEARCH.md). No `bun add` runs — package-legitimacy gate N/A. |
| T-02-NA | (all other STRIDE) | tokens.css / globals.css / tokens.test.ts | accept | No runtime threat surface: static CSS custom properties compiled at build time, a pure-formula Vitest guard over hex literals. No input validation, session, access control, or crypto applies (02-RESEARCH.md Security Domain: ASVS surface near-empty; V14 marginal — self-hosting fonts via @fontsource already removes the third-party CDN origin). |
</threat_model>

<verification>
- `cd frontend2 && bun run test src/styles/tokens.test.ts` → 16/16 green (TOKEN-04)
- `cd frontend2 && bun run build` → exits 0, compiled CSS emits bg-bg-panel/text-fg-ink/bg-titlebar-mint/gap-sp-2/rounded-chip/shadow-hard-ink (TOKEN-02)
- globals.css has a `*` border-radius:0 reset + `.badge` 2px exception; tokens.css zeroes Tailwind default radius scale (TOKEN-03 gap closed)
- globals.css documents the font-display/FOUT decision (TOKEN-03 polish)
- TOKEN-01 verbatim-port + 2 derived tokens recorded in SUMMARY
</verification>

<success_criteria>
- TOKEN-01, TOKEN-02, TOKEN-04 asserted green against committed code (no re-port)
- TOKEN-03 radius-0 gap closed: square corners everywhere, 2px badges only, build still green
- font-display decision named in globals.css
- Full contrast test still 16/16 after edits
</success_criteria>

<output>
Create `.planning/phases/02-tokens-type-system/02-01-SUMMARY.md` when done
</output>
