---
phase: 02-tokens-type-system
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - frontend2/src/styles/glyph-coverage.test.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [TOKEN-05, TOKEN-01, TOKEN-02, TOKEN-03, TOKEN-04]
must_haves:
  truths:
    - "A repo-resident automated check asserts IBM Plex Mono ships the cyrillic AND latin-ext woff2 subsets that Estonian + Russian glyphs route through (TOKEN-05 coverage half)"
    - "The check confirms IBM Plex Mono is a true monospace face used for data columns with tabular-nums (the 'no column drift' baseline guarantee)"
    - "REQUIREMENTS.md TOKEN-01..05 prose describes the retro-os direction (retro-os.css, Silkscreen + IBM Plex Sans + IBM Plex Mono, cream dot-dither, radius-0+2px badges, WCAG AA), NOT the scrapped premium-terminal direction"
  artifacts:
    - path: "frontend2/src/styles/glyph-coverage.test.ts"
      provides: "Vitest assertion over @fontsource/ibm-plex-mono subset files (cyrillic + latin-ext present)"
      contains: "cyrillic"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected TOKEN-01..05 prose (retro-os)"
      contains: "IBM Plex Mono"
  key_links:
    - from: "frontend2/src/styles/glyph-coverage.test.ts"
      to: "node_modules/@fontsource/ibm-plex-mono/files"
      via: "fs read of the subset woff2 filenames"
      pattern: "ibm-plex-mono.*cyrillic"
---

<objective>
Close the two remaining gaps in Phase 2: (1) TOKEN-05 has confirmed glyph COVERAGE (Plex Mono ships cyrillic + latin-ext subsets) but no repo-resident check exists; add a lightweight Vitest assertion. (2) REQUIREMENTS.md TOKEN-01..05 prose (lines 27-31) still describes the SCRAPPED premium-terminal aesthetic; rewrite it to the retro-os direction so requirement sign-off measures against the correct spec.

Purpose: Without the coverage check, TOKEN-05 has no automatable guard. Without the prose correction, any verifier reading REQUIREMENTS.md would validate the (correct) retro-os implementation against the wrong (premium-terminal) acceptance criteria and mark it non-compliant (02-RESEARCH.md Pitfall 3).

Output: A green glyph-coverage test + corrected TOKEN prose. Perceived column drift remains a documented manual verification (02-VALIDATION.md) — a brittle pixel test is explicitly NOT in scope.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tokens-type-system/02-CONTEXT.md
@.planning/phases/02-tokens-type-system/02-RESEARCH.md
@.planning/phases/02-tokens-type-system/02-VALIDATION.md

<interfaces>
<!-- Files the executor reads/creates. Read them before touching anything. -->

frontend2/src/styles/tokens.test.ts (committed pattern to MIRROR for the new test):
- Pure-Node Vitest: `readFileSync` + `join(process.cwd(), ...)`, `describe`/`it`/`expect` from "vitest", `it.each` for table-driven cases. jsdom env note: import.meta.url is http-scheme, so resolve from `process.cwd()` (the frontend2 package root, = vitest cwd).

node_modules layout (verified 2026-06-12, 02-RESEARCH.md):
- `frontend2/node_modules/@fontsource/ibm-plex-mono/files/` contains woff2 subset files named like `ibm-plex-mono-cyrillic-400-normal.woff2`, `ibm-plex-mono-latin-ext-400-normal.woff2`, `ibm-plex-mono-latin-400-normal.woff2`.
- @fontsource/silkscreen ships latin + latin-ext ONLY (no cyrillic — correct, display-only).

REQUIREMENTS.md TOKEN block (STALE — to rewrite):
- Line 27 TOKEN-01: names `default.css` + premium-terminal vars (--bg-base, --amber, --fg-glow, --border-glow).
- Line 28 TOKEN-02: `bg-fg-mid`, `text-fg-bright`.
- Line 29 TOKEN-03: scanline + radial-vignette, `--font-sans: var(--font-mono)` monospace anchor, `@fontsource-variable/jetbrains-mono`.
- Line 30 TOKEN-04: WCAG AAA against --bg-panel.
- Line 31 TOKEN-05: JetBrains Mono glyph metrics, fallback to IBM Plex Mono.
- Lines 215-219: traceability table, TOKEN-01..05 status "Pending".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a repo-resident glyph-coverage check for IBM Plex Mono (TOKEN-05)</name>
  <files>frontend2/src/styles/glyph-coverage.test.ts</files>
  <behavior>
    - Test 1: @fontsource/ibm-plex-mono ships a `cyrillic` woff2 subset file in its `files/` directory (the subset Russian glyphs route through).
    - Test 2: @fontsource/ibm-plex-mono ships a `latin-ext` woff2 subset file (the subset Estonian õäöüšž route through).
    - Test 3: globals.css imports the ibm-plex-mono weight CSS (`@fontsource/ibm-plex-mono/400.css`) that pulls those subsets — confirms the data face is actually wired, not just installed.
    - Test 4 (baseline guarantee, not pixel measurement): globals.css `.rtable .mono` (or tokens.css `--font-mono`) binds the mono data face with `font-variant-numeric: tabular-nums` — the "no column drift" guarantee for numeric columns. Assert the tabular-nums declaration is present in globals.css.
  </behavior>
  <read_first>
    - frontend2/src/styles/tokens.test.ts (the committed Vitest pattern to mirror: readFileSync + process.cwd() + it.each)
    - frontend2/src/styles/globals.css (the @fontsource @import lines + `.rtable .mono` tabular-nums rule — verify exact strings before asserting them)
    - .planning/phases/02-tokens-type-system/02-RESEARCH.md (Open Question 1 — coverage is automatable offline, perceived drift is manual; recommends a lightweight check NOT a brittle pixel test)
    - .planning/phases/02-tokens-type-system/02-VALIDATION.md (Manual-Only Verifications row — perceived column drift stays manual)
    - .planning/phases/02-tokens-type-system/02-CONTEXT.md (specifics: "Plex Sans/Mono cover Estonian + Cyrillic; glyph coverage note goes in tokens.css comment")
  </read_first>
  <action>
    Create `frontend2/src/styles/glyph-coverage.test.ts` mirroring the pure-Node pattern in `tokens.test.ts` (vitest `describe`/`it`/`expect`, `readFileSync`, paths resolved from `process.cwd()` = frontend2 package root). This is the AUTOMATABLE half of TOKEN-05 (subset presence); perceived column drift stays a manual VALIDATION row — do NOT write a pixel/screenshot test.

    Implement the four behaviors above:
    (1+2) `readdirSync(join(process.cwd(), "node_modules/@fontsource/ibm-plex-mono/files"))` and assert at least one filename matches `/ibm-plex-mono-cyrillic-\d+-normal\.woff2$/` and at least one matches `/ibm-plex-mono-latin-ext-\d+-normal\.woff2$/`. Add an assertion that @fontsource/silkscreen does NOT ship a cyrillic subset (documents that Silkscreen is correctly latin-only display-only — read silkscreen `files/` and assert no `silkscreen-cyrillic` match; if silkscreen has no `files/` dir, assert that absence instead).
    (3) `readFileSync` globals.css and assert it contains `@fontsource/ibm-plex-mono/400.css`.
    (4) Assert globals.css contains `font-variant-numeric: tabular-nums` (the no-drift numeric guarantee on `.rtable .mono`).

    TDD: write the test first; it should pass immediately against the already-installed packages and committed globals.css (the subsets and the import already exist — this test ASSERTS existing state, the RED would only appear if a future change removed coverage). Confirm green via `bun run test src/styles/glyph-coverage.test.ts`.
  </action>
  <acceptance_criteria>
    - `frontend2/src/styles/glyph-coverage.test.ts` exists and is a pure-Node Vitest file (no DOM, no Playwright, no screenshot)
    - `cd frontend2 && bun run test src/styles/glyph-coverage.test.ts` exits 0 with all assertions passing
    - The test asserts an ibm-plex-mono `cyrillic` woff2 subset is present (grep test file for `cyrillic`)
    - The test asserts an ibm-plex-mono `latin-ext` woff2 subset is present (grep test file for `latin-ext`)
    - The test asserts globals.css imports `@fontsource/ibm-plex-mono/400.css` and contains `tabular-nums`
    - `cd frontend2 && bun run test` (full suite) still green — the new file does not break the existing 16/16 contrast suite
  </acceptance_criteria>
  <verify>
    <automated>cd frontend2 && grep -q 'cyrillic' src/styles/glyph-coverage.test.ts && grep -q 'latin-ext' src/styles/glyph-coverage.test.ts && bun run test src/styles/glyph-coverage.test.ts && echo GLYPH_OK</automated>
  </verify>
  <done>glyph-coverage.test.ts asserts Plex Mono cyrillic + latin-ext subset presence + wired import + tabular-nums; green; full suite still green; perceived drift left as the documented manual VALIDATION row.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite REQUIREMENTS.md TOKEN-01..05 prose to the retro-os direction</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 27-31 stale TOKEN prose + lines 215-219 traceability table — read exact current text before editing)
    - .planning/phases/02-tokens-type-system/02-RESEARCH.md (Phase Requirements table rows TOKEN-01..05 give the authoritative retro-os definitions; Pitfall 3; State of the Art table)
    - .planning/phases/02-tokens-type-system/02-CONTEXT.md (D-01..D-07 — the authoritative decisions the prose must reflect)
    - .planning/sketches/themes/retro-os.css (the token-source the corrected TOKEN-01 must name)
    - frontend2/src/styles/tokens.css + globals.css (the AS-BUILT implementation the prose now describes — including the radius-0 strategy Plan 02-01 implemented)
  </read_first>
  <action>
    Rewrite REQUIREMENTS.md TOKEN-01..05 (lines 27-31) so each requirement describes the retro-os direction (authoritative per CONTEXT.md + ROADMAP + 02-RESEARCH.md Phase Requirements table). Replace the premium-terminal prose:
    - TOKEN-01: `styles/tokens.css` ports the retro-os pastel palette from `.planning/sketches/themes/retro-os.css` (mirrored at `.claude/skills/sketch-findings-home-warehouse-system/sources/themes/retro-os.css`) verbatim — surfaces (--bg-desktop/panel/panel-2/pressed), ink (--fg-ink/muted/faint), pastel fills (--titlebar-blue/pink/mint/butter), deep companions (--accent-blue-deep/pink-deep/mint-deep, --warn-deep), status (--danger/danger-bg/warn-bg/ok-bg/info-bg), bevel system (--border-ink, --bevel-light/shade, --shadow-hard, --shadow-hard-ink, --table-rule, --table-stripe), type (--font-display/body/mono), spacing (--sp-1..6). Remove ALL of: --bg-base, --amber, --amber-bright, --fg-glow, --fg-dim/mid/bright, --border-glow, default.css.
    - TOKEN-02: Tailwind v4 `@theme inline` block exposes tokens as utilities whose names mirror the var names (D-04) — `bg-bg-panel`, `text-fg-ink`, `bg-titlebar-mint`, `p-sp-3`, `gap-sp-2`, `rounded-chip`, `shadow-hard-ink`. (`inline` required for var-indirected tokens.) Remove `bg-fg-mid`/`text-fg-bright`.
    - TOKEN-03: Body globals apply cream desktop (--bg-desktop) + barely-there dot-dither background, `font-family: var(--font-body)` (IBM Plex Sans humanist anchor, NOT a monospace anchor), radius 0 everywhere except badges (2px, rounded-chip), and self-host Silkscreen + IBM Plex Sans + IBM Plex Mono via `@fontsource/*` (per-weight, not variable). Remove scanline + radial-vignette, `--font-sans: var(--font-mono)`, JetBrains Mono, `@fontsource-variable/jetbrains-mono`.
    - TOKEN-04: WCAG **AA** (>=4.5:1) verified in `src/styles/tokens.test.ts` over the revised pair list (ink/panel, ink/desktop, muted/panel, muted/desktop, ink/each-of-4-titlebars, each *-deep/panel, two *-deep/desktop, danger/danger-bg); `prefers-contrast: more` overrides only --fg-faint → --fg-muted. Remove AAA and the --fg-mid/base/bright pairing.
    - TOKEN-05: Cyrillic + Estonian glyph coverage verified in **IBM Plex Mono** (ships cyrillic + latin-ext subsets; tabular-nums data face, no column drift); coverage asserted by `src/styles/glyph-coverage.test.ts`, perceived drift confirmed manually. Remove JetBrains Mono + the "fallback to IBM Plex Mono if drift observed" clause (Plex Mono is now the primary, not a fallback).

    Use the auditable-history annotation pattern already used elsewhere in this repo (e.g. STATE.md / 65-CONTEXT.md): preserve the original premium-terminal text inside an `ORIGINAL (2026-04-30, premium-terminal — SCRAPPED):` note and the new text as `REVISED (2026-06-11, retro-os):` so the spec swap is traceable, not silently overwritten. Do NOT flip the lines 215-219 traceability table statuses to satisfied here — that is a verification-pass action, not a planning-doc edit; leave them "Pending".
  </action>
  <acceptance_criteria>
    - `grep -c 'JetBrains Mono' .planning/REQUIREMENTS.md` returns 0 within the TOKEN section, OR JetBrains Mono appears only inside an `ORIGINAL`/`SCRAPPED` annotation (not as the active requirement) — the active TOKEN-05 line names IBM Plex Mono
    - TOKEN-03 active prose contains `IBM Plex Mono` and `@fontsource/` and does NOT actively require `scanline`, `radial-vignette`, or `--font-sans: var(--font-mono)` (those appear only in ORIGINAL annotations if at all)
    - TOKEN-01 active prose names `retro-os.css` and does NOT actively require `default.css` or `--amber`
    - TOKEN-04 active prose says WCAG **AA** / `>=4.5:1`, not AAA / 7:1
    - TOKEN-02 active prose names `bg-bg-panel` / `text-fg-ink` (not `bg-fg-mid`)
    - Each rewritten requirement uses the ORIGINAL/REVISED annotation pattern so the premium-terminal history is preserved
    - The lines 215-219 traceability statuses remain "Pending" (not flipped in this plan)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/antti/Repos/Misc/home-warehouse-system && grep -A2 'TOKEN-03' .planning/REQUIREMENTS.md | grep -q 'IBM Plex Mono' && grep -A2 'TOKEN-01' .planning/REQUIREMENTS.md | grep -q 'retro-os' && grep -A2 'TOKEN-04' .planning/REQUIREMENTS.md | grep -qE 'AA|4\.5' && echo PROSE_OK</automated>
  </verify>
  <done>REQUIREMENTS.md TOKEN-01..05 prose describes retro-os (retro-os.css, Silkscreen + IBM Plex Sans/Mono, dot-dither, radius-0+2px badges, AA) with premium-terminal text preserved as ORIGINAL annotations; traceability table left Pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Phase 2 has no untrusted-input boundary. This plan adds a pure-Node Vitest file (reads font-package filenames + CSS strings off disk) and a planning-doc prose edit. No user input, no auth, no data flow, no network at runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | @fontsource/ibm-plex-mono (subset files asserted by the new test) | accept | No new packages installed; the test reads already-present, pinned, lockfile-tracked woff2 subset filenames. No `bun add` — package-legitimacy gate N/A. The test itself is a positive supply-chain guard (it fails if the cyrillic/latin-ext subsets disappear from a future dependency bump). |
| T-02-NA | (all other STRIDE) | glyph-coverage.test.ts / REQUIREMENTS.md | accept | No runtime threat surface: a build/CI unit test over hardcoded filename regexes + a documentation edit. No input validation, session, access control, or crypto applies (02-RESEARCH.md Security Domain). |
</threat_model>

<verification>
- `cd frontend2 && bun run test src/styles/glyph-coverage.test.ts` → green (TOKEN-05 coverage half automated)
- `cd frontend2 && bun run test` → full suite green (existing 16/16 contrast + new glyph coverage)
- REQUIREMENTS.md TOKEN-01..05 active prose = retro-os; no active JetBrains Mono / AAA / scanline / default.css; premium-terminal preserved as ORIGINAL annotations (TOKEN-01..05 prose corrected — Pitfall 3 closed)
- Perceived column drift remains the documented manual VALIDATION row (not a pixel test)
</verification>

<success_criteria>
- TOKEN-05 coverage half has a repo-resident automated guard; drift half documented as manual
- REQUIREMENTS.md TOKEN-01..05 prose measures the correct (retro-os) spec; verification can now sign off against it
- Full Vitest suite green; no regression to the contrast guard
</success_criteria>

<output>
Create `.planning/phases/02-tokens-type-system/02-02-SUMMARY.md` when done
</output>
