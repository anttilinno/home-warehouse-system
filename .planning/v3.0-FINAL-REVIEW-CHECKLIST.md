# v3.0 Final Review Checklist (for Antti)

Accumulated manual-verification residues from the autonomous parity run
(2026-06-12 →). Each item passed all automated gates; these are the
human-eye checks the orchestrator could not perform. Review when checking
the final result.

## Phase 2 — Tokens + Type System

- [ ] **FOUT visual check** — hard-reload dev server, watch Silkscreen headings on first paint. Decision on record: `font-display: swap` accepted for all faces (Silkscreen `optional` override evaluated, declined as brittle under Vite hashed assets). If the flash annoys, revisit via `@font-face` re-declare.
- [ ] **Cyrillic/Estonian mono column drift** — render a `.rtable .mono` column with ru + et glyphs; confirm alignment vs ASCII numerics. Coverage + tabular-nums are test-guarded (glyph-coverage.test.ts 5/5); perceived drift is eyeball-only.

## Phase 3 — Layout Primitives + Bottombar

- [ ] **Sidebar rail collapse feel** — 160ms transition, glyph centering at 60px rail.
- [ ] **FAB keycap stack on real mobile** — touch, stagger animation, safe-area inset.
- [ ] **768px breakpoint boundary** — drawer/FAB vs Bottombar swap in a real browser (viewport E2E lands in Phase 17).
- [ ] **Retro OS visual treatment vs sketches 006-008** — chrome eyeball pass (tokens code-verified; rendering not).

## Phase 4 — Retro Atoms

- [ ] **/demo visual pass vs sketches 006-008** — open `/demo` (DEV builds only), eyeball all 5 atom families.
- [ ] **Toast feel** — fire several toasts on /demo; stacking, hover-pause, danger-never-dismisses.
- [ ] **Combobox keyboard feel** — arrow/type/select flow beyond RTL coverage.
- [ ] **Popover viewport-flip** — popovers near screen edges.

## Phase 5 — Auth

- [ ] **Google OAuth live round-trip** — needs provider creds configured (GOOGLE_CLIENT_ID/SECRET).
- [ ] **GitHub OAuth incl. private-email account** — needs provider creds.
- [ ] **Authelia SSO round-trip** — build with `VITE_AUTHELIA_ENABLED=true` against homelab ingress.
- [ ] **Workspace switcher with a real multi-workspace account** — switch → lists refetch under new wsId.
- [ ] **Login page visual vs sketch 007** — OAuth group + register link hierarchy.

## Phase 6 — Providers

- [ ] **SSE reconnect feel** — kill backend ~5s, watch ONLINE dot drop then recover (capped backoff).
- [ ] **LAST SYNC ticking** — create an entity in a second tab, watch the PageHeader meta update.

## Phase 7 — Items + Photos

- [ ] **EXIF-rotated phone JPEG upload** — sideways portrait renders upright (createImageBitmap bakes orientation; jsdom couldn't test canvas).
- [ ] **Lightbox zoom + zip download** — +/-/0 keys; DOWNLOAD ALL produces a .zip with real photos.
- [ ] **List density vs sketch 008** — eyeball 30+ rows.
- [ ] **Known stub:** list category/location/qty cells show "—" until Phase 7b feeds the inventory wire (Item type lacks those fields).
- [ ] **POL-04 bundle:** Vite chunk-size warning present — address in Phase 17.

## E2E infra note (2026-06-13)

- **Full `bun run test:e2e` trips the auth rate limiter (20 req/min/IP, hardcoded `router.go:342`).** 10 login-bearing specs × 2 browsers + register/seeding POSTs exceed 20/min from one IP → back-half specs 429 and appear to "fail login". NOT a product bug. To run the full suite green: batch specs (e.g. by file) with a >60s cooldown, or restart the backend (clears the in-memory limiter) between batches. Each spec passes in isolation. A dev-only env override for the auth limit would make CI E2E runnable in one shot — candidate for Phase 17.

## Phase 7b — Inventory

- [ ] **Inline edit optimistic + revert** — qty/status/condition cells update instantly, revert + toast on error.
- [ ] **Move dialog** — location-only (no qty field), MOVE disabled when target unchanged.
- [ ] **Expiring view** — butter "in {n}d" vs danger "⚠ −{n}d" with real data.
- [ ] **Item-detail InventoryPanel** — real panel (IN STOCK total + entry rows), not the stub copy.
- [ ] **Deferred:** global + per-location movements views; >100-item workspaces show "—" for item names beyond first page (batch by-id resolution is the future fix).

## Phase 8 — Loans (human visual/interaction residues)
- [ ] **Overdue row triple-cue** — bg-danger-bg tint + "Overdue" pill word + ⚠ −{n}d chip, all three present on a real overdue loan row (LoansListPage + item/borrower panels).
- [ ] **`?itemId=` picker pre-filter** — `/loans/new?itemId=X` narrows the inventory picker to that item's entries + shows the FROM ITEM badge.
- [ ] **Loan dialog keyboard/focus** — Return/Extend/Edit dialogs: ESC closes via modal stack, focus returns to opener, Tab stays trapped.
