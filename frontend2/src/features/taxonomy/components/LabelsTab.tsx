// Phase 10 Plan 02 — STUB. The TaxonomyPage shell imports this by its FINAL
// export name so the page never needs re-editing; 10-04 (Labels) fills the body
// IN-PLACE (a legal cross-WAVE handoff — W4 is a later wave).
export function LabelsTab() {
  return <div data-testid="tab-labels-pending" />;
}
